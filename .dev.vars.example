# 本地用 `wrangler dev` 測試 AI 時,把這個檔複製成 .dev.vars 並填入你的金鑰。
# .dev.vars 已被 .gitignore 忽略,不會進版控。
# 程式優先用 Gemini(免費層),沒有才用 Claude。二選一即可。
# 正式環境改用:npx wrangler secret put GEMINI_API_KEY
GEMINI_API_KEY=AIza-xxxxxxxxxxxxxxxxx
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxx
