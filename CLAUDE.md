# 《房東監視中》Claude Code 協作規則

本檔適用於整個 `rent_house` 專案。

## 開始工作前只需讀 `STATUS.md`

1. **開始工作前只需讀根目錄 `STATUS.md`**（≤120 行，含現在狀態、下一步、待使用者決策、
   已知阻塞與完整文件地圖）。其餘文件一律照 `STATUS.md` 的「文件地圖」**按需查閱**，
   不要預先通讀。
2. **不要預先通讀 `工作日誌.md` 或 `docs/工作日誌-封存.md`**——它們是**考古用檔案**
   （合計數千行）。要查特定日期或工作項編號請用 grep，例如：
   ```
   rg -n "^### 2026-08-02" 工作日誌.md
   rg -n "CAFE-18" 工作日誌.md docs/工作日誌-封存.md
   ```
3. 待辦與技術債的**唯一權威清單**是 `docs/待辦.md`，每條 `- [ ]` 都帶可 grep 的 🔍 驗收錨點；
   已完成的系統、地雷紀錄與檔案地圖在 `docs/系統總覽.md`。

## 每次提交都必須更新 `STATUS.md` 與工作日誌

1. 只要本次工作會修改任何受 Git 追蹤的檔案，提交前必須同步更新兩個檔案：
   - 根目錄 **`STATUS.md`** 的「**現在狀態**」與「**下一步**」區塊（HEAD、未 push 的 commit、
     測試／build 結果、`SAVE_VERSION`、下一個工作項）；
   - 根目錄 **`工作日誌.md`** 檔尾追加點的一筆逐日紀錄。
2. 程式、測試、文件、`STATUS.md` 與工作日誌必須放在**同一個 commit**。不要等 commit 或
   push 完才補寫，否則會留下另一筆未提交變更。
3. 若發現上一個 commit 漏寫，在尚未 push 且安全時補寫後 amend；若已 push，下一個修正
   commit 要明確註明補記哪一筆提交，不可悄悄忽略。
4. 純閱讀、分析、回答問題、執行未造成追蹤檔案變更的檢查，不需要新增日誌。
5. 若本次動到待辦狀態，同步更新 `docs/待辦.md` 的 `- [ ]` ／ `- [x]` 與 🔍 驗收錨點。

### 逐日日誌條目的硬性格式：**每筆 ≤15 行**

每筆條目標題為 `### YYYY-MM-DD — 主題（執行 Agent）`，全條**不得超過 15 行**，只寫四件事：

- **目的／使用者需求**（1～2 行）
- **實際變更與主要檔案**（檔名 + 一句話，不逐行說明）
- **驗證結果**（測試數字、build、balance 快照是否漂移）
- **已知限制／後續**（含平衡快照是否 `--update`）

**推理過程、設計論證、逐行程式碼說明一律不入日誌**——需要留存的設計決策寫進 `docs/` 下
對應的設計文件；需要追蹤的殘留工作開一條 `docs/待辦.md` 的 `- [ ]` 並附驗收錨點。

## 提交前檢查

- 先用 `git status --porcelain` 與 `git diff -- STATUS.md 工作日誌.md docs/待辦.md`
  確認本次確實有 `STATUS.md` 與日誌的變更（動到待辦時 `docs/待辦.md` 也要有）。
- 相關功能至少跑對應測試；跨系統或平衡改動跑 `npm test` 與 `npm run build`。
- **動到 `src/` 或 `worker/` 的批次,提交前必須跑存檔啟動煙霧測試 `npm run smoke:save`**
  (`vite build` → 把既有存檔種進 localStorage → 無頭瀏覽器真的啟動 app → 快轉 48 小時)。
  `npm test`／UI Lab／`balance-test` **全部是全新 session、全新 state**,抓不到
  「只在載入舊存檔才炸」與「打包後才現形」的問題(2026-08-18 線上白畫面事故)。
- **部署一次只推一批,等線上驗證通過再推下一批。** 2026-08-18 六批一起推,
  壞掉時無法定位是哪一批,只能整包回退。
- 刻意改變平衡時，確認差異合理後執行 `npx tsx scripts/balance-test.ts --update`，並在日誌說明原因。
- 保留未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單／夾值／消毒等既有硬規則。

## 共用 Mobile UI Lab

UI 修改後，從 `C:\Users\User\claude_try` 執行 `npm run ui:shot -- rent`。此命令會在共用容器內安裝依賴、建置、啟動 Vite，並以三種手機尺寸截取樓層、動態與收支頁面。完成前必須檢查 `artifacts/ui-lab/rent/` 內所有 PNG 與 `report.json`。互動式檢查網址為 `http://127.0.0.1:5173/`。
