# 一樓寵物咖啡廳 — 工作分解與認養表

> 設計依據：`docs/一樓寵物咖啡廳-設計.md`
> 建立：2026-08-01 ・ 基準 commit `d1af916`
> **本文件只描述工作切分。認養一項工作＝從 `coordination/templates/task.md` 開一張 lease。**

> 🧭 **現況（2026-08-02）：CAFE-01～18（含 CAFE-18B）、CAFE-19、CAFE-22 ✅ 完成；CAFE-20～21 是可延後擴充。**
> 公開上線的最低範圍是 CAFE-01～15 加上提前納入的 CAFE-22 ⇒ **最低功能閘門已達成**。
> 🔴 **接手前請讀 `工作日誌.md` 的「2026-08-02 — 咖啡廳工作交接給 Codex」那一則**：
> 那是自足的交接文件，含已完成部分的可用介面（真實簽名）、CAFE-14／15 的原始施工說明、
> 三條會讓 balance 全紅的鐵則，以及數值基準。`coordination/tasks/` 的 lease **不進版控**，讀不到。

---

## 零、認養規則

### 怎麼認養

1. 從本文件挑一個**依賴已滿足**的項目（見 §1 依賴圖）
2. 從 `coordination/templates/task.md` 複製一張 lease 到 `coordination/tasks/`，檔名用項目 ID，例如 `cafe-03-map-grid.md`
3. lease 的「允許修改／禁止修改」**直接抄本文件該項目的清單**，不要自己重寫
4. 完成後刪除 lease

### 兩個 Agent 的工作方式不同，這會影響認養

| | Claude Code | Codex |
|---|---|---|
| 慣用作法 | 開 feature branch，完成後由中控合併 | **直接在 `main` 上提交並推送** |
| 共用 checkout | 是（`C:\Users\User\claude_try\rent_house`） | 是（同一個） |

🔴 **因為共用同一個工作樹，同時只能有一方持有未提交的變更。**
所以認養時的鐵則是：

- **開工前先 `git status --short`**。若有不屬於你的 dirty 檔案，**不要動手**，先確認對方是否在進行中
- **一次只認養一項**，做完提交（或合併）再認養下一項
- 兩個 Agent 可以同時認養**檔案不重疊**的項目，但要錯開提交時機

### 依賴滿足的判定

每個項目有「驗收錨點」——一行可 grep 的指令。**要判斷前置項目是否完成，跑它的錨點即可**，不必問人。
這沿用 `工作日誌.md` 第四、五節既有的驗收錨點制，包含它的三條規則：

1. 完成時若實際符號與錨點不符，**改狀態的同時要把錨點改寫成真實符號**
2. 「函式體內」這類限定不能用單行 `rg`，要用兩段式
3. 已完成項目的內文若含「尚未／留待」，必須另開一條

---

## 一、依賴圖

```
CAFE-01 FloorViewport 抽出
   └─ CAFE-02 獨立樓層頁面切換
         └─ CAFE-03 地圖擴充(GRID_H + Region)          ← 全案的關鍵路徑
               ├─ CAFE-04 樓梯與樓板美術
               ├─ CAFE-05 地板 tint 與 cafe atlas
               ├─ CAFE-06 咖啡廳家具 catalog
               ├─ CAFE-20 at_cafe visualState
               └─ CAFE-22 寵物下樓

CAFE-07 CafeGuest 型別與姓名池        ← 可與 CAFE-01~03 完全平行
   ├─ CAFE-08 顧客走位與繪製  (需 CAFE-03)
   │     └─ CAFE-10 意圖泡泡
   ├─ CAFE-09 認養意圖 → 送養
   └─ CAFE-19 租屋意圖 → 招租

CAFE-11 state.cafe schema             ← 可與上面全部平行
   ├─ CAFE-12 常備訂單與庫存
   ├─ CAFE-13 日結 pass 與客流
   │     └─ CAFE-14 投資項
   ├─ CAFE-15 咖啡廳面板 UI
   └─ CAFE-16 研發資料與倒數
         ├─ CAFE-17 菜單與客單價
         └─ CAFE-18 研發面板 UI

CAFE-21 groupScene cafe venue   (需 CAFE-03 + CAFE-07)
CAFE-00 balance hook 逐項驗證   (貫穿全程，不可認養，由中控執行)
```

**三條可平行的主幹**：地圖線（01→03→…）、顧客線（07→…）、經濟線（11→…）。
顧客線與經濟線在 CAFE-03 完成前就能開始，只是最後要接起來。

---

## 二、貫穿全程的規則（每一項都適用）

寫進每一張 lease 的「禁止修改」：

| 禁止 | 原因 |
|---|---|
| 🔴 `src/sim/comfort.ts` 的 `COMMUNAL_AREA_IDS` 加入任何 cafe region | `COMMUNAL_NEUTRAL = 35.28233009708738` 立刻失效，全樓租客基準線位移，快照全紅 |
| 🔴 `INITIAL_PLACEMENTS`（`map.ts:150`）**中段插入** | `resolveTarget`（`routine.ts:176`）取陣列第一個符合的，插入會讓全樓租客的沙發/電視/廚房目標靜默改道。**只能 append** |
| 🔴 `map.ts` rows 0–32 的任何既有矩形 | 現有三樓一格都不能動，這是零漂移的第一道防線 |
| 🔴 `scripts/balance-snapshot.json` | 除非該項目**刻意**改平衡且經中控核可，否則紅燈代表實作有誤 |
| 🔴 `src/pixel/parts.ts` 的 `SKIN_TONES` / `ALL_HAIR_STYLES` / `ALL_ACCESSORIES` | 順序與長度被 `randomAppearance()` 的 `pick()` 消耗，動了會位移 seeded 序列 |
| 新增 `Math.random()` 呼叫 | 內容選擇一律用確定性雜湊（抄 `outing.ts:75` 的 `outingIndex`） |

**每一項的收工檢查**：`npm test`、`npm run build`、`npm run typecheck`、`npx tsx scripts/balance-test.ts`（**不得 `--update`**）。
程式與 `工作日誌.md` 同一個 commit（專案硬規則）；日誌加在檔尾「新紀錄追加點」以下並同步檔頭索引。

---

## 三、工作項目

### 第 0 期：Viewport（前置，無遊戲內容）

---

#### CAFE-01 抽出 `FloorViewport`
- **狀態** ✅ **完成**(commit `55e7a70`)
- **規模** 🟡 中 ・ **依賴** 無 ・ **可平行** 與 07、11 平行
- **做什麼**：`PixelDollhouse.vue:122` 的房間特寫**已經在用 `composeFloor` + scale/translate 畫鏡頭窗格**。把那段邏輯抽成共用元件／函式，改成接受**任意矩形**而非房間。
- **允許修改**：`src/components/PixelDollhouse.vue`、新增 `src/floor/viewport.ts`（或等價位置）
- **禁止修改**：`src/floor/floorScene.ts` 的 `composeFloor()` 本體、`src/components/FloorMap.vue`（留給 CAFE-02）
- **驗收錨點**：`rg -n "renderFloorViewport|viewportRect" src/floor/viewport.ts src/components/`
  （完成時改寫：實際抽出的是函式而非 Vue 元件，真實符號為 `viewportRect()` / `centeredViewportRect()` / `renderFloorViewport()` / `FloorViewportRect`，沒有叫 `FloorViewport` 的東西）
- **完成條件**：`roomcam-test.ts` 全綠且**行為逐位元不變**（房間特寫的輸出必須與重構前相同）；三種手機寬度 `ui:shot` 無破圖
- **風險**：`roomcam-test` 有亮度門檻，重構若改變繪製順序可能擦到

---

#### CAFE-02 獨立樓層頁面切換
- **狀態** ✅ **完成**(commit `cf36aea`，UX 修訂 `dff61c5`)
- **規模** 🟢 小 ・ **依賴** CAFE-01
- **做什麼**：`FloorMap.vue` 用 `FloorViewport` 建立兩個獨立樓層頁面（3F `(0,0,16,33)` / 1F `(0,34,16,18)`）。3F 只顯示「前往 1F 寵物咖啡廳」，1F 只顯示「返回 3F 租屋樓」；按下後替換整個樓層畫面。**不用同頁雙 tab、不呈現連續畫布、不做自由捲動或縮放手勢。**
- **允許修改**：`src/components/FloorMap.vue`、`src/App.vue`（僅切換器的接線）
- **禁止修改**：`src/floor/floorScene.ts`、`src/floor/agents.ts`
- **驗收錨點**：`rg -n "switchFloorPage|floorPageNavigation|FLOOR_VIEWS" src/components/FloorMap.vue`
- **完成條件**：3F→1F 與 1F→3F 都只有一顆目的地按鈕；切換後標題、畫布尺寸與 aria-label 一起改變；390/412/430 三寬度 `ui:shot` 無破圖
- **備註**：玩家介面是兩個頁面，底層仍共用單一 grid 與模擬，避免引入 `floorId`／存檔遷移／跨樓層尋路特例

---

### 第 1 期：地圖與空間

---

#### CAFE-03 地圖擴充：`GRID_H` 與 Region 🔴 關鍵路徑
- **狀態** ✅ **完成**(commit `5d43e0f`)
- **規模** 🟡 中 ・ **依賴** CAFE-02
- **做什麼**：`GRID_H` 33 → 52；新增 6 個 Region（`stairs` / `cafe_floor` / `cafe_counter` / `cafe_pet` / `cafe_back` / `cafe_entrance`，矩形見設計文件 §3.4）；同步三處硬編白名單中的**兩處**。
- **允許修改**：`src/floor/map.ts`（**僅新增 rows 33 以下**）、`src/sim/placements.ts`（**僅** `FLOOR_REGIONS`）、`src/sim/routine.ts`（**僅** `COMMUNAL`）
- **禁止修改**：🔴 `comfort.ts` 的 `COMMUNAL_AREA_IDS`（**絕對不加 cafe**）、rows 0–32 的任何既有矩形、`INITIAL_PLACEMENTS` 中段插入
- **驗收錨點**：`rg -n "GRID_H = 52|cafe_floor" src/floor/map.ts`
- **完成條件**：`balance-test` **零漂移**；`findPath` 可從 3F 任一格走到 1F 門口；`npm test` 全綠
- **風險**：`pathfind.ts:53` 的 key 編碼 `r * GRID_W + c` 會自動適應（`GRID_W` 不變），但 `floorScene.ts:37` 與 `petAgents.ts:33` 的 `const GRID = buildGrid()` 是模組載入時算的，要確認擴充後仍正確

---

#### CAFE-04 樓梯與樓板分隔帶美術
- **狀態** ✅ **完成**(commit `2b1b37c`)
- **規模** 🟢 小 ・ **依賴** CAFE-03 ・ **可與 05、06 平行**
- **做什麼**：row 33 的樓板橫切面、樓梯的俯視表現。素材用 `17_Visibile_Upstairs_System_16x16.png`。
- **允許修改**：`src/floor/floorScene.ts`（**僅新增繪製函式與 dispatch**）、`src/art/limezu.ts`、`scripts/limezu-manifest.json`
- **禁止修改**：既有的 18 個 frame 座標、`furniture.png`（見 CAFE-05）、`drawAgent` 與人物繪製
- **驗收錨點**：`rg -n "drawStairs|drawFloorDivider" src/floor/floorScene.ts`
- **完成條件**：`limezu-art-test.ts` 全綠；離線 `render-floor.ts` 出圖目視兩層樓分隔清楚

---

#### CAFE-05 地板 tint 與 cafe atlas
- **狀態** ✅ **完成**(commit `59a1585`)
- **規模** 🟡 中 ・ **依賴** CAFE-03 ・ **可與 04、06 平行**
- **做什麼**：6 個新 Region 的 `FLOOR_TINT`；新增 **`public/assets/limezu/cafe.png`**（獨立一張，**不要擴充 `furniture.png`**——它有 128×128 尺寸斷言且 18 個 frame 已逐像素驗證過）。
- **允許修改**：`src/floor/floorScene.ts`（**僅** `FLOOR_TINT`）、`src/art/limezu.ts`、`scripts/limezu-manifest.json`、新增 `public/assets/limezu/cafe.png`
- **禁止修改**：`public/assets/limezu/furniture.png`、`floors.png`、`walls.png`
- **驗收錨點**：`rg -n "cafe\.png|LIMEZU_CAFE" src/art/limezu.ts scripts/limezu-manifest.json`
- **完成條件**：`limezu-art-test.ts` 全綠（含既有的 furniture.png 尺寸斷言仍過）；atlas 建置可重現（跑兩次 sha256 相同）

---

#### CAFE-06 咖啡廳家具 catalog 條目與 atlas runtime 接線
- **狀態** ✅ **完成**(commit `4b5a43f`)
- **規模** 🟢 小 ・ **依賴** CAFE-03 ・ **可與 04、05 平行**
- **做什麼**：吧台、咖啡機、展示櫃、菜單板、店內桌椅、貓跳台、軟墊、貨架、木箱、冷藏。每件都要有 `sprite: { recipe: [...] }` 程序 fallback；CAFE-05 已建立的 `cafe.png` 則走獨立非阻塞 preload 與 1:1 繪製，載入失敗時自動回到 recipe。
- **允許修改**：`src/furniture/catalog.ts`（**僅 append 新條目**）、`src/art/limezu.ts`（僅 cafe atlas preload／draw／reset）、`src/main.ts`（僅新增一個非阻塞 preload）、`scripts/data-catalog-test.ts`、`scripts/furniture-art-test.ts`、`scripts/limezu-art-test.ts`
- **禁止修改**：既有家具的任何欄位、`src/furniture/tier.ts`、`INITIAL_PLACEMENTS` 中段
- **驗收錨點**：`rg -n "cafe_counter|espresso_machine|preloadLimezuCafeAtlas" src/furniture/catalog.ts src/art/limezu.ts src/main.ts`
- **完成條件**：`data-catalog-test.ts` 全綠（含 tier fallback 斷言）；`furniture-art-test.ts` 全綠；`limezu-art-test.ts` 驗證 cafe atlas 並行 preload、1:1 繪製、失敗 fallback
- **備註**：新家具**必須標 tier**（未標會 fallback 到 standard，見 `tier.ts:23`）

---

### 第 2 期：顧客與認養

---

#### CAFE-07 `CafeGuest` 型別與獨立姓名池
- **狀態** ✅ **完成**(commit `c5288f8`)
- **規模** 🟡 中 ・ **依賴** 無（**可最早開工，與地圖線完全平行**）
- **做什麼**：`CafeGuest` 介面（欄位見設計文件 §6.2）、獨立姓名池、生成與離開的純函式；外觀沿用既有白名單但以 seed 雜湊選取，不能直接呼叫 `randomAppearance()`。**不接任何 UI、不進 `state.runtimes`。**
- **允許修改**：新增 `src/sim/cafeGuests.ts`、`src/types.ts`（僅新增型別）、新增 `src/content/cafeGuestNames.ts`、新增 `scripts/cafe-guest-test.ts`、`scripts/run-all.ts`
- **禁止修改**：🔴 `src/sim/gameState.ts` 的 `state.runtimes`、`src/sim/recruit.ts` 的 `NAME_IDENTITIES`（只有 20 筆且與租客共用，**不要動它，另開池**）
- **驗收錨點**：`rg -n "CafeGuest|cafeGuestNames" src/sim/ src/content/ src/types.ts`
- **完成條件**：新增 `scripts/cafe-guest-test.ts` 並**加進 `run-all.ts` 的 `REGRESSION`**；斷言零 RNG（覆寫 `Math.random` 計數）、生成決定性、姓名不與 `NAME_IDENTITIES` 重疊、cap 6 與 `state.runtimes` 零污染
- **風險**：`run-all.ts` 是 Codex 也會改的檔案（它加測試時也要改那一行），**認養前確認對方沒在改**

---

#### CAFE-08 顧客走位與繪製
- **狀態** ✅ **完成**(commit `4ce7f2c`)
- **規模** 🟡 中 ・ **依賴** CAFE-03 + CAFE-07
- **做什麼**：顧客從 `cafe_entrance` 進場、走到未被家具阻擋的座位、停留，再走回入口離場；生命週期為 `entering → seated → leaving → departed`。繪製沿用人物 sprite 與 y-sort，但不進租客 agent/runtime。
- **允許修改**：`src/floor/floorScene.ts`（**僅新增顧客繪製分支**）、`src/sim/cafeGuests.ts`、新增 `src/floor/guestAgents.ts`、新增 `scripts/cafe-guest-agent-test.ts`、`scripts/render-floor.ts`（僅離線顧客旗標）、`scripts/run-all.ts`
- **禁止修改**：🔴 `src/floor/agents.ts`（Codex 活躍區，且顧客不該進 `tickAgents`）、`petAgents.ts`、`groupScene.ts`
- **驗收錨點**：`rg -n "createGuestAgents|tickGuestAgents|drawGuest" src/floor/`
  （完成時改寫：原錨點的 `guestAgents` 只命中 import 路徑字串，改成真實導出符號 `createGuestAgents()` / `tickGuestAgents()`（`src/floor/guestAgents.ts`）與 `drawGuest()`（`floorScene.ts:179`））
- **完成條件**：顧客 agent 專項 17 項斷言全綠；六位顧客能同時進場、入座、離場且零 RNG；`balance-test` 零漂移；`render-floor --cafe-guests` 離線出圖看得到顧客。CAFE-11 建立 `state.cafe` 前不接正式 `FloorMap` 客流，未開張頁面不得硬塞示範顧客。

---

#### CAFE-09 認養意圖 → 寵物送養
- **狀態** ✅ **完成**(commit `4e0da33`)
- **規模** 🟢 小 ・ **依賴** CAFE-07 ・ **可與 08 平行**
- **做什麼**：`intent === "adopt"` 的顧客觸發抉擇；同意時 `acceptCafeGuestAdoption()` 驗證顧客、永久樓寵物與媒合狀態，再直接走既有 `finishRehoming`，將新名冊項目的 `destination` 寫成「咖啡廳客人領養」。同一顧客以穩定名冊 id 防止重複領養第二隻。
- **允許修改**：`src/sim/cafeGuests.ts`、`src/sim/pets.ts`（**僅新增一個 destination 值或一個入口函式**）、新增 `scripts/cafe-adoption-test.ts`、`scripts/run-all.ts`
- **禁止修改**：`beginRehoming` / `finishRehoming` 的既有邏輯、`state.petHomes` 的資料形狀、`rehomeDurationMs`
- **驗收錨點**：`rg -n "acceptCafeGuestAdoption|CAFE_GUEST_ADOPTION_DESTINATION" src/sim/pets.ts`
  （完成時改寫：`cafe_adopter` 這個符號從未存在；真實符號是 `pets.ts:65` 的 `CAFE_GUEST_ADOPTION_DESTINATION = "咖啡廳客人領養"` 與 `pets.ts:171` 的 `acceptCafeGuestAdoption()`）
- **完成條件**：專項 17 項斷言涵蓋立即完成、正確 destination、存檔、清理引用、非認養／房客寵物／媒合中／重複點擊零變更與零 RNG；`pet-test.ts` 全綠。CAFE-15 前不接接受／婉拒 UI，CAFE-22 前不以直接呼叫函式宣稱玩家端可達。

---

#### CAFE-10 意圖泡泡
- **狀態** ✅ **完成**(commit `a066576`)
- **規模** 🟢 小 ・ **依賴** CAFE-08
- **做什麼**：顧客頭上的思考泡泡（喝咖啡／想認養／想租屋三種）。素材 `UI_thinking_emotes_animation_16x16.png`；每種兩幀、約 1 秒循環，只在已入座且停止移動時顯示。
- **允許修改**：`src/floor/floorScene.ts`（僅泡泡繪製）、`src/art/limezu.ts`、`scripts/limezu-manifest.json`、`public/assets/limezu/cafe.png`（只 append y=64 意圖列）、`scripts/limezu-art-test.ts`、新增 `scripts/cafe-intent-bubble-test.ts`、`scripts/render-floor.ts`、`scripts/run-all.ts`
- **禁止修改**：人物 sprite、`src/pixel/*`
- **驗收錨點**：`rg -n "drawIntentBubble|tryDrawLimezuThinkingEmote" src/floor/floorScene.ts`
  （完成時改寫：`thinking_emote` 只出現在 `src/art/limezu.ts` 的 frame id，不在 `floorScene.ts`；`floorScene.ts` 這一側的真實符號是 `:197` `drawIntentBubble()` 與 `:203` 呼叫的 `tryDrawLimezuThinkingEmote()`）
- **完成條件**：專項 12 項斷言全綠；`limezu-art-test.ts` 99/99；離線 `render-floor --cafe-guests` 同時看得到咖啡杯／愛心／信封，atlas 不可用時三種程序 fallback 仍可辨識；進場、離場、移動中與 hidden 顧客不顯示，輸入資料與 RNG 都不變。CAFE-11／13 接正式客流前，公開 1F 不塞示範顧客或泡泡。

---

### 第 3 期：經濟迴圈

---

#### CAFE-11 `state.cafe` schema 與存檔
- **狀態** ✅ **完成**(commit `53b42c7`)
- **規模** 🟢 小 ・ **依賴** 無（**可最早開工**）
- **做什麼**：`state.cafe` 一個 top-level key（欄位見設計文件 §8）＋ 存檔讀寫。**additive 選填欄位，不升 `SAVE_VERSION`**（慣例白紙黑字寫在 `persistence.ts:242-245`）。
- **允許修改**：`src/sim/gameState.ts`（**僅新增 `state.cafe`**）、`src/sim/persistence.ts`（**僅 save/load 各一行**）、`src/types.ts`
- **禁止修改**：🔴 `SAVE_VERSION`、`MIGRATIONS`、任何既有 state 欄位
- **驗收錨點**：`rg -n "state\.cafe|defaultCafe" src/sim/gameState.ts src/sim/persistence.ts`
- **完成條件**：舊存檔載入 → `state.cafe` 為預設值且不報錯；存檔往返測試；`SAVE_VERSION` 仍為 6

---

#### CAFE-12 常備訂單與庫存
- **狀態** ✅ **完成**(commit `8394ecf`)
- **規模** 🟡 中 ・ **依賴** CAFE-11
- **做什麼**：原料清單、常備量設定、每日自動補貨與扣款、生鮮損耗、缺貨處理（**推日誌而非懲罰**）。
- **允許修改**：`src/sim/cafe.ts`（新增）、新增 `src/content/cafeIngredients.ts`
- **禁止修改**：`src/sim/economy.ts` 的既有函式本體（**只能呼叫 `addMoney`**）
- **驗收錨點**：`rg -n "standingOrders|CAFE_INGREDIENTS" src/sim/cafe.ts src/content/cafeIngredients.ts`
  （完成時改寫：`cafeIngredients` 只命中 import 路徑字串，資料的真實符號是 `src/content/cafeIngredients.ts` 導出的 `CAFE_INGREDIENTS`；`standingOrders` 是 `cafe.ts:165` `restockPlan()` 的第一個參數）
- **完成條件**：錢不夠時只補得起多少補多少且不變負數；零 RNG

---

#### CAFE-13 日結 pass 與客流公式
- **狀態** ✅ **完成**(commit `aa6507f`)
- **規模** 🟡 中 ・ **依賴** CAFE-12
- **做什麼**：日結 pass 掛在 `tick.ts` 換日區塊的 **`collectRent()` 正後方**（讓咖啡廳損益與租金/管理費落在同一帳日）。客流公式抄 `coinLaundryIncome()`（`economy.ts:27`）的形狀。天氣走 `weatherForDay()`、星期走 `week.ts`（**兩者都是現成的確定性來源**）。
- **允許修改**：`src/sim/tick.ts`（**僅換日區塊插入一行**）、`src/sim/cafe.ts`、`src/sim/gameState.ts`（僅 `TxnCategory` 加 `"cafe"`）
- **禁止修改**：🔴 `collectRent()` 本體、`coinLaundryIncome()`、`BASE_UPKEEP` / `PER_ROOM_UPKEEP`、換日區塊既有 pass 的順序
- **驗收錨點**：`rg -A5 "collectRent\(\)" src/sim/tick.ts | rg "cafeDailyPass"`
- **完成條件**：**未開張時 `balance-test` 零漂移**（種子局不會開張，這是天然閘門）；成熟期日淨利 ≈ 日租金的 30–50%

---

#### CAFE-14 投資項
- **狀態** ✅ **完成**（本批；完整實作紀錄見 `工作日誌.md` 的 CAFE-14 條目）
- **規模** 🟢 小 ・ **依賴** CAFE-13
- **做什麼**：5 個投資項（設計文件 §5.4）與一次性的 `openCafe()` 開張動作。語意與 `buyUpgrade()`（`economy.ts:358`）同構：一次性、不可退、永久生效；開張費依種子局資金與基本營業設備定標為 $12,000。純交易結果由 CAFE-15 透過既有金流入口提交。
- **允許修改**：`src/sim/cafe.ts`、`src/content/cafeUpgrades.ts`（新增）、新增 `scripts/cafe-upgrade-test.ts`、`scripts/run-all.ts`、對應設計／工作分解／工作日誌
- **禁止修改**：`buyUpgrade()` 本體、`upgradeState`
- **驗收錨點**：`rg -n "export function (openCafe|buyCafeUpgrade)|export const CAFE_UPGRADES" src/sim/cafe.ts src/content/cafeUpgrades.ts`
- **完成條件**：專項 27/27；餘額不足、重複開張、未知／未開張／重複／餘額不足投資全都零扣款且不修改輸入；五種 capability 生效；零 RNG；未開張 balance 快照零漂移

---

#### CAFE-15 咖啡廳面板 UI
- **狀態** ✅ **已完成**（commit 見 `工作日誌.md` 的 CAFE-15 紀錄）
- **規模** 🟡 中 ・ **依賴** CAFE-09 + CAFE-14
- **做什麼**：新 Vue 元件。版型參照既有的 `FinancePanel` / `UpgradePanel`。未開張時顯示開張成本與按鈕；開張後顯示今日單數、熱銷品、缺貨警告、常備量設定、投資項，以及顧客認養詢問的接受／婉拒操作。
- **允許修改**：新增 `src/components/CafePanel.vue`、`src/App.vue`（僅接線）
- **禁止修改**：其他既有 `.vue`
- **驗收錨點**：`rg -n "CafePanel" src/components/ src/App.vue`
- **完成結果**：新增獨立 bottom sheet 與樓層頁操作按鈕；UI 可完成開張、修改並保存常備量、五項永久投資，
  以及認養詢問的接受／婉拒接線。開張與投資都經既有 `addMoney` 留下 `cafe` 收支流水並存檔；接受認養
  只有在 sim 命令成功後才移除顧客。
- **完成條件**：390/412/430 三寬度 `ui:shot` 共 18 張全部人工檢視，0 errors／0 warnings、無橫向溢出；
  瀏覽器端到端完成開張、常備量持久化、投資與收支流水，console 0 errors。認養 UI／17 項 sim 斷言已完成，
  但實際玩家端認養決策仍依賴 CAFE-22 讓永久樓寵物進入 `cafe_pet`，併入公開上線整合驗收。

#### 公開上線閘門（第 3 期收尾）

- **狀態**：✅ CAFE-01～15 + CAFE-22 已完成；最低功能閘門已達成，
  **並已於 `8d753d8` push 至 `main`、由 Cloudflare 自動 build+deploy 完成公開上線與 production 驗證**
  （2026-08-02 更新；原文寫「尚未 push／deploy」已過期）。
- **最低功能範圍**：CAFE-01～15 全部完成，另將 `CAFE-22` 提前納入；CAFE-16～21 可延後。
- **理由**：01～06 只有空間與家具；07～10 才有顧客與認養核心；11～15 才形成開張、補貨、日結、再投資與玩家操作的完整閉環；22 確保認養流程在實際畫面中可達。
- **整合驗收**：CAFE-00 逐 hook balance 記錄、舊存檔載入、開張→補貨→日結→投資、顧客認養端到端、390/412/430 UI Lab、正式 build 與 browser console 0 errors。
- **預覽版界線**：CAFE-06 完成後可放內部／測試環境看空間與商店資料，不建議當成公開功能發布。

---

### 第 4 期：研發（可無限期延後）

---

#### CAFE-16 研發資料與倒數
- **狀態** ✅ **已完成**（commit 見 `工作日誌.md` 的 CAFE-16 紀錄）
- **規模** 🟡 中 ・ **依賴** CAFE-11
- **做什麼**：研發樹前兩層（設計文件 §5.3）、日數倒數。**照抄既有的日數倒數**（`wishes.ts`、`floorChain.ts`、`pets.ts:133` 的 `rehomingAtMs`），**不要發明新機制**。同時只能研發一項。
- **允許修改**：`src/sim/cafe.ts`、新增 `src/content/cafeResearch.ts`
- **驗收錨點**：`rg -n "cafeResearch|CAFE_RESEARCH" src/`
  CAFE-13 原本的 `cafeResearchTicketBonus()` 恆回 0 樁會造成假陽性；本項完成後真正的判準
  `src/content/cafeResearch.ts` 已存在，且由 `src/sim/cafe.ts` 匯入 `CAFE_RESEARCH`。
  ⚠️ 另見 `工作日誌.md` 交接紀錄第六節：現在日淨利是日租金的 39.1%。CAFE-16 實作時重算發現
  原交接的「單一 +8% 約 43%」低估了：$36 取整到 $39 後，成熟期約多 $119／日，會到約
  **50.8%**，單項就越過 50% 上緣。因此客單價效果留給 CAFE-17 連同菜單一起重新定標，不能直接往上加。
- **完成結果**：`src/content/cafeResearch.ts` 定義前兩層 10 項、研究／設備前置、2～4 天與
  $2,500～5,000 成本；`startCafeResearch()` 維持純交易與單一 active，`cafeResearchDaysLeft()`／
  `advanceCafeResearch()` 以遊戲日序號倒數並冪等完成。CAFE-17 尚未接菜單／客單價，所以本項維持
  `cafeResearchTicketBonus() === 0`，成熟期 39.1% 基準不變。
- **完成條件**：`cafe-research-test.ts` 42/42，咖啡廳 12/12 支專項回歸全綠；完整回歸、typecheck、
  build 與 balance snapshot 結果見同批工作日誌。

---

#### CAFE-17 菜單與客單價
- **狀態** ✅ **已完成**（commit 見 `工作日誌.md` 的 CAFE-17 紀錄）
- **規模** 🟢 小 ・ **依賴** CAFE-16
- **驗收錨點**：`rg -n "menuItems|avgTicket" src/sim/cafe.ts`
- **完成結果**：開張基礎菜單 3 項；10 項研發各新增菜名、標價與客群標籤。`menuItems()` 對 unknown／
  duplicate 安全並維持宣告順序；`avgTicket()` 以第二層完成 2／5 項跨 $37／$38，硬上限 $38，
  `cafeTicketPrice()` 保留既有日結接線。完整研發 56 日成熟期實測 `$477／日 = 日租金 46.9%`。
- **完成條件**：`cafe-menu-test.ts` 23/23、咖啡廳 13/13 支回歸全綠；完整回歸、typecheck、build 與
  balance snapshot 見同批工作日誌。沒有修改 tick、CafeState、顧客意圖比例或 UI。

---

#### CAFE-18 研發面板 UI
- **狀態** ✅ **已完成**（commit 見 `工作日誌.md` 的 CAFE-18 紀錄）
- **規模** 🟢 小 ・ **依賴** CAFE-16 + CAFE-15
- **允許修改**：`src/components/CafePanel.vue`
- **完成結果**：營運面板可開始單一研發並走既有 cafe 金流／存檔；顯示 active 倒數、投入金額、進度，
  鎖定項目會列出研究／設備前置。菜單同步顯示基礎與研發新品、售價、客群、平均客單價及 $37／$38
  里程碑。面板開啟時依 `gameDayIndex` 自動補結算到期項目；沒有擴大修改全域 tick。
- **完成條件**：CAFE-16／17 專項、完整回歸、typecheck、build、balance snapshot、三手機寬度 UI Lab
  與瀏覽器開始／倒數／到期／菜單／收支互動驗證結果見同批工作日誌。
- **範圍界線**：本 UI 項沒有擴張 tick；面板關閉期間的 lazy settlement 缺口已由下列 CAFE-18B 獨立補上。

---

#### CAFE-18B 背景研發結算
- **狀態** ✅ **已完成**（commit 見 `工作日誌.md` 的 CAFE-18B 紀錄）
- **規模** 🟢 小 ・ **依賴** CAFE-16 + CAFE-17 + CAFE-18
- **允許修改**：`src/sim/tick.ts`、`scripts/cafe-daily-test.ts`
- **完成結果**：`cafeDailyPass()` 在客流與營收前先呼叫冪等 `advanceCafeResearch()`；完成時寫回既有
  `state.cafe` 並推一則通知。面板關閉、快轉與離線補進度都能在每日結算完成研發，完成當天立即使用
  新菜單客單價；未到期／未知 id／重跑不修改研發或重複通知。
- **完成條件**：`cafe-daily-test.ts` 新增背景完成、$36→$37 當日營收、通知一次、未到期與零 RNG 時序
  驗證；完整回歸、typecheck、build 與 balance snapshot 見同批工作日誌。

---

### 第 5 期：租屋詢問與同框（可無限期延後）

---

#### CAFE-19 租屋意圖 → 招租
- **狀態** ✅ **已完成**（commit 見 `工作日誌.md` 的 CAFE-19 紀錄）
- **規模** 🟡 中 ・ **依賴** CAFE-07
- **做什麼**：`intent === "rent"` 生成一個 `Applicant` 放進 `state.applicantPools[roomId]`。這條顧客看過咖啡廳，**帶初始好感加成**。房間歸屬用 `rescoreApplicants`（`recruit.ts:273`，正好為此存在）重算。
- **允許修改**：`src/sim/cafeGuests.ts`、`src/sim/recruit.ts`（**僅新增一個入口函式**）
- **禁止修改**：🔴 `NAME_IDENTITIES`、`matchStars()` / `offeredRent()` 本體、`generateApplicants()`
- **驗收錨點**：`rg -A10 "export function .*[Cc]afe.*[Aa]pplicant" src/sim/recruit.ts`
- **完成結果**：`acceptCafeGuestApplicant(guest, roomId)`（`recruit.ts`，比照 CAFE-09 認養入口回傳
  `{ ok, text }`）只收 `intent === "rent"`、空房且尚未處理過的顧客，沿用原型／`offeredRent()`／
  `matchStars()` 組出應徵者後追加進當日池；顧客不進 `state.runtimes`，UI 接線留給後續。
  **初始好感 = 偏好權重 ×1.25（單項上限 10）**：寫進 `preferences` 而不是 `stars`，這樣
  `rescoreApplicants()` 每次覆寫 `stars` 後加成仍在；星等仍由 `matchStars()` 夾在 1～5。
  姓名綁定性別改由 `cafeGuests.ts` 的 `cafeGuestGender()` 提供，避免重演姓名／性別分開亂抽的舊 bug。
- **完成條件**：新增 `scripts/cafe-rent-intent-test.ts`（26 條：拒絕條件、欄位相容、加成與上限、
  重算後加成不消失、決定性與零 `Math.random`、冪等與池量上限）；`npm test` 83/83、typecheck、
  build 全綠，balance 快照零漂移（此入口只在玩家觸發時寫 state，種子局不受影響）。
- **UI 接線（✅ 已完成）**：`CafePanel.vue` 新增「租屋詢問」卡（比照同檔認養卡，改用藍紫色系），
  列出 `intent === "rent"` 顧客 → 下拉選空房（`ROOM_APPEARANCE` ∩ `isVacant`，顯示成「303 房」）→
  `onAcceptRentInquiry()` 呼叫入口，`result.ok` 才用既有 `removeHandledGuest()` 移除顧客並 `emit("done", text)`；
  `onDeclineRentInquiry()` 直接移除並回婉拒訊息。沒有空房時顯示警示且「安排看房」`:disabled`。
  下拉為避免空的 `v-model` 顯示空白列，改用 `:value` + `@change`，預設值＝第一間空房，與接受時的回退一致。
  驗收錨點：`rg -n "acceptCafeGuestApplicant" src/components`（命中 `CafePanel.vue`）；
  build 後 `grep -F "這位顧客目前沒有租屋意願" dist/assets/*.js` 命中 = 不再被 tree-shake。

---

#### CAFE-20 `at_cafe` visualState
- **規模** 🟡 中 ・ **依賴** CAFE-03
- **做什麼**：租客下樓坐咖啡廳。**這是本案唯一必須新增 visualState 的地方，也是最可能造成漂移的點。**
- **允許修改**：`src/types.ts`（`TENANT_VISUAL_STATES` 新增一個）、`src/sim/routine.ts`、`data/routines.json`、`src/content/observationLines.ts`
- **禁止修改**：🔴 既有 visualState 的任何行為、`EFFECT` 表既有數值
- **驗收錨點**：`rg -n "at_cafe" src/types.ts src/sim/routine.ts`
- **完成條件**：🔴 **必須設 `CAFE_FIRST_DAY` 閘門**（比照 `outing.ts:55` 的 `OUTING_FIRST_DAY = 14`），讓 10 日快照窗內不可能觸發；`balance-test` 零漂移

---

#### CAFE-21 `groupScene` 的 cafe venue
- **規模** 🟢 小 ・ **依賴** CAFE-03 + CAFE-07
- **做什麼**：`GroupSceneVenue`（`groupScene.ts:17`）加 `"cafe"`，讓租客/顧客/寵物在咖啡廳同框。
- **允許修改**：`src/floor/groupScene.ts`（**僅新增 venue**）
- **禁止修改**：既有的 `lounge` / `rooftop` venue 行為
- **⚠️ 認養前必讀**：`groupScene.ts` 是 Codex 新建的檔案，**優先讓 Codex 認養這一項**
- **驗收錨點**：`rg -n '"cafe"' src/floor/groupScene.ts`

---

#### CAFE-22 寵物下樓
- **狀態** ✅ **已完成**（commit 見 `工作日誌.md` 的 CAFE-22 紀錄）
- **規模** 🟢 小 ・ **依賴** CAFE-03
- **做什麼**：`petAgents.ts` 的走位範圍加上 `cafe_pet` 區；樓寵物白天有機率下樓。
- **允許修改**：`src/floor/petAgents.ts`
- **禁止修改**：`src/sim/pets.ts` 的行為邏輯
- **⚠️ 認養前必讀**：`petAgents.ts` 是 Codex 近期改過的檔案（`0b40d42`），**優先讓 Codex 認養**
- **驗收錨點**：`rg -n "cafe_pet" src/floor/petAgents.ts`
- **公開閘門**：雖編號位於第 5 期段落，本項必須提前與第 2 期一起完成；否則咖啡廳認養在實際畫面不可達。
- **完成結果**：咖啡廳開張後，永久樓寵物在 10:00～15:59 以每遊戲小時 35% 的穩定雜湊判定
  前往 `cafe_pet`；closed／非白天／房客寵物／中途寵物／配對演出全部維持原 `hangout`。不修改
  sim state、不新增 RNG，沿既有跨樓層 BFS 走下樓。
- **完成條件**：專項 12/12，涵蓋 guard、決定性／零新增 RNG、建立位置、跨樓層路徑、`hangout`
  不變與 cafe 認養整合；完整回歸 80/80、balance 零漂移、typecheck／build 通過；三手機寬度 UI Lab
  18 張為 0 errors／0 warnings 並逐張檢視。
- **已知界線**：CAFE-15 UI 仍列出所有永久樓寵物，沒有把 ephemeral agent 座標當成接受認養的硬條件；
  本項完成公開設計要求的畫面可達性，嚴格「現場才可認養」若要實作需另開共享位置狀態工作項。

---

### 貫穿全程

---

#### CAFE-00 balance hook 逐項驗證（**不可認養，由中控執行**）
- **做什麼**：每個會碰模擬的項目完成後，**單獨跑一次 `balance-test`** 並記錄「哪個 hook 造成哪幾個欄位動了多少」。
- **為什麼不可認養**：這是整合責任，且需要跨項目的全域視角。專案 `CLAUDE.md` 明訂中控不得接受未驗證的部署類交接。
- **原則**：把每個 hook 拆成獨立可關閉的開關，一次只打開一個。**這比一次全開然後解釋一整面紅字容易十倍。**

---

## 四、建議的認養順序

### 若只有一個 Agent
`01 → 02 → 03 → 06 → 05 → 04 → 07 → 08 → 09 → 10 → 11 → 12 → 13 → 15 → 14`
（第 4、5 期延後）

### 若兩個 Agent 平行

| 時段 | Claude Code | Codex |
|---|---|---|
| 1 | CAFE-01 FloorViewport | CAFE-07 CafeGuest 型別 |
| 2 | CAFE-02 獨立樓層頁面切換 | CAFE-11 state.cafe schema |
| 3 | CAFE-03 地圖擴充 🔴 | CAFE-12 常備訂單 |
| 4 | CAFE-06 家具 catalog | CAFE-13 日結 pass |
| 5 | CAFE-05 cafe atlas | CAFE-15 面板 UI |
| 6 | CAFE-08 顧客走位 | CAFE-04 樓梯美術 |
| 7 | CAFE-09 認養接線 | CAFE-14 投資項 |

**時段 3 是唯一的序列瓶頸**——CAFE-03 動 `map.ts` 且是關鍵路徑，那段期間另一方要避開所有 `src/floor/`。

**Codex 優先認養**：CAFE-21（`groupScene.ts` 是它建的）、CAFE-22（`petAgents.ts` 是它近期改的）、CAFE-04（美術第一、二批都是它做的）。

---

## 五、與既有待辦的收斂

實作到第 1~2 期時，既有清單的兩條 🔴 需要收斂（**不是另開新條**）：

| 既有 🔴 | 何時收斂 | 錨點該改成 |
|---|---|---|
| 訪客 NPC | CAFE-07 + CAFE-08 已完成資料、走位與繪製抽象；CAFE-11／13 再接正式生成 | `rg -n "CafeGuest|GuestAgent|drawGuest" src/sim/ src/floor/` |
| G-12 樓層擴建 | CAFE-03 完成後 | `rg -n "GRID_H = 52" src/floor/map.ts` |

這是驗收錨點制第一條規則的直接應用：**完成時若實際符號與錨點不符，改狀態的同時要把錨點改寫成真實符號**，讓錨點從「猜測」變成「這條為什麼算完成」的證據。
