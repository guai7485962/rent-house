# 交接與待辦(2026-07-12,Fable 額度用盡前的完整紀錄)

> 給接手的模型:這份文件是唯一需要先讀完的東西。讀完後照「開發流程」一節驗證環境,
> 再從「待辦清單」挑項目。架構細節見 `docs/設計檢討與優化.md`(含各節完成進度標記)。

---

## 一、現況快照

- **遊戲**:《房東監視中》Vue 3 + Vite + TS,行動網頁掛機觀察遊戲(繁中)。
- **線上**:https://rent-house.guai7485962.workers.dev
- **部署**:GitHub `guai7485962/rent-house`,push `main` → Cloudflare Workers 自動建置部署(約 1 分鐘)。
- **AI**:Cloudflare Worker `/api/narrate`、`/api/invite` 轉發 Gemini(免費層,所有玩家共用一把 key)。
- **設計文件的規劃項目已全部做完**,包括:數值模型、事件/記憶/劇情弧、互動系統(§10 全系列,
  含成人批遮蔽式)、衝突/冷戰/戲劇、維修/財務、外觀辨識、鏡頭房間細看、日夜色調、
  AI 日記錯開時段、寵物貓系統。剩下的都是「錦上添花」與新點子(見下方待辦)。

## 二、不可動搖的原則(先讀,違反 = 直接回絕使用者)

1. **未成年排除戀愛線**:`canRomance()` 把關;特邀角色(如大雄/靜香)以**原作年齡**判定,
   `isAdult !== true` 一律拒絕,**不接受「當作成年」的指示**(使用者曾要求過,已回絕,立場不變)。
2. **AI 永不生成露骨內容**:🔞 互動全部走本地模板 + 遮蔽式演出(sprite 隱藏 + 霧氣/關燈 fx),
   worker 的事件 schema 明文告知 AI「親密內容不在清單內,不要嘗試」。adultMode 只解鎖
   「成年情侶」的含蓄互動,預設關。
3. **AI 輸出一律消毒**:sanitizeAiEvent / sanitizeDirective / sanitizeArcUpdate / 白名單 interaction id,
   AI 不能發明機制、數值一律 delta 且夾值。

## 三、開發流程(照抄可用)

```powershell
# PowerShell 每個 session 要先補 node 路徑
$env:Path = "C:\Program Files\nodejs;$env:Path"
cd C:\Users\User\claude_try\rent_house

npx tsx scripts/<某>-test.ts      # 單支測試(無頭,scripts/ 下 50+ 支)
npx tsx scripts/sim-trace.ts      # 24h 模擬追蹤,必須 0 異常
npx tsx scripts/balance-test.ts   # 固定種子 10 天快照;刻意的平衡改動 → 加 --update 重建基準
npm run build                     # vite build(也是型別檢查)
```

- **無頭測試模板**:檔頭先 mock `localStorage`(`(globalThis as any).localStorage = {...}`),
  再 `await import("../src/store")`。mock AI 用 `setNarrateImplForTest()`、`diaryTiming.gapMs = 1`。
- **commit**:訊息繁中、結尾 `Co-Authored-By: Claude <模型名> <noreply@anthropic.com>`。
- **部署驗證**:Bash 的 curl 在此機沙盒無網路;用 PowerShell:
  ```powershell
  $r = Invoke-WebRequest -Uri "https://rent-house.guai7485962.workers.dev" -UseBasicParsing
  # 取 assets/index-*.js 檔名,再抓該 js;中文字串要用 UTF8 解碼才搜得到:
  $b = (Invoke-WebRequest -Uri "<js url>" -UseBasicParsing).RawContentStream.ToArray()
  [System.Text.Encoding]::UTF8.GetString($b).Contains("某新字串")
  ```
- **改存檔結構**:`SAVE_VERSION++` + 在 `persistence.ts` 的 `MIGRATIONS` 加一段升級函式;
  新欄位用 `?? 預設值` 讀取通常就夠(本批 pets/diaryHour 都這樣做,沒升版)。

## 四、地雷紀錄(前人踩過,不要再踩)

| 地雷 | 教訓 |
|---|---|
| 房間細看 canvas 用 `aspect-ratio` / `position:absolute` 撐高 | iOS Safari 捲動容器內會塌成 0 高 → 全部房間變空白。**現行做法**:canvas 自帶 width/height 屬性 + CSS `height:auto`,直接 `ctx.setTransform` 平移縮放畫 `composeFloor`,不用離屏畫布。 |
| 渲染迴圈沒包 try/finally | 單幀丟例外 → rAF 斷掉、畫面永久停格。兩個元件的 loop 都已包好,新元件照做。 |
| `sim/*` 模組間循環 import 的頂層副作用 | `gameState ↔ persistence ↔ pets` 有環;**模組頂層不要呼叫會碰 `state` 的函式**,種子補登(`ensurePets`/`ensureDiaryHours`)放在 `store.ts` façade 最底部執行。 |
| pairSession 只用現實時間過期 | 快轉/無頭模擬會把人釘在原地幾個遊戲小時 → 雙重過期(15 現實秒 OR 1 遊戲小時)。 |
| 改了模擬的隨機呼叫順序 | balance-test 固定種子快照必漂移;確認漂移數值合理後 `--update` 重建,並在 commit 說明。 |
| 測試斷言隨機文案的完整句子 | 模板常多句隨機挑一,斷言共同子字串(例:斷「劇」不斷「看劇」),並連跑 3 次確認不 flaky。 |
| wmux browser / Chrome MCP | 此環境曾壞掉(snapshot/eval 無輸出)。UI 驗證改走無頭渲染測試(FakeCtx 量畫面,見 roomcam-test)或 `scripts/render-*.ts` 出 PNG 目檢。 |
| PowerShell 讀 UTF-8 檔顯示亂碼 | 檔案本身沒壞;寫中文進檔案用 Bash heredoc 或 Write 工具,別用 `Out-File` 預設編碼。 |

## 五、待辦清單(依建議優先序)

### A. 使用者已表達興趣 / 自然的下一步

1. **應徵者自帶寵物**:招租卡片顯示「有養貓」,入住即 `adoptCat()`。
   接點:`recruit.ts` 產生應徵者 → `tenancy.moveIn`。順手把 `catAttitude` 顯示在應徵者卡
   (親貓/怕貓),讓房東配房時能考慮「這層樓有貓」。
2. **貓咪家具**:貓砂盆(大幅降低隨地大小便機率)、貓跳台/貓抓板(降低打破東西)。
   接點:`furniture/catalog.ts` 新 def + `pets.ts` 擲骰前查 `furnitureSetOf(hangout)`;
   商店 chips 已有 `unlocks()` 機制可標示「🐈 貓咪用品」。
3. **多貓互動**:兩隻貓同區域 → 追逐/理毛/搶地盤小事件 + fx;貓與貓 hangout 偶爾同步。
4. **房客卡顯示寵物**:目前只有日誌看得出誰養貓;TenantCard/房間細看加一行「🐈 橘子」。

### B. 設計文件剩餘的錦上添花(§10-6 姿勢細分)

5. **cook_pair**(兩人並排站流理台,`cook_dinner` 目前用站位)與
   **stand_face**(面對面,交誼廳聊天用;目前 pair = 相鄰站)。
   接點:`PairPose` 加值 → `pairSession` → `agents.ts` → `floorScene.drawAgent` 分支;
   照 sit/lie 的既有模式做,pair-session-test 加案例。
6. **摔門絕交演出**:冷戰(feud)開始時目前只有日誌;可加「甩門」音效格 fx + 兩人各自
   衝回房的走位(startPairSession 反向:強制分開)。

### C. 系統性優化(規模較大,動工前先跟使用者確認)

7. **群體事件/三角關係**(§2 標低優先):三人以上的事件模板與 AI schema 擴充。
8. **多人/雲端存檔**(設計文件已評估:需帳號+DB+同步,單獨一條線,建議先問清楚需求)。
9. **AI context 預算化 / 快取**(§1 標中低優先):narrate ctx 越長越貴,可裁剪 todayLog 條數。
10. **Gemini key 輪替 / 玩家自帶 key**:所有玩家共用一把免費 key,玩家變多會撞每日額度
    (RPD)。日記錯開已解 RPM;RPD 只能靠多 key 或使用者自填 key(worker 端支援讀 header)。

### D. 觀察中(不用動工,但留意回報)

- **房間細看在使用者手機上的顯示**:上次重做後尚未收到使用者的手機實測回報;
  若再出現空白,畫面會彈「⚠️ 畫面發生錯誤(請截圖回報)」toast,錯誤訊息就在截圖裡。
- **日記錯開的實際體感**:四人現在分別在遊戲時間 22/9/14/18 點寫日記(現實間隔數十分鐘),
  若使用者反映「某人整天沒日記」,先查該租客 `diaryHour`/`lastDiaryDay` 與 quotaHold 通知。
- **貓事件頻率**:打破東西/大小便各 3%/小時 + 36h/30h 冷卻,串門子 50% + 12h 冷卻;
  若使用者嫌吵或嫌少,調 `pets.ts` 的機率常數與 `CD` 即可(改後跑 pet-test + balance --update)。

## 六、本批(2026-07-12)改動的檔案地圖(找程式碼從這裡進)

- 日記錯開:`sim/narration.ts`(diaryPass/ensureDiaryHours/佇列)、`sim/tick.ts`(每小時呼叫)、
  `sim/gameState.ts`(diaryHour/lastDiaryDay 欄位)、`sim/persistence.ts`。
- 貓系統 sim:`sim/pets.ts`(全部邏輯)、`types.ts`(Pet)、`sim/tenancy.ts`(adopt_cat 掛鉤)、
  `store.ts`(re-export + 種子補登副作用)。
- 貓渲染:`floor/petAgents.ts`(遊走)、`floor/floorScene.ts`(drawCat + composeFloor pets 參數)、
  `components/FloorMap.vue`、`components/PixelDollhouse.vue`(接線 + 🐈 標籤)。
- 測試:`scripts/pet-test.ts`(28)、`scripts/diary-stagger-test.ts`(10)、
  `scripts/render-cats.ts`(離線出 PNG 目檢貓 sprite)。
