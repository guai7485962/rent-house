# 一樓寵物咖啡廳 — 經營玩法重設計

> 狀態：🎉 **P1／P2／P3／P4a／P4b 全數實作完成**（2026-08-04）——整個重設計結案
> 建立：2026-08-04 ・ 基準 commit `f75dc73`（CAFE-01～22 已全數上線）
> 需求來源：使用者實玩後的四點要求（見 §2）；四個待決問題已於 2026-08-04 拍板（見 §0）

## P1 完成狀態（2026-08-04）

| 項目 | 結果 |
|---|---|
| 菜單配方 + 基礎熱門度 | ✅ `cafe.ts` 的 `CAFE_BASE_MENU_ITEMS` / `CAFE_RESEARCH_RECIPES`，13 個品項毛利 48.7～57.9%（見 §4.1 實測表） |
| 原料單價重訂 | ✅ 六種全部重訂（低單價 × 多單位），舊值是「每客攤提價」不是「一份的料錢」 |
| 顧客選品（零 RNG） | ✅ `chooseCafeMenuItem()`，FNV-1a 決定性雜湊，權重 = 熱門度 × 客群 × 天氣 × 時段 |
| 逐位結帳 | ✅ `tick.ts` 的 `cafeHourlyPass(hour)`，掛在 `cafeGuestPass()` 正後方 |
| 銷售紀錄 | ✅ `CafeState.sales`（逐日逐品項 `sold` / `missed`），cap 14 天 |
| 存檔升級 | ✅ `SAVE_VERSION 8 → 9` + `MIGRATIONS[8]`，舊檔補空 `sales`，`standingOrders` / `stock` 原封保留 |
| 驗證 | ✅ `npm test` 89/89、typecheck、build 全綠；**balance 快照零漂移（未用 `--update`）** |
| 新測試 | `scripts/cafe-per-guest-test.ts`（65 條斷言，含離線一致性對打） |
| 實測腳本 | `scripts/cafe-opening-sim.ts`（§4.7 開張期試算）、`scripts/cafe-growth-sim.ts`（P4a：§4.7 四階段成長曲線）。兩支只印數字不斷言，**刻意不列入回歸集** |

**P1 不做的事（照分期）**：不改任何 `.vue`、不改 `src/floor/*`。畫面上的顧客動線與
點餐演出仍是舊的 —— 那是 P2；銷售排行面板是 P3。

---

## P3 完成狀態（2026-08-04）

| 項目 | 結果 |
|---|---|
| 🔴 進貨移到開店前 | ✅ `tick.ts` 的 `cafeRestockPass(hour)`，09:00～20:00 之間、當日尚未進貨時扣款一次；掛在 `hourlyTick()` 的 `cafeGuestPass()` 正前方 |
| 一天只扣一次 | ✅ 旗標 `CafeSalesDay.restocked`（比照 P1 的 `settled`），**進存檔** ⇒ 離線補進度、重讀存檔、同小時重入都不會重扣 |
| 日結縮編 | ✅ `cafeDailyPass()` 只剩「研發結清 + 固定開銷 + 生鮮損耗 + 寫 history」；損耗的對象因此變成**打烊後真正剩下的庫存** |
| 損耗調校 | ✅ `SPOILAGE_FREE_UNITS` 15 → **23**、`SPOILAGE_RATE` 0.1 → **0.9**（推導寫在 `cafe.ts` 的常數註解） |
| 一鍵建議常備量 | ✅ `suggestStandingOrdersFromSales()` + 面板「依上週銷量建議」按鈕；`ceil(7 日單日尖峰 × 1.15)`，無歷史時 fallback 到內建建議值 |
| 銷售排行 | ✅ `cafeSalesRanking()` + `CafePanel.vue` 的 SALES 區塊：過去 7 個遊戲日各品項賣出杯數 + **缺貨次數（紅色徽章）** |
| 驗證 | ✅ `npm test` **91/91**、typecheck、build 全綠；**balance 快照零漂移（未用 `--update`）** |
| 新測試 | `scripts/cafe-p3-economy-test.ts`（57 條斷言，含離線一致性跨午夜對打） |
| UI 實拍 | `artifacts/ui-lab/rent/cafe-p3-panel/`（`npm run ui:shot -- rent` 0 errors，另以容器內 Playwright 實拍排行與建議按鈕） |

**P3 不做的事**：不動 `src/floor/*`（P2 已定案）、不新增招牌分級與員工（P4a／P4b）。

---

## 〇、使用者拍板紀錄（2026-08-04）

| 問題 | 決定 | 對設計的影響 |
|---|---|---|
| 咖啡廳該賺多少？ | **可以慢慢成長取代收租** | 🔴 **推翻原提案的「副業」定位**。§4.7 整節重寫成四階段成長曲線，天花板必須超過淨租金 |
| 失敗要多痛？ | 照提案 —— **滾雪球但不倒店** | 不加倒閉/強制歇業機制。虧損靠聲譽複利自然懲罰 |
| 玩家要盯多勤？ | **保留懶人路線** | 「設一次常備量就放著」必須維持可行，只是低效（見 §4.3.1） |
| 固定座位鎖多死？ | **可以拆，但要看得到顧客入座；以好做為主** | 不做 UI 鎖定。改用「座位 = 內用容量」的自然後果（見 §4.6） |

---

## 一、為什麼要重設計：現況診斷

使用者實玩後的原話是「**他們有點餐嗎 他們有消費嗎 什麼都不知道**」。
追進程式碼後確認，這不是玩家沒看懂，而是**系統真的沒有把這件事做出來**：

| 現況 | 事實 |
|---|---|
| 畫面上的顧客 | 營業時段每小時用固定門檻 `CAFE_ARRIVAL_CHANCE = 55` 決定生不生，**純裝飾** |
| 你賺的錢 | 換日時由 `cafeCrowd(天氣 × 星期 × 招牌 × 產能 × 人氣 × 戶外座位 × 氛圍)` 一次算出 |
| 兩者的關係 | **完全獨立。看到 2 個人或 6 個人，跟今天賺多少一毛錢關係都沒有** |
| 菜單品項 | `CafeMenuItem` 結構已存在，但只被拿去算「平均客單價」，**沒有任何顧客點過任何一項** |
| 原料 | 只有 `perGuest` 平均消耗量，跟品項無關；補貨是「設一次常備量就三個月不用管」 |
| 顧客座位 | 有 6 格 `CAFE_GUEST_PREFERRED_SEATS`，但開張贈品**刻意避開**這六格 ⇒ 顧客站在空地板上 |
| 虧錢 | 幾乎不可能。滿客 26 人時淨利約 +$156/日；缺貨只讓人氣 −4，不直接損失 |

**核心問題**：咖啡廳是一台「換日時吐出一個數字」的黑箱，而畫面上的人是螢幕保護程式。

---

## 二、設計目標（對應使用者的四點要求）

1. **有經營的感覺** — 玩家要看著「哪些產品賣得好」決定補什麼料；**顧客買不到會不高興，不只賺不到錢，還會掉聲譽**
2. **每天開店關店，營業期間畫面上真的看到人點餐、消費**
3. **要有可能虧錢**
4. **咖啡廳座位可以固定**（使用者主動放寬，換取實作單純）

---

## 三、核心轉變

```
現在：  換日 → 一條公式 → 一個營收數字
        （畫面上的顧客與此無關）

改成：  每個營業小時 → 幾位顧客進場 → 各自點一項商品
                                    ├─ 有料 → 收錢、坐下、喝完離開
                                    └─ 沒料 → 不高興、$0、聲譽 −、轉頭就走
        當日營收 = Σ 每位顧客實際點到的商品售價
```

**畫面上發生的事，就是帳本上發生的事。** 這是整個重設計的唯一主張，其餘都是它的推論。

### 為什麼架構上做得到（關鍵技術前提）

`syncToNow()`（`tick.ts:944`）是**逐小時**補進度的 —— 離線 8 小時就跑 8 次 `hourlyTick()`。
所以「每小時結帳」**天生支援離線**，不需要另外寫一套批次對帳，也不會有「線上結果 ≠ 離線結果」的分歧。
這是本設計成立的前提，也是它比「即時逐秒模擬」務實的地方。

---

## 四、系統設計

### 4.1 菜單品項：從裝飾變成真的商品

`CafeMenuItem`（`cafe.ts:831`）已有 `id / name / price / track / level / audience`，
研發（CAFE-16～18）已經會解鎖新品項。**只需要補兩個欄位**：

```ts
interface CafeMenuItem {
  // ...既有欄位不動
  recipe: Partial<Record<CafeIngredientId, number>>;  // 新：一份要用掉哪些料、各幾單位
  baseWeight: number;                                  // 新：基礎熱門度（選品權重）
}
```

### ✅ P1 實作結果：重訂後的原料單價

初版提案的配方（咖啡豆 ×1 等）在舊單價下毛利落差過大（咖啡 56% vs 烘焙 28%）。
實作時改成 **低單價 × 多單位**，用整數粒度換到「13 個品項全部落在 45～60%」：

| 原料 | 舊單價 | **新單價** | 舊常備量 | **新常備量** | 生鮮 |
|---|---|---|---|---|---|
| 咖啡豆 | $15 | **$4** | 20 | **130** | 否 |
| 牛奶 | $12 | **$3** | 15 | **24** | 是 |
| 麵粉 | $8 | **$3** | 10 | **52** | 否 |
| 奶油 | $18 | **$10** | 8 | **24** | 是 |
| 貓罐頭 | $25 | **$6** | 8 | **56** | 否 |
| 寵物鮮食 | $20 | **$5** | 6 | **24** | 是 |

常備量取自「成熟期（產能 40、人氣滿）實測 200 天的單日尖峰消耗再留餘裕」
（尖峰：咖啡豆 124 / 牛奶 12 / 麵粉 49 / 奶油 21 / 貓罐頭 51 / 寵物鮮食 21），
三種生鮮一律 ≤ 24，維持「照建議值玩 → 零缺貨、零損耗」的懶人路線（§4.3.1）。
配套把 `SPOILAGE_FREE_UNITS` 從 6 調到 15 —— 週轉量變大了，免損耗額度得跟著等比放大，
語意仍是「一天用得完的量不會壞」。

**13 個品項的實測毛利**（`scripts/cafe-per-guest-test.ts` 逐項硬斷言）：

| 品項 | 售價 | 配方 | 成本 | 毛利率 |
|---|---|---|---|---|
| 招牌美式咖啡 | $34 | 咖啡豆 ×4 | $16 | 52.9% |
| 每日烘焙小點 | $36 | 麵粉 ×2 + 奶油 ×1 | $16 | 55.6% |
| 基礎寵物小點 | $38 | 貓罐頭 ×3 | $18 | 52.6% |
| 本日配方咖啡 | $36 | 咖啡豆 ×4 | $16 | 55.6% |
| 今日手沖單品 | $42 | 咖啡豆 ×5 | $20 | 52.4% |
| 經典拉花拿鐵 | $40 | 咖啡豆 ×4 + 牛奶 ×1 | $19 | 52.5% |
| 慢萃冰咖啡 | $39 | 咖啡豆 ×5 | $20 | 48.7% |
| 每日磅蛋糕 | $38 | 麵粉 ×3 + 奶油 ×1 | $19 | 50.0% |
| 奶油司康 | $38 | 麵粉 ×2 + 奶油 ×1 + 牛奶 ×1 | $19 | 50.0% |
| 貓咪造型餅乾 | $37 | 麵粉 ×2 + 奶油 ×1 | $16 | 56.8% |
| 基礎寵物餐 | $38 | 貓罐頭 ×2 + 寵物鮮食 ×1 | $17 | 55.3% |
| 寵物友善點心 | $38 | 貓罐頭 ×1 + 寵物鮮食 ×2 | $16 | 57.9% |
| 寵物生日蛋糕 | $42 | 寵物鮮食 ×3 + 麵粉 ×2 | $21 | 50.0% |

**高價品同時也高成本**，所以「研發完就穩賺」不成立 —— 這正好給研發線一個真正的取捨。

> ✅ **快照零漂移已實測確認**：balance 快照局永遠不會開張咖啡廳
> （`cafeHourlyPass` / `cafeDailyPass` 第一行都是 `if (!cafe.open) return`），
> 全部重訂完 `npx tsx scripts/balance-test.ts` 仍然一致，**沒有動用 `--update`**。

### 4.2 熱門度與顧客選品（零 RNG）

每位顧客用**決定性雜湊**選一項商品，權重 =

```
weight(item) = item.baseWeight
             × 客群修正(guest.intent / audience)
             × 天氣修正      // 冷天熱飲 ↑、熱天冷萃 ↑
             × 時段修正      // 早上咖啡 ↑、下午烘焙 ↑
```

雜湊取自 `顧客 id | 遊戲日 | 小時`，抄 `cafeGuests.ts` 既有的 `cafeGuestHash` 作法，
**不新增任何 `Math.random()`**（多一次亂數就會位移整個模擬序列）。

**玩家從哪知道熱門度？** ✅ **P3 已實作**：咖啡廳面板的「**銷售排行**」區塊
（`cafeSalesRanking()`）列出過去 7 個遊戲日各品項的賣出杯數與缺貨次數，由高到低。
這就是需求 1「視顧客購買的產品熱門度做補貨」的資訊來源 ——
**玩家不是被告知權重，而是從銷售紀錄自己讀出來**，這才是經營感。
缺貨次數用紅色徽章單獨標出（那是玩家正在流掉的錢），下方再補一行本週撲空總人次。

### 4.3 庫存、補貨與缺貨

**補貨時機從「日結後」移到「開店前」**（每日 10:00 前一刻）：

1. 依常備量計算缺口 → **立刻扣款進貨**（錢不夠就只補得起的部分）
2. 開店，帶著這批庫存做一整天生意
3. 20:00 關店 → 扣固定開銷 → 易腐品損耗結算

這個順序讓玩家**先付錢、後賺錢**，是「經營」與「領被動收入」的分野。

**缺貨處理（需求 1 的核心）**：

| 情況 | 結果 |
|---|---|
| 配方所需原料齊全 | 扣料、`money += item.price`、聲譽 **+**、日誌「有人點了拿鐵」 |
| 缺任一項原料 | **$0 收入**、聲譽 **−**、顧客立刻離場、日誌「有人想點拿鐵，牛奶沒了」 |
| 連續缺同一品項 | 該品項的 `baseWeight` 短期下修（客人不再期待）＋額外聲譽懲罰 |

第三條是刻意的：**放著不管會愈來愈糟**，這是「經營壓力」的來源。

#### 4.3.1 懶人路線必須維持可行（拍板 Q3）

使用者要求保留現在「設一次常備量就能放著」的路線，**作為低效但可行的選項**。
設計上這靠三件事保證：

1. **常備量沿用既有機制**（`state.cafe.standingOrders`），不改成「每天手動下單」。
   設一次就會一直照那份清單補貨
2. **`suggestedStandingOrders()` 要跟著菜單走** ✅ **P3 已實作**：面板的
   「**依上週銷量建議**」按鈕（`suggestStandingOrdersFromSales()`）一鍵把常備量草稿設成
   `ceil(過去 7 個遊戲日的單日尖峰需求 × 1.15)`。三個細節：
   - **尖峰而非平均**（週末客流是平日的 1.39 倍，用平均會週週缺貨）
   - **把 `missed` 一起算進需求**（依「實際賣出」補貨會讓缺貨自我實現）
   - **菜單上的原料有「至少兩份」的底線**，剛研發完的新品不會被建議成 0
   沒有可用歷史（剛開張／舊存檔沒有 `sales`）時整份 fallback 回內建建議值，
   **永遠不會算出全 0 或 NaN**
3. **偷懶的代價是「賺得少」，不是「會倒」**：
   - 均勻備貨 → 熱門品偶爾缺、冷門品偶爾爛掉 ⇒ 毛利被兩頭吃掉
   - 估計落在**勤勞玩家的 60～70% 淨利**，仍為正值
   - 但**不足以支撐擴張**（雇人、升招牌都要現金）⇒ 想長成主業就得開始盯

⇒ 懶人玩得下去，但停在「開張期～成長期」；想走到名店期必須真的經營。
這與 §4.7 的成長曲線是同一件事的兩面。

### 4.4 開店 / 關店與一日節奏

| 遊戲時間 | 事件 | 畫面 |
|---|---|---|
| 09:00 | 進貨扣款 ✅ P3 | `cafeRestockPass()`；一天只扣一次（`sales[].restocked` 旗標） |
| **10:00** | **開店** | 招牌亮起、門打開、一樓地板恢復正常亮度 |
| 10:00–20:00 | 每小時 `ceil(當日客流 / 11)` 位顧客進場 | 逐位走進來點餐（見 §4.5） |
| **20:00** | **關店** | 顧客清場、門關、招牌熄、一樓轉暗 |
| 20:00 | 日結（實作掛在午夜換日） | 扣固定開銷 $370、**對打烊後剩餘庫存**算易腐損耗、寫 history、推日誌 |

非營業時段一樓是暗的、空的 —— **玩家一眼分得出「營業中」和「打烊」**，這本身就是需求 2。

### 4.5 看得見的消費（需求 2 的核心）

`GuestAgent` 的 phase 從 `entering → seated → leaving → departed`
擴充成：

```
entering  → ordering → (成功) served → seated → leaving → departed
                     → (缺貨) refused →                   leaving → departed
```

| phase | 畫面演出 |
|---|---|
| `entering` | 從店門走向**吧台**（固定位置） |
| `ordering` | 站在吧台前，頭上泡泡顯示**他要點的商品圖示**（不再是抽象的意圖符號） |
| `served` | **`+$34` 綠色浮字**從頭上升起、金錢數字跳動 |
| `refused` | **紅色 ❌ 泡泡**、聲譽數字閃一下紅色 |
| `seated` | 走到**固定座位**坐下（真的坐在椅子上，不是站空地板） |
| `leaving` | 起身走向店門 |

**意圖泡泡一併重做**（解決使用者「不知道標誌什麼意思」）：
- 點餐時顯示**商品圖示 + 價格**，不是抽象符號
- 「想認養寵物」「想租房」兩種可互動意圖，改成**帶文字標籤的醒目提示**，
  並在咖啡廳面板同步列出（現在只有面板有，畫面上的符號沒人看得懂）

### 4.6 座位：不鎖，但座位本身就是容量（拍板 Q4）

現況的衝突：顧客有 6 格 `CAFE_GUEST_PREFERRED_SEATS`，而開張贈品**刻意避開它們** ⇒
顧客站在空地板上。

**廢除 `CAFE_GUEST_PREFERRED_SEATS` 這張硬編座位表**，改成
**「顧客坐在玩家實際擺的椅子上」** —— 座位不是寫死的座標，而是查 `placements`：

```
內用席次 = 一樓四區裡 `seat` 欄位不為空的家具數量
（`seat` 欄位是 2026-08-03 CAFE-20 renderer 那批已經加好的，直接沿用）
```

顧客動線：**店門 → 吧台（固定 (3,38)）→ 任一空席 → 店門**

這個設計同時解決四件事，而且**不需要任何 UI 鎖定邏輯**（拍板要求「以好做為主」）：

| | |
|---|---|
| 看得到入座 | 顧客坐在真的椅子上，不再站空地板 |
| 可以拆 | 拆了不會壞，只是**席次變少** |
| 拆掉有代價 | 席次不足 → 後到的顧客**只能外帶** |
| 家具有意義 | 買椅子 = 買容量，和既有的氛圍加成（`cozy + style` → 客流）疊成兩條回饋 |

**外帶的代價**（這是「拆掉有後果」的具體化）：
- 外帶客**只點飲料，不點烘焙/寵物餐** ⇒ 客單價明顯較低
- 外帶客**不貢獻氛圍評價**，聲譽只給一半
- 全店零席次 = 純外帶店，能活但收入明顯較差，且畫面上永遠看不到人坐著

⇒ 玩家會自己發現「多擺幾張椅子比較賺」，不需要我們鎖住任何東西。

### 4.7 成長曲線：從副業長成主業（拍板 Q1）

**這一節推翻了初版的「副業」定位。** 使用者要求咖啡廳「可以慢慢成長取代收租」，
所以天花板必須真的超過租金。

#### 對照基準：租金收入

| 項目 | 數字 |
|---|---|
| 四房滿租月租 | 約 $52,000（原型 $10,000～$16,000，均值約 $13,000） |
| 換算日收 | **$1,733/日** |
| 管理費 | `BASE_UPKEEP 250 + 4 房 × 100` = **−$650/日** |
| **淨租金** | **≈ $1,083/日** ← 咖啡廳要追上的靶 |

#### 現行設計的天花板（為什麼追不上）

產能上限 40（`CAFE_BASE_CAPACITY 26 + CAFE_CAPACITY_PER_MACHINE 14`）
× 客單價硬上限 $38（`CAFE_MAX_AVG_TICKET`）= 日營收上限 $1,520
⇒ 扣成本後淨利上限僅約 **$390/日，只有淨租金的 36%**。

**三個天花板都要打開**：

| 天花板 | 現況 | 改成 | P4a 狀態 |
|---|---|---|---|
| 招牌等級 | 單一升級（`CAFE_CROWD_PER_SIGN_LEVEL = 22`，等於只有 Lv1） | **Lv1～Lv4**，逐級加價、逐級加客流 | ✅ 已做（$30,000 / $60,000 / $110,000） |
| 產能 | 26 + 14/台咖啡機 | 改由**席次 + 員工**共同決定（見下） | ✅ 已做（`min(10 + 席次×5, 員工×35)`） |
| 客單價 | 硬上限 $38 | 隨研發階段解鎖到 **$55**，仍保留硬上限防呆 | ⚠️ 上限已放寬到 $55，但**餘裕是空的**——現行菜單標價平均就是 $38，要等第三層研發才用得到（見下方 P4a 實測值） |

#### 新增成長軸：員工（同時是新的虧損管道）

產能不能只靠買機器 —— 那不叫經營。改成：

```
日產能 = min(席次 × 迴轉率, 員工能處理的杯數)
員工每人日薪 $260，可處理 35 杯/日
```

- 客流成長 → 產能不足 → 顧客排隊/流失 → **要雇人**
- 但雇人是**固定成本**，淡季（雨天/平日）照付
- ⇒ **過度擴張會虧錢**，這是第五條虧損管道，也是「經營感」最強的一條

#### 四階段成長曲線

| 階段 | 招牌 | 席次 | 額外員工 | 日客流 | 客單價 | 日營收 | 日淨利 | 佔淨租金 |
|---|---|---|---|---|---|---|---|---|
| **開張期** | Lv1 | 6 | 0 | ~26 | $36 | $936 | **+$98** | 9% |
| **成長期** | Lv2 | 12 | +1 | ~48 | $41 | $1,968 | **+$430** | 40% |
| **成熟期** | Lv3 | 20 | +2 | ~75 | $47 | $3,525 | **+$900** | 83% |
| **名店期** | Lv4 | 32 | +4 | ~110 | $53 | $5,830 | **+$1,620** | **150%** |

> 「額外員工」不含**開張費已包含的第一位店員**（其薪資計入 `CAFE_FIXED_COST = 370`）。
> 吧台後永遠至少有一個人，詳見 §4.9。

⇒ **名店期的咖啡廳淨利是淨租金的 1.5 倍**，真的可以取代收租。
但那需要一路投資招牌、席次、研發與人力，而每一步都會先增加固定成本再回收。

> 上表是**設計目標值**，實作時要用模擬腳本跑過確認，不是拍腦袋。
> 每個階段之間刻意留長 ——「慢慢成長」是需求，不是缺點。

#### 虧錢的五條管道（需求 3）

| 管道 | 機制 |
|---|---|
| **固定開銷** | $370/日 + 員工薪資，不管有沒有客人都要付 |
| **先進貨後賣** | 進貨在開店前扣款；客人沒來就是純支出 ✅ **P3 已實作**（`cafeRestockPass`） |
| **易腐損耗** | 牛奶／奶油／寵物鮮食過夜就壞（沿用 `applySpoilage`）；**備太多反而虧** ✅ **P3 已調校成立** |
| **缺貨的複利** | 缺貨 → 聲譽↓ → 隔日客流↓ → 更難補貨。**放著不管會滾雪球** |
| **過度擴張** 🆕 | 雇了 4 個人卻只有成長期的客流 ⇒ 薪資吃掉全部毛利 |

**開張期試算**（招牌 Lv1、日客流 26、平均毛利率 50%）—— 這是**設計目標值**：

| 情境 | 營收 | 進貨 | 固定 | 損耗 | 淨利 |
|---|---|---|---|---|---|
| 補貨精準、零缺貨 | $936 | −$468 | −$370 | $0 | **+$98** |
| 缺貨 20% | $749 | −$468 | −$370 | $0 | **−$89** |
| 備貨過量（易腐多備 50%） | $936 | −$600 | −$370 | −$80 | **−$114** |
| 完全放著不管（庫存見底） | $200 | −$100 | −$370 | $0 | **−$270** |
| **成長期卻雇了 4 人** | $1,968 | −$984 | −$1,410 | $0 | **−$426**（P4b） |

#### ✅ P1 實測值（`npx tsx scripts/cafe-opening-sim.ts`，112 個遊戲日平均）

實測跑的是真的模擬迴圈（逐小時 `cafeHourlyPass` + 換日 `cafeDailyPass`），
招牌 Lv1、零投資。**沒有為了對上設計值調任何參數。**

| 情境 | 日客流 | 撲空率 | 營收 | 進貨 | 損耗 | **實測淨利** | 設計值 |
|---|---|---|---|---|---|---|---|
| ① 補貨精準、零缺貨 | 24.5 | 0% | $867 | −$402 | $0 | **+$95** | +$98 ✅ |
| ② 缺貨（咖啡豆只備 30%） | 19.2 | 22% | $687 | −$317 | $0 | **−$0** | −$89 ⚠️ |
| ③ 備貨過量（易腐多備 50%） | 24.5 | 0% | $867 | −$437 | −$36 | **+$59** | −$114 ⚠️ |
| ④ 完全放著不管（常備量歸零） | 0.7 | 97% | $24 | $0 | $0 | **−$346** | −$270 ✅ |

（人氣固定 100 以讓四個情境的客流可比；解除固定後 ② 會因缺貨複利再掉到 −$21、人氣歸零。）

**兩個與設計值的落差是結構性的，不是調參數能補的**：

- **② 缺貨沒有設計值那麼痛**。設計表假設「照常備量付了 −$468 卻還是缺貨」，
  但 P1 的進貨是「補到常備量」＝ 只補實際用掉的量，**少備料同時也少付錢**，
  所以缺貨當天大致損益兩平。真正的懲罰是 §4.8 的聲譽複利：人氣一路掉到 0、客流跟著縮，
  自然模式下才變成 −$21。⇒ **缺貨在 P1 是「賺不到」而不是「倒賠」**。
  若要讓它真的痛，需要 P3 把進貨時機移到開店前（先付整批錢再開門），這是 P3 的範圍。
- **③ 備貨過量還是賺錢**。損耗只有 −$36/日（設計值 −$80），因為 CAFE-12 的
  `SPOILAGE_FREE_UNITS` / `SPOILAGE_RATE` 是刻意設得溫和的（庫存永遠收斂、不會歸零），
  P1 沒有動這個定調。⇒ **「備太多反而虧」目前只成立在「賺得比較少」這個層次**。

⇒ 賺錢的唯一路徑仍然是**盯著銷售排行補貨、並且在對的時機才擴張**，
但「虧錢的五條管道」裡的第 3 條（易腐損耗）與第 4 條（缺貨複利）力道都比設計預期弱。
要不要加碼是使用者的決定，**P1 刻意不擅自調**。

#### ✅ P3 實測值（進貨移到開店前 + 損耗調校後，同一支腳本、同樣 112 個遊戲日）

P3 動了兩件事：**進貨從日結搬到開店前 09:00**（先付錢、後賺錢），
以及 **`SPOILAGE_FREE_UNITS` 15 → 23、`SPOILAGE_RATE` 0.1 → 0.9**。
第二件事之所以能調得動，是因為第一件事改變了損耗的對象：
日結時看到的不再是「剛補滿的常備量」，而是**打烊後真正沒用完的庫存**。

| 情境 | 日客流 | 撲空率 | 營收 | 進貨 | 損耗 | **P3 實測** | P1 實測 | 設計值 |
|---|---|---|---|---|---|---|---|---|
| ① 補貨精準、零缺貨 | 24.5 | 0% | $867 | −$398 | $0 | **+$99** | +$95 | +$98 ✅ |
| ② 缺貨（咖啡豆只備 30%） | 19.2 | 22% | $687 | −$315 | $0 | **+$2** | −$0 | −$89 ⚠️ |
| ③ 備貨過量（易腐多備 50%） | 24.5 | 0% | $867 | −$531 | −$135 | **−$35** | +$59 | −$114 ⚠️ |
| ④ 完全放著不管（常備量歸零） | 0.7 | 97% | $24 | $0 | $0 | **−$346** | −$346 | −$270 ✅ |
| ⑤ 備錯料（咖啡豆 30% + 其餘多備 50%）🆕 | 19.2 | 22% | $687 | −$448 | −$135 | **−$132** | — | （P3 新增的對照組） |

（以上為「靜態」模式：人氣固定 100 讓各情境客流可比。解除固定後 ② 是 **−$18**、⑤ 是 **−$158**，
人氣都掉到 0 —— 那是 §4.8 的缺貨複利在作用。）

**三個結論：**

1. **③「備太多反而虧」成立了**：+$59 → **−$35**。損耗從 −$36 拉到 −$135，
   而且它現在是**穩態成本**（每天壞掉多少，隔天早上就得買回多少）。
   離設計值 −$114 還有距離，但方向與量級都對了 —— 這條虧損管道從「賺得少」變成「真的賠」。
2. **②「只砍一種原料」仍然接近損益兩平，而且這是結構性的，不是參數問題。**
   常備訂單的語意是「補到水位」而不是「每天固定買一批」：少備料 ⇒ 少付錢。
   ② 少賣了 $180 的東西，但同時也少付了 $83 的料錢，剩下的差額大致等於毛利。
   **要讓「缺貨」真的痛，缺的必須是「錢已經花下去了、卻花錯地方」** —— 那就是 ⑤。
3. **⑤ 才是設計表 ②「缺貨 20% ⇒ −$89」真正想描述的形狀**，實測 **−$132**（自然模式 −$158）。
   玩家照樣付了一大筆進貨錢，卻做不出客人要的東西，兩頭一起被吃掉。
   ⇒ 這正是「盯著**銷售排行**補貨」有價值的原因：排行告訴你錢該花在哪一項。

**沒有為了對上設計值硬調參數**：損耗的兩顆旋鈕是由一條不等式夾出來的
（「懶人路線必須零損耗」×「多備 50% 必須虧錢」），推導完整寫在
`src/sim/cafe.ts` 的 `SPOILAGE_FREE_UNITS` 註解裡。

**懶人路線仍然可行（§4.3.1 沒有被打死）**：`CAFE_INGREDIENTS` 的建議常備量三種生鮮都是 24，
而收斂上界 `23 + ceil(1/0.9) − 1 = 24`。進貨只會把庫存補「到」常備量、消耗只會讓它變少
⇒ **夜間剩餘永遠 ≤ 24 ⇒ 損耗恆為 0**。這是數學保證，不是實測巧合
（`scripts/cafe-p3-economy-test.ts` 有直接斷言）。

#### ✅ P4a 實測值：四階段成長曲線（`npx tsx scripts/cafe-growth-sim.ts`）

P4a 打開了上表的三個天花板中的兩個：**招牌 Lv1→Lv4**、
**產能改成 `min(外帶底量 10 + 席次 × 迴轉率 5, 員工數 × 每人 35 杯)`**。
每個階段都真的擺出對應張數的椅子（走 `cafeSeatSpots()`）、真的雇人、真的跑
28 天暖身 + 112 天量測，常備量走「依上週銷量建議」那條懶人路線。

| 階段 | 招牌 | 席次 | 額外員工 | 產能(席/員) | 日客流 | 客單價 | 日營收 | **實測淨利** | 佔淨租金 | 設計值 |
|---|---|---|---|---|---|---|---|---|---|---|
| 開張期 | Lv1 | 6 | 0 | 35 (40/35) | 28.1 | $35.4 | $998 | **+$166** | 15% | +$98 |
| 成長期 | Lv2 | 12 | +1 | 70 (70/70) | 58.4 | $36.1 | $2,107 | **+$501** | 46% | +$430 |
| 成熟期 | Lv3 | 20 | +2 | 105 (110/105) | 90.7 | $36.8 | $3,338 | **+$887** | 82% | +$900 |
| 名店期 | Lv4 | 32 | +4 | 170 (170/175) | 129.9 | $37.4 | $4,855 | **+$1,092** | **101%** | +$1,620 |
| **⚠️ 過度擴張** | Lv2 | 12 | +4 | 70 (70/175) | 58.4 | $36.1 | $2,107 | **−$279** | −26% | −$426 |
| · 名店期 +3 人 | Lv4 | 32 | +3 | 140 (170/140) | 123.2 | $37.4 | $4,607 | **+$1,256** | **116%** | — |
| · 名店期全設備 | Lv4 | 32 | +3 | 170 (170/180) | 135.5 | $37.4 | $5,067 | **+$1,543** | **142%** | — |

（「佔淨租金」對的是 §4.7 的設計靶 **$1,083/日**＝四房滿租 $52,000/月 ÷ 30 − 管理費 $650。
對種子局實際收得到的淨租金 $567/日，四階段依序是 29% / 88% / 156% / 193%。）

**兩條硬性結論都成立**（`scripts/cafe-p4a-growth-test.ts` 有直接斷言）：

- 🔴 **名店期超過 100% 淨租金** —— 照設計表的 +4 人是 101%；把人力調到剛好夠用的
  +3 人是 116%；再把五項永久投資買齊是 142%。**咖啡廳真的可以取代收租。**
- 🔴 **過度擴張會虧** —— 成長期的客流卻雇 4 個人：+$501 → **−$279**，
  多雇的 3 人剛好吃掉 3 × $260 = $780 的毛利。第五條虧損管道成立。

**三個與設計值的落差，誠實記錄（沒有為了對上數字調參數）：**

1. **客單價停在 ~$37，到不了設計表的 $41 / $47 / $53。**
   `CAFE_MAX_AVG_TICKET` 已由 $38 放寬到 **$55**，但那個餘裕現在**是空的**：
   P1 之後營收是逐位顧客照 `item.price` 真的結出來的，而現行菜單
   （3 個基礎品 + 10 個研發品）標價只有 $34~$42，平均就是 $38。
   ⇒ **抬高上限本身不會多賺一毛錢**；要真的漲到 $50 以上，得等
   **第三層研發**（季節限定豆、造型拿鐵、下午茶套餐）進 `content/cafeResearch.ts`。
   P4a 因此完全靠**招牌分級 + 席次/員工產能**達成成長曲線，客單價這條軸留給後續。
2. **名店期只到 101%（設計值 150%）** —— 直接源於第 1 點：客單價少了 $16，
   129.9 位客人就少了約 $2,000 的日營收。補上第三層研發之後這一格會自然往上走。
3. **開張期比設計值好賺（+$166 vs +$98）。** 兩個原因：
   (a) P4a 之前 `CAFE_BASE_CAPACITY = 26` 這個硬產能**同時在當開張期的平衡錨**，
   人氣滿 + 氛圍加成之下基礎客流本來就有 30 人、全被那個 26 夾掉；
   換成新公式後一位店員的 35 杯不再夾住 30 人的客流。
   (b) 設計表用 50% 毛利率估算，實際菜單毛利率是 52~56%。

**🔴 連帶影響（需要使用者決定要不要回頭調）**：第 3 點讓 §4.7 的
「備太多反而虧」從 P3 的 −$35 變回 **+$74**（精準備貨是 +$195，差距仍有 $121）。
損耗的兩顆旋鈕**已經頂到極限、加不動了**：懶人路線必須零損耗這條硬性條件是
`(24 − FREE_UNITS) × RATE < 1`，而 `RATE` 又不能 > 1 ⇒ (23, 0.9) 就是那條不等式的唯一解。
⇒ 這是**量體變大**而不是機制退化：`cafe-p3-economy-test.ts` 加了一條斷言，
把店拆到只剩 3 張椅子（產能被夾到 25）之後，同一份備貨過量照樣是明確的負值。
要不要為了維持「開張期只賺 $98」而回頭調整（例如提高 `CAFE_FIXED_COST`
或降低氛圍加成上限），**P4a 刻意不擅自決定**。

#### 不倒店（拍板 Q2）

刻意**不加**倒閉/強制歇業機制。資金為負時：
- 進貨只補得起買得起的部分（既有 `restockPlan` 已經這樣做）
- 缺貨 → 聲譽掉 → 客流掉，自然縮回小店規模
- 玩家隨時可以資遣員工止血

⇒ 懲罰是「**縮水**」而不是「**歸零**」，符合本作既有的「有代價但不是失敗狀態」基調。

### 4.9 員工與吧台：產能必須看得見（2026-08-04 使用者追問後補）

使用者問：「員工要真的有面板嗎？畫面上看得到他在做什麼嗎？會看到他幫顧客結帳嗎？
結帳在櫃檯嗎？」——**四題的答案都是「要」。**

初版 §4.7 只寫了「員工每人日薪 $260，可處理 35 杯/日」，是一個**純數字、沒有任何畫面表現**。
那等於重蹈本次要修的覆轍：看不見的數字驅動經濟，畫面上演別的事。本節補上。

#### 員工是畫面上的 agent，不是面板上的數字

新增 `src/floor/staffAgents.ts`。這是走過三次的路（`guestAgents` 229 行、
`petAgents` 220 行、`vacuumAgents` 366 行），規模預估 ~200 行。

| 狀態 | 畫面 |
|---|---|
| 待機 | 站在**吧台後方**，偶爾擦杯子/整理 |
| 結帳中 | 面向吧台前的顧客，頭上短暫出現製作中圖示 |
| 忙碌 | 連續動作不停，吧台前開始**排隊** |

#### 結帳動線（固定在吧台）

```
店門 → 吧台前排隊 → 員工結帳(+$34 浮字) → 拿了飲料 → 走到空席坐下 → 離開
                  └─ 缺貨 → ❌ → 直接離開
                  └─ 無空席 → 外帶，結完帳直接離開
```

吧台位置固定 `(3,38)`（開張贈品的吧台就在這裡）。

#### 🔴 排隊 = 產能不足的視覺信號（本節最重要的一條）

**這是整個員工系統存在的理由。**

```
客流成長 → 員工處理不完 → 吧台前排隊變長 → 你看到了 → 雇人
                        └─ 排太久的顧客會放棄離開（$0 + 聲譽 −1）
```

玩家**不需要讀任何數字**就知道該擴張了 —— 看到排隊就懂。
這比面板上的「產能 35/48」好一百倍，也是「經營感」最直接的來源。

反過來，**淡季時看到員工站著發呆**，就是該考慮資遣的信號 ——
過度擴張的懲罰同樣是看得見的。

#### 人力區塊放哪（不開新面板）

咖啡廳面板新增「**人力**」區塊，與既有的「永久投資」並列：

| 顯示 | 內容 |
|---|---|
| 目前人力 | 👤 ×2 ・ 日薪合計 −$520 |
| 今日負荷 | 已處理 48 / 產能 70 杯（帶進度條，滿載時轉紅） |
| 動作 | 「雇用（−$260/日）」「資遣」 |

#### 開張期沒有員工怎麼結帳？（初版的洞）

初版成長曲線寫「開張期 0 員工」，但那樣**沒人能結帳**。修正：

- **開張費 $22,000 已包含第一位店員**，其薪資計入既有的 `CAFE_FIXED_COST = 370`
  （該常數原本代表水電雜支，語意擴充為「水電 + 首位店員」）
- ⇒ 吧台後**永遠至少有一個人**，畫面不會出現空店卻有人結帳的矛盾
- ⇒ 開張期試算維持 **+$98/日 不變**（薪資本來就已經在 $370 裡）
- 第二位起才是額外的 −$260/日

§4.7 成長曲線的「員工」欄改讀為**額外雇用人數**：
開張期 0（含首位）、成長期 +1、成熟期 +2、名店期 +4。

#### ✅ P4b 完成狀態（2026-08-04）

| 項目 | 結果 |
|---|---|
| 員工 agent | ✅ 新增 `src/floor/staffAgents.ts`（約 250 行）。三狀態 `idle` / `serving` / `busy`；站位一律查 `cafeStaffSpots()` |
| 站在吧台**後面** | ✅ `placements.ts` 新增 `cafeStaffSpots()`：`cafeCounterSpots()` 的**另一側**，同一組 lane 展開序 ⇒ 吧台搬到哪、轉幾度，員工就站到那裡的後面 |
| 結帳演出 | ✅ 面向吧台前的顧客、頭上「製作中」杯子泡泡；`busy` 時動作節奏加倍 |
| 待機演出 | ✅ 面向店內、每 4.2 秒拿起杯子擦兩下（累計秒數取模，零 RNG） |
| 🔴 排隊 | ✅ `placements.ts` 新增 `cafeQueueTiles()`；`guestAgents` 的 `entering` 多一段排隊。同時能結帳的人數 = `min(員工數, 點餐位數)`，其餘排進人龍 |
| 人力區塊 | ✅ `CafePanel.vue` 新增「人力」section（👤 ×N・日薪合計、今日負荷進度條滿載轉紅、雇用／資遣） |
| 產能顯示 bug | ✅ 面板的 `cafeCapability()` 補上 `{ seats: cafeSeatSpots().length, extraStaff }`。修前 1 人 15 席顯示 26、雇到 4 人還是 26；修後 4 人顯示 **85**（`min(10 + 15×5, 4×26)`） |
| 驗證 | ✅ `npm test` **93/93**、typecheck、build 全綠；balance 快照**零漂移**（未用 `--update`） |
| 新測試 | `scripts/cafe-p4b-staff-test.ts`（54 條斷言） |
| UI 實拍 | `artifacts/ui-lab/rent/cafe-p4b-staff/`（容器內 Playwright，`timezoneId: Asia/Taipei`）+ `ui:shot` 18 張 0 error |

**本期完全沒有動模擬層**：`cafe.ts` 的產能公式、`tick.ts` 的結帳與薪資一行未改。
排隊畫的是「已經發生的產能吃緊」，不是新增一套模擬；排太久也**沒有**設計初稿寫的
「放棄離開 $0 + 聲譽 −1」——那會改到金流，留給日後拍板。

#### ✅ 排太久放棄離開（2026-08-05 使用者拍板，A 批已實作）

| 項目 | 結果 |
|---|---|
| 觸發條件 | 一小時內「想上門的人 − 產能」**超過 8 位**（`CAFE_ABANDON_QUEUE_TOLERANCE`）才開始有人走 |
| 誰放棄 | `cafe.ts` 的純函式 `cafeAbandonCount(base, capacity, hourIndex)`，每小時上限 3 位 |
| 收入 | **$0** —— 這批人本來就不在 `cafeCrowd()` 的 `guests = min(base, capacity)` 裡，**沒有任何金流算式被改動，也不需要退款** |
| 聲譽 | **−1/人**，走 P1 建立的 `cafeServicePopularity()` 第四個（選填）參數；比缺貨的 −2 輕 |
| 日誌 | 一天最多一則（比照賣出／撲空的稀疏敘事），`cafeOrderLine({ kind: "abandoned" })` |
| 畫面 | 走進門 → 站進 P4b 的人龍（永遠拿不到點餐位）→ 等滿 `GUEST_ABANDON_SECONDS = 18` 現實秒 → 沿用既有的 `leaving` **走回店門**才消失 |
| 存檔 | `CafeGuestOrder.abandoned` / `CafeSalesDay.abandoned` 都是選填欄位 + `sanitizeCafeState` 預設值 ⇒ **不必升 `SAVE_VERSION`** |
| 測試 | `scripts/cafe-abandon-test.ts`（58 條斷言） |

**為什麼判定放在模擬層而不是渲染層**（三個理由，寫在 `cafeHourlyPass()` 的註解裡）：
① $0 必須是「從沒收過」而不是「收了再退」，否則收支頁會出現玩家沒做過的交易；
② 渲染層走現實秒數，玩家沒開 1F 頁面就不會演，懲罰掛在那裡會讓「有沒有在看」改變帳本，
P1／P3 的離線一致性當場破功；③ 零 RNG（純累積差分）。

**為什麼要有 8 人的容忍線**：`base > capacity` 是一間經營良好的店的**常態**
（客流公式的 `min` 就是為此存在），天天罰它等於把 §4.7 整條成長曲線打掉，也會讓稀疏敘事
天天推同一則。有隊伍不等於有人走掉，**隊伍站不下才有人走掉** —— 8 取自
`placements.CAFE_QUEUE_MAX_DEPTH = 14` 的一半多一點。

**實測**（聲譽 100、開張贈品的席次，連跑 7 個營業日；`scripts/cafe-abandon-test.ts` 末段自動印出）：

| 設定 | 產能／日 | 一週成功 | 一週放棄 | 一週營收 |
|---|---|---|---|---|
| 招牌 Lv2・員工 1 位 | 26 | 91 | **0** | $3,094 |
| 招牌 Lv3・員工 1 位 | 26 | 91 | **0** | $3,094 |
| 招牌 Lv4・員工 1 位 | 26 | 91 | **13** | $3,094 |
| 招牌 Lv4・員工 2 位 | 52 | 183 | **0** | $6,222 |
| 招牌 Lv4・員工 4 位 | 55 | 194 | **0** | $6,596 |

⇒ 唯一會踩到的形狀是「**招牌一直升、店卻沒跟著擴張**」；雇第二位店員就同時
消滅隊伍、營收翻倍 —— 反制手段清楚，而且正是 §4.9 想要的成長迴圈。
這條也順手把下方張力 ② 補起來了：客流雖然仍被產能夾住，但「被夾掉的人」
現在看得見、算得出來，不再無聲蒸發。

⚠️ 既有的六支平衡測試（`cafe-daily` / `cafe-menu` / `cafe-p2-flow` / `cafe-p3-economy` /
`cafe-p4a-growth` / `cafe-per-guest`）**一條斷言都沒有改**，balance 快照零漂移。

#### ⚠️ P4b 實作後發現的兩條張力（未擅自處理）

1. **同時能結帳的人數被吧台寬度夾住**。點餐位由吧台前那一排可走格算出來，
   贈品的 2 格吧台 ⇒ 4 個位子。雇到第 5 位以上的員工不會再增加同時服務人數
   （他仍然站在吧台後、仍然算產能與薪資）。要突破得讓玩家能擺更寬／第二座吧台。
2. **客流本來就被產能夾住**（`cafeCrowd()` 的 `min(base, capacity)`），所以
   「人手不足 ⇒ 客流暴增到排爆」不會發生；隊伍長度大致是
   `每小時到客 − 同時服務人數`。**在席次是瓶頸時**（產能固定）雇人確實會讓隊伍變短；
   在員工是瓶頸時，雇人同時抬高客流，隊伍不會消失而是換成更多營收。
   兩者都合理，但與初稿「排隊長度 = 缺人程度」的直覺不完全相同。

---

### 4.10 地板分區產生機能差異（A 批，2026-08-16 實作）

一樓有四個區域（`cafe_floor` / `cafe_counter` / `cafe_pet` / `cafe_back`），
但在本批之前它們只是**底色不同的地板**：家具擺哪裡都只算同一份氛圍加成
（`cafeAmbiancePoints()` 的 `cozy + style`），玩家沒有任何理由把冷藏櫃放進後場。
本批給四個區域各自的機能，並且只用一條規則：

> **家具擺在對的區才有機能效果；擺錯區只剩氛圍（`cozy + style`）。**

**擺錯區不倒扣、不是失敗狀態** —— 氛圍照算，只是少拿一份機能。
`cafeAmbiancePoints()` 因此**一行未改**（改了客流乘數就會漂移）；
機能一律走 `placements.ts` 尾段新增的、**區域受限**的查詢函式。

| 區域 | 查詢函式 | 機能 |
|---|---|---|
| `cafe_back` 後場 | `cafeBackStoragePoints()` | 庫存容量上限 = `min(4000, 360 + 點數 × 40)` |
| `cafe_pet` 寵物區 | `cafePetComfortPoints()` | 寵物停留更久 + 認養詢問更容易出現 |
| `cafe_counter` 吧台區 | `cafeCounterSpan()` / `cafeCounterTechPoints()` | 同時服務人數 = `1 + 吧台寬度 + floor(tech / 3)` |

#### 三條機能的推導

- **後場選「容量」而不是「降損耗」**。降損耗會撞上既有投資項「大型冷藏」，
  而且 `SPOILAGE_FREE_UNITS = 23` / `SPOILAGE_RATE = 0.9` 是不等式 `(24 − F) × R < 1`
  的唯一解 —— 任何額外的免損耗來源都會把 §4.7 的「備太多反而虧」推回正值。
  底量 360 高於建議常備量總和 310 ⇒ **開張期玩家永遠碰不到上限**，
  補貨行為與本批之前逐字相同；名店期常備量約 900，需要「貨架 ×2 + 木箱 ×1」（$13,600）。
- **寵物區兌現了兩句一直沒接線的投資項說明**：貓跳台與軟墊的「寵物停留更久、
  認養詢問更容易出現」、店面招牌的「租屋詢問更容易出現」。`intent` 不參與結帳
  ⇒ **營收零影響**，改變的只有面板上會出現哪一種詢問卡。
  判準用 `category === "ambiance"` 而不是白名單：桌椅是 `seating`，
  玩家不能把桌椅塞進寵物區刷分。
- **吧台的「+1」是刻意的**：吧台後方的收銀／出餐口一定存在，所以「拆光吧台」
  退化成 1 個服務位而不是 0（同 `CAFE_TAKEAWAY_CAPACITY` 的哲學）。
  tech 這一項兌現了濃縮咖啡機的 `tech 3`；冷藏櫃的 `tech 2` 擺在後場不生效 ——
  **那正是分區規則本身**。必須區域受限，否則玩家可以在主廳拉一條 14 格的吧台。

#### 這一批解掉了 §4.9 的張力 ①

上面「P4b 實作後發現的兩條張力」第 1 條（同時能結帳的人數被吧台寬度夾住、
雇第 5 位以上的員工不會再增加同時服務人數）在本批得到正式答案：
**服務位就是產能公式的第三條腿**，`cafeCapability()` 用
`min(staffCount, stations)` 算出真的做得出杯子的人數，面板用 `idleStaff`
明說「有 N 位沒有吧台位置，加寬吧台才能讓他們上工」。
`cafeStaffSpots()` / `cafeCounterSpots()` / `cafeQueueTiles()` 的 lane 展開序
改讀**整個連通塊**，所以第二座吧台一接上去，員工站位與隊伍就自動變寬，
沒有第二套幾何。

⚠️ **這是唯一會咬既有存檔的改動**（使用者已核可）：雇 4 人以上、吧台仍是贈品那座的
存檔，產能 `4 × 26 = 104 → 3 × 26 = 78`。收斂性有既有論證背書，不會死亡螺旋，
而且面板會直接告訴玩家該做什麼。

#### 刻意不接：家具的 `tier` 屬性

咖啡廳的 13 件家具**每件功能唯一**，沒有「同功能不同 tier」的組
（不像三樓的沙發/電視/浴缸/書桌各有 budget/standard/premium 三檔）。
把 `tier` 接進咖啡廳的機能只會變成**雙重計價** —— 同一件家具的價格已經
反映在它的屬性點數上，再按 tier 乘一次沒有任何設計意義。
**本結論不留待辦**，日後不必再評估一次。

---

### 4.11 常客系統（B 批，2026-08-16 實作）

> **「有人記得你，也有人被你記得。」**

在本批之前，咖啡廳的每一位顧客都是**一小時的臨時體**：`CafeGuest.id` 由
`(seed, sequence, arrivedMs)` 雜湊而來，天生每次不同；下一個整點他就從陣列裡消失，
玩家永遠不可能對任何一位客人產生印象。本批讓其中 **6 位**變成跨日認得出來的人。

#### 為什麼身分鍵是「姓名」而不是 id

`CafeGuest.id` 含 `arrivedMs`，**設計上就不可能穩定**。唯一穩定的識別是
`content/cafeGuestNames.ts` 的姓名池 —— 而且這個專案早就採用了
「**同名 = 同一個人**」的語意：`cafeGuests.ts` 的 `CAFE_GUEST_GENDERS` 註解寫著
「只能 append，改動既有對應會讓舊存檔裡的同名應徵者換性別」，也就是說姓名
在這個系統裡**已經是身分**了，本批只是把這條既有語意用出來。

代價是「同名撞人」：兩位不同的臨時顧客抽到同一個姓名時會被算成同一個人。
這正是本批把姓名池從 32 擴到 **64** 的理由（純 append，前 32 筆逐字未動）——
單一姓名的日到訪期望從 0.8 降到 0.4 次，撞人率減半，同時 6 位常客佔比從 19% 降到 ~10%，
名店期（一小時 12 位客人）才不會變成「來來去去都這幾個人」。

#### 為什麼回訪節奏「不乘客流」

直覺的寫法是「每位顧客有 X% 機率是常客」，但那會被客流放大：
名店期 130 客/日 × 12% ≈ **一天來 15 次**，常客立刻從熟面孔退化成背景板。

改成與客流完全脫鉤的**每小時抽籤**：

```
每個營業小時 18% 機率有一位常客上門 → 11 小時 ≈ 2.0 位/日（開張期與名店期一致）
⇒ 6 位常客各約 3 天回訪一次
```

3 天這個數字不是湊出來的，它與**好感寬限 `CAFE_REGULAR_GRACE_DAYS = 3` 對齊**：
正常經營時常客永遠落在寬限內 ⇒ **好好開店的人不會掉好感**；
只有真的怠慢（關店、天天缺貨、離線太久）才開始每日 −1。這條對齊讓好感變成
「你有沒有在照顧這間店」的量尺，而不是一個會自然衰減的體力條。

抽籤只依賴 `(day, hourIndex, 排序後的名冊)`，零 `Math.random()`
⇒ **線上逐時跑 11 小時 = 離線 `syncToNow()` 一次補 11 小時**，
論證與 `cafeHourlyGuestCount()` 完全相同。名冊固定以 `(sinceDay, name)` 排序，
所以「今天輪到誰」與陣列插入序、存檔往返、升格順序都無關。

#### 「帶朋友」為什麼改成救回一位放棄客

原案是「高好感常客有 35% 機率多帶一位顧客進來」。**那會打破兩條已經咬合的不變式**：

1. `crowd.guests = min(base, capacity)` —— P4a 建立的產能上限語意；
2. 「當日營收 = Σ 每位顧客實際賣出」—— 整個 P1 重設計的唯一主張。

多生一位客人等於在產能上限之外憑空開一條金流管道，兩條同時破功。
改成 **該小時 `abandonCount −= 1`**（把本來會排到放棄的一位留下來）之後：

- 零產能突破（`crowd.guests` 一位都沒動）、零新金流管道（那個人本來就不在營收裡）；
- 玩家**看得見**：人龍裡少了一個轉身離開的背影；
- 只在「已經有人排到放棄」時才生效 ⇒ 生意還沒好到會漏客人的店感覺不到，
  這反而是正確的：帶朋友的價值本來就該在店最忙的時候才顯現。

#### 三個機能效果與平衡上界

| 效果 | 條件 | 內容 | 平衡上界 |
|---|---|---|---|
| ① 老樣子 | 常客到店 | 直接點 `itemCounts` 最高的那一項；沒有紀錄時改用他的 `taste` 當客群修正 | 客單價位移 < $1/客 ≈ **$0** |
| ② 小費 | `affection >= 60` | 結帳 `revenue += $3` | 2 位/日 × $3 = **+$6/日**（開張期淨利 +$109 的 5.5%） |
| ③ 帶朋友 | `affection >= 80` 且該小時抽中 35% | 該小時 `abandonCount −= 1` + 一則敘事 | 聲譽最多 +0.7/日，且只在已經有人放棄時 |

**口味 `taste` 綁姓名不綁日期**：`雜湊(cafe-regular-taste|姓名) % 3`，
所以同一個人在任何存檔、任何一天都同一個口味。這順手解掉舊設計的一個彆扭 ——
`chooseCafeMenuItem()` 的口味偏好本來是「每位顧客當場雜湊出來的」，
不存在任何人身上，玩家永遠感覺不到「她就是喜歡甜的」。有了固定口味之後，
**針對常客備料**才成為一個玩家講得出口的行為（「她天天點拉花拿鐵，牛奶不能斷」）。

`chooseCafeMenuItem()` 新增的 `preferTrack?` / `forceItemId?` **兩個參數都是選填，
省略 = 逐項等於今日輸出**，既有呼叫端與 `cafe-menu-test` / `cafe-per-guest-test` 零改動。

#### 升格、衰退與流失

```
成功被服務 → 不是常客：candidates[姓名]++（一天最多一次）
             累到 3 且名額未滿 → 升格，推一則日誌
           → 已是常客：visits++、好感 +4、itemCounts[品項]++（一天最多一次）
缺料撲空   → 好感 −6（不算來訪日，不推遲衰退計時）
日結       → 超過寬限 3 日才每日 −1
流失       → 好感 <= 0 **且** 14 日未來（兩個條件都要）
```

- **「一天最多一次」是必要的**：名店期一天 130 位客人，沒有這道閘門的話同一個姓名
  一天就能累積好幾次，3 次的門檻形同虛設。常客靠自己的 `lastVisitDay` 擋；
  候選人沒有那個欄位，所以存檔多了一個 `regularCandidateDays`（姓名 → 上次計數的遊戲日）。
  **它必須進存檔**：若改用模組層的暫存 Set，「線上逐時 vs 離線一次補」就會分岔，
  那是本專案最在意的離線一致性破口。
- **候選人每 7 個遊戲日全體 −1、歸零者移除**：3 次必須在**滾動窗**內累積，
  不會出現「兩個月前來過一次」的殭屍候選人，常客因此永遠代表「最近真的常來」。
- **刻意不因「撲空太多次」直接流失**。那會把一次缺貨變成永久損失，
  也就是整份設計一直在避免的失敗狀態；缺貨的懲罰留在聲譽那條路（−2/人）就夠了。

#### 存檔與安全底線

- `CafeState` append 三個欄位（`regulars` / `regularCandidates` / `regularCandidateDays`），
  **`SAVE_VERSION` 維持 10**（additive + 舊檔補安全值，先例是 `sideArc` / `arcHistory`）。
- **刻意不回填舊存檔**，理由同 `MIGRATIONS[8]`：回填假資料只會讓面板顯示玩家
  從未發生過的事。約 4~5 個遊戲日就會出現第一位常客。
- 🔴 **安全底線**：常客**不進 `state.runtimes`**、**不建 `relationships`**、
  **不參與戀愛線／同居／未成年判定**。他們只是 `state.cafe` 底下的一筆觀察資料，
  整個社交/戀愛子系統的入口（`runtimes` / `relationships`）一個都碰不到 ⇒
  既有硬規則不需要、也無從被繞過。常客要走進租客的人生仍然只有一條既有的路：
  以 `intent === "rent"` 的顧客身分被帶看、經 `recruit.ts` 成為應徵者、再入住 ——
  那條路上的所有把關一行未改。這段話也寫在 `sim/cafe.ts` 第 16 節的檔頭註解裡。

#### 面板：常客是觀察物，不是決策卡

第 10 個可收合區塊，**沒有任何按鈕**。每列純文字（同認養卡／租屋卡的作法，
不做頭像元件）：`☕ 方雨晴 · 來訪 12 次 · 老樣子「經典拉花拿鐵」 · [好感條 72] · 3 天前來過`，
好感 >= 80 前綴換成 🌟，14 天沒來顯示「快要失聯了」。
認養卡／租屋卡上若該顧客是常客，`<small>` 多一句「（常客・來訪 N 次）」——
**不改任何星等／開價算式**。

不加決策的理由：咖啡廳已經有兩張會過期的詢問卡，再多一張只會讓玩家在掛機遊戲裡
多一件「不處理就虧」的事。常客應該是**你經營出來的結果**，不是又一個待辦。

---

### 4.12 打烊後的租客聚會（CAFE-21 的內容端，2026-08-16 實作）

> **「店關了，燈留一半，樓上的人下來坐。」**

CAFE-21 早就把 `GroupSceneVenue` 的 `"cafe"` 做好了（`groupScene.ts` 的
`cafeTiles()` / `cafeAnchorFor()`），但**全專案零呼叫者**——所有聚會一律
`lounge` / `rooftop`，能力等於沒有。本節補的是內容端。

#### 為什麼是「打烊後」而不是營業時間內

1. **畫面會打架**。`cafeTiles()` 只看 grid 與 `currentBlocked()`，
   完全不知道 `guestAgents` 的座位／吧台／人龍格與 `staffAgents` 的站位在哪。
   營業時間內開場，3～4 位租客極可能站進排隊隊伍或客人已入座的位置上。
   要修就得讓 `groupScene` 認得顧客佔用表——那是一套新的耦合，而且修好了畫面
   仍然是「自己人在自家店裡插隊」。反過來，`cafeBusinessOpen()` 為 false 時
   顧客與店員 agent 都已清場（`FloorMap.vue` 的 `staffAgentSignature`、
   `tick.ts:601` 的離場），整層地板是空的，`cafeTiles()` 的既有邏輯直接就對。
2. **白天的畫面已經有了**。CAFE-20（`routine.ts` 的 `at_cafe`）本來就會讓租客
   各自下樓坐咖啡廳。缺的是**群體**的畫面；「打烊後自己人包場」和白天的散客感
   剛好互補，是一個新畫面而不是同一個畫面加人數。
3. 時段取 `CAFE_CLOSE_HOUR + 1 = 21:00`（含尾的營業時段之後第一個整點）。
   七種作息原型有五種此時仍在樓裡，`scheduledCommunityPass()` 本來就會把答應
   參加的人拉回來入鏡。

#### 為什麼不併進 `COMMUNITY_EVENTS`

`communityPass()` 是 `eligible[Math.floor(rng() * eligible.length)]`，
把兩件咖啡廳事件塞進同一個陣列，會讓既有 11 件的相對機率從 1/N 掉到 1/(N+2)
——**那就動到 lounge / rooftop 的平衡了**。改成獨立池 `CAFE_COMMUNITY_EVENTS`，
只在**當天沒有任何其他社群／群體事件發生時**才補抽一次（`rollCafeGathering()`）：
既有事件的絕對與相對觸發率**完全不變**，咖啡廳只吃原本空著的日子。

| 事件 | 條件 | 時段 | layout（錨點） | 效果 |
|---|---|---|---|---|
| `cafe_afterhours` | 平日 · 冷卻 3 日 | 21:00 | `table`（吧台前） | 兩兩好感 +2、心情 +4、壓力 −5 |
| `cafe_weekend_night` | 週末 · 冷卻 3 日 | 21:00 | `cluster`（座位區中央） | 兩兩好感 +3、心情 +5、壓力 −6 |

補抽機率 `CAFE_GATHER_CHANCE = 0.3`。兩件依 `isWeekend()` 互斥，所以任一天
最多只有一件可選，節奏約每 8～10 個遊戲日一場。

#### 零漂移論證

`rollCafeGathering()` 第一行是 `if (!state.cafe.open) return false;`——未開張
**連一次 `rng()` 都不抽**，呼叫端後續的亂數序列與本批之前位元級相同。
無頭的 balance 快照局永遠不會開張咖啡廳 ⇒ 快照零漂移（實測一致）。
文案選句沿用 `sceneIndex()` 的既有決定性雜湊，開演過程零 `Math.random()`。

#### 畫面：橫幅要跟著樓層走

`FloorMap.vue` 原本的現場橫幅寫死 `v-if="eventScene && isTenantFloor"`。
`cafe` 場地的走位在一樓，掛在三樓會變成「橫幅寫著現場、畫面上一個人也沒有」。
改成 `sceneOnThisFloor`：`cafe` 只在一樓頁面顯示，`lounge` / `rooftop` 維持只在
三樓顯示，前綴 `☕ 店裡`，並壓在 1F viewport 最上緣避免蓋住 41～44 列的演出。

---

### 4.13 讓 AI 看見咖啡廳（D 批，2026-08-16 實作）

> **「AI 只能讀咖啡廳，一個字都寫不回去。」**

在本批之前，`NarrateCtx`（`narrate.ts`）、worker 的鏡像介面與 `buildPrompt()`
**三處零咖啡廳欄位**。唯一接點是硬編模板日誌（`cafeDailyLine()` / `cafeOrderLine()` /
`cafeRegularLine()` / `cafeRegularFriendLine()`），而 `pushCafeLog()` 把它們全掛在
「id 排序第一位」的在住租客身上 ⇒ **只有一位租客的 AI 看得到樓下發生什麼事**。

#### 四個拍板決定

1. **咖啡廳 context 送給全體在住租客**（不是每日輪一位）。咖啡廳在所有人樓下，是共享環境；
   現行掛第一位的作法本來就脆弱（那位退租，日誌就整批跳人）。若實玩後發現蓋台，
   降級開關是改成雜湊輪一位（單行改動），本批不做。
2. **接受常客姓名收緊**（NFKC → 字元白名單 → 夾 12 字）。這是**補既有漏洞**，不是新規則。
3. **「去換行」本批只做新欄位**，不擴及 `clampStr` 的所有字串欄位——那會改變既有 prompt
   內容、可能影響既有敘事品質測試。全域缺口另立 `docs/待辦.md` 一條。
4. **AI 只能讀咖啡廳，零寫入面。**

#### 資料模型：送「已組好的短句」而不是原始數值

`NarrateCafeCtx`（`narrate.ts`）：`brief`（≤48 字）、`trend`（`up`/`flat`/`down`）、
`regulars`（≤2 條、每條 ≤28 字）、`ops?`、`pets?`（各 ≤28 字）。`NarrateCtx.cafe?:`
是 additive，舊 ctx 缺省 = 不進 prompt（同 `weather` / `wish` 慣例）。

為什麼不送原始數值：測試與領域知識留在 app 端（`cafe.ts` 已有全套純函式與 12 支測試）；
離線 fallback 共用同一份素材；worker 的消毒面因此收斂成「夾長 + 去換行 + enum 白名單」，
不需要理解咖啡廳語意。**最大 payload 約 200 字**，距 `MAX_BODY`（16KB）餘裕極大。

素材來源全部零新狀態、零亂數（`cafe.ts` §17，選句一律走既有的 `lineHash()`）：
`brief` 取 `sales` 最後一筆（空手離開 ≥4 → 忙碌 → 沒客人，三分支）、`trend` 取 `history`
末筆對前三筆平均（±15%）、`regulars` 取 `sortedCafeRegulars()`（今天來過的 → 快失聯的）、
`ops` 取研發中 → 後場塞滿 → 吧台太窄、`pets` 取三個遊戲日內的咖啡廳顧客認養。
`cafe.ts` **仍然不 import `placements`**：後場容量與吧台位由 `narration.ts` 算好當參數餵進去。

#### 🔴 消毒三層（缺一不可）

| 層 | 位置 | 做什麼 |
|---|---|---|
| 一、存檔載入 | `gameState.ts` 的 `sanitizeCafeRegularName()` | NFKC → 字元白名單 → 夾 `CAFE_REGULAR_NAME_MAX = 12`；清乾淨後為空 ⇒ 整筆丟掉 |
| 二、app 端組 ctx | `narration.ts` 的 `buildCafeNarrateCtx()` | 每欄過一次 `sanitizeContextLine()`；`brief` 為空 ⇒ 整個 `cafe` 不送 |
| 三、worker 端 | `worker/index.ts` 的 `clampCafeCtx()` | 同一份 `sanitizeContextLine()` 再夾一次 + `trend` enum 白名單 |

新的 `sanitizeContextLine()`（`narrativeQuality.ts`，**app 與 worker 共用一份實作**）：
NFKC → 去 `\p{C}` → 空白（含換行）壓成單一空格 → 去 `[ ] 【 】` → 去行首 `- # >` → 夾長。

**去換行是最重要的一項**：`buildPrompt()` 用 `\n` 串行、`[...]` 當區塊標記、`  - ` 當條列符，
一個帶換行的字串足以偽造 `[今日唯一主線…]` 這樣的假指令行。既有的 `clampStr()`
只做 `.slice()`，擋不住。刻意**不**沿用 `sanitizeReasonText()`——它會做逗號子句語意去重，
對單句是破壞性的。

第三層為什麼不可省略（「不信任前端」有真實繞道）：`state.pendingDiaries[].ctx` 是**整份**
存進 localStorage 的 ctx，`persistence.ts` 載入時完全不消毒，`resumeDeferredDiaries()`
之後原封 POST 回 `/api/narrate`。

#### 🔴 安全底線

- **本批新增零個寫入面**：`NarrateResult` 不新增欄位、`applyDiaryEffects()` 一行未改、
  AI 輸出的 JSON schema 不新增 key、`narration.ts` 只讀 `state.cafe` 永不寫。
  四項都有原始碼掃描斷言釘住（`cafe-narrate-ctx-test.ts` 第九節）。
  論證同 `sideArc` / `wish` 的既有取捨：**進度由本地決定，AI 只當動機素材**；
  讓 AI 回寫庫存／聲譽／好感只會得到「AI 宣布好感上升、隔天算式扣回去」的自相矛盾。
- **新增硬不變式「熟客名字 ∩ 租客名字 = ∅」**：組 `regulars` 時濾掉任何與現任租客同名的常客。
  動機一，常客可循 `intent === "rent"` 真的搬進來（那時他就**是**租客，該出現在 `neighbors`）；
  動機二，特邀租客讓玩家自訂姓名，可能與常客池撞名。效果是 `applyRelNudge` 的
  「名字出現在 `todayLog`」條件永遠無法被 `ctx.cafe` 這條路滿足。
- **未成年**：`CafeRegular` 與 `NarrateCafeCtx` 都沒有年齡／性別／取向欄位，
  既有「未成年排除戀愛線」規則無從被咖啡廳這條路繞過（掃碼斷言釘住）。

#### 四道防線：為什麼不會蓋台

1. **`focus` 一行未改**——新 `ctx.cafe` 不參與主線計算，咖啡廳只有在日誌真的落進那位
   租客的 `todayLog` 時才可能成為主線，與本批之前完全相同。
2. 咖啡廳四行放在 `[背景資料—只供理解，除非今天片段有新進展，不可直接寫成今日事件]`
   區塊內，繼承一條已經在運作的守則（`relationships` / `neighbors` 就靠它沒有蓋台）。
3. SYSTEM 新增兩條 bullet：條一限制成「一個短句為限的背景印象」且不得開咖啡廳劇情弧；
   條二寫明「熟客不是租客」，名字不得填進 `arcUpdate.with` / `event.with` / `observation.rel.name`。
4. 12 類主題清單不動，`pastArcThemes` 去重不受影響。

#### provider：一行都不改

`narrateProviderOrder()` 與 `chooseGeminiModel()` **不納入咖啡廳素材**。咖啡廳是背景不是連載；
把背景素材當成升級 Gemini 的理由，會在平淡日子燒掉免費層額度。真正重要的咖啡廳事件本來
就會以日誌進 `todayLog`，由 `focusNotable` 撿。`worker-test.ts` 有反向斷言把這個決定釘死。

#### 離線與零漂移

既有模板日誌完全不動（它已是離線主通道）。`templateDiary()` 新增 `DAILY_CAFE_TEMPLATES`，
只在 `ctx.cafe` 存在**且**（有熟客素材或 `trend !== "flat"`）時混入，位置在天氣句之前
（維持「天氣句在池尾」的既有測試假設）。

🔴 `templateDiary()` 只抽一次亂數，改變 pool 長度就會改變抽樣結果——但 balance 快照局
**永遠不開張** ⇒ `ctx.cafe` 全程 `undefined` ⇒ pool 不變 ⇒ 抽樣序列位元相同
（`cafe-narrate-ctx-test.ts` 第十一節有明文斷言，實測快照零漂移）。
`SAVE_VERSION` 維持 10：無新持久化欄位，唯一持久化面變化是姓名消毒的**收緊**（載入時冪等）。

---

### 4.8 聲譽（`popularity`）重新定義

現況：`nextCafePopularity(current, { shortages, underfunded })`，換日時 +2 / −4 / −1。
改成**逐位顧客即時累積**：

| 事件 | Δ |
|---|---|
| 成功服務一位顧客 | **+0.15** |
| 缺貨導致顧客離開 | **−2.0** |
| 座位滿導致外帶 | 0（不罰，這是好事） |

夾在 0～100，仍然只影響客流 ±25%（`CAFE_POPULARITY_SWING = 0.25` 不動）。
這樣「一天服務 26 人（+3.9）」大致抵銷「兩次缺貨（−4）」—— **偶爾缺貨可以接受，
持續缺貨會崩**，符合設計檢討「有代價但不是失敗狀態」的既有基調。

---

## 五、技術約束（實作時不可違反）

1. 🔴 **零 RNG** — 顧客到店、選品、座位分配全部用決定性雜湊。多一次 `Math.random()`
   就會位移整個模擬序列、讓 balance 快照全盤漂移
2. 🔴 **`cafe.open` 閘門保留** — `cafeDailyPass` / 新的 `cafeHourlyPass` 第一行都必須是
   `if (!state.cafe.open) return;`。這是快照零漂移的天然保證
3. 🔴 **`cafe.ts` 的純函式界線** — 該檔不 import `state`、不呼叫 `addMoney`、不推日誌。
   算式放 `cafe.ts`、副作用放 `tick.ts`。`cafe-supply-test.ts` 有硬斷言掃描原始碼守這條線
4. **離線一致性** — 每小時 pass 必須讓「線上跑 11 小時」與「離線回來補 11 小時」結果完全相同
5. **存檔升級** — 新增 `state.cafe.sales`（銷售紀錄）、品項庫存語意變更 ⇒ 需要
   `SAVE_VERSION 8 → 9` 與對應 migration。舊檔的 `standingOrders` / `stock` 要能平順轉換

---

## 六、分期實作建議

| 期 | 內容 | 可獨立上線？ |
|---|---|---|
| ~~**P1**~~ ✅ | 菜單配方 + 顧客選品 + 逐位結帳（模擬層，無新 UI）— **2026-08-04 完成** | 可（行為已改變，但畫面還是舊的） |
| ~~**P2**~~ ✅ | 開店/關店節奏 + 顧客動線與點餐演出 + 坐在真的椅子上 — **2026-08-04 完成** | 可（需求 2、Q4 完成） |
| ~~**P3**~~ ✅ | 銷售排行面板 + 進貨扣款時機 + 損耗調校 + 一鍵建議常備量 — **2026-08-04 完成** | 可（需求 1、Q3 完成） |
| ~~**P4a**~~ ✅ | 聲譽即時化 + 招牌分級 + 員工/產能公式 — **2026-08-04 完成** | 可 |
| ~~**P4b**~~ ✅ | 員工系統（`staffAgents` + 吧台結帳 + 排隊 + 人力區塊）— **2026-08-04 完成** | 可（需求 3、Q1 完成） |

**建議 P1 → P2 → P3 → P4 依序做**，每期各自可驗證、可上線、可回頭調。
P1 做完就能用測試證明「畫面上的人 = 帳本上的錢」，那是整個重設計的地基。

⚠️ **P4 已拆成 P4a / P4b**。Q1 的拍板把 P4 從「調幾個數字」變成「新增兩套系統」，
而 2026-08-04 追問後 §4.9 又把員工從純數字變成畫面上的 agent（`staffAgents`），
規模再增。拆開後每半都能獨立驗證上線。

**P2 與 P4b 有共用的地基**：兩者都要動 `guestAgents` 的動線與 phase。
建議 P2 先把「店門 → 吧台 → 座位」的骨架做完，P4b 再在吧台端接上員工與排隊，
不要兩期各做一半。

---

## 七、四個問題已拍板

見 §0 的拍板紀錄。四個決定對設計的影響已分別寫進
§4.3.1（懶人路線）、§4.6（座位不鎖）、§4.7（成長曲線與不倒店）。

**還沒決定、留給實作時定案的細節**：

- ~~六種原料的 `unitPrice` 重訂後的確切數字~~ ✅ **P1 已定案，見 §4.1 的對照表**
- 招牌 Lv2～Lv4 的價格與客流增量
- 員工的雇用/資遣 UI 放哪（咖啡廳面板的新分頁 or 投資項旁邊）
- 研發線如何對應四階段（現有 10 項要重新分配到 Lv1～Lv4）

這些都不影響架構，可在 P1～P4 各期實作時邊做邊定。

---

## 八、這份設計刻意不做的事

- **不做即時逐秒模擬**。遊戲是「異步掛機觀察型」，每小時結算一次已經足夠，
  而且與既有的 `hourlyTick` / `syncToNow` 架構天然相容
- **不動三樓租客系統**。咖啡廳與租客的交集只有 CAFE-19（租屋詢問）、CAFE-20（下樓喝咖啡）
  與 CAFE-09（認養），這三條介面保持不變
- **不改五項永久投資的既有數值**。招牌/咖啡機/戶外座位/冷藏/貓跳台的效果沿用，
  只是它們現在作用在一個真的會動的系統上
