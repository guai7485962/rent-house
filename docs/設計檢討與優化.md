# 《房東監視中》設計檢討與優化提案

> 版本:2026-07-10 · 作者:Opus(Fable 週額度已滿,由 Opus 代寫);第 7、8 節由 Fable 補
> 性質:**只設計、不含實作程式碼**。供之後交給 Sonnet/Opus 逐項實作。
> 讀法:每節皆為「現況 → 問題 → 具體優化(含型別草稿/模組名)→ 優先序」。優先序 = 高(高槓桿、先做)/中/低。

### 給實作模型的通用守則(每一項都適用)
1. **驗證方式**:功能寫完先跑無頭測試(`scripts/` 現有 pattern,`npx tsx scripts/xxx-test.ts`),再 `npm run build`;不要只靠瀏覽器目測。回歸:`sim-trace.ts 24` 必須 0 異常。
2. **存檔相容**:任何 state 新欄位,save 直接加、load 一律 `?? 預設值`,舊存檔不能壞。
3. **AI 影響一律三道關卡**:schema 驗證 → store 端夾值 → 玩家抉擇拍板;不開放驅逐。
4. **金錢進出只走 `addMoney()`**(store.ts),分類記帳。
5. 家具/障礙一變要 `placements.version++`(add/remove 內建);租客定位用「對全員 `applyHour(rt, hour, false)`」重算,不要手動搬 agent。

---

## 0. 一句話總評

遊戲的**系統廣度已經很完整**(觀察/經濟/張力/社交/AI 敘事/AI 抉擇/記憶改行為全都有),接下來的高槓桿不在「加更多系統」,而在 **(a) 把 `store.ts` 拆開讓它可維護、(b) 讓數值模型與各系統扣得更緊、(c) 讓 AI 敘事有「跨日的長期劇情弧」而非每天各自為政**。前三名建議:**A. store 模組化 → B. 數值模型重整(含啟用 hygiene/新增 energy)→ C. 劇情弧與記憶生命週期**。

---

## 1. AI 生成文本

### 現況
- `worker/index.ts` 的 `/api/narrate` 一次呼叫 Gemini,回 `{diary, newMemory, event}`。
- context 由 `store.buildNarrateCtx()` 組:當天日誌、性格、記憶、關係、鄰居、房東抉擇。
- 每「遊戲日」換日、且為 live 時觸發(每租客一次);離線/超額 fallback 模板(`narrate.ts` + `content/observationLines.ts`)。

### 問題
1. **每天各自為政**:AI 只看「當天」片段 + 靜態記憶,缺「昨天寫了什麼」的連續感 → 容易同質、劇情不連貫。
2. **context 沒有壓縮/摘要**:租客住越久,memoryTags/關係越多,prompt 越長、成本越高、重點越糊。
3. **額度脆弱**:Gemini 免費層每分鐘/每日上限低,4 租客同時換日 = 瞬間 4 次;快轉跨多日會爆。
4. **無快取/去重**:相似情境重複呼叫。

### 優化設計
- **加「連續性摘要」**:每位租客維護一段滾動 `narrativeSummary`(≤200 字),每次 AI 回傳時**順便更新它**(prompt 要求輸出 `summaryUpdate`),下次把它餵進去。→ 用一個短摘要取代「塞一堆歷史」,又保連貫。
  ```ts
  interface NarrateResult { diary; newMemory; event; summaryUpdate?: string }  // worker 回傳新增
  TenantRuntime.narrativeSummary: string                                      // 取代/補強 tenant.recentSummary
  ```
- **context 預算化**:`buildNarrateCtx` 只帶「最近 N 筆重點日誌 + 最新摘要 + 最多 K 個最相關記憶 + 進行中關係」,其餘丟摘要。
- **佇列/節流**:換日時把多位租客的 narrate 請求**排隊**(間隔 ≥ RPM 限制),而非並發;可在 `store` 加一個簡單的 `narrationQueue`。
- **供應商抽象**:worker 內把「呼叫哪個模型」抽成 `providers/{gemini,claude}.ts`,方便日後換 2.0/2.5 或加重試/退避(429 自動退避 N 秒)。
- **可選:輕量快取**:對「狀態雷同、無新記憶」的日子直接走模板,省呼叫。
- 優先序:連續性摘要 **高**;節流/退避 **高**(額度痛點);context 預算化 **中**;快取 **低**。

---

## 2. AI 影響劇情(讓 AI 更深、更安全地影響世界)

### 現況
- AI 事件可影響單一租客,或**牽涉第二位鄰居**(`other`/`rel`,嚴格夾值、取向把關);記憶標籤已能**改行為**(`memoryEffects.ts`)。

### 問題
1. **沒有「長期劇情弧」**:事件是一次性的,缺「連載」——第 3 天埋的伏筆第 10 天回收。
2. **記憶會無限累積又同權**:8 個上限、先進先出,沒有「淡出/強化/演化」(失戀該慢慢好起來)。
3. **事件彼此獨立**:缺因果連鎖(A 事件的選擇 → 解鎖 B 事件)。

### 優化設計
- **記憶生命週期**:給記憶加 `intensity`(0~1)與 `decayPerDay`,每日衰減;`memoryDrift` 乘上 intensity;衰到 0 自動移除。讓「失戀」隨時間淡化、痊癒。
  ```ts
  interface MemoryTag { …; intensity: number; decayPerDay: number }
  ```
- **劇情弧 (Arc)**:每位租客可有 0~1 條進行中的 arc(`ThreadState`),AI 每日推進一步;arc 提供「連載」骨架。
  ```ts
  interface StoryArc { id; theme: string; stage: number; maxStage: number; summary: string }
  TenantRuntime.arc?: StoryArc
  ```
  prompt 告知目前 arc 與 stage,要求 AI「推進或收束」。
- **事件連鎖**:AI 事件的選擇可留一個 `flag`(記入租客),之後 `rollEvent`/AI 依 flag 解鎖後續事件。
- **關係網 → 群體事件**:目前社交是兩兩;可加「三角關係/室友公約/公共衝突」等牽涉 3+ 人的事件(需擴充事件 target 為多人)。
- **安全**:所有新增影響一律延續現有原則(store 端夾值、玩家抉擇、不開放驅逐、名字要對得上 roster、忽略未知欄位)。arc/flag 也要 schema 驗證。
- 優先序:記憶生命週期 **高**(便宜、直接讓劇情更真);劇情弧 **中**;事件連鎖 **中**;群體事件 **低**。

---

## 3. UI / UX

### 現況
- 樓層總覽(可捲動)+ 房間細看;面板:商店/招租/收支/關係/抉擇/同居/家具資訊。
- 提示靠樓層徽章(未讀數、紅點事件)+ toast。

### 問題
1. **動線分散**:功能都塞在樓層底部一排按鈕,面板各自為政;缺一個「今天發生了什麼」的彙整入口。
2. **看故事 vs 做經營混在一起**:玩家想「讀劇情」時要一間間點進房。
3. **事件提示弱**:紅點容易忽略;抉擇是遊戲的高潮卻不夠醒目。
4. **房間細看美術債**:仍是舊 3/4,與樓層 tile 比例不一致(已知)。

### 優化設計
- **首頁加「動態消息 (Feed) 分頁」**:把所有租客的重要日誌/事件/關係變化匯成一條時間軸(像社群動態),玩家一眼看完全棟今天的故事;點某則 → 跳對應房間。這是「看故事」的主動線。
- **事件中心**:所有待決抉擇集中一個醒目入口(數字 badge + 顏色),而非散在各房紅點。
- **底部導覽列**:把「樓層 / 動態 / 收支 / 關係」做成固定 4 分頁,取代一排浮動按鈕,手機更好按。
- **房間細看重畫**:改用與樓層一致的 tile 比例(重用 `floorScene` 的繪法放大),並補齊看電視/用餐/看書等狀態 sprite。
- **日夜光線**:樓層依 `gameMs` 疊一層時段色調,增加「時間在走」的臨場感。
- 優先序:動態 Feed **高**(直接放大核心體驗);底部導覽 **中**;事件中心 **中**;房間細看重畫 **中**(美術工);日夜 **低**。

---

## 4. 租客數值模型(重點)

### 現況
- `Tenant.stats = { mood, stress, hygiene, affinity }` + `TenantRuntime.satisfaction`(衍生)。
- **問題最集中的地方**:
  1. **`hygiene` 幾乎沒用**(只有記憶漂移剛接上,無日常來源/後果)。
  2. **會飽和**:mood/stress 容易黏在 0 或 100。
  3. **語意重疊**:mood 與 satisfaction、stress 與 satisfaction 部分重疊。
  4. **和經濟脫節**:租客沒有「自己的錢/財務壓力」,繳租只看 reliability+affinity+satisfaction。

### 優化設計(建議的數值軸重整)
把數值分成**三類**,語意清楚、彼此有因果:

**A. 即時情緒(短期、每小時波動)**
- `mood` 心情、`stress` 壓力、`energy`(**新增**,精力:睡覺/放鬆↑、工作/加班↓;energy 低 → 效率差、易 stress)。

**B. 狀態(中期、緩慢變化)**
- `hygiene` → 改名/擴為 `wellbeing`(身心健康:運動/衛生↑、熬夜/高壓↓),並**給它後果**(低 → 生病事件、mood 上限下降)。
- `affinity` 對房東好感(保留)。

**C. 衍生(唯讀,由上面算)**
- `satisfaction` 居住滿意(現有公式再納入 wellbeing/energy)。
- **新增 `socialFulfillment`**:由社交關係算出(有朋友/戀人↑、孤立↓)→ 影響 mood,讓社交系統回饋到數值。

**租客財務(新增,和經濟扣緊)**
```ts
Tenant.wallet: { balance: number; monthlyIncome: number }   // 租客自己的收支
```
- 租客有收入/開銷;`monthsOverdue` 真正由 wallet 決定 → 「繳不出租」變成有根據的劇情,而非隨機。

**抗飽和機制**
- 所有每小時 delta 改成**朝基準值回歸 (homeostasis)**:`stat += (baseline - stat) * k + event/memory/routine deltas`。個性決定 baseline(工作狂 stress baseline 高)。→ 自然不黏 0/100,且性格差異體現在「回到哪」。

- 優先序:抗飽和(homeostasis)**高**(治本、影響全域手感);啟用/重定義 wellbeing + 後果 **高**;energy **中**;socialFulfillment **中**;租客 wallet **中**。
- 風險:改數值軸會牽動 satisfaction/事件/社交/收租多處,需集中一次重構 + 大量回歸測試(`sim-trace`、新增 balance 快照測試)。

---

## 5. 整體架構 / 擴充性 / 效能 / 資料模型

### 現況與問題
1. **`store.ts` 過大**(~700+ 行,tick/作息/事件/社交/AI/收支/存檔/擺放全在一起)→ 難維護、易改壞。
2. **存檔**:單一 localStorage blob、手寫欄位、`?? 預設` 式相容;版本欄位 `v:2` 但無正式遷移層。
3. **reactive 效能**:每 rAF 重繪整個 canvas;租客多時 `Object.values(runtimes)` 反覆掃描。目前規模 OK,但要留意。
4. **內容資料驅動**:家具/文本已資料化;事件(規則式)、作息仍寫死在程式。

### 優化設計
- **拆 store 成多個 slice/模組**(仍共用一個 reactive `state`):
  - `sim/tick.ts`(hourlyTick + applyHour + 偏離)、`economy.ts`(addMoney/collectRent/upkeep/ledger)、`tenancy.ts`(moveIn/moveOut/招租/佔用/配色)、`narration.ts`(每日 AI/摘要/佇列)、`persistence.ts`(save/load/migrate)。`store.ts` 只留 `state` 定義 + 對外 re-export。
- **正式存檔遷移層**:`persistence.ts` 內 `migrate(raw): SaveV3`,一段一段版本升級;`SAVE_VERSION` 常數;壞檔安全 fallback。
- **事件與作息資料化**:把 `events.ts` 的規則式事件、`routine.ts` 的作息抽成 `data/*.json` 或 catalog 式陣列,讓「加事件/改作息 = 改資料」。
- **效能**(有需要再做):agents/canvas 只在「有變化」時重繪;`occupancy`/`runtimes` 掃描結果快取。
- **測試策略**:除了 `sim-trace`,加「**balance 快照測試**」——固定隨機種子跑 N 天,快照關鍵數值/金錢曲線,改動時 diff,避免手感悄悄跑掉。
- 優先序:store 拆分 **高**(所有後續工作的地基);存檔遷移層 **高**(改資料結構前必備);事件/作息資料化 **中**;效能 **低**;balance 快照測試 **中**。

---

## 6. 建議實作順序(可交給 Sonnet/Opus 的獨立工作項)

> 每項:目標 · 涉及檔案 · 相依 · 難度 · 優先序

1. ~~**store 模組化重構**~~ ✅ 2026-07-10 完成(拆成 gameState/tick/economy/tenancy/narration/persistence/lifecycle,store.ts 為 façade,呼叫點不變)
2. ~~**存檔遷移層 + SAVE_VERSION**~~ ✅ 2026-07-10 完成(`sim/persistence.ts`:SAVE_VERSION + MIGRATIONS 逐版升級 + migrateSave;匯入也走遷移)
3. ~~**數值模型重整(homeostasis + 啟用 wellbeing + energy)**~~ ✅ 2026-07-10 完成(含 socialFulfillment 簡版=社交影響 mood 基準、生病事件、SAVE_VERSION 3 遷移;驗收 `scripts/stats-model-test.ts`)
4. ~~**AI 連續性摘要 + 節流/退避**~~ ✅ 2026-07-10 完成(recentSummary 滾動閉環 + narrate 串行 4s 間隔 + Worker 429 退避;驗收 `scripts/summary-test.ts`)
5. ~~**記憶生命週期(intensity/decay)**~~ ✅ 2026-07-10 完成(intensity 每日依語意衰減、漂移乘強度、歸零淡忘;decayPerDay 改為由標籤語意推導,不入存檔;驗收 `scripts/memory-lifecycle-test.ts`)
6. **首頁動態 Feed + 底部導覽** · `App.vue`/新元件 · 相依:無 · 難度 中 · **中**
7. **劇情弧 (StoryArc) + 事件連鎖 flag** · `types.ts`/`events`/`worker`/`narration` · 相依:4,5 · 難度 中-高 · **中**
8. **事件/作息資料化** · `data/*`、`events.ts`/`routine.ts` · 相依:1 · 難度 中 · **中**
9. **房間細看重畫(tile 一致)+ 新狀態 sprite + 日夜** · `scene.ts`/`PixelDollhouse`/`floorScene` · 相依:無 · 難度 中(美術)· **中**
10. ~~**balance 快照測試**~~ ✅ 2026-07-10 完成(`scripts/balance-test.ts` 固定種子 10 日快照 + `--update` 重建基準)

**最推薦的前三**:**1 → 2 → 3**(地基:先讓程式可維護、存檔可遷移,再動數值模型)。之後 **4(AI 連續性)** 與 **6(動態 Feed)** 對「玩起來的感覺」提升最明顯。

---

## 7. 補強 review(2026-07-10):對準兩大核心體驗

> 明確目標:**(a) 當房東的投資遊戲性、(b) 觀察 AI 生成的事件「真實發生」在遊戲畫面裡**。
> 用這兩把尺重新檢視,前面各節少了以下幾塊:

### 7-1. 投資遊戲性(目前最弱的一環)
現況:錢只能買家具;租金固定;收入幾乎無決策。「投資」需要**有風險的選擇 + 長期資本目標**:
- ~~**調租談判(高)**~~ ✅ 2026-07-10 完成(`sim/tenancy.ts` proposeRent/previewRent + `RentPanel.vue`;容忍度公式、冷卻 5 日、記憶標籤接 AI;驗收 `scripts/rent-test.ts`)。
- **房間升級階梯(高)**:隔音改建/精裝修等「一次性大額投資」→ 永久提高該房月租上限與吸引高星應徵者。給錢一個**長期去處**,和家具(短期)區分開。
- **維修/損壞風險事件(中)**:熱水器壞了、漏水 → 立刻花錢修 vs 拖延(滿意度持續掉)。讓「支出」也有決策性,並可由 AI 事件觸發(接到 7-2)。
- **月報表/資產淨值(中)**:FinancePanel 已有帳,補一個「本月損益 + 資產估值」就有成長曲線的感覺。
- **招租費用(低)**:「重新刊登、換一批應徵者($)」——順便給玩家主動權(應徵者池現已改為每遊戲日一批)。

### 7-2. 讓 AI 事件「看得見」(關鍵缺口)
現況 AI 事件的後果 = 數值/關係/記憶,全都**不可見**;玩家在畫面上感受不到「AI 說的事真的發生了」。補一個安全的落地機制:
- ~~**行為指令白名單(高)**~~ ✅ 2026-07-10 完成(`sim/directives.ts` 六種指令,掛事件選項 effect、玩家拍板生效;驗收 `scripts/directive-test.ts`):AI 事件/日記可從**固定 enum**中選一個「之後 N 遊戲日的行為改變」,由前端套用:
  - `night_owl_shift`(作息整段位移=熬夜)、`hermit`(不去交誼廳)、`social`(每天多去交誼廳)、`adopt_cat`(房間出現貓 prop)、`workout`(每天多一段運動)…
  - 全部是既有 visualState/props/routine 的組合 → **schema 驗證 + 白名單**,AI 不能發明機制,但玩家能在娃娃屋裡「親眼看到」劇情後果。
  - 這一項是「AI 影響劇情」從文字層跨到畫面層的關鍵,建議排在記憶生命週期之後、劇情弧之前。
- **事件後果接經濟(中)**:AI 事件選項可造成「家具損壞(prop 顯示破損)→ 修理費」──同時餵 7-1 的維修系統,一魚兩吃。

### 7-3. 對 roadmap 的修訂
把第 6 節的順序改為(★=本次新增):
1 store 模組化 → 2 存檔遷移 → **★調租談判** → 3 數值模型 → **★行為指令白名單** → 4 AI 連續性摘要 → 5 記憶生命週期(矛盾淡出已先做)→ **★房間升級階梯** → 6 動態 Feed → 其餘照舊。
理由:調租+升級直接補「投資性」;行為指令直接補「AI 真實發生」;兩者都不依賴 store 拆分,可穿插做。
另:第 8 節的 QoL 項(家具移動 8-1 為首)**不依賴任何其他項**,適合當作交給新模型的「暖身第一題」——範圍小、驗收明確,又能熟悉 placements/store 的模式。

### 7-4. 本次已順手落地的(不用再排)
- 記憶「矛盾淡出」:數值已走出記憶方向 → 標籤自動移除 + 「心境轉變」日誌(第 2 節記憶生命週期的一半)。
- 同居改為真同居(不刪角色、免一份租、分手搬離/接手承租)——同居本身現在就是一個**投資決策**(空出一間房再招租 vs 少收一份租)。
- 招租應徵者池:每遊戲日一批、星等隨裝潢即時重算。

---

## 8. 操作/QoL 細節優化(施工級規格,2026-07-10 補)

> 第 3 節的 UI 優化偏「方向」;這一節是可以直接照做的規格:每項含**互動流程 / 改動點(檔案+函式)/ 驗收條件**。
> **狀態:8-1 ~ 8-6 已全數完成(2026-07-10)**,驗收見 `scripts/move-test.ts` 與 `scripts/qol-test.ts`;細節記錄在工作日誌第二節 K。

### 8-1. 家具移動(高;目前只能賣掉重買,體驗最差的缺口)
**互動流程**:點家具 → `FurnitureInfo` 資訊卡新增「📦 移動」按鈕 → 進入移動模式(頂部出現與 `place-bar` 同款的「移動中:XX — 點地圖放置 / 取消」列)→ 玩家點目標格 → 成功即搬過去(**免費**,家具已是玩家資產),失敗 toast 原因、家具留在原地。

**改動點**:
- `src/store.ts`:新增 `state.pendingMove: { c: number; r: number; defId: string } | null` + `startMoving(c, r)` / `cancelMoving()` / `moveFurnitureTo(c, r)`。
- **實作關鍵——判定要排除自己**:`canPlaceFree()` 會把這件家具的舊佔位算成「被擋」。正確作法:先 `removePlacementAt(原c, 原r)` 把它暫時拿起來 → `canPlaceFree(新c, 新r, w, h)` → 成功就 `addPlacement({ defId, room: 回傳的新room, c, r })`;失敗把原 placement `addPlacement` 放回原位(等於沒動)。version++ 由 add/remove 內建,不用另外處理。
- 允許跨房間搬(`room` 欄位跟著換,`roomAttributes()` 自動反映 → 招租星等/需求滿足都會正確變化,這正是玩法)。
- 搬完對全員 `applyHour(rt, hour, false)` 重新定位(有租客的目標家具被搬走時,下一步會走去新位置或改發呆)。
- `src/components/FurnitureInfo.vue`:加 `move` emit + 按鈕。
- `src/App.vue`:`inspectItem` 面板收到 move → 關卡片、`startMoving()`;地圖點擊分支——`pendingMove` 優先於 `pendingPlace` 處理;移動列 UI 複用 `.place-bar`。
- `src/components/FloorMap.vue`:擺放模式的點擊 emit(`place`)在移動模式共用即可,不用新 event。

**驗收**:
1. 移到同房/跨房合法格 → 成功、錢不變、`roomAttributes` 跟著新房間走。
2. 移到牆上/壓到其他家具/跨房間邊界 → 失敗 toast,家具**還在原位**(不會消失)。
3. 移走某租客正在用的家具 → 該租客下一小時走向新位置,sim-trace 0 異常。
4. 存檔重載後位置正確。

### 8-2. 擺放/移動預覽(中;現在是「點了直接扣款」,誤觸成本高)
**互動流程**:擺放/移動模式中,**第一次**點某格 → 不成交,改在地圖上畫半透明 footprint(可放=綠、不可=紅)+ 底部列變成「✓ 確認放這裡 / 換一格 / ✕ 取消」;**再點確認**才真正 `placeAt`/`moveFurnitureTo`(此時才扣款)。點另一格 = 換預覽位置。
**改動點**:`FloorMap.vue` 加 `previewTile` 狀態並在 `composeFloor` 之後疊畫(`floorScene.ts` 加一個 `drawFootprintPreview(ctx, c, r, w, h, ok)`;ok 用 `canPlaceFree` 判);`App.vue` 底部列加確認鈕(紅色時 disabled)。
**驗收**:紅色預覽時確認鈕按不了;取消不扣款;確認後才出現 ledger 記錄。

### 8-3. 通知中心(中;現在 toast 2.2 秒即逝,錯過就沒了)
`state.noticeLog: { gameMs, text }[]`(cap 30、入存檔);現有 `state.notice` toast 機制不動,但每次同時 push 進 noticeLog。header 加 🔔(有未讀顯示數字)→ 開簡單列表面板(新 `NoticePanel.vue`,樣式抄 `FinancePanel`)。
**驗收**:退租/戀愛/分手/同居通知都留存;重整頁面還在。

### 8-4. 存檔管理(中;目前玩壞了只能自己清 localStorage)
新 `SettingsPanel.vue`(header ⚙️ 進入):**重新開始**(二次確認 → `localStorage.removeItem(SAVE_KEY)` + `location.reload()`)、**匯出存檔**(save JSON 複製到剪貼簿)、**匯入存檔**(貼上 JSON → 驗 `v` 欄位 → 寫入 + reload)。`store.ts` 匯出 `SAVE_KEY` 與 `exportSave()/importSave(raw): boolean`。
**驗收**:匯出→重新開始→匯入 = 完整還原(含家具/關係/帳)。

### 8-5. 數值說明(低)
房間細看的五條數值 label 可點 → toast 一句話說明(心情=短期情緒/壓力=高會崩潰爆事件/好感=對房東、影響繳租/整潔=房間狀態/滿意=綜合、過低會退租)。純 `App.vue`,一個 `STAT_HELP` 表。

### 8-6. 快轉體驗(低)
`fastForward(24)` 一次跑 24 tick 會卡 UI 一下:改成分批(每 frame 跑 4~6 tick,期間按鈕顯示「快轉中…」disabled)。加「⏩ 快轉 1 天」按鈕(跨午夜觸發 AI 日記,方便驗證 AI 功能)。

### 8-7. 明確不做(避免實作模型自行發揮)
- 家具旋轉:catalog 的 footprint/繪製不分向,成本高收益低,**不做**。
- 擺放 undo:有 8-2 預覽 + 移動 + 賣半價,夠了,**不做**。

---

## 9. 未來擴充:角色庫豐富化 + 自訂角色(AI 生成屬性)

> 需求(2026-07-10):角色太少、外觀變化太小;未來想加入動漫角色(大雄、柯南…),由 AI 依角色個性設計屬性。

### 9-1. 角色外觀「部件化」(基礎工程,先做)
現況:外觀 = 一組配色 Theme(6 色池),臉孔/髮型/服裝全都一樣,角色多了必然臉盲。
- 把 sprite 拆成**可組合部件層**:髮型(短/長/馬尾/呆毛…)× 髮色 × 上衣款式 × 配件(眼鏡/蝴蝶結/帽子/耳機)× 膚色。
  ```ts
  interface Appearance { hairStyle: string; hairColor: string; top: string; accessory?: string; skin: string }
  Tenant.appearance?: Appearance   // 沒有則退回現行 Theme 池(存檔相容)
  ```
- `scene.ts`/`floorScene.ts` 繪製改為逐層疊畫;每類部件先各做 4~6 款,組合數就足夠幾十位角色不重臉。
- 這是**加入任何新角色前的前置工程**——不然加 20 個角色也只是 6 種顏色輪流出現。難度:中(美術工);優先:此線第一。

### 9-2. 內建角色庫擴充(原創角色)
- `ARCHETYPES`(recruit.ts)從 4 種擴到 10~15 種:護理師(輪班日夜顛倒)、退休老師(白天在家、愛管閒事)、健身教練、漫畫家(截稿地獄)、調酒師(夜歸)、直播主、研究生……每種含:作息 archetype、coreTags、偏好、租金帶、**個性相容度規則**(social.ts compatibility 對應擴充)。
- 這批可交給 **Fable 製作階段一次性生成**(同 observationLines 的做法):給 schema,讓它產 15 種完整 archetype + 各 3 條專屬觀察句,人工審核後入庫。

### 9-3. 自訂角色:「特邀租客」系統(AI 依個性生成屬性)
玩家輸入**名字 + 一段個性描述**(例:「膽小懶惰但心地善良的小學生,愛睡午覺和射擊」)→ AI 生成完整角色:
- **流程**:招租面板加「✉️ 特邀租客」→ 輸入名字/個性 → 打 `/api/narrate` 新增的 `mode: "genTenant"` → Gemini 依 schema 回傳 `{ coreTags, stats, preferences, archetypeKey(從現有作息中選), monthlyRent, bio, appearance(從部件枚舉中選), isAdult }` → **前端消毒**:數值夾 0~100、archetypeKey/部件必須在白名單內、租金夾範圍 → 成為應徵者入住。
- **AI 不會發明機制**:它只是在「現有枚舉」裡替角色選一組最像的參數——柯南會被配到「觀察力強/夜貓(查案)/整潔」,大雄會是「愛睡/懶散/心地善良/成績差→租金便宜」。
- **版權注意(重要)**:遊戲**不內建**任何版權角色;特邀租客是玩家自己輸入、存在自己存檔裡的自訂內容。若未來要做分享/共享世界功能,自訂角色不隨之公開散佈。
- **年齡欄位**:AI 生成時一併判定 `isAdult: boolean`(小學生角色 = false),供第 10 節的成人互動硬性把關。
- 相依:9-1(不然生成的 appearance 沒東西可選)。難度:中。

---

## 10. 未來擴充:互動系統大改版(親密/衝突/成人模式)

> 需求(2026-07-10):加入更豐富的租客互動——含輕度成人內容(一起洗澡、床上/沙發親熱)與激烈衝突(吵架、打架)。

### 10-0. 三條硬規則(實作模型不得省略)
1. **成人內容 = 明確 opt-in**:設定面板「🔞 成人模式」開關,預設**關**;關閉時親密互動最高到「接吻/擁抱」。遊戲是公開網址,預設體驗必須全年齡。
2. **僅限成年角色**:任何親密/成人互動的前置條件包含 `a.isAdult && b.isAdult`(內建角色全為成人;特邀租客由 9-3 的 isAdult 把關)。此檢查寫在互動系統核心,不是 UI 層。
3. **AI 不生成露骨內容**:Gemini 安全過濾會拒絕,強求只會拿到空回應。露骨層全部用**本地模板 + 遮蔽式演出**;AI 只寫「事後」的日記與情緒(「昨晚之後,兩人之間的空氣都是甜的」),prompt 明示避免露骨描寫。

### 10-1. 互動框架:資料驅動的 InteractionDef
現行 encounter() 只有「交誼廳聊天/衝突」一種場景,寫死在程式。改成目錄式:
```ts
interface InteractionDef {
  id: string;
  tier: "stranger" | "friend" | "close" | "romantic" | "couple"; // 關係門檻
  location: "lounge" | "room" | "bathroom" | "kitchen";           // 需要兩人同處
  needs?: string[];        // 需要的家具角色(bed / sofa / bathtub…)
  adult?: boolean;         // true = 需成人模式 + 雙方 isAdult
  weight: number;          // 出現權重
  lines: string[];         // 模板文本({o}=對方)
  effects: { rel: number; moodA?: number; moodB?: number; stressA?: number; stressB?: number };
  visual?: "hearts" | "steam" | "fight_cloud" | "lights_off" | "zzz" | "music"; // 娃娃屋演出
  cooldownHours?: number;
  memoryLabel?: string;    // 觸發後留的記憶標籤(接 memoryEffects)
}
```
- 每小時 socialPass 擴充:除了交誼廳,**同房/同設施**的兩人也判定互動(同居情侶在自己房裡才會發展親密線)。
- `visual` 對應 floorScene 的疊加演出(見 10-4)——這同時就是第 7-2 節「AI 事件看得見」的基礎設施,一魚兩吃。

### 10-2. 互動目錄(首發清單,約 30 種)
**日常/友誼**:一起打電動(需 tv/console,雙 gamer 加成)、一起做飯、深夜談心(壓力互降)、借錢(接租客 wallet)、生病照顧(一方 wellbeing 低時)、幫追劇補進度、搶浴室(輕衝突)。
**曖昧/戀愛**:共用耳機看劇(sofa)、餵對方吃宵夜、天台看星星(away 同時段)、告白(成功→情侶/失敗→尷尬期 N 天)、吃醋(三角關係:C 對 A 好感高且 A 有伴)、初吻、慶祝交往紀念日(需玩家送禮抉擇→接經濟)。
**親密(成人模式,全部遮蔽式)**:
- 一起洗澡:兩人同時走進浴室 → 門上鎖圖示 + 霧氣 `steam` 演出 → 出來後雙方 mood↑ stress↓、留 `[臉紅的祕密]` 記憶。
- 床上/沙發親熱:同居或關係 ≥ couple、深夜、同房 → **關燈 + 愛心粒子 + 「請勿打擾」門牌**(角色 sprite 不做任何露骨動作,全靠演出暗示)→ 大幅 mood↑ 關係↑;隔天 AI 日記寫餘韻。
- 早晨賴床:親熱事件的隔天早上,兩人 late 起床(作息位移 1 小時)——讓玩家「從結果推知昨晚」,觀察系遊戲最對味的敘事法。
- 被撞見:低機率第三位租客剛好進交誼廳/浴室 → 三方尷尬事件(撞見者 stress↑ + `[撞見不該看的]` 記憶,AI 可拿來寫八卦)。
**衝突/激烈**:
- 冷戰:大吵後 N 天互相迴避(路線繞開對方、不去交誼廳),關係持續小扣——「看得見的不和」。
- 打架:關係 <20 + 相容度 ≤ -3 + 雙方 stress ≥80 才觸發 → `fight_cloud` 演出(卡通打鬥雲,不見血)→ 雙方受傷狀態(wellbeing↓)、**家具損壞(接 7-1 維修系統)**、必發房東抉擇:調解(耗好感判定)/各打五十大板/警告最兇的一方。
- 摔門絕交、偷吃冰箱食物被抓、噪音糾紛(音樂人 vs 在家工作)、劈腿抓包(有伴者與第三人曖昧值過高 → 修羅場事件,分手+全樓八卦)。

### 10-3. AI 的角色
- AI 事件(worker SYSTEM)新增可引用互動結果:互動留下的 memoryLabel 與 `noticeLog` 進 context → AI 寫日記/生事件時能接續(「自從那晚之後,他刻意避開交誼廳」)。
- AI **可以提議**互動類事件(從 InteractionDef id 白名單選),經 sanitize 後觸發——但露骨層永遠是本地模板演出。

### 10-4. 娃娃屋演出層(visual overlay)
`floorScene.ts` 新增輕量粒子/圖示系統:`hearts`(上飄愛心)、`steam`(霧氣)、`fight_cloud`(打鬥雲+星星)、`lights_off`(房間變暗)、`zzz`、`music`。每種 = 幾幀簡單 canvas 動畫,掛在房間或角色座標上,由互動觸發、數秒~數小時後消失。**這層做完,第 7-2 的行為指令也直接受益。**

### 10-5. 關係門檻矩陣(哪個階段能做什麼——硬性,寫在核心不寫在 UI)

**所有互動的資格檢查收斂到一個函式**,禁止散落各處(否則成人門檻遲早被某條路徑繞過):
```ts
function canInteract(def: InteractionDef, a: Tenant, b: Tenant, ctx: InteractCtx): boolean
// 依序檢查:關係階層 → 戀愛取向(曖昧以上需 attractedMutual)→ 成人(def.adult ⇒ 成人模式開 && 雙方 isAdult)
//        → 地點可達且無第三人(私密互動)→ 家具需求 → 時段窗 → 冷卻 → 雙方非 away/非 pendingEvent
```

| 關係階層(rel.value) | 解鎖互動 | 額外條件 |
|---|---|---|
| 陌生(0-14) | 點頭寒暄;口角(相容度差) | — |
| 點頭之交(15-34) | 閒聊、搶浴室糾紛 | — |
| 朋友(35-49) | 一起打電動、一起做飯、小額借錢 | 打電動需 tv/console |
| 好朋友(50-74) | 深夜談心、生病照顧、共用耳機看劇 | — |
| 曖昧(75+,互有好感) | 餵宵夜、天台看星星、**告白**、初吻(告白成功時) | attractedMutual;**到此為止,不能洗澡/親熱** |
| **情侶(romantic)** | 約會、紀念日、**一起洗澡**、**床上/沙發親熱** | 洗澡/親熱:🔞模式開 + 雙方 isAdult + 私密(無第三人)+ 時段窗(親熱=夜間) |
| 同居(cohabits) | 賴床、早安吻;親密類權重提高 | — |

- 明確規則:**非情侶絕不觸發洗澡/親熱**(曖昧再高也不行);分手後 romantic=false 即刻降回對應階層。
- 衝突線走相反軸:打架需「關係 <20 + 相容度 ≤-3 + 雙方 stress ≥80」三條件齊備,不受成人模式影響(全年齡可見,卡通化)。

### 10-6. 雙人互動圖式 + 地點鎖定(讓互動「演出來」而不只是日誌一行字)

**互動 session 生命週期**:判定成立 → 兩人**各自尋路走到錨點**(都到齊才開演;任一方走不到就取消)→ 播放雙人圖式 N 遊戲分鐘 → 套效果 + 雙方日誌 + 記憶標籤。
```ts
interface InteractionSession { defId; aId; bId; anchorA: Tile; anchorB: Tile; untilMs: number }
state.interactions: InteractionSession[]   // 進行中;agent 的 targetTile 被 session 錨點覆寫
```

**錨點(地點鎖定)**:互動綁定實體地點與家具,由 placements 反查:
```ts
anchor:
  | { kind: "furniture"; role: "sofa" | "bed" | "bathtub" | "tv" | "dining"; slots: [dA, dB] } // 兩人相對家具的偏移格
  | { kind: "faceToFace" }                                                                     // 任意空地面對面
```
- 沙發 = 並肩兩格;雙人床 = 左右兩側;餐桌 = 對坐;浴缸 = 見遮蔽式。
- **地點條件即玩法**:交誼廳沒沙發就不會有「共用耳機」;房裡是單人床就不會觸發「親熱」(要買雙人床——**家具投資直接解鎖互動**,接回經濟系統)。
- 私密互動(洗澡/親熱/談心)要求該房間/設施**無第三人**;「被撞見」= 演出期間第三人剛好走進來的例外分支。

**雙人圖式(pose)枚舉**——floorScene 新增「互動演出層」,session 期間取代兩人的一般 sprite:
| pose | 畫面 | 用於 |
|---|---|---|
| `sit_pair` | 並肩坐(沙發座位圖式)+ fx | 共用耳機、看劇、談心 |
| `stand_face` | 面對面站 + 對話/怒氣泡泡 | 閒聊、口角、告白 |
| `cook_pair` | 並排站在流理台 | 一起做飯 |
| `game_pair` | 並肩坐 + 螢幕閃爍 + 手把 | 一起打電動 |
| `lie_pair` | 雙人床左右兩側躺 + `lights_off` | 賴床(全年齡)|
| `bath_hidden` | **兩人 sprite 隱藏**,浴室門上鎖圖示 + `steam` | 一起洗澡(🔞,遮蔽式)|
| `intim_hidden` | **兩人 sprite 隱藏**,房間變暗 + 愛心粒子 + 門牌「請勿打擾」 | 親熱(🔞,遮蔽式)|
| `fight_ball` | 兩 sprite 換成一團打鬥雲 + 星星 | 打架 |

- 每個 pose = 2~4 幀的小合成圖,錨在家具座標上;🔞 類**一律 hidden(遮蔽式)**,畫面上永遠沒有露骨圖像——分級靠「暗示密度」不靠圖。
- InteractionDef 增欄:`pose: PoseId; anchor: Anchor; timeWindow?: [number, number]; privacy?: boolean; durationMin: number`。
- 改動點:`floorScene.ts`(演出層)、`agents.ts`(session 錨點覆寫 targetTile)、`store.ts`(interactions 陣列、session 推進/結算)、`social.ts` → 新 `sim/interactions.ts`(目錄+canInteract)。
- **驗收**:非情侶任何路徑都進不了浴室/床互動(單元測試窮舉 tier);session 中兩人確實站/坐在錨點格;第三人進入私密互動房間觸發撞見分支;任一方走不到錨點時互動取消且無效果殘留。

### 10-7. 優先序與相依
```
9-1 部件化外觀 ──→ 9-3 特邀租客(AI 生成屬性)
10-4 演出層 ──→ 10-1 框架 + 10-5 門檻矩陣 ──→ 10-6 雙人圖式/地點鎖定 ──→ 10-2 目錄(日常/戀愛先、成人批最後)──→ 10-3 AI 接續
9-2 角色庫擴充(獨立,可隨時做,交 Fable 生成)
```
建議順序:**10-4 演出層 → 10-1 框架 + 10-5 canInteract → 10-6 雙人圖式 → 10-2 日常+戀愛批 → 9-1 外觀 → 9-2 角色庫 → 10-2 成人批(含設定開關)→ 9-3 特邀租客**。
風險:成人模式務必檢查三條硬規則;打架的家具損壞相依 7-1 維修系統,先做維修再做打架。

---

## 附:不建議現在做 / 風險提醒
- **多人共享世界**:需帳號 + DB + 同步,是另一個量級的工程,建議等單機體驗打磨完再評估(見工作日誌待辦)。
- **一次改太多數值軸**:務必集中在單一 PR + 快照測試護航,否則手感會悄悄崩。
- **AI 影響力放太寬**:任何讓 AI 動到世界狀態的新能力,都要維持「夾值 + 玩家抉擇 + schema 驗證」三道關卡。
