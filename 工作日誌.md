# 《房東監視中》開發工作日誌

> 更新日期:2026-07-18
> 一款「異步掛機觀察型」直式手機模擬遊戲:玩家是房東,透過俯視娃娃屋觀察租客生活、佈置家具、招租收租;劇情由 **AI(Gemini)依歷史即時生成**,並能影響走向。

## 線上位置
- **遊戲網址(手機可玩):** https://rent-house.guai7485962.workers.dev
- **Repo:** github.com/guai7485962/rent-house(push 到 `main` → Cloudflare 自動 build + deploy)
- **架構:** 靜態前端(Vue+Vite)+ Cloudflare Worker(同時服務網站與 `/api/narrate`);AI 金鑰存 Worker Secret `GEMINI_API_KEY`。

---

## 一、目前完成度總覽

「**觀察 + 房東經營 + AI 敘事 + 張力/社交**」的完整骨架都做完並上線了。核心體驗:掛機觀察租客 → 佈置招租收租 → 處理突發抉擇 → 看 AI 把每天寫成有記憶、會演進的故事,鄰居之間還會發展友情/戀愛/衝突。**沒金鑰/離線/超額時自動 fallback 成模板,遊戲照跑。**

---

## 二、已完成的系統

### A. 核心資料與美術
- `src/types.ts` — Tenant/Furniture/事件、visual_state、房間屬性、性別與戀愛取向、記憶標籤。
- 種子 `data/tenants.json`;`src/pixel/` 像素美術(俯視 3/4)、動態租客配色(依房間指派、保證不撞色)。

### B. 樓層地圖(tile 網格)
- `src/floor/map.ts` — 16×33 網格:4 套房 + 交誼廳 + 浴室(乾濕分離)+ 洗衣間 + 大門,可捲動。
- `floorScene.ts` + `FloorMap.vue` — 渲染、房間標籤、未讀/事件徽章、點擊進房/擺放/查看家具。

### C. 家具目錄 + 動態擺放
- `src/furniture/catalog.ts`(~35 件,資料驅動)、`render.ts`;`src/sim/placements.ts`(買/擺/賣/查、房間屬性、找空位)。
- 點家具 → 資訊卡 + 賣掉(退半價)`FurnitureInfo.vue`。

### D. 走動與尋路
- `pathfind.ts`(BFS + 家具障礙)、`agents.ts`(狀態驅動,走到當前活動家具旁;站立點自動避牆)。

### E. 異步掛機模擬
- `src/sim/clock.ts` — **7× 掛機時鐘(現實 1 天 = 遊戲 7 天)**,關掉再開自動補進度。
- `src/sim/routine.ts` — 24 小時作息 + 招租原型 + 家具角色反查(只用自房/共用區)。
- `src/sim/generate.ts` — 每小時觀察句(用 Fable 文本庫,隨機挑、時段安全)。
- `src/store.ts` — 每小時 tick、作息+偏離、未讀、存檔(localStorage)、快轉。

### F. 房東經營 + 收支系統
- 商店 `FurnitureShop.vue`(選→點地圖擺放)、招租 `recruit.ts`+`RecruitPanel.vue`(依裝潢契合度★)、收租。
- **收支帳** `state.ledger` + `addMoney()`(所有金錢進出單一入口)+ **每日管理費** + **財務面板 `FinancePanel.vue`(💰 收支)**:餘額/收入/支出/分類/交易明細。

### G. 張力系統(數值後果)
- **滿意度**(由心情/好感/壓力 + 房間是否滿足需求算出)。
- **突發事件抉擇**:規則式(壓力/滿意/好感觸發)+ **AI 生成**;`DecisionModal.vue` + `decide()` 套用夾值後果。
- **退租**:長期不滿 → 搬走、空房、系統通知。收租綁滿意度。

### H. 鄰居社交(完整版)
- `src/sim/social.ts` — 關係值 + 個性相容度 + 性別/取向;交誼廳相遇 → 聊天/衝突;認識→朋友→曖昧→情侶→**同居/分手**。
- **💞 關係面板**、同居抉擇彈窗、房間細看顯示感情狀態。

### I. AI 敘事(靈魂,已上線)
- `worker/index.ts` — `/api/narrate` 呼叫 **Gemini 2.5-flash**(免費層),依「當天日誌+性格+記憶+關係+抉擇」生成:
  - **每日觀察日記**(📖 卡片;AI 版加 ✨AI 標,模板版乾淨卡片)。
  - **記憶標籤**(讓歷史累積、劇情演進;去重+上限 8)。
  - **房東抉擇事件**:AI 依處境即時生成(嚴格夾值消毒),**可牽涉第二位鄰居**改動雙方數值與關係(戀情加速/室友吵架/和好…),不開放驅逐。
- `src/sim/narrate.ts` — 前端同源 fetch + 模板 fallback;供應商可切換(Gemini 優先,Claude 備援)。
- **內容變化**:`src/content/observationLines.ts`(**Fable 一次性生成**:170 句觀察 + 16 每日模板,時段安全),讓模板層與 AI 素材都不再千篇一律。

### U-1. 舒適/整潔敘事鉤子:正向慶祝 cozyHomePass(2026-07-24;Claude Code;舒適度第二波 B)
- **目的/需求**:舒適度系統原本只有負向的 `dirtyComplaintPass`(髒亂抱怨),缺少對稱的正向回饋。補一個「房間既舒適又乾淨」的正向慶祝,鼓勵玩家用心佈置與維持整潔,維持「慢變環境品質、非 Sims 式照顧」的定位。
- **實際變更與主要檔案**:`src/sim/tick.ts` 新增 `cozyHomePass(day)`(export)——房間 `roomComfort ≥ 52` 且 `cleanliness ≥ 80` 時,每 3 遊戲日偶爾一句 🏡 正向日誌 + 極小加成(mood +2、satisfaction +1);文案用日期+租客雜湊 `cozyHomeIndex` 穩定選字,**不擲 Math.random**(與 `dirtyComplaintPass` 同樣的日數冷卻風格,不打亂 balance RNG 序列);在午夜區塊 `dirtyComplaintPass` 後呼叫;另 export `resetCozyHomeCooldown()` 供測試。`scripts/comfort-test.ts` 加 4 項斷言(高舒適乾淨房會慶祝、給微小加成、日數冷卻、髒/低舒適房沉默)。
- **測試/build/驗證**:`npm run typecheck` 乾淨;`TZ=Asia/Taipei npm test` 全綠 59/59;`npm run build` 成功。
- **平衡快照**:**未移動**。慶祝在 balance 種子中確實會觸發(林小婕乾淨房),但 10 日終點的 mood/satisfaction 漂移落在既有 ±0.6 浮點容差內、且日誌數已在 LOG_CAP=60 飽和,故 `balance-snapshot.json` 維持一致(未 --update)。
- **已知限制/後續**:慶祝門檻(comfort 52 / clean 80)是依現行家具經濟(單房舒適度上限約 56)抓的;若日後大幅擴充高屬性家具,可再上調門檻。可選的「舒適社群事件」本次未做(留待後續)。
- **硬規則保留**:未動未成年戀愛線排除、成人內容遮蔽預設關、AI 白名單/夾值/消毒;新鉤子純敘事 + 夾值加成,無 RNG。

### T. 劇情弧 StoryArc + 事件連鎖 flag(2026-07-11;設計檢討 §2 工作項 7)
- 新模組 `sim/arcs.ts`:每位租客 0~1 條進行中的弧 `{id, theme, stage, maxStage, summary}`,**純敘事骨架不帶數值**;AI 每日 narrate 回 `arcUpdate` 推進。
- **消毒(sanitizeArcUpdate)延續 AI 安全原則**:主題截 14 字/摘要截 160/maxStage 夾 2~6;進行中 stage 只能 +1 不可倒退、theme/id 鎖定(AI 不能中途換故事);`done:true` 或推到 maxStage → 收束:清弧 + 留 `[經歷:主題]` 記憶 + 「📕 篇章落幕」notable 日誌(開弧也有「📖 新篇章」,兩者都進動態 Feed)。
- **事件連鎖 flag**:事件選項 effect 可留 `flag`(≤16 字伏筆旗標,cleanEffect 消毒)→ decide 記到 `rt.flags`(去重、cap 12)→ 每日 narrate context 餵回 AI(「未回收的伏筆旗標」),prompt 要求後續回收。
- Worker prompt 新增 arc 規則(沒弧可開新弧/有弧必須推進或收束/平淡別硬開)+ flags 說明;context 帶「進行中的劇情弧」與伏筆清單。
- UI:房間細看標籤列藍色弧 chip(`📖 主題 · 2/3`);摘要展開時多一行弧進展。
- 存檔:runtimes 加 `arc`/`flags`(additive,load `?? null`/`?? []`);模板 fallback 不動弧(離線遊戲照跑)。
- 測試:`scripts/arc-test.ts` 25 項(消毒單元 + mock AI 三日開/推/收全流程 + flag 傳遞/去重/cap + 存檔往返);全 34 腳本 + build + wrangler dry-run 通過。

### S. 動態 Feed + 底部導覽(2026-07-11;設計檢討 §3 工作項 6)
- 新模組 `sim/feed.ts`(唯讀彙整,不碰模擬):`buildFeed()` 把**全棟**租客的重要日誌(notable/major)、📖 當日觀察、🏠 房東介入(decisionNote)與 📢 系統通知匯成一條新到舊時間軸(cap 60);minor 流水帳不進,想看細節仍點房間。
- **底部導覽列**(§3:樓層/動態/收支/關係四分頁,取代樓層底部一排浮動按鈕):樓層與動態是主畫面切換,收支/關係從導覽開既有底部面板;樓層列只留 🛒 商店 + 兩顆 ⏩ 快轉。
- **動態分頁 = 看故事主動線**(`FeedPanel.vue`):最上方紅色「🔴 事件中心」集中所有待決抉擇(醒目色塊,點了跳進該房直接開抉擇彈窗);時間軸每則可點 → 跳該租客房間;從動態進房,返回鍵變「← 回動態」。
- **未讀徽章**:`state.feedSeenMs`(入存檔,additive `?? 0`)記上次看動態的遊戲時間;導覽「動態」上紅badge=待決事件數、紫badge=未讀動態數;進分頁快照已讀點(NEW 標記基準)後標已讀,離開時再吸收停留期間湧入的。
- 測試:`scripts/feed-test.ts` 14 項(彙整規則/排序/cap/未讀/存檔往返);全部 33 腳本 + build 通過,balance 快照不變(Feed 唯讀)。

### R. 房間升級階梯(2026-07-11;設計檢討 7-1「投資遊戲性」第二項)
- `sim/upgrades.ts` 三種**一次性大額投資(不可退)**:🔇 隔音改建 $12k(隔音+8)/ ✨ 精裝修 $18k(品味+7 療癒+6)/ 📡 智慧家居 $15k(科技+8)。
- 投資回報三條路:**(1)** 永久房間屬性 → 應徵者星等(roomAttributes 疊加);**(2)** 應徵者租金行情 +12~18%(Applicant 新增 baseRent,rescore 即時反映);**(3)** 對在住租客談漲租容忍度 +5~7%(previewRent 疊加,上限 0.35)——改建完去談漲租,原本會翻臉的幅度變談得成。
- 在住房改建立即回饋:滿意+10/心情+8/清不滿倒數 + `[房間精裝修了]` 記憶(AI 可寫進日記)。
- 入口:房間細看「🔨 改建」+ 招租面板「🔨 改建」(空房先改建再招高星租客);新帳目分類 `upgrade`(財務面板「房間改建」)。
- 存檔:`upgrades` 欄位(additive、舊存檔缺 = 空)。
- 測試:`scripts/upgrade-test.ts` 18 項(含「+16% 漲租改建前被拒/改建後談得成」對照組)。

### Q. 記憶生命週期(2026-07-10;設計檢討 §6 工作項 5)
- `MemoryTag.intensity`(0~1,選配欄位,舊存檔缺 = 1,免遷移):新記憶 1 起跳,**每日依語意衰減**——強情緒(失戀/心碎/崩潰)0.09/天(約 11 天痊癒)、一般 0.05/天(約 20 天)、持續狀態(貓/寵物/同居/交往)不衰減(由分手/矛盾淡出/搬走清理收尾)。
- `memoryDrift` 乘上 intensity:記憶越淡影響越弱;歸零自動移除 + 「🍂 記憶漸漸淡了」日誌(與既有 🕊️ 矛盾淡出並存,一慢一快)。
- UI:記憶 chip 透明度隨強度變淡(生命週期可視化)。
- balance 基準已因此刻意更新(林小婕的[頻道破萬]漂移隨時間減弱,mood 97.6→94.2,同時改善黏高)。
- 測試:`scripts/memory-lifecycle-test.ts` 11 項全過。

### P. AI 連續性摘要 + 節流退避(2026-07-10;設計檢討 §6 工作項 4)
- **滾動摘要閉環**:`Tenant.recentSummary` 變成真正的「連載前情提要」——每日 narrate 把現有摘要餵給 AI(prompt 要求接續、伏筆推進),AI 回 `summaryUpdate`(50~150 字,前後端都夾 220 字)回寫,次日再餵回去。UI 的「📋 近期摘要(AI 記憶)」從此會隨劇情演進。
- **節流**:多位租客的 narrate 串行、間隔 4 秒(`NARRATE_GAP_MS`);**退避**:Worker 端 Gemini 429 → 等 5 秒重試一次,再失敗才 fallback 模板。
- 測試:`scripts/summary-test.ts`(mock fetch)6 項——含「次日 context 餵的是更新後摘要」的閉環驗證與節流間隔實測。

### O. 行為指令白名單(2026-07-10;設計檢討 7-2「讓 AI 事件看得見」)
- 新模組 `sim/directives.ts`:六種白名單指令,全部是既有 visualState/作息/props 的組合,AI 不能發明機制:
  `night_owl`(作息後移 3h)/`early_bird`(提前 2h)/`hermit`(不去交誼廳)/`social`(傍晚泡交誼廳看電視)/`adopt_cat`(房裡常駐貓+每晚 20 點逗貓)/`binge_watch`(22-23 點黏在電視前)。
- **走既有三道關卡**:指令掛在事件「選項」的 effect 上(`EventEffect.directive`)→ `sanitizeDirective` 白名單驗證 + days 夾 1~7 → 玩家抉擇拍板才生效。Worker prompt 已告知 Gemini 可用(附使用時機指引)。
- 套用在 `tick.decideState/applyHour`:作息位移查表、時段插入活動、目標改寫(hermit 交誼廳目標改回自房/social 自房休閒改去交誼廳);到期自動恢復 + 「恢復往常」日誌;生效當下立即重定位。
- 規則事件首例:生病事件「讓他自己休養」→ hermit 2 天(便宜不花錢,但他會閉門不出)。
- UI:房間細看標籤列最前顯示綠色指令 chip(例「🌙 作息大亂(熬夜中)· 剩 2 天」)。
- `TenantRuntime.directive` 入存檔(additive,load `?? null`);修 header 在窄螢幕被 🔔⚙️ 撐成兩行(nowrap+窄版藏 prototype 徽章)。
- 測試:`scripts/directive-test.ts` 18 項全過;balance 快照不變(未觸發指令時零影響)。

### N. 數值模型重整 + balance 快照(2026-07-10;設計檢討 §6 工作項 3、10 完成)
- **homeostasis 抗飽和**(`sim/tick.ts` applyStat):mood/stress 每小時朝「性格基準」回歸 6% 再吃活動增量,不再黏死 0/100。基準由 coreTags 決定(工作狂 stress+12、社恐+8、自律−4…),再疊三個回饋:**社交**(戀人 mood 基準+6/朋友+3/孤立−4,= socialFulfillment 簡版)、**健康**(wellbeing<30 → mood 基準−10)、**精力**(energy<25 → stress 基準+10)。affinity 是關係累積,不回歸。
- **hygiene → wellbeing(身心健康)+ 後果**:長期 stress≥80 或 energy<20 每小時蛀健康;wellbeing≤28 觸發**生病事件**(看醫生 $800 快好/送湯 $200/放著慢好);wellbeing 也有 1%/h 弱回歸避免黏 100。
- **新增 energy(精力)**:資源型——睡覺 +9/h 充電、工作/直播 −4、外出 −3…(`generate.ts` EFFECT 表);記憶漂移也接上(熬夜 −0.4、運動 +0.3)。
- **滿意度公式納入新軸**:0.25 mood + 0.3 affinity + 0.25 (100−stress) + 0.1 wellbeing + 0.1 energy,再乘房間需求滿足率。
- **存檔遷移層第一次實戰**:SAVE_VERSION 2→3,MIGRATIONS[2] 把舊檔 stats.hygiene 改名 wellbeing、補 energy;qol-test 實測 v2 舊檔匯入自動升級。
- UI:房間細看數值列加「精力/健康」兩條(共 7 條),顯示值取整;STAT_HELP 更新。
- **balance 快照測試**(工作項 10):`scripts/balance-test.ts` 固定種子(mulberry32)跑 10 遊戲日、事件自動選第一項,關鍵數值快照存 `scripts/balance-snapshot.json`;之後手感悄悄跑掉會直接 diff 現形,刻意調平衡用 `--update` 重建基準。
- 測試:新增 `stats-model-test.ts`(9 項);全部 23 個腳本 + build 通過。

### M. 調租談判(2026-07-10;設計檢討 7-1「投資遊戲性」第一項)
- 房間細看新增「💲 談房租」(僅承租人;同居者不付租談不了)→ `RentPanel.vue`:±5% 一格(-20%~+30%)、即時顯示提案金額與**模糊預感**(會感激/應會接受/有點冒險/八成翻臉)。
- 機制(`sim/tenancy.ts`):容忍度 = (滿意度×0.5+好感×0.5−35)/200(0~25%)。**降租**必成:好感/滿意↑、降 ≥10% 留 `[房東主動降租]` 記憶;**容忍內漲租**勉強接受:租金↑但好感/滿意↓;**超過容忍**拒絕:房租不變、關係惡化、不滿累積 +10、留 `[對漲租不滿]` 記憶(AI 可拿去寫劇情)。單次夾 ±30%,每人冷卻 5 遊戲日(成敗都算)。
- 新欄位 `TenantRuntime.rentChangeDay`(存檔相容:load `?? -99`);收租(月租/30)自動跟著新租金走。
- 測試:`scripts/rent-test.ts` 19 項全過。

### L. store 模組化 + 存檔遷移層(2026-07-10;設計檢討 §6 工作項 1、2 完成)
- **store 拆分**:`store.ts`(原 ~1100 行)拆成 7 個模組,`store.ts` 只剩 façade re-export,**所有元件/scripts 的 import 路徑不變**:
  - `sim/gameState.ts` reactive state + 共用常數/型別/小工具(notify/pushMemory/refreshAppearances…)
  - `sim/tick.ts` 每小時模擬(作息+偏離/事件/社交/換日)、syncToNow、快轉(同步版+分批版)
  - `sim/economy.ts` addMoney 唯一入口、收租/管理費、家具買/擺/移/賣
  - `sim/tenancy.ts` 招租/入住/退租/同居、房東抉擇 decide
  - `sim/narration.ts` 每日 AI 日記(之後的連續性摘要/narrate 佇列放這)
  - `sim/persistence.ts` 存檔/載入/遷移 + 匯出入
  - `sim/lifecycle.ts` initGame/掛機計時器/除錯鉤子
- **存檔遷移層**:`SAVE_VERSION` 常數 + `MIGRATIONS` 逐版升級表 + `migrateSave()`;壞檔/未知版本/未來版本安全 fallback 新遊戲;`importSave` 也走遷移(存「遷移後」的檔)。**之後改存檔結構:SAVE_VERSION++ 並在 MIGRATIONS 加一段升級函式即可,舊檔不會壞。**
- 純搬移、零行為變更;驗證:全部 17 個測試腳本通過 + `qol-test` 加測存檔往返(匯出→改壞 state→initGame 重載→還原)18/18 + build + wrangler dry-run。

### K. 操作 QoL(2026-07-10;設計檢討第 8 節六項全數完成)
- **8-1 家具移動**:家具資訊卡「📦 移動」→ 點地圖搬過去(免費、可跨房,房間屬性/招租星等跟著變);失敗留原位。`store.ts` pendingMove、`canPlaceFree` 支援排除自己。
- **8-2 擺放/移動預覽(確認制)**:點格先畫半透明 footprint(綠=可放/紅=不可),再按「✓ 確認」才成交扣款;紅色時確認鈕 disabled。`floorScene.drawFootprintPreview` + `store.canDropAt`。
- **8-3 通知中心**:header 🔔 → 通知歷史面板(`state.noticeLog`,cap 30、入存檔)。
- **8-4 存檔管理**:header ⚙️ → `SettingsPanel.vue`:重新開始(二次確認)/匯出存檔(剪貼簿,失敗退 textarea)/匯入存檔(驗 v=2 才寫入);匯入/清檔後封鎖 save 防止 reload 前被舊狀態蓋回。
- **8-5 數值說明**:房間細看五條數值 label 可點 → toast 一句話說明。
- **8-6 快轉體驗**:UI 快轉改分批(每批 5 tick、批間讓出主執行緒不卡畫面),按鈕顯示「快轉中…」;新增「⏩ 1 天」鈕(跨午夜觸發 AI 日記);同步版 `fastForward` 保留給測試腳本。
- 測試:`scripts/move-test.ts`(13 項)、`scripts/qol-test.ts`(16 項,mock localStorage 驗存檔管理/預覽判定/分批快轉)。

### J. 部署與自我檢測
- Cloudflare Worker + 靜態資產;`keep_vars=true`(部署不洗掉 Secret);push 即自動部署。
- `scripts/` 無頭測試:`sim-trace`、`stakes-test`、`social-test`、`ai-event-test`、`part-test`(跨租客/收支/時間)、`variety-test`、`bugfix-test` 等。

---

## 三、已修掉的 bug
- 房客插牆(站立點避牆、不瞬移)/ 家具無法指定位置(改點地圖擺放)/ 動態租客跑別人房間(只用自房+共用區)。
- **第四位租客撞色** → 改依房間(301-304)指派配色,保證不撞。
- **白天卻說「半夜泡麵」** → 模板句改時段感知({time}→早晨/午後/傍晚/深夜)。
- **AI 記憶不變動** → prompt 加強 + 去重上限;(註:實際 AI 版需 Gemini 額度未超額時才出現)。
- (2026-07-10)**同居後角色消失** → 原本直接把搬過去的人整個刪除;改為真同居(`state.cohabits`:住伴侶房、免一份租、關係保留;分手搬回空房或搬離;伴侶退租時接手承租;房間細看可切換查看同住者)。
- (2026-07-10)**退租後別人身上還留著他的 tag** → moveOut 清掉提到他的記憶標籤;親近者留一筆「搬走了」的失落記憶。
- (2026-07-10)**心情 100 卻掛 [情緒低落]** → 每日「矛盾記憶淡出」:數值已明顯走出該記憶方向 → 標籤移除 + 心境轉變日誌;補「低落/搬走」等漂移關鍵字。
- (2026-07-10)**招租應徵者刷新過頻** → 應徵者池存進 store,每遊戲日換一批(開關面板/重整不重抽),星等隨裝潢即時重算、排除同名。

---

## 四、待辦事項(依優先序)

### 讓 AI 影響更深(延續已做的 AI 抉擇)
- [x] **記憶標籤影響機制**:已由 `memoryEffects.ts` 讓失戀/熱戀/噪音/感激等記憶產生數值漂移與生命週期;養貓、行為指令也已成為實際模擬。新標籤若要改行為,仍需加入白名單規則,不可讓 AI 任意發明機制。
- [ ] AI 每日微調數值(隱性持續影響,需嚴格夾值)。

### 平衡與數值
- [ ] 平衡調整:事件頻率、滿意度門檻、管理費/租金、家具價格皆為暫定,需實玩微調。基準已由 `balance-test` 鎖住(改完記得 `--update`)。
- [ ] 數值模型延伸(設計檢討 §4 的「中」優先項):租客 wallet(繳租由財務決定)、完整 socialFulfillment 衍生值。

### 內容與呈現(美術債)
- [x] **房間細看重畫**:已改為跟拍樓層實景鏡頭,並補日夜光線、錯誤回報與 iOS Safari 畫布塌陷修正。
- [ ] 四方向走路 sprite、更多姿勢細分(cook_pair/stand_face)、音效/音樂。角色原型與家具目錄已擴充。

### 未來大型擴充(設計已寫好:docs/設計檢討與優化.md 第 9、10 節)
- [x] ~~sprite 部件化~~(§9-1 已完成:5 髮型 × 6 配件 × 多色,新租客隨機組合)
- [x] ~~特邀租客~~(§9-3 已完成:招租面板「✉️ 特邀」→ 名字+描述 → AI 生成入住;**僅限成年角色**,未成年描述前端快篩+AI isAdult 雙重拒收;canRomance 加 isAdult 把關)
- [x] 內建角色原型擴充至 15 種、作息擴充至 7 套。
- [x] **互動系統大改版**:已完成資料驅動 `InteractionDef`、`canInteract()` 單一把關、雙人 session/家具錨點、戀愛/親密/衝突/戲劇事件與娃娃屋演出層。🔞 模式預設關、僅限成年情侶、全程遮蔽式,AI 不生成露骨內容。

### 多人 / 帳號(視需求)
- [ ] **跨裝置存檔**:登入 + 雲端存檔(Cloudflare D1/KV)——目前存檔綁單一瀏覽器。
- [ ] 共享世界 / 互訪 / 排行榜(需帳號 + DB + 同步)。
- [ ] Gemini 免費層每分鐘/每日額度有限;多人同時玩或大量快轉可能超額→fallback。需要時可加節流或每人自帶 key。

### 系統/工程
- [x] 通知中心、存檔管理(重置/匯出/匯入)→ 2026-07-10 完成(K)。
- [x] 測試 runner + GitHub Actions CI;正式回歸集與 24h `sim-trace` 可用 `npm test` 一次執行。
- [x] Worker AI 端點同源檢查、16KB 請求上限、server 端 context 夾值;特邀租客使用 Gemini structured output。
- [ ] PWA(需真機驗證 service worker,採 `autoUpdate` 避免玩家卡舊版)。
- [ ] Cloudflare Dashboard `/api/*` 每 IP rate limit;目前只有同源與 payload 防護,尚未根治共用 Gemini key 額度風險。
- [ ] 新手引導、手機打包(Capacitor)。
- [x] **設計檢討 7-3 主序列完成**:store 模組化、存檔遷移、調租、數值模型、行為指令、AI 摘要/節流、記憶生命週期、房間升級、Feed、StoryArc、事件/作息資料化皆已完成。

---

## 五、已知小問題 / 技術債
- 數值已多輪調整並有 `balance-test` 快照護航,仍需依真機實玩持續微調。
- PWA 尚未做;存檔仍只在單一瀏覽器 `localStorage`,跨裝置需雲端帳號/同步系統。
- Gemini 與 Workers AI 都有共用免費額度;目前已會錯開、限次、跨供應商 fallback 並保存待補佇列,但多人同時玩仍可能把當日免費額度用完。
- `/api/*` 尚未設定 Cloudflare Dashboard rate limit;所有玩家仍共用同一把 Gemini key。
- `npm run build` 是 Vite production build,並不等於完整 Vue SFC 型別檢查;目前尚未加入 `vue-tsc`。
- `/api/invite` 的 Gemini structured output 已有無頭測試,但仍待使用正式金鑰做一次 live smoke test。
- 開發機瀏覽器自動化工具不穩,靠無頭測試 + 編譯檢查 + 線上 curl 驗證。

---

## 六、如何執行 / 驗證
```bash
npm run dev                      # 本地開發(http://localhost:5173)
npx tsx scripts/sim-trace.ts 24  # 無頭:走位健全性
npx tsx scripts/part-test.ts     # 跨租客事件 + 收支 + 時間 7×
npx tsx scripts/qol-test.ts      # QoL:擺放預覽判定 + 分批快轉 + 存檔匯出入/重載還原
npx tsx scripts/move-test.ts     # 家具移動(跨房/失敗留原位)
npx tsx scripts/rent-test.ts     # 調租談判(降租/容忍/拒絕/冷卻/同居者擋下)
npx tsx scripts/stats-model-test.ts  # 數值模型(homeostasis/生病/精力)
npx tsx scripts/balance-test.ts      # balance 快照 diff(刻意調平衡後 --update)
npx tsx scripts/variety-test.ts  # 文本庫變化 + 時段替換
npx tsx scripts/feed-test.ts     # 動態 Feed 彙整/未讀/存檔往返
npx tsx scripts/arc-test.ts      # 劇情弧消毒/開推收全流程/伏筆 flag
npm run build                    # 產出 dist/
npx wrangler deploy --dry-run    # 檢查 Worker 打包
# 部署:git push（Cloudflare 自動 build+deploy）
```

## 附:AI 供應商設定
- 執行時日記 AI = **Gemini 免費層 → Cloudflare Workers AI(Qwen3)免費額度 → 內建模板**。平淡日常優先用 `gemini-3.1-flash-lite`,事件機會或劇情弧等重要日用具免費層的 `gemini-3-flash-preview`。
- Gemini key 設為 Worker Secret `GEMINI_API_KEY`;Workers AI 使用 `wrangler.toml` 的 `AI` binding,不需要另放 key。日記路徑不會自動使用可能計費的 Claude。
- Fable(claude-fable-5)僅用於**製作階段**一次性生成文本庫(走 Claude 訂閱,非執行時)。

---

## 七、進度補記(2026-07-12～2026-07-14)

> 7/12～7/13 的詳細施工紀錄原先只寫在 `docs/交接與待辦.md`;本節將已上線的實際進度補回主工作日誌。後續以本檔作為 Codex 與 Claude Code 的共同更新紀錄。

### 2026-07-12 — AI 日記、寵物、工程體質與內容擴充

- **AI 日記錯開與佇列**:每位租客有獨立 `diaryHour`,一天中分時生成;佇列節流、429 重試與每日額度提示避免多位租客同時打爆免費層。
- **寵物貓系統**:貓會在房間、交誼廳與鄰居房遊走,觸發串門、打破東西、大小便及每週貓咪觀察筆記;應徵者可自帶貓,房間細看顯示寵物。貓砂盆與貓跳台會降低搗蛋機率。
- **測試與 CI**:`scripts/run-all.ts` + `npm test` 串起正式回歸集與 `sim-trace`;GitHub Actions 固定 `TZ=Asia/Taipei`,避免作息/換日時區造成假性紅燈。
- **Worker 防護**:`/api/narrate`、`/api/invite` 加同源檢查、16KB 上限與 context 夾值;invite 接 Gemini response schema。仍待 Dashboard rate limit 與 live invite smoke test。
- **成就與歷任房客**:新增成就冊、歷任房客名冊、退租代表記憶與入住天數。
- **互動/戲劇完整化**:完成家具門檻、雙人 session、坐/躺/hidden 姿勢、家具座位錨點、AI 提議互動、冷戰、打架、劈腿抓包、偷吃冰箱及遮蔽式成人互動。
- **共用設施與群體事件**:洗衣房、浴室 ambient 互動;公共設備、噪音裁決、樓層聚餐等有房東抉擇的三人以上事件。
- **經濟與 QoL**:每日淨現金流、備份提醒、入口門面/favicon、收租好感涓流、管理費調整及多輪 balance 快照更新。

### 2026-07-13 — 賺錢節奏與使用者回報修正

- **收入強化**:每存檔一次開辦補助 `$30,000`;新租客入住收一個月押金;投幣洗衣機依機台數 × 入住房數產生每日被動收入。
- **五項修正**:隔音後清噪音記憶、同居關係標記精確到正確配對、非伴侶不可共用淋浴、互動圖式維持到下一個動作、載入時清除不合法戀愛關係。
- **共浴地點修正**:成年情侶且開啟成人模式時,一起洗澡的遮蔽式演出改到共用淋浴間;非情侶仍維持單人使用。
- **朋友串門子**:朋友以上在休閒時段可拜訪彼此房間;新增房內聊天與合作遊戲互動,並接入既有尋路、家具條件及雙人演出。
- 本批完成時全套回歸與 `sim-trace` 為 35/35 綠,balance 因串門子的正向社交效果按預期更新。

### 2026-07-14 — Codex 接手盤點與雙 Agent 協作規則

- Codex 完整閱讀專案、Git 歷史、設計文件、交接文件、核心程式、資料、UI、Worker 與測試架構。
- 驗證結果:`npm test` 35/35 通過;`npm run build` 成功(108 modules);接手時 `main` 與 `origin/main` 同步、工作樹乾淨。
- 新增根目錄 `AGENTS.md`(Codex)與 `CLAUDE.md`(Claude Code):任何會提交的檔案變更,都必須先更新本工作日誌,並讓程式與日誌包含在**同一個 commit**;純閱讀/分析且沒有檔案變更則不需寫日誌。

### 2026-07-14 — 免費 AI 日記觸發率補強

- **掛機模板可補成 AI**:原本 `live=false` 生成的內建日記現在會留下可持久化的待補工作;回到前景後自動嘗試 AI,成功時原地替換同一篇文字,不會在 Feed 多出重複日記。
- **免費額度保護**:每位房客只保留最新一篇待補內容、每個瀏覽器工作階段最多補四篇,各次補寫錯開 60～90 秒;舊掛機內容不會把免費請求全部吃光。退租與讀檔時也會清掉失效工作。
- **免費模型分流與備援**:平淡日常走 Gemini 3.1 Flash-Lite,事件機會、伏筆或進行中的劇情弧走具免費層的 Gemini 3 Flash Preview;Gemini 失敗後改用 Cloudflare Workers AI `@cf/qwen/qwen3-30b-a3b-fp8`,兩者皆失敗才顯示內建模板。日記不使用可能計費的 Claude 備援。
- **同次請求提高事件機會**:context 新增 `eventDue`;事件冷卻滿三天時提示 AI 優先評估房東抉擇事件,不額外增加 API 呼叫。
- **玩家可看懂狀態**:日記/動態標籤會顯示實際來源(Gemini Flash、Gemini Lite、Workers AI),或「等候 AI」與內建原因(掛機補進度、離線、額度、格式異常等)。
- **回歸保護**:新增 `diary-upgrade-test.ts`,涵蓋佇列存檔、原地升級、每房客只留最新篇與失敗原因;Worker 測試增加事件欄位及模型分流。驗證結果:`npm test` 36/36、`npm run build` 成功、`wrangler deploy --dry-run` 成功並辨識 `env.AI` binding。
- **正式部署相容性修正**:commit `7529b13` 推上 `main` 後,線上 smoke test 發現 Gemini 2.5 Flash-Lite 已不再提供給新用戶,且 Workers AI Qwen3 已改回 OpenAI Chat Completions 的 `choices[].message.content` 格式;依 2026-07 官方文件改用 Gemini 3.1 Flash-Lite / 3 Flash Preview,並讓 Workers AI 同時解析新舊格式與要求 JSON mode。
- **正式部署完成**:相容性修正 commit `4cb2954` 已推上 `origin/main`,GitHub CI 成功、Cloudflare 自動部署完成;正式站 `/api/narrate` smoke test 成功回傳完整日記、摘要、劇情弧及 `provider: "gemini-flash-lite"`,確認 Gemini 3.1 Flash-Lite 線上路由可用。

### 2026-07-14 — 好友串門必定一起活動（Codex）

- **目的**:修正好友進入別人房間後仍各做各的,甚至出現主人睡覺、訪客發呆的割裂演出。
- **配對時機**:`tick.ts` 保留原本逐人擲骰時序,但等全員本小時作息確定後才安排拜訪;訪客必須處於休閒狀態,主人必須在家、清醒且可被打斷,睡覺、外出、洗澡、崩潰或有待決事件時不接受拜訪。好友門檻與 `InteractionTier.close` 統一為關係值 50。
- **共同活動保證**:`interactions.ts` 在拜訪前先確認聊天或合作遊戲已解鎖且不在冷卻;拜訪成立後雙方暫停原單人活動,鎖定正確主人並必定建立一小時雙人 session。串門配對優先於同房其他互動,且每人每小時最多一場,避免演出被第三人覆蓋。
- **狀態與載入**:`TenantRuntime` 新增不入存檔的 `visitHostId`,讀檔補為 `null`;待決事件會清除殘留的拜訪狀態。
- **回歸**:`fixes-test.ts` 增加正確主人、雙方清醒及共同 session 斷言(12→15 項)。`npm test` 36/36、`npm run build` 成功、`sim-trace` 與 `balance-test` 通過;刻意保留既有亂數呼叫次序,因此不需更新平衡基準。

### 2026-07-14 — 雙人姿勢與冷戰摔門演出（Codex）

- **目的**:依使用者決定先強化雙人遊玩的畫面表現,多人／雲端功能暫不列近期優先;補完原待辦的 `cook_pair`、`stand_face` 與摔門絕交演出。
- **面對面聊天**:`PairPose` 新增 `stand_face`,交誼廳一般相遇與朋友房內聊天改為水平相鄰、朝向彼此;agent 將朝向傳給 `floorScene.ts`,以眼神／鼻尖像素提示面向。
- **一起料理**:`cook_dinner` 改到交誼廳共用流理臺,新增 `cook_pair` 姿勢與鍋鏟像素;`furnitureStandingPair()` 依家具 footprint/interact 反查檯前兩個可走格,流理臺不存在或站位受阻時不硬觸發。
- **冷戰退場**:`startFeud()` 會中止原本相遇並以 `apart` session 讓兩人各自走回住處,房門出現無音效也看得懂的震動／驚嘆號 `slam` FX;打架路徑保留既有打鬥雲優先,避免新退場蓋掉遮蔽式演出。
- **主要檔案**:`src/floor/{pairSession,agents,floorScene,fx}.ts`、`src/sim/{interactions,tick,conflicts}.ts`;測試擴充 `pair-session-test.ts` 29→36、`conflict-test.ts` 34→36。
- **驗證**:針對性 `pair-session/conflict/interactions/sim-trace` 全過;`npm test` 36/36、`balance-test` 與 `sim-trace` 通過;`npm run build` 成功(108 modules)。純演出改動不影響數值與亂數順序,不需更新 balance 基準。Build 曾在接續全套測試後遇一次 Windows 目錄權限暫時錯誤,單獨重跑立即成功,未重現。

### 2026-07-14 — 內建遊戲內容大幅擴充（Codex）

- **目的**:讓兩人遊玩時即使沒有 AI 額度,每日觀察、日記、鄰居相遇、雙人活動與公共事件仍有足夠變化,減少短期內看到相同台詞。
- **觀察與日記**:`observationLines.ts` 的 17 種活動各由 10 增至 13 句(共 221 句);一般每日總結增至 24 句,另新增高壓、低落、開心、社交各 6 句的狀態池(每日總結共 48 句)。`templateDiary()` 依數值與關係混入適合情境,仍只抽一次亂數。
- **人際內容**:`interactions.ts` 的 13 種雙人活動全部至少 3 句(共 42 句),包含一起料理、串門聊天、合作遊戲、談心、情侶日常與遮蔽式成人互動;`social.ts` 的一般／戀愛／衝突相遇共擴充至 39 種說法。
- **公共事件**:`community.ts` 為洗衣房、浴室尖峰、團購、噪音公審與頂樓乘涼加入共 41 條情境變體;文案依遊戲日與參與者決定,不額外消耗 `Math.random`,因此不影響既有觸發率和平衡序列。
- **事件連鎖**:`data/events.json` 新增兩條玩家選擇後續:高壓事件選「給他空間」會留下「壓力自己扛」,恢復後觸發關係追蹤;低滿意事件只口頭承諾會留下「答應改善房間」,若仍未改善便收到住戶追問。兩者皆消耗旗標,不會重複觸發。
- **回歸保護**:新增 `content-variety-test.ts`(13 項)並加入 CI,鎖住句庫下限、情境日記選擇、雙人文案數與成人遮蔽規則;`data-catalog-test.ts` 27→33,另修正 `ai-interaction-test.ts` 不再綁死特定台詞,改驗證雙方實際新增互動日誌。
- **驗證**:`npm test` 37/37、`balance-test`、`sim-trace` 全通過;`npm run build` 成功(108 modules)。本批不增加執行時 AI 呼叫、不改事件觸發率,平衡快照無漂移、不需更新基準。

### 2026-07-14 — 每週生活報告卡（Codex）

- **目的**:在不增加 AI 費用、也不依賴多人功能的前提下,把每天分散的金流與住戶故事整理成可回顧的遊戲進度,讓兩人遊玩時更容易看見一週內發生的變化。
- **每七日自動結算**:`weeklyReportPass()` 於遊戲日第 7、14、21 日等日界線產生報告;統計該七日收入、支出與淨額,從既有動態挑出最多三則重要事件,並比較關係快照列出最多三組好感升降。報告只讀取既有結果,不呼叫 AI、不改角色數值或事件亂數順序。
- **動態頁呈現**:`FeedPanel.vue` 在一般動態上方顯示最近四週的可展開報告卡,最新一週預設展開;摘要直接顯示週次、日期範圍與收支正負,內文分成財務、重要事件與關係變化。新報告也會計入原本的 Feed 未讀徽章。
- **存檔相容性**:新增報告歷史、上次結算日與關係快照三個可選欄位,最多保留 12 週。舊存檔載入時以當前遊戲日和現有關係建立基準,不會立即捏造過去一週或誤報歷史關係變化;不需提高 `SAVE_VERSION`。
- **回歸保護**:新增 `weekly-report-test.ts` 15 項並加入 `scripts/run-all.ts`,涵蓋七日門檻、週內金流、重要事件篩選、關係差額、同日防重複、12 週上限、未讀數、存檔往返與舊存檔基準。
- **驗證**:針對性 `weekly-report/feed/finance/qol/sim-trace` 全通過;完整 `npm test` 為 38/38;`npm run build` 成功(109 modules)。曾在測試與 build 串接執行時遇一次既有 Windows/esbuild 目錄權限暫時錯誤,單獨重跑 build 立即成功。內建瀏覽器於本工作階段不可用,因此手機畫面以 Vue SFC 正式建置通過驗證,尚未進行自動點擊測試。

### 2026-07-14 — 防止第三人同居申請覆蓋原同居（Codex）

- **問題**:角色可以同時和不只一人成為伴侶,但接受第三人的同居申請時會直接改寫 `state.cohabits[租客]`,導致原本的同居對象與房間在沒有分手的情況下被蓋掉。
- **一對一同居規則**:新增共用的 `cohabitingPartnerId()` 與 `canStartCohabit()`,同時辨識「搬進別人房」和「讓別人搬進自己的房」兩個方向。多段戀愛關係仍可保留,但只要任一方已有同居對象,就不能再提出新的同居申請。
- **雙層防護**:`tick.ts` 在產生申請前先檢查雙方狀態;`resolveCohabit()` 在玩家按下同意時再次檢查,防止待決視窗期間狀態改變造成競態。失效申請只會取消並通知原因,不改住處、不強制分手,且保留日後重新申請的機會。
- **UI 規則統一**:`RelationshipsPanel.vue` 改用同一個模擬層判定顯示「同居中」,避免關係頁和實際住處對同居配對有不同解讀。
- **回歸保護**:新增 `cohabit-integrity-test.ts` 12 項並加入 CI,涵蓋同居房主與搬入者兩個方向、第三人原承租房、原同居映射、其他戀愛關係與取消提示。針對性四支測試全過;完整 `npm test` 為 39/39;`npm run build` 成功(109 modules),balance 與 `sim-trace` 無漂移。

### 2026-07-14 — 日常坐躺像素姿勢與家具四方向旋轉（Codex）

- **日常姿勢正式接入**:原本只供少數雙人互動使用的 `CHAR_SIT`／躺姿,現在會依每小時作息套到單人活動。睡床、睡沙發會先尋路到家具旁再跨上家具格躺下;看書、沙發休閒與逗貓會坐在座位上;桌前工作、直播、打電動與看電視則顯示坐姿並在角色背後補畫工作椅。
- **像素美術方向**:躺姿會跟著床的 0°／90°／180°／270° 方向旋轉,仍使用現有角色髮色、膚色與衣服色辨識本人。家具商店新增可購買的「木質單椅」與專屬像素圖,椅背、椅面、椅腳和接地陰影均延續原家具風格。
- **家具旋轉操作**:購買擺放或既有家具「移動／旋轉」時,頂部操作列新增 `↻` 按鈕,每次順時針轉 90°;預覽框會交換寬高並顯示方向箭頭,確認後才扣款或改動原家具。家具資訊卡會顯示目前角度與旋轉後尺寸。
- **規則完整旋轉**:`Placement.rotation` 為可選欄位,舊存檔缺少時自動正規化為 0°。畫面、佔位、點擊命中、牆／家具碰撞、尋路、作息互動點、雙人沙發座位與流理臺並肩站位全部共用 `rotation.ts` 的尺寸／座標轉換,避免只轉圖片卻仍用舊碰撞。
- **平衡保護**:新姿勢與旋轉不消耗亂數、不改角色數值;懶骨頭沒有納入自動作息選位,避免陳家豪因此留在房內而改變既有交誼廳社交節奏。`balance-test` 快照無漂移。
- **回歸保護**:新增 `rotation-pose-test.ts` 21 項並加入 CI,涵蓋角度正規化、旋轉尺寸／互動點／碰撞、擺放搬動、雙人站位、床／沙發／桌前姿勢與新單椅;`sim-trace` 更新為辨識合法家具姿勢,仍會回報其他未抵達問題。完整 `npm test` 40/40、`npm run build` 成功(110 modules)。本工作階段沒有可用的內建瀏覽器,因此手機自動點擊測試未執行,Vue 正式建置與元件回歸均已通過。

### 2026-07-15 — 隔音抗議一致性與洗衣間實際演出（Codex）

- **隔音不再只是面板數字**:新增 `acoustics.ts` 作為規則事件與 AI 敘事的共用聲學判定。`$12,000 隔音改建`完成後,一般噪音公審與噪音裁決不再把該房住戶選為當事人;隔音窗簾等家具屬性也會抵銷住戶標籤與設備噪音。
- **事件隔音永久化**:噪音裁決的`$3,000 花錢做隔音`會在當事人房間留下可存檔的`噪音改善工程`（隔音 +4、無租金加成）,不再只清掉當下記憶。舊存檔中已掛起、尚未帶新欄位的噪音裁決仍會依事件／選項 id 補上永久工程。
- **AI 尊重房間現況**:每日敘事 context 新增噪音、隔音、永久工程及抗議風險;Worker 端完成夾值並在 prompt 明確禁止隔音已阻隔時再生成室內噪音抗議。舊版 context 缺欄位時保守視為仍有風險,避免部署交界或待補日記被誤判成已隔音。
- **洗衣事件真的看得見**:洗衣房友善聊天／搶洗衣機事件會尋找兩台洗衣機前的合法站位,把兩位清醒住戶帶到現場建立一小時`stand_face` session,並在實際位置顯示聊天或生氣 FX;睡覺、淋浴、崩潰或焦慮踱步者不會被半夜硬拉去洗衣。
- **日常洗衣作息**:`routine`新增`laundry`角色並限定使用真正洗衣間的洗衣機／曬衣架。每位住戶約每四個遊戲日會在原本清醒、在家的時段去洗衣一次;時段由穩定雜湊決定、不消耗 RNG,數值效果沿用被取代的原作息,因此只增加可見內容而不改長期平衡。`using_appliance`新增 13 條洗衣觀察文案。
- **回歸與建置**:新增`acoustics-laundry-test.ts` 17 項,涵蓋兩種永久隔音、存檔、舊待決事件相容、AI context、洗衣站位／session及四日作息;Worker 測試擴充至 31 項。完整`npm test` 41/41、`balance-test`快照與`sim-trace`通過;`npm run build`成功(111 modules,`index-DZK2BPB8.js`)。

### 2026-07-15 — 唯一伴侶制與多重戀情存檔修復（Codex）

- **問題根因**:五位房客可出現五組情侶並非單純負面事件太少;原本自然相遇達 75 就自動交往,沒有檢查任一方是否已有伴侶。第三人一旦也被標成`romantic`,既有`affairPass`反而不再把他視為曖昧第三者,形成多段戀情免疫修羅場的漏洞。
- **正式伴侶唯一制**:`social.ts`新增`romanticPartnerId()`與`canBecomeCouple()`,並由`setCouple()`統一把關成年／取向與雙方現有伴侶。自然相遇及 AI 事件的`couple:true`都走同一入口,任何一方已有其他正式伴侶時都不能建立第二段戀情。
- **第三者仍保留劇情**:已有伴侶者和第三人跨過 75 時,關係值會保留並顯示「曖昧」,不會直接成為第二位情侶。因第三人維持非正式戀情,原有 4% 修羅場風險現在能正確抓到;AI 嘗試撮合被擋時也會替雙方留下「已有伴侶、停在曖昧」日誌,避免選項文字與實際狀態矛盾。
- **舊存檔自動整理**:`load()`會以一對一 matching 修復既有多重戀情。同居配對優先保留;沒有同居衝突時按關係值由高到低保留。其餘關係只解除`romantic`並重置同居申請旗標,原關係值不清空,因此會自然降回曖昧而不是失憶或強制歸零。五位在住房客同時最多兩組正式情侶。
- **回歸保護**:新增`romance-integrity-test.ts` 17 項,涵蓋雙向唯一伴侶、自然跨線、修羅場、AI couple、同居優先、最高關係保留與五人上限;`cohabit-integrity-test.ts`更新為 13 項的新規則。完整`npm test` 42/42、`balance-test`與`sim-trace`通過;`npm run build`成功(111 modules,`index-ByqExKiw.js`)。

### 2026-07-15 — 睡床姿勢方向與置中修正（Codex）

- **問題根因**:床的 0° 圖面是床頭朝上,角色躺姿的 0° 圖面卻是頭朝左,先前直接共用家具角度會讓角色橫躺。尋路又刻意選最靠近走道的床格,抵達後也沿用該格繪製,因此角色看起來縮在床角。
- **方向校正**:只有睡床躺姿會在家具角度上額外順時針 90°,確保 0°／90°／180°／270° 四種床向都讓角色頭部貼近床頭;睡沙發維持原本方向,不受床圖基準差影響。
- **整床置中**:行走與跨上家具仍使用可尋路的整數床格,抵達後則依旋轉後完整 footprint 計算像素位移,把角色置中在整張單人床或雙人床。睡眠 Zzz 也跟著姿勢位移,不會留在原床角。
- **回歸與建置**:`rotation-pose-test.ts`由 21 項擴充為 26 項,新增床頭方向、整床中心及四方向旋轉驗證,26/26 通過;完整`npm test` 42/42 通過（含 balance、`sim-trace`與 Worker 31 項）,`npm run build`成功(111 modules,`index-CcMPTp5s.js`)。

### 2026-07-15 — 摯友稱呼與劈腿雙重背叛（Codex）

- **高好感不再一律曖昧**:`tierLabel()`會連同雙方性別／戀愛可能判定關係。75 以上且不走戀愛線時,兩位女性顯示「閨密」、兩位男性顯示「哥們」,其他組合顯示「摯友」;只有具戀愛可能的高好感非情侶才顯示「曖昧」,因此第三者判定仍維持原規則。
- **摯友里程碑**:非戀愛朋友第一次跨過 75 時會產生「成了無話不談的閨密／哥們／摯友」重要日誌與通知。關係面板拆成情侶、曖昧、摯友、朋友與認識中五組,房間抬頭、每日 AI 敘事 context、生活週報也共用同一稱呼。
- **劈腿雙重背叛**:修羅場若第三者原本是被劈腿者的摯友,兩人的朋友好感額外扣 70（例如 96 直接跌到 26）,雙方新增`[摯友背叛]`／`[背叛摯友]`記憶並進入冷戰。抓包日誌會點明對象竟是自己的閨密／哥們／摯友,不再把主動越界描述成「莫名捲入」。
- **回歸與建置**:`romance-integrity-test.ts`由 17 項增至 20 項,新增閨密、哥們與跨線里程碑;`drama-test.ts`由 27 項增至 31 項,驗證好感重挫、雙方記憶、冷戰與抓包文案。完整`npm test` 42/42 通過（balance、`sim-trace`無漂移）,`npm run build`成功(111 modules,`index-DM09UhBa.js`)。

### 2026-07-15 — 浴室家具日常、排隊與現場劇情擴充（Codex）

- **家具不再只是擺設**:修正原本`bathroom`作息永遠選到清單第一座淋浴間的問題,現在淋浴、馬桶、洗手台與浴缸會依`showering`、`using_toilet`、`washing_at_sink`、`taking_bath`精確選位。馬桶／洗手台／浴缸的家具目錄也會標示各自解鎖的活動;沒有擺浴缸時會合理退回淋浴,不會走向不存在的目標。
- **四種浴室生活輪替**:每個原洗澡時段會依住戶、日期與小時穩定輪替成淋浴、如廁、刷牙洗臉或泡澡,不消耗模擬 RNG。新活動沿用原洗澡數值效果,因此增加可見內容但不改長期平衡;新增的排隊狀態則會反映少量壓力與體力消耗。
- **坐躺與環境像素**:如廁時角色會跨上馬桶使用坐姿,泡澡會躺進浴缸,洗手台有流水、浴缸有泡泡與蒸氣,門外排隊會顯示等待提示。主樓層、房間預覽、舊版房間視圖與狀態標籤均辨識新狀態,避免不同畫面顯示不一致。
- **真正的設備占用**:共用浴室改為逐件家具判定占用,不同設備及兩座同型但座標不同的設備可同時使用;同一座設備已有住戶時,下一位會走到對應浴室門外排隊,不再重疊在同一格或被送回房間發呆。
- **浴室事件現場化**:原本只有中央特效的浴室友善／衝突事件,現在會把兩位清醒住戶安排在浴室門口面對面聊天或爭執,睡覺與崩潰者不會被硬拉入場。友善與衝突文案各增至 6 組,涵蓋搶廁所、洗手台鏡子、熱水、毛巾與互相整理儀容。
- **免費內容擴充**:四種新浴室狀態各有 13 句本地觀察,共新增 52 句,整體每小時觀察句庫增至 286 句;內容完全在本機選取,不新增 AI API 呼叫或費用。
- **回歸與建置**:新增`bathroom-life-test.ts` 20 項並加入 CI,涵蓋狀態／句庫、精確家具、四日輪替、馬桶坐姿、浴缸躺姿、同型設備識別、門外排隊與浴室事件演出;相關舊測試放寬為接受新浴室情境。完整`npm test` 43/43 通過（含 balance、`sim-trace`及 Worker 31 項）,`npm run build`成功(111 modules,`index-BmsPJllC.js`)。

### 2026-07-15 — 房東主動協議解約與強制請離（Codex）

- **租約管理入口**:房間細看原本的「談房租」升級為所有在住者都能開啟的「租約管理」。承租人仍可在同一面板調租;不另付租的同居者也能被單獨請離。面板限制為`92vh`並可垂直捲動,適合手機底部操作。
- **協議解約**:玩家可支付該住戶一個月租金作為搬遷補償,現金不足時按鈕會停用且規則層再次拒絕。支出會進收支帳,其他住戶數值不受影響;搬走原因與補償金額會記入歷任房客名冊。
- **強制請離後果**:玩家也可不付補償直接請離,但所有留下住戶會各扣房東好感 8、滿意度 10、心情 4,壓力增加 4,並累積 12 小時退租不安。每位住戶會留下目睹請離的記憶及重要日誌,讓每日敘事與後續退租風險能延續這次決定。
- **雙重確認與後果預覽**:兩種請離都必須先選方式、再按一次確認;確認區會先顯示費用、房間是否空出、同居者是否接手租約及全樓信任代價。完成後自動回樓層,避免停在已不存在的住戶頁面。
- **沿用完整搬離清理**:`evictTenant()`統一包裝既有`moveOut()`,因此寵物、關係、同居映射、待補 AI 日記、待決同居、房間占用與歷任名冊皆沿用既有安全清理。若請離的是承租人且有同居者,同居者會接手房間及租約;AI 事件的`evict`仍維持消毒禁用,只有玩家二次確認能執行。
- **回歸與手機驗證**:新增`eviction-test.ts` 19 項並加入 CI,涵蓋金錢不足、補償支出、旁觀者數值、同居接手、目前角色切換、記憶／日誌及名冊原因。完整`npm test` 44/44 通過（含 balance、`sim-trace`與 Worker 31 項）。Mobile UI Lab 三種手機尺寸共產生 18 張基準截圖,報告為 0 errors／0 warnings;另以 390×844 實際點開租約管理與兩種確認狀態,頁面無錯誤且操作區完整可捲動。正式建置成功(111 modules,`index-CU5IZr3Y.js`,`index-Ce264Zp-.css`)。

### 2026-07-15 — 共用 Mobile UI Lab 接入（Codex）

- **目的**:讓 Codex CLI、Claude Code CLI、桌面 Agent 與開發者本人共用同一套可重現的手機 UI 建置、啟動、截圖與錯誤檢查流程,並同時支援本專案與 `NBA/nba-dream-gm`。
- **專案規則**:`AGENTS.md`與`CLAUDE.md`新增共用 UI Lab 指令與完成條件;工具本體放在兩個 Git repo 外的`C:\Users\User\claude_try\.ui-lab`,產物也寫到外部`artifacts/ui-lab/rent`,不污染本 repo。
- **視覺情境**:Playwright 以 390×844、412×915、430×932 三種直式手機尺寸檢查樓層、動態與收支面板,同時收集 page error、console error與核心資源失敗;每個畫面保留 viewport 與 full-page 截圖。
- **驗證**:`npm run ui:test -- all`成功;本專案在 Linux container 內`npm ci`與`npm run build`通過（Vite 6.4.3、111 modules、`index-CU5IZr3Y.js`）,產生 18 張租屋遊戲截圖,`artifacts/ui-lab/rent/report.json`為 Errors 0。人工檢視 390×844 的樓層與收支畫面正常。這次只改協作文件,未改遊戲邏輯或平衡,不需更新 balance 快照。

### 2026-07-15 — 廚房與冰箱家具生活劇情包（Codex）

- **目的**:依使用者希望優先豐富家具互動與劇情的需求,把原本主要提供作息站位／屬性的冰箱、瓦斯爐、流理臺、餐桌與咖啡機接成免費本地事件;不新增 AI API 呼叫或費用。
- **冰箱兩幕連鎖**:`community.ts`新增「冰箱食物失蹤」劇情。第一幕會依關係演成分享食物,或由失主懷疑另一位房客並讓關係、心情受影響;衝突分支會在雙方留下互相對應的伏筆旗標。下一次冰箱事件會鎖回同一對當事人,以找到食物、拿錯保存盒、補買等四種真相收束,消耗雙方旗標並修復部分關係,不會突然換人或永遠懸而未決。
- **共同料理與廚房口角**:只有交誼廳同時存在瓦斯爐、流理臺及餐桌時才可能觸發。關係良好的兩人會一起切菜、顧鍋、清冰箱或包餃子,增加關係、心情並降低壓力;關係低於 35 時則可能為調味、髒鍋、公共食材與廚房動線爭吵,讓關係下降、壓力上升。文案分成雙方視角,不再只是兩人看到完全相同的一句話。
- **咖啡機小日常**:咖啡機新增替對方多沖一杯、記住口味、交換行程與安靜和解等四種低強度善意;每次只小幅增加關係,讓不熟或剛有小尷尬的房客能自然回溫,避免所有感情變化都靠大型事件。
- **家具現場演出**:三類事件都會先確認對應家具實際存在,再從旋轉後家具互動格尋找相鄰合法站位。當事人會中止原活動並走到冰箱、流理臺或咖啡機前,以`stand_face`／`cook_pair`搭配聊天或生氣 FX 演出一個遊戲小時;睡覺、如廁、洗澡、崩潰與焦慮踱步者不會被硬拉進廚房。
- **回歸保護**:新增`kitchen-life-test.ts` 23 項並加入完整 runner,涵蓋三種家具可達性、家具條件、伏筆配對鎖定／消耗、關係與日誌效果、友善／衝突分支及實際雙人 session。專項`community/content-variety/kitchen` 3/3 支通過;完整`npm test`為 45/45 支通過（含 balance 與`sim-trace`）,`npm run build`成功（111 modules,`index-omIwPJDu.js`）。本批刻意沿用既有事件頻率與數值範圍,balance 快照無漂移、不需更新;沒有修改 UI,因此不需重跑 Mobile UI Lab。

### 2026-07-15 — 修復每日 AI 實際失效與待補佇列（Codex）

- **正式站診斷**:直接 POST 已部署的`/api/narrate`重現`502 upstream`。回傳細節顯示 Gemini 因 Worker 出口位置遭`User location is not supported for the API use`拒絕,原本第二順位的 Workers AI 又被前端記為`empty response`,所以即使玩家不快轉,每篇即時日記仍只能落到內建模板。畫面上的「等候 AI」其實是失敗後排入待補,不是請求持續生成中。
- **Workers AI 改為免費主力**:依 Cloudflare 官方模型／JSON Mode 文件調整 provider 順序。先使用較適合繁中的`@cf/qwen/qwen3-30b-a3b-fp8`並靠既有 prompt 產生 JSON（不再對未列入保證清單的 Qwen 強開 JSON Mode）;失敗時改用官方支援 JSON Mode 的`@cf/meta/llama-3.1-8b-instruct-fast`;兩個 Workers AI 都失敗才嘗試 Gemini,避免每篇先撞已知地區限制。
- **修正空回應誤判**:`extractWorkersAiText()`不再用空`response`遮掉有效的 OpenAI`choices`;同時支援舊字串、content parts、OpenAI choices及 JSON Mode 直接回物件四種官方格式。物件型`response`會序列化後交給既有`parseResult()`消毒,不再把成功結果當成空字串。模型來源新增`Workers Qwen`／`Workers Llama`標籤。
- **待補會自行運作**:玩家一直停留在遊戲前景時,App 現在每 3 個現實分鐘自動啟動一輪,每輪最多補 2 篇且篇間仍錯開 60~90 秒;重新開頁、切回前景與網路恢復也會立即嘗試。補寫預算由原本「整個瀏覽器工作階段固定 4 次」改為每個遊戲日 6 次並隨換日重置,足以涵蓋五位房客,又保留免費額度防暴衝。
- **狀態文字誠實化**:房間日誌與全樓動態把容易誤解的「等候 AI」改成「待補 · 實際原因」,例如免費額度已滿、目前離線、AI 暫時無回應或格式異常;玩家看到的文字仍是可讀的內建日記,不是空白載入狀態。
- **回歸與打包**:`worker-test.ts`擴至 33 項,新增 JSON 物件與空 response／有效 choices 驗證;`diary-upgrade-test.ts`擴至 13 項,驗證預算用盡不白打及每日重置後恢復。完整`npm test`為 45/45 支通過（含 balance、`sim-trace`）,`npm run build`成功（111 modules,`index-Bnp9tyun.js`,`index-8lsAuZJN.css`）,Wrangler 4.110.0`deploy --dry-run`成功並辨識 AI／ASSETS bindings。Mobile UI Lab 三種手機尺寸共 18 張截圖、0 errors／0 warnings,已逐張檢視;本批不改數值平衡,不需更新快照。正式端點仍需在 commit／push／部署後以線上 Worker 再驗證一次。

### 2026-07-15 — 雙貓互動與戀人家具內容包（Codex）

- **雙貓不再各自遊蕩**:`pets.ts`新增追逐、互相理毛、靠著共眠、地盤對峙與聯手搗蛋五種事件。同區域可自然觸發;沒有巧遇時也有低機率主動找貓伴,同一對有 10 遊戲小時冷卻且每小時最多一場,避免日誌洗版。兩位飼主都會收到`🐾 雙貓互動`重要日誌,事件依內容小幅影響心情、壓力、飼主關係或現場清潔度。
- **雙貓同步像素演出**:`Pet`保存短期配對對象、動作與到期時間;`petAgents.ts`讓追逐中的後方貓跟隨領頭貓,理毛／共眠／對峙／搗蛋則由一隻靠近另一隻。共眠會讓兩隻一起捲起睡,領頭貓上方另有依事件變化的像素泡泡,到期後自動回到原本的獨立遊蕩。
- **三件戀人家具**:家具商店新增`戀人雙人沙發`、`帷幔雙人床`與`雙人約會餐桌`,分別有專屬沙發飾邊、床柱帷幔、燭光餐桌像素細節。家具會解鎖戀人依偎、雙人約會晚餐、帷幔枕邊話等一般內容;帷幔床也能完整替代雙人床承接賴床與原有親密夜晚。
- **成人內容標記與硬規則**:新增`沙發私密時光`及`帷幔私密夜晚`兩項含蓄、遮蔽式成人互動。所有成人日誌改由互動執行層統一加`🔞`圖式標記,家具商店的解鎖標籤也直接顯示`🔞`;成人模式仍預設關閉,且必須雙方成年、可戀愛、正式伴侶及沒有第三人在場,畫面維持關燈／隱藏角色而無露骨圖像,AI 白名單未加入成人互動。
- **回歸、建置與部署準備**:`pet-test.ts`擴至 48 項,驗證五種雙貓事件、雙方日誌、冷卻與 agent 同步;`interactions-test.ts`擴至 31 項,驗證三種家具門檻、成人模式／第三人防線及所有成人日誌都有`🔞`。完整`npm test`為 45/45 支通過（balance 快照與`sim-trace`無漂移）,`npm run build`成功（111 modules,`index-Ddvv2EPP.js`）。Mobile UI Lab 三種手機尺寸共 18 張截圖、0 errors／0 warnings並已逐張檢視;額外互動式瀏覽器本工作階段沒有可用後端,因此未再手動點開商店清單。本批沒有刻意調整全局平衡快照;由`main`push 觸發正式部署,線上驗證結果於交付回報確認。

### 2026-07-16 — 貓咪互動美術與樓層人物防重疊（Codex）

- **五種互動真正畫出來**:原本領頭貓上方的單一小泡泡改為五套無文字像素演出。追逐會帶速度線與灰塵、理毛會浮出愛心與舔毛線、共眠會共享縫線軟墊與 Z 字、地盤對峙會炸毛並出現鋸齒火花、聯手搗蛋會拆開紙箱並彈出紙屑星芒。兩隻貓仍依 y 軸和人物混排,共享道具先鋪底、前景特效最後畫,不會把任一隻蓋成一團。
- **美術構圖參考**:使用內建 imagegen 建立橘貓／黑貓五幕像素互動概念,保存於`docs/art-reference/cat-interactions-concept.png`;實際遊戲沒有直接縮放大圖,而是依既有 16px Canvas 限色像素系統重畫,避免抗鋸齒與畫風突兀。`scripts/render-cats.ts`同步加入五組固定配對,可離線輸出一次目檢所有動作。
- **人物目標與路徑防重疊**:`agents.ts`每幀先為相同活動錨點分配鄰近空格,人物建立時也不再生成在同一格;移動途中會預約下一格並避開仍被他人佔用的格位,窄走道不再讓兩條路徑同時穿進一個 sprite。坐床／沙發等家具跨格演出保留,但跨上前也會再確認沒有其他人物。
- **標籤碰撞排版**:新增`floor/tagLayout.ts`,依人物／貓咪名稱估算膠囊寬度,在相鄰時依序左右錯位與向上堆疊,並夾在 canvas 邊界內;因此即使角色靠得很近,姓名也不再完全互蓋。
- **回歸與手機驗證**:`rotation-pose-test.ts`由 26 項擴至 29 項,新增同目標初始站位、每幀 goal 與人物／貓咪標籤錯位驗證;`pet-test.ts`48/48 通過。完整`npm test`為 45/45 支通過（含 balance 與`sim-trace`）,正式`npm run build`成功（112 modules,`index-DjussC_q.js`）。Mobile UI Lab 三種手機尺寸共 18 張新截圖,`report.json`為 0 errors／0 warnings,連同既有退租流程圖皆已逐張目檢;主手機截圖的人物、貓咪、標籤與面板無裁切或覆蓋。

### 2026-07-16 — AI 當日觀察去流水帳與去重（Codex）

- **根因修正**:上一個遊戲日的 AI 當日觀察原本可能在相同日記時刻再次落入 24 小時素材窗,讓模型摘要自己的舊摘要;同日完全相同或高度相似的事件也會全部送出。現在 context 明確排除`daily`日誌,素材與決策備註各自做近似去重並限制數量,只把具差異的原始觀察依時間順序交給模型。
- **提示詞改成主線敘事**:Worker 要求從素材挑 2～3 個彼此相關、最有意義的畫面,而非逐條交代;禁止重述開頭、重複「他還／她還／看起來」及「沒有太大變化／變得比較積極／行為模式」等空泛結論。當日觀察以 3 個完整句、120～220 字為目標,只有重大轉折才可用第 4 句;滾動摘要縮為 2 句且只保留重要事實、旗標與新變化。
- **輸出品質閘門**:新增共用`narrativeQuality.ts`,在 Worker 回傳與前端接收兩端統一標點、修正常見中文語序,以中文字詞 bigram 相似度刪除重複／近似句,並只在完整句界內限制 4 句、320 字。舊存檔載入時也會清理既有的 AI 當日觀察,但不改玩家選擇或一般事件日誌。
- **玩家案例回歸**:將邱柏翰的實際重複範例加入`narrative-quality-test.ts`,鎖住聊天進展只說一次、視力句不重複、貓咪畫面保留、標點與語序自然、舊摘要不回灌及舊存檔可清理等 13 項。`worker-test.ts`擴至 36 項,另把`sim-trace`改為固定亂數種子,消除完整 runner 偶發的煙霧測試抖動。
- **驗證**:完整`npm test`為 46/46 支通過（含 balance、固定種子的`sim-trace`、敘事品質 13 項與 Worker 36 項）;`npm run build`成功（113 modules,`index-DXe7Jipp.js`,`index-Cs8iZbCE.css`）,Wrangler 4.110.0`deploy --dry-run`成功並辨識 AI／ASSETS bindings。本批未改 UI 或數值平衡,因此不需重跑 Mobile UI Lab或更新 balance 快照。

### 2026-07-16 — AI 觀察回饋第一期:每日情緒微調 statNudge(Claude Code)

- **目的**:使用者反映 AI 當日觀察只是流水帳,希望觀察與事件能回饋劇情(調整數值、改變行為)。本批新增三期規劃設計文件 `docs/AI觀察回饋設計.md`,並實作第一期 statNudge——「日記寫什麼,人就受影響」。
- **機制**:narrate 回應新增 `observation`(`{nudge, reason}`);Worker prompt 指示 AI 對今天素材做「情緒解讀」——只填有依據的欄位、reason 必須指向具體事件、平淡日填 null、不得連日往同一方向堆。前端新模組 `sim/observationEffects.ts` 統一消毒夾值:只開放 mood/stress/energy/wellbeing(各 ±3)與 affinity(±2,量級對齊收租涓流);satisfaction(衍生公式)與 money(房東代理權)不開放;全 0 推力或缺 reason 整包丟棄。套用後寫「🔮 觀察影響:理由(心情 -2、壓力 +3)」日誌,玩家看得到因果。
- **接線**:`narration.applyDiaryEffects()` 掛上消毒+套用(即時日記與待補升級兩條路徑共用);Worker `parseResult` 照 event 慣例原樣透傳、消毒統一在前端。效果只在 AI 成功路徑生效、不消耗模擬 RNG,balance 快照無漂移、不需 --update。
- **驗證**:新增 `observation-test.ts` 12 項(夾值上限、白名單外欄位丟棄、全 0/缺 reason 丟棄、整合套用與 🔮 日誌、0~100 邊界、待補升級路徑)並加入 run-all REGRESSION;`worker-test.ts` 擴至 39 項(prompt 指引、observation 透傳/非物件擋下)。完整 `npm test` 47/47 通過(含 balance 與 sim-trace),`npm run build` 成功(114 modules,`index-ByAQ4nHJ.js`),Wrangler 4.111.0 dry-run 成功並辨識 AI/ASSETS bindings。未改 UI 版面,不需重跑 Mobile UI Lab。
- **後續**:第二期 selfBehavior(AI 讓房客自發改變 1~2 天行為,白名單擴充 4 種)與第三期 arc tone(劇情弧接上數值脈衝)見設計文件;部署後請以正式站做一次煙霧驗證(觀察租客日誌是否出現 🔮 行)。
- **補記**:另補提交 2026-07-15「共用 Mobile UI Lab 接入」批次遺留未 commit 的 `AGENTS.md`/`CLAUDE.md` UI Lab 章節(該批工作日誌已有紀錄,僅檔案漏提交)。

### 2026-07-16 — AI 觀察回饋第二期:自發行為 selfBehavior(Claude Code)

- **目的**:接續第一期,讓 AI 觀察不只推數值,還能讓房客「自己決定」改變 1~2 天的行為(照 `docs/AI觀察回饋設計.md` 第二期)。定位:這是房客自己的決定,不需房東拍板;玩家拍板保留給「房東的介入」。
- **行為白名單擴充 4 種**(`sim/directives.ts`,全部是既有 visualState/作息/目標的組合):`comfort_seek` 想找人談心(串門意願 15%→60%)、`overtime` 加班晚歸(19-20 點釘在書桌前,走 working_at_desk 既有數值)、`self_care` 好好照顧自己(22-23 點提早上床)、`sulk` 悶悶不樂(不去交誼廳、不主動串門,但接受別人來訪——與 hermit 的差異)。玩家拍板的事件 directive 也同步可用(prompt 白名單 6→10)。
- **自發通道防線**(`observationEffects.ts` + `sanitizeSelfBehavior`):id 限自發子集(**adopt_cat 排除**——真實領養影響經濟,屬房東層級,仍走 event);days 夾 1~2(玩家拍板維持 1~7);已有進行中指令(不論來源)直接丟棄、玩家拍板永遠優先;每租客冷卻 3 遊戲日(`TenantRuntime.lastSelfBehaviorDay`,存檔 `?? -99`);待補日記隔 >1 遊戲日才升級 → 行為過時不套、數值照補。生效寫「🌀 理由——行為開場白」重要日誌;`ActiveDirective.source`("choice"/"ai",舊存檔缺省=choice)讓房間細看 chip 標示「(自發)」。
- **RNG 次序保護**:串門擲骰照擲(sulk 只在骰後擋、comfort_seek 只改門檻),未觸發指令時亂數消耗與原版完全一致;balance 快照零漂移、不需 --update。不立即重定位(避免 observationEffects→tick 循環 import),下一個 hourlyTick 自然生效。
- **驗證**:`observation-test.ts` 擴至 24 項(behavior 消毒/adopt_cat 拒收/生效+冷卻+優先權/過期補寫);`directive-test.ts` 擴至 21 項(overtime/self_care/sulk 作息接線);`worker-test.ts` 擴至 41 項(behavior 指引/10 指令清單)。完整 `npm test` 47/47(含 balance、sim-trace),`npm run build` 成功(114 modules,`index-DD1LOaCk.js`),Wrangler dry-run 成功。UI 僅 chip 文字加註,無版面變動,不需重跑 Mobile UI Lab。
- **後續**:第三期 arc tone(劇情弧接上數值脈衝)見設計文件。

### 2026-07-16 — AI 觀察回饋第三期:劇情弧 tone 脈衝(Claude Code)

- **目的**:完成 `docs/AI觀察回饋設計.md` 的最後一期——劇情弧不再只是文字,推進/收束時反映在數值曲線上(例:「準備證照考試」的弧推進期間壓力逐段上升,考過收束時明顯放鬆)。
- **機制**:`arcUpdate` 新增選填 `tone`(enum 白名單 `up/down/tense`,未知值忽略、退回純敘事);脈衝表寫死在 `arcs.ts` 的 `ARC_TONE_PULSE`——推進:up=心情+3/down=心情-3/tense=壓力+4;收束:up=心情+8壓力-6/down=心情-8/tense=壓力-8(如釋重負)。**AI 只能選方向,不能自訂數值**;開新弧不帶脈衝。套用在 `narration.applyArcUpdate`(夾 0~100),與觀察回饋同樣只走 AI 成功路徑、不耗模擬 RNG。
- **prompt**:arcUpdate 指引補 tone 三值語意與「除 tone 之外弧仍是純敘事骨架,較大數值影響照常用 event」;輸出格式加 tone 欄位。
- **驗證**:`arc-test.ts` 擴至 31 項(tone 消毒三案 + 推進 tense/收束 up 的脈衝整合 + 收束記憶照留);`worker-test.ts` 擴至 42 項(tone prompt 指引)。完整 `npm test` 47/47(balance、sim-trace 無漂移),`npm run build` 成功(114 modules,`index-D1Q_urwO.js`),Wrangler dry-run 成功。無 UI 變動。
- **設計文件三期至此全部完成**;後續若要擴充,依設計文件的防呆原則(白名單/夾值/AI 成功路徑限定)照做即可。

### 2026-07-16 — 豐富批:關係推力 + 天氣系統 + 敘事品質(Claude Code)

- **目的**:三期收尾後的優化評估獲使用者同意,一批做三個小項——讓觀察回饋補完關係維度、世界有生活感、AI 文字品質變好。
- **跨租客關係 nudge(受限版)**:`observation` 新增 `rel:{name,delta}`(夾 ±2)。防線:對象必須是現任租客**且真的出現在今日素材**(applyDiaryEffects 傳入 todayLog 驗證,AI 不能憑空點名);**向上永遠推不過 74**(戀愛/摯友門檻 75 前一格,`getRel` 查現值後夾),已在 74 的正向推力整筆丟棄;負向不受上限。套用寫進 🔮 日誌(「與陳家豪的關係 +2」)。另補防呆:**🔮/🌀 系統日誌不再回流當明日素材**(buildNarrateCtx 排除),避免 AI 摘要自己的回饋(同舊日報回灌問題)。
- **天氣系統**:新模組 `sim/weather.ts`——每遊戲日一種天氣(晴 40%/陰 25%/雨 25%/悶熱 10%),由日序決定性雜湊決定,**不耗模擬 RNG、不入存檔**。接點:header 時鐘前顯示天氣 emoji(hover 看全名);AI context 新增 `weather`(Worker clampCtx/buildPrompt 對應,附「自然融入、別每天以天氣開頭」指引);模板日記混入 12 句天氣情境句(候選池加大、仍只抽一次亂數);社群事件加 `when` 環境門檻——雨天不上頂樓,新增雨天限定「窩交誼廳」事件(4 句文案)。
- **敘事品質批**(修上兩輪線上煙霧測試發現的漏洞):`narrativeQuality.ts` 新增 `toTraditional()` 簡轉繁(詞級處理一簡對多繁如泡面→泡麵,字級白名單約 130 字只收簡體獨有字,繁體原文不受影響),掛進 splitNarrativeSentences(日記/摘要前後端共用);標點連用清理(「,。」→「。」、句號後殘留標點);新增 `sanitizeReasonText()`——觀察 reason 也過閘門(轉繁+冗贅子句去重+去尾標點+截 60),擋掉「她們還是…她們還是…」式輸出。
- **Mobile UI Lab**:header 有 UI 變動,三種手機尺寸 18 張截圖 0 errors/0 warnings 並逐張目檢;第一輪發現 390px 標題被天氣 emoji 擠成「房東監…」,改為天氣 emoji 併入時鐘欄(取代裝飾性 🕐)後復原。⚠️ 環境註記:本機 Windows 保留埠範圍(7912-8011)吃掉 8000,UI Lab 容器綁不上;已在 `.ui-lab/compose.yaml` 暫時註解 NBA 的 8000 映射(附註解說明),截圖流程在容器內不受影響,跑 NBA 互動檢查前需還原或重設 winnat。
- **驗證**:新增 `weather-test.ts` 8 項(決定性/分佈/label/ctx 接線/模板天氣句/事件天氣門檻)加入 CI;`observation-test.ts` 擴至 33 項(rel 消毒/套用/74 夾/素材驗證/負向);`narrative-quality-test.ts` 擴至 19 項(簡繁/標點/reason);`worker-test.ts` 擴至 45 項(rel 指引/weather 夾值與 prompt)。完整 `npm test` 48/48(balance 無漂移——天氣門檻未影響固定種子序),`npm run build` 成功(115 modules,`index-CV0ptJWG.js`),Wrangler dry-run 成功。

### 2026-07-16 — 後續優化規劃(Claude Code;規劃紀錄,尚未動工)

> 使用者要求「評估還能做什麼讓劇情/遊戲更豐富」後拍板的路線圖。上面豐富批(關係推力+天氣+敘事品質)
> 是此評估的第一波,已完成;以下為**尚未動工**的項目,依建議順序排列。規模標記沿用專案慣例:
> 🟢 小(半天內)/ 🟡 中(一兩天)/ 🔴 大(動工前先與使用者確認方向)。

**建議下一批(中型深化,擇一)**

1. ~~🟡 **成長標籤:劇情弧改變性格**~~ ✅ 已完成(見下方 2026-07-16 Codex 條目)。
2. ~~🟡 **雙人劇情弧**~~ ✅ 已完成(見下方 2026-07-16 Claude Code 條目)。
3. 🟡 **租客錢包與繳租戲劇**(設計檢討 §4 遺留)——租客自有財務:失業弧 → 遲繳 → 求情事件
   (寬限/催繳/減租換打掃),還錢附感激記憶。把收租從按鈕變成劇情來源,與調租談判、協議解約串成租約戲劇線。
4. 🟡 **月度全樓事件鏈**——用既有群體事件+伏筆旗標做多日連鎖(都更傳聞/颱風夜停電全員擠交誼廳),
   每月一條,給掛機生活「章節感」。

**低成本備選(隨時可插)**

5. 🟢 **夢境彩蛋**——睡覺時段低頻(每人每 3~4 天、決定性排程)生成夢境日誌,素材取自記憶標籤與關係,
   純模板零 AI 成本。
6. 🟢 **樓外世界錨點**——「外出」加目的地文本(便利商店/早餐店/加班),兩人同時外出小機率「樓外巧遇」
   → 回樓各留一句日誌+關係微升。純文字,世界變大。

**工程項(舊待辦,順序不衝突)**

7. 🟢 **I-3 Cloudflare Dashboard `/api/*` rate limit**(零程式碼,dashboard 設定)。
8. 🟡 **E-3 PWA**(vite-plugin-pwa,`registerType: 'autoUpdate'`;需真機驗證 service worker)。

**🔴 大題目(一律先與使用者確認)**:樓層擴建(二樓)、訪客 NPC(房客的朋友/前任來訪,需新 sprite 與場景)、
多人/雲端存檔、玩家自帶 API key。

已使用者婉拒/擱置:音效(使用者先前表示略過)。

### 2026-07-16 — 中型深化:劇情弧永久成長標籤（Codex）

- **目的**:使用者選擇路線圖的中型深化,依建議順序實作第一項「成長標籤」,讓多日劇情弧收束後不只留下文字記憶與一次性 tone 脈衝,還能永久塑造角色的性格基準。
- **八個白名單特質**:新增`sim/growth.ts`,內建`[更有自信]`、`[更能承受挫折]`、`[學會示弱]`、`[腳步更穩]`、`[重新抱有希望]`、`[更有耐心]`、`[更敢做決定]`、`[學會放下]`。每個 id 的標籤、說明與 mood／stress baseline 效果都寫死在前端;AI 不能自訂名稱或數值,每位租客最多累積 4 個。
- **只在真正收束時授予**:`arcUpdate.growthTag`只於弧收束的`conclude`動作消毒採用;開弧、中途推進、未知 id、重複 id 或超過上限都不生效,但不會妨礙故事本身正常收束。Worker prompt 會列出白名單及角色已獲得的永久成長,要求沒有明確改變就填 null,避免每條弧硬塞獎勵。
- **永久但不暴衝**:`tick.baselines()`讀取固定特質表,只改 homeostasis「逐小時回到哪裡」的心情／壓力基準,不直接灌當下數值;既有 tone 仍負責收束當刻的情緒脈衝。預設角色沒有成長標籤,因此原 balance 固定種子完全不漂移。`🌱 成長`系統日誌會解釋取得原因,並與`🔮/🌀`一樣排除隔日 AI 素材回灌。
- **存檔與可見性**:`Tenant.growthTags`採 additive 選填欄位,新 runtime 與載入舊檔都正規化為陣列;匯入髒資料時自動去未知、去重並限 4 個,無需升存檔版本。房客標籤列新增綠色`🌱`chip與說明 tooltip,永久特質也會進後續 AI context,避免模型重複授予。
- **回歸與建置**:`arc-test.ts`由 31 項擴至 45 項,涵蓋收束限定、白名單、去重／上限、baseline 組合、全流程授予、可見日誌、context 與存檔重載;`worker-test.ts`由 45 項擴至 48 項,涵蓋 payload 限量、白名單 prompt 與既有特質背景。完整`npm test`48/48 通過（balance 與固定種子`sim-trace`無漂移）,`npm run build`成功（116 modules,`index-PhNaK3d7.js`,`index-7pE5YXLI.css`）,Wrangler 4.111.0 dry-run 成功。
- **手機驗證與限制**:Mobile UI Lab 三尺寸產生 18 張截圖,報告 0 errors／0 warnings;連同目錄內既有 3 張退租流程圖均逐張檢視,無新裁切、覆蓋或導覽退化。標準新開局尚無成長特質,本工作階段的 in-app Browser 又沒有可用後端,所以未額外注入標籤拍房客頁;綠色 chip 的 Vue 編譯與資料整合測試已通過,後續首次在線取得特質時可再補一次實機目檢。

### 2026-07-17 — 中型深化:租客錢包與繳租戲劇(Claude Code → Codex)

> Claude Code 完成功能初稿與交接紀錄，Codex 接手執行測試、平衡檢查、BOM 清理、
> 回歸保護、建置與 Mobile UI Lab 驗收；確認全綠後才提交與部署。

**這批做什麼**(路線圖中型深化第 3 項):租客有自己的錢包——薪水入帳、生活開銷、從錢包繳租;
偶發「財務困難」讓收入中斷,錢包見底才是真正欠租(進 `arrears`),欠滿 3 天日租上門求情
(寬限/催繳/一筆勾銷),回穩後自動補繳;曾被寬限的補清時留感激記憶+好感。收租從按鈕變成劇情來源。

**已完成的變更(檔案地圖)**:

- `src/sim/economy.ts`(主體):常數旋鈕區(收入=日租×3、開銷×1.6、錢包上限 3 個月租、
  困難機率 4%/冷卻 12 日、求情門檻 3 日租/冷卻 6 日、寬限 7 日、補繳保留 5 日生活費)、
  `ensureWallets()`(冪等補登,舊存檔相容)、`inHardship()`/`triggerHardship()`(測試與擲骰共用)、
  `tenantFinanceBrief()`(AI context 一句話)、`buildRentPleaEvent()`(求情事件三選)、
  `applyRentAction()`(grace/collect/forgive 處置)、`walletDailyPass()`(薪水/開銷/困難擲骰與回穩)、
  `arrearsPass()`(自動補繳→感激/掛求情)。`collectRent()` 改為:先 walletDailyPass →
  意願公式(既有 factor)算 willing → `paid = min(willing, wallet)` → 錢包不夠的短繳記入 arrears
  (意願性短繳維持既有邏輯不列欠)→ 繳租日誌三型 → arrearsPass。
- `src/sim/gameState.ts`:TenantRuntime 加 6 個選填欄位(wallet/arrears/hardshipUntilDay/
  lastHardshipDay/rentGraceUntilDay/lastRentPleaDay)。
- `src/sim/persistence.ts`:6 欄位存讀(additive,`?? 0`/`?? -99`;wallet 缺省 undefined 由 ensureWallets 補)。
- `src/sim/events.ts`:EventEffect 加 `rentAction?: "grace"|"collect"|"forgive"`
  (只給程式建構的事件;cleanEffect 建新物件不透傳,AI 不能觸發)。
- `src/sim/tenancy.ts`:applyEffect 接 `eff.rentAction → applyRentAction`;moveOut 帶欠款時 notify。
- `src/store.ts`:façade 底部 `_ensureWallets()`(照 ensurePets 模式)。
- `src/sim/narration.ts`:buildNarrateCtx 加 `finance: tenantFinanceBrief(rt) ?? undefined`。
- `src/sim/narrate.ts`:NarrateCtx 加 `finance?: string`。
- `worker/index.ts`:NarrateCtx.finance、clampCtx 截 40、buildPrompt 條件行
  「財務狀況:…(沒欠租的日子不要提錢的事)」。
- `src/App.vue`:標籤列最前新增橘色 `financeChip`(💸 欠租 $X(寬限中)/💼 手頭拮据)+
  `.chip.warn-chip` 樣式。
- `scripts/wallet-test.ts`(新,24 項):初始化/正常日流水/錢包上限/困難→欠租/
  求情掛起與不重複/寬限→自動補繳感激/催繳/勾銷/存檔往返/moveOut 通知。已加進 run-all REGRESSION。
- `scripts/worker-test.ts`:+2 項(finance 夾值/prompt 條件行),完整檔案共 53 項通過。

**驗證與收尾**:

- `npx tsx scripts/wallet-test.ts` 24/24、`npx tsx scripts/worker-test.ts` 53/53 通過；
  完整 `npm test` 49/49 支通過，包含 balance、固定種子 `sim-trace` 與新錢包回歸。
- 首輪完整測試唯一失敗為預期的 balance 漂移：金錢 $58,729→$58,727，兩位租客的壓力／滿意度
  因每日困境擲骰改變後續 RNG 序而位移。固定種子 10 日模擬確認兩人錢包分別為 $23,813／$26,950、
  `arrears=0` 且未觸發困境；以 `--update` 更新基準，並把 `wallet`／`arrears` 納入快照回歸保護。
- `npm run build` 成功（116 modules，`index-B1GMmfDV.js`、`index-Bguzv8KY.css`）；
  Wrangler 4.111.0 `deploy --dry-run` 成功並辨識 AI／ASSETS bindings。
- Mobile UI Lab 三種手機尺寸產生 18 張截圖，`report.json` 為 0 errors／0 warnings；逐張檢視樓層、
  動態與收支頁，無裁切、覆蓋或導覽退化。預設局沒有欠租／拮据，finance chip 不顯示，版面維持原樣。
- 清除 `economy.ts` 與 `App.vue` 的 UTF-8 BOM；`git diff --check` 通過。

**已知注意點**:

- `economy.ts` 與 `App.vue` 原先因 PowerShell 5.1 重寫產生的 UTF-8 BOM 已在驗收時移除。
- 循環 import:narration → economy → tick → narration 是新增的環(economy↔tick 原本就有);
  模組頂層都沒有碰 state 的副作用,符合地雷紀錄的規範,但別在這些模組頂層加會跑的程式。
- 求情事件走既有 pendingEvent 槽位,與規則/AI 事件互斥;`rentAction` 消毒面:AI 的 cleanEffect
  不透傳(已保證),`data/events.json` 的規則事件屬信任資料。
- 同居者(cohabits)整段跳過錢包流程(不付租);接手承租時 ensureWallets 會補登。
- 路線圖剩餘:第 4 項「月度全樓事件鏈」未動工;低成本備選(夢境彩蛋/樓外錨點)與工程項
  (rate limit/PWA)照舊,見上方 2026-07-16 規劃條目。

### 2026-07-16 — 中型深化:雙人劇情弧(Claude Code)

- **目的**:路線圖中型深化第 2 項——劇情弧從單人連載升級為可「兩位租客共演一條線」(合寫小說/頂樓菜園/戀情發展),兩本日記互相呼應,收束時一起落幕。
- **開弧**:AI 開新弧可附 `with:"鄰居名"`。守門(`resolveArcPartner`):對方必須是現任租客、**沒有進行中的弧**、關係至少朋友(35)或情侶——任一不符自動降級為單人弧,AI 不必自行判斷。成立後兩人各持一份**同 id** 的弧(`StoryArc.partnerId/partnerName` 選填欄位,存檔 additive 免升版),雙方都留「📖 新篇章開始(與 X 共同)」日誌。
- **推進與收束**:任一方日記推進 → 對方的 stage/summary 同步(`pairArcPartner` 以同 id 驗證);任一方收束 → 兩人一起落幕、都留 `[經歷:主題]` 記憶與 📕 日誌。**tone 脈衝與 growthTag 只作用在該篇日記的主人**——對方的情緒與成長由他自己的日記決定,避免同日雙推雙倍脈衝。
- **context 與 prompt**:`NarrateCtx.arc` 新增 `with`(雙人弧顯示「與 X 共同的雙人篇章」);Worker prompt 指示「從他自己的視角寫、劇情與同一條線一致」、開弧時機(戀情/共同企劃/恩怨和解)與降級規則。
- **搬離處理**:`moveOut` 時對方的雙人弧**降級為單人繼續**(斷開 partner 連結 + 「另一位主角搬走了」日誌),不強制腰斬故事——AI 會自然把「對方離開」寫進後續推進。
- **UI**:房間細看弧 chip 與摘要行顯示「(與X)」;僅文字加註、無版面變動,依慣例不需重跑 Mobile UI Lab。
- **驗證**:`arc-test.ts` 擴至 56 項(with 透傳、關係守門、雙份同 id 互指、推進同步、共同收束雙記憶、對方有弧降級、搬離降級與日誌);`worker-test.ts` 擴至 51 項(雙人弧 prompt、arc.with 夾值、共同篇章 context 行)。完整 `npm test` 48/48(balance、sim-trace 無漂移——雙人弧僅走 AI 成功路徑),`npm run build` 成功(116 modules,`index-CjxnCfP5.js`),Wrangler dry-run 成功。

### 2026-07-17 — 修正 AI 觀察混入英文與姓名羅馬化（Codex）

- **玩家案例**:AI 當日觀察把「陳家豪」寫成「Chen 家豪」，並出現「吃飯的方式很慢lane」；
  兩段英文都不是劇情設定，而是 Workers AI 偶發的 code-switching 輸出瑕疵。
- **雙層防線**:Worker prompt 明定 diary／摘要／記憶／事件文案／觀察理由只能使用自然繁體中文，
  中文姓名必須逐字照抄 context，不得翻成拼音或羅馬字；JSON key、白名單 id 與 enum 仍維持英文機制格式。
- **共用清理器**:`narrativeQuality.ts` 新增 `normalizeNarrativeLanguage()`：先依已知中文姓名的後半段
  將 `Chen 家豪` 還原成 `陳家豪`，再把 `AI`／`ASMR` 轉成「人工智慧」／「耳語」，移除其餘未知
  拉丁字母並整理中文字間空格與標點。前端即時回應、Worker 回傳、滾動摘要、觀察 reason 與舊存檔
  AI 日記均套用同一層；機制 id 不受影響。
- **玩家實例回歸**:`narrative-quality-test.ts` 增至 22 項，直接鎖住姓名還原、`慢lane→慢`、常見縮寫
  中文化；`worker-test.ts` 增至 55 項，驗證 prompt 防線與 `parseResult` 伺服器端修復。原 observation／summary
  測試的純英文 fixture 改為等價繁中，避免測試資料繞過正式輸出規則。
- **驗證**:敘事品質 22/22、Worker 55/55、observation 33/33、summary 6/6；完整 `npm test` 49/49
  通過（balance 與 `sim-trace` 無漂移）。`npm run build` 成功（116 modules，`index-_nZW1eYL.js`、
  `index-Bguzv8KY.css`），Wrangler 4.111.0 `deploy --dry-run` 成功並辨識 AI／ASSETS bindings。
  本批未改 UI 或遊戲數值，因此不需重跑 Mobile UI Lab 或更新 balance 快照。

### 2026-07-17 — 玩家目標批第 1 項:租客人生心願系統(Claude Code)

- **目的**:玩家回饋「觀察久了有點膩,缺少目標感」。本批為每位租客加一個**長期人生目標**
  (依職業指派:咖啡師存開店基金、漫畫家完成代表作、研究生畢業、上班族站穩腳步、
  夜班護理師養回健康、鼓手登台、退休教師把樓住成家、其他職業 fallback「把日子過舒服」,
  共 8 種),讓「把租客照顧好」有了可見的長線意義。
- **進度引擎(`src/sim/wishes.ts` 新模組)**:進度 0~100 **完全由本地規則每日推進**——
  租客當天過得好(條件依心願而異:錢包水位/壓力與精力/身心/心情/鄰居關係/滿意度)+4、
  勉強 +2、困頓(財務困難、壓力爆表等)-2;劇情弧好好收束(非 down 基調)另 +6
  (`boostWishFromArc`,篇章落幕=人生前進)。**全程零 RNG**,不影響其他系統的亂數次序。
  跨過 25/50/75% 留 🎯 里程碑日誌(進 Feed、也是隔日 AI 素材)。
- **實現與圓夢離開**:進度滿 → 🎉 major 日誌 + `[心願成真]` 記憶 + mood+10/stress-8 +
  **永久成長特質**(走既有 growth 白名單:decisive/more_confident/grounded 等)+ 成就。
  開店/畢業兩種心願會在 6 天後「**圓夢離開**」:前兩天 📦 打包預告,到期由 tick 換日
  moveOut(原因「圓夢離開:…」進歷任名冊)——送走房客從損失變成收藏。
  wishes 不 import tenancy(wishPass 回傳名單、tick 執行),避免循環依賴。
- **AI 接線**:`buildNarrateCtx` 新增 `wish` 一句話(進行中給進度、剛實現 3 天內給餘韻、
  打包中給搬離預告,之後不再提);Worker `clampCtx` 截 48、prompt 條件行明定
  「進度由系統決定,不得自行宣布實現或跳進度」。AI 只能拿心願當動機素材,不能推進度。
- **成就 + UI**:成就冊 10→12 項(🎯 圓夢推手/🌠 夢想孵化器,配 `state.wishesFulfilled`
  計數入存檔);房間細看標籤列新增紫色心願 chip(`☕ 存一筆自己的小店基金 · 40%`,
  實現後顯示 ✓,tooltip 是心願描述)。
- **存檔**:`TenantRuntime.wish`(additive `?? null`)+ `wishesFulfilled`(`?? 0`),免升版;
  `ensureWishes()` 冪等補指派(store façade/載入/入住三處),未知心願 id 防線=丟棄重指派。
- **驗證**:新增 `scripts/wish-test.ts` 31 項(指派/冪等/壞檔防線/推進與倒退/里程碑一次性/
  弧加成/實現全套/不再推進/AI 三態/圓夢離開全流程/三度圓夢成就/存檔往返),已入 run-all;
  `worker-test.ts` +2 → 57 項;`legacy-test.ts` 成就數斷言 10→12。完整 `npm test` **50/50**
  (balance 無漂移——日誌數在 LOG_CAP 封頂、種子局 10 日內不觸發實現;`sim-trace` 無異常),
  `npm run build` 成功(117 modules,`index-Be4t1M7o.js`),Wrangler 4.112.0 dry-run 成功。
  Mobile UI Lab 三尺寸 18 張 + 退租流程 3 張逐張檢視,0 errors、無版面退化。
- **已知限制**:截圖流程只涵蓋樓層/動態/收支頁,心願 chip 在房間視圖標籤列
  (與 🌱 成長 chip 同區,沿用同一套 chip 樣式),本工作階段瀏覽器自動化不可用,
  留待下次實機順手目檢。「玩家主動幫一把」的關懷互動屬玩家目標批第 2 項(心意互動),
  尚未動工。

### 2026-07-18 — 房客性別、姓名標籤與房東收支平衡（Codex）

- **性別誤判根因與修正**:舊版隨機應徵者把中文姓名與性別分開亂抽,因此會出現
  「邱柏翰」實際被存成女性,高好感關係便依既有規則顯示「閨密」。`recruit.ts` 改為姓名／性別
  成對資料,邱柏翰固定為 `male`;新產生的 20 組姓名都不再錯配。載入舊存檔與舊應徵者池時也會
  依同一份表校正,所以玩家現有存檔不用重開。關係面板仍沿用女性雙方「閨密」、男性雙方
  「哥們」、其他組合「摯友」的規則。
- **建立角色可選性別**:`RecruitPanel.vue` 的特邀租客表單新增男／女／非二元選單,一般應徵者卡也
  顯示性別 chip。`/api/invite` 請求會帶玩家選擇,Worker prompt 明定不得依名字自行猜測；前端消毒
  再以玩家選擇覆寫 AI 回傳,形成雙重保證。`invite-test` 新增「玩家指定優先」回歸案例。
- **姓名固定跟人**:移除 `tagLayout` 依鄰居即時左右換位／向上堆疊的碰撞排版,名牌只固定在角色
  錨點正上方並做畫布邊界夾值；`FloorMap` 同時移除 150ms 節流與 CSS 位置補間,改為每幀使用角色
  當下座標。角色本身仍由既有站位避碰保證不重疊,姓名不再走動時突然換邊或落後角色。
- **故障與收入再平衡**:每日每間有人房的自然故障率 `BREAK_CHANCE` 從 5% 降為 2%；事件造成的
  家具毀損與一房最多一件故障規則不變。投幣洗衣機每台每住人房日收入從 $50 提高為 $80；種子
  兩台洗衣機在 2 人入住時由 $200／日提高為 $320／日,滿租 4 人時由 $400／日提高為 $640／日。
  固定種子 10 日 balance 現金 $58,727→$59,927,精確等於每天多 $120 × 10 日,其他租客數值、
  欠租、日誌與 `sim-trace` 皆未漂移,已更新平衡基準。
- **驗證**:針對性測試 `invite` 25/25、`rotation-pose` 29/29、`maintenance` 24/24、`finance`
  27/27；完整 `npm test` 50/50 通過。`npm run build` 成功(117 modules,
  `index-_X1aHZnI.js`,`index-BiVon_nW.css`),Wrangler 4.112.0 dry-run 成功並辨識 AI／ASSETS bindings,
  `git diff --check` 通過。Mobile UI Lab 三尺寸共 18 張新圖 + 3 張既有退租流程圖逐張檢視,
  `report.json` 0 errors／0 warnings；收支卡正確顯示投幣洗衣收入 $320。內建瀏覽器本次沒有可用
  工作階段,所以特邀表單未補互動截圖；其 Vue／TypeScript 編譯、資料傳遞與消毒回歸均已通過。

### 2026-07-18 — 補記:心願 chip 實機目檢完成 + 容器化 UI 驗證方法(Claude Code)

- 前一批「已知限制」中的心願 chip 目視驗證已補做:改在 **UI Lab 容器內用 Playwright**
  寫一次性腳本(`artifacts/ui-lab/wish-chip-check.mjs`,不入 Git)點進 301 房間視圖,
  390×844 全頁截圖確認紫色 chip「💼 在工作上站穩腳步 · 0%」正確顯示於標籤列最前,
  無裁切、無 page error;chip 文字同時以 DOM 斷言驗證。
- 驗證方法備忘:自訂瀏覽器流程一律走容器(playwright 在 `.ui-lab/node_modules`,
  ESM 腳本用 `createRequire("/workspace/.ui-lab/scripts/")` 解析),先
  `project-service.mjs start rent` 起 dev server 再 exec 腳本;**不使用 wmux**
  (本機安裝不完整,browser 指令會卡住)。
- 本批僅文件補記,無程式變更;不需跑測試或更新 balance。

### 2026-07-18 — 玩家目標批第 2 波:成就冊大擴充 + 心意互動(Claude Code)

- **目的**:延續玩家目標批(點子 2+3)——成就冊從 12 項擴到 **20 項**並引入**隱藏成就**,
  新增「心意互動」讓玩家能主動對租客做點什麼,做了之後劇情會回應(日誌成為隔日 AI 素材)。
- **成就擴充(`legacy.ts`)**:8 個新成就覆蓋近期新系統——🤝 寬限之恩/🕊️ 佛心房東/
  🧾 鐵面房東(隱藏:催繳落難租客)/💝 暖心房東(累計 10 次心意)/🌱 見證成長/
  🎓 桃李滿樓(隱藏:單人集滿 4 個成長特質)/👥 共同篇章(雙人弧收束)/
  ☔ 雨天的交誼廳(隱藏:rainy_lounge 群體事件)。`AchievementDef.hidden` 選填欄位,
  LegacyPanel 未解鎖的隱藏成就連描述都遮蔽(顯示「???(隱藏成就,達成後揭曉)」)。
  鉤子位置:繳租處置在 `tenancy.applyEffect`(避免 economy→legacy→finance→economy 循環)、
  雙人弧在 `narration` 收束分支、雨天在 `community.rainy_lounge.fire`、
  成長兩項走 `legacyPass` 每日輪詢(與既有累積型一致)。
- **心意互動(`src/sim/kindness.ts` 新模組)**:三種主動關懷,房間視圖租約列下方新增
  「💝 心意」按鈕列(紫色,樣式呼應心願 chip):
  - 🍜 **送宵夜** $120:mood+6、好感+4;
  - 📝 **塞紙條** 免費:壓力−5、好感+2;
  - 💼 **介紹案子** $600(僅財務困難時出現,橘色急件樣式):提早結束收入中斷、
    半個月租頭款進錢包(夾上限)、好感+8、[手頭拮据] 記憶翻頁成 [房東拉了一把]。
  規則:**每位租客每遊戲日一次**(`rt.lastCareDay`);動作日誌(🍜/📝/💼)進 Feed 且
  不在 AI 素材排除清單 → 明天的日記會寫出租客對心意的反應,形成「做了有回應」的閉環。
  `state.careGiven` 累計(入存檔),滿 10 次解鎖 💝 暖心房東。全程零 RNG、僅玩家觸發。
- **存檔**:`lastCareDay`(`?? -99`)與 `careGiven`(`?? 0`)additive 免升版。
- **驗證**:新增 `kindness-test.ts` 22 項(三動作效果/扣款/每日一次/介紹案子門檻與全套效果/
  金錢不足/10 次成就/繳租三處置成就走 decide 全流程/存檔往返),入 run-all;
  `legacy-test.ts` 21 項(成就數 20、隱藏標記、成長雙成就輪詢)、`arc-test.ts` 57 項(+共同篇章)。
  完整 `npm test` **51/51**(balance 無漂移——新鉤子只在玩家操作或既有事件路徑觸發),
  `npm run build` 成功(118 modules,`index-CkgRE8BY.js`),Wrangler dry-run 成功。
  UI Lab 標準 18 張 0 errors;容器內自訂腳本(`artifacts/ui-lab/wish-chip-check.mjs` 擴充)
  實測房間視圖:心意列兩鈕正常、心願 chip 共存不擠版,成就冊面板渲染 20 項、
  隱藏遮蔽恰好 3 項,`legacy-panel.png` 截圖留檔。
- **已知限制**:☔ 雨天成就的觸發鉤子未寫自動化測試(需固定 rng+雨天+3 人在場,
  成本高於風險;unlock 冪等、一行呼叫)。介紹案子的 hardship 由 wallet 系統擲骰觸發,
  玩家可能數日看不到第三顆按鈕——屬設計預期(稀有時刻才有的英雄時刻)。

### 2026-07-18 — 房客頁顯示性別 + 星期幾系統(Claude Code)

- **目的**:使用者要求(1)房客頁面顯示性別方便 debug;(2)加入星期幾系統。
- **性別顯示(App.vue)**:房間細看標頭(`.room-head`)在職業後新增 `.rgender`
  (♂ 男/♀ 女/⚧ 非二元);資料缺漏時顯示紅色「性別未設定」,debug 時一眼看出髒資料。
- **星期系統(`src/sim/week.ts` 新模組)**:仿 weather.ts 的省成本模式——星期幾直接取
  遊戲時間戳的本地日曆(`getDay()`,與時鐘/換日同時區基準),純函式、零 RNG、不入存檔。
  GAME_START 2026-07-05 為週日。四個接點:
  - **header 時鐘**:`clockLabel` 改為「7/18(五) 14:00」(日誌 timeLabel 的 `fmt()` 不動,
    既有 Feed/日誌格式零影響);
  - **AI context**:`buildNarrateCtx` 新增 `weekday`(例「週五」),dayLabel 也帶星期;
    worker 端同步 ctx 型別 + `clampStr(weekday, 8)` 夾值 + prompt 行(明示「作息仍以
    今日片段為準,不得虛構休假或上班」,避免 AI 與實際作息矛盾);
  - **模板日記**:`DAILY_WEEKEND_TEMPLATES` 三句週末情境句,週六/週日混入候選池
    (刻意放在天氣句**之前**,維持 weather-test「天氣句在池尾」的抽尾假設);
  - **預留**:`isWeekend()` 導出,未來週末作息/事件可直接取用(本批不動平衡)。
- **驗證**:新增 `week-test.ts` 8 項(星期對應/週末判定/clockLabel/ctx 接線/週末句混入
  與平日不混),入 run-all;完整 `npm test` **52/52**(balance 無漂移——星期系統零 RNG)、
  `npm run build` 成功(119 modules)、Wrangler dry-run 成功。UI Lab 標準 18 張 0 errors
  (header 已見「7/5(日) 14:00」);容器內自訂腳本 `artifacts/ui-lab/rent/room-gender/shot.mjs`
  實測 301/302 房間視圖:DOM 斷言「♂ 男」「♀ 女」+ 截圖留檔(`room-301-gender.png`)。
- **已知限制**:星期幾依玩家瀏覽器本地時區(與既有「換日/作息」設計一致,見交接文件
  第十一節);週末目前只影響敘事氛圍,不改租客作息與平衡。

### 2026-07-18 — 關係頁性別 + 週末劇情掛鉤(Claude Code)

- **目的**:使用者要求(1)關係頁每列姓名後顯示性別符號方便 debug;(2)補性別系統的
  「資料完整性」測試證據;(3)讓劇情與星期系統掛鉤(週末/平日事件差異 + AI 敘事節奏)。
- **關係頁性別(RelationshipsPanel.vue)**:每列 `.pair` 的兩個姓名後加小號性別符號
  (♂ 藍 #9ec5e8/♀ 粉 #f0a8c6/⚧ 紫/資料缺漏顯示紅色「?」供 debug);10px、
  `vertical-align` 微調,手機直式不擠爆行。
- **性別資料完整性測試(`scripts/gender-rel-test.ts` 新增,8 項)**:
  (a)種子租客 + `generateApplicants` 抽 24 位應徵者全部都有 gender 與非空 attractedTo
  (保證關係頁永遠不會出現紅色「?」);(b)`setCouple` 對同性兩人直接設 true 被拒
  (回 false 且 romantic 未寫入)——UI 之下的寫入口徑也把關;(c)互相吸引的異性成年
  租客可成情侶(正向對照)。已登錄 `run-all.ts` REGRESSION(字母序)。
- **週末劇情掛鉤(community.ts / worker)**:
  - `morning_rush` 加 `when: () => !isWeekend(state.gameMs)`(週末沒有上班尖峰);
  - 新增週末限定社群事件 `weekend_movie`「週末電影夜」(need 3、冷卻 3 日、交誼廳):
    全員 mood+5 壓力−5、兩兩關係 +2、🎬/🍿 日誌進 Feed,文案 3 種隨機變化(sceneIndex 選句,零 RNG);
  - `GroupTemplate` 加選填 `when?`,`rollGroupEvent` 挑模板前過濾(過濾後空陣列直接
    return false 不炸);`floor_party`(樓層聚餐)設為週末限定;
  - worker `buildPrompt` 的 weekday 行擴充:週六/週日適合把劇情弧與事件往聚會、休閒、
    感情互動推,平日以工作日常為主,仍不得虛構與今日片段矛盾的作息(靜態附在同一行,無新欄位)。
- **驗證**:`community-test.ts` +10 項(morning_rush/weekend_movie 的 when 週日/週一/週六
  開關、weekend_movie fire 效果與 Feed、floor_party 平日 60 次 roll 不中/週末可中)、
  `worker-test.ts` +1 項(週末節奏提示有 weekday 才出現)。完整 `npm test` **53/53**
  (gender-rel-test 入列後),`npm run build` 成功(119 modules,`index-DHDBI2XD.js`),
  Wrangler dry-run 成功。**balance 快照無漂移、未 --update**——固定種子 10 遊戲日模擬未觸發
  受 when 門檻影響的抽選分歧(種子局僅 2 位租客,need 3 的社群/群體事件不會成立)。
  UI 目檢:容器內自訂腳本 `artifacts/ui-lab/rent/rel-gender/shot.mjs`(種入陳家豪×林小婕
  朋友關係後開關係頁),DOM 斷言「陳家豪♂ × 林小婕♀」通過 + `rel-panel.png` 截圖留檔
  (注意:Windows 綁定掛載不傳 watch 事件,改 UI 後需 `project-service.mjs start rent`
  重啟容器內 Vite 才會看到新碼)。
- **已知限制**:⚧(非二元)與紅色「?」目前只有樣式、無實際資料會顯示(招募池與種子
  租客都是 male/female 且資料齊全——正是 gender-rel-test 保證的);週末電影夜未接樓層
  演出(比照 rooftop/rainy_lounge 純日誌 + 通知);weekday 節奏提示為靜態文案,依賴
  既有 `weekday` ctx 欄位,無 schema 變更。

### 2026-07-18 — LimeZu 301 五家具美術 pilot(Codex)

- **範圍**:只替換 301 起始配置會看到的 `single_bed`、`gaming_desk`、`beanbag`、
  `tv_console`、`cat_tower` 五件家具；人物、地板、牆、門與其餘家具維持原樣。
- **素材封裝**:`public/assets/limezu/mvp301.png` 是 128×48 的專案專用小 atlas，僅含
  五張遊戲實際使用的透明裁切 sprite。原始付費素材包、單張 singles、原始檔名與資料夾
  均未納入 repo。LimeZu 原像素維持 1:1、不做平滑縮放；較高的桌子/電視由 footprint
  往上延伸，旋轉仍沿用既有 Canvas transform 與碰撞格。
- **runtime 與 fallback**:`src/art/limezu.ts` 提供冪等、非阻塞 atlas 預載與 furniture-id
  映射；Node 沒有 `Image`、HTTP 載入失敗、未知 id 或 `drawImage` 失敗時皆回傳 false，
  只警告一次且不 reject。`render.ts` 只在 atlas 成功繪製時提前返回，否則完整執行原本
  程序家具繪圖；`main.ts` 預載不阻塞 Vue mount。
- **授權註記**:新增 `THIRD_PARTY_ASSETS.md`，標註
  `Modern Interiors by LimeZu — limezu.itch.io`、專案用途與不可轉售/散布素材包限制；
  `SettingsPanel.vue` 底部也顯示可點擊的 `Modern Interiors by LimeZu` 官方素材頁連結，
  讓署名隨遊戲部署可見(`target="_blank"` + `rel="noopener noreferrer"`)。
- **驗證**:新增 `limezu-art-test.ts` 9 項並登錄 run-all，覆蓋 Node safe false、並行預載
  冪等、五家具映射來源格位、1:1 偏移、未知 id、不疊程序像素、繪製失敗 fallback、
  未載入 fallback 與 warn-once。完整 `npm test` **54/54**(53 支回歸 + sim-trace)通過；
  `npm run build` 成功(120 modules)。Mobile UI Lab 必須在整合回主 checkout 後執行，
  因共用 UI Lab 不指向隔離 worktree。
- **已知限制**:這是家具垂直切片；角色動畫、房間結構與完整家具目錄尚未改用 LimeZu。
  `cat_tower` pilot 使用較像貓用品的彩色籃狀寵物窩，但仍沿用既有 1×2 碰撞 footprint，
  後續可在不動玩法的前提下替換 atlas 對應 frame。

### 2026-07-18 — LimeZu 家具錯誤引用回退與完整資產稽核(Codex)

- **使用者回饋與稽核結果**:`beanbag` 的外部 frame 實為直背單椅、`tv_console` 缺少清楚的
  遊戲主機且 40px 高度／透視與 2×1 footprint 突兀、`cat_tower` 實為 15×17 寵物籃／窩，
  且只畫在 1×2 footprint 的下半格。完整掃描 runtime raster／SVG URL、家具 id 映射、尺寸與
  footprint 後，`single_bed`、`gaming_desk` 引用正確，未發現第 4 個錯引；`favicon.svg` 路徑正確。
  其他家具、角色與貓咪皆是 Canvas 程序像素，UI 圖示是 emoji／CSS；
  `docs/art-reference/cat-interactions-concept.png` 僅為文件概念參考，不會載入遊戲。
- **修正**:`src/art/limezu.ts` 的 runtime 白名單與 frame 表只保留核准的 `single_bed`、
  `gaming_desk`；三個錯誤 id 因此回到 `render.ts` 原有程序繪圖。`mvp301.png` 無損裁成 64×30，
  保留床與桌原本的 0..63 source 座標，移除右側全部錯誤像素；`THIRD_PARTY_ASSETS.md` 同步改為
  兩張核准 sprite。
- **回歸保護**:`limezu-art-test.ts` 現在鎖定只准兩個 id 走 atlas、三個退役 id 必須程序 fallback，
  並直接讀 PNG IHDR 驗證 atlas 尺寸、所有 source rect 不越界且互不重疊。隔離 worktree 無可用
  `tsx`／Vite 安裝；`npm ci` 先遇 npm cache EPERM，升權重重試又被中斷，因此本分支只能完成
  64×30 atlas 靜態／人工檢視與 `git diff --check`，針對性測試、完整 `npm test` 與 build 留待
  合併主 checkout 後使用既有工具鏈執行。
- **已知範圍**:LimeZu 映射以 furniture id 全域生效，不限 301 房；所以核准的 `single_bed` 也會
  套用於 302～304 與日後購買的同 id 家具。這是既有架構範圍，不是 frame 錯引。本批不改玩法、
  數值或 RNG，不需更新 balance 快照；整合後仍應跑 Mobile UI Lab 逐張確認程序 fallback 外觀。

### 2026-07-18 — LimeZu 301-only 地板比較版(Codex)

- **目的／範圍**:依使用者要求，只替換 301 室內地板作視覺比較；牆、門、`map.ts`、碰撞、
  家具映射與角色／房間鏡頭均不改，不擴大為整棟素材重構。
- **素材切片**:`public/assets/limezu/r301-floor.png` 從本機已授權的 Modern Interiors
  `Room_Builder_Floors_16x16.png` 精確裁出 source `(64,208,48,16)`，只含三個完全不透明的
  16×16 地板變體；PowerShell `System.Drawing` 驗證輸出為 48×16、768 像素 alpha 全為 255。
  原始付費素材與來源目錄未納入版控，也未使用生成式影像。
- **runtime 與 fallback**:`src/art/limezu.ts` 新增獨立、冪等、非阻塞的 301 地板 loader 與
  `tryDraw`，不共用家具載入狀態；`main.ts` 啟動時不等待地預載。`composeFloor` 先畫既有程序
  地板，再只於 301 的 5×7 格逐格 1:1 覆蓋，變體固定用 `(c + 2*r) % 3`，目的座標範圍嚴格為
  `[16,96) × [16,128)`。未載入、HTTP/error、非法 frame 或 `drawImage` 例外時一律回傳 false，
  下層程序地板完整保留；既有日夜 tint 仍在全場景最後套用，FloorMap 與 PixelDollhouse 繼續
  共用同一個 `composeFloor`。
- **測試／驗證**:`scripts/limezu-art-test.ts` 擴充 PNG 48×16、三 frame 邊界／互斥、Node-safe、
  loader 冪等與失敗、1:1 單格、非法 frame、compose 35 calls 全落在 301、固定 pattern、
  draw failure warn-once 與程序 fallback。指定隔離 worktree 沒有 `node_modules`；`npx tsx`
  等候 30 秒逾時，直接借主 checkout 的 tsx 則因 worktree 無法解析 `vue` 而停止。依協作要求
  未執行 `npm ci`；已通過 `git diff --check`，專項、完整 `npm test`、`npm run build` 與 UI Lab
  留待整合主 checkout 後執行。
- **已知限制／平衡**:本批只比較 301 地板，不處理牆與門；無玩法、RNG、存檔或平衡變更，
  不更新 balance 快照。整合後需在三種手機尺寸及 301 房間鏡頭目檢木紋密度與家具辨識度。
### 2026-07-18｜301 懶骨頭、電視遊戲櫃、貓跳台像素重畫（Codex）

- **原因**：逐區稽核已購買的 LimeZu Modern Interiors 後，沒有能高可信代表懶骨頭或貓跳台的素材；唯一明確電視是 32×32 老式 CRT，也不符合「現代電視＋遊戲主機」。因此不再硬套近似但錯誤的付費素材。
- **美術調整**：重畫 `src/furniture/render.ts` 的三個 Canvas 程序 drawer。懶骨頭改成上窄下寬、帶塌陷與高光的 16×16 輪廓；電視櫃改成 32×16 薄型螢幕、暖木低櫃、白色直立主機與控制器；貓跳台改成 16×32 錯位平台、纏繩抓柱、貓洞及垂掛玩具。
- **契約保持**：未修改家具 id、footprint、interact、價格、屬性、初始格位、旋轉、商店、寵物減損、互動解鎖或 LimeZu atlas 映射。所有本體維持 footprint 內，只有既有接地陰影可下伸 2px。
- **測試**：新增 `scripts/furniture-art-test.ts` 並納入 `run-all.ts`，鎖定整數像素、畫框 envelope、四方向 Canvas transform 及三件家具的辨識特徵。

### 2026-07-18｜LimeZu batch1:16 件家具 + 7 房地板全面上素材(Claude Code)

- **目的**:依中控核准的校樣選件,把 LimeZu Modern Interiors 素材從「301 兩件家具 + 301 地板」
  擴大到全樓層:家具白名單 18 個 id、地板七房(r301/r302/r303/r304/lounge/bathroom/laundry)
  各三個 16×16 變體,並建立可重現的 atlas 建置管線。
- **選件規則與跳過件**:sprite 裁透明邊後寬 ≤ footprint 寬×16+4px 才可用;高度可超、
  dy 底邊錨定往上長、dx 置中。依規則裁決:coffee_machine 主選 Kt178(22px)超寬改備選 Kt177;
  counter 主選 Kt135 為直向件與 2×1 橫向 footprint 不符,改橫向備選 Kt121;
  washing_machine/laundry_washer 主選 Ba87(20px=上限)可用,兩 id 共用同一 frame。
  跳過五件保留程序繪圖:double_bed(Bd226/250/235 皆直向 2×3,Bedroom 無橫向雙人床,
  125-130 為雙層床側視)、bookshelf(Lb64/57/65 裁後皆 27px > 20px)、bathtub(Ba151/152 直向,
  無橫向浴缸)、bath_sink(Ba5/6/7 皆 31px > 20px)、lounge_plant(Lv18=22px、Lv14=23px 皆超寬)。
- **管線**:新增 `scripts/limezu-manifest.json`(id → 來源 single 相對路徑 + 裁切框 + atlas 位置;
  地板 room → sheet 格座標;含跳過件原因)與 `scripts/build-limezu-atlas.py`(Pillow;來源缺檔、
  裁切框不貼齊、越界、重疊一律報錯不落地),產出 `public/assets/limezu/furniture.png`(128×96,
  17 frames)與 `floors.png`(48×112,7 列)。舊 `mvp301.png`/`r301-floor.png` 已刪:single_bed
  溯源為 `Bedroom_Singles_1`、gaming_desk 為 `Living_Room_Singles_19`(像素逐位相同),
  r301 地板列與舊檔位元相同,遷移無視覺差。
- **runtime**:`src/art/limezu.ts` 白名單擴為 18 id、地板改 roomId → 3 變體表,floor API 更名
  (`preloadLimezuFloorAtlas`/`tryDrawLimezuFloor`);維持不縮放 1:1、載入或繪製失敗回 false 走
  程序 fallback 的契約。`src/floor/floorScene.ts` 的 `drawR301FloorOverlay` 一般化為
  `drawLimezuFloorOverlay`:範圍取自 map.ts(四房 ROOM_RECTS + 新匯出的 LOUNGE_HALL_RECT +
  FACILITY_RECTS),逐格檢查 GRID 區域(浴室隔間牆列自動跳過),變體沿用房內左上角原點的
  `(c+2r)%3`,r301 視覺與單房版完全一致;走廊與大門不鋪。`src/main.ts` preload 對應新檔名。
- **測試/驗證**:重寫 `scripts/limezu-art-test.ts`(42 項):manifest↔frame↔PNG IHDR 三方一致、
  dx/dy 公式與寬度規則逐 id 驗證、跳過件不得入白名單、21 地板 frame 邊界/互斥、compose 覆蓋
  289 格(四房 140+大廳 84+浴室 30+洗衣間 35)與 pattern 逐格比對、走廊/大門/隔間牆不覆蓋、
  tv_console 未載入 atlas 時仍走程序繪圖(保住 furniture-art-test 指紋前提,該檔未動)。
  `npm test` 55/55 通過(balance 無漂移)、`npm run build` 通過、`npx wrangler deploy --dry-run`
  通過;容器內 Playwright 截圖(artifacts/ui-lab/rent/limezu-batch1/)逐張目檢 1:1 清晰度、
  無越格穿模、地板成套。
- **已知限制**:五件跳過件仍為程序繪圖(見 manifest `skipped`);LimeZu 映射依 furniture id
  全域生效,303/304 的 single_bed 與日後購買的同 id 家具都會套用;純渲染變更,無玩法/RNG/
  平衡影響,balance 快照未動。

### 2026-07-18｜LimeZu 第二批:Generic 主題表 5 件家具入 atlas(Claude Code)

- **目的**:依中控目檢選件,從 `Theme_Sorter/1_Generic_16x16.png`(256×1248)新增第二批
  家具映射;中控給的格座標為約略值,實際以像素連通元件分析(alpha 8-連通 bbox)微調貼合。
- **入選 5 件**(manifest 直接記 theme sheet 檔 + 像素裁切框;atlas 由 96 高擴為 128×128,
  新件全部貼在 y=96 新帶):
  - `shared_sofa` crop(96,176,48,26)→atlas(0,96):紅色金框「雙座沙發+同款單人椅」相連組,
    整組恰 48px 貼滿 3×1 footprint(中控目檢的「紅色三座沙發」實為此 2+1 組合,輪廓相連、
    bbox 恰 48×26,朝下);
  - `loveseat` crop(1,254,30,18)→atlas(48,96):米色雙座木沙發(主選),dx=1 置中;
  - `lounge_plant` crop(240,457,16,31)→atlas(78,96):木盆闊葉樹(16px 寬恰合 1 格;
    原主選棕櫚 (212,408) 裁邊 23px 超寬,備選區實為地毯,改挑同表列 28-30 的盆栽樹);
  - `wood_chair` crop(49,186,14,22)→atlas(94,96):淺木椅面向下(背橫桿在上、椅面在下;
    中控指的列 50-51 該區實為背面/側面視角,面向下的淺木椅在列 11-12);
  - `coffee_table` crop(81,81,14,14)→atlas(108,96):淺木圓小桌(主選 (8,33) 區為薄牆架,
    列 33-38 小桌裁邊皆 22px > 1 格上限 20px,改挑列 5 的 14×14 圓桌)。
- **跳過 2 件**(寫入 manifest `skipped`,保留程序繪圖):
  - `double_bed`:Generic 表唯一橫向床為 (60,253) 四柱床 61×62,寬 61 > 3 格上限 52 且
    床架鏤空,其餘床皆直向 top-down;
  - `lounge_rug`:規則要求整張恰 footprint 4×3 = 64×48;米色綠邊毯 (3,706) 實測 43×28、
    藍邊 (0,738) 48×28 皆不合,列 66-75 的地毯為 9-slice 模組件無法單一裁切整張。
- **變更檔案**:`scripts/limezu-manifest.json`(atlas 128×128、+5 件、skipped 更新)、
  `public/assets/limezu/furniture.png`(重跑 build-limezu-atlas.py,22 frames + 1 alias;
  floors.png 位元不變)、`src/art/limezu.ts`(白名單 18→23、frames +5)、
  `scripts/limezu-art-test.ts`(42→47 項:id 數 23、atlas 128×128、skipped 改列 lounge_rug、
  新增第二批 y≥96 帶/shared_sofa 48px 全寬/5 件可繪製/lounge_rug 不誤畫/shared_sofa
  未載入 fallback);`scripts/furniture-art-test.ts` 未動。
- **驗證**:`npm test` 55/55 全綠(limezu-art-test 47 項;balance-test OK 無漂移——純渲染)、
  `npm run build` 成功(120 modules)、`npx wrangler deploy --dry-run` 成功;容器內 Playwright
  截圖 `artifacts/ui-lab/rent/limezu-batch2/`(樓層全景、交誼廳特寫、雙人床房間)逐張目檢
  1:1 清晰、無穿模。
- **已知限制**:double_bed/bookshelf/bathtub/bath_sink/lounge_rug 仍為程序繪圖;
  shared_sofa 為 2+1 相連組合件,兩單元交界有 2px 深色框線(原素材如此,非裁切瑕疵);
  純渲染變更,無玩法/RNG/平衡影響,balance 快照未動。

### 2026-07-19｜LimeZu 第三批:整棟牆面換素材 + 茶几移位(Claude Code)

- **目的**:依中控拍板把整棟程序牆(#5b5470 頂/#413a55 面)換成 LimeZu 貼圖:
  「紫色牆身+暖色(橙)踢腳線」;另修交誼廳茶几被新沙發 sprite(往上長 10px)遮住的問題。
- **選件確認(逐格開圖)**:`Room_Builder_Walls_16x16.png`(512×640)右側欄群列 4-5 為紫色套
  (牆身 #8185C1、深紫飾線 #685AA3、白頂蓋條 #F8F8F8);全表掃描確認**沒有**「紫身+橙踢腳」
  一體成形的套組(該套自帶踢腳為深紫+灰)。橙踢腳實際來自專用的
  `Room_Builder_Baseboards_16x16.png`(96×96)橙色組——LimeZu 的模組化設計即為「任一牆身 ×
  任一踢腳板」組合,故取紫套牆身/頂蓋 8 件 + 橙踢腳橫條(tile(1,2) y10-15,16×6,恰等於
  程序牆南面 6px)。`Room_Builder_3d_walls_16x16.png` 為帶透視的斜面立面件,與本作正俯視
  3/4 幾何不合,不採用。
- **實作(只換貼圖、不換幾何)**:`drawWalls` 保留「wall 格整格畫頂 + 南鄰非牆畫 6px 面」的
  既有判定,僅把填色換成貼圖:北鄰非牆 → cap(白頂蓋條+牆身),否則 body(牆身延伸);
  西/東鄰非牆側用帶深色收邊線的 left/right/both 變體(素材原生件,轉角即頂蓋直接延伸,
  無自創幾何);南向面(含房間分隔牆、外牆、浴室乾濕隔間、走廊邊牆、大門列上緣)鋪 16×6
  橙踢腳條。大門開口與 entrance 處理不動。日夜色調為全畫布後製,自然作用於新牆。
- **管線/檔案**:`scripts/limezu-manifest.json` 新增 `wall_atlas`(64×48)+`walls` 9 件;
  `scripts/build-limezu-atlas.py` 新增牆段(全不透明檢查+越界/重疊防呆),產出
  `public/assets/limezu/walls.png`(furniture.png/floors.png 位元不變);`src/art/limezu.ts`
  新增 `LIMEZU_WALL_PIECE_IDS/FRAMES`、`preloadLimezuWallAtlas`、`tryDrawLimezuWallPiece`
  (1:1 不縮放、未載入/未知件/繪製失敗回 false 走程序牆,warn-once,契約與家具/地板一致)、
  `resetLimezuWallAtlasForTests`;`src/main.ts` 加 preload;`src/floor/floorScene.ts` 換裝。
- **茶几移位**:`src/floor/map.ts` 種子 coffee_table (11,12)→(11,11)(沙發正前一格改上移一格,
  仍在地毯上、互動點落回空出的 (11,12),不擋 cols 7-8 走道與大廳動線)。
- **測試/驗證**:`scripts/limezu-art-test.ts` 47→64 項(walls.png 尺寸/manifest↔frame 一致/
  來源檔白名單/互不重疊/16×6 踢腳、未載入時 composeFloor 零 drawImage 且程序牆保留、
  並行預載共用 promise、逐牆格鄰格選件與踢腳鋪設全圖比對、(0,0)=cap_left 抽查、繪製失敗
  warn-once 回退、載入失敗 resolve false);`furniture-art-test.ts` 未動。`npm test` 55/55
  全綠(**balance-test OK 無漂移,未跑 --update**——茶几移位未改變現行路徑/互動結算)、
  `npm run build` 成功(120 modules)、`npx wrangler deploy --dry-run` 成功。容器截圖
  `artifacts/ui-lab/rent/limezu-batch3/`(白天/夜間全景、301 細看、交誼廳+浴室局部)逐張目檢。
- **已知限制**:單列牆(如房間與大廳之間)受 16px 幾何壓縮,紫牆身在頂蓋與踢腳之間僅露
  約 4px(素材原設計為兩格高牆面);直向牆(南北走向)沿用牆身延伸件,側邊僅深色收邊線、
  不做東西向踢腳(程序牆本來也無側面);純渲染+種子擺位變更,無 RNG/平衡影響。

### 2026-07-19 — 交接給 Codex(Claude Code 額度用盡前的移交)

**今日(07-18~19)Claude Code 完成並已上線的批次**:
1. `2a32ed4` 房客頁性別 chip + 星期系統(`sim/week.ts`,header 時鐘帶星期、AI ctx weekday)
2. `22cd26a` 關係頁性別符號 + 週末劇情掛鉤(週末電影夜、樓層聚餐/早晨尖峰 when 門檻、`gender-rel-test`)
3. `4466f44` LimeZu 第一批:7 區地板 + 15 件家具(manifest+build-limezu-atlas.py 管線建立)
4. `44a1ca2` LimeZu 第二批:交誼廳沙發組/雙人沙發/木椅/圓茶几/盆栽樹(白名單 23 id)
5. 本 commit LimeZu 第三批:整棟牆面(紫牆+橙踢腳)+ 茶几移位——**中控已目檢白天全景與
   balance/limezu-art-test(64/64)/build,但夜間全景與 301/浴室細看截圖未經人工複核**,
   接手者請先看 `artifacts/ui-lab/rent/limezu-batch3/` 其餘截圖或線上實測,有問題再調。

**LimeZu 管線使用法(接手必讀)**:選件先出帶編號 contact sheet 目檢(工具腳本在
`C:\Users\User\AppData\Local\Temp\...\scratchpad\make-contacts.py`,產物在
`C:\Users\User\claude_try\input_image\limezu-modern-interiors\*.png`,素材包在同目錄 `extracted\`)
→ `scripts/limezu-manifest.json` 增列(id/來源/裁切框;不合身寫進 `skipped`)→
`python scripts/build-limezu-atlas.py` 重建 atlas → `src/art/limezu.ts` frames/白名單同步 →
`limezu-art-test.ts` 擴充。**鐵律:sprite 寬 ≤ footprint.w×16+4、朝向要合,不合身不硬套**
(Codex 第一輪與 Claude 兩輪共 5 件因此保留手繪:double_bed/bookshelf/bathtub/bath_sink/lounge_rug,
原因都在 manifest.skipped)。UI 改動後容器 vite 必須重啟才 serve 新碼(Windows 掛載無 watch)。

**即將要做/建議續作(依優先序)**:
1. **牆面上線後的實測回饋**:單列牆 4px 牆身的觀感若使用者不滿意,備案是改用素材的
   單格矮牆變體或調整頂蓋高度比例(都只動 `limezu.ts` 座標與 `floorScene.drawWalls` 貼圖段)。
2. **LimeZu 動畫件評估**:`3_Animated_objects` 有電視/洗衣機動畫幀,現行渲染是靜態 Canvas,
   若要動畫需在 floorScene 加幀計時(中型工程,先問使用者要不要)。
3. **niche 主題補件**:mic_desk(23_TV_Studio)、easel(7_Art)、bar_counter/lounge_console
   (26_Condominium/16_Grocery)等尚未掃過的主題,照管線流程逐批做。
4. 非美術線待辦不變:PWA(E-3,需真機)、音效(G-9,使用者曾說略過)、週報卡(G-10)、
   dashboard 速率限制(I-3,零程式碼)。🔴 項(樓層擴建/多人存檔/自帶 key)先問使用者。

**其他狀態**:外層 workspace repo 仍有 `publish-multiagent-rules` lease 卡在 gh token 失效,
等使用者 `gh auth login` 後由 Codex 收尾;`WORKSPACE_STATUS.md` 一併更新(記到本批 commit)。

### 2026-07-19 — 待辦:AI 事件品質崩壞的診斷與修法(交 Codex 實作,已與使用者確認方向)

**使用者回報**:近期 AI 房東抉擇事件不知所云。實例(截圖存
`C:\Users\User\claude_try\input_image\怪異AI事件.jpg`):陳家豪頁面彈出「保护橘子」——
「橘子对邱柏翰产生了依恋,可能影响其生存安全」,選項「收养邱柏翰/驱逐邱柏翰/继续加班」。
三大病徵:簡體字直接上畫面、貓與房客角色錯置+素材亂入、出現驅逐選項且掛錯租客頁。

**診斷(Claude Code 2026-07-19 唯讀調查,行號以當日 HEAD=eef47d2 為準)**:
1. **根因:provider 順序 07-15 被翻轉**。commit `9b4178a` 為省 Gemini 額度把 narrate 的
   attempts 改為 Qwen 30B → Llama 8B → Gemini(worker/index.ts:499-508)。從此只要 Workers AI
   活著,事件就由免費弱模型產出;`chooseGeminiModel`(index.ts:305-308)的 eventDue 分流只影響
   「輪到 Gemini 時用哪個型號」,救不了排序。時間點與「最近變差」吻合。
2. **事件文案是唯一漏接品質閘門的 AI 文字**:`sanitizeAiEvent`(src/sim/events.ts:195-221)對
   title/description/choices[].label/hint 只做型別+長度截斷,**沒過** narrativeQuality 的
   `normalizeNarrativeLanguage()`(簡轉繁+英文/羅馬化姓名清除)——diary/summary 都有接
   (worker index.ts:168-170、前端 sanitizeDiaryText/sanitizeSummaryText),event 漏了。
3. **驅逐禁令只在 prompt 與效果層**:`cleanEffect`(events.ts:147-188)不讀 AI 的 evict 旗標
   (效果安全),但選項**文字**無關鍵字過濾 → 「文字說驅逐、按了沒事」的語意脫節。
4. **人名一致性無檢查**:事件掛陳家豪、主角卻是邱柏翰;沒有「描述人名須=本人或 event.with」的驗證。
5. 小噪音:eventDue 門檻 prompt 側 ≥3 天(narration.ts:406)、採納側 ≥2 天(narration.ts:199),
   不一致但無害,可順手統一。

**修法(依成本效益排序,①+②可解九成)**:
1. 🟢 **事件文案套完整敘事品質閘門**(純前端):sanitizeAiEvent 對四類文字過
   `normalizeNarrativeLanguage`;加語意黑名單——label/hint/description 含「驅逐/趕走/退租/收養+
   租客名」→ **整個 event 丟棄退回 null**(事件本可為 null,寧缺勿爛)。
2. 🟢 **事件只信 Gemini**(worker 小改):qwen/llama 產的回應剝掉 event 只留 diary(parseResult
   或 handleNarrate 依 provider 判斷);事件等輪到 Gemini 那次再生。日記可將就、抉擇事件不行;
   事件有 3 日冷卻屬低頻,額度影響極小。
3. 🟡 **人名一致性檢查**:description/選項出現的在住租客名若非事件主人且不在 with → 丟棄。
4. 🟢 **回歸測試**:把截圖案例做成 fixture(模擬 qwen 回傳該 event),斷言被丟棄/轉繁;
   新測試記得加進 run-all.ts REGRESSION。
5. 實作後照慣例:`npm test` 全綠 + build + Wrangler dry-run;此為 AI 消毒鏈變更,
   屬「AI 輸出一律消毒」硬規則的加固,不動 canRomance/成人內容等既有底線。

### 2026-07-19 — AI 房東抉擇事件品質雙層修復(Codex)

- **目的／重現**:接手上節待辦並以 `input_image/怪異AI事件.jpg` 的「保護橘子」案例建立
  正式回歸；目標是阻止簡體字、收養／驅逐租客、無關租客名字與事件主人錯置再次上畫面。
- **provider 防線**:`worker/index.ts` 保留平淡日記由 Workers AI Qwen → Llama 優先的免費策略，
  但 `eventDue=true` 時改為 Gemini Flash 優先。無論請求順序如何，Qwen／Llama 成功回應中的
  `event` 都會在 Worker 回應層強制改成 `null`，只有 Gemini provider 可以送出房東抉擇；Gemini
  失敗時仍採 Workers 日記，但寧可沒有 AI 抉擇，也不再讓弱模型事件進前端。
- **前端品質閘門**:`sanitizeAiEvent()` 新增事件主人姓名，title／description／choice label／hint
  及 effect.memory 全部先過 `normalizeNarrativeLanguage()`；補齊本案例與常見事件詞的簡轉繁。
  任何文案含「驅逐／趕走／退租」、把「收養」施加於在住租客，或點名非 owner／合法 `with`
  的其他在住租客，整個事件直接回 `null`。正常收養寵物與 owner + with 跨租客事件仍保留；
  數值夾值、directive／interaction 白名單、成人與戀愛硬規則未放寬。
- **冷卻一致**:AI event 採納端由 2 天改為 3 天，與送給 Worker 的 `eventDue` 條件一致；規則式
  本地事件仍維持原本 2 天節奏。
- **回歸／驗證**:新增 `scripts/ai-event-quality-test.ts` 10 項並納入正式 runner；`worker-test.ts`
  擴充至 62 項，鎖定平日／事件日 provider 順序及 Workers event 剝除。`npm test` 56/56 支全綠
  (含 balance 與 sim-trace，未更新 balance 快照)、`npm run build` 成功(120 modules)、
  `npx wrangler deploy --dry-run` 成功(AI／ASSETS bindings)、`git diff --check` 通過。無 UI 變更，
  不需跑 Mobile UI Lab。
- **已知取捨**:2026-07-15 正式 Worker 曾因出口地區遭 Gemini 拒絕；若部署環境仍相同，事件日
  會先嘗試 Gemini、再落回「只有 Workers 日記、沒有 AI 抉擇」。本批依已確認方向選擇品質優先；
  規則式抉擇事件仍照常運作，不會讓遊戲完全沒有事件。

### 2026-07-19 — 關係升級原因與房客目標指引(Codex)

- **問題釐清**:關係頁顯示「摯友」並非漏掉戀愛升級；現行規則要求雙方皆為成人、戀愛取向互相
  符合，且沒有其他伴侶。異性組合也可能因取向不互相符合而固定維持摯友。若條件符合，關係達
  75 後還需在交誼廳發生一次自然相遇；相性為負或已有伴侶也會阻擋成為情侶。
- **關係提示**:`src/sim/social.ts` 新增逐對關係的進展說明，`RelationshipsPanel.vue` 直接顯示
  當前阻擋原因或下一步，包括未成年、取向不符、已有伴侶、相性不足、尚未達 75，以及等待交誼廳
  相遇；不再只靠籠統頁尾文字讓玩家猜測。
- **目標指引**:`src/sim/wishes.ts` 將 8 種房客目標說明改成可執行的數值門檻；`App.vue` 在房客
  詳情常駐顯示目標、完成百分比、進度條與達成方式，完成後也有明確狀態，不再只藏在 tooltip。
- **回歸測試**:`romance-integrity-test.ts` 新增取向不符與可升級提示案例；`wish-test.ts` 鎖定目標
  說明的關鍵門檻。`npm test -- romance wish` 通過(23／32 assertions)，完整 `npm test` 56/56
  支全綠(含 balance 與 sim-trace，未更新 balance 快照)，`npm run build` 成功(120 modules)，
  `git diff --check` 通過。
- **UI 驗證**:`npm run ui:shot -- rent` 產出 3 種手機尺寸共 18 張，`report.json` 全部 0 error／
  0 warning；另以 390×844、430×932 各檢查關係提示與房客目標畫面共 4 張，文字完整且無溢位。
- **已知取捨**:本批只提高規則透明度，沒有擅自修改既有房客的性別或戀愛取向，也沒有遷移舊存檔；
  因此舊角色若本來取向不互相符合，仍會維持摯友，但現在玩家能直接看到原因。

### 2026-07-22 — 圓夢畢業第一批:兩軌制心願 + 房東獎勵核心(Claude Code)

- **目的**(使用者已拍板):心願實現後不再只是「留一個 ✓」——分成兩軌讓結局有重量:
  畢業型好好送走(有謝禮與口碑),安居型留下來變成公寓的資產;順手根治孤兒貓 bug。
- **設計決策**:
  1. **兩軌制**:`stage_dream`、`finish_masterwork` 改 `graduates: true`(告別文案分別為
     被劇團簽走/搬進工作室);畢業型 4 條(開店/論文/舞台/代表作)、安居型 4 條
     (career_step/recover_rhythm/feel_at_home/settle_life)。
  2. **模範房客**(安居型實現時授予,`rt.modelTenant` 入存檔):租金自願 +3%(一次性,
     只有承租人;major 日誌「主動說要多付一點」)、在住期間全樓其他租客每日 mood +0.5
     (掛在 wishPass 每日入口,多位模範不疊超過 +1,模範不吃自己的光環)、續住宣言
     major 日誌 + notify;房客頁金色 chip(比照 growth chip 樣式)。
  3. **謝禮紅包+押金退還**(僅 wishPass 排定的圓夢離開,經新函式 `tenancy.graduateFarewell`;
     房東請離/分手搬走不適用):紅包 = round(0.5×月租×(1+好感/100)) 上限 1×月租,
     押金退還 = DEPOSIT_MONTHS×離開時月租;兩筆都走 addMoney 進 ledger + notify + major 日誌。
  4. **口碑系統**:選**新檔 `src/sim/reputation.ts`** 而非併入 legacy.ts——legacy 檔頭明訂
     「只寫成就/名冊/通知,不動數值、不影響 balance」,口碑會改招租出價與星等,併入會破壞
     該契約,且 recruit 若 import legacy 會連帶拉進 finance 依賴。`state.reputation`(0~100,
     入存檔):畢業 +8、安居圓夢 +3;效果 = matchStars raw + reputation×0.3(滿口碑約一個
     星等區間)+ 應徵者開價 +reputation×0.05%(滿口碑 +5%,接在 `offeredRent` 的行情乘數;
     recruit 沒有獨立 budget 欄位,開價即租金容忍的落點)。FinancePanel 餘額下方顯示
     「⭐ 房東口碑 N/100」,title 註明來源與效果。
  5. **貓去留 + 孤兒貓根治**:畢業型實現當下若有貓,掛規則式 pendingEvent
     `wish_cat_farewell`(不經 AI,兩選項;未決時離開日預設帶走)。「留下成樓貓」=
     `pet.ownerId = "landlord"`(Pet.ownerId 型別是 string,哨兵值合法、零遷移;
     state.pets 的 key 維持原飼主 id 保唯一)。pets.ts 各 pass 特判樓貓:遊蕩錨點改交誼廳、
     串門/搗蛋/貓週記照常(文案改「樓貓」、無主時通知房東、不動關係);貓的識別一律改用
     record key(catId)——樓貓的 ownerId 都是 "landlord",不能再當 key(petAgents/floorScene
     的配對同步改 catId,舊存檔 pairWith/冷卻 key 因一般貓 catId===ownerId 完全相容)。
     孤兒貓修復:`repairOrphanCats()` 在 ensurePets(load 入口)與 petsPass 防禦執行,
     無主貓轉樓貓 + notify。**moveOut 通用路徑**(含請離/分手):自己養的貓一律 delete
     跟著走 + 名冊 memory 提一筆「帶著愛貓離開」。
  6. **成就 4 項**(共 24):🎓 首位畢業生、🎓 桃李天下(×3,靠新計數 `state.graduateCount`,
     不依賴 50 筆上限的 alumni)、🏠 第一位模範房客、💑 雙雙圓夢(隱藏,情侶皆已實現,
     fulfillWish 時查 romantic 關係對象)。LegacyPanel 全動態,無需改。
- **主要檔案**:`src/sim/wishes.ts`(兩軌/模範/貓抉擇/光環)、`src/sim/reputation.ts`(新)、
  `src/sim/tenancy.ts`(graduateFarewell/decide 貓去留/moveOut 帶貓)、`src/sim/pets.ts`
  (樓貓+catId 化+修復)、`src/sim/legacy.ts`(成就+名冊記貓)、`src/sim/recruit.ts`(口碑效果)、
  `src/sim/gameState.ts`+`persistence.ts`(reputation/graduateCount/modelTenant 入存檔,
  `?? 預設` 讀取、不升版)、`src/floor/petAgents.ts`+`floorScene.ts`+`FloorMap.vue`(catId)、
  `src/App.vue`(模範 chip)、`FinancePanel.vue`(口碑列)。
- **驗證**:`wish-test.ts` 擴充至 72 項(兩軌/模範+3%與光環封頂/紅包押金記帳/口碑增長與
  星等開價效果/貓去留三情境/孤兒貓修復/新成就/存檔往返含 load);`legacy-test.ts` 成就數
  斷言更新(24 項/隱藏 4)。完整 `npm test` 56/56 支全綠、`npm run build` 成功(121 modules)、
  `npx wrangler deploy --dry-run` 成功(AI/ASSETS bindings)。
- **balance 快照未更新且無漂移(合理)**:基準是固定種子 10 遊戲日模擬,心願進度最快約 40%,
  無人實現 → 兩軌/光環/口碑全在實現後才生效,快照零差異是正確結果,不需 `--update`。
- **UI 驗證**:容器重啟 vite 後以一次性腳本(`artifacts/ui-lab/wish-graduate-check.mjs`,
  localStorage 注入模範房客+口碑 42 後截圖、截完 clear)產出 3 張到
  `artifacts/ui-lab/rent/wish-graduate-1/`:金色「🏠 模範房客」chip 與心願 ✓、收支頁
  「⭐ 房東口碑 42/100」、成就冊 2/24(新成就入列、隱藏遮蔽正常),皆已目檢無溢位。
- **已知限制/後續**:第二批待做 = 送別會/告別信/紀念物;口碑目前只影響招租(星等+開價),
  未接在住租客的漲租容忍;樓貓無人可餵的長期照顧成本未建模(現為零成本彩蛋);
  種子租客林小姐(ASMR 實況主)因 finish_masterwork 改畢業型,長線會圓夢離開——屬設計
  預期,若使用者希望種子主角常駐可再談豁免。

### 2026-07-22 — 待辦交接:圓夢畢業第二批(交 Codex 實作,設計已與使用者確認)

> 第一批已上線(commit 3d37d80,HEAD)。第二批中控原本派了實作代理,但使用者決定
> 改由 Codex 接手,已停掉代理;**工作樹乾淨、無半成品**,Codex 從 3d37d80 直接開工即可。

**目標**:為「圓夢離開」補上送別的儀式感與收藏層。四項規格(依建議順序):

1. **送別會(離開當天 ambient 演出,非抉擇)**:圓夢租客離開日,若當時全樓在住 ≥2 人 →
   交誼廳歡送會,全員 mood +4/壓力 -4、兩兩關係 +2,🎉 送別日誌進全樓 Feed(major),
   文案 2~3 種隨機(繁中、含離開者名字與心願)。人數 <2 → 改發離開者獨白 major 日誌。
   接點:`wishes.ts` 的 `graduateFarewell`(第一批新增的圓夢離開路徑)結算「之前」呼叫,
   確保離開者還在名單;寫法比照 `community.ts` 的 rooftop/weekend_movie(但為離別觸發、非隨機)。
2. **告別信(模板生成,零 AI 成本)**:`AlumniEntry` 加選填 `farewell?: string`
   (persistence `?? undefined` 相容,不必升版)。模板取 心願 label/doneText + 住了幾天 +
   最好鄰居名 + 代表記憶(recordAlumnus 已有 representativeMemory);句庫依畢業軌別分組
   (登台/開店/論文/代表作各語氣)。**必須過 narrativeQuality 的 toTraditional**(比照事件消毒防簡體)。
   LegacyPanel 名冊頁顯示「✉️ 展開告別信」(手機直式勿擠)。
3. **紀念物家具(收藏層)**:每位畢業生在其原房間留一件專屬裝飾(不可賣、不佔招租、純裝飾)——
   🎭 登台=簽名海報、🏪 開店=小招牌、🎓 論文=裱框證書、📖 代表作=簽名書。
   `furniture/catalog.ts` 新 def(程序繪圖/recipe,**勿硬套 LimeZu**);紀念物綁房間不綁租客
   (空房招租後仍保留)。若「空房保留」結構困難,退而求其次:紀念物收進 alumni 條目當收藏
   圖示在 LegacyPanel 顯示,並在日誌說明原因。微光環(有紀念物房招租星等 +0.2 或租客 mood +0.3)
   可選,成本高就跳過並說明。
4. **成就**:🏛️ 名人堂(alumni 畢業生達 5 位)、✉️ 見字如面(首次展開告別信,可選)。沿用 unlock。

**收尾規範(照本專案慣例)**:wish-test/legacy-test 擴充(送別會人數門檻/告別信轉繁/紀念物留存/
新成就)+ 新 test 檔登錄 `run-all.ts`;完整 `npm test` 全綠 + `npm run build` +
`npx wrangler deploy --dry-run`;balance 若漂移檢視合理後 `--update` 並說明;工作日誌新節;
UI 動了就容器實機截圖目檢。保留既有硬規則(未成年排除戀愛線、AI 消毒/夾值/白名單)。

**第一批已完成參照(Codex 續作的地基,commit 3d37d80)**:
- 兩軌制在 `wishes.ts`:WISH_DEFS 的 `graduates` 欄位(畢業 4 條 true);fulfillWish 按此分流,
  安居軌設 `rt.modelTenant=true`;圓夢離開走 `graduateFarewell`(紅包+退押金,經 ledger)。
- 口碑在**新檔 `sim/reputation.ts`**(`state.reputation` 0~100;matchStars raw +rep×0.3、
  應徵者開價 +rep×0.05%);FinancePanel 顯示。
- 貓去留:`ownerId="landlord"` 哨兵=樓貓;貓識別已全面改用 record key(catId);
  `repairOrphanCats()` 掛 load 入口。moveOut 通用路徑一律帶貓走。
- 成就已 24 項(含隱藏「雙雙圓夢」);`state.graduateCount` 追蹤畢業人數(不受 alumni 50 上限影響)。

**其他跨批狀態(給 Codex 一併知悉)**:
- AI 房東抉擇事件品質問題**已由 Codex 於 caa311c 修復**(含 ai-event-quality-test 10 項),該待辦已結案。
- LimeZu 美術三批已上線(地板+牆面+約 20 件家具);5 件無合適素材維持手繪(見 manifest.skipped)。
- 外層 workspace 的 `publish-multiagent-rules` lease 仍卡在 gh token 失效,待使用者 `gh auth login`。

### 2026-07-22 — 狗寵物第一批:應徵者、生活事件與四花色像素美術(Codex)

- **目的／協作邊界**:使用者指定 Claude Code 繼續處理租客目標第二段,本批改在獨立
  `codex/dog-pet-art` worktree 延伸美術與寵物線,不修改心願進度／獎勵規則。因狗的圓夢去留
  必須和既有貓使用同一路徑,僅把該抉擇的硬編貓文案泛化為寵物文案;整合回 main 前仍需以
  Claude 完成後的版本 rebase,避免共享檔案互相覆蓋。
- **資料與取得方式**:`Pet.kind` 擴成 `cat | dog`;舊存檔與舊 applicant pool 缺 kind 時一律視為貓,
  不升 SAVE_VERSION。應徵者自帶寵物總率維持約 22%,其中 65% 貓／35% 狗(狗約占全部應徵者
  7.7%),不因新增物種提高事件密度。一位租客仍限一隻寵物;既有 `adopt_cat` 指令仍只領養貓。
- **狗的生活層**:`pets.ts` 新增 8 個狗名、`adoptPet/randomDogPreset/randomPetPreset`、物種相性
  (怕狗不再誤判怕貓)、狗較常待交誼廳的 hangout 權重、狗專屬串門／咬壞靠墊／如廁意外／
  七日觀察筆記。搬家、孤兒修復、AI 日記 context、歷任名冊與圓夢去留都依物種顯示繁中圖示
  與文案;舊 `wish_cat_farewell` 存檔事件仍可結算。
- **美術／渲染**:`PetAgent` 泛化為 petId + kind,沿用既有尋路、休息、睡眠與 y-sort;Canvas 新畫
  四種狗毛色(柴色／黑犬／白棕／灰犬)的走路左右、坐姿、側睡,用厚身、垂耳與前伸口鼻和貓輪廓
  區分。LimeZu 素材只有 animated cat、沒有 dog,故依專案規則採程序像素圖、不硬套錯素材。
  雙貓互動明確過濾 `kind === cat`,狗不會被寫成或畫成雙貓。另把原本只看寵物數量的 agent
  重建條件改為 ID／物種／花色／名字 signature,修掉「一進一出但總數不變」時新寵物不渲染的舊缺口。
- **UI**:招租卡可顯示「🐕 自帶狗」及樓內貓／狗相性;樓層浮標、房客頁依物種顯示 🐈／🐕。
  本機 production build 預覽實際抽到「陳思妤 + 小虎」入住,樓層與房客頁皆正確,瀏覽器
  console 0 error／0 warning。
- **測試／驗證**:`pet-test.ts` 擴至 60 項(狗領養/物種相性/agent/同數量換寵物/貓配對隔離/舊 preset fallback/
  招租貓狗比例/存檔/搬家/AI flag),`wish-test.ts` 73 項(養狗畢業去留 + 孤兒公寓犬)。完整
  `npm test` 56/56 支全綠(含 balance-test 無漂移),`npm run build` 成功(121 modules),
  `npx wrangler deploy --dry-run` 成功,`git diff --check` 通過。`scripts/render-cats.ts` 已擴成
  貓狗 contact sheet,產物 `artifacts/ui-lab/rent/dog-pet-art/pets.png` 逐姿勢目檢,不提交生成圖。
- **已知限制／下一批**:狗目前只由應徵者帶入,沒有主動領養狗事件;第一批不做狗狗配對、貓狗互動、
  遛狗、狗床／尿墊、飼養成本或狗成就。狗未使用貓跳台／貓砂盆減免。上述可作第二批,其中狗床與
  尿墊適合連同下一批程序家具美術一起做。

### 2026-07-22 — 狗寵物第二批:專屬家具、減免效果與商店提示(Codex)

- **協作邊界**:沿用獨立 `codex/dog-pet-art-batch2` worktree，只做狗寵物家具、美術、
  效果與對應 UI；不碰 Claude Code 的租客目標第二段、心願或畢業流程。
- **三件房內家具**:新增狗狗睡墊($2,600、2×1、療癒 +5)、耐咬玩具($900、1×1、
  療癒 +1)、寵物尿墊($600、1×1)。三件都用專屬 `FurnKind` 程序像素圖，維持 16px
  整數像素與四方向旋轉；睡墊用暖紅厚邊、內凹米色墊與骨頭徽記，玩具用紅色橡膠
  骨頭輪廓，尿墊用藍邊、白色吸水層，未硬套不相符的 LimeZu 素材。
- **實際效果**:`mischiefRelief()` 依飼主目前房間即時計算。耐咬玩具把狗狗破壞倍率
  從 1 降為 0.25(破壞機率 -75%)；寵物尿墊把狗狗如廁意外倍率降為 0.15(-85%)。
  狗不會誤吃貓跳台／貓砂盆效果，貓也不會誤吃狗家具；狗狗睡墊提供房間舒適度，
  不假裝有尚未實作的尋路上床功能。
- **效果可讀性**:`FurnitureDef` 新增選填 `effectHint`；商店與家具資訊卡直接顯示寵物
  家具的真實減免。既有貓跳台／貓砂盆也補上 -70%／-85% 提示，玩家不用從日誌猜效果。
- **測試／驗證**:`furniture-art-test.ts` 擴至 46/46(三件目錄設定、效果提示、像素輸出、
  整數座標、邊界與 90/180/270 度 transform)；`pet-test.ts` 擴至 64/64(狗家具分別／同時
  生效、睡墊不冒充功能品、貓狗效果隔離)。完整 `npm test` 56/56 支全綠，production
  build 成功(121 modules)，`git diff --check` 通過。
- **實機 UI 目檢**:在本機 production 等價 Vite 頁面實際購買三件家具並放入 302；商店
  的狗破壞／如廁提示、家具資訊卡、樓層上的睡墊／紅骨頭／藍白尿墊皆清楚且無溢位，
  browser console 0 error。截圖保存在 `artifacts/ui-lab/rent/dog-pet-art-batch2/`，不提交。
- **後續**:本批刻意不把雙貓配對鏈硬擴成狗／貓狗互動；可獨立做第三批「狗狗追球、
  互聞、共眠／貓狗追逐或退避」。主動領養狗事件、遛狗、飼養成本與狗成就也仍待設計。

### 2026-07-22 — 狗寵物第三批:雙狗與貓狗互動像素演出(Codex)

- **目的／協作邊界**:使用者要求繼續優化狗寵物與美術；本批在獨立
  `codex/dog-pet-interactions` worktree 泛化既有雙貓 pairing，集中修改寵物模擬、agent 與
  Canvas 樓層演出，不碰 Claude Code 的租客目標第二段、心願／畢業／招租規則與 UI。
- **雙狗互動**:新增 `fetch / sniff / nap` 三種兩小時同步演出，同一對沿用 10 遊戲小時
  冷卻。追球讓兩隻狗小跑、球在中間彈跳並帶速度線；互聞會靠近、面向彼此、顯示鼻尖嗅聞點
  與搖尾線；共眠沿用共享軟墊與 Z 字節奏。飼主會收到「雙狗互動」日誌，依事件取得 mood
  或 stress 小幅改善，互聞可讓兩位飼主關係 +1。
- **貓狗相遇**:新增 `greet / avoid`。友善時貓狗靠近、鼻尖上浮愛心，雙方 mood +2／stress -2、
  飼主關係 +1；退避時其中一方尋路到同區域較遠的安全格，雙方背向並顯示雙色警覺線與中間
  安全距離，stress +1、飼主關係 -1。樓貓／公寓犬沒有飼主時仍可演出並至少通知房東。
- **共用配對基礎**:`PetPairAction` 擴成貓 5 種、狗 3 種、貓狗 2 種；仍只使用既有
  `pairWith/pairAction/pairUntilMs`，不升 SAVE_VERSION。`petsPass()` 同一遊戲小時最多開一場
  寵物互動，避免新增物種後事件密度暴增；既有 `resolveCatPairs` API 與雙貓 RNG 路徑保留。
- **舊存檔修正**:`ensurePets()` 會把早期存檔缺少 `kind` 的寵物正規化為貓，避免舊貓被
  pairing filter 漏掉；不需修改 persistence 結構。
- **測試／驗證**:`pet-test.ts` 擴至 73/73，涵蓋雙狗三事件、日誌、同步、共眠、冷卻、貓狗
  友善／退避、agent 拉開距離及舊 kind fallback；完整 `npm test` 56/56 支全綠且 balance 無
  漂移，production build 成功(121 modules)，`git diff --check` 通過。
- **美術目檢**:`scripts/render-cats.ts` contact sheet 加入雙狗 3 幕與貓狗 2 幕，輸出
  `artifacts/ui-lab/rent/dog-pet-interactions/pets.png`；逐圖確認紅球、嗅聞／搖尾、共享睡墊、
  友善愛心與退避警覺線在木地板與暗牆上皆可讀，生成圖不提交。
- **已知限制／後續**:雙狗互動需要樓內同時有兩隻狗，自然出現率低，定位為稀有彩蛋；尚未做
  單狗玩球、主動領養狗、遛狗、飼養成本或狗成就。若下一批要提高可見度，應優先做「單狗拿
  耐咬玩具自娛」或遛狗事件，而不是拉高所有寵物事件機率。

### 2026-07-22 — 特邀租客自選貓狗(Codex)

- **目的／協作邊界**:依使用者要求，在特邀租客建立流程加入寵物選擇；獨立
  `codex/invite-pet-choice` worktree 只修改招租表單、特邀資料消毒與測試，不碰 Claude Code
  的圓夢畢業第二批、心願、告別信或紀念物流程。
- **玩家選擇**:特邀表單在性別下新增「不帶寵物／🐈 帶一隻貓／🐕 帶一隻狗」選單，
  預設維持不帶寵物。性別與寵物都由玩家直接決定，不送進 AI prompt；名字與花色使用既有
  白名單隨機 preset，不增加 API 額度或模型輸出風險。
- **既有系統串接**:`sanitizeInvited()` 第四參數只接受 `none | cat | dog`，把合法選擇寫入
  `Applicant.pet`；入住仍沿用 `moveIn → adoptPet`，因此飼主、樓層 agent、存檔與事件系統
  自動生效，不升 SAVE_VERSION、不修改 Worker/API。未知寵物值安全回退為不帶寵物。
- **測試／驗證**:`invite-test.ts` 擴至 31/31，涵蓋預設無寵物、指定貓狗、非法值回退、
  狗隨特邀租客入住及 ownerId。完整 `npm test` 56/56 支全綠且 balance 無漂移；
  `npm run build` 成功(121 modules)，`git diff --check` 通過。整合回 main 後執行 Mobile UI Lab，
  三尺寸 18 張截圖與 report 逐一目檢，0 errors／0 warnings；另以 390×844 實際展開特邀表單、
  選擇「🐕 帶一隻狗」，確認可捲動、無水平溢位、選值正確且 browser console 0 error。

### 2026-07-22 — 圓夢畢業第二批:送別會／告別信／紀念物／成就(Claude Code)

- **目的／使用者需求**:接續圓夢畢業第一批(兩軌制心願 + 謝禮紅包/退押金/口碑),為畢業
  離開補上情感留存的四項:送別會、告別信、紀念物家具、兩個新成就。零 AI 成本、不升
  SAVE_VERSION、保留既有硬規則(未成年排除戀愛線、AI 消毒/夾值/白名單)。
- **開工前地基確認**:HEAD=be4465d、工作樹乾淨。逐一確認 Codex 四批狗系統後接點結構未變——
  `graduateFarewell`(紅包+退押金,再 moveOut)、`recordAlumnus`(名冊 + representativeMemory)、
  `wish_pet_farewell`/`resolvePetFarewell`(貓狗泛化)、catalog 的 `FurnKind`/`recipe`/`effectHint`
  皆與規格相符,結構未偏移,照原設計實作。
- **1. 送別會(tenancy.farewellSendoff)**:圓夢離開當天、`graduateFarewell` 結算「之前」呼叫
  (離開者仍在名單)。全樓在住 ≥2 人 → 交誼廳歡送會:全員 mood +4/壓力 -4、兩兩關係 +2,
  🎉 送別 major 日誌(2 種文案、含離開者名字與心願 label)掛在關係最好的鄰居身上,確保
  離開者搬走後仍留在全樓 Feed;<2 人 → 改發離開者獨白 major 日誌。選句用日期+姓名雜湊,
  **不消耗 Math.random**,不擾動其他系統 RNG 次序與 balance 快照。
- **2. 告別信(legacy.recordAlumnus)**:`AlumniEntry` 新增選填 `farewell?`(persistence `{...p}`
  自動相容、undefined 不落盤,不升版)。模板依畢業軌別分組(登台/開店/論文/代表作各語氣)+
  住了幾天 + 最好鄰居名 + 代表記憶,只對畢業型(fulfilledDay 且 graduates)生成;輸出過
  `narrativeQuality.toTraditional` 防簡體。LegacyPanel 名冊頁新增「✉️ 展開/收起告別信」,
  手機直式排版(左側強調邊、行高 1.75)。
- **3. 紀念物家具(收藏層)**:catalog 新增 4 件 recipe 程序繪圖家具(簽名海報/小招牌/裱框
  證書/簽名書,對應登台/開店/論文/代表作),price 0、空 attributes(不影響招租星等)、
  純裝飾。`Placement` 新增 `memorial?`;`graduateFarewell` 在 moveOut 前於畢業生原房間
  `findFreeSlot(1×1)` 擺放(綁房間不綁租客,placements 獨立於 occupancy → 空房招租後仍保留;
  房滿則靜默略過並通知)。`sellFurnitureAt`/`startMoving` 擋下紀念物(不可賣/不可移),
  FurnitureInfo 隱藏賣/移按鈕改顯示「畢業生的紀念物」。**微光環(星等/mood 加成)評估後跳過**:
  需改動招租評分或新增每日 pass,成本高且會動 balance,收益低;紀念物定位為純情感收藏。
- **4. 成就(legacy.ts)**:🏛️ 名人堂(graduateCount ≥ 5,於 `graduateFarewell` 判定)、
  ✉️ 見字如面(首次於 LegacyPanel 展開告別信,`unlock` 冪等)。ACHIEVEMENTS 24 → 26,隱藏數不變。
- **主要檔案**:`src/types.ts`、`src/floor/map.ts`(Placement.memorial)、`src/furniture/catalog.ts`
  (4 紀念物 def)、`src/sim/tenancy.ts`(送別會/紀念物/名人堂)、`src/sim/legacy.ts`(告別信/成就)、
  `src/sim/economy.ts`(擋賣/擋移)、`src/components/LegacyPanel.vue`、`src/components/FurnitureInfo.vue`、
  `src/App.vue`(擋賣 toast);測試 `scripts/wish-test.ts`、`scripts/legacy-test.ts`。
- **循環相依**:legacy 新增 `import { WISH_DEFS } from "./wishes"`,與既有 wishes→legacy(unlock)
  形成 2-cycle,但兩端皆函式內延遲取用 live binding,執行期安全(全測試載入 store 已驗證)。
- **測試／驗證**:`wish-test` 擴至 85/85(送別會人數門檻、紀念物依軌別/不可賣/招租後保留、
  名人堂 5 位)、`legacy-test` 擴至 26/26(告別信生成+轉繁冪等、非畢業無信、成就 26 項)。
  完整 `npm test` **56/56 全綠、balance 無漂移(未 --update)**,`npm run build` 成功(121 modules)、
  `npx wrangler deploy --dry-run` 通過。
- **UI 目檢**:容器重啟 vite 後以 390×844 跑 `artifacts/ui-lab/rent/wish-graduate-2/shot.mjs`
  (注入一位帶告別信的畢業生 + 兩間房紀念物 + 解鎖新成就,截完清 localStorage)。三張圖逐一
  確認:告別信展開排版清楚無擠壓、成就頁 3/26 且 🏛️名人堂/✉️見字如面 已解鎖、樓層 301/304
  各留一件紀念物家具且 r304(招租中空房)仍保留。DOM 斷言告別信含署名通過。
- **已知限制／後續**:送別會 major 日誌掛在最好鄰居身上(全樓 Feed 單筆、不洗版),而非每位
  住戶各一筆;紀念物微光環刻意未做(見上);紀念物 1×1 在樓層總覽較小,近看需點房間細看。
  同一房間若二度有人畢業會疊放多件紀念物(視為合理的情感堆疊)。

### 2026-07-22 — 心願獎勵說明 + 中途劇情(Claude Code)

- **目的／使用者需求**:讓玩家在心願「未達成時」就看得到達成後會怎樣(獎勵/後續行為
  可見化),並讓追夢過程有戲(中途劇情)。零 AI 成本、不升 SAVE_VERSION、保留既有硬規則。
- **開工前地基確認**:HEAD=21e9c70(圓夢畢業第二批)、工作樹乾淨。逐一核對現況數值:
  fulfillWish 共通 mood+10/stress-8;畢業型 GRADUATE_AFTER_DAYS=6、REP_GRADUATE=8;
  安居型 becomeModelTenant +3% 月租/全樓每日 mood+0.5(封頂+1)/REP_SETTLE=3;文案如實對應。
- **A. 達成後可見化(wishes.ts + App.vue)**:新增純函式 `wishOutcomeBrief(def)` 回傳結構化
  `{ graduates, headline, lines[], growthLabel, growthHint }`——畢業型走向=歡送會/告別信/
  紀念物/謝禮紅包(約半月租×好感)/退還押金/口碑+8;安居型=模範房客長住/+3% 月租/全樓心情/
  口碑+3;成長特質由 `GROWTH_TAGS[def.growthTag]` label(去括號)+hint 顯示。房客頁綠色
  「房客目標」卡(wish-guide)未達成時新增一段「🎁 達成後」渲染 headline+走向+🌱成長特質;
  已達成則隱藏該段(維持原「已實現」文案)。wishChip 加 `outcome`/`outcomeTip` 供 chip
  tooltip。文案純靜態繁中常數(手檢無簡體)。手機 390 寬直式目檢無擠版。
- **B1. 心願專屬里程碑句庫**:`MILESTONE_TEXT`(3 句通用)→ `WISH_MILESTONES`(8 心願 × 25/50/75
  = 24 句專屬劇情,如 open_shop 25%「開始偷偷記帳、研究開店要備多少本」)。**沿用既有里程碑
  日誌槽、不新增日誌條目數**(每條心願一生仍最多 3 筆 notable)→ 零 balance 影響。日誌格式
  改為「🎯 朝『label』邁進:句」,仍以 🎯 開頭(既有測試相容)。
- **B2. wishBrief 帶追夢階段語氣**:進行中的 AI context 由「label(進度約 X%)」強化為附上
  起步(<34)/過半(34~66)/接近實現(≥67)的語氣尾句;仍保留「進度約 X%」相容既有斷言,
  且 worker prompt 既有禁令(不得自行宣布實現或跳進度)不動 → 純 context 文字強化。
- **B3. 受挫劇情:評估後不做**。balance 快照以 `rt.log.length` 逐租客整數比對,任何在 10 天
  seeded 模擬中可能觸發的受挫日誌都會改變 log 數 → 逼出快照漂移且難穩定控制;依需求「寧可不做
  也不製造洗版/不穩 balance」的指示,只做正向里程碑與 context 強化。
- **主要檔案**:`src/sim/wishes.ts`(WISH_MILESTONES/wishOutcomeBrief/wishBrief 階段/里程碑
  日誌)、`src/App.vue`(wishChip.outcome、wish-guide「達成後」段 + CSS)、`scripts/wish-test.ts`
  (§22 outcome 兩軌/成長特質、§23 里程碑句庫分流+涵蓋 8 條+實跑分流、§24 wishBrief 階段)。
- **測試／驗證**:`wish-test` 85→101(+16),全綠;完整 `npm test` **56/56 全綠**;
  `balance-test` **快照一致、零漂移(未 --update)**;`npm run build` 成功(121 modules)、
  `npx wrangler deploy --dry-run` 通過。
- **UI 目檢**:容器重啟 vite 後以 390×844 跑 `artifacts/ui-lab/rent/wish-reward/shot.mjs`
  (注入同一畢業型心願 finish_masterwork/林小婕的三態 wish.progress)。三張卡片特寫 + 全頁逐一
  確認並附 DOM 斷言:22%/88% 兩態同時顯示「達成方式」與「🎁 達成後(📦 圓夢後風光搬離 + 走向
  + 🌱習得『更有自信』)」、排版清晰無擠版;已達成態轉綠底「🌟 已達成」且「達成後」段隱藏。
- **已知限制／後續**:達成後段只描述該心願所屬軌別的共通走向,未逐一列出每條心願的專屬紀念物
  名稱(紀念物名稱在畢業結算才決定);受挫劇情如上刻意未做,若日後要做需先設計 balance 中立的
  冷卻/首次標記機制再重建快照。

### 2026-07-22 — 心願結果常駐顯示(會不會離開)(Claude Code)

- **目的／使用者需求**:房客頁「房客目標」卡的「達成後」段原本只在未達成時顯示,達成後就隱藏。
  使用者要求「不管有沒有達成,都要顯示這個目標的**結果**,尤其是這房客**會不會離開**」,好判斷
  怎麼處理該房客。改為結果段**在所有狀態常駐**,且依當前狀態把「會搬離/會留下」講清楚。
- **開工前地基確認**:HEAD=741e2f2(心願獎勵說明+中途劇情)、工作樹乾淨。核對欄位實際位置:
  `graduateDay`/`fulfilledDay`/`announced` 在 `rt.wish`(TenantWish);`modelTenant` 在 `rt`
  (TenantRuntime);`gameDayIndex()` 由 `state.gameMs` 推算。皆與程式一致,無臆測。
- **1. wishes.ts 新增純函式 `wishResult(rt)`(零副作用)**:依 runtime 當前 wish 狀態回傳
  `{ phase, graduates, leaves, headline, verdict, lines[], daysLeft?, growthLabel?, growthHint? }`,
  三態:**preview**(progress<100,未達成)= 沿用 `wishOutcomeBrief` 的走向+成長特質,verdict
  依 `def.graduates` 給「圓夢後會搬離公寓」/「圓夢後會留下,成為模範房客長住」;**leaving**
  (已達成畢業型,`daysLeft = max(0, graduateDay - gameDayIndex())`)verdict「已圓夢,將於 N 天後
  搬離」(N=0→「即將搬離」),提醒歡送會/紀念物/謝禮;**stayed**(已達成安居型)verdict「已圓夢,
  留下成為模範房客長住」+ 效果。**「會/不會離開」一律以 `def.graduates` 判定**(leaves 旗標)。
- **2. App.vue:wish-guide「結果」段改常駐**:移除「已達成就隱藏 outcome 段」的 `v-if`,改為
  只要 `wishChip.result` 存在就渲染,標題/內容依 phase 切換(🎁 達成後會怎樣 / 📦 即將搬離 /
  🏠 已長住)。新增最醒目的一句 `.wish-outcome-verdict`(粗體、放最前):會離開→「🚪 會離開」橘字、
  會留下→「🏠 會留下」綠字,即使卡片主標題是綠底「🌟 已達成」仍清楚呈現去留結論。preview 才附
  🌱成長特質;「達成方式」段仍只在未達成時顯示。wishChip 加 `result: wishResult(rt.value!)`。
- **主要檔案**:`src/sim/wishes.ts`(WishResult 型別 + wishResult 純函式)、`src/App.vue`
  (wishChip.result、wish-guide 結果段常駐 + verdict CSS)、`scripts/wish-test.ts`(§25)。
- **測試／驗證**:`wish-test` 101→112(+11:§25 涵蓋三態 phase/leaves 正確、畢業 vs 安居的
  「會離開」旗標由 def.graduates 決定、leaving 剩餘天數以固定注入 gameMs=開場+30 天計為 4、
  邊角當天到期 daysLeft=0「即將搬離」、stayed 不會離開);完整 `npm test` **56/56 全綠**;
  `balance-test` **快照一致、零漂移(未 --update)**(純 UI/純函式,不碰模擬);`npm run build`
  成功(121 modules)、`npx wrangler deploy --dry-run` 通過。
- **UI 目檢**:重啟容器內 vite(舊 dev server 因 Windows bind-mount 未偵測到本次原始碼變動、
  模組快取過期,重啟後才正確編譯)後,以 390×844 注入同一租客(林小婕)三態截圖至
  `artifacts/ui-lab/rent/wish-outcome-always/`:preview(finish_masterwork progress=40)、
  leaving(progress=100+graduateDay=gameDayIndex+4)、stayed(改安居型 career_step+modelTenant
  =true)。逐張目檢 + DOM 斷言 verdict:三態都在卡片顯眼處顯示去留結論——preview「🚪 會離開:
  圓夢後會搬離公寓」、leaving 綠底仍顯示「🚪 會離開:已圓夢,將於 4 天後搬離」、stayed 綠底
  「🏠 會留下:已圓夢,留下成為模範房客長住」,手機直式排版無擠版、底部導覽未擋卡片。
- **已知限制／後續**:leaving 天數在畫面停留時隨遊戲換日遞減(即時反映,無快取問題);
  結果段仍以軌別共通走向描述,未列每條心願專屬紀念物名稱(同前批,結算才定名)。保留未成年
  排除戀愛線、成人內容遮蔽式預設關、AI 白名單/夾值/消毒等硬規則(本次純 UI 與純函式,未觸及)。

### 2026-07-23 — 安居型模範房客圓滿搬離(Claude Code)

- **目的／使用者需求**:安居型心願達成後成 modelTenant 永久佔位、不會離開 → 掛機後期沒有
  新租客流入,循環停滯。方案:模範房客享受一段較長安居期後,因人生下一步(成家/外派/買房)
  溫馨搬離、釋出房間;並順帶做「房東主動送別鍵」。零 AI 成本、不升 SAVE_VERSION、保留硬規則。
- **開工前地基確認**:HEAD=95735d8(心願結果常駐顯示)、工作樹乾淨。核對欄位:modelTenant/
  wish 在 rt;graduateFarewell/placeMemorial/MEMORIAL_DEF/moveOut 在 tenancy;告別信
  buildFarewellLetter/FAREWELL_BODY 在 legacy;wishPass/wishResult 在 wishes。
- **A. 安居期後圓滿搬離(主機制)**:
  - 常數 `SETTLE_TENURE_DAYS=20`(wishes.ts;明顯長於畢業型 6 天,體現忠誠;> balance 10 天窗)。
  - 新欄位 `rt.modelSinceDay`(gameState;入存檔):becomeModelTenant 設模範時記錄當前遊戲日。
  - `wishPass` 新增安居分支:模範房客且安居型,`settleDepartDay = modelSinceDay + 20`,前 2 天
    打包預告(settleFarewellText 3 選 1 決定性)+ 掛貓去留(複用 maybeAttachCatFarewell),
    到期進搬離名單(reason = 成家/外派/買房 3 選 1)。**不寫 w.graduateDay**(那是畢業型專用,
    留 -99 供主動送別鍵判斷)。
  - `graduateFarewell` 改為兩軌共用:以 `rt.modelTenant && !def.graduates` 判定 isSettle。
    安居型口碑 +REP_SETTLE_GRADUATE、紅包/notify 文案改「圓滿」、**不計 graduateCount/畢業成就**
    (安居圓滿非「畢業」;送走 N 位房客的通用成就仍由 recordAlumnus 觸發)。送別會/紀念物/
    紅包/退押金/貓去留全部複用。
  - 紀念物:新增家具 `memorial_frame`(🏠 全家福相框,catalog.ts,純裝飾/不可賣);MEMORIAL_DEF
    的 4 條安居心願都指向它。
  - 告別信:legacy.ts FAREWELL_BODY 補 4 條安居軌語氣(感謝像家的時光、帶著溫暖前往下一步),
    放寬 guard 為「有句庫即生成」,仍必過 toTraditional。
  - 口碑決策:`REP_SETTLE_GRADUATE=8`(= 畢業;圓滿收尾同等口碑印記)。安居軌一生累計
    (成為模範 +3 → 圓滿搬離 +8 = 11)刻意高於畢業軌(+8),獎勵更長更忠誠的居住關係(見 reputation.ts 註)。
  - 舊存檔相容:persistence 載入時已是 modelTenant 但缺 modelSinceDay → 補當前遊戲日(給滿
    安居期,不立刻踢走既有模範)。存檔一併新增 modelSinceDay 欄位。
- **B. 房東主動送別鍵**:`proactiveSettleFarewell(id)`(wishes.ts)把 modelSinceDay 往前挪到
  只剩 2 天,立即預告 + 掛貓去留,未來 2 遊戲日由 wishPass 自然走完搬離(儀式全複用)。
  已在打包/非模範/未圓夢則拒絕。App.vue 模範房客的 stayed 結果段下方加「🎓 祝福他展開新生活」
  鍵,二次點擊確認(armed 態,5 秒未確認自動復位)避免誤觸;非 stayed 態不顯示。
- **C. wishResult stayed 態更新**:安居型現在也會離開 → stayed 顯示「🏠 安居中(模範房客),
  將於 N 天後圓滿展開人生下一步」(N = modelSinceDay+20−今日,≥0),verdictTag「🏠 安居後圓滿
  搬離」;preview 安居型改「先當一陣子模範房客安居,之後圓滿搬離」(不再說永久留下)。新增
  `verdictTag` 欄位(短標,顏色改依 graduates 分流:畢業橘/安居綠暖);wishOutcomeBrief 安居
  走向同步改寫。App.vue verdict 由 leaves 改讀 verdictTag+graduates。modelChip tooltip 補「安居
  一段時間後圓滿搬離」。
- **主要檔案**:`src/sim/wishes.ts`(SETTLE_TENURE_DAYS/modelSinceDay 記錄/settle 搬離管線/
  proactiveSettleFarewell/wishResult+wishOutcomeBrief 改寫)、`src/sim/tenancy.ts`(graduateFarewell
  兩軌/MEMORIAL_DEF)、`src/sim/reputation.ts`(REP_SETTLE_GRADUATE)、`src/sim/legacy.ts`(安居告別信)、
  `src/sim/gameState.ts`(modelSinceDay 欄位)、`src/sim/persistence.ts`(存/載+補值)、
  `src/furniture/catalog.ts`(memorial_frame)、`src/App.vue`(主動送別鍵+verdictTag+CSS+tooltip)、
  `scripts/wish-test.ts`(§22 改字、§25 改語意、§26 modelSinceDay+補值、§27 安居期滿搬離全儀式、
  §28 主動送別+貓去留)。
- **測試／驗證**:`wish-test` 112→134(+22),全綠;完整 `npm test` **56/56 全綠**;
  `balance-test` **快照一致、零漂移(未 --update)**(安居期 20 天 > 10 天快照窗,理論零漂移,實測
  確認);`npm run build` 成功(121 modules)、`npx wrangler deploy --dry-run` 通過。
- **UI 目檢**:容器重啟 vite 後 390×844 注入截圖至 `artifacts/ui-lab/rent/settle-graduate/`:
  模範房客心願卡(顯示安居倒數 N 天 + 主動送別鍵)、送別後名冊的安居告別信。(見下方截圖結論)
- **已知限制／後續**:主動送別採「2 天後搬離」而非立即,以保留打包預告與貓去留抉擇窗(與自然
  到期一致);安居告別信/搬離文案為模板(零 AI)。保留未成年排除戀愛線、成人內容預設關、AI
  白名單/夾值/消毒等硬規則(AI 未觸及)。


### 2026-07-23 — 所有離開原因都留告別留言(Claude Code)

- **目的／使用者需求**:「每個離開公寓的租客都對房東留一段話,不管開心畢業還是被迫不爽
  離開。」技術定案:純系統模板(不用 AI)——AI 走共用免費額度會 fallback/離線,無法保證
  「一定有」,而離開是一次性稀有事件漏掉就沒了;模板 100% 保底。現況缺口:`AlumniEntry.farewell`
  原本只有畢業型 + 安居圓滿離開有,被迫/非圓滿離開沒有留言。本次補齊所有 moveOut 原因。
- **moveOut 路徑盤點與語氣歸類**(程式碼盤點全部呼叫來源):
  - `evictTenant` agreement → reason「與房東協議解約(補償 $X)」→ **agreement**(體面但失落、感謝)。
  - `evictTenant` forced → reason「遭房東強制請離」→ **forced**(委屈、不甘、怨懟;不辱罵)。
  - `decide` AI 事件 evict → reason「你請他搬走了」→ **forced**(最貼近:無協議被請走)。
  - `endCohabitOnBreakup` 無房搬走 → reason「分手後無處可住,搬離公寓」→ **breakup**(黯然、感情創傷、仍道別)。
  - `tick.ts` 長期不滿自動退租 → reason「對居住品質長期不滿」→ **unhappy**(失望、帶著遺憾主動離開)。
  - `graduateFarewell` → moveOut(圓夢/安居 reason):仍走既有 `buildFarewellLetter` 專屬句庫(未動)。
  - 任何未預期 reason(含測試「測試退租」)→ **generic** 兜底通用道別。
- **統一機制**:`recordAlumnus` 改為 `farewell = buildFarewellLetter(...) ?? buildDepartureLetter(...)`
  ——圓夢畢業/安居軌先用既有專屬句庫(**未重寫、未破壞**);非圓夢一律以被迫離開句庫兜底,
  farewell **必為非空字串**(使用者硬底線)。
- **句庫**(legacy.ts):`classifyDeparture(reason)`(**已 export 供測試**)歸 5 類語氣;
  `DEPARTURE_BODIES` 每類 2~3 句變化,每句摻入住了幾天 + 最好鄰居(bestNeighborName)+ 性格
  (coreTags[0])+ 代表記憶(representativeMemory);選句用決定性 hash(租客 id + gameMs,**不耗
  Math.random**,不擾動 RNG 次序);全部過 `toTraditional` 防簡體。被迫離開措辭維持遊戲分級
  (失望與委屈,非攻擊,無辱罵/不當內容)。
- **UI**:LegacyPanel 的「✉️ 展開告別信」原本 `v-if="al.farewell"`,現在每位離開者都有 farewell
  → 所有離開原因的信都能展開;沿用既有機制,未改結構(以 leftMs 為穩定 key 可獨立展開)。
- **主要檔案**:`src/sim/legacy.ts`(classifyDeparture/DEPARTURE_BODIES/DEPARTURE_SIGN/
  departureIndex/buildDepartureLetter + recordAlumnus 兜底)、`src/types.ts`(AlumniEntry.farewell
  註解更新)、`scripts/legacy-test.ts`(§改寫:路徑分類 6 例 + 每種原因非空/轉繁/摻入住幾天/
  代表記憶/最好鄰居 + 強制信摻性格)。
- **測試／驗證**:`legacy-test` 由既有擴充到 **62 通過 / 0 失敗**;完整 `npm test` **56/56 全綠**;
  `npm run build` 成功(121 modules)、`npx wrangler deploy --dry-run` 通過。
- **balance**:告別信只寫 `state.alumni` 欄位、不寫 rt.log/ledger,選句用 gameMs hash 不耗
  Math.random → **理論零漂移**;`balance-test`(在 npm test 內)**快照一致、零漂移(未 --update)**,實測確認。
- **UI 目檢**:容器重啟 vite(ui:serve)後,以自訂 Playwright 流程(localStorage 注入真實管線
  生成的 alumni 條目 → 開傳承/歷任房客 → 展開告別信 → 截完 removeItem 清除)390×844 截圖至
  `C:\Users\User\claude_try\artifacts\ui-lab\rent\all-farewell\`:01 名冊(4 封被迫離開信,各帶
  ✉️ 展開鍵與離開原因)、02 強制驅逐信展開(陳柏宇,委屈不甘)、03 分手搬走信展開(許雅晴,
  黯然)。逐張目檢:繁體正確、語氣對、可獨立展開(展一封另一封收起)、摻入性格(完美主義)/
  住了幾天/最好鄰居(林曉彤)/代表記憶;首次展開觸發「見字如面」成就。
- **已知限制／後續**:告別信為模板(零 AI,設計如此);措辭固定 5 類語氣。保留未成年排除
  戀愛線、成人內容遮蔽式預設關、AI 白名單/夾值/消毒等硬規則(本次未觸及 AI;被迫離開信不含
  辱罵/不當內容)。

### 2026-07-23 — 設定頁完整除錯 Log 匯出(Codex)

- **目的／使用者需求**:在設定頁提供一鍵輸出當下遊戲狀態的工具，讓玩家可把資料複製給
  開發者除錯；必須包含每位現役房客的 tags／logs、歷任房客資料與前租客 logs，並參考隔壁
  NBA 遊戲既有的文字匯出與複製流程。
- **進度盤點／參考設計**:開工前確認 HEAD=19e3014、`rent_house` 工作樹乾淨；最新功能為所有
  離開原因皆留告別留言。唯讀盤點 NBA 後確認其入口是單場比賽「匯出文字」，可借鏡 readonly
  textarea + Clipboard API + `execCommand` + 手動全選三層備援；租屋版不使用 NBA 的 HTML 字串
  插值，直接以 Vue `:value` 綁定，避免特殊字元提前關閉 textarea。
- **除錯輸出**:`src/sim/debugExport.ts` 新增 versioned、pretty-printed JSON 快照（不是可匯入
  存檔），包含 metadata／summary、排除重複房客陣列後的全域 state、家具／房間升級／關係、
  現役房客完整 runtime（tenant.coreTags、memoryTags、growthTags、log、wish、flags、數值等）與
  歷任房客。輸出只在玩家按鈕時於本機產生，不自動上傳。
- **前租客 logs 留存**:`AlumniEntry.debugSnapshot?` 新增離場診斷快照；`recordAlumnus` 在刪除
  runtime 前複製 tenant id、房號、三類 tags、stats／finance、摘要、整潔／滿意、decisions 與
  完整 log。欄位選填、不升 SAVE_VERSION；舊名冊可直接載入，匯出時明示「功能推出前未保存
  完整 tags / logs」，之後離開者則都有快照。
- **設定頁 UX**:新增「🛠️ 除錯 Log」說明、`📋 產生並複製除錯 Log` 按鈕與 180px 高 readonly
  全文框；成功後仍保留全文供檢查。複製依序走 Clipboard API、`execCommand("copy")`、手動
  全選。互動測試發現部分 WebView 的 Clipboard Promise 可能永久 pending，故新增 1.2 秒逾時
  並讓既有「匯出存檔」共用此安全 helper；結果訊息加 `aria-live`。
- **主要檔案**:`src/sim/debugExport.ts`、`src/sim/legacy.ts`、`src/types.ts`、`src/store.ts`、
  `src/components/SettingsPanel.vue`、`scripts/debug-export-test.ts`、`scripts/run-all.ts`。
- **測試／驗證**:新 `debug-export-test` **10/10**（全域／世界／現役 tags+logs／歷任快照／舊檔
  說明／JSON 解析）；完整 `npm test` **57/57**（56 支正式測試 + sim-trace，全綠，balance 無
  漂移）；最終 Clipboard 逾時修正後再跑新測試 10/10、`npm run build` 成功（122 modules）；
  `git diff --check` 通過。
- **UI 驗證**:清掉 UI Lab 內殘留的舊 Vite 程序後重跑 `npm run ui:shot -- rent`，三種手機尺寸
  18 張截圖逐一目檢，report 0 errors／0 warnings。另以 Browser 在 390×844 實際開設定、點擊
  匯出：產生 48,194 字元 JSON，確認含 `currentTenants`／`formerTenants`／`coreTags`／`log`、
  readonly 全文框與成功提示；panel/body 的 scrollWidth 等於 clientWidth、可直向捲動、console
  0 error。Browser 技能的實際互動驗證直接促成 Clipboard pending 逾時保護。
- **已知限制／後續**:2026-07-23 此功能上線前已離開的舊房客，當年未存完整 runtime，無法
  回溯補出 tags/logs，只能輸出既有名冊摘要、原因與告別信並附缺資料說明；功能推出後離開者
  完整保留。除錯 Log 可能很長（本次實測約 48KB），因此保留可檢視／手動複製的 textarea。

### 2026-07-23 — 同居一起離開 + 情侶思念劇情（Claude Code）

- **目的／使用者需求**:讓情侶關係在離開節點更有份量。以 `cohabitingPartnerId(leaverId)` 分流:
  有同居伴侶→A;否則有 `romanticPartnerId` 且非同居→B(兩者互斥)。
- **A 同居正向離開(只限正向)**:僅 `graduateFarewell`(圓夢/安居圓滿)路徑,同居伴侶追隨
  一起走(浪漫的追隨,非被連坐)。**實作順序決策**:不用「先移除誰」硬排序,改在 `moveOut`
  新增選填 `opts.followPartnerId`——標記「這位是一起走的伴侶」,對其**略過「同居者轉正接手」
  分支**、也略過互相注入的「被留下失落」記憶;`graduateFarewell` 末端偵測 `cohabitingPartnerId`,
  存在則 `moveOut(伴侶,{followPartnerId:主角})` + `moveOut(主角,{followPartnerId:伴侶})`,兩人
  都進名冊、都有告別信。因 followPartnerId 已擋掉轉正,**兩個方向(主角是 host 或 guest)皆乾淨、
  房間直接空出**,不依賴呼叫順序。**金錢去重**:謝禮紅包/退押金在 graduateFarewell 上半段按
  主離開者算一次(不動),伴侶本就不另收租/押金 → 不重複發。**送別會去重**:仍只呼叫一次
  `farewellSendoff`(主離開者),不辦兩場;`moveOut` 本身不辦送別會。被趕走(evictTenant)/主動
  退租/分手**不帶 followPartnerId → 走現行轉正接手,完全不變**。
- **A 告別信新語氣**:`legacy.ts` 新增 `DepartureTone="follow_partner"`;`classifyDeparture` 最前面
  加「跟隨/追隨」關鍵字判斷;`DEPARTURE_BODIES`/`DEPARTURE_SIGN` 補 3 句(為了和愛人在一起、
  跟著他展開新生活、捨不得這棟樓但更想在一起),摻性格+住幾天+代表記憶,過 `toTraditional`。
  `recordAlumnus` 對 follow_partner **優先用該語氣**(即使伴侶自己剛好也圓夢,也不被圓夢信蓋掉);
  主離開者仍用其原本圓夢/安居告別信。
- **B 未同居情侶留下方(中等、會恢復)**:`moveOut` 失落記憶分支升級——留下方與離開者為情侶
  (romantic)且非同居時:當下 **mood −13、stress +12(夾值)**;**不打 satisfaction 永久值**(可恢復);
  設 **`sulk` 2 遊戲日**(既有 directive 未過期則不覆蓋,並 applyHour 重定位);pushMemory `[思念X]`
  含「思念/難過」字樣(被 memoryEffects 既有 drift/衰減接住,隨時間恢復);💔 major 社交日誌。
  思念記憶走既有 memoryTags 管道自動進 narration context 餵 AI。**同居轉正接手方、普通朋友
  (value≥50 非情侶)維持原本普通失落記憶,不升級**。
- **主要檔案**:`src/sim/tenancy.ts`(graduateFarewell 帶伴侶走、moveOut 新增 followPartnerId +
  轉正接手守門 + B 中等難過分支)、`src/sim/legacy.ts`(follow_partner 語氣/句庫/署名 +
  recordAlumnus 優先序)、`scripts/couple-departure-test.ts`(新增)、`scripts/run-all.ts`(登錄)。
- **測試／驗證**:新 `couple-departure-test` **23/23**(A 兩人都走+都進名冊+follow_partner 信、
  A 反向 guest 圓夢、A2 被趕走仍轉正接手不變、B 中等難過情緒打擊+sulk+思念記憶+不打
  satisfaction、分流普通朋友原失落);相關 `cohabit-integrity/eviction/drama/legacy/wish` 全綠;
  完整 `npm test` **58/58**(含 sim-trace 無異常)、`npm run build`(122 modules)、
  `npx wrangler deploy --dry-run` 通過。
- **balance**:`balance-test`(在 npm test 內)**快照一致、零漂移**——種子 10 天 trace 內兩位種子
  租客未在窗內成情侶後離開,故新連動未被觸發,`balance-snapshot.json` 未變動、**未 --update**。
- **已知限制／後續**:follow_partner 告別信為模板(零 AI,設計如此),未帶入伴侶姓名(用「他/
  愛人」通稱)。B 的思念恢復依賴既有每小時 drift 與每日衰減。保留未成年排除戀愛線、成人內容
  預設關、AI 白名單/夾值/消毒等硬規則(本次僅動離開連動,未觸及 AI 生成)。


### 2026-07-23 — 房間舒適度系統(第一波:舒適度核心+整潔)（Claude Code）

- **目的／使用者需求**:大改動第一波。**數值哲學=慢變環境品質**:佈置好家具→租客「慢慢」
  變舒適→墊高心情/健康基準,佈置一次長期受益,不是 The Sims 式快速衰減需照顧。本波做
  A 舒適度核心 + B 整潔翻身 + C 房間細看顯示。
- **A 舒適度分數(新純函式模組 `src/sim/comfort.ts`)**:`roomComfort(roomId, cleanliness)`
  回 0~100 = ( 屬性飽和加權 + 家具種類齊全度 ) × 整潔乘子。
  - **屬性加權**(依 catalog 實際值域盤點:單房 cozy 常見 10~20、style 3~13、tech 5~14、
    soundproof 僅隔音窗簾 6、noise 電競桌/體感機 2):`cozy×1.0 + style×0.6 + soundproof×0.8
    + tech×0.35 − noise×0.7`,再過飽和函式 `60×w/(w+18)`(半值 18、上限 60),避免狂堆爆表。
  - **種類齊全度**:五大類(睡眠=sleep、娛樂=av/work、收納=storage、裝飾=ambiance、
    生活=seating/kitchen)每具備一類 +6,滿 30。
  - **整潔乘子**:`clamp(0.5 + 0.5×clean/100, 0.5, 1)`——clean100→×1、50→×0.75、0→×0.5(下限
    保護不歸零)。
  - 種子實測:301(髒 35、無收納、工作狂設備)=36.6、302(乾淨 92、書架/香氛/綠植)=54.2。
- **接基準線(慢變核心)**:`comfortBaselineDelta(comfort)`,`cd=comfort−50`:
  `mood 基準 += cd×0.16`、`stress 基準 += −cd×0.10`、`wellbeing 回歸錨 += cd×0.08`。係數刻意小
  (comfort 80→mood 基準 +4.8;comfort 30→−3.2),改的是 homeostasis「回到哪」,租客靠既有
  K=0.06 慢慢趨近 → 佈置後「慢慢」變舒適,不瞬跳。`baselines(rt, comfort?)` 新增選填參數
  (applyStat 傳入避免重算;外部如 arc-test 就地算,comfort 為兩側相同常數 → 既有 +3 斷言不受影響)。
- **needsMet 協調決策(避免雙重計算)**:comfort **只走基準線**這條新管道(→ mood/stress/
  wellbeing → 間接進 satisfaction),**不再另外把 comfort 乘進 satisfaction**。`updateSatisfaction`
  的 `needsMet` 維持原樣:它衡量的是「作息家具的功能可達性(含共用浴室/廚房)」,常態下
  ≈1.0、只有拆掉必要家具才咬人,與 comfort 的「房間品質/療癒/整潔」是不同軸;既然 comfort
  已從基準線進到 satisfaction,就不重複相乘。結論:單一環境品質管道(comfort→基準線)+ 既有
  功能守門(needsMet),零雙重計算。
- **B 整潔翻身**:
  - **從裝飾變有用**:整潔已納入 comfort 乘子(髒→舒適打折→拉低基準)。
  - **緩慢衰減+維持(homeostasis 版,非硬衰減)**:整潔每小時朝「收納決定的自然水位」極慢回歸
    `CLEANLINESS_K=0.015`(約一週收斂),再吃本小時活動增量(煮飯/洗澡)。`cleanlinessBaseline
    (roomId)=clamp(50 + storage×2, 50, 80)`——**收納家具墊高基準 = 減緩衰減、常保整潔**(無收納
    錨 50、書架 6→錨 62)。用回歸而非硬衰減,體現「別逼玩家一直打掃」的慢變哲學:髒房會自己
    慢慢回穩,收納讓它回穩得更高。
  - **太低後果**:`dirtyComplaintPass`——整潔<40 時每 2 遊戲日抱怨一次(壓力 +2 + 🧹 社交日誌);
    用日數冷卻**不擲 RNG**(避免打亂模擬亂數序列與 balance)。
- **C 房間細看顯示**:房間細看新增「🛋️ 舒適度 XX」進度條(<35 紅/>60 綠)+ 動態改善提示
  `comfortHints`(太髒→「該打掃了」、缺類別→「缺娛樂設備」「加收納更整齊」等,至多 3 條、
  手機直式不擠)。點標題有 STAT_HELP 說明。主要檔案 `src/App.vue`。
- **測試/驗證**:新 `comfort-test`(16 項:值域/種類齊全/整潔乘子單調/基準線方向/接基準線
  /整潔回歸+收納墊高/提示)、`stats-model-test` 擴充 2 項(舒適度接基準線);登錄 run-all。
  完整 `npm test` **59/59**(含 sim-trace 無異常)、`npm run build`(123 modules)、
  `npx wrangler deploy --dry-run` 通過。
- **balance 漂移(關鍵:安全閥判定)**:先跑未 --update 看漂移——**種子局數值健康**,只有 301
  (陳家豪,髒亂起手+無收納+工作狂簡陋房)溫和微動、方向正確、幅度極小,302(林小婕,佈置
  尚可+乾淨)在容差內未動:
    - 301 mood 95.6→94.9、stress 37→38.7、wellbeing 73.2→72.3、satisfaction 73.6→**72.9**
      (仍遠高於退租門檻,未逼近退租)、money 59927→59919。
  無租客崩壞/逼近退租,亦非全滿甜;符合「乾淨佈置好的房略正/簡陋髒房略罰」的設計預期 →
  判定健康,**已 `npx tsx scripts/balance-test.ts --update` 重建 `balance-snapshot.json`**。
- **截圖驗證**:容器內 Playwright 自訂腳本 `artifacts/ui-lab/comfort-check.mjs`(localStorage 注入
  兩態、產物在 workspace 根 `artifacts/ui-lab/rent/comfort-v1/`,不入 repo):
  A 高舒適房(301 換療癒佈置+五類齊+整潔100)= **舒適度 75 綠、無提示**;
  B 簡陋髒房(301 只剩床+整潔12)= **整潔 12 紅、舒適度 3 紅**,提示「太髒亂了,該打掃了/
  缺娛樂設備/加收納更整齊」。兩張逐一目檢、console 0 error。
- **第二波留項**:家具品質分級(同類家具好壞差異)、舒適度/整潔更深的劇情掛鉤(規則事件化的
  抱怨/慶祝)、玩家可主動「打掃」動作或打掃相關家具(掃地機器人等)、共用區(交誼廳/浴室)
  的舒適度延伸。
- **硬規則**:本波未觸及 AI 生成與戀愛線,未成年排除/成人內容預設關/AI 白名單夾值消毒等
  硬規則全數保留。

### 2026-07-24 — 修正 Worker AI 供應商註解 + 導入 vue-tsc SFC 型別檢查(Claude Code)

- **目的／需求**:兩件低風險正確性項目。(A) `worker/index.ts` 檔頭關於 AI 供應商的註解疑似
  過時,需依實際程式碼校正。(B) 專案一直缺 Vue SFC 完整型別檢查(見既有技術債),補上
  `vue-tsc`。
- **A. 供應商註解校正(依程式碼實測,非臆測改寫)**:核對 `narrateProviderOrder` 與
  `providerEvent`、`handleInvite` 後確認實際行為——
    - `/api/narrate` 日記:平日(非 `eventDue`)**Workers AI(qwen→llama)優先、Gemini 備援**;
      但事件機會日(`eventDue`)順序反轉為 **Gemini 優先**、Workers AI 備援。
    - 房東抉擇事件:`providerEvent` 只放行 Gemini 產生的 event,**Workers AI 只供無事件日記**。
    - `/api/invite`:**Gemini 優先、Claude(Anthropic)備援**。
    原註解把 Gemini 一律寫成備援、且未提 eventDue 反轉與 invite 供應商,易誤導;僅改動檔頭第
    5 行為兩行精簡繁中敘述以符實況(未動程式邏輯)。主要檔案 `worker/index.ts`。
- **B. vue-tsc 型別檢查**:`npm install -D vue-tsc`(安裝成功,解析到 `vue-tsc@3.3.8`,peer
  `typescript >=5.0.0` 相容本專案 `~5.7.2`);`package.json` 新增 `"typecheck": "vue-tsc --noEmit"`。
  首次 `npm run typecheck` 出 4 個型別錯誤,皆以最小、不改邏輯方式修正:
    - `src/components/DollhouseView.vue`:`STATE_MAP`(`Record<TenantVisualState, …>`)漏 5 個
      狀態(watching_tv/eating_at_table/reading/painting/using_appliance),補上對應定位點與
      標籤(標籤沿用 `PixelDollhouse.vue`)。
    - `src/sim/growth.ts`:`GROWTH_TAGS` 為 `as const`,`baseline` 被窄化成字面型別,讀 `.mood`/
      `.stress` 報錯;改用 `GrowthTagDefinition["baseline"]` 型別中介取值,**行為不變**。
    - `src/sim/wishes.ts`:`boostWishFromArc` 參數放寬為 `tone?: string | null`(呼叫端傳
      `ArcTone | null`);`tone === "down"` 判斷不受影響。
  修正後 `npm run typecheck` 乾淨(0 錯)。
- **測試／build 驗證**:`npm test` **58/59 通過**(含 sim-trace);`npm run build` 成功
  (Vite 6.4.3,123 modules)。唯一失敗 `balance-test.ts` 經核實為**既有問題**:在乾淨 HEAD
  (git stash 掉本次變更)上同樣失敗,且加 `TZ=Asia/Taipei` 重跑差異完全相同(非時區敏感),
  漂移值與本次一致 → 證明與本次變更無關。上一 commit(e4ccfa3)日誌雖稱已 `--update`,但
  倉庫內 `balance-snapshot.json`(mood 95.6)與現行程式輸出(94.9)不符,屬 main 上既有的快照
  不一致。本次**未動平衡、未 `--update`**,故不重建快照,僅回報此既有問題留待決定。
- **已知限制／後續**:balance-snapshot 與現行程式不一致為既有狀態(非本次造成),需由負責平衡
  的一方確認 301 微幅漂移是否為預期後再決定是否 `--update`。`worker/` 未納入 tsconfig `include`,
  `typecheck` 目前只涵蓋 `src/**`(SFC + ts)與 `vite.config.ts`,未檢查 Worker 端。
- **硬規則**:未觸及 AI 生成、戀愛線與消毒/白名單;未成年排除、成人內容預設關等硬規則全數保留。

### 2026-07-24 — 補提交房間舒適度系統的 balance 快照(Claude Code)

- **目的／需求**:接續上一則工作,處理 `balance-test.ts` 在乾淨 HEAD 上就失敗的既有問題
  (承接方追查根因後再決定是否 `--update`,避免用 `--update` 掩蓋潛在回歸)。
- **根因診斷(先查再改)**:完整漂移僅集中在租客 301(陳家豪)——
  `mood 95.6→94.9、stress 37→38.7、wellbeing 73.2→72.3、satisfaction 73.6→72.9、
  money 59927→59919、wallet 23813→23821`;302(林小婕)僅次容差微動故未被 flag。
  `git log -- scripts/balance-snapshot.json` 顯示快照最後一次更新是 `3f8a7d5`,**不是**最近
  的 `e4ccfa3`。`e4ccfa3`「房間舒適度系統(第一波)」新增 `src/sim/comfort.ts` 並把 comfort
  偏離 50 接進 `tick.ts` 慢變基準(comfort<50 → 心情/健康↓、壓力↑),其 commit 訊息/日誌
  已明載「balance 漂移健康(僅髒亂 301 房輕微下滑、方向正確、遠離退租區)已 --update」,
  **但該 commit 的檔案清單未包含 `scripts/balance-snapshot.json`**——作者跑了 `--update`
  卻漏 stage 該檔。
- **判定:既有的「刻意調整+快照漏提交」,非回歸**。三項佐證:(1)`e4ccfa3` 日誌記載的預期
  值與現行輸出**逐項完全吻合**(95.6→94.9…);(2)漂移方向符合設計——只有刻意設成髒亂/簡陋/
  無收納的 301 被溫和扣分,乾淨的 302 幾乎不動;(3)幅度極小且遠離退租門檻。舊快照 95.6 之所以
  仍能當基準,代表 `e4ccfa3` 之前(含 `eff40f4`)快照是綠的,漂移純由 `e4ccfa3` 引入。
- **實際變更**:`TZ=Asia/Taipei npx tsx scripts/balance-test.ts --update` 重建
  `scripts/balance-snapshot.json`(301 五項 + money/wallet 更新到現行值;302 亦同步次容差微動)。
- **驗證**:`TZ=Asia/Taipei npm test` **59/59 全通過**(含 sim-trace)。未改任何遊戲程式,故不需
  重跑 build。
- **已知限制／後續**:此為補提交 `e4ccfa3` 應含而未含的快照,基準值反映的是房間舒適度系統的
  預期手感;後續若再調舒適度公式需一併 `--update`。
- **硬規則**:僅更新測試基準檔,未觸及任何 AI/戀愛/消毒邏輯;既有硬規則全數保留。

### 2026-07-24 — 分支收尾:合回 main 並 push(Claude Code)

- **目的／需求**:將本日兩筆工作(worker 註解校正 + vue-tsc 導入、balance 快照補提交)整合進主線。
- **實際變更**:`chore/worker-comment-vue-tsc`(`3037535`、`aa5e854`)以 fast-forward 合回
  `main`(`e4ccfa3..aa5e854`,無衝突),並 push 至 `origin/main`;隨後刪除已合併的本地分支。
  無新增程式/測試變更,僅版本控制收尾與本則日誌。
- **驗證**:合併後主線 `TZ=Asia/Taipei npm test` **59/59**、`npm run typecheck` 乾淨、
  `npm run build` 成功;`git status` 顯示 `main` 與 `origin/main` 同步。
- **已知限制／後續**:`worker/` 仍未納入 `typecheck` 涵蓋範圍(僅 `src/**` + `vite.config.ts`)。
- **硬規則**:純版本控制整合,未觸及任何遊戲/AI/戀愛/消毒邏輯;既有硬規則全數保留。
