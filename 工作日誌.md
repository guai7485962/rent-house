# 《房東監視中》開發工作日誌

> 更新日期:2026-08-01
> 一款「異步掛機觀察型」直式手機模擬遊戲:玩家是房東,透過俯視娃娃屋觀察租客生活、佈置家具、招租收租;劇情由 **AI(Gemini)依歷史即時生成**,並能影響走向。

## 線上位置
- **遊戲網址(手機可玩):** https://rent-house.guai7485962.workers.dev
- **Repo:** github.com/guai7485962/rent-house(push 到 `main` → Cloudflare 自動 build + deploy)
- **架構:** 靜態前端(Vue+Vite)+ Cloudflare Worker(同時服務網站與 `/api/narrate`);AI 金鑰存 Worker Secret `GEMINI_API_KEY`。

## 目錄

> 🧭 **接手者請從第四節開始讀**:那裡是現行待辦與驗收錨點。
> 想知道「最近發生什麼」看第七節最後幾則日期紀錄。

- [一、目前完成度總覽](#一目前完成度總覽) — 一句話現況
- [二、已完成的系統](#二已完成的系統) — 已上線系統清單(A～U)
- [三、已修掉的 bug](#三已修掉的-bug) — 歷史 bug 修正
- [四、待辦事項(依優先序)](#四待辦事項依優先序) — ⭐ **現行待辦(唯一權威清單,含驗收錨點制)**
- [五、已知小問題 / 技術債](#五已知小問題--技術債) — ⭐ **現行已知小問題／技術債**
- [六、如何執行 / 驗證](#六如何執行--驗證) — 測試與部署指令
- [附:AI 供應商設定](#附ai-供應商設定)
- [七、進度補記(逐日日期紀錄,2026-07-25 起;更早見封存檔)](#七進度補記逐日日期紀錄2026-07-25-起更早見封存檔) — 逐日施工紀錄(節內另有索引)
- [新紀錄追加點(APPEND HERE)](#新紀錄追加點append-here) — 📌 **檔尾;下一則日期紀錄加在這裡**

**其他文件**
- [`docs/工作日誌-封存.md`](docs/工作日誌-封存.md) — 2026-07-12 ～ 2026-07-24 的日期紀錄(逐字封存,零刪改)
- `docs/交接與待辦.md` — 第四、五節的摘要版交接文件
- `docs/設計檢討與優化.md` — 原始設計檢討

---

## 一、目前完成度總覽

「**觀察 + 房東經營 + AI 敘事 + 張力/社交**」的完整骨架都做完並上線了。核心體驗:掛機觀察租客 → 佈置招租收租 → 處理突發抉擇 → 看 AI 把每天寫成有記憶、會演進的故事,鄰居之間還會發展友情/戀愛/衝突。**沒金鑰/離線/超額時自動 fallback 成模板,遊戲照跑。**

---

## 二、已完成的系統

### A. 核心資料與美術
- `src/types.ts` — Tenant/Furniture/事件、visual_state、房間屬性、性別與戀愛取向、記憶標籤。
- 種子 `data/tenants.json`;`src/pixel/` 像素美術(俯視 3/4)、動態租客配色(依房間指派、保證不撞色)。

### B. 樓層地圖(tile 網格)
- `src/floor/map.ts` — 16×33 網格:4 套房 + 交誼廳 + 浴室(乾濕分離)+ 洗衣間 + 大門,可捲動。
- `floorScene.ts` + `FloorMap.vue` — 渲染、房間標籤、未讀/事件徽章、點擊進房/擺放/查看家具。

### C. 家具目錄 + 動態擺放
- `src/furniture/catalog.ts`(~35 件,資料驅動)、`render.ts`;`src/sim/placements.ts`(買/擺/賣/查、房間屬性、找空位)。
- 點家具 → 資訊卡 + 賣掉(退半價)`FurnitureInfo.vue`。

### D. 走動與尋路
- `pathfind.ts`(BFS + 家具障礙)、`agents.ts`(狀態驅動,走到當前活動家具旁;站立點自動避牆)。

### E. 異步掛機模擬
- `src/sim/clock.ts` — **7× 掛機時鐘(現實 1 天 = 遊戲 7 天)**,關掉再開自動補進度。
- `src/sim/routine.ts` — 24 小時作息 + 招租原型 + 家具角色反查(只用自房/共用區)。
- `src/sim/generate.ts` — 每小時觀察句(用 Fable 文本庫,隨機挑、時段安全)。
- `src/store.ts` — 每小時 tick、作息+偏離、未讀、存檔(localStorage)、快轉。

### F. 房東經營 + 收支系統
- 商店 `FurnitureShop.vue`(選→點地圖擺放)、招租 `recruit.ts`+`RecruitPanel.vue`(依裝潢契合度★)、收租。
- **收支帳** `state.ledger` + `addMoney()`(所有金錢進出單一入口)+ **每日管理費** + **財務面板 `FinancePanel.vue`(💰 收支)**:餘額/收入/支出/分類/交易明細。

### G. 張力系統(數值後果)
- **滿意度**(由心情/好感/壓力 + 房間是否滿足需求算出)。
- **突發事件抉擇**:規則式(壓力/滿意/好感觸發)+ **AI 生成**;`DecisionModal.vue` + `decide()` 套用夾值後果。
- **退租**:長期不滿 → 搬走、空房、系統通知。收租綁滿意度。

### H. 鄰居社交(完整版)
- `src/sim/social.ts` — 關係值 + 個性相容度 + 性別/取向;交誼廳相遇 → 聊天/衝突;認識→朋友→曖昧→情侶→**同居/分手**。
- **💞 關係面板**、同居抉擇彈窗、房間細看顯示感情狀態。

### I. AI 敘事(靈魂,已上線)
- `worker/index.ts` — `/api/narrate` 呼叫 **Gemini 2.5-flash**(免費層),依「當天日誌+性格+記憶+關係+抉擇」生成:
  - **每日觀察日記**(📖 卡片;AI 版加 ✨AI 標,模板版乾淨卡片)。
  - **記憶標籤**(讓歷史累積、劇情演進;去重+上限 8)。
  - **房東抉擇事件**:AI 依處境即時生成(嚴格夾值消毒),**可牽涉第二位鄰居**改動雙方數值與關係(戀情加速/室友吵架/和好…),不開放驅逐。
- `src/sim/narrate.ts` — 前端同源 fetch + 模板 fallback;供應商可切換(Gemini 優先,Claude 備援)。
- **內容變化**:`src/content/observationLines.ts`(**Fable 一次性生成**:170 句觀察 + 16 每日模板,時段安全),讓模板層與 AI 素材都不再千篇一律。

### U-4. 掃地機器人「會遊走」的純外觀動畫(方案 1)(2026-07-25;Claude Code)
- **目的/需求**:讓已存在的 `robot_vacuum` 家具在樓層上「看起來活著」——變成一台會在地板巡邏的 sprite:每個遊戲小時輪到一個區域走過去、抵達後就近小幅遊蕩並眨感應燈/微晃,且會避讓租客不疊格。**純視覺**:不改任何整潔/舒適/經濟/tick 模擬,cleanPower→cleanlinessBaseline 的機械效果與今天完全一致,位移不額外結算整潔。
- **實際變更與主要檔案**:
  - 新增 `src/floor/vacuumAgents.ts`(仿 `petAgents.ts`):偵測有 `cleanPower` 的家具擺放 → 每台生成一個遊走 agent;`tickVacuumAgents` 逐格插值(SPEED 22,略慢於租客)。**每小時區域輪替是確定性的**(`gameHourIndex(gameMs)` → `vacuumTargetArea()` 走 `VACUUM_AREAS` 七區:四套房+浴室+洗衣間+交誼廳;`pickAreaCell()` 以 seed 確定性挑可走格,**全程無 `Math.random`**);抵達後閒置也用確定性序號就近小巡,看起來一直在動。位置為暫態(每次掛載重生,不持久化)。
  - `src/floor/floorScene.ts`:`composeFloor` 加第 7 參 `vacuums?`;**有傳 vacuums 時略過靜態 robot_vacuum 家具**,改由 y-sort 混排中的遊走 sprite 取代(避免同台出現兩份);新增 `drawVacuum()` 沿用 `render.ts` 的 robot_vacuum 原圖 + 感應燈閃爍 + 1px 晃動。未傳 vacuums(離線預覽/舊測試)時照舊畫靜態,行為零變。
  - `src/floor/agents.ts`:`tickAgents` 加選配第 3 參 `blockedCells`(預設空集合 → 既有行為零變),租客不會踏上掃地機當下所在格。避讓雙向:掃地機下一步踩到租客會讓(等一拍),租客也不會疊上掃地機。
  - `src/components/FloorMap.vue`、`src/components/PixelDollhouse.vue`:比照 pets 接線 create/tick/signature,並把掃地機當下格傳給 `tickAgents` 做避讓、把 agents 傳給 `tickVacuumAgents`。
  - 測試/工具:新增 `scripts/vacuum-test.ts`(headless、無畫布,17 斷言:區域輪替確定性/週期、`gameHourIndex`、`pickAreaCell` 落區+可走+避開被擋格、`vacuumWillYield`、以及多幀整合模擬證明會位移+換區域+**全程 0 次與租客同格**,並反向驗證下一格有人時原地不動、人走後續走);新增 `scripts/render-vacuum.ts` 離線產出 3 張不同時間點 PNG 視覺驗證。`vacuum-test.ts` **已併入 `run-all.ts` 正式回歸集**(比照 `pet-test.ts` 先例,避免孤兒測試爛掉),故 `npm test` 由 59 升為 **60/60**;`render-vacuum.ts` 為離線產圖工具(非 `*-test.ts`)不入迴歸。
- **測試/build/驗證**:`npm run typecheck` 乾淨;`TZ=Asia/Taipei npm test` **全綠 60/60**(含新併入的 vacuum-test;無 sim/平衡變動的證明改以 balance-snapshot diff 為空 + `src/sim/` 未觸碰為準);`npm run build` 成功;`npx tsx scripts/vacuum-test.ts` **17/17 通過**;`npx tsx scripts/render-vacuum.ts` 產出 `artifacts/ui-lab/rent/vacuum_0..2.png`,掃地機分別在 (6,4) 走廊→(3,24) 浴室→(3,17) 空房 r303,**三幀位置皆不同且全程不與租客疊格**。
- **平衡快照**:**未移動**。`git diff main -- scripts/balance-snapshot.json` 為空;未觸碰 `src/sim/` 任何檔案(comfort/tick/economy/gameState/persistence 全未動)。
- **已知限制/後續**:(1) 視覺上掃地機巡遍整層,但機械 cleanPower 仍只墊高「它被擺放的那間房」的整潔基準——外觀與機制刻意分離,不代表它真的在打掃別間;(2) 若掃地機長時間閒置在唯一走廊格上,理論上會擋到必經的租客(等它下一次小巡或換小時就解開),已用「閒置也會就近小巡」降低機率,未做「主動讓路」;(3) 種子房預設未擺放掃地機,故需玩家購置後才會出現(視覺驗證用 in-memory 擺放,不進 INITIAL_PLACEMENTS 以免動到平衡)。
- **硬規則保留**:本次為純外觀改動,未成年戀愛線排除、成人內容遮蔽預設關、AI 白名單/夾值/消毒均未觸及。

### U-3. 掃地機器人:自動維持整潔的家具(2026-07-24;Claude Code;舒適度第二波 C)
- **目的/需求**:給玩家一個「花錢換省心」的整潔投資——買了掃地機器人,房間會慢慢常保乾淨,而不是 Sims 式的瞬間清潔按鈕,符合「慢變環境品質」定位。
- **實際變更與主要檔案**:`src/furniture/catalog.ts` 新增選配欄位 `cleanPower?: number` + 新家具 `robot_vacuum`(掃地機器人,utility/room,tier=standard,price 6000,cleanPower 25);`src/sim/comfort.ts` 的 `cleanlinessBaseline()` 改為 `clamp(50 + storage*2 + roomCleanPower, 50, 90)`——用新增的 `roomCleanPower(roomId)` 掃房內 `cleanPower` 總和(**用專屬訊號偵測,不硬寫 id**),上限由 80 放寬夾到 90;`src/furniture/render.ts` 新增 `robot_vacuum` kind 的像素繪圖(圓盤機身+感應燈+掃刷)。商店/資訊卡透過既有 CATALOG 分組與 tier 自動呈現。`scripts/comfort-test.ts` 加 3 項斷言(機器人墊高整潔基準、基準夾 ≤90、有機器人時整潔隨時間爬過舊基準 50)。
- **測試/build/驗證**:`npm run typecheck` 乾淨;`TZ=Asia/Taipei npm test` 全綠 59/59;`npm run build` 成功。從工作區根 `npm run ui:shot -- rent` 產出 18 張截圖 **0 errors**(app 載入我的改動後正常渲染、種子房完好);tier chip 與機器人 sprite 只在商店浮層/擺放後可見,預設截圖流程未開商店,已於下方限制註記。
- **平衡快照**:**未移動**。種子房沒有機器人 → `roomCleanPower=0`;基準夾上限由 80→90 對種子房無影響(r302 基準 62、r301/r304 為 50,皆 <80);已跑 balance-test 確認一致。
- **已知限制/後續**:掃地機器人維持的整潔穩態受作息活動拖累(陳家豪房約收斂到 ~54~56),這是刻意的「維持而非爆乾淨」;預設 ui:shot 未涵蓋商店浮層,tier 星標與機器人外觀以程式碼審查 + typecheck + build + 樓層截圖 0 error 佐證,未來可加自訂 Playwright 流程截商店。
- **硬規則保留**:未動未成年戀愛線排除、成人內容遮蔽預設關、AI 白名單/夾值/消毒;cleanPower 純墊高 homeostasis 基準且夾值,無 RNG、無瞬間狀態改寫。

### U-2. 家具品質層級 tier + 平價入門變體(2026-07-24;Claude Code;舒適度第二波 A)
- **目的/需求**:家具原本只有隱性階梯(單/雙/帷幔床同名同胞),缺一眼可辨的品質分級;也缺給手頭緊玩家的便宜入門選項。
- **實際變更與主要檔案**:`src/furniture/catalog.ts` 新增選配 `tier?: "budget" | "standard" | "premium"`(純標示,**不影響任何模擬數值**——舒適度/招租星等仍只看 attributes),並替所有可販售家具標上合理 tier(single/double/canopy 床 = budget/standard/premium 等);新增三件 budget 變體 `folding_bed`/`plastic_stool`/`bare_bulb`(便宜、低屬性,沿用既有 kind 繪圖,**刻意不放進種子房 r301~r304**)。`FurnitureShop.vue`、`FurnitureInfo.vue` 以星數+中文(★平價/★★標準/★★★精品)顯示層級。`scripts/data-catalog-test.ts` 加 6 項斷言(tier 值域合法、床鋪三階價格/屬性遞增、新變體皆 budget 且比同類較貴同胞便宜/低屬性)。
- **測試/build/驗證**:`npm run typecheck` 乾淨;`TZ=Asia/Taipei npm test` 全綠 59/59(data-catalog 39、furniture-art 46、limezu 64 皆過);`npm run build` 成功。
- **平衡快照**:**未移動**(新家具未擺放、tier 不進任何數值計算;已跑 balance-test 確認一致)。
- **已知限制/後續**:budget 變體用既有 kind 圖(折疊床=床、椅凳=椅、裸燈泡=燈),外觀與標準版相同,尚未畫專屬像素;之後可補。tier 目前純視覺,未接招租/口碑加權(刻意,避免動平衡)。
- **硬規則保留**:未動未成年戀愛線排除、成人內容遮蔽預設關、AI 白名單/夾值/消毒;負屬性通道仍只有 noise,budget 件為低正屬性而非負分。

### U-1. 舒適/整潔敘事鉤子:正向慶祝 cozyHomePass(2026-07-24;Claude Code;舒適度第二波 B)
- **目的/需求**:舒適度系統原本只有負向的 `dirtyComplaintPass`(髒亂抱怨),缺少對稱的正向回饋。補一個「房間既舒適又乾淨」的正向慶祝,鼓勵玩家用心佈置與維持整潔,維持「慢變環境品質、非 Sims 式照顧」的定位。
- **實際變更與主要檔案**:`src/sim/tick.ts` 新增 `cozyHomePass(day)`(export)——房間 `roomComfort ≥ 52` 且 `cleanliness ≥ 80` 時,每 3 遊戲日偶爾一句 🏡 正向日誌 + 極小加成(mood +2、satisfaction +1);文案用日期+租客雜湊 `cozyHomeIndex` 穩定選字,**不擲 Math.random**(與 `dirtyComplaintPass` 同樣的日數冷卻風格,不打亂 balance RNG 序列);在午夜區塊 `dirtyComplaintPass` 後呼叫;另 export `resetCozyHomeCooldown()` 供測試。`scripts/comfort-test.ts` 加 4 項斷言(高舒適乾淨房會慶祝、給微小加成、日數冷卻、髒/低舒適房沉默)。
- **測試/build/驗證**:`npm run typecheck` 乾淨;`TZ=Asia/Taipei npm test` 全綠 59/59;`npm run build` 成功。
- **平衡快照**:**未移動**。慶祝在 balance 種子中確實會觸發(林小婕乾淨房),但 10 日終點的 mood/satisfaction 漂移落在既有 ±0.6 浮點容差內、且日誌數已在 LOG_CAP=60 飽和,故 `balance-snapshot.json` 維持一致(未 --update)。
- **已知限制/後續**:慶祝門檻(comfort 52 / clean 80)是依現行家具經濟(單房舒適度上限約 56)抓的;若日後大幅擴充高屬性家具,可再上調門檻。可選的「舒適社群事件」本次未做(留待後續)。
- **硬規則保留**:未動未成年戀愛線排除、成人內容遮蔽預設關、AI 白名單/夾值/消毒;新鉤子純敘事 + 夾值加成,無 RNG。

### T. 劇情弧 StoryArc + 事件連鎖 flag(2026-07-11;設計檢討 §2 工作項 7)
- 新模組 `sim/arcs.ts`:每位租客 0~1 條進行中的弧 `{id, theme, stage, maxStage, summary}`,**純敘事骨架不帶數值**;AI 每日 narrate 回 `arcUpdate` 推進。
- **消毒(sanitizeArcUpdate)延續 AI 安全原則**:主題截 14 字/摘要截 160/maxStage 夾 2~6;進行中 stage 只能 +1 不可倒退、theme/id 鎖定(AI 不能中途換故事);`done:true` 或推到 maxStage → 收束:清弧 + 留 `[經歷:主題]` 記憶 + 「📕 篇章落幕」notable 日誌(開弧也有「📖 新篇章」,兩者都進動態 Feed)。
- **事件連鎖 flag**:事件選項 effect 可留 `flag`(≤16 字伏筆旗標,cleanEffect 消毒)→ decide 記到 `rt.flags`(去重、cap 12)→ 每日 narrate context 餵回 AI(「未回收的伏筆旗標」),prompt 要求後續回收。
- Worker prompt 新增 arc 規則(沒弧可開新弧/有弧必須推進或收束/平淡別硬開)+ flags 說明;context 帶「進行中的劇情弧」與伏筆清單。
- UI:房間細看標籤列藍色弧 chip(`📖 主題 · 2/3`);摘要展開時多一行弧進展。
- 存檔:runtimes 加 `arc`/`flags`(additive,load `?? null`/`?? []`);模板 fallback 不動弧(離線遊戲照跑)。
- 測試:`scripts/arc-test.ts` 25 項(消毒單元 + mock AI 三日開/推/收全流程 + flag 傳遞/去重/cap + 存檔往返);全 34 腳本 + build + wrangler dry-run 通過。

### S. 動態 Feed + 底部導覽(2026-07-11;設計檢討 §3 工作項 6)
- 新模組 `sim/feed.ts`(唯讀彙整,不碰模擬):`buildFeed()` 把**全棟**租客的重要日誌(notable/major)、📖 當日觀察、🏠 房東介入(decisionNote)與 📢 系統通知匯成一條新到舊時間軸(cap 60);minor 流水帳不進,想看細節仍點房間。
- **底部導覽列**(§3:樓層/動態/收支/關係四分頁,取代樓層底部一排浮動按鈕):樓層與動態是主畫面切換,收支/關係從導覽開既有底部面板;樓層列只留 🛒 商店 + 兩顆 ⏩ 快轉。
- **動態分頁 = 看故事主動線**(`FeedPanel.vue`):最上方紅色「🔴 事件中心」集中所有待決抉擇(醒目色塊,點了跳進該房直接開抉擇彈窗);時間軸每則可點 → 跳該租客房間;從動態進房,返回鍵變「← 回動態」。
- **未讀徽章**:`state.feedSeenMs`(入存檔,additive `?? 0`)記上次看動態的遊戲時間;導覽「動態」上紅badge=待決事件數、紫badge=未讀動態數;進分頁快照已讀點(NEW 標記基準)後標已讀,離開時再吸收停留期間湧入的。
- 測試:`scripts/feed-test.ts` 14 項(彙整規則/排序/cap/未讀/存檔往返);全部 33 腳本 + build 通過,balance 快照不變(Feed 唯讀)。

### R. 房間升級階梯(2026-07-11;設計檢討 7-1「投資遊戲性」第二項)
- `sim/upgrades.ts` 三種**一次性大額投資(不可退)**:🔇 隔音改建 $12k(隔音+8)/ ✨ 精裝修 $18k(品味+7 療癒+6)/ 📡 智慧家居 $15k(科技+8)。
- 投資回報三條路:**(1)** 永久房間屬性 → 應徵者星等(roomAttributes 疊加);**(2)** 應徵者租金行情 +12~18%(Applicant 新增 baseRent,rescore 即時反映);**(3)** 對在住租客談漲租容忍度 +5~7%(previewRent 疊加,上限 0.35)——改建完去談漲租,原本會翻臉的幅度變談得成。
- 在住房改建立即回饋:滿意+10/心情+8/清不滿倒數 + `[房間精裝修了]` 記憶(AI 可寫進日記)。
- 入口:房間細看「🔨 改建」+ 招租面板「🔨 改建」(空房先改建再招高星租客);新帳目分類 `upgrade`(財務面板「房間改建」)。
- 存檔:`upgrades` 欄位(additive、舊存檔缺 = 空)。
- 測試:`scripts/upgrade-test.ts` 18 項(含「+16% 漲租改建前被拒/改建後談得成」對照組)。

### Q. 記憶生命週期(2026-07-10;設計檢討 §6 工作項 5)
- `MemoryTag.intensity`(0~1,選配欄位,舊存檔缺 = 1,免遷移):新記憶 1 起跳,**每日依語意衰減**——強情緒(失戀/心碎/崩潰)0.09/天(約 11 天痊癒)、一般 0.05/天(約 20 天)、持續狀態(貓/寵物/同居/交往)不衰減(由分手/矛盾淡出/搬走清理收尾)。
- `memoryDrift` 乘上 intensity:記憶越淡影響越弱;歸零自動移除 + 「🍂 記憶漸漸淡了」日誌(與既有 🕊️ 矛盾淡出並存,一慢一快)。
- UI:記憶 chip 透明度隨強度變淡(生命週期可視化)。
- balance 基準已因此刻意更新(林小婕的[頻道破萬]漂移隨時間減弱,mood 97.6→94.2,同時改善黏高)。
- 測試:`scripts/memory-lifecycle-test.ts` 11 項全過。

### P. AI 連續性摘要 + 節流退避(2026-07-10;設計檢討 §6 工作項 4)
- **滾動摘要閉環**:`Tenant.recentSummary` 變成真正的「連載前情提要」——每日 narrate 把現有摘要餵給 AI(prompt 要求接續、伏筆推進),AI 回 `summaryUpdate`(50~150 字,前後端都夾 220 字)回寫,次日再餵回去。UI 的「📋 近期摘要(AI 記憶)」從此會隨劇情演進。
- **節流**:多位租客的 narrate 串行、間隔 4 秒(`NARRATE_GAP_MS`);**退避**:Worker 端 Gemini 429 → 等 5 秒重試一次,再失敗才 fallback 模板。
- 測試:`scripts/summary-test.ts`(mock fetch)6 項——含「次日 context 餵的是更新後摘要」的閉環驗證與節流間隔實測。

### O. 行為指令白名單(2026-07-10;設計檢討 7-2「讓 AI 事件看得見」)
- 新模組 `sim/directives.ts`:六種白名單指令,全部是既有 visualState/作息/props 的組合,AI 不能發明機制:
  `night_owl`(作息後移 3h)/`early_bird`(提前 2h)/`hermit`(不去交誼廳)/`social`(傍晚泡交誼廳看電視)/`adopt_cat`(房裡常駐貓+每晚 20 點逗貓)/`binge_watch`(22-23 點黏在電視前)。
- **走既有三道關卡**:指令掛在事件「選項」的 effect 上(`EventEffect.directive`)→ `sanitizeDirective` 白名單驗證 + days 夾 1~7 → 玩家抉擇拍板才生效。Worker prompt 已告知 Gemini 可用(附使用時機指引)。
- 套用在 `tick.decideState/applyHour`:作息位移查表、時段插入活動、目標改寫(hermit 交誼廳目標改回自房/social 自房休閒改去交誼廳);到期自動恢復 + 「恢復往常」日誌;生效當下立即重定位。
- 規則事件首例:生病事件「讓他自己休養」→ hermit 2 天(便宜不花錢,但他會閉門不出)。
- UI:房間細看標籤列最前顯示綠色指令 chip(例「🌙 作息大亂(熬夜中)· 剩 2 天」)。
- `TenantRuntime.directive` 入存檔(additive,load `?? null`);修 header 在窄螢幕被 🔔⚙️ 撐成兩行(nowrap+窄版藏 prototype 徽章)。
- 測試:`scripts/directive-test.ts` 18 項全過;balance 快照不變(未觸發指令時零影響)。

### N. 數值模型重整 + balance 快照(2026-07-10;設計檢討 §6 工作項 3、10 完成)
- **homeostasis 抗飽和**(`sim/tick.ts` applyStat):mood/stress 每小時朝「性格基準」回歸 6% 再吃活動增量,不再黏死 0/100。基準由 coreTags 決定(工作狂 stress+12、社恐+8、自律−4…),再疊三個回饋:**社交**(戀人 mood 基準+6/朋友+3/孤立−4,= socialFulfillment 簡版)、**健康**(wellbeing<30 → mood 基準−10)、**精力**(energy<25 → stress 基準+10)。affinity 是關係累積,不回歸。
- **hygiene → wellbeing(身心健康)+ 後果**:長期 stress≥80 或 energy<20 每小時蛀健康;wellbeing≤28 觸發**生病事件**(看醫生 $800 快好/送湯 $200/放著慢好);wellbeing 也有 1%/h 弱回歸避免黏 100。
- **新增 energy(精力)**:資源型——睡覺 +9/h 充電、工作/直播 −4、外出 −3…(`generate.ts` EFFECT 表);記憶漂移也接上(熬夜 −0.4、運動 +0.3)。
- **滿意度公式納入新軸**:0.25 mood + 0.3 affinity + 0.25 (100−stress) + 0.1 wellbeing + 0.1 energy,再乘房間需求滿足率。
- **存檔遷移層第一次實戰**:SAVE_VERSION 2→3,MIGRATIONS[2] 把舊檔 stats.hygiene 改名 wellbeing、補 energy;qol-test 實測 v2 舊檔匯入自動升級。
- UI:房間細看數值列加「精力/健康」兩條(共 7 條),顯示值取整;STAT_HELP 更新。
- **balance 快照測試**(工作項 10):`scripts/balance-test.ts` 固定種子(mulberry32)跑 10 遊戲日、事件自動選第一項,關鍵數值快照存 `scripts/balance-snapshot.json`;之後手感悄悄跑掉會直接 diff 現形,刻意調平衡用 `--update` 重建基準。
- 測試:新增 `stats-model-test.ts`(9 項);全部 23 個腳本 + build 通過。

### M. 調租談判(2026-07-10;設計檢討 7-1「投資遊戲性」第一項)
- 房間細看新增「💲 談房租」(僅承租人;同居者不付租談不了)→ `RentPanel.vue`:±5% 一格(-20%~+30%)、即時顯示提案金額與**模糊預感**(會感激/應會接受/有點冒險/八成翻臉)。
- 機制(`sim/tenancy.ts`):容忍度 = (滿意度×0.5+好感×0.5−35)/200(0~25%)。**降租**必成:好感/滿意↑、降 ≥10% 留 `[房東主動降租]` 記憶;**容忍內漲租**勉強接受:租金↑但好感/滿意↓;**超過容忍**拒絕:房租不變、關係惡化、不滿累積 +10、留 `[對漲租不滿]` 記憶(AI 可拿去寫劇情)。單次夾 ±30%,每人冷卻 5 遊戲日(成敗都算)。
- 新欄位 `TenantRuntime.rentChangeDay`(存檔相容:load `?? -99`);收租(月租/30)自動跟著新租金走。
- 測試:`scripts/rent-test.ts` 19 項全過。

### L. store 模組化 + 存檔遷移層(2026-07-10;設計檢討 §6 工作項 1、2 完成)
- **store 拆分**:`store.ts`(原 ~1100 行)拆成 7 個模組,`store.ts` 只剩 façade re-export,**所有元件/scripts 的 import 路徑不變**:
  - `sim/gameState.ts` reactive state + 共用常數/型別/小工具(notify/pushMemory/refreshAppearances…)
  - `sim/tick.ts` 每小時模擬(作息+偏離/事件/社交/換日)、syncToNow、快轉(同步版+分批版)
  - `sim/economy.ts` addMoney 唯一入口、收租/管理費、家具買/擺/移/賣
  - `sim/tenancy.ts` 招租/入住/退租/同居、房東抉擇 decide
  - `sim/narration.ts` 每日 AI 日記(之後的連續性摘要/narrate 佇列放這)
  - `sim/persistence.ts` 存檔/載入/遷移 + 匯出入
  - `sim/lifecycle.ts` initGame/掛機計時器/除錯鉤子
- **存檔遷移層**:`SAVE_VERSION` 常數 + `MIGRATIONS` 逐版升級表 + `migrateSave()`;壞檔/未知版本/未來版本安全 fallback 新遊戲;`importSave` 也走遷移(存「遷移後」的檔)。**之後改存檔結構:SAVE_VERSION++ 並在 MIGRATIONS 加一段升級函式即可,舊檔不會壞。**
- 純搬移、零行為變更;驗證:全部 17 個測試腳本通過 + `qol-test` 加測存檔往返(匯出→改壞 state→initGame 重載→還原)18/18 + build + wrangler dry-run。

### K. 操作 QoL(2026-07-10;設計檢討第 8 節六項全數完成)
- **8-1 家具移動**:家具資訊卡「📦 移動」→ 點地圖搬過去(免費、可跨房,房間屬性/招租星等跟著變);失敗留原位。`store.ts` pendingMove、`canPlaceFree` 支援排除自己。
- **8-2 擺放/移動預覽(確認制)**:點格先畫半透明 footprint(綠=可放/紅=不可),再按「✓ 確認」才成交扣款;紅色時確認鈕 disabled。`floorScene.drawFootprintPreview` + `store.canDropAt`。
- **8-3 通知中心**:header 🔔 → 通知歷史面板(`state.noticeLog`,cap 30、入存檔)。
- **8-4 存檔管理**:header ⚙️ → `SettingsPanel.vue`:重新開始(二次確認)/匯出存檔(剪貼簿,失敗退 textarea)/匯入存檔(驗 v=2 才寫入);匯入/清檔後封鎖 save 防止 reload 前被舊狀態蓋回。
- **8-5 數值說明**:房間細看五條數值 label 可點 → toast 一句話說明。
- **8-6 快轉體驗**:UI 快轉改分批(每批 5 tick、批間讓出主執行緒不卡畫面),按鈕顯示「快轉中…」;新增「⏩ 1 天」鈕(跨午夜觸發 AI 日記);同步版 `fastForward` 保留給測試腳本。
- 測試:`scripts/move-test.ts`(13 項)、`scripts/qol-test.ts`(16 項,mock localStorage 驗存檔管理/預覽判定/分批快轉)。

### J. 部署與自我檢測
- Cloudflare Worker + 靜態資產;`keep_vars=true`(部署不洗掉 Secret);push 即自動部署。
- `scripts/` 無頭測試:`sim-trace`、`stakes-test`、`social-test`、`ai-event-test`、`part-test`(跨租客/收支/時間)、`variety-test`、`bugfix-test` 等。

---

## 三、已修掉的 bug
- 房客插牆(站立點避牆、不瞬移)/ 家具無法指定位置(改點地圖擺放)/ 動態租客跑別人房間(只用自房+共用區)。
- **第四位租客撞色** → 改依房間(301-304)指派配色,保證不撞。
- **白天卻說「半夜泡麵」** → 模板句改時段感知({time}→早晨/午後/傍晚/深夜)。
- **AI 記憶不變動** → prompt 加強 + 去重上限;(註:實際 AI 版需 Gemini 額度未超額時才出現)。
- (2026-07-10)**同居後角色消失** → 原本直接把搬過去的人整個刪除;改為真同居(`state.cohabits`:住伴侶房、免一份租、關係保留;分手搬回空房或搬離;伴侶退租時接手承租;房間細看可切換查看同住者)。
- (2026-07-10)**退租後別人身上還留著他的 tag** → moveOut 清掉提到他的記憶標籤;親近者留一筆「搬走了」的失落記憶。
- (2026-07-10)**心情 100 卻掛 [情緒低落]** → 每日「矛盾記憶淡出」:數值已明顯走出該記憶方向 → 標籤移除 + 心境轉變日誌;補「低落/搬走」等漂移關鍵字。
- (2026-07-10)**招租應徵者刷新過頻** → 應徵者池存進 store,每遊戲日換一批(開關面板/重整不重抽),星等隨裝潢即時重算、排除同名。

---

## 四、待辦事項(依優先序)

> ⚠️ **可信度提示**:本清單為人工維護,曾落後實際程式碼約一個月。
> 使用前請與本檔尾端的**日期紀錄**(第七節「進度補記」逐日條目;2026-07-24 以前的在
> [`docs/工作日誌-封存.md`](docs/工作日誌-封存.md))對照——
> **日期紀錄是最終事實來源**;本清單與其衝突時,一律以日期紀錄為準。
> 上次與程式碼對帳:**2026-08-01**(HEAD `21b8b5c`)。
>
> 🔍 **驗收錨點制(2026-07-30 起強制)**:第四、五節**每一條 `- [ ]` 後面都必須附一行
> `🔍 驗收:<可 grep 的檔名或符號> → 有命中即代表已完成`**。理由:「月度全樓事件鏈」
> 這條孤兒條目曾在本檔路線圖與 `docs/交接與待辦.md` **兩個位置各殘留一次**,證明
> 「交付時人工記得回來劃掉」這個做法會失效。有了錨點,下一位接手者不需要讀懂條目、
> 也不需要翻日期紀錄,只要**逐條把錨點 grep 一遍**就能機械式判斷這條是否已被實作:
> 有命中 → 改標 `[x]` 並補上完成日期與 commit;沒命中 → 這條是真的還沒做。
> 新增任何 `- [ ]` 時**一併想好錨點**;想不出錨點(例如「持續微調平衡」這種沒有終點的項目)
> 就標 `🔍 驗收:長期項,無完成訊號`,明確告訴後人不要期待它會被劃掉。
>
> 📐 **錨點三規則(2026-08-01 對帳後新增)**:這輪機械執行 25 條錨點時抓到 3 條錯的,
> 三個錯誤指向同一件事——**寫錨點的當下,錨點是「預言」;只有完成時回頭改寫,它才變成「證據」**。
> 三條規則就是把預言轉成證據的最小約束:
>
> 1. **完成一條時,若實際符號與錨點不符,改 `[x]` 的同時必須把錨點改寫成真實符號。**
>    錨點的角色從「我猜完成後程式碼長這樣」變成「這條為什麼算完成」的證據。
>    (實例:四方向人物的錨點原本猜 `CHAR_WALK_(UP|DOWN)|facingDir|facing4`,
>    實作採 `CharView`/`CHAR_STAND_BACK`/`agentView`;多人站位猜 `startGroupSession`,
>    實作是 `startGroupScene`。兩條都已做完卻 grep 0 命中,機械執行會判定「還沒做」。)
> 2. **「函式體內」「僅限某檔某段」這類限定條件不能用單行 `rg` 表達,必須用兩段式**
>    `rg -A<n> "<錨>" <檔> | rg "<樣式>"`。單行 `rg` 只知道「這個檔裡有這個字」,
>    不知道它落在哪個函式;限定條件寫在條目內文而錨點沒有,就會產生**偽陽性**——
>    這比漏判更危險,因為機械執行會把沒做的事劃成完成。
>    (實例:`buyFurniture` 那條原錨點 `rg -n "price <= 0|memorial" src/sim/economy.ts`
>    會命中 `:304` 的 `movePlacement` 與 `:389` 的 `sellFurniture`,而條目要求的
>    `buyFurniture` 函式體內其實仍無守門。)
> 3. **任何 `[x]` 條目的內文若含「尚未／留待／第 N 階段」字樣,必須另開一條 `- [ ]` 並自帶錨點。**
>    掛在 `[x]` 內文裡的殘留工作對機械掃描是隱形的——掃描只看 checkbox 狀態。
>    (實例:家具 tier 的**第三階段**整整兩輪對帳都只寫在一個 `[x]` 條目的內文裡,
>    沒有自己的 checkbox 也沒有錨點,直到 2026-08-01 才獨立成條。)

### 讓 AI 影響更深(延續已做的 AI 抉擇)
- [x] **記憶標籤影響機制**:已由 `memoryEffects.ts` 讓失戀/熱戀/噪音/感激等記憶產生數值漂移與生命週期;養貓、行為指令也已成為實際模擬。新標籤若要改行為,仍需加入白名單規則,不可讓 AI 任意發明機制。
- [x] **AI 每日微調數值(隱性持續影響,需嚴格夾值)** ✅ 已完成(2026-07-16,見
  `src/sim/observationEffects.ts` 的 `statNudge`:僅開放 mood/stress/energy/wellbeing ±3 與
  affinity ±2 並夾值,satisfaction/money 不開放,寫 🔮 日誌)。

### 平衡與數值
- [ ] 平衡調整:事件頻率、滿意度門檻、管理費/租金、家具價格皆為暫定,需實玩微調。基準已由 `balance-test` 鎖住(改完記得 `--update`)。
      🔍 驗收:長期項,無完成訊號
- [x] **租客 wallet 與繳租戲劇** ✅ 已完成(2026-07-17;薪資／生活費／財務困難／欠租求情／
  自動補繳、AI context、存檔與欠租 chip 均已接線)。
- [ ] 數值模型延伸剩餘項:完整 `socialFulfillment` 衍生值。現況只有 `tick.ts:224` 註解
      自稱的「簡版」(有戀人/朋友 → 心情基準↑、完全孤立 → ↓),沒有獨立模組或欄位。
      🔍 驗收:`rg -l "socialFulfillment" src/` 命中**不只** `src/sim/tick.ts` 一支 → 已完成

### 內容與呈現(美術債)
- [x] **房間細看重畫**:已改為跟拍樓層實景鏡頭,並補日夜光線、錯誤回報與 iOS Safari 畫布塌陷修正。
- [x] **姿勢細分 cook_pair / stand_face** ✅ 已完成(2026-07-14,見 `src/floor/pairSession.ts`
  的 `PairPose`、`src/sim/community.ts` 取用、`src/floor/floorScene.ts` 的 drawAgent 分支)。
- [x] **四方向走路 sprite** ✅ **已完成(2026-08-01;`569d763` + review 收尾 `21b8b5c`)**。
      音效/音樂已由使用者表示可略過(見封存檔 2026-07-16 路線圖末「已使用者婉拒/擱置」
      與交接文件 G-9),不再列為待辦。
      實作:`src/pixel/sprites.ts:263` 新增 `export type CharView = "front" | "back" | "side_r" | "side_l"`,
      `:416-418` 三組 stand/walkA/walkB 基底(`side_l` 由 `mirrorSprite` 鏡射);
      `src/floor/floorScene.ts:359` 的 `agentView()` 依 `cook_pair → stand_face → 移動方向 →
      沿用最後方向` 四條規則推導視角;`src/pixel/parts.ts:343-345` 的 `overlayViewOf()`
      決定髮型/配件疊層表與是否鏡射。
      ⚠️ **本條與下方「美術第二批留項」的「四方向人物」是同一項**(重複列),
      兩處已一起劃掉;**完整完成說明以本條為準**,下方那條只保留指標。
      🔍 驗收:`rg -n "CharView|CHAR_STAND_BACK|agentView" src/pixel/sprites.ts src/floor/floorScene.ts`
      有命中 → 已完成(**2026-08-01 錨點改寫**:原錨點猜 `CHAR_WALK_(UP|DOWN)|facingDir|facing4`,
      實作採 `BACK`/`SIDE_R`/`SIDE_L` 命名,舊錨點 0 命中會誤判未完成;依錨點規則 1 改寫為真實符號)
      ⚠️ **本項只覆蓋站與走**,坐/躺/`cook_pair` 仍一律正面——**已獨立成第五節的一條 `- [ ]`**,
      不要因為本條 `[x]` 就以為四方向全做完了(錨點規則 3)。

### 內容擴充項(彙整,詳細規格見對應日期紀錄)

> 本節原標題是「**尚未動工**的內容擴充」,但 2026-07-30 對帳時七條裡已有六條完成——
> 標題本身在騙人,故改名。狀態一律看每條的 `[ ]`／`[x]` 與 🔍 錨點,不要看小節標題。
- [x] **月度全樓事件鏈**(🟡)✅ 已完成(2026-07-28;3 條鏈 × 4 階段、獨立抉擇槽、
  零 RNG、動態頁章節卡;見 `src/sim/floorChain.ts` 與本檔尾端同名日期紀錄)。
- [x] **夢境彩蛋**(🟢)✅ 已完成(2026-07-29;睡覺時段每人每 3~4 遊戲日一則、零 RNG、
  素材取自記憶標籤與關係;見 `src/sim/dreams.ts`、`src/content/dreamLines.ts`
  與本檔尾端同名日期紀錄)。
- [x] **樓外世界錨點**(🟢)✅ 已完成(2026-07-29;`c4ee922` 外出目的地文本 + 樓外巧遇,
  `ce4be56` review 修正加上同地點條件與每日全域鎖;見 `src/sim/outing.ts`、
  `src/content/outingLines.ts` 與本檔尾端同名日期紀錄)。原規格見封存檔 2026-07-16
  「後續優化規劃」第 6 項。
- [x] **共用區(交誼廳/浴室/洗衣間)舒適度延伸** ✅ 已完成(2026-07-30;`4fdb79c` +
  review 修正 `99b9a36`。走 `communalQuality()` → `communalBaselineDelta()` 的**獨立管道**,
  刻意不塞進 `roomComfort` 以避開 `cozyHomePass` 的翻轉窗口;`comfort.ts:243-433`、
  `tick.ts` 兩處接點、`App.vue` 新增「🏢 公共空間」列,`communal-comfort-test` 41 項
  已在 REGRESSION 內;見本檔尾端同名日期紀錄)。原規格見封存檔 2026-07-23 房間舒適度第一波
  「第二波留項」與 2026-07-24 U-2 條目。**軟性限制四條見第五節。**
- [x] **家具品質 tier 接上行為差異** ✅ 已完成(2026-07-29;`TIER_POINTS` 逐件加總成為
  舒適度第三個加項 `tierPart`,未標 tier fallback 為中性 standard;
  見 `src/furniture/tier.ts`、`src/sim/comfort.ts` 與本檔尾端同名日期紀錄)。
  **夾值:私人房 `TIER_MAX = 10`(`comfort.ts:58`;`8504345` 已從 12 降到 10),
  共用區另用較寬的 `COMMUNAL_TIER_MAX = 20`(`comfort.ts:265`——種子交誼廳的 tierPart
  已經 7.0,沿用 10 幾乎沒有升級空間)。** 本檔與 `docs/交接與待辦.md` 曾誤寫「0~12」,
  已於 2026-07-30 對帳修正。
  ✅ **睡眠效率已於 2026-07-30 接上(第二階段)**——`SLEEP_MULT = { budget: 1.0,
  standard: 1.12, premium: 1.22 }`(review 後由 1.15/1.35 下修,理由見日期紀錄),
  **只乘 energy 與 stress**(`src/furniture/tier.ts` 的
  `sleepMultiplier()` + `src/sim/generate.ts` 的 EFFECT 展開;見本檔尾端同名日期紀錄)。
  **尚未接**招租星等/租金/損壞率,以及睡眠以外的「用家具恢復」活動
  (沙發休息/看電視/泡澡/書桌),留**第三階段**——**已於 2026-08-01 獨立成下一條 `- [ ]`**
  (依錨點規則 3:掛在 `[x]` 內文裡的殘留工作對機械掃描是隱形的)。
- [ ] 🔴 **家具 tier 第三階段:睡眠以外的「用家具恢復」活動**(2026-08-01 對帳:
      從上一條 `[x]` 的內文提升為獨立條目)——第一階段=舒適度 `tierPart`(2026-07-29)、
      第二階段=睡眠效率 `sleepMultiplier()`(2026-07-30)都已完成,**第三階段整批仍未動工**。
      範圍:**沙發休息／看電視／泡澡／書桌**四類活動,以及招租星等／租金／損壞率。
      ⚠️ **這條不是小改動,列 🔴 的理由**:種子局的這些活動**全部踩在 premium 家具上**
      (`mic_desk` 72h、`gaming_desk` 41h、`shared_sofa`、`lounge_tv`、`bathtub`),
      一接就會整片改變 balance 快照——**必須接受這是刻意的平衡調整並跑
      `npx tsx scripts/balance-test.ts --update` 重建基準**,不能當成漂移壓回去。
      另外**不可照抄舒適度那套形狀**(`TIER_POINTS` 的加總 + 夾值):恢復效率是乘法通道、
      作用在不同的資源軸上,ROI 表要**重新推導**(第二階段的係數就是 review 時
      從 1.15/1.35 下修到 1.12/1.22 的)。
      📋 接手前先讀第七節 2026-07-30「睡眠效率 review 收尾」的「給第三階段的檢查清單」,
      以及下方第五節的 `usedDefId` 排隊分支地雷那條。
      🔍 驗收(**兩段式**:確認乘數真的擴出睡眠以外,而不是只改了註解):
      `rg -n "restMultiplier|activityMultiplier|RECOVER_MULT" src/furniture/tier.ts src/sim/generate.ts`
      有命中,**且** `git log --oneline -- scripts/balance-snapshot.json` 有一筆說明第三階段的重建
      → 已完成
- [x] **掃地機器人主動讓路** ✅ 已完成(2026-07-25;BFS 固定順序選退讓格、零亂數,
  `vacuum-test` 36 項;見本檔尾端同名日期紀錄)。
- [ ] **AI context 快取**(交接文件 C-9,**2026-07-30 對帳後重新定義**)——
      ⚠️ C-9 原本寫「narrate ctx 越長越貴,**可裁剪 todayLog 條數**」,但**裁剪那半已經實作了**:
      `worker/index.ts:319` 是 `selectDiverseNarrativeLines(clampArr(c.todayLog, 20, 200), 10)`
      (先 server 端夾 20 筆、再選 10 句多樣化),`:361` 的 `events` 同樣夾 12 選 6。
      **真正還沒做的只剩「快取」**——目前 worker 沒有任何 cache 層,相同 ctx 重複打會重複計費。
      本條已窄化成純快取;是否值得做(免費層 + 有模板 fallback)**待使用者決定是否直接結案**。
      🔍 驗收:`rg -n "caches\.default|Cache-Control|KVNamespace" worker/index.ts` 有命中 → 已完成

### 美術第二批留項(2026-07-30 對帳:從 Codex 兩批日期紀錄內文提升為正式條目)

> 這五條原本只寫在 `:2961-2965`(2026-07-29「電視／椅子像素美術第一批」的「已知限制／後續」)
> 與 `:2994`(2026-07-30「人物美術第一批」的「刻意不納入本批」)的**內文**裡,
> 接手者不逐條翻日期紀錄就不會知道。(⚠️ 行號會隨本檔增長而漂移,認**日期紀錄標題**比較可靠。)

- [x] 🟡 **AI 色碼對比安全化** ✅ **2026-07-30 完成**(見同日「AI 色碼對比安全化」日期紀錄)
      ——**五條中唯一有實際壞畫面風險的一條**,已優先做完。
      ⚠️ **2026-07-30 對帳更正**:Codex 日誌把位置寫成 `generate.ts`,實際在
      **`src/sim/invite.ts:63-64` 的 `pickColor()`**:`typeof v === "string" && HEX.test(v) ? v : pool[…]`
      ——只驗**格式**是合法 hex,**沒有白名單也沒有對比度檢查**,`HAIR_COLORS`/`SKIN_TONES`/
      `SHIRT_COLORS`/`PANTS_COLORS` 四個池子只是 AI 給錯格式時的 fallback。
      也就是 AI 只要回一個語法正確的 `#fdfdfd` 髮色就會被原封不動採用,可能與背景/膚色同色。
      接線處 `invite.ts:110-114`。
      **實際做法**:新增 `src/pixel/color.ts`(純函式)+ `parts.ts` 的 per-slot 亮度帶與
      `sanitizeAppearanceColors()`;膚色 snap 白名單、髮/衣/褲亮度帶夾值(色相保留)、
      髮膚 ΔL ≥ 0.10;掛在 invite / `makeRuntime` / `load` 三道邊界。
      **刻意不用「vs 背景」的 WCAG 對比**——實測自家色池對背景的 CR 幾乎全是 1.0~1.3,會誤殺現有美術。
      🔍 驗收:`rg -n "contrast|luminance|relativeLuma|deltaE" src/sim/invite.ts src/pixel/parts.ts src/pixel/color.ts` 有命中 → 已完成
      (⚠️ 錨點路徑已補上 `src/pixel/color.ts`:實作落在新檔,只掃原本兩個檔會掃不到而誤判未完成。)
- [x] 🟡 **四方向人物** ✅ **已完成(2026-08-01;`569d763` + `21b8b5c`)**——
      ⚠️ **本條與上方「內容與呈現(美術債)」的「四方向走路 sprite」是同一項**,
      是本檔長期存在的**重複列**(這種重複本身就是孤兒條目的來源:一處劃掉、另一處忘了劃)。
      **完整完成說明與驗收錨點一律看上方那條**,本條只保留指標避免兩邊各寫一份而再度分歧。
      🔍 驗收:同上方「四方向走路 sprite」條
      (`rg -n "CharView|CHAR_STAND_BACK|agentView" src/pixel/sprites.ts src/floor/floorScene.ts`)
- [ ] 🟢~🟡 **舊房間 renderer 的躺姿認不出是誰**——`src/pixel/scene.ts:511` 的 `lie()`
      直接 `drawSprite(ctx, CHAR_LIE, …)`,**沒有** `drawAppearanceOverlay`(同檔的 `stand()`
      與 `sit()` 都有疊),所以房間特寫的睡姿看不出髮型與配件。樓層 renderer 的躺姿已於
      人物美術第一批補上,只剩這個舊 renderer。
      🔍 驗收:`rg -n -A2 "const lie = " src/pixel/scene.ts` 出現 `drawAppearanceOverlay` → 已完成
- [x] 🟢 **桌前／電視前的臨時活動椅只有單一棕色造型**——(2026-07-31 完成,見同日
      「人物美術第三批」日期紀錄)`drawActivityChair` 改為先跑 `activityChairStyle(a)` 反查
      身下家具的 sprite kind,再分派到電競椅／辦公椅／矮凳三種造型,反查不到才回退原棕色木椅。
      家具種類在 `floorScene` 內唯讀反查(比對 `placementInteract` + Chebyshev ≤ 2),
      **零 `agents.ts` 改動**。
      🔍 驗收:`rg -n "function drawActivityChair" -A4 src/floor/floorScene.ts` 出現分支參數
      (如 `kind`/`variant`)而非寫死單一色組 → 已完成
- [x] 🟢 **atlas PNG／manifest 仍含 5 張不再使用的舊 crop**——(2026-07-31 完成,見同日
      「人物美術第三批」日期紀錄)`tv_console`、`lounge_tv`、`shared_sofa`、`loveseat`、
      `wood_chair` 已從 `scripts/limezu-manifest.json` 的 `furniture` 區塊刪除並重跑
      `build-limezu-atlas.py`;`furniture.png` 8,696 → 6,992 bytes,尺寸維持 128×128,
      其餘 18 個 frame 的 atlas 座標與像素**逐格證明完全不變**,故 `src/art/limezu.ts` 與
      `scripts/limezu-art-test.ts` 零改動。
      ⚠️ **驗收錨點已改寫**:manifest 內新增了 `replaced_by_procedural` 說明區塊(記錄這 5 件
      被哪張程序圖取代、原 crop 座標為何,避免日後有人重加),所以**原本的 `rg` 錨點會照樣命中
      而誤判未完成**。改用只看 `furniture` 區塊的精確檢查:
      🔍 驗收(**兩段都要過**:manifest 清了、atlas 上的死像素也真的清了):
      ```
      python -c "import json;from PIL import Image;m=json.load(open('scripts/limezu-manifest.json',encoding='utf-8'));a=Image.open('public/assets/limezu/furniture.png').convert('RGBA');d={'tv_console':(0,0,32,40),'lounge_tv':(32,0,32,40),'shared_sofa':(0,96,48,26),'loveseat':(48,96,30,18),'wood_chair':(94,96,14,22)};print('manifest殘留',[k for k in d if k in m['furniture']],'|尺寸',a.size,'|死區非全透明',[k for k,(x,y,w,h) in d.items() if a.crop((x,y,x+w,y+h)).getchannel('A').getextrema()!=(0,0)])"
      ```
      印出 `manifest殘留 [] |尺寸 (128, 128) |死區非全透明 []` → 已完成
      (18 個 frame 逐像素不變的完整證明見同日「atlas 清理」日期紀錄的「兩個硬性限制的達成證明」段)

### 測試債(2026-07-30 對帳:從日期紀錄內文提升為正式條目)

> 四條原本寫在 `:2844-2857`(2026-07-29「家具 tier 第二批」的「本次一併記錄的待辦與風險」)
> 與 `:2916-2917`(2026-07-29「畢業生紀念物從商店下架」的新留待辦);
> `buyFurniture` 那條原本**只**藏在 `:2900-2903` 的驗證段落內文,連 `- [ ]` 都沒有。
> (⚠️ 行號會隨本檔增長而漂移,認**日期紀錄標題**比較可靠。)

- [ ] 🟡 **`buyFurniture("memorial_*")` 仍可買到 $0 紀念物,且沒有測試釘住這條路徑**——
      已複核 `src/sim/economy.ts:252` 的 `buyFurniture()`:只檢查 `state.money < def.price`
      與 `findFreeSlot`,**沒有** `price <= 0` 或 memorial 的守門。2026-07-29 的商店下架
      **只改了 `FurnitureShop.vue` 的 `groups` UI 過濾**,`data-catalog-test` 的 tripwire
      也只釘上架清單。任何直接呼叫 store 的路徑(舊存檔遷移、未來的自動擺放/獎勵流程、
      或任何繞過 SFC 的程式)仍能免費塞滿 `tierPart`。
      🔍 驗收(**兩段式,兩段都要過**):
      ```
      rg -A10 "export function buyFurniture" src/sim/economy.ts | rg "price <= 0|memorial"
      rg -n "buyFurniture\(\"memorial" scripts/
      ```
      兩段都有命中 → 已完成
      ⚠️ **2026-08-01 錨點改寫(原錨點是偽陽性,本輪三個錯誤裡最危險的一個)**:
      原本寫 `rg -n "price <= 0|memorial" src/sim/economy.ts`,而「在 `buyFurniture` 函式體內」
      這個限定條件只寫在條目內文、錨點表達不出來。該單行 `rg` **會命中 `:304` 的
      `movePlacement` 與 `:389` 的 `sellFurniture`**(這兩處確實有 memorial 守門),
      但 `:252-261` 的 `buyFurniture` 本身**仍然只檢查 `state.money < def.price` 與
      `findFreeSlot`**——照「命中即完成」機械執行會把這條沒做的事劃成完成。
      改成兩段式後實跑為 0 命中,正確反映未完成(見錨點規則 2)。
- [ ] 🟡 **`logs` 欄位在 `LOG_CAP = 60` 飽和 → 對「多推日誌」零偵測力**——balance 快照兩位
      租客的 `logs` 都是 60 已飽和,**永久失去偵測力**(tier 第二批就是靠自製探針直接數 🏡
      筆數才確認沒多慶祝;共用區批的第一版探針 `cozyLogs` 兩邊都是 0,也是零偵測力)。
      相對地 `ledgerCount: 40` 未飽和仍有效。建議改存「特定 emoji 前綴的計數」或日誌內容雜湊。
      🔍 驗收:`rg -n "logs" scripts/balance-snapshot.json` 之外另有 emoji 計數/雜湊欄位 → 已完成
- [ ] 🟢 **balance 快照缺 `rentPaidTotal`**——金錢漂移目前要反推錢包遞推式才能歸因
      (tier 第二批的 ±$2 就是這樣查的)。建議加一個收租金額總和欄位。
      🔍 驗收:`rg -n "rentPaidTotal" scripts/` 有命中 → 已完成
- [ ] 🟡 **`cozyHomePass` 的 `comfort >= 52` 翻轉窗口待重估**——反解出的窗口是
      clean **74.8 ~ 84.2**(舊公式要 `clean >= 84.1`,加了 tierPart 後只要 74.8),
      而林小婕日間整潔**實際 ≈ 83 正落在窗口正中央**;沒翻過去只因 `cozyHomePass` 在換日
      那一刻跑,而換日正是她整潔的每日低點。
      ⚠️ **風險未消除,只是被 2026-07-30 共用區舒適度的「獨立管道」繞開**——共用區刻意不碰
      `roomComfort` 正是為了避開這個窗口,但**窗口本身還在**:任何墊高午夜整潔的改動
      (多一台掃地機、`cleanlinessBaseline` 微調、加收納家具)都會讓它翻過去。
      好消息是翻過去會讓 mood +2 遠超 ±0.6 容差 → `balance-test` 紅燈,屬**可偵測的脆弱**。
      重估 `COZY_COMFORT` 時直接用這三個數字:**74.8 / 84.1 / 實際 83**。
      🔍 驗收:`rg -n "COZY_COMFORT|COZY_CLEAN" src/sim/tick.ts` 的值有變、或測試新增窗口斷言 → 已完成
- [ ] 🟢 **`scripts/shop-test.ts` 是零斷言的早期煙霧腳本**——只有 `console.log`,
      `run-all.ts` 的判定(exit code ≠ 0 或「N 通過 / M 失敗」中 M > 0)對它幾乎不會紅燈,
      直接併入 REGRESSION 只會多一支不會失敗的腳本、徒增 CI 時間與虛假安全感。
      應先改寫成 `check()` + 「結果:N 通過 / M 失敗」+ `exit 1` 的斷言形式再併入。
      ⚠️ **改寫時注意它會把 `state.money` 設成 100**,要確認不影響同進程其他斷言。
      🔍 驗收:`rg -n "shop-test" scripts/run-all.ts` 有命中(且該檔已有 `check(`) → 已完成

### 未來大型擴充(設計已寫好:docs/設計檢討與優化.md 第 9、10 節)
- [x] ~~sprite 部件化~~(§9-1 已完成:5 髮型 × 6 配件 × 多色,新租客隨機組合)
- [x] ~~特邀租客~~(§9-3 已完成:招租面板「✉️ 特邀」→ 名字+描述 → AI 生成入住;**僅限成年角色**,未成年描述前端快篩+AI isAdult 雙重拒收;canRomance 加 isAdult 把關)
- [x] 內建角色原型擴充至 15 種、作息擴充至 7 套。
- [x] **互動系統大改版**:已完成資料驅動 `InteractionDef`、`canInteract()` 單一把關、雙人 session/家具錨點、戀愛/親密/衝突/戲劇事件與娃娃屋演出層。🔞 模式預設關、僅限成年情侶、全程遮蔽式,AI 不生成露骨內容。

### 多人 / 帳號(視需求)
- [ ] **跨裝置存檔**:登入 + 雲端存檔(Cloudflare D1/KV)——目前存檔綁單一瀏覽器。
      🔍 驗收:`rg -n "d1_databases|kv_namespaces" wrangler.toml` 有命中 → 已完成
- [ ] 共享世界 / 互訪 / 排行榜(需帳號 + DB + 同步)。
      🔍 驗收:同上(需先有 D1/KV binding);🔴 動工前先問使用者
- [ ] Gemini 免費層每分鐘/每日額度有限;多人同時玩或大量快轉可能超額→fallback。需要時可加節流或每人自帶 key。
      🔍 驗收:`rg -n "x-user-key|BYOK|userApiKey" worker/index.ts` 有命中 → 已完成(玩家自帶 key 版)

### 系統/工程
- [x] 通知中心、存檔管理(重置/匯出/匯入)→ 2026-07-10 完成(K)。
- [x] 測試 runner + GitHub Actions CI;正式回歸集與 24h `sim-trace` 可用 `npm test` 一次執行。
- [x] Worker AI 端點同源檢查、16KB 請求上限、server 端 context 夾值;特邀租客使用 Gemini structured output。
- [ ] PWA(需真機驗證 service worker,採 `autoUpdate` 避免玩家卡舊版)。
      🔍 驗收:`rg -n "vite-plugin-pwa" package.json vite.config.ts` 有命中 → 已完成
- [ ] Cloudflare Dashboard `/api/*` 每 IP rate limit;目前只有同源與 payload 防護,尚未根治共用 Gemini key 額度風險。
      🔍 驗收:**無程式碼訊號**(純 dashboard 設定)。改用:Cloudflare Dashboard →
      Security → WAF → Rate limiting rules 是否存在 `/api/*` 規則;做完請在此條註明設定日期
- [ ] 新手引導、手機打包(Capacitor)。
      🔍 驗收:`rg -n "@capacitor" package.json` 有命中 → 手機打包已完成;
      `rg -rn "onboarding|Onboarding" src/` 有命中 → 新手引導已完成
- [x] **設計檢討 7-3 主序列完成**:store 模組化、存檔遷移、調租、數值模型、行為指令、AI 摘要/節流、記憶生命週期、房間升級、Feed、StoryArc、事件/作息資料化皆已完成。

---

## 五、已知小問題 / 技術債

> ⚠️ 同上:本節與第七節的**日期紀錄**衝突時,以日期紀錄為準(對帳日 **2026-08-01**,HEAD `21b8b5c`)。
> 本節的 `- [ ]` 條目同樣適用第四節開頭的 🔍 **驗收錨點制**。

- 數值已多輪調整並有 `balance-test` 快照護航,仍需依真機實玩持續微調。
- PWA 尚未做;存檔仍只在單一瀏覽器 `localStorage`,跨裝置需雲端帳號/同步系統。
- Gemini 與 Workers AI 都有共用免費額度;目前已會錯開、限次、跨供應商 fallback 並保存待補佇列,但多人同時玩仍可能把當日免費額度用完。
- `/api/*` 尚未設定 Cloudflare Dashboard rate limit;所有玩家仍共用同一把 Gemini key。
- **型別檢查已涵蓋前端與 Worker**:`npm run typecheck` 會依序執行
  `typecheck:app`(`vue-tsc --noEmit`)與 `typecheck:worker`
  (`tsc --noEmit -p tsconfig.worker.json`);`worker/**/*.ts` 已於 2026-07-25 納入。
  前端與 Worker 刻意使用兩份 tsconfig；若日後需要跨端共用型別，再評估 references/paths。
- `/api/invite` 的 Gemini structured output 已有無頭測試,但仍待使用正式金鑰做一次 live smoke test。
- `conflict`／`drama`／`relist`／`rent`／`move`／`qol` 六支機率型測試已於 2026-07-25
  改用固定 mulberry32 種子並驗證輸出可重現；`run-all.ts` 的失敗重試仍保留，供其他尚未
  明確去隨機化的測試當安全網。新增機率型測試仍須在匯入 store 前固定亂數來源。
- 遊戲的「一天」與作息綁**瀏覽器本地時區**(設計基準 UTC+8),不同時區玩家看到的作息時鐘不同;
  跑測試需在 UTC+8 或 `TZ=Asia/Taipei`。改成固定基準時區屬較大改動,尚未評估。
- 開發機瀏覽器自動化工具不穩,靠無頭測試 + 編譯檢查 + 線上 curl 驗證。

### 共用區舒適度的四條軟性限制(2026-07-30 對帳:從日期紀錄內文提升為正式條目)

> 前三條原本只寫在 `:3095-3102`(2026-07-30「共用區舒適度延伸」的「已知限制/風險」)內文裡,
> 第四條在 `:3165`(同日 review 修正批的新留待辦)。四條都是**刻意的設計取捨、不擋上線**,
> 列出來是為了讓下一位不用重新推導。(⚠️ 行號會漂移,認**日期紀錄標題**比較可靠。)

- [ ] 🟢 **共用區分數是全樓共享的單一值**——四位租客的舒適度面板顯示同一個數字。
      語意如此(它描述整棟樓的公共空間),**非 bug**;若日後要做「每人感受不同的公共空間」
      才需要動。
      🔍 驗收:`rg -n "communalQuality\(" src/` 出現吃 tenantId 的多載 → 已改成分人
- [ ] 🟢 **三區的 `attrPart` 天花板 50 實務上很難逼近**——共用區家具 attributes 普遍偏低、
      洗衣間五件全空,主要升分空間在 categoryPart 與 tierPart。這是刻意的(共用區不該靠
      堆療癒家具刷分),列在此僅供日後調整手感時參考。
      🔍 驗收:`rg -n "COMMUNAL_ATTR_MAX = " src/sim/comfort.ts` 不再是 `50` → 已重新平衡
- [ ] 🟢 **`upgradeAttributes` 理論上會進共用區的 `attrPart`**——房間改建升級會經
      `roomAttributes()` 流進共用區,但現況改建只賣給 r301~r304,**實務上不會發生**。
      日後若開放共用區改建,要先確認這條路徑的預期行為。
      🔍 驗收:`rg -n "upgradeAttributes" src/` 出現共用區 id 的守門或明確允許 → 已處理
- [ ] 🟢 **`communalBaselineDelta` 的 wellbeing 係數 0.05 實質不可感知**——它只把
      wbAnchor(65)上下挪約 1 點,而 wellbeing 的回歸只有 1%/h,玩家幾乎感覺不到。
      日後要加強共用區手感,**這是第一個該動的旋鈕**(mood 0.10 / stress 0.06 相對有感);
      調整時共用區影響力應維持低於私人房(0.16 / 0.10 / 0.08)。
      🔍 驗收:`rg -n "0\.05" src/sim/comfort.ts` 在 `communalBaselineDelta` 內已改值 → 已完成

### 月度事件鏈／夢境彩蛋的五條小殘留(🟢,都不擋上線)

> 這五條原本**只**存在於 `docs/交接與待辦.md` 第七節末的 J 段,主工作日誌沒有對應條目——
> 而交接文件自己宣告權威清單在本檔第四、五節。2026-07-30 對帳時一併補齊,兩份才真的一致。

- [ ] 🟢 月度事件鏈:章節卡與滿版 feed(事件中心 + 週報 + 大量時間軸列)並存時的**堆疊間距未驗**
      (三個 section 同層 block,風險低)。
      🔍 驗收:長期項,需人工目檢(跑 `npm run ui:shot -- rent` 並注入含章節卡的存檔);
      做完請在此條註明驗證日期與截圖結論
- [ ] 🟢 月度事件鏈:**離線太久觸發收束時,若被跳過的正好是抉擇那一話,該次抉擇不會補彈**
      (章節卡會標示被跳過)。
      🔍 驗收:`rg -n "pendingChainEvent" src/sim/floorChain.ts` 出現離線補彈/queue 邏輯 → 已完成
- [x] 🟢 月度事件鏈:「全員擠交誼廳」的**多人站位演出** ✅ **已完成(2026-07-31;Codex `eb8305f`)**。
      ⚠️ **2026-08-01 對帳更正內文**:本條原本寫「目前只有文字 + 交誼廳特效,待演出層支援
      多人 session 後再補(`startPairSession` 仍是 pair-only)」——**這段敘述現在是錯的**,
      演出層已經支援多人。實作:新檔 `src/floor/groupScene.ts`(159 行)的
      `startGroupScene()` / `groupSceneFor()` / `groupSceneView()`,`loungeTiles()` 依家具
      阻擋狀態挑出不重疊的固定站位(全程決定性,不耗 `Math.random`);
      `src/sim/floorChain.ts:56` 新增 `scene?: { layout: GroupSceneLayout }` 且
      `:98`(cluster)/`:163`/`:186`(storm)三個 stage 已接上;
      `src/floor/agents.ts:98` 的 `groupSceneFor()` **優先於** pair session 覆寫走位。
      🔍 驗收:`rg -n "startGroupScene|groupSceneFor" src/floor/groupScene.ts src/floor/agents.ts src/sim/floorChain.ts`
      有命中 → 已完成(**2026-08-01 錨點改寫**:原錨點猜 `startGroupSession|startMultiSession`,
      實作是 `startGroupScene`,舊錨點 0 命中會誤判未完成;依錨點規則 1 改寫為真實符號)
- [ ] 🟢 夢境彩蛋:租客搬走後 **`dream|<id>` 冷卻鍵殘留存檔**(無功能影響,鍵不會再被讀到);
      清理點在 `src/sim/social.ts:485` 的 `removeTenantRelations()` 一帶
      (⚠️ **2026-08-01 對帳更正行號**:原寫 `:368`,實際 `removeTenantRelations` 在 `:485`;
      `:368` 附近是 `encounter()` 的衝突判定,與本條無關。鍵的前綴定義在
      `src/sim/dreams.ts:55` 的 `DREAM_CD_PREFIX = "dream|"`),
      當時 lease 禁改該檔故留待順手處理。
      🔍 驗收:`rg -n "dream\|" src/sim/social.ts` 有命中 → 已完成
- [ ] 🟢 夢境彩蛋:**未跑 `ui:shot`**(未新增 UI 元件);若要目檢需比照 floor-chain 用注入存檔
      跳到第 14 遊戲日以後(`DREAM_FIRST_DAY = 14`)。
      🔍 驗收:長期項,需人工目檢;做完請在此條註明驗證日期

### 跨專案(工作區根,不屬本專案 lease)

- [ ] 🟡 **還原 `.ui-lab/compose.yaml` 的 NBA 8000 埠映射**——2026-07-16 因本機 Windows
      保留埠範圍(7912-8011)吃掉 8000,該行被**暫時註解**掉(見封存檔 2026-07-16「豐富批」
      的 Mobile UI Lab 環境註記)。本檔與 `docs/交接與待辦.md` 都只寫「跑 NBA 互動檢查前要還原」,
      **但一直沒有任何待辦追蹤這個還原狀態**,2026-07-30 對帳時補上本條。
      rent 的截圖流程不受影響,**只有 NBA 的互動檢查會踩到**。兩條路:還原該行,或先用
      `netsh int ipv4 set dynamicport tcp start=49152 num=16384` 重設 winnat 保留範圍再還原。
      ⚠️ **`.ui-lab/compose.yaml` 在工作區根、屬於別的 lease、且目前有未提交變更**——
      要改請先確認 lease 與 dirty 狀態,不要順手覆蓋別人的改動。
      🔍 驗收:工作區根 `rg -n "8000:8000" .ui-lab/compose.yaml` 命中且**未被 `#` 註解** → 已還原

### 活動椅造型用幾何啟發式反查家具(2026-07-31 人物美術第三批留下)

- [ ] 🟡 **活動椅造型是「猜」出來的,而上游其實明確知道答案**——
      `floorScene.ts` 的 `computeChairStyle()` 靠「互動格吻合 + Chebyshev ≤ 1 最近」
      去反推角色坐的是哪一件家具,但 `tick.ts:439-441` 當下**手上就有 `tgt.placement`**,
      連 `usedDefId` 都已經被 hoist 出來(睡眠效率那批留下的)。當初走幾何路線是為了守
      本批 lease 的「零 `agents.ts` 改動」硬性要求(Codex 正在 `agents.ts:98-121` 活躍施工),
      取捨正確,但這是本批唯一的架構性缺陷。
      **修法**:等 Codex 的 `agents.ts` 活動告一段落後,改成由 `tick.ts` 在
      `setFurniturePose()` 內把 `p.defId`(或其 sprite kind)存進 `rt.activitySurfaceDefId`,
      經 `agents.ts` 直傳給 renderer,**用事實取代幾何猜測**。順帶一次消掉兩個現存限制:
      (a) **跨房/跨牆誤吸**——啟發式完全不看牆,玩家靠牆擺 chair-kind 家具時可能吸到
      隔壁房的桌子(半徑收到 1 後風險大降但未消失);
      (b) **每幀全表掃描**——目前靠 `placements.version` memo 壓下來,直傳後連 memo 都不需要。
      🔍 驗收:`rg -n "activitySurfaceDefId|seatKind" src/sim/tick.ts src/floor/agents.ts src/floor/floorScene.ts`
      有命中,且 `floorScene.ts` 不再有 `CHAIR_LOOKUP_RADIUS` → 已完成

### `stand_face` 垂直相鄰時朝向不精確(2026-08-01 四方向人物留下)

- [ ] 🟢 **面對面但上下相鄰的兩人,不保證真的面向對方**——
      `pairSession.ts:79-82` 的 `facingToward()` 只在**水平相鄰**時給 `±1`
      (`mine.r !== other.r` 就回 `0`),垂直排列一律 `0`。四方向這批之後,
      `facing === 0` 會落到 `agentView()` 的規則 3「沿用最後走過的方向」:
      從下方走上來剛好是 `back`(正對樓上的同伴,**比舊版好**),但從左側繞過來站定
      就會渲染成 `side_r`,看起來像在看別的地方。
      **這不是退化**——改動前是一律正面,同樣沒面向對方,只是換了一種不精確;
      而且渲染仍是決定性的。
      **修法**:`sessionFor()` 已經同時握有 `tileA` 與 `tileB`,把**對方的 tile**
      (或已算好的四方向 `dir`)一起回傳,`agents.ts` 原樣帶到 `Agent` 上,
      renderer 的規則 1 就能同時處理水平與垂直,不必再靠 `facing: -1|0|1` 這個
      只表達得了左右的型別。⚠️ 這需要動 `agents.ts` 與 `pairSession.ts`,
      本批 lease 明文禁止,故留待下批。
      🔍 驗收:`rg -n "facingToward" src/floor/pairSession.ts` 的實作**不再有**
      `mine.r !== other.r` 的早退,且 `appearance-test.ts` 有一條「垂直相鄰的
      `stand_face` 會朝向對方(上/下)」的斷言 → 已完成

### Codex 三批留下的漏記待辦(2026-08-01 對帳:從日期紀錄內文提升為正式條目)

> 這五條原本只寫在 2026-07-30「樓寵物中途送養」、2026-07-31「群體劇情與樓層畫面同步」、
> 2026-07-31「房客相性積怨與畢業紀念物收起」三則日期紀錄的**內文**(「已知範圍」/「驗證結果」段)裡,
> 第四、五節完全沒有對應條目,機械掃描看不到。(⚠️ 行號會漂移,認**日期紀錄標題**比較可靠。)

- [ ] 🟢 **每日 2 場自然社交／打鬥上限「不含」劇本排程與群體事件**——
      `src/sim/tick.ts:669-687` 的 `conflictsToday`(存進 `state.interactionCooldowns`
      的固定鍵)只節流**自然**相遇:`tryFight()` 與 `encounter({ allowConflict })`。
      `communityPass()` 的群體事件與 `floorChain` 的劇本排程**完全不經過這個計數器**,
      所以「全樓每天最多 2 場衝突」的體感承諾在事件密集的日子會被突破。
      目前是刻意的(劇本事件應該保證發生),列出來是為了讓下一位不用重新推導。
      🔍 驗收:`rg -n "conflictsToday" src/sim/tick.ts src/sim/community.ts src/sim/floorChain.ts`
      在 `community.ts` 或 `floorChain.ts` 也有命中 → 已納入統一節流
- [ ] 🟡 **永久隔音可擋噪音公審,但尚未降低自然社交的噪音相性口角率**——
      `src/sim/upgrades.ts:64` 的 `EVENT_SOUNDPROOFING_ID` 經
      `src/sim/acoustics.ts:38` 的 `noiseComplaintEligible()` 擋掉 `community.ts:602`
      的 `noise_tribunal` 群體事件,**但 `social.ts` 的 `encounter()` 完全不看隔音**——
      `compatibilityDetail()` 算出的噪音負相性照舊推高口角機率。
      玩家花 $3,000 做了隔音卻仍為噪音吵架,會讀成「錢白花了」。
      🔍 驗收(**兩段式**:確認 `encounter()` 本體真的讀到隔音,而不是只在別處 import):
      `rg -A25 "export function encounter" src/sim/social.ts | rg "soundproof|acoustics|roomAcoustics"`
      有命中 → 已完成
- [ ] 🟢 **寵物面板展開後未補拍人工點擊視覺檢查**——2026-07-30 樓寵物退場批的
      `ui:shot` 18 張 0 errors 只涵蓋收合狀態,展開後的 🐾 面板(送養／取消媒合／歷史)
      當時因無可用互動瀏覽器而未拍。這是該批**唯一未完成的視覺人工檢查**。
      🔍 驗收:長期項,需人工目檢(Lab 容器內用自訂 Playwright 流程點開 🐾 面板截圖);
      做完請在此條註明驗證日期與截圖結論
- [ ] 🟢 **租客張力批的低頻彈窗未補拍**——2026-07-31 相性積怨批的
      **招租相性風險提示**與**紀念物收起的二次確認**兩個低頻彈窗未補拍,
      當時只由型別、正式 build、存檔往返與重疊移除測試覆蓋。
      🔍 驗收:長期項,需人工目檢(同上,需構造招租中且有現住戶／房內有紀念物的存檔);
      做完請在此條註明驗證日期與截圖結論
- [ ] 🟢 **天台場景是「樓層內夕景小舞台」而非真實地圖區域**——
      `src/floor/groupScene.ts:17` 的 `GroupSceneVenue = "lounge" | "rooftop"`,
      但 `rooftop` 在 `src/floor/map.ts` 裡**沒有對應的地圖區域**:`:153` 是靠
      `hidden: active.venue === "rooftop"` 隱藏參與者在主平面的重複 sprite,
      再另畫一個小舞台。現況正確且決定性,**日後若真的加天台地圖區域需回頭處理這條**
      (否則會同時存在真實天台與假舞台兩套)。
      🔍 驗收:`rg -n "rooftop" src/floor/map.ts` 有命中(天台成為真實地圖區域)→ 已處理

### 睡眠效率批與四方向人物批留下的漏記待辦(2026-08-01 對帳:從日期紀錄內文提升為正式條目)

> 這四條原本只寫在 2026-07-30「睡眠效率 review 收尾」與 2026-08-01「四方向人物」兩批的
> 日期紀錄**內文**(「必修 2」/「我沒做什麼」/「已知限制」段)裡。
> ⚠️ 其中 `usedDefId` 那條是**有實際靜默錯誤風險**的一條,不是純美術債。
> (家具 tier 第三階段本身已於同日獨立成**第四節**的一條 `- [ ]`。)

- [ ] 🟡 **`usedDefId` 的賦值位置是第三階段的地雷,但沒有任何待辦或測試追蹤它**——
      現況**正確**:`src/sim/tick.ts:390` hoist、`:440` 在「目標格 = 家具互動格」的判斷內賦值、
      `:475` 以 `furnitureDefId` 傳出,該處也留了警語註解。
      風險在於**這是個只靠註解守住的位置**:賦值一旦被改回浴室排隊分支之後,
      被擠成 `waiting_for_bathroom`(沒真的用到設備)的租客就會帶著那件家具的 id 出去,
      第三階段接上浴缸時**排隊中的人會靜默吃到 premium 浴缸的乘數**。
      現在無害純粹是因為只有睡眠吃乘數、而睡眠不排隊。
      **要做的是加一條釘住位置的斷言**(例如構造一個排隊中的租客,斷言其
      `furnitureDefId` 為 `undefined`),讓改動當下就紅燈,而不是等第三階段才發現。
      🔍 驗收(**兩段式**):`rg -n "waiting_for_bathroom" -A8 scripts/ | rg "furnitureDefId"`
      有命中 → 已完成
      ⚠️ **本條錨點在 2026-08-01 當場被自己的規則 2 抓到並改寫過一次**:初稿寫的是單行
      `rg -n "usedDefId|furnitureDefId" scripts/`,實跑**命中**
      `scripts/furniture-tier-test.ts:266` 的 `function deltasOf(st, furnitureDefId?)`
      ——那只是呼叫 `generateHourly` 的測試輔助參數,**完全沒有斷言排隊者不吃乘數**,
      是不折不扣的偽陽性。改成綁定 `waiting_for_bathroom` 情境的兩段式後實跑 0 命中。
- [ ] 🟡 **四方向只覆蓋「站」與「走」——坐、躺、`cook_pair` 一律正面**——
      `src/floor/floorScene.ts:360` 的 `agentView()` 對 `cook_pair` 直接 `return "front"`
      (刻意:`drawCookingCue()` 是照正面基底畫的抬手 + 鍋鏟,套到背面/側面會錯位);
      `:389-395` 的 `pose === "sit"` 分支在推導 view **之前**就 `return`,一律畫 `CHAR_SIT`;
      躺姿 `:385` 的 `drawLying()` 同理。
      ⚠️ **坐姿要接需與活動椅那批的 `seatBack` 同批做**——`CHAR_SIT_BACK` 目前由
      `src/pixel/scene.ts` 的房間鏡頭自己決定,兩邊各自推方向會打架。
      🔍 驗收(**兩段式**:確認坐姿分支本身吃到視角,而不是檔案裡別處有 `agentView`):
      `rg -A6 'a.pose === "sit"' src/floor/floorScene.ts | rg "CHAR_SIT_BACK|agentView|CharView"`
      有命中 → 已完成
- [ ] 🟢 **`lastFacingDir` Map 單調成長**——`src/floor/floorScene.ts:338` 的
      `new Map<string, CharView>()` 以 tenantId 為鍵,而 tenantId 是帶時間戳的唯一值
      (如 `tenant_r303_1785546924760_0`),**退租時 key 不會被移除**,
      所以上限是「本次 session 曾出現過的所有租客數」而非在住租客數。
      ⚠️ **2026-08-01 review 已決議不改程式**(每筆一個短字串鍵 + 一個列舉值,
      幾百筆也在數十 KB 等級,且 session 結束就整份消失),只把註解改成正確描述。
      本條追蹤的是「若日後要清」的掛點:`clearFacingMemory()` 已 export
      (`floorScene.ts:341`),最自然的呼叫點是 `createAgents()`,**但那需要動 `agents.ts`**。
      🔍 驗收:`rg -n "clearFacingMemory" src/floor/agents.ts` 有命中 → 已接上清理掛點
- [ ] 🟢 **背面的長髮／馬尾會蓋住上背,走路時手臂擺動較不明顯**——
      `src/pixel/parts.ts` 的 back 疊層裡 `long` 與 `ponytail` 覆蓋範圍較大,
      壓過基底 sprite 走 A/走 B 兩幀在上背的差異。純觀感項、不影響辨識度
      (背面五髮型兩兩最少換色 10px,已有 ≥8px 斷言護住)。
      🔍 驗收:長期項,需人工目檢(重產四方向 × 五髮型 × 站/走A/走B 對照表 PNG,
      確認 back 的 `long`/`ponytail` 兩幀擺臂可辨);做完請在此條註明驗證日期

---

## 六、如何執行 / 驗證
```bash
npm run dev                      # 本地開發(http://localhost:5173)
npx tsx scripts/sim-trace.ts 24  # 無頭:走位健全性
npx tsx scripts/part-test.ts     # 跨租客事件 + 收支 + 時間 7×
npx tsx scripts/qol-test.ts      # QoL:擺放預覽判定 + 分批快轉 + 存檔匯出入/重載還原
npx tsx scripts/move-test.ts     # 家具移動(跨房/失敗留原位)
npx tsx scripts/rent-test.ts     # 調租談判(降租/容忍/拒絕/冷卻/同居者擋下)
npx tsx scripts/stats-model-test.ts  # 數值模型(homeostasis/生病/精力)
npx tsx scripts/balance-test.ts      # balance 快照 diff(刻意調平衡後 --update)
npx tsx scripts/variety-test.ts  # 文本庫變化 + 時段替換
npx tsx scripts/feed-test.ts     # 動態 Feed 彙整/未讀/存檔往返
npx tsx scripts/arc-test.ts      # 劇情弧消毒/開推收全流程/伏筆 flag
npm run build                    # 產出 dist/
npx wrangler deploy --dry-run    # 檢查 Worker 打包
# 部署:git push（Cloudflare 自動 build+deploy）
```

## 附:AI 供應商設定
- 執行時日記 AI = **Gemini 免費層 → Cloudflare Workers AI(Qwen3)免費額度 → 內建模板**。平淡日常優先用 `gemini-3.1-flash-lite`,事件機會或劇情弧等重要日用具免費層的 `gemini-3-flash-preview`。
- Gemini key 設為 Worker Secret `GEMINI_API_KEY`;Workers AI 使用 `wrangler.toml` 的 `AI` binding,不需要另放 key。日記路徑不會自動使用可能計費的 Claude。
- Fable(claude-fable-5)僅用於**製作階段**一次性生成文本庫(走 Claude 訂閱,非執行時)。

---

## 七、進度補記(逐日日期紀錄,2026-07-25 起;更早見封存檔)

> 7/12～7/13 的詳細施工紀錄原先只寫在 `docs/交接與待辦.md`;本節將已上線的實際進度補回主工作日誌。後續以本檔作為 Codex 與 Claude Code 的共同更新紀錄。
>
> 📦 **2026-07-12 ～ 2026-07-24 的紀錄已逐字搬到 [`docs/工作日誌-封存.md`](docs/工作日誌-封存.md)**
> (未摘要、未刪改)。本節保留 **2026-07-25 起**的紀錄。

<details>
<summary><b>日期紀錄索引(2026-07-25 起;含檔尾追加點之後的新紀錄)(點開)</b></summary>

- [2026-07-25 — 文件同步:待辦清單與實際程式碼對帳(Claude Code)](#2026-07-25--文件同步待辦清單與實際程式碼對帳claude-code)
- [2026-07-25 — 房間細看:舒適度拆解面板(Claude Code)](#2026-07-25--房間細看舒適度拆解面板claude-code)
- [2026-07-25 — 掃地機器人主動讓路(純視覺層,確定性)(Claude Code)](#2026-07-25--掃地機器人主動讓路純視覺層確定性claude-code)
- [2026-07-25 — `worker/` 納入型別檢查(新增 tsconfig.worker.json)(Claude Code)](#2026-07-25--worker-納入型別檢查新增-tsconfigworkerjsonclaude-code)
- [2026-07-25 — 機率型測試去隨機化(六支改固定種子)(Claude Code)](#2026-07-25--機率型測試去隨機化六支改固定種子claude-code)
- [2026-07-25 — 舒適度面板三條長條對齊同一軌道(Claude Code)](#2026-07-25--舒適度面板三條長條對齊同一軌道claude-code)
- [2026-07-27 — 文件現況對帳與過期待辦結案(Codex)](#2026-07-27--文件現況對帳與過期待辦結案codex)
- [2026-07-27 — 修正模範房客安居倒數永遠停在 20 天(Codex)](#2026-07-27--修正模範房客安居倒數永遠停在-20-天codex)
- [2026-07-27 — 安居倒數改以午夜換日並還原真正完成日(Codex)](#2026-07-27--安居倒數改以午夜換日並還原真正完成日codex)
- [2026-07-27 — 修復舊檔到期後「0 天無數字、仍等下一午夜」(Codex)](#2026-07-27--修復舊檔到期後0-天無數字仍等下一午夜codex)
- [2026-07-28 — 修復最早期舊檔「顯示 0 天但永遠不進搬離名單」(Codex)](#2026-07-28--修復最早期舊檔顯示-0-天但永遠不進搬離名單codex)
- [2026-07-28 — 房客七項數值與舒適度整合為「居住體驗」(Codex)](#2026-07-28--房客七項數值與舒適度整合為居住體驗codex)
- [2026-07-28 — 月度全樓事件鏈:3 條鏈 × 4 階段的章節感(Claude Code)](#2026-07-28--月度全樓事件鏈3-條鏈--4-階段的章節感claude-code)
- [2026-07-28 — 補記月度全樓事件鏈的 UI 驗證與部署結果(Claude Code)](#2026-07-28--補記月度全樓事件鏈的-ui-驗證與部署結果claude-code)
- [2026-07-29 — 夢境彩蛋:睡覺時段的零成本敘事彩蛋(Claude Code)](#2026-07-29--夢境彩蛋睡覺時段的零成本敘事彩蛋claude-code)
- [2026-07-29 — 文件同步:`docs/交接與待辦.md` 對齊 HEAD `2f1be4e`(Claude Code)](#2026-07-29--文件同步docs交接與待辦md-對齊-head-2f1be4eclaude-code)
- [2026-07-29 — 樓外世界錨點:外出目的地文本 + 樓外巧遇(Claude Code)](#2026-07-29--樓外世界錨點外出目的地文本--樓外巧遇claude-code)
- [2026-07-29 — 樓外巧遇 review 後修正:同地點條件與每日全域鎖(Claude Code)](#2026-07-29--樓外巧遇-review-後修正同地點條件與每日全域鎖claude-code)
- [2026-07-29 — 家具品質 tier 接上行為差異:舒適度第三個加項(Claude Code)](#2026-07-29--家具品質-tier-接上行為差異舒適度第三個加項claude-code)
- [2026-07-29 — 家具 tier review 後修正:上限收到 100 與「有分就有標示」(Claude Code)](#2026-07-29--家具-tier-review-後修正上限收到-100-與有分就有標示claude-code)
- [2026-07-29 — 家具 tier 第三批:畢業生紀念物從商店下架(Claude Code)](#2026-07-29--家具-tier-第三批畢業生紀念物從商店下架claude-code)
- [2026-07-29 — 電視／椅子像素美術第一批：修掉壁爐錯圖並建立七件專屬造型(Codex)](#2026-07-29--電視椅子像素美術第一批修掉壁爐錯圖並建立七件專屬造型codex)
- [2026-07-30 — 人物美術第一批：共用骨架、臉部、髮型配件與躺姿身份（Codex）](#2026-07-30--人物美術第一批共用骨架臉部髮型配件與躺姿身份codex)
- [2026-07-30 — 共用區舒適度延伸:交誼廳/浴室/洗衣間影響全體租客(Claude Code)](#2026-07-30--共用區舒適度延伸交誼廳浴室洗衣間影響全體租客claude-code)
- [2026-07-30 — 共用區舒適度 review 後修正:不變量收緊、面板分母、效能與警語(Claude Code)](#2026-07-30--共用區舒適度-review-後修正不變量收緊面板分母效能與警語claude-code)
- [2026-07-30 — 文件對帳:清孤兒條目、收散落待辦、加驗收錨點制(Claude Code)](#2026-07-30--文件對帳清孤兒條目收散落待辦加驗收錨點制claude-code)
- [2026-07-30 — AI 色碼對比安全化:亮度帶夾值、膚色白名單、髮膚分離、三道防線(Claude Code)](#2026-07-30--ai-色碼對比安全化亮度帶夾值膚色白名單髮膚分離三道防線claude-code)
- [2026-07-30 — AI 色碼安全化 review 後修正:膚色池產品決定、格式回退補三道等強、ΔL 影響面釘死(Claude Code)](#2026-07-30--ai-色碼安全化-review-後修正膚色池產品決定格式回退補三道等強δl-影響面釘死claude-code)
- [2026-07-30 — 睡眠效率:床的 tier 放大恢復效率(家具 tier 第二階段;Claude Code)](#2026-07-30--睡眠效率床的-tier-放大恢復效率家具-tier-第二階段claude-code)
- [2026-07-30 — 睡眠效率 review 收尾:係數下修、排隊分支地雷、死碼與凍結(Claude Code)](#2026-07-30--睡眠效率-review-收尾係數下修排隊分支地雷死碼與凍結claude-code)
- [2026-07-30 — 樓寵物中途送養與「幸福新家」退場機制（Codex）](#2026-07-30--樓寵物中途送養與幸福新家退場機制codex)
- [2026-07-31 — 群體劇情與樓層畫面同步（Codex）](#2026-07-31--群體劇情與樓層畫面同步codex)
- [2026-07-31 — 房客相性積怨與畢業紀念物收起（Codex）](#2026-07-31--房客相性積怨與畢業紀念物收起codex)
- [2026-07-31 — 工作日誌可導覽化:加目錄、封存 07-24 以前紀錄、設追加點(Claude Code)](#2026-07-31--工作日誌可導覽化加目錄封存-07-24-以前紀錄設追加點claude-code)
- [2026-07-31 — 人物美術第三批:活動椅依家具種類分造型(Claude Code)](#2026-07-31--人物美術第三批活動椅依家具種類分造型claude-code)
- [2026-07-31 — atlas 清理:移除 5 筆已被程序美術取代的死 crop(Claude Code)](#2026-07-31--atlas-清理移除-5-筆已被程序美術取代的死-cropclaude-code)
- [2026-07-31 — 活動椅 review 收尾:半徑收窄至 1、加版本號 memo、補技術債(Claude Code)](#2026-07-31--活動椅-review-收尾半徑收窄至-1加版本號-memo補技術債claude-code)
- [2026-08-01 — 四方向人物:背面/側面基底 sprite、疊層與 renderer 內方向推導(Claude Code)](#2026-08-01--四方向人物背面側面基底-sprite疊層與-renderer-內方向推導claude-code)
- [2026-08-01 — 四方向人物 review 收尾:側面馬尾拉開辨識度、鏡射軸守衛、註解修正(Claude Code)](#2026-08-01--四方向人物-review-收尾側面馬尾拉開辨識度鏡射軸守衛註解修正claude-code)
- [2026-08-01 — 文件對帳:清兩個孤兒、修三個壞錨點、補 10 條漏記、錨點制加三規則(Claude Code)](#2026-08-01--文件對帳清兩個孤兒修三個壞錨點補-10-條漏記錨點制加三規則claude-code)
- [2026-08-01 — 一樓寵物咖啡廳設計草案:畫進樓層地圖、常備訂單研發、租客維持三樓(Claude Code)](#2026-08-01--一樓寵物咖啡廳設計草案畫進樓層地圖常備訂單研發租客維持三樓claude-code)
- [2026-08-01 — 一樓寵物咖啡廳工作分解:22 個可認養工作項、三條平行主幹、CAFE-03 唯一瓶頸(Claude Code)](#2026-08-01--一樓寵物咖啡廳工作分解22-個可認養工作項三條平行主幹cafe-03-唯一瓶頸claude-code)
- [2026-08-01 — CAFE-01 FloorViewport 抽取:任意矩形鏡頭、房間特寫逐位元不變(Codex)](#2026-08-01--cafe-01-floorviewport-抽取任意矩形鏡頭房間特寫逐位元不變codex)
- [2026-08-01 — CAFE-02 樓層切換器:固定 3F/1F 機位、空白一樓驗收(Codex)](#2026-08-01--cafe-02-樓層切換器固定-3f1f-機位空白一樓驗收codex)
- [2026-08-01 — CAFE-03 地圖擴充:52 rows、六個 Region、跨樓層尋路(Codex)](#2026-08-01--cafe-03-地圖擴充52-rows六個-region跨樓層尋路codex)
- [2026-08-01 — CAFE-04 樓梯與樓板分隔帶美術:LimeZu 木階、離線 fallback、兩層清楚斷面(Codex)](#2026-08-01--cafe-04-樓梯與樓板分隔帶美術limezu-木階離線-fallback兩層清楚斷面codex)
- [2026-08-01 — CAFE-02 UX 修訂:雙分頁改為獨立樓層頁面與單一目的地按鈕(Codex)](#2026-08-01--cafe-02-ux-修訂雙分頁改為獨立樓層頁面與單一目的地按鈕codex)
- [2026-08-01 — CAFE-05 咖啡廳分區底色與獨立 LimeZu atlas(Codex)](#2026-08-01--cafe-05-咖啡廳分區底色與獨立-limezu-atlascodex)
- [2026-08-01 — CAFE-06 咖啡廳家具 catalog、atlas runtime 與公開上線閘門(Codex)](#2026-08-01--cafe-06-咖啡廳家具-catalogatlas-runtime-與公開上線閘門codex)
- [2026-08-01 — CAFE-07 獨立顧客資料層：姓名池、決定性外觀與純函式離場(Codex)](#2026-08-01--cafe-07-獨立顧客資料層姓名池決定性外觀與純函式離場codex)
- [2026-08-01 — CAFE-08 顧客進出走位與獨立人物繪製(Codex)](#2026-08-01--cafe-08-顧客進出走位與獨立人物繪製codex)
- [2026-08-01 — CAFE-09 認養意圖接入幸福新家(Codex)](#2026-08-01--cafe-09-認養意圖接入幸福新家codex)
- [2026-08-01 — CAFE-10 三種顧客意圖泡泡與 atlas fallback(Codex)](#2026-08-01--cafe-10-三種顧客意圖泡泡與-atlas-fallbackcodex)

</details>

### 2026-07-25 — 文件同步:待辦清單與實際程式碼對帳(Claude Code)

- **目的／需求**:兩份文件的「待辦清單」落後實際程式碼約一個月,一批早就上線的項目仍掛在待辦,
  導致每次接手都得重新考古。本次只做**文件同步**,把清單校正成可信狀態。
- **實際變更(僅 2 個檔案,未動任何程式碼)**:
    - `工作日誌.md`:標頭更新日期 07-18 → 07-25;第四節待辦與第五節技術債各加「以日期紀錄為
      最終事實來源」的可信度提示;「AI 每日微調數值」與「姿勢細分 cook_pair/stand_face」改標 ✅
      並註明完成日期與程式位置;新增「尚未動工的內容擴充」彙整(月度事件鏈/夢境彩蛋/樓外世界
      錨點/共用區舒適度/家具 tier 行為差異/掃地機主動讓路/AI context 預算化);技術債的
      「尚未加入 vue-tsc」改寫為**精確描述**(vue-tsc 已導入,但 `worker/` 未納入 `include`,
      此點仍為待辦),並補記機率型測試仍用真實 `Math.random` 與遊戲日綁本地時區兩項;
      2026-07-24 條目的 balance-snapshot 不一致就地標註**已於同日下一則結案**(不再是 open issue)。
    - `docs/交接與待辦.md`:標頭「更新至 2026-07-16」→「2026-07-25」(檔內已有 07-23 段落);
      第五節待辦區加可信度提示;A 批四項(應徵者自帶寵物/貓咪家具/多貓互動/房客卡顯示寵物)、
      B 批兩項(cook_pair・stand_face 姿勢、摔門絕交演出)、E-2(index.html 門面)、
      G-10(週報卡)、G-11(貓咪視角彩蛋)就地標 ✅ + 完成日期 + 程式依據;C-7 拆成
      「群體事件已完成 / 三角關係仍為 🔴 未做」;第七節末「建議的接手順序」改寫為對帳後的實際
      剩餘項;第九、十、十四節三處過時的「剩餘建議序」加上補註指向現行清單。
- **保留原則**:未完成項目一律保留(E-3 PWA、I-3 rate limit、`/api/invite` live smoke test、
  C-9 context 預算化、`worker/` 未納 typecheck、測試真實亂數、共用區舒適度、掃地機讓路、
  家具 tier 行為差異、月度事件鏈、夢境彩蛋、樓外世界錨點、遊戲日時區,以及全部 🔴 大題目);
  已完成項目**不刪除**,原編號(A-1/C-7/E-2/G-10…)與 🟢🟡🔴 標記慣例全部沿用,避免打斷跨文件引用。
- **驗證**:逐項用 Grep 複驗程式碼命中位置後才標 ✅——`src/sim/observationEffects.ts`
  `NUDGE_LIMITS`(mood/stress/energy/wellbeing 3、affinity 2)、`package.json` 的
  `typecheck: vue-tsc --noEmit` 與 `tsconfig.json` 的 `include` 僅 `src/**`+`vite.config.ts`、
  `src/floor/pairSession.ts` 的 `PairPose` 與 `floorScene.ts` 341-342、`src/sim/conflicts.ts`
  `playDoorSlamExit` + `fx.ts` 的 `slam`、`src/sim/recruit.ts` 303 應徵者帶寵物、
  `src/sim/pets.ts` 170 `litter_box`／256・293 雙貓互動／481 `catJournalPass`、
  `src/App.vue` 544 房間細看寵物列、`index.html` title/favicon/og、`src/sim/weeklyReport.ts`
  + `FeedPanel.vue` 週報卡。**8 項全部確認已完成,無誤刪**。
  `git status --short` 僅這兩個 .md;因未動程式碼,不需跑測試或 build(最近一次基準:
  2026-07-25 U-4 的 `npm test` 60/60、`npm run typecheck` 乾淨)。
- **已知限制／後續**:文件清單仍是人工維護,未來仍可能落後;因此本次在兩份文件都寫明
  「以檔案尾端的日期紀錄為最終事實來源」,讓下一位接手者知道該查哪裡。
- **硬規則**:**未動任何程式碼**(`src/`、`worker/`、`scripts/`、`package.json`、設定檔全未觸碰),
  未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒等硬規則全數保留。

### 2026-07-25 — 房間細看:舒適度拆解面板(Claude Code)

- **目的／需求**:舒適度做完兩波機制後,房間細看只有一條 0-100 總分長條 + 最多三句改善提示,
  玩家看不出分數是被**家具屬性**、**種類齊全度**還是**整潔倍率**拖住,無法有意識地佈置。
  慢變數系統最需要可讀性,因此把 `roomComfortBreakdown()` 已經算好的細項攤開顯示。
  本次為**純呈現**改動,不動任何舒適度數學。
- **實際變更(3 個檔案)**:
    - `src/sim/comfort.ts`:僅**新增兩個純讀取匯出**供 UI/測試使用,未改任何既有係數、公式或
      `roomComfortBreakdown` 回傳語意——`COMFORT_LIMITS`(`attrMax` = `ATTR_MAX` 60、
      `categoryMax` = 類別數 × `CATEGORY_POINTS` = 30、`categoryPoints` = 6)與
      `COMFORT_BUCKET_LABELS`(五大類顯示名稱,順序同 `missing` 判定順序)。
    - `src/App.vue`:`roomComfort` 改用 `roomComfortBreakdown`(`comfortBd` computed,總分仍為
      `Math.round(comfort)`,顯示值不變);新增 `comfortParts` computed 只做四捨五入與百分比換算;
      `.comfort` 區塊在總分列下方加拆解區:①「家具屬性」細長條 + `35/60`、②「種類齊全」細長條 +
      `24/30`、③五顆類別膠囊(`✓睡眠`/`✗生活`,缺的走紅色系)、④「🧹 整潔倍率 ×0.78」+ 右側
      「小計 59 → 46」(未打折時顯示「(未折損)」);既有 `comfortHints` 三句提示原樣保留
      (提示講「怎麼改善」,拆解講「為什麼是這個分數」)。樣式沿用既有 `.bar` 與配色變數,
      新增 `.cbd-*` / `.cbucket` class,全部直式堆疊 + `flex-wrap`,無表格、無新依賴、無外部字型圖示。
    - `scripts/comfort-test.ts`:新增 12 項斷言鎖住面板依賴的不變量(23 → 35 項),包含
      `(attrPart + categoryPart) × cleanMult` 確實等於 `comfort`(7 組房間×整潔組合)、
      `cleanMult` 在 0/50/100 = 0.5/0.75/1、拆解的 `cleanMult` 與 `cleanlinessMultiplier` 同源、
      上限常數 60 + 30 = 90、五大類 label 固定五類且順序不變、屬性部分過飽和收斂(狂堆 24 件
      仍 56.8 < 60)、齊全度 =(五類 − 缺的類數)× 6、空房缺全部五類、五類全齊房 `missing` 為空
      且齊全度滿分、`missing` 恆為五大類子集、`roomId` 為 null 時給中性拆解。
- **驗證**:`npm run typecheck` 乾淨;`TZ=Asia/Taipei npm test` **60/60 支通過**(comfort-test 35 項);
  `npm run build` 成功;`git diff main -- scripts/balance-snapshot.json` **為空**(純 UI 改動,
  未影響平衡,未執行 `balance-test.ts --update`)。截圖驗收由合併後統一執行。
- **已知限制／後續**:小計採「兩部分各自四捨五入後相加」,乘上整潔倍率後與總分可能有 ±1 的
  顯示落差(故用「→」而非「=」);拆解區恆常展開未做收合,若日後這一頁再長就該考慮折疊;
  共用區(客廳/衛浴)舒適度仍未納入,拆解面板目前只服務私人房間。
- **硬規則**:未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒等既有硬規則
  全數保留;本次未觸碰 AI、戀愛、平衡與任何模擬邏輯。

### 2026-07-25 — 掃地機器人主動讓路(純視覺層,確定性)(Claude Code)

- **目的／使用者需求**:補掉掃地機遊走 sprite 的已知限制——它閒置在「唯一必經格」
  (例如房門開口 (6,4)、(6,19):上下都是牆,只剩房內與走廊各一格)時會把租客擋住。
  原本只有**被動**避讓(下一步有人就停一拍)與「閒置也會就近小巡」降低機率,沒有主動退讓。
- **實際變更與主要檔案**
  - `src/floor/vacuumAgents.ts`
    - 新增 `vacuumBlocksTenant(cell, tenants)`:判定「掃地機這格是否在某位在場且正在移動的
      租客剩餘路徑上」。關鍵前提是租客尋路(`pathfind.ts`)**不**把掃地機算進 blocked 網格、
      只會在要踏進去的那一幀停下來等(`agents.ts` 的 `blockedCells`),所以「我在你的路徑上」
      就等於「你遲早被我卡住」,不必再另外算有無替代路徑。
    - 新增 `pickYieldCell(from, blocked, tenantCells, tenantPathCells, maxRadius=4)`:BFS 逐層
      擴散 + **固定四鄰順序**挑最近、可走、沒人站、且不在任何租客路徑上的退讓格;找不到完全
      乾淨的格時 fallback 到「至少能離開現在這格」的空格;完全無處可退回傳 `null`。
    - `tickVacuumAgents` 加入主動讓路:偵測到擋路就繞去退讓格,退讓路徑本身也不規劃穿過租客
      (`blockedPlusTenants`)。`VacuumAgent` 新增 `yielding` / `elapsed` / `yieldReadyAt`。
    - 新增 `VacuumTenantView`(租客最小視圖:`c/r/hidden?/moving?/path?`),`tickVacuumAgents`
      第三參數改用它;`FloorMap.vue`／`PixelDollhouse.vue` 傳的 `Agent[]` 結構相容,**未改動**。
  - `scripts/vacuum-test.ts`:17 → **36** 個斷言。新增(a)門口單一走廊格擋路會移開、
    (b)讓路確定性(`pickYieldCell` 同輸入同輸出 + 兩次全新模擬的**逐幀位置序列完全一致**)、
    (c)無退讓格時回 `null`／不崩潰／原地不動不穿牆、(d)沿用既有 1500 幀整合模擬新增
    「租客被卡住的連續幀數有上限」,並保留既有「0 次租客重疊」不變式;另加一支迷你租客
    端到端模擬(複製 `agents.ts` 的停等規則)驗證租客**最終一定穿得過**被占住的門口。
- **確定性(硬性約束)**:全檔**零 `Math.random`**(僅註解提及)。讓路節流刻意用**累計 tick 秒數**
  (`elapsed`,dt 之和)而不是 `Date.now()`,否則讓路時機會隨真實時間漂移、無頭測試無法重現。
  退讓格挑選只靠 BFS 層序 + 固定 `DIRS` 順序,沒有任何亂數或雜湊抽樣。
- **不卡死／不抖動**:讓路途中不重新判定(`yielding`)避免每幀改主意;讓完路設短冷卻
  (`YIELD_COOLDOWN_S`)後由既有 wander 時鐘把它帶回本小時的區域目標;若退路上臨時站了租客,
  會丟掉這條退路並在下一幀重算(否則雙方互等就是永久卡死)。
- **測試/build 結果**:`npx tsx scripts/vacuum-test.ts` **36 通過 / 0 失敗**;
  `TZ=Asia/Taipei npm test` **60/60 支通過**;`npm run typecheck` 乾淨;`npm run build` 成功。
  另做**反向驗證**:暫時關掉主動讓路的判定式後,新增斷言中有 **4 支立刻失敗**
  (`maxStall=399`、租客還剩 3 步走不完),確認新測試不是空轉的。
- **已知限制／後續**:(1) 這是**純渲染層**,完全不碰 `src/sim/`,不結算任何整潔/舒適/經濟數值;
  `git diff <base> -- src/sim/ scripts/balance-snapshot.json` 為空,平衡快照未動。
  (2) 讓路半徑固定 4 步;極端擁擠(退讓格全被家具與租客占滿)時仍會維持原地不動——這是刻意的
  安全行為,不會穿牆也不會疊格,但那一刻租客仍需等到有人先移動。
  (3) 租客尋路仍不會主動繞過掃地機(維持原設計),避讓完全由掃地機這一側負責。
- **硬規則**:未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒全數保留(未觸碰)。

### 2026-07-25 — `worker/` 納入型別檢查(新增 tsconfig.worker.json)(Claude Code)

- **目的／使用者需求**:結清技術債「`worker/` 未納入 `include`」。根目錄 `tsconfig.json` 的
  `include` 只有 `src/**/*.ts`、`src/**/*.vue`、`vite.config.ts`,`worker/index.ts`(約 1.1k 行)
  完全沒有型別檢查,只靠 `wrangler deploy --dry-run` 與 `worker-test.ts` 兜著。
- **實際變更與主要檔案**
  - **新增** `tsconfig.worker.json`:`include: ["worker/**/*.ts"]`,`strict: true`(與前端一致)。
    關鍵是 lib/types 要對準 Cloudflare Workers runtime——**不是** DOM 也**不是** Node:
    `lib: ["ES2022"]`(刻意不含 DOM/DOM.Iterable,免得誤用瀏覽器 API 卻型別過關)、
    `types: ["@cloudflare/workers-types"]` 提供 `Request`/`Response`/`URL`/`fetch`/`Fetcher`
    /`setTimeout` 等 Workers 全域。
  - `package.json`:新增 devDependency `@cloudflare/workers-types`;`typecheck` 拆成
    `typecheck:app`(`vue-tsc --noEmit`)+ `typecheck:worker`(`tsc --noEmit -p tsconfig.worker.json`),
    `typecheck` 改為依序跑這兩支。**`npm run typecheck` 這個既有入口維持可用**,
    需要單獨跑時才用細分的兩支。
- **未修改任何 worker 原始碼**:設定好 lib/types 之後 `worker/index.ts` **零型別錯誤**,
  不需要任何修正,也沒有動到 `strict`、沒有塞 `any` 或 `@ts-ignore`。
  值得記一筆的是:先前用「只有 ES2022、沒有 workers-types」試跑時出現的
  `worker/index.ts(497,11) TS7006: Parameter 'b' implicitly has an 'any' type`
  並不是真的缺標註——那一行是 `msg.content.filter((b) => b.type === "text")`,
  因為缺少 `fetch`/`Request`/`Response` 全域導致 `@anthropic-ai/sdk` 的型別無法解析、
  `msg` 退化成 `any`,`b` 才跟著變隱含 any。補上 workers-types 後 SDK 型別正常解析、
  `b` 自動推導為 ContentBlock,錯誤自然消失(是真的修好,不是被壓掉)。
- **測試/build 結果**:`npx tsc --noEmit -p tsconfig.worker.json` **0 錯誤**;
  `npm run typecheck`(app + worker)乾淨;`TZ=Asia/Taipei npm test` **60/60 支通過**;
  `npm run build` 成功;`npx wrangler deploy --dry-run` **成功**
  (Total Upload 435.75 KiB,bindings `env.AI` / `env.ASSETS` 都正常解析)。
  另做**反向驗證**:在 `worker/index.ts` 尾端暫時插入 `const __probe: number = "not a number";`
  後 `typecheck:worker` 如預期噴 TS2322 並回傳 exit 2,確認新設定不是「include 抓不到檔案」
  的假通過;驗完已還原,`git diff -- worker/index.ts` 為空。
- **已知限制／後續**:(1) worker 與前端用兩份 tsconfig,共用型別若日後要跨邊界共享需另設
  `references`/`paths`,目前沒有這個需求。(2) `@cloudflare/workers-types` 的版本需與
  `wrangler.toml` 的 `compatibility_date`(2026-06-17)大致對齊,升 compat date 時要一併留意。
  (3) `worker/` 目前只有 `index.ts` 一檔;`include` 已寫成 `worker/**/*.ts`,新增檔案會自動納入。
- **硬規則**:未動任何執行邏輯,未成年排除戀愛線、成人內容遮蔽式且預設關、
  AI 輸出白名單/夾值/消毒全數保留。平衡快照未動。

### 2026-07-25 — 機率型測試去隨機化(六支改固定種子)(Claude Code)

- **目的／使用者需求**:`conflict` / `drama` / `relist` / `rent` / `move` / `qol` 六支測試直接吃
  真實 `Math.random()`,偶爾假性失敗,一直靠 `run-all.ts` 的「失敗重試一次」兜著。
  改成固定種子、可重現,讓失敗一律代表真的有 bug。
- **實際變更與主要檔案**:六支各自在**檔頭**加入與 `balance-test.ts` 相同的 mulberry32
  固定種子(`seed = 20260710`),覆寫 `Math.random`。關鍵是**必須在 import store 之前**覆寫,
  否則模組初始化階段(產生初始狀態/應徵者)就已經吃掉真實亂數:
  - `scripts/conflict-test.ts`、`scripts/drama-test.ts`、`scripts/relist-test.ts`、
    `scripts/qol-test.ts`:本來就是 `await import(...)` 動態匯入,只加種子區塊即可。
  - `scripts/rent-test.ts`、`scripts/move-test.ts`:原本用**靜態** `import`,靜態匯入會在
    種子生效前就被提升執行,因此改成 `await import(...)` 動態匯入(語意等價、tsx 支援頂層 await)。
- **沒有放寬任何斷言**:第一個嘗試的種子(20260710,與 balance-test 同值)六支就全過,
  不需要換種子、也沒有任何一支需要重構或跳過。斷言數量與去隨機化**之前完全相同**
  (conflict 36、drama 31、relist 9、rent 19、move 13、qol 22),沒有刪除或調鬆任何一條。
- **測試/build 結果**
  - 六支各跑 **3 次**,每支三次的**完整輸出 SHA256 完全相同**(uniqueOutputs=1),
    確認真的可重現而不是碰巧通過。
  - `TZ=Asia/Taipei npm test` **60/60 支通過**;`npm run typecheck`(app + worker)乾淨;
    `npm run build` 成功。
- **已知限制／後續**:(1) 依需求**未改動 `scripts/run-all.ts`**,所以「失敗重試一次」的機制還在;
  這六支已不再需要它,但其他仍吃真實亂數的測試還靠它,要不要移除留給後續決定。
  (2) 種子固定代表只覆蓋**一條**亂數路徑;它保證的是「可重現」,不是「窮舉所有分支」。
  若日後要提高覆蓋率,建議做法是同一支測試跑多個明列的種子,而不是回頭用真實亂數。
  (3) 六支的斷言都沒依賴特定亂數序列的巧合(條件矩陣多半是用 `() => 0` 明確注入 rng),
    因此換種子的風險低。
- **硬規則**:未動任何模擬邏輯與平衡數值,平衡快照未動;未成年排除戀愛線、
  成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒全數保留。

### 2026-07-25 — 舒適度面板三條長條對齊同一軌道(Claude Code)

- **目的／使用者需求**:`.comfort` 面板有三條水平長條——虛線之上是總分
  「🛋️ 舒適度 [長條] 46」,虛線之下是拆解「家具屬性 [長條] 35/60」「種類齊全 [長條] 24/30」。
  三條沒有共用同一條視覺軌道,左右兩端都差了幾 px,玩家沒辦法跨虛線用眼睛比對
  「總分」與「它的兩個組成部分」,而那正是這個面板存在的意義。
- **實際變更與主要檔案**:只改 `src/App.vue` 的 `<style>`,**純 CSS、未動模板結構、未動任何計算**。
  在 `.comfort` 上新增兩個自訂屬性 `--cbar-label-w: 58px`、`--cbar-value-w: 36px`,
  讓標籤欄與數值欄成為三列共用的固定欄寬:
  - `.comfort-head label`、`.cbd-row label`:加 `flex: 0 0 var(--cbar-label-w)`
  - `.comfort-head span`(原 `width: 24px`)、`.cbd-row > span`(原 `min-width: 46px`)
    :改為 `flex: 0 0 var(--cbar-value-w)`
  兩列的 `gap` 本來就都是 8px,固定住左右兩欄之後 `.bar`(`flex: 1`)的左右端就必然重合。
  欄寬取值依據實測文字內寬:標籤「🛋️ 舒適度」54.61px / 「家具屬性」46px → 取 58px(留 3.4px
  給不同平台 emoji 字形差異);數值「30/60」33.56px、總分三位數約 21.6px → 取 36px。
- **對齊前後實測 x 座標**(容器內 Playwright、`isMobile`、DPR 2、深色、zh-TW):
  - 360px 視口(面板 16→344):
    - 前:總分 `93.61→297`(w203.39)、拆解兩條 `85→275`(w190);左端差 **8.61px**、右端差 **22px**
    - 後:三條全部 `97→285`(w188);左端差 **0**、右端差 **0**
  - 390px 視口:前:總分 `93.61→327`(w233.39)、拆解 `85→305`(w220);
    後:三條全部 `97→315`(w218),左右端差皆 **0**
  - 長條寬度 360px 下 190→**188px**(-2px),390px 下 220→**218px**,沒有被壓窄;
    總分長條反而從 203.39 縮到 188 才與拆解同軌。
  - 數值欄右緣維持在 329(360px)/ 359(390px),與對齊前完全相同;
    `text-align: right` 與 `font-variant-numeric: tabular-nums` 三列都保留。
- **畫面數字零變動**(r301 與 r302 前後逐項比對):r301 總分 37、`30/60`、`24/30`、
  `×0.68`、「小計 54 → 37」、膠囊 `✓睡眠 ✓娛樂 ✗收納 ✓裝飾 ✓生活`;
  r302 總分 54、`32/60`、`24/30`、`×0.96`、「小計 56 → 54」、膠囊 `✓睡眠 ✓娛樂 ✓收納 ✓裝飾 ✗生活`。
  長條百分比字串也一字不差(37% / 50.4132% / 80%、54% / 54.1401% / 80%)。
- **測試/build 結果**:`npm run typecheck`(app + worker)乾淨;
  `TZ=Asia/Taipei npm test` **60/60 支通過**;`npm run build` 成功。
  UI 量測與截圖:360 / 390 × r301(髒房、缺收納、×0.68)/ r302(乾淨、缺生活、×0.96)四組,
  五顆類別膠囊全部仍在**同一列**(`bucketRowCount=1`),面板內無任何元素溢出
  (`scrollWidth/clientWidth` 全數相符、`documentElement` 360/360 與 390/390,無橫向捲動),
  console **0 errors / 0 warnings**。產物在 `artifacts/ui-lab/rent/comfort-v3/`
  (`align-before.json` / `align-after.json` 與局部截圖,含疊上左右端輔助線的 `-guides.png`)。
- **已知限制／後續**:(1) 欄寬是固定 px,若日後有人把標籤改成 5 個字或換更寬的 emoji 字形,
  超出 58px 會壓到 8px 的 gap;要改標籤文字時請一併調 `--cbar-label-w`。
  (2) 目前只讓三條長條共軌,`.cbd-clean` 那列(整潔倍率/小計)仍是原本的
  `margin-left: auto` 靠右排法,刻意不動以免改變既有視覺。
  (3) 量測時發現共用 UI Lab 容器的 Vite dev server 對 Windows bind mount **不會觸發檔案監看**
  (改檔後 5173 仍供應舊 CSS,`touch` 也無效),因此本次先以 `vite build` 產物在容器內另開
  preview 埠量測,確認後重啟 5173 讓互動網址同步;後續若要靠 HMR 驗證 UI,需要
  `server.watch.usePolling`。
  (4) 平衡快照**未動**,未跑 `balance-test --update`。
- **硬規則**:未動 `src/sim/`、`scripts/`、`worker/` 與任何模擬邏輯;未成年排除戀愛線、
  成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒全數保留。

### 2026-07-27 — 文件現況對帳與過期待辦結案(Codex)

- **目的／使用者需求**:修正現行摘要仍把 2026-07-25 已完成工作列為待辦的文件矛盾，
  讓下一位接手者不必再次考古。
- **實際變更**:
  - 本檔第四節將「掃地機器人主動讓路」改標完成，並校正家具 tier 目前是 UI 標示、
    attributes 才是既有數值來源。
  - 本檔第五節把 Worker 型別檢查改為已涵蓋 app + worker 的現況；把六支機率型測試改為
    已固定種子的現況，同時保留 `run-all.ts` 對其他測試的重試說明。
  - `docs/交接與待辦.md` 更新最新交接、剩餘工作與 CI 歷史段落的結案註記。
  - 外層 `WORKSPACE_STATUS.md` 同步實際 HEAD，並移除另建 `docs/PROJECT_STATUS.md` 的過時建議；
    本專案仍以 `工作日誌.md` 作為唯一正式進度來源。
- **驗證**:以 `rg` 重新搜尋三個已結案項目，確認現行待辦不再誤列；執行
  `git diff --check`，並核對 `git status --short`、`git log -1`。本次只改 Markdown，
  未動程式、測試、平衡或部署設定，因此未重跑 `npm test`／build。
- **已知限制／後續**:歷史日期段落保留當時原始驗證數字與判斷，必要處只加後續結案註記；
  現行狀態以文件前段摘要與尾端最新日期紀錄為準。
- **硬規則**:純文件修正，未觸及 AI、戀愛、成人模式、存檔與任何模擬邏輯。

### 2026-07-27 — 修正模範房客安居倒數永遠停在 20 天(Codex)

- **目的／使用者回報**:房客目標達成後，安居型模範房客的「20 天後圓滿搬離」一直維持
  20 天，沒有隨遊戲日下降，也沒有真的搬離。
- **根因**:`settleDepartDay()` 在 `modelSinceDay` 缺失時以「當前遊戲日」作 fallback。
  2026-07-23 前已成為模範房客的舊狀態沒有此欄位；雖然 `load()` 會在記憶體補值，但若玩家
  開啟未滿一個遊戲小時便關閉，`syncToNow()` 不會存檔，下次載入又重新以當天補值。
  因此 UI 倒數與 `wishPass()` 的實際搬離期限都每天向後推，確實會永久卡在 20。
- **修正**:
  - `src/sim/persistence.ts`:載入時偵測已是模範房客但 `modelSinceDay` 缺失／非有限數字，
    以當前遊戲日給滿一段新的 20 天安居期，並在完成載入後**立即回存**；短暫開啟也不會
    下次再重設。
  - `src/sim/wishes.ts`:`ensureWishes()` 新增 runtime 安全修復，涵蓋長時間未重載分頁或
    HMR 保留下來的記憶體狀態；只修安居型模範房客，不改畢業型 6 天離開流程。
  - 不在 `wishResult()`／UI selector 內存檔，也不讓 wishes import persistence，避免副作用
    與循環依賴。
- **回歸測試**:`scripts/wish-test.ts` 134 → **139 項**，新增：
  1. 舊存檔補出起點後立即寫回 localStorage。
  2. 記憶體缺欄位時一次性補固定起點。
  3. 下一遊戲日倒數由 20 單調降為 19。
  4. 真正走 `hourlyTick → wishPass → graduateFarewell` 的 20 日端到端流程：
     第 19 天仍在且剩 1 天，第 20 天自動清除 runtime／occupancy 並進安居圓滿名冊。
- **完整驗證**:`TZ=Asia/Taipei npm test` **60/60 支通過**（wish 139/139、balance 與
  sim-trace 均綠）；`npm run typecheck`（app + worker）乾淨；`npm run build` 成功
  （Vite 124 modules）。`scripts/balance-snapshot.json` 無變更。
- **既有行為／非本 bug**:20 天從「心願達成、成為模範房客」起算，不是入住日起算；
  離線補進度仍有每次最多 48 遊戲小時的既有上限，超額時間會捨棄，本次未擴大修改範圍。
- **UI 與硬規則**:未改模板／CSS／Canvas，不需重跑 UI 截圖；未觸及 AI、戀愛、成人模式、
  寵物去留與任何內容安全規則。

### 2026-07-27 — 安居倒數改以午夜換日並還原真正完成日(Codex)

- **目的／使用者複驗**:上一版部署後，玩家確認頁首日期已跨日，安居型房客仍顯示 20 天。
  需要讓畫面倒數、每日結算與實際搬離使用同一個換日邊界，並避免舊房客在升級時被重新
  贈送完整 20 天。
- **第二層根因**:
  1. 頁首與 `hourlyTick` 每日結算在本地午夜換日，但既有 `gameDayIndex()` 以
     `GAME_START` 的 22:00 為 24 小時邊界；玩家看到日期跨日，不代表安居倒數已換日。
  2. 上一版對缺少 `modelSinceDay` 的舊存檔刻意以「載入當天」補值，無法保留目標真正
     完成後已經過的天數；所有舊模範房客會同時重新顯示 20 天。
- **修正**:
  - `src/sim/gameState.ts`:新增 `calendarGameDayIndex()` 與
    `modelSinceCalendarDay`，以玩家本地日曆午夜為唯一安居換日邊界；舊
    `modelSinceDay` 僅保留相容。
  - `src/sim/wishes.ts`:新完成的安居型心願記錄午夜制起點；UI 倒數、前兩日打包預告、
    自然期滿搬離及主動送別全部改用同一日曆日。舊資料優先從保存的
    `🎉 心願成真` 日誌 `gameMs` 還原真正完成日，日誌被裁掉時才以 `fulfilledDay`
    推估每日結算午夜；也會自癒上一版已錯存成載入當天的起點。
  - `src/sim/persistence.ts`:存檔版本 `3 → 4`，保存／載入午夜制起點；遷移修復完成後
    立即回存，下一次開啟不會重跑。
- **回歸測試**:`scripts/wish-test.ts` 139 → **142 項**，新增／改寫舊檔晚五天升級仍剩
  15 天、上一版錯補資料自癒、頁首 23:00 → 00:00 時倒數同步 20 → 19、完成日誌被裁掉時
  由 `fulfilledDay` 還原、主動送別起點不被自癒回改，以及第 20 天
  `hourlyTick → wishPass → graduateFarewell` 真正搬離。
- **完整驗證**:`npm test` **60/60 支通過**（wish 142/142、balance、sim-trace 全綠）；
  `npm run build` 成功（Vite 124 modules）；`npm run ui:shot -- rent` 在 390×844、
  412×915、430×932 三個手機尺寸共 18 張截圖，`report.json` 為 0 errors／0 warnings，
  全數人工檢視無版面回歸。`git diff --check` 乾淨，平衡快照未變。
- **遷移效果**:若舊房客的目標完成已超過 20 個日曆日，升級後會顯示 0 天，並在下一次
  每日結算／掛機補進度時依原本完成日正常圓滿搬離；這是恢復原定期限，不再額外延期。
- **硬規則**:未改畢業型 6 天流程、20 天平衡常數、租金／口碑／押金／紅包公式、AI、
  戀愛、成人模式、寵物去留或內容安全規則。

### 2026-07-27 — 修復舊檔到期後「0 天無數字、仍等下一午夜」(Codex)

- **目的／使用者截圖複驗**:安居卡顯示「安居圓滿,即將展開人生下一步」，但看不到任何
  倒數數字，房客也仍留在房間。
- **第三層根因**:
  1. `wishResult()` 已把這位房客算成 `daysLeft=0`，但舊文案在 0 天分支刻意省略數字，
     因此玩家只看見「即將」，無法判斷倒數是否真的運作。
  2. 舊存檔載入後才從心願日誌還原真正完成日；若還原結果已超過 20 天，但當下沒有任何
     離線整小時需要補，`syncToNow()` 會直接返回。實際離場原本只在跨午夜的
     `wishPass()` 執行，所以截圖中的 05:00 狀態會卡到下一個遊戲午夜。
- **修正**:
  - `src/sim/wishes.ts`:安居結果固定明示「剩 N 天」；0 天顯示
    「剩 0 天,今日結算後展開人生下一步」。新增只讀的 `settleDeparturesDue()`，
    只收集已到期的安居型模範房客，不推進其他心願、不套每日模範光環。
  - `src/sim/tick.ts`／`src/sim/lifecycle.ts`:載入成功後立即呼叫
    `reconcileDueSettleDepartures()`，沿用正式 `graduateFarewell` 完成歡送會、紅包、
    押金、口碑、紀念物、名冊與搬離；`moveOut()` 會立即存檔，因此重載不會重複結算。
- **回歸測試**:`scripts/wish-test.ts` 142 → **147 項**，新增 0 天文案、05:00 讀檔即
  到期立即移除 runtime／occupancy 並進名冊、其他未完成心願不被推進、只套正式歡送會
  心情效果而沒有每日光環，以及重複呼叫不會重複紅包／押金／名冊。
- **完整驗證**:`npm test` **60/60 支通過**（wish 147/147、balance、sim-trace 全綠）；
  `npm run typecheck`（app + worker）乾淨；`npm run build` 成功（Vite 124 modules，
  `index-Cj8nUt-m.js`）；`npm run ui:shot -- rent` 產出三種手機尺寸共 18 張截圖，
  `report.json` 為 0 errors／0 warnings，全部人工檢視無版面回歸；`git diff --check`
  乾淨，平衡快照未變。
- **遷移效果**:目前已停在「安居圓滿,即將…」的舊房客，在載入此版本後會立即完成原定
  的圓滿搬離，不必再等下一個午夜；尚未到期者則持續明確顯示「剩 N 天」。
- **硬規則**:未改 20 天常數、畢業型 6 天流程、租金／口碑／押金／紅包公式、AI、戀愛、
  成人模式、寵物去留或內容安全規則。

### 2026-07-28 — 修復最早期舊檔「顯示 0 天但永遠不進搬離名單」(Codex)

- **目的／使用者再次複驗**:新版已明確顯示「剩 0 天」，但即使再過數個遊戲日，房客仍
  不會搬離。這次以最早期玩家存檔的實際資料形狀重現，不再只測新欄位齊全的合成狀態。
- **最終根因**:最早期版本的安居型心願已可寫入 `wish.fulfilledDay`，但當時尚無
  `modelTenant` 欄位；後來新增欄位時沒有對這批歷史資料做遷移，載入後被正規化為
  `false`。`wishResult()` 只看「心願已完成且為安居型」，所以 UI 照樣算出 0 天；
  舊 `settleDeparturesDue()`／`wishPass()` 卻額外要求 `modelTenant=true`，因此同一位
  房客永遠不會進離場名單。修正前已用舊檔重現：
  `UI daysLeft=0`、`settleDeparturesDue=[]`、`wishPass=[]`、`modelTenant=false`。
- **修正**:
  - `src/sim/wishes.ts`:`ensureWishes()` 將「已完成且非畢業型」定義為安居完成的正式
    不變條件；舊檔缺旗標時只補 `modelTenant=true` 與真正完成日起點，不呼叫
    `becomeModelTenant()`，因此不會重發 +3% 租金、口碑、成就或通知。
  - `settleDeparturesDue()` 直接使用和 UI 相同的 `wishResult()`：只要畫面資料會顯示
    `phase=stayed` 且 `daysLeft=0`，就必須列入到期名單，消除第二套資格條件。
  - `src/sim/tick.ts`:到期修復改為每次 `syncToNow()`（包含 0 小時補進度、每 5 秒前景
    同步、分頁恢復）與每個遊戲小時都執行，不再只押在初始化或午夜；修復但尚未到期的
    狀態會立即持久化。
  - `src/sim/tenancy.ts`:安居／畢業結算分類也以「已完成且非畢業型」判定，避免缺舊旗標
    時雖然搬走，卻誤增 `graduateCount` 或發成畢業型口碑／紅包文案。
- **回歸測試**:`scripts/wish-test.ts` 147 → **153 項**。新增真正 v3 舊檔案例：完成的
  安居心願缺 `modelTenant` 時會還原身分與倒數並立即回存；未完成安居型與已完成畢業型
  不會誤補；修復不改租金、口碑或日誌；已超過 20 天者在 05:00、0 個補進度小時下由
  `initGame()` 立即清除 runtime／occupancy、進安居圓滿名冊，且口碑走安居值、
  `graduateCount` 不變。
- **完整驗證**:`npm test` **60/60 支通過**（wish 153/153、balance、sim-trace 全綠）；
  `npm run typecheck`（app + worker）乾淨；`npm run build` 成功（Vite 124 modules，
  `index-MSs6qs26.js`）；`git diff --check` 乾淨。平衡常數與快照未變。
- **UI／硬規則**:本次未改 Vue 模板、CSS 或 Canvas，沿用上一版「剩 N 天／剩 0 天」
  顯示，因此不重跑 UI 截圖；未改 20 天與 6 天常數、金錢公式、AI、戀愛、成人模式、
  寵物去留或內容安全規則。

### 2026-07-28 — 房客七項數值與舒適度整合為「居住體驗」(Codex)

- **目的／使用者需求**:原房客頁把心情、壓力、精力、健康、好感、整潔、滿意七條長條
  與另一張舒適度卡並列，玩家難以理解哪些是原因、哪個才是退租結果。重新規劃成一個
  玩家可理解的系統，同時避免再次改動剛修好的離場與存檔邏輯。
- **介面重構**:`src/App.vue` 將原本七條平行數值與獨立舒適度卡合併成單一卡
  「🏡 居住體驗」：
  - 主結果改名為「續住意願」，直接沿用原 `rt.satisfaction`，明示長期過低才會累積
    退租風險，並依分數顯示「有退租風險／仍在觀望／願意續住／住得很安心」。
  - 三個可理解面向為「🌿 身心狀態」（心情、壓力、精力、健康的閱讀摘要）、
    「🤝 房東信任」（原好感）與「🛋️ 房間品質」（原舒適度）；房間品質同列顯示整潔
    與原滿意公式使用的生活機能滿足率，並明示舒適是慢慢影響身心、生活機能才直接影響意願。
  - 「目前狀態與改善方向」最多列三個可行動診斷，例如壓力、精力、信任、髒亂、家具不足；
    若當前續住意願與公式目標相差 8 分以上，會依既有 20%/h 慣性顯示正在逐步回升／下降。
    原始四項身心長條與家具屬性／種類／整潔倍率移進可展開的「診斷細項」，資料沒有消失。
  - 點新標題仍會顯示說明；畫面文案刻意區分房間品質的間接效果與生活機能的直接來源，
    不宣稱一套不存在的新模擬公式。
- **純呈現摘要**:新增 `src/presentation/livingExperience.ts`。身心摘要只把既有滿意度
  公式中的四個身心權重正規化為 0~100；分數狀態與診斷也只讀既有 runtime，不寫存檔、
  不影響退租／收租／事件／心願。退租風險標籤精確使用真實 `<25` 安全線。將生活機能
  判定抽成 `routineNeedsMet()`，並新增 `src/sim/satisfaction.ts` 的 canonical
  `satisfactionTarget()`，讓 tick 與 UI 共用同一套需求判定與目標公式，計算與平衡不變。
- **測試**:新增 `scripts/living-experience-test.ts` 21 項，覆蓋身心摘要、0~100 夾值、
  真實 25 分退租門檻、四段狀態標籤、續住目標公式、慣性回升／下降提示、
  風險排序／三項上限與穩定狀態文案，並納入 `run-all.ts` 正式回歸集。
  `npm test` 共 **60 支回歸 + sim-trace，61/61 全數通過**（wish 153/153、comfort
  35/35、balance 與 sim-trace 均綠）；`npm run typecheck`（app + worker）乾淨；
  `npm run build` 成功（Vite 126 modules）。
- **手機 UI 驗證**:重啟殘留舊 Vite 程序的 UI Lab 後，`npm run ui:shot -- rent` 在
  390×844、412×915、430×932 產出 18 張截圖，`report.json` 為
  **0 errors／0 warnings**，全數人工檢視。另實際進入房客頁檢查摘要與展開細項：
  360／390／430px 三種寬度的 document、卡片、三來源區、細項區
  `scrollWidth-clientWidth` 全為 0，console 0 errors／0 warnings；願望卡與底部導覽仍正常。
- **提交／部署補記**:依使用者要求，在後續純文件提交中補記已推送的功能 commit
  `b2048cd`（`feat(ui): 整合房客居住體驗`）。該提交已同步至 `origin/main`；
  GitHub Actions run [30363485791](https://github.com/guai7485962/rent-house/actions/runs/30363485791)
  全數成功。Cloudflare 正式站
  [驗證網址](https://rent-house.guai7485962.workers.dev/?verify=b2048cd) 已載入
  `/assets/index-CiqEW5EW.js`，並確認 bundle 包含「居住體驗」、「安全線 25」與
  「生活機能」新版內容。
- **平衡／相容性**:`src/sim/tick.ts` 只把原本檔內完全相同的 `needsMet` 與滿意目標公式
  改呼叫共用的 `routineNeedsMet()`／`satisfactionTarget()`；未修改公式、係數、
  更新頻率或 RNG。`src/sim/comfort.ts`、
  tenancy、wishes、persistence 與任何狀態欄位均未變；沒有存檔遷移，平衡快照未變。
  舊數值仍各自驅動生病、崩潰、事件、繳租、漲租與願望，只是預設畫面改以單一因果層級呈現。
- **硬規則**:未改 20 天／6 天離場流程、租金與金錢公式、AI、戀愛、成人模式、寵物、
  內容安全或任何模擬 RNG。

### 2026-07-28 — 月度全樓事件鏈:3 條鏈 × 4 階段的章節感(Claude Code)

- **目的／使用者需求**:實作第四節待辦「月度全樓事件鏈(🟡)」。用跨日連鎖劇情給掛機
  生活「章節感」——每條鏈四話、約 30 遊戲日走完一輪,關鍵一話彈房東抉擇。三條鏈主題:
  **都更傳聞**(紅單 → 交誼廳分兩派 → 建商上門 → 塵埃落定)、**颱風夜停電**
  (氣象預警 → 停電擠交誼廳 → 共度長夜 → 復電收拾)、**頂樓漏水整修**
  (天花板水漬 → 師傅勘查 → 施工擾民高峰 → 完工)。
- **實際變更與主要檔案**
  - 新增 `src/sim/floorChain.ts`:章節鏈定義(3 條 × 4 話,純模板文案)、推進 pass、
    抉擇拍板、動態頁唯讀視圖。四個刻意的設計約束寫在檔頭:
    1. **零 RNG**——選鏈與選文案一律用決定性雜湊(比照 `community.sceneIndex`
       與 `weather` 的作法),完全不呼叫 `Math.random`,不動既有亂數次序。
    2. **純模板文案**——章節文字全部寫死,離線補進度或 AI 額度用盡時照樣成立;
       只在第一話對在場租客掛**一個純中文伏筆旗標**(「都更傳聞在耳邊」等),
       讓既有 AI 每日觀察的 `focus.kind === "flag"` 機制自然回收。旗標不含系統鍵,
       可安全餵給模型,不必加進 `narration` 的過濾名單;鏈結束時一併清掉,
       不會擠掉 `addFlag` cap 12 內的既有伏筆。
    3. **獨立抉擇槽**——新增 `state.pendingChainEvent`,刻意不共用
       `state.pendingGroupEvent`(後者有 `group_any` 3 日冷卻,共用會互相餓死)。
    4. **可跳階、冪等**——推進條件是「距上次推進 ≥ 4 遊戲日」而非「第 N 天必定推進」,
       每次 pass 最多推一話(同日重複呼叫不重複發);拖過 30 遊戲日(長時間掛機、
       `MAX_CATCHUP_HOURS` 重錨)則跳過中間話、只留章節紀錄並直接收束到最後一話。
  - `src/sim/gameState.ts`:新增 `floorChain` / `lastChainEndDay` /
    `pendingChainEvent` 三個狀態槽;`src/types.ts` 新增 `FloorChainState`、
    `FloorChainEntry`、`ChainEvent`(`ChainEvent extends GroupEvent`,可直接餵既有 Modal)。
  - `src/sim/tick.ts`:換日區塊在 `communityPass()` 之後呼叫 `floorChainPass()`。
  - `src/sim/persistence.ts`:三個新欄位加進 `save()` 白名單與 `load()` 的 `?? 預設`。
    皆為選填欄位,**`SAVE_VERSION` 維持 4、不需遷移**;舊檔載入 = 尚無章節。
  - UI:`FeedPanel.vue` 新增可摺疊的「📖 本月章節」卡(仿週報卡 `<details>` 範本),
    顯示第 N 話 / 共 4 話、進度條與已發生階段,補進度跳過的話以淡化樣式標示,
    關鍵一話拍板後補記房東選擇。`GroupDecisionModal.vue` 加一個 `tag` 選填 prop
    (預設維持原字串),`App.vue` 以 `v-else-if` 復用同一元件彈出章節抉擇——
    群體事務未決時先讓那件事處理完,不會兩層 overlay 疊在一起。
  - 演出層維持既有能力範圍:多人場景**不做多人站位**(`startPairSession` 仍是 pair-only),
    只在交誼廳大廳中央掛 `spawnFx`,並同時給現實時間與遊戲時間**雙重過期**,快轉不殘留。
  - 優雅降級:在場判定會濾掉外出與卡著 `pendingEvent` 的租客;真的湊不到人時,
    章節照常推進、只留全樓公告與章節卡紀錄,不會卡住或炸掉。
- **測試、build 與驗證結果**
  - 新增 `scripts/floor-chain-test.ts`(**50 項全過**),覆蓋:開章門檻、依日數推進、
    同日冪等、待決期間停在原地、抉擇金錢/滿意度/關係效果、章節卡補記、旗標清理、
    鏈間休息與不連續同題材、離線補進度一次收束、存檔往返(含待決抉擇)、
    全員外出的降級、以及**零 RNG 與決定性**(統計 `Math.random` 呼叫數為 0、
    同輸入兩次跑出完全相同章節與文案)。已納入 `run-all.ts` 正式回歸集。
  - `npm test`:**62/62 全綠**(原 61 支 + 新測試,含 sim-trace 無異常)。
  - `npm run typecheck`(app + worker)乾淨;`npm run build` 成功(Vite 127 modules);
    `npx wrangler deploy --dry-run` 通過;`git diff --check` 無空白問題。
- **平衡快照**:`npx tsx scripts/balance-test.ts` **零漂移、未更新**
  (`scripts/balance-snapshot.json` 未修改)。零 RNG 之外另加一道保險——
  `CHAIN_FIRST_DAY = 14`,新局前 14 個遊戲日不開章,balance-test 的 10 遊戲日
  固定種子跑程完全碰不到本系統。
- **已知限制與後續**
  - 章節抉擇是 modal,若玩家一直不決定,鏈會停在該話等待(與既有群體事務同行為);
    刻意不做自動代決,免得替玩家花錢。
  - 離線太久觸發收束時,若被跳過的正好是抉擇那一話,該次抉擇不會補彈(章節卡會標示
    這幾話被跳過)。
  - 「全員擠交誼廳」目前是文字 + 交誼廳特效;真正的多人站位演出超出本次範圍,
    待演出層支援多人 session 後可再補。
  - 鏈的選擇只排除「上一條」,長期仍可能出現隔一輪重複同題材;鏈數擴充後自然改善。
- **保留的硬規則**:未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒、
  AI 不可驅逐租客等既有底線全部未動。本次章節抉擇的效果**不含驅逐或強制搬離**
  (測試以正則把關),也沒有動 `worker/`、沒有擴 AI schema、沒有新增 AI 呼叫成本。

### 2026-07-28 — 補記月度全樓事件鏈的 UI 驗證與部署結果(Claude Code)

- **目的**:補記上一則 `cbbdfbc` 的 UI Lab 目檢與正式站部署驗證結果(該提交本身只跑到
  typecheck / test / build / dry-run,截圖與部署由中控在合併後執行)。
- **UI Lab 驗證**
  - `npm run ui:shot -- rent`:18 張、**0 errors**;容器建置出的 bundle 雜湊與本機
    一致,確認截的是新版而非容器舊快取(此坑先前踩過多次)。
  - **預設流程的盲點**:UI Lab 是全新遊戲(遊戲日 7/5),而 `CHAIN_FIRST_DAY = 14`
    新局前 14 天不開章,**預設 18 張拍不到章節卡**——只能證明無回歸,不能證明新功能。
  - 因此另以「注入存檔」自訂 Playwright 流程補拍(直接改 `gameMs` 錨點跳到第 22 遊戲日,
    `need <= 0` 故不觸發任何 `hourlyTick`,零模擬副作用),產出
    `artifacts/ui-lab/rent/floor-chain/` 共 9 張:章節卡摺疊、章節卡展開、章節抉擇 Modal,
    各 390×844 / 412×915 / 430×932。三尺寸皆 0 console error、無裁切、無水平溢出;
    摺疊卡可正常收合展開,進度圓點 3/4 亮暗對比明確,Modal 的 `tag` 顯示
    「📖 本月章節 — 房東抉擇」有別於群體事務預設標籤。
  - 殘餘未驗:章節卡與滿版 feed(事件中心 + 週報 + 大量時間軸列)並存時的堆疊間距。
    三個 section 為同層 block 結構,風險低,待日後實玩自然覆蓋。
- **部署**:`feat/monthly-floor-chain` 以 fast-forward 併入 `main` 並推送
  (`36a9f26..cbbdfbc`)。GitHub Actions CI 對 `cbbdfbc` **conclusion: success**。
- **線上驗證**:`https://rent-house.guai7485962.workers.dev/` HTTP 200。
  - **注意**:Cloudflare 建置產出的 bundle 雜湊(`index-m3PvzD4P.js`)與本機/容器建置
    (`index-BUu-ddGb.js`)**不同**,應為建置環境依賴版本差異所致。**不可只憑雜湊比對
    判斷部署成功**;本次改以下載 bundle 位元組後用 ripgrep 驗內容
    (PowerShell 的 `-match` / `Contains` 對 CJK 會假陰性,先前踩過)。
  - 內容驗證命中:`都更傳聞`、`颱風夜停電`、`頂樓漏水整修`、`本月章節`、
    `chain-card`、`pendingChainEvent` 全部存在於線上 bundle,確認新功能已上線。
- **本則為純文件補記**,未改任何程式;平衡快照未動。

### 2026-07-29 — 夢境彩蛋:睡覺時段的零成本敘事彩蛋(Claude Code)

- **目的／使用者需求**:實作第四節待辦「夢境彩蛋(🟢)」(規格見 2026-07-16
  「後續優化規劃」第 5 項)。租客睡著時低頻(每人每 3~4 遊戲日)留下一則夢境日誌,
  素材取自**記憶標籤**與**鄰居關係**,純模板、零 AI 成本、零數值副作用——
  讓「睡覺」這個一天佔最多小時、卻最沒有內容的時段也有東西可看。
- **實際變更與主要檔案**
  - 新增 `src/content/dreamLines.ts`:14 個句池共 **182 句**(每池 13 句)。
    分池軸**直接對齊 `memoryEffects.MEMORY_RULES` 的關鍵字分類**——戀愛/失戀/低落/
    搬走/寵物/運動/熬夜/感激/孤獨/事業順遂/財務打擊共 11 組記憶主題,
    另加關係主題兩池(親近 `DREAM_BOND_LINES` / 疏遠 `DREAM_DISTANT_LINES`)
    與保底池 `DREAM_NEUTRAL_LINES`。佔位符沿用 `community.COMMUNITY_LINES` 的慣例:
    `{m}` 記憶文字、`{o}` 對方名字、`{tier}` 親疏文字(`social.tierLabel`)。
  - 新增 `src/sim/dreams.ts`(`dreamPass` / `pickDreamTheme` / `dreamLine` /
    `resetDreamCooldown`)。五個刻意的設計約束寫在檔頭:
    1. **零 RNG**——選主題、選文案、決定下次間隔一律走決定性雜湊
       (同 `tick.cozyHomeIndex` / `floorChain.chainIndex`),整支檔案不呼叫
       `Math.random`;多一次呼叫就會位移整個模擬亂數序列、讓 balance 快照全盤漂移。
    2. **純風味,不改任何數值**——不碰 mood/stress/energy、不呼叫
       `adjustRelationship`、不呼叫 `pushMemory`、不寫回 `MemoryTag.intensity`,只讀不寫。
    3. **冷卻入存檔**——寫在 `state.interactionCooldowns['dream|<id>']`
       (比照 pets.ts 借用同一張表,persistence 已在存讀,**`SAVE_VERSION` 維持 4、
       不需改 persistence.ts**)。刻意**不用模組層 `Map`**(如 `tick.dirtyComplaintDay`),
       那種冷卻 reload 就清空,會變成「每次重開頁面當晚必做夢」。
    4. **用 `gameDayIndex()` 而非 `calendarGameDayIndex()`**——前者以 22:00 為換日邊界,
       22:00→06:00 整夜同屬一個遊戲日,天然保證「一夜最多一夢」;後者午夜換日會一夜兩夢。
    5. **`DREAM_FIRST_DAY = 14`**——同 `floorChain.CHAIN_FIRST_DAY` 的理由:
       `balance-test` 把 `logs: rt.log.length` 當**整數精確比對**存進快照,而快照窗只有
       10 遊戲日,第 14 天起才觸發 ⇒ 快照零漂移。
    主題選材優先序:記憶標籤 → 鄰居關係 → 保底池;記憶關鍵字用**錨點反查**
    (`MEMORY_RULES.find(r => r.keywords.includes(anchor))`)而非寫死索引或複製一份,
    關鍵字表日後擴充時夢境主題自動跟著走。記憶文字進日誌前一律去方括號。
  - `src/sim/tick.ts`:每租客迴圈之後、`roomVisitPass(hour)` 之前呼叫 `dreamPass(hour)`
    (此位置全員本小時 `visualState` 已定,且不影響既有 RNG 呼叫次序)。
    觸發條件為 `visualState` 是 `sleeping_on_bed` / `sleeping_on_couch`,
    並比照 `cozyHomePass` 跳過 `pendingEvent` 的租客——被事件凍結者 `visualState`
    停在舊值,對他發夢等於憑空捏造。
  - `src/sim/narration.ts`(**最關鍵**):把 `💤` 加進 `buildNarrateCtx` 的
    `/^[🔮🌀🌱]/u` 系統回饋過濾字元類。夢境本身就是既有日誌的再敘述,
    回流當 AI 素材會讓同一件事被放大兩次(同舊日報回灌問題)。
  - `src/sim/weeklyReport.ts`:週報候選素材一併排除 `💤` 開頭的條目,
    避免純風味的夢境擠掉真實事件的週報名額。
  - `src/store.ts`:façade 補上 `dreamPass` / `resetDreamCooldown` re-export。
- **測試、build 與驗證結果**
  - 新增 `scripts/dream-test.ts`(**35 項全過**),覆蓋:第 14 天門檻、一夜一夢
    (22:00→07:00 跨 10 個小時只產生一則)、冷卻間隔決定性落在 3~4 遊戲日、
    冷卻存檔往返(reload 後不會當晚再做夢)、`pendingEvent` 不做夢、醒著不做夢、
    沙發睡死也算、記憶/關係/保底三種主題選材與佔位符代換、優先序、
    **零 RNG**(`Math.random` 呼叫數為 0)、**決定性**(兩次跑出完全相同文案)、
    以及**零數值副作用**(跑前後 mood/stress/energy/滿意度/關係值/金錢完全不變)。
    已加入 `run-all.ts` 正式回歸集(字母序插在 `drama-test.ts` 與 `eviction-test.ts` 之間)。
  - 擴充 `scripts/content-variety-test.ts`(20 項全過):夢境句庫池數、每池 ≥13 句、
    總數 ≥150、池內無重複、💤 前綴、佔位符可代換乾淨、保底池不含佔位符。
  - 擴充 `scripts/narrative-quality-test.ts`(34 項全過):斷言 `buildNarrateCtx` 的
    `todayLog` 不含 💤 條目。**順手補上既有盲點**——`/^[🔮🌀🌱]/u` 這條過濾原本
    零測試覆蓋,一併加上斷言(含「過濾沒有誤殺一般日誌」的反向驗證)。
  - `npm run typecheck`(app + worker)乾淨;`npm test` **63/63 全綠**
    (原 62 支 + 新測試,含 sim-trace 無異常);`npm run build` 成功
    (Vite **129 modules**,`dist/assets/index-DOz7BTle.js` 486.91 kB / gzip 188.24 kB)。
- **平衡快照**:**未移動、未更新**。`scripts/balance-snapshot.json` 的 `git status` 全程乾淨;
  `balance-test.ts` 在回歸集中通過。零 RNG 之外另有 `DREAM_FIRST_DAY = 14` 這道保險。
- **Review 後修正(同日第二個 commit)**
  - **必修:第 14 天全樓集體做夢**。原實作在新局(與舊存檔升級)時所有人的冷卻鍵同時
    不存在,第一個符合條件的睡眠小時會**全員一起發夢**,之後才靠 3/4 天的不等間隔
    分成兩個 cohort,長期仍會群聚——彩蛋整齊劃一就不像彩蛋。改為首次觸發時套一個
    決定性相位偏移 `dreamIndex('phase|<id>', DREAM_PHASE_SPAN=4)`,把首夢散到
    第 14~17 天。相位**只往後推**,`DREAM_FIRST_DAY = 14` 的門檻與快照零漂移不受影響;
    仍然零 RNG。實測種子房兩位租客的首夢日分別落在第 14 / 15 天。
  - `fillDream` 的代換值改走 function replacement(`() => value`)。`{m}` 來源是 AI 產生的
    記憶標籤,字面若含 `$&` / `` $` `` 會被 `String.replace` 當成 replacement pattern 展開。
    **只改 dreams.ts 自己的;community.ts 的 `fillCommunity` 不在本次範圍,未動。**
  - 檔頭註解修正:原本宣稱靠「22:00 換日」保證一夜一夢,但 `gameDayIndex` 的換日邊界
    只有在 UTC+8(設計基準時區)才剛好落在 22:00,玩家在其他時區會落進睡眠時段中間。
    實際的硬保證是 `DREAM_MIN_GAP_DAYS = 3`,22:00 換日只是次要保險。**僅改註解,
    邏輯未動**(一夜一夢本來就成立)。
  - 註解筆誤:`DREAM_THEMES` 寫「十組」實際 11 組,已更正。
  - 冷卻寫入處補註解:此鍵存的是**遊戲日序號**,與 `interactionCooldowns` 表中其他鍵的
    gameMs 單位不同(依 review 決定不改鍵名,避免無謂的存檔鍵變動)。
  - 句庫措辭:`{tier}` 會代入 `tierLabel` 的「陌生」「曖昧」等不能直接當名詞用的詞
    (原句「只是{tier}」代入後變成「只是陌生」,缺「人」字讀起來卡)。關係主題兩池的
    `{tier}` 一律改成用「」包住,任何親疏值代入都通順;`tierLabel` 未動。
  - 測試補強:`dream-test.ts` **35 → 41 項**(新增相位散開/相位決定性/相位不早於門檻、
    `$&` 跳脫兩項);`content-variety-test.ts` **20 → 22 項**(跨池無重複、`{tier}` 引號慣例)。
  - 修正後複驗:`npm run typecheck` 乾淨;`npm test` **63/63 全綠**;
    `npm run build` 成功(129 modules,`index-sDHVsCJe.js` 487.00 kB / gzip 188.28 kB);
    `scripts/balance-snapshot.json` 仍未被改動。
- **已知限制與後續**
  - 夢境**只讀不寫**:不會因為做了夢就沖淡失戀記憶或改變心情。刻意如此——
    一旦碰數值就不再是彩蛋,且必然動到 balance。
  - 租客搬走後 `dream|<id>` 冷卻鍵會殘留在存檔裡(無功能影響:鍵永遠不會再被讀到)。
    清理點在 `social.removeTenantRelations` 一帶,本次 lease 禁改該檔,故留待日後順手處理。
  - 夢境不進 AI context(這是設計要求),所以 AI 當日觀察**不會提到昨晚的夢**;
    夢只出現在租客日誌與動態 Feed。
  - 沒有做「白天小睡不做夢」的時段閘門:作息表中睡眠時段跨度很大
    (有 `0-5`、也有 `7-14`),硬加夜間窗口會讓晚睡晚起的原型永遠夢不到。
    改以「每 3~4 遊戲日一次」的冷卻控制頻率。
  - UI 未新增任何元件(夢境走既有日誌/Feed 呈現),故本次未跑 `ui:shot`;
    如需目檢需比照 floor-chain 用注入存檔跳到第 14 遊戲日以後。
- **硬規則保留**:純模板文字,零 AI 呼叫、未動 `worker/`、未擴 AI schema;
  未成年排除戀愛線、成人內容遮蔽式且預設關、AI 白名單/夾值/消毒等既有底線全部未觸及。
  夢境句庫不含露骨內容,戀愛主題池僅止於情感描寫。

### 2026-07-29 — 文件同步:`docs/交接與待辦.md` 對齊 HEAD `2f1be4e`(Claude Code)

- **目的／使用者需求**:`docs/交接與待辦.md` 的基準停在 `98fe7e5`(2026-07-27),落後 HEAD
  10 個 commit。其中月度全樓事件鏈(`cbbdfbc`)與夢境彩蛋(`3683dd1` + `2f1be4e`)都已完成
  並部署上線,文件卻仍把兩者列為「內容候選」與「實際剩餘項」,接手者第一眼就會被誤導。
- **實際變更(僅 2 個檔案,未動任何程式碼)**:`docs/交接與待辦.md` 六處——
  1. 標頭「更新至 2026-07-27」→「2026-07-29,基準 commit `2f1be4e`」。
  2. 「最新交接」段改寫為 2026-07-29 / HEAD `2f1be4e`,新增 07-27～07-29 三批
     (安居倒數三層修復、居住體驗整合、月度全樓事件鏈、夢境彩蛋)的完成摘要與最新基準
     `npm test` 63/63、Vite 129 modules、balance 零漂移;原 07-25 段落降為歷史快照
     (「最新基準」→「當時基準」),保留原數字與 HEAD `3ed6c2f`。
  3. 「目前優先候選」移除月度事件鏈與夢境彩蛋,內容候選只剩樓外世界錨點、共用區舒適度
     延伸與家具 tier 行為差異;另加一則「兩批留下的小殘留」摘要。
  4. 雙人劇情弧條目末的「下一項為租客錢包或月度事件鏈」加 2026-07-29 補註:中型深化四項
     已全數結案,該路線圖僅剩第 6 項樓外世界錨點。
  5. 第七節末「2026-07-25 對帳後的實際剩餘項」改為「2026-07-29 對帳後」,拆成工程／內容／
     J 段殘留三塊,補進本檔第四節有而該文件缺的項目(完整 `socialFulfillment`、四方向走路
     sprite、遊戲日時區議題、新手引導/Capacitor),並新增 J 段五項殘留(章節卡與滿版 feed
     堆疊間距未驗、離線跳過的抉擇不補彈、多人站位演出、`dream|<id>` 冷卻鍵殘留、夢境未跑
     `ui:shot`);已完成項目改列於「已完成,不再列為剩餘」。
  6. 第十四節末已被 2026-07-25 補註結案的四項孤兒句(週報卡片/多貓互動/cook_pair・stand_face/
     摔門絕交)加刪除線,避免仍被當成有效待辦。
- **驗證**:以 `git log --oneline 98fe7e5..HEAD` 與本檔第四節、尾端 07-28／07-29 日期紀錄
  逐項對帳;`git diff --check` 乾淨。未動程式、測試、平衡或部署設定,因此未重跑
  `npm test`／build(最近一次基準:2026-07-29 夢境彩蛋的 63/63、typecheck 乾淨、
  Vite 129 modules、balance 快照未動)。
- **保留原則**:歷史日期段落的原始判斷與驗證數字一律不改,只在其上加「歷史快照／補註」
  標記並指向現行剩餘項;未完成項目一律保留,已完成項目標 ✅ 或刪除線而不刪除,
  原編號(E-3/I-3/C-9/G-12…)與 🟢🟡🔴 慣例沿用。
- **已知限制／後續**:文件清單仍是人工維護;同步後 `docs/交接與待辦.md` 的「實際剩餘項」
  與本檔第四節逐項一致,兩份文件都寫明以日期紀錄為最終事實來源。
- **硬規則**:純文件同步,未動 `src/`、`worker/`、`scripts/`、`data/` 與任何設定檔;
  未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒等底線全數保留。

### 2026-07-29 — 樓外世界錨點:外出目的地文本 + 樓外巧遇(Claude Code)

- **目的／使用者需求**:實作第四節待辦最後一項「樓外世界錨點(🟢)」(規格見 2026-07-16
  「後續優化規劃」第 6 項)。既有 `away` 的 13 句觀察文案**全部只寫「空房間長什麼樣」**,
  沒有一句寫租客去了哪裡;外出時段因此成了整個模擬裡最沒有世界感的一段。本次補上
  (1) 依目的地分池的外出觀察文本,與 (2) 兩人同時外出時低頻成立的「樓外巧遇」。
  純模板、零 AI 成本、零新增 RNG。
- **兩段式切分(風險等級不同,刻意分開)**
  - **第一段:目的地文本(零漂移,不設閘門,立即生效)**。`away` 觀察句改從目的地句池取,
    每小時的一般日誌就會寫出他大概在哪。只換句池、不改日誌筆數、不新增 `Math.random`
    呼叫 ⇒ balance 快照零漂移(快照沒有文字欄位),因此**不需要**日數閘門。
  - **第二段:樓外巧遇(會多推日誌 + 動關係,必須設閘門)**。`OUTING_FIRST_DAY = 14`,
    理由同 `dreams.DREAM_FIRST_DAY` / `floorChain.CHAIN_FIRST_DAY`:`balance-test` 把
    `logs` 當**整數精確比對**、快照窗只有 10 遊戲日;更隱蔽的是關係值會間接改變
    `socialPass` / `interactions` / `conflicts` 的擲骰次數 → RNG 序列位移 → 全盤漂移。
- **實際變更與主要檔案**
  - 新增 `src/content/outingLines.ts`:7 個句池共 **92 句**。目的地 6 池(便利商店/早餐店/
    加班/超市/公園/夜市,各 13 句)**一律無 emoji 前綴、無佔位符**——它們走 `generate.ts`
    的一般觀察日誌路徑,那條路的句子從來沒有前綴。語氣是「房東從房內線索推測他在哪」
    (鞋櫃少了哪雙鞋、購物清單被撕走、識別證不見了),不是全知敘述:監視器看不到樓外。
    巧遇池 14 句**一律以 `🏪 ` 開頭**,佔位符 `{o}`(對方名字)、`{spot}`(目的地)。
  - 新增 `src/sim/outing.ts`(`outingSpot` / `outingSpotLines` / `outingSpotLabel` /
    `outingEncounterPass` / `resetOutingCooldown` / `outingIndex`)。設計約束寫在檔頭:
    1. **零 RNG**——選目的地、選文案、決定下次間隔一律走決定性雜湊(抄 `dreams.dreamIndex`,
       該寫法在專案內已被複製 4 次,是既有慣例;刻意**不**為此抽共用模組——抽出來要動 4 個檔)。
    2. **固定迭代次序**——判定「同時外出的人」時 `.sort((a,b) => a.tenant.id.localeCompare(b.tenant.id))`。
       `Object.values(state.runtimes)` 的順序取決於鍵插入順序、會被搬入/搬出改變,
       不排序會讓同一存檔在不同 session 巧遇到不同對象(理由同 `floorChain.presentTenants`)。
    3. **排除 `pendingEvent`**——被事件凍結的租客 `visualState` 停在舊值,看起來像在外面其實不是。
    4. **pair 級冷卻、值存遊戲日序號**——鍵 `outing|<pairKey(a,b)>`,寫在
       `state.interactionCooldowns`(persistence 已在存讀,**`SAVE_VERSION` 維持不變、
       不需改 `persistence.ts`**)。刻意**不用 gameMs 差值**:`syncToNow()` 離線補進度一次會跑
       最多 48 個 `hourlyTick`,日序號冷卻天然冪等,gameMs 短冷卻會一次刷出一整排巧遇。
    5. **相位偏移**——新局所有 pair 的冷卻鍵同時不存在,不處理的話第 14 天會**兩兩同時**
       巧遇一輪、一次刷出 C(n,2) 條日誌;用 `phase|<pairKey>` 把每對的第一次散到第 14~17 天。
    6. **一次 pass 最多成立 1 對**——即使冷卻是 pair 級,同一小時 3 人外出仍可能同時成立 3 對
       (稀疏哲學同 `floorChain`「每次 pass 最多推一話」)。
    7. **間隔 4~5 遊戲日**(`OUTING_MIN_GAP_DAYS = 4` + `outingIndex('gap|…', 2)` 的不等間隔);
       外出窗口本來就稀疏,不再設更大。**關係 +1**(對照:寵物同框 +1、週末電影夜 +2、
       洗衣房口角 −3;巧遇是「路上點個頭」,比一起看電影輕)。`importance: "notable"`。
    8. **不做「回樓才留日誌」的延遲寫入**——那要新增 runtime 欄位、處理離線補進度的懸空狀態
       與巧遇後對方退租。改成巧遇當下就對兩人各推一句、文案用回憶語氣,效果相同而狀態機乾淨。
    目的地以**時段**(邊界對齊 `generate.timeWord`)為單位穩定,不是每小時重抽,
    否則同一個下午會出現「在超市 → 在公園 → 在超市」;各目的地另有合理小時區間
    (凌晨三點不會在公園或早餐店),便利商店與加班涵蓋 0~23 確保大夜班時段也有候選。
  - `src/sim/generate.ts`:`GenCtx` 加 `outingSpot?: string`;`state === "away"` 時改從
    目的地池取句,取不到(沒帶 id 或 id 不認得)則 fallback 回 `OBSERVATION_LINES.away`。
    **仍然只有原本那一次 `Math.random()`**,只換池不換流程。
  - `src/sim/tick.ts`:(a) `applyHour` 的 `generateHourly({...})` 多傳 `outingSpot`;
    (b) `dreamPass(hour)` 之後插入 `outingEncounterPass(hour)`——**位置關鍵**,必須在
    `applyHour` 每租客迴圈全部跑完之後,全員本小時 `visualState` 才定案(同 `dreamPass` 的理由)。
  - `src/store.ts`:façade 補上 `outingEncounterPass` / `resetOutingCooldown` / `outingSpot` re-export。
  - **刻意不做的兩件事(與夢境彩蛋最大的差異)**:`🏪` **不**加進 `narration.ts` 的
    `/^[🔮🌀🌱💤]/u` 系統回饋過濾字元類,也**不**加進 `weeklyReport.ts` 的排除清單。
    該過濾的判準是「(a) 這行字來自 AI 自己,或 (b) 是既有日誌的二次敘述」——巧遇是
    **第一手世界事件**,兩條都不命中,**應該**進 AI 素材讓日記自然提到它、也該有機會上週報
    (即 `floorChain` 示範的「純模板餵給 AI 當素材」模式)。這兩個檔案本次未修改。
- **測試、build 與驗證結果**
  - 新增 `scripts/outing-test.ts`(**54 項全過**),覆蓋:零 RNG(呼叫次數精確為 0)、決定性、
    第 14 天門檻、相位偏移、pair 冷卻落在 4~5 天且不等長、一次 pass 最多 1 對、
    `pendingEvent` 不參與、**排序無關性**(打亂 `state.runtimes` 鍵插入順序後對象與文案不變)、
    **精確副作用**(關係恰好 +1 且只動該對,所有 stats/金錢不變)、save/load 冷卻還原、
    `{o}` 含 `` $& / $` `` 時不被展開成 replacement pattern、目的地穩定性與句池來源、
    fallback 路徑、稀疏度(固定兩人 40 遊戲日觸發 9 次)。
    註:新局種子租客只有兩位,單一 pair 驗不出節流/排序/相位,測試以 `makeRuntime`
    補兩位純測試租客(不進 `occupancy`,存檔區塊前先移除)。
  - `scripts/content-variety-test.ts`:加 `import * as OUTING_LINES` 整檔掃描與 9 條斷言
    (池數/每池句數/總數下限/池內無重複/跨池無重複/巧遇池 `🏪 ` 前綴與最小長度/
    佔位符代換後不殘留 `[{}]`/目的地池不含佔位符與 emoji 前綴)。30 項全過。
  - `scripts/run-all.ts`:`REGRESSION` 陣列依字母序插入 `"outing-test.ts"`。
  - `npm test` **64/64 支全過**(含 `sim-trace` 無異常),耗時 65.5s;
    `npm run build` Vite 131 modules、2.29s;`npm run typecheck`(app + worker)乾淨。
  - **平衡快照零漂移**:`balance-test.ts` 顯示「快照一致」,`git status` 中
    `scripts/balance-snapshot.json` **完全未出現**,未執行也不需要 `--update`。
    這同時實測驗證了「目的地文本換池不影響平衡」的設計前提。
  - 端對端接線驗證(臨時腳本,未入庫):跑 30 遊戲日 `hourlyTick`,113 筆 `away` 觀察日誌
    **全部**來自目的地池、0 筆落回舊空房池;另補一位 `student` 原型租客後,
    60 遊戲日內該對觸發 7 次巧遇,冷卻鍵 `outing|…` 正常寫入。
- **已知限制／後續**
  - **預設兩位種子租客永遠不會巧遇**:陳家豪(chen 作息 15-18 外出)與林小婕
    (lin_asmr 完全不外出)的 `away` 窗口沒有交集。要看到巧遇,得先招租一位外出時段
    重疊的租客(`student` 15-16、`office` 8-17、`early_riser` 6-13 之間才有重疊)。
    這是作息表既有的稀疏性,不是本功能的 bug;若日後想提高出現率,應調 `data/routines.json`
    的外出時段而不是放寬冷卻。
  - 樓層 UI 的「外出中」徽章仍只顯示狀態,**沒有**顯示目的地(需動 `.vue`,不在本次範圍)。
  - 巧遇雙方拿到的目的地取自兩人其中一位,因此另一位當下的觀察日誌可能寫著別的地點;
    這是刻意的取捨(要求兩人同地點會讓觸發率再降到 1/6),文案上讀起來仍通順。
  - 未跑 `npm run ui:shot -- rent`:本次未修改任何 `.vue`,無 UI 變更需要截圖驗證。
- **硬規則**:未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒等底線
  全數保留;巧遇只加 +1 好感,不觸碰 `setCouple` / `canRomance` 任何戀愛路徑。

### 2026-07-29 — 樓外巧遇 review 後修正:同地點條件與每日全域鎖(Claude Code)

- **目的／使用者需求**:承上一筆 `c4ee922`,review 未發現阻斷性問題(零 RNG、閘門、排序、
  冷卻語意、關係增量、句庫皆經獨立查證屬實),但指出兩個必修項與測試斷言過弱。
- **必修 1:巧遇成立條件加上「兩人目的地必須相同」**——一刀解兩個問題:
  - **文案矛盾(比原本自評的嚴重)**:`App.vue` 的單一租客日誌面板是
    `<LogFeed :entries="rt.log" />`,直接吃整條 `rt.log`**含 minor**。而每小時觀察句用
    **他自己的** spot、巧遇句原本取**兩人其中一位的** spot,兩者共用同一個 `state.gameMs`
    ⇒ **timeLabel 完全相同、相鄰兩行、約一半機率並排矛盾**(「他在超市」+「🏪 在公園碰到…」)。
    彙整 Feed 因為 `feed.ts` 濾掉 minor 而看不到,租客詳情面板卻一眼就看到。
  - **密度**:`return` 只保證每小時 1 對,`outingEncounterPass` 每個遊戲小時都被呼叫,
    原本**沒有每日全域閘門**;pair 冷卻擋得住同一對、擋不住不同對。
  - 做法:配對成立處之前比對 `outingSpot(a) !== outingSpot(b)` 就 `continue`;
    同時移除原本 `outingIndex('met|…', 2)` 隨機挑一邊地點的邏輯,改用兩人共同的 spot。
    地點一致後 `{spot}` 與雙方觀察句自然全部對齊,任何介面都不再矛盾。
- **必修 2:實測密度後追加每日全域鎖**——量測方法沿用 reviewer 的條件
  (外出窗 9-18、全員外出、50 遊戲日,量單日最大與日均):

  | 住戶數 | 修正前 | 只加同地點 | 同地點 + 每日全域鎖 |
  |---|---|---|---|
  | 4(6 pair) | 單日最大 3 / 日均 1.38 | 單日最大 3 / 日均 1.12 | **單日最大 1 / 日均 0.96** |
  | 6(15 pair) | 單日最大 6 / 日均 3.46 | 單日最大 5 / 日均 2.44 | **單日最大 1 / 日均 1.00** |
  | 8(28 pair) | 單日最大 10 / 日均 6.44 | 單日最大 7 / 日均 4.08 | **單日最大 1 / 日均 1.00** |

  (reviewer 實測基準為 4/6/8 住戶單日最大 2/5/9、8 住戶日均 4.84,與本機 harness 同一量級。)
  只加同地點時 8 住戶單日最大仍是 **7 > 3**,未達 review 給的免加鎖門檻,因此**依約追加**
  每日全域鎖:`state.interactionCooldowns['outing|__day']` 存遊戲日序號,
  在 `OUTING_FIRST_DAY` 閘門之後 `if (… === day) return;`,成立時一併寫入。
  與現有存檔格式相容(仍是 `interactionCooldowns` 的一個鍵)、零 RNG、
  鍵名不可能與 `pairKey` 相撞(`pairKey` 恆含 `|` 分隔的兩個 id)。
  加鎖後全樓一天最多一次巧遇 = 每日最多 2 則 notable,佔 `FEED_CAP = 60` 的 1/30。
  之所以敢收到這麼緊:巧遇本來就是「彩蛋級」事件,對照 `floorChain` 每月一條鏈、
  夢境每人每 3~4 日一則,一天一次已經是同級系統裡偏密的一端。
- **必修 3 + 測試連帶調整**(`scripts/outing-test.ts`,**63 項全過**,原 54 項):
  - 相位分佈改用**真實 `pairKey`**(原本用合成鍵 `t_i|t_{i+1}`,驗的不是實際行為);
    實測 6 對的相位為 `[2,1,0,0,3,2]`,涵蓋 4 個值中的 4 個。
  - 相位斷言從 `new Set(firstDays).size >= 2` 改為**分佈斷言**「單日首次巧遇數 ≤ 總 pair 數
    的一半」——原斷言在「N-1 對擠同一天 + 1 對落另一天」的雜湊退化下仍會綠燈。
  - 間隔斷言從「一律 4~5 天」改為「**不短於 `OUTING_MIN_GAP_DAYS`**」並**保留上界**
    (`GAP_SANE_MAX = 12`,超過即視為設計上幾乎不會再發生),另加一條「仍有間隔落在純冷卻
    下限帶 4~5 天」證明同地點條件沒有拖垮節奏。實測固定兩人 41 遊戲日觸發 8 次,
    間隔 `[5,4,4,7,4,5,6]`(下限 4、上限 7)。
  - **稀疏度斷言在新條件下仍成立**:原「40 遊戲日內至少觸發若干次」實測 8 次,
    未因同地點條件變得過稀,門檻設為 `>= 6` 留餘裕,**未放寬到形同虛設**。
  - 因為「哪一對在哪個小時成立」已不可用寫死索引預測,新增 `probeFire()` 掃出真正成立的
    `(day, offset)` 再斷言,所有相關區塊改用它,不再假設是 `sortedIds[0..1]`。
  - 新增斷言:同地點(兩位的 `outingSpot` 相同、文案地點就是共同目的地)、每日全域鎖
    (成立當天其餘 23 小時不再有第二對、全員外出 31 天每天都只成立 1 對、鎖入存檔並還原)。
- **測試、build 與驗證結果**:`npx tsx scripts/outing-test.ts` **63 通過 / 0 失敗**;
  `npm test` **64/64 全過**(64.8s);`npm run build` Vite 131 modules、2.20s;
  `npm run typecheck`(app + worker)乾淨。`scripts/balance-snapshot.json` 全程未出現在
  `git status`,平衡快照零漂移(巧遇仍在第 14 天後才觸發,目的地文本仍不改 RNG 次數)。
- **已知限制／後續**
  - 上一筆日誌記的「預設兩位種子租客永遠不會巧遇」仍然成立(陳家豪 15-18 外出、
    林小婕完全不外出,`away` 窗口無交集),且加上同地點條件後條件更嚴;要看到巧遇仍需
    先招租外出時段重疊的租客。
  - 密度收到「全樓一天最多一次」後,住戶多時**個別 pair 的等待會變長**(28 pair 時
    平均每對約 28 天輪到一次)。這是刻意的取捨:Feed 不被洗版優先於單對頻率。
    若日後覺得太稀,應優先放寬每日全域鎖(例如改成每日 2 次)而不是拿掉同地點條件——
    同地點條件同時扛著文案一致性,拿掉會讓詳情面板重新出現矛盾。
  - 退租者的 `outing|<pairKey>` 冷卻鍵殘留:與 `dream|<id>` 相同的既有問題,非本次引入,
    依 review 指示跳過。
  - 未跑 `npm run ui:shot -- rent`:本次仍未修改任何 `.vue`。
- **硬規則**:未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒等底線
  全數保留;未修改 `scripts/balance-snapshot.json`、`src/sim/narration.ts`、
  `src/sim/weeklyReport.ts` 與任何 `.vue`。

### 2026-07-29 — 家具品質 tier 接上行為差異:舒適度第三個加項(Claude Code)

- **目的／使用者需求**:`src/furniture/catalog.ts` 的 tier 自 2026-07-24(U-2)以來自承是
  「純標示」——55 件家具標了 45 個 tier,但 `src/sim/**` 底下零引用,只在 `FurnitureShop.vue`
  與 `FurnitureInfo.vue` 畫星星(且 `TIER_INFO` 兩處重複定義)。玩家花更多錢買高 tier,
  數值上什麼都拿不到。本次把 tier 接上實際模擬數值。
- **實際變更與主要檔案**
  - **新增 `src/furniture/tier.ts`(單一資料來源)**:`TIER_POINTS = { budget: 0, standard: 0.5,
    premium: 1.5 }`、`TIER_INFO`(星數/中文,從兩個 SFC 抽上來)、`tierOf()`/`tierPoints()`
    (含 `?? "standard"` fallback)、`tierChipText()`。**係數與 UI 文案住同一個檔**,避免改了
    係數忘了改文案。檔頭比照 `src/sim/dreams.ts` 寫明「零 RNG」約束。
  - **`src/sim/comfort.ts`**:`roomComfortBreakdown` 新增第三個加項 `tierPart`,
    `comfort = clamp((attrPart + categoryPart + tierPart) * cleanMult, 0, 100)`。tierPart 在
    `cleanMult` **之前**相加(線性、可手算、面板列得出來;乘法會放大 cleanMult 變異)。
    新增私有 `roomTierPoints(roomId)` 自己遍歷 placements 並 **clamp 到 0~12**(`TIER_MAX`,
    防狂塞 premium 小物刷分),形狀照抄同檔 `roomCategories()`/`roomCleanPower()`。
    `COMFORT_LIMITS` 同步新增 `tierMax`。
  - **刻意不改 `src/sim/placements.ts` 的 `roomAttributes()`**:它同時餵 comfort /
    `acoustics.noiseComplaintEligible` / `recruit.matchStars`(→ 租金),動它會連鎖三個系統,
    而快照的 `rent` 是整數精確比對。tier 邏輯全寫在 `comfort.ts` 內部。
  - **採「獨立加分項」而非「attributes 乘子」**:約 1/3 的目錄家具 attributes 是空的
    (`single_bed`、`folding_bed`、`plastic_stool`、`stove`、`toilet`、`shower`、`laundry_washer` 等),
    乘子對它們是 0×n=0 的空砲。獨立加分才能讓零屬性家具也有 tier 差——這是本功能的核心驗收點。
  - **fallback 是 `standard` 不是 `budget`**:5 件畢業生紀念物(`catalog.ts` price 0)與
    `UNKNOWN_DEF` 沒標 tier,它們是「畢業生留下的禮物、純裝飾」,fallback 到 budget 會讓
    紀念物變成相對扣分,語意完全相反。已用測試釘住。
  - **UI 三處**:`src/App.vue` 的 `comfortParts` computed + 房間品質拆解面板新增第四列
    「✨ 品質」(含 `.cbd-tier` CSS 與 `tierMax` 進度條;品質是 0.5 的倍數 → 保留一位小數,
    小計改成三部分相加才對得起來);`FurnitureShop.vue` / `FurnitureInfo.vue` 改 import 共用的
    `tierChipText()`,tier chip **內含數值**(例 `★★★ 精品 +1.5`)而不是新增一個 chip——
    390px 下 `.chips` 已很擠,內含數值是零額外寬度。兩處重複的 `TIER_INFO` 一併刪除。
  - **註解／文件同步**:`catalog.ts` 的「純標示用,不影響任何模擬數值」與欄位註解改寫成
    實情;`docs/交接與待辦.md`「tier 目前只作 UI 標示」同步;本檔待辦打勾。
- **測試**
  - 新增 `scripts/furniture-tier-test.ts` **24 通過 / 0 失敗**,並**已加進 `scripts/run-all.ts`
    的 `REGRESSION`**(字母序,`furniture-art` 之後)——不加就不會被 `npm test` 跑到。
    涵蓋:TIER_POINTS 值域/standard 中性、未標 tier 與 `UNKNOWN_DEF` fallback standard、
    **同型三階遞增**(single 0 < double 0.5 < canopy 1.5,舒適 6.00 < 24.64 < 32.88)、
    **零屬性家具也有 tier 差**(曬衣架 budget vs 淋浴間 standard,同 utility、attributes 皆空 →
    attrPart/categoryPart 相同,舒適度差額剛好 = `TIER_POINTS.standard − budget`)、
    上限夾值(20 件 premium → 30 夾成 12)、空房 tierPart 0、`roomId === null` 中性、
    `COMFORT_LIMITS.tierMax` 與實測上限一致、加法恆等式、**零 RNG**(覆寫 `Math.random`
    計數,實測 0 次)+ 決定性。
  - 擴充 `scripts/comfort-test.ts`(35 → **38 通過**):加法恆等式改成三項;上限常數斷言加
    `tierMax`;空房/中性拆解加 `tierPart === 0`;新增三項(品質進入總分、品質不隨整潔改變
    〔證明是加項不是乘子〕、空房仍為 0)。
  - 擴充 `scripts/data-catalog-test.ts`(39 → **42 通過**):把「純標示」的敘述改成
    「tier 必須全部可解析成係數」,加三項(每件都解析得出合法係數、未標的 5 件 fallback
    standard 而非 budget、TIER_POINTS 三階遞增)。
- **測試/build 結果**:`npx tsx scripts/furniture-tier-test.ts` **24/24**;`npm test`
  **65/65 全過**(65.8s;比上次 64/64 多的就是新測試);`npm run build` Vite 132 modules、
  2.28s 成功;`npm run typecheck`(app + worker)乾淨。
- **平衡快照:本次刻意調整,已 `--update`**。逐欄實測(左=舊基準、右=本次):
  - `money` **59919 → 59921(+2)**、`tenant_chen_engineer.wallet` **23821 → 23819(−2)**
    ——同一筆 $2 從租客錢包移到房東。來源是 `economy.collectRent()` 的
    `willing = Math.round(daily * factor)`,`factor` 含 `(satisfaction − 50) * 0.2`;
    滿意度微升讓某幾天的日租進位多 $1。**不是 bug,是本功能唯一且最小的整數外溢**;
    未觸碰 `recruit.ts`/`tenancy.ts`,`rent`(月租)完全沒動。
  - 浮點欄位全部**方向正確且落在既有 ±0.6 容差內**(所以 balance-test 沒把它們列為差異):
    陳家豪 mood 94.9→95.0、stress 38.7→38.6、wellbeing 72.3→72.4、satisfaction 72.9→73.0;
    林小婕 stress 8.8→8.7、wellbeing 79.5→79.6、satisfaction 89.8→89.9
    (mood 99.5、affinity 79.6 已到頂/不變)。
  - **完全沒動的欄位**:`ledgerCount` 40、`decisions` 0、`tenantCount` 2、兩人的 `rent`
    (14000 / 16500)、`arrears` 0、`logs` 60、`memories` 3 / 2、林小婕 `wallet` 26950。
  - 種子房 tierPart 實測:**r301 = 2.5、r302 = 3.0、r303 / r304 = 0**(只有一張 budget 單人床)。
- **`cozyHomePass` 門檻實測(comfort ≥ 52 且 clean ≥ 80)**:用同種子(20260710)跑 240 小時的
  探針比對「有無 tierPart」——**🏡 慶祝日誌實際筆數兩邊都是 1 筆,同一位租客、同一個 `gameMs`,
  沒有房間新跨過門檻**。逐小時取樣確實有 8 個小時出現門檻翻轉(林小婕 clean≈83:舊 51.7 <52、
  新 54.4 ≥52),但 `cozyHomePass` 只在換日那一刻跑且有 3 遊戲日冷卻,那個窗口沒落在換日點,
  所以沒有多出慶祝。**注意 `logs: 60` 已在 `LOG_CAP = 60` 飽和,不能拿來當「沒多一筆日誌」的
  證據**——本次是用探針直接數 🏡 筆數確認的。
  另:2026-07-24 U-1 記過「慶祝門檻是依現行家具經濟(單房舒適度上限約 56)抓的」;
  **tierPart 把單房上限推到約 68(60+30+12 經整潔乘子前為 102,實務種子房約 59),
  門檻 52 的相對位置變低了**——現在比較容易達成,日後若再擴充高屬性家具應一併重估門檻。
- **UI 驗證**:從工作區根 `npm run ui:shot -- rent`,**18 張截圖、report.json 三個 viewport 皆
  `errors: []` / `warnings: []`**。預設流程不涵蓋商店浮層與房間拆解面板(U-3 記過的缺口),
  另用一次性 Playwright 流程(放在 gitignore 的 `artifacts/` 下、跑完刪除,**未新增任何受追蹤
  檔案**)在 390 / 412 / 430 三種寬度各補 4 張:商店浮層上/中/下 + 展開「查看診斷細項」後的
  舒適度面板。量測結果三種寬度一致:**54 件商品、49 個 tier chip、名稱列換行 0 筆、
  tier chip 與購買鍵重疊 0 筆、文件與浮層橫向溢出皆 0px**;最長品名「淋浴間(玻璃隔間)
  ★★ 標準 +0.5」在 390px 仍單行不擠到 $9,000 按鈕。舒適度面板實測四列
  **家具屬性 30/60、種類齊全 24/30、✨ 品質 2.5/12、🧹 整潔倍率 ×0.68 → 小計 56.5 → 38**,
  30 + 24 + 2.5 = 56.5 加法對得起來,且 2.5 正是 r301 的 tierPart。
- **已知限制／後續**
  - tier 只接舒適度,**未接招租星等、租金、損壞率、睡眠效率**(lease 禁止清單)。睡眠效率是
    主題最貼切的方案,但種子房兩人都睡 budget 的 `single_bed`,energy 是連鎖起點會讓快照大幅
    重排,留第二階段。
  - `TIER_MAX = 12` 對單房是寬鬆上限(約 8 件精品滿檔),一般房間拿 2~5 分;若日後家具數量
    大幅增加,可能需要重估夾值。
  - 共用區(交誼廳/浴室/洗衣間)的家具 tier 目前不進任何房間的 tierPart(`roomTierPoints` 只看
    `p.room === roomId`),與「共用區舒適度延伸」待辦一起處理較合理。
- **硬規則保留**:未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒等底線
  全數保留;本次為純數值/UI 改動,`tier.ts` 與 `comfort.ts` 的新程式碼**零 RNG**(有測試把關)。

### 2026-07-29 — 家具 tier review 後修正:上限收到 100 與「有分就有標示」(Claude Code)

承接上一筆(`92eeff3`)的 review 意見,同 branch `feat/furniture-tier` 收尾。

- **必修 1:舒適度上限突破 100,面板加法可能對不起來(採 (a) 降 `TIER_MAX`)**
  - **問題**:改動前理論上限是 `60 + 30 = 90`,所以 `roomComfortBreakdown` 的
    `clamp(…, 0, 100)` **從不生效**;加了 `tierPart` 之後上限變成 `60 + 30 + 12 = 102`,
    滿配房在高整潔下會出現「小計 × 倍率 = 102」但「房間品質」顯示 100 的情形,
    四列加起來對不上總分——與加第四列的初衷(`comfort.ts:45-48`:面板不能自訂數字、
    要能對得起來)直接衝突。
  - **修法**:`TIER_MAX` **12 → 10**,三個加項總上限剛好 `60 + 30 + 10 = 100`,clamp 永遠不生效。
    理由:(1) 12 本來就沒有特殊意義,降到 10 = 約 6~7 件精品滿檔,對一般房間(拿 2~5 分)
    完全無感,種子房 r301 2.5 / r302 3.0 更是遠低於上限;(2) 相較於「面板顯示時特別處理夾值」
    (方案 b),把不變量做進常數比在 UI 補救更難退化——UI 補救只要有人漏改一處就破功,
    而常數層的約束有測試直接把關;(3) `attrPart` 是過飽和函式,**永遠取不到 60**
    (實測狂堆 200 件帷幔床 = 59.60),所以總分實務上連 100 都碰不到,不是「剛好卡在邊界」。
    已把「改動任何上限都必須維持 `attrMax + categoryMax + tierMax ≤ 100`」寫進 `comfort.ts` 註解。
  - **新增測試**(依 review 要求):滿配房(五類全齊 + 200 件 premium 帷幔床 + 整潔 100)
    實測 `attr 59.60 + cat 30 + tier 10 = 99.60 ≤ 100`,且 `comfort === 三項相加`
    (差 < 1e-9)、`comfort < 100` → **證明 clamp 永遠不生效、面板加法恆等**;
    另在 `comfort-test.ts` 加一條常數層斷言 `attrMax + categoryMax + tierMax ≤ 100`。
- **必修 2:未標 tier 的家具有分卻沒 chip**
  - `FurnitureShop.vue` 與 `FurnitureInfo.vue` 原本用 `v-if="def.tier"`,導致 5 件紀念物
    fallback 拿到 standard 的 **+0.5 卻不顯示任何品質標示**,玩家看不出這 0.5 從哪來。
  - 改成一律用 `tierOf(def)` 渲染(class 與文字都走同一個 helper)。實測商店的 tier chip
    數由 **49 → 54**,`itemsWithoutTierChip: 0`,補上的 5 件正是紀念物。
- **註解補充**(review 第 4 點):`roomTierPoints` 註明「兩個加項的來源集合刻意不同」——
  `roomAttributes()` 會疊 `upgradeAttributes(roomId)`(改建升級的永久加成),但 tierPart
  **只算家具擺放**,因為房間升級沒有 tier 的概念,硬給等級只會是憑空捏造的數字。
- **驗證**:`npx tsx scripts/furniture-tier-test.ts` **28 通過 / 0 失敗**(24 → 28);
  `scripts/comfort-test.ts` **39 通過**(38 → 39);`data-catalog-test.ts` 42 通過;
  `npx tsx scripts/balance-test.ts`(**未加 --update**)→ **✅ 快照一致**
  (種子房 tierPart 2.5 / 3.0 遠低於新舊上限,降 `TIER_MAX` 對種子局零影響,如預期);
  `npm test` **65/65 全過**(65.6s);`npm run build` 132 modules、2.23s;
  `npm run typecheck`(app + worker)乾淨。
- **UI 驗證**:`npm run ui:shot -- rent` 18 張、三個 viewport 全 `errors: []`;
  自訂流程(同樣放 gitignore 的 `artifacts/` 下、跑完刪除)三種寬度各補商店 3 張 +
  舒適度面板 1 張 + 家具資訊卡 1 張。實測:商店 **54 件 / 54 chip / 未帶 chip 0 件 /
  名稱列換行 0 筆 / chip 與購買鍵重疊 0 筆 / 橫向溢出 0px**,390px 下 5 件紀念物的
  「★★ 標準 +0.5」皆單行且不擠到 $0 按鈕;資訊卡(單人床)chip「★ 平價 +0」單行、
  chips 溢出 0px、操作鈕未被遮;舒適度面板第四列已變成 **✨ 品質 2.5/10**,
  小計 56.5 → 38(30 + 24 + 2.5 = 56.5,加法仍對得起來)。

#### 本次一併記錄的待辦與風險(review 指出,程式未改)

- [ ] **`logs` 欄位是 balance 快照的既有盲點**:`LOG_CAP = 60`,快照兩位租客都是 60
      **已飽和**,對「多推一筆日誌」**永久失去偵測力**(本次就是靠自製探針直接數 🏡 筆數
      才確認沒多慶祝)。相對地 `ledgerCount: 40` 未飽和仍有效。建議快照改存
      「特定 emoji 前綴的計數」或日誌內容雜湊,取代會飽和的 `logs` 長度。
- [ ] **快照缺 `rentPaidTotal`**:本次 ±$2 要靠反推錢包遞推式才能歸因。建議加一個
      rent 金額總和欄位,讓金錢漂移可以直接歸因到收租而不是靠推理。
- [ ] **`cozyHome` 門檻目前是「運氣安全」不是「結構安全」**:反解翻轉窗口寬達
      **約 9.3 個整潔點**——舊公式要 `clean ≥ 84.1` 才過 `comfort ≥ 52`,加了 tierPart 後
      只要 `clean ≥ 74.8`(實際仍受 `COZY_CLEAN = 80` 這道門檻限制)。**林小婕日間整潔 ≈ 83
      正落在窗口正中央**,這次沒跨過純粹是因為 `cozyHomePass` 在換日那一刻跑,而換日正是
      她整潔的每日低點。**任何墊高午夜整潔的改動(多一台掃地機、`cleanlinessBaseline` 微調、
      加收納家具)都會讓它翻過去。** 好消息:翻過去會讓 mood +2 遠超 ±0.6 容差 →
      `balance-test` 會紅燈,屬**可偵測的脆弱**而非靜默地雷。第二階段重估 `COZY_COMFORT`
      時直接用這三個數字:**74.8 / 84.1 / 實際 83**。
- [x] **(本次 UI 驗證新發現)畢業生紀念物在商店以 $0 上架** ✅ **已於 2026-07-29 第三批修掉**
      (中控裁示採方案 a,詳見下一筆日誌)。原況:`FurnitureShop.vue` 的 `groups` 只濾掉
      `placement === "wall"`,5 件 price 0 的紀念物因此出現在「氛圍」分類、可用 $0 購買;
      tier 接上舒適度後每件免費 +0.5,塞滿就能零成本把 `tierPart` 拉到上限。
- **硬規則保留**:未成年排除戀愛線、成人內容遮蔽式且預設關、AI 白名單/夾值/消毒全數保留;
  本次仍為純數值/UI 改動,零 RNG(測試把關)。

### 2026-07-29 — 家具 tier 第三批:畢業生紀念物從商店下架(Claude Code)

承接 `8504345` 的 review 裁示,同 branch `feat/furniture-tier`。上一筆日誌把
「紀念物在商店以 $0 上架」記成待辦,中控裁示**採方案 (a)、合併前修掉不留待辦**,理由:
這是本次改動造成的 regression(改動前 $0 買到的是零屬性純裝飾、無害;改動後每件免費 +0.5,
20 件拉滿 `tierPart` = 10,直接推翻「花更多錢買高 tier」的前提),不能把自己引入的漏洞
當既有問題留給下一個人;而且從源頭修比加特判乾淨——方案 (b) 讓紀念物 `tierPoints` 特判為 0
會讓 chip 顯示「★★ 標準 +0.5」但實際給 0,正是剛修掉的「顯示與計算脫鉤」。

- **實際變更**:`src/components/FurnitureShop.vue` 的 `groups` 過濾條件加一行
  `if (d.price <= 0) continue;`(非賣品不上架)。
- **判準選擇:`price <= 0`(非賣品),不是 `id.startsWith("memorial_")`**
  - `FurnitureDef` 上**沒有** memorial 欄位——那面旗標掛在 `Placement`(`floor/map.ts:135`),
    由 `tenancy.placeMemorial()` 寫入;而 `MEMORIAL_DEF` 是 `tenancy.ts` 的 module-private const
    (未 export,且 `tenancy.ts` 在本次 lease 的禁止修改清單)。**def 層唯一可用的訊號就是價格。**
  - 「非賣品不上架」是關於**商店本身**的通則(商店是花錢換東西的地方,沒有標價的東西
    本來就不該在貨架上),未來若再有其他贈品/獎勵型家具也會自動擋掉,比綁 id 前綴穩健。
  - 判準過寬的風險(「未來出現應該可購買的 $0 家具」)已用**反向斷言轉成 tripwire**:
    測試釘住「被擋掉的**正好**是那 5 件紀念物」,日後多出任何 $0 家具都會紅燈,
    強迫做一次明確決定,而不是被 price 判準靜默擋掉。
  - 現況兩種判準選到的完全是同一組:`price <= 0` 與 `memorial_` 前綴都正好是那 5 件,
    且最便宜的付費品是塑膠椅凳 $400,中間有明顯間隔,不是險勝。
- **只擋購買入口,沒有誤傷畢業生回饋**(依裁示):紀念物本身的 tier fallback(standard、+0.5)
  與已擺放/存檔中的紀念物完全不動——畢業生留下的紀念物給舒適度加分是設計意圖不是漏洞。
- **新增測試**(依裁示兩條都加)
  - `data-catalog-test.ts`(42 → **46 通過**):商店清單不含任何紀念物(上架 49 件)、
    不含任何 `price <= 0` 品項、**被擋掉的正好是那 5 件紀念物**(tripwire)、
    紀念物都是 price 0 且最便宜的上架品仍有價($400)。
  - `furniture-tier-test.ts`(28 → **31 通過**):紀念物**仍可被擺放**且照常計入
    (5 件 × 0.5 = tierPart 2.5)、`tierOf` 仍是 standard 且 `tierPoints > 0`、
    擺了紀念物的房舒適度確實 > 0(8.50)——確保只擋購買、沒誤傷畢業生回饋。
- **驗證**:`npx tsx scripts/furniture-tier-test.ts` **31 通過 / 0 失敗**;
  `data-catalog-test.ts` **46 通過**;`npx tsx scripts/balance-test.ts`(**未加 --update**)
  → **✅ 快照一致**(商店上架清單不進任何模擬,種子房擺放完全沒變);`npm test` **65/65 全過**
  (66.2s);`npm run build` 132 modules、2.21s;`npm run typecheck` 乾淨。
- **`scripts/shop-test.ts` 手動跑過**(它不在 REGRESSION 白名單):輸出正常——買電競桌/電視/
  盆栽三件成功、金錢 52000→10500、屬性累加 `{tech:14,noise:2,style:6,cozy:6}`、錢不夠時
  「正確擋下:金錢不足」、`findFreeSlot` 回 `{c:1,r:18}`。**本次改動不影響它**:它走的是
  `store.buyFurniture()`,而我改的是 SFC 的 `groups` 上架清單(兩者不同層)。
  **判斷:不建議就這樣併進白名單**——它是早期煙霧腳本,**零斷言、只 console.log**,
  `run-all.ts` 的判定條件(exit code ≠ 0 或「N 通過 / M 失敗」中 M > 0)對它幾乎不會紅燈,
  只有硬拋例外才抓得到,加進去等於多跑一支不會失敗的腳本、徒增 CI 時間與虛假安全感。
  要納入應**先改寫成 `check()` + 「結果:N 通過 / M 失敗」+ `exit 1` 的斷言形式**
  (且它會把 `state.money` 改成 100,改寫時要確認不影響同進程其他斷言)——那是獨立的
  測試債清理任務,超出本次 lease,已於下方留待辦。
- **UI 驗證**:`npm run ui:shot -- rent` 18 張、三個 viewport 全 `errors: []`;自訂流程
  (同樣放 gitignore 的 `artifacts/` 下、跑完刪除)三種寬度各截商店 3 張。實測三寬度一致:
  **上架 49 件(54 → 49,少的正是 5 件紀念物)、tier chip 49 個、未帶 chip 0 件、
  `$0` 購買鍵 0 個、清單中殘留的紀念物 0 件、空分類 0 個(8 個分類全都還有商品)、
  名稱列換行 0 筆、chip 與購買鍵重疊 0 筆、橫向溢出 0px**。氛圍分類版面銜接乾淨
  (裸燈泡 → 大地毯 → 角落盆栽 → 浴室小盆栽 → 機能),沒有因為少 5 件而留下空洞或空標題。
- [ ] **(新留待辦)`scripts/shop-test.ts` 是零斷言的早期煙霧腳本**:應改寫成斷言形式後
      再考慮併入 `run-all.ts` 的 REGRESSION;現況直接併入只會多一支不會紅燈的腳本。
- **硬規則保留**:未成年排除戀愛線、成人內容遮蔽式且預設關、AI 白名單/夾值/消毒全數保留;
  本次僅動商店上架清單(純 UI 過濾),不碰任何模擬數值,零 RNG。

### 2026-07-29 — 電視／椅子像素美術第一批：修掉壁爐錯圖並建立七件專屬造型(Codex)

- **目的／使用者需求**：使用者指出多款電視與椅子顯示成奇怪美術，要求開始優化。盤點後確認不只是
  風格問題：`tv_console` 與 `lounge_tv` 在正式瀏覽器載入 LimeZu atlas 後，實際會取到兩張
  **壁爐／煙囪素材**，而既有測試只驗 frame 座標與程序 fallback，因而長期全綠卻在線上畫錯。
  另外三款 TV 共用同一 fallback、塑膠椅凳會畫成木製高背椅，兩種沙發也缺少用途辨識度。
- **實際變更與主要檔案**：
  - `src/furniture/render.ts` 在 atlas 前新增精確到 furniture id 的程序美術 override，讓正式畫面與
    atlas 未載入 fallback 永遠使用同一套像素圖。共完成七件：房內 `tv_console`（薄螢幕＋白色
    直立主機＋控制器）、`lounge_tv`（滿版螢幕＋聲霸＋遙控器）、`lounge_console`
    （雙色感應條＋兩支體感手把）、`wood_chair`（鏤空直條椅背）、`plastic_stool`
    （藍綠一體成形矮凳）、`loveseat`（酒紅雙座＋雙色抱枕）、`shared_sofa`
    （藍綠三座＋暖色抱枕）。全部維持原 footprint、碰撞、互動點與旋轉契約。
  - `src/art/limezu.ts` 從 runtime atlas 映射移除 `tv_console`、`lounge_tv`、`shared_sofa`、
    `loveseat`、`wood_chair` 五個已被專屬程序圖取代的 frame。原始 atlas PNG／manifest 保留為
    未使用素材，不重建付費素材檔；任何未來直接呼叫 `tryDrawLimezuFurniture` 也不會再把壁爐畫成 TV。
  - `scripts/furniture-art-test.ts` 由 46 擴成 **90/90**：七件全部驗程序輸出、整數像素、
    footprint 畫框、90/180/270° 安全 transform，另以像素指紋鎖住三款 TV 配件差異、
    木／塑膠材質差異與雙／三座沙發輪廓。
  - `scripts/limezu-art-test.ts` 由 64 擴成 **79/79**：runtime 白名單縮成 18 件、
    錯誤 TV frame 無法再畫、五件舊 frame 確實不在 runtime 映射、atlas 已載入時七件仍固定走
    專屬程序圖，同時確認 `gaming_desk` 等其餘核准家具仍走 atlas、不受影響。
- **測試／build／人工驗證**：
  - `npx tsx scripts/furniture-art-test.ts`：**90/90**。
  - `npx tsx scripts/limezu-art-test.ts`：**79/79**。
  - `npm test`：**65/65 支全過**；`npm run typecheck`（app＋worker）乾淨；
    `npm run build`：Vite 132 modules、2.42s 成功；`git diff --check` 通過。
  - 用 `scripts/render-floor.ts` 產完整樓層 PNG，另以只存在於 ignored `artifacts/` 的一次性
    placement 腳本把七件排入 303／304 做 3× nearest-neighbor 圖集；逐件目視確認螢幕比例、
    主機／聲霸／感應器、椅背／凳腳、座位分界與抱枕皆可辨識，且開場樓層的房內 TV、
    大廳 TV、三人沙發與周邊家具無重疊。一次性腳本已刪除，PNG 不進版控。
  - Codex in-app Browser 在本次環境沒有可用 browser backend，故未以它操作；視覺驗證改由
    專案既有離線完整樓層 renderer 完成。
  - 取得使用者授權接管部署後，從工作區根暫時把 UI Lab 的 rent path 指向本 worktree，
    執行 `npm run ui:shot -- rent`：**18 張截圖、三個 viewport 全 `errors: []` /
    `warnings: []`**；已逐張檢查樓層／動態／收支的 viewport 與 fullpage PNG，房內 TV、
    大廳 TV、共用沙發在 390/412/430px 均清楚且無重疊，其他頁面沒有版面回歸。
    `.ui-lab/projects.json` 已立即還原為主 checkout 路徑，未納入專案提交。
- **平衡快照**：**未移動**。本次不改 `catalog.ts`、placement、價格、屬性、tier、舒適度、
  碰撞或任何 `src/sim/**`；`balance-test` 已隨全套回歸確認一致。
- **已知限制／後續**：
  - atlas PNG 與 manifest 仍含五張不再使用的舊 crop，刻意不重建付費素材管線；runtime 型別與
    frame map 已移除，產品不會載入它們。日後重建 atlas 時可順便清理未使用區。
  - 本批聚焦 catalog 家具；角色坐在桌前／電視前時由 `floorScene.ts` 臨時補畫的棕色活動椅仍是
    單一造型，可列入第二批，依電競桌／一般桌／交誼廳做不同椅背。
- **硬規則保留**：純美術與測試改動；未成年戀愛線、成人內容遮蔽預設關、AI 白名單／夾值／
  消毒與所有模擬安全規則均未觸及。

### 2026-07-30 — 人物美術第一批：共用骨架、臉部、髮型配件與躺姿身份（Codex）

- **目標**：優化所有資料驅動人物（包含 AI 產生人物）共用的像素美術；不改人物生成 schema、數值、模擬或存檔格式。
- **共用人物精靈**（`src/pixel/sprites.ts`）：
  - 重畫 11px 寬站姿、坐姿與走路雙幀，改善頭部輪廓、下巴、雙眼與嘴部表情。
  - 坐姿改為手放腿上與彎腿輪廓；走路 A/B 同時擺動手臂與腿，不再只平移下半身。
  - 新增 `m` 臉部暖色 token，所有既有角色色票仍相容。
- **髮型與配件**（`src/pixel/parts.ts`）：
  - 長髮、馬尾、刺蝟頭、鮑伯頭改為更清楚的外輪廓，並加入同髮色高光。
  - 一般眼鏡由連續黑帶改為分離鏡框；圓框眼鏡改為深色框，避免像白色面罩。
  - 保留原本 5 種髮型、6 種配件與 Appearance schema，舊存檔不需遷移。
- **各 renderer 身份一致性**：
  - `src/pixel/scene.ts` 的房間特寫正面站／坐姿現在會疊加人物髮型與配件。
  - `src/floor/floorScene.ts` 的無 agent 預覽 fallback 也會疊加外觀。
  - 樓層躺姿依髮型與配件補上可辨識輪廓，並沿用既有 0/90/180/270 旋轉座標。
- **回歸測試**（`scripts/appearance-test.ts`）：
  - 11 → **18 項通過**：新增垂直對位、全姿勢尺寸與高度、token 合法性、臉部雙眼／嘴、走路手腳雙區差異、5 髮型與 6 配件輪廓唯一性。
- **完整驗證**：
  - `npm test`：**65/65 支通過**（`appearance-test.ts` 18/18）。
  - `npm run typecheck`：app + worker 通過。
  - `npm run build`：Vite 132 modules，正式建置通過（既有 >500kB chunk 警告不變）。
  - `git diff --check`：通過。
- **視覺驗證**：
  - `scripts/render-preview.ts` 與 `scripts/render-floor.ts` 產圖人工檢查通過。
  - UI Lab `npm run ui:shot -- rent`：390×844、412×915、430×932 共 **18 張**，`errors: []`、`warnings: []`；逐張檢查人物比例、遮擋與版面，未見穿模或溢位。產物維持在 ignored `artifacts/`。
- **刻意不納入本批**：AI 色碼對比安全化、四方向人物、舊房間 renderer 的背面／躺姿外觀專用圖層；這些會牽涉生成規則、方向狀態或另一套橫向精靈，應另開小批。
  （✅ **後續**：其中「AI 色碼對比安全化」已於 **2026-07-30** 另開小批完成，見同日「AI 色碼對比安全化」日期紀錄；其餘兩條仍未動工。）
### 2026-07-30 — 共用區舒適度延伸:交誼廳/浴室/洗衣間影響全體租客(Claude Code)

- **目的/使用者需求**:共用區(`lounge`/`bathroom`/`laundry`)的家具**一分錢都不進模擬**——
  `roomComfort` 的兩個 sim 呼叫點都由 `roomOfTenant()` 驅動,而它只查 `state.occupancy`
  (r301~r304)與 `state.cohabits`,**永遠不會回傳共用區 id**。玩家花錢佈置公共空間毫無後果。
- **🔴 走獨立管道,刻意不把共用區分數塞進 `roomComfort`**(本次最重要的架構決定):
  新增 `communalQuality()` → `communalBaselineDelta(q)`,在 `tick.ts` 疊加到既有 baseline 上。
  理由是 `cozyHomePass`(`tick.ts:602`)讀的就是 `roomComfort(roomId, rt.cleanliness)`——
  上次 review 反解出 r302 的 52 門檻翻轉窗口是 clean ∈ [74.9, 84.2],而日間實際 ≈83
  **正落在窗口正中央**;只要碰 `roomComfort` 就幾乎必翻,多推一次慶祝日誌 → mood +2 /
  satisfaction +1 遠超 balance 的 ±0.6 容差。獨立管道讓這個風險歸零。
  同理**沒動 `COMFORT_BUCKETS` / `COMFORT_LIMITS`**:把 `utility` 加成第六個 bucket 會讓
  `categoryMax` 30→36、總上限破 100 的不變量,而且 `robot_vacuum` 是 utility,會連帶
  改變**所有私人房**的分數。共用區用自己的 `COMMUNAL_LIMITS`,自己維持自己的 ≤ 100。
- **為何不能沿用私人房那套 bucket**:`COMFORT_BUCKETS` 只涵蓋 7 種 category,但 `FurnCategory`
  有 8 種——**`utility` 落單不在任何 bucket 裡**。而浴室 3/4 件、洗衣間 5/5 件全是 utility
  且 attributes 全空。舊公式實測:交誼廳 51.9、浴室 9.4、**洗衣間 0.75(結構性拿 0 分,
  玩家怎麼買都拿不到)**。所以三區各有自己的 bucket 表,用 `sprite.kind` 細分 utility。
- **採用的 bucket 表**(`cats` 比對家具分類、`kinds` 比對 `sprite.kind`;每區 `categoryPoints`
  = 30 ÷ 該區類數 → 不論幾類,「機能齊全」對每一區都值 30 分):
  - **交誼廳(4 類,權重 0.5)**:社交 `[seating, av]`・餐廚 `[kitchen]`・裝飾 `[ambiance]`・
    收納 `[storage]` → 種子局 **3/4**(缺收納,擺書架/衣櫃/抽屜櫃補得起來)
  - **浴室(5 類,權重 0.3)**:淋浴 `kinds[shower, bathtub]`・如廁 `kinds[toilet]`・
    盥洗 `kinds[sink]`・裝飾 `[ambiance]`・收納 `[storage]` → 種子局 **4/5**(缺收納)
  - **洗衣間(4 類,權重 0.2)**:洗滌 `kinds[washer, sink]`・晾曬 `kinds[drying_rack]`・
    整理 `kinds[laundry_basket]`+`[storage]`・裝飾 `[ambiance]` → 種子局 **3/4**(缺裝飾)
  - 刻意**不含「睡眠」**這種對共用區永遠不可能達成的類別(測試有斷言把關)。
- **上限**:三區共用一組 → `attrMax 50 + categoryMax 30 + tierMax 20 = 100`(每區都恰好 100,
  同私人房 60+30+10 的慣例,clamp 永不生效、面板加法恆等)。三區同尺規,加權合成才有意義。
  **共用區 tier 上限 20 > 私人房 10**:種子交誼廳的 tierPart 已經 7.0,沿用 10 幾乎沒有升級空間。
  `src/furniture/tier.ts` 的 `tierOf`/`tierPoints` **直接複用,一行沒改**;把
  `roomTierPoints` 抽成可傳 max 的 `tierPointsIn(roomId, max)`、屬性飽和曲線抽成
  `attrPartOf(roomId, max)`,私人房與共用區共用同一份夾值/曲線邏輯。
- **整潔用 `cleanlinessBaseline(roomId)` 推導,零新狀態**:共用區沒有租客、沒有 `rt.cleanliness`,
  直接把區 id 餵給既有純函式(`clamp(50 + storage*2 + cleanPower, 50, 90)`)。實測交誼廳 62
  (冰箱 storage 4 + 流理臺 2)、浴室 50、洗衣間 50。**零存檔改動、沒升 `SAVE_VERSION`、
  沒寫 migration、零 tick 工作**——分數 100% 由 `placements.list` 推導,而 placements 早已
  入存檔,舊檔載入自動一致。
- **🔴 `COMMUNAL_NEUTRAL = 35.28233009708738` 的實測推導**(中性錨點校準成種子樓層分數,
  **不是 50**;取 50 會讓 cd ≈ −16~−29 → mood −2.6~−4.7,功能一上線就是全樓大扣分、快照全盤重排):
  - 交誼廳:attr `50 × 33.5/(33.5+18)` = **32.5242718446602**(cozy 23・style 14・tech 6
    → 加權 33.5)+ 齊全 **22.5**(3/4 × 30)+ 品質 **7.0** = 小計 62.0242718446602
    × **0.81**(整潔錨 62)= **50.23966019417476**
  - 浴室:attr **5.0**(僅浴室小盆栽 cozy 2)+ 齊全 **24**(4/5 × 30)+ 品質 **0.5**
    = 29.5 × **0.75**(錨 50)= **22.125**
  - 洗衣間:attr **0**(五件全零屬性)+ 齊全 **22.5**(3/4 × 30)+ 品質 **1.0**
    = 23.5 × **0.75**(錨 50)= **17.625**
  - 加權:`0.5 × 50.23966019417476 + 0.3 × 22.125 + 0.2 × 17.625` = **35.28233009708738**
  - 常數是把實測的 double **原封不動貼回來**(第一版寫成 `…087375` 差一個 ULP,
    實測 `cd = 7.1e-15`、delta ≈ 7.1e-16,已改成精確值)→ 種子局 **`cd` 恰好 === 0**
    → 三個 delta 全 0 → **balance 快照零漂移**。
- **係數刻意小於私人房**(私人房 0.16/0.10/0.08):`mood cd×0.10`、`stress −cd×0.06`、
  `wellbeing cd×0.05`——公共空間是共享環境,影響力應該低於「自己的房間」。
- **`tick.ts` 只動兩處**(`cozyHomePass`/`dirtyComplaintPass` **一行沒碰**):
  `baselines()` 疊 communal 的 mood/stress、`applyStat()` 的 `wbAnchor` 加 wellbeing 一項。
- **UI**(`src/App.vue`):拆解區**下方**加一列「🏢 公共空間 XX」,完全複用第四列「✨ 品質」的
  `.cbd-row` 結構;點擊展開三區細項(每區一條 bar + 分數 + 權重 chip + bucket ✓/✗ + 🧹 倍率),
  預設收合(390px 直式下四列全開太長)。`.cbd-communal` 只加寬標籤/數值欄
  (`--cbar-label-w: 74px`、`--cbar-value-w: 46px`),其餘沿用既有 class。
  `STAT_HELP` 加「公共空間」一條(點標籤 `@click.stop` 說明,點 bar 展開)。
  **刻意沒讓 bathroom/laundry/lounge 可進入完整房間面板**——那個面板到處假設有 `rt`
  (`activeRoomId`/`comfortBd`/`livingExperience` 全要有租客),且 **lounge 連 `ROOM_INFO`
  條目都沒有**(`map.ts` 只有 bathroom/laundry),改動成本高;`map.ts` 本次未動。
- **順帶打開一個真的玩法鉤子**:`robot_vacuum` 的 `placement: "room"` 其實**沒有任何地方強制執行**
  (`FurnitureShop.vue:52` 只排除 `placement === "wall"`,`canPlaceFree` 只檢查 `FLOOR_REGIONS`
  而共用區都在裡面),所以玩家本來就能把掃地機器人買進交誼廳,只是**沒後果**。
  現在共用區整潔改由 `cleanlinessBaseline` 推導 → 實測交誼廳整潔錨 **74 → 90**、
  乘子 ×0.870 → ×0.950、區分數 77.187 → 84.760、整體 52.789 → 56.576。測試有涵蓋。
- **變更檔案**:`src/sim/comfort.ts`(共用區區塊 + 兩個共用 helper)、`src/sim/tick.ts`(兩處接點)、
  `src/App.vue`(UI 列 + computed + CSS + STAT_HELP)、**新增 `scripts/communal-comfort-test.ts`**、
  `scripts/comfort-test.ts`(補 6 條對稱斷言)、`scripts/run-all.ts`(REGRESSION 加一項,字母序)。
- **驗證(全跑,實際數字)**:
  - `npx tsx scripts/communal-comfort-test.ts` → **38 通過 / 0 失敗**
  - `npx tsx scripts/balance-test.ts`(**未加 `--update`**)→ **✅ 快照一致**,
    `scripts/balance-snapshot.json` **git status 乾淨、零位元改動**(零漂移如設計)
  - `npm test` → **66/66 全過**(69.4s);`comfort-test.ts` 39 → **45 通過**、
    新測試 **38 通過**、`stats-model-test.ts` **11 通過**(`:91-94` 的
    「舒適度墊高心情基準」clean 59.1 > dirty 54.8 仍成立——種子 delta 為 0,`baselines()` 沒動)
  - `npm run build` → 132 modules、2.29s;`npm run typecheck` → 乾淨
- **🔴 `cozyHomePass` 不變的探針實測**(不靠推算):把 `7ce59a7` 用 `git archive` 展開到
  untracked 的 `.probe-baseline/`(**沒用 worktree、沒 stash、沒動任何追蹤檔**),兩邊跑同一支探針:
  (1) 固定種子跑 168 遊戲小時,逐小時記錄每位租客的 `roomComfort(roomId, rt.cleanliness)`;
  (2) 直接掃 `cozyHomePass` 的翻轉窗口(clean 60.0~95.0,0.1 步進 = 351 檔 × 2 租客),
  每一步記 `roomComfort` 值**與該步是否真的推了 🏡 日誌**。
  結果 **兩邊輸出 byte-for-byte 完全相同**(各 52,184 bytes),`sweepFired` 兩邊都是 **120**。
  **刻意沒拿 `logs` 沒動當證據**——`logs: 60` 已在 `LOG_CAP` 飽和、對此永久失去偵測力;
  第一版探針只跑模擬時 `cozyLogs` 兩邊都是 0(**零偵測力**),所以才補了 phase 2 的窗口掃描。
  探針腳本(`scripts/_probe.ts`、`scripts/_cozyprobe.ts`)與 `.probe-baseline/` **跑完已全部刪除,未進 commit**。
  測試裡另留兩條永久把關:(a) 釘死 7ce59a7 實測的 4 房 × 8 檔整潔 = **32 個 `roomComfort`
  逐位元值**(取樣穿過 74.9/80/83/84.2 翻轉窗口);(b) 結構性不變量——大改共用區
  (公共空間 35.282 → 52.789)後所有私人房 `roomComfort` **一位元都沒動**。
- **UI 驗證**:`npm run ui:shot -- rent` → 18 張、三個 viewport 全 `errors: []`。
  預設 18 張**照不到拆解面板**(它在「查看診斷細項」裡),另跑一次性 Playwright 流程
  (放 gitignore 的 `artifacts/` 下、**跑完已刪**)補截 390/412/430 三種寬度 × 收合/展開。
  實測三寬度:**橫向溢出 0px**(`docScrollW === docClientW`)、`.cbd-communal` 不溢出容器也不溢出
  viewport、四列的**標籤與數值欄零截斷**(labelScrollW 74 = clientW 74、valScrollW 46 = clientW 46)、
  四列**都沒換行**(rowH 15px)。bucket chip 列在 390 下三區都換成 2 行(40px)、412/430 下只有
  浴室(7 個 chip)換行——`.cbd-buckets` 本來就是 `flex-wrap: wrap`,與私人房那列同一設計,非溢出。
  新列在三種寬度都讀得出「35 ▾ / 交誼廳 50 / 浴室 22 / 洗衣間 18」與「開場基準 35 · 目前 +0」。
- **已知限制/風險**:
  - 共用區分數是**全樓共享的單一值**,四位租客的面板顯示同一個數字(語意如此,非 bug)。
  - `COMMUNAL_NEUTRAL` 是**硬編的種子實測 double**:日後若改 bucket 表、上限、權重、
    或改動 `INITIAL_PLACEMENTS` 的共用區家具,**種子 cd 就不再是 0、balance 會漂移**。
    正確做法是重新校準這個常數(測試會紅燈提醒),**不是跑 `--update` 掩蓋**。
  - 三區的 `attrPart` 天花板實務上很難逼近 50(共用區家具 attributes 普遍偏低,洗衣間五件全空),
    主要升分空間在 categoryPart 與 tierPart;這是刻意的(共用區不該靠堆療癒家具刷分)。
  - 房間改建升級(`upgradeAttributes`)理論上會經 `roomAttributes()` 進到共用區的 attrPart,
    但現況改建只賣給 r301~r304,實務上不會發生。
  - 零 RNG(測試覆寫 `Math.random` 計數 === 0)、決定性(連跑兩次完全相同)。
- **硬規則保留**:未成年排除戀愛線、成人內容遮蔽式且預設關、AI 白名單/夾值/消毒全數保留;
  本次未動 `placements.roomAttributes()`、`cozyHomePass`/`dirtyComplaintPass`、`furniture/tier.ts`、
  `floor/map.ts`、`floor/vacuumAgents.ts`、`sim/persistence.ts`、`scripts/balance-snapshot.json`。

### 2026-07-30 — 共用區舒適度 review 後修正:不變量收緊、面板分母、效能與警語(Claude Code)

接續 `4fdb79c` 的 review。無阻斷性問題,以下五項為 reviewer 指出的必修;**沒有任何行為/平衡改變**
(唯一動到模擬程式的是純效能重構,已證明逐位元不變)。

- **1. 不變量斷言從 `<= 100` 收成 `=== 100`(4 處)**:文件與 commit message 都宣稱「= 100」,
  但斷言寫成 `≤`。若有人把 `COMMUNAL_TIER_MAX` 從 20 改成 15,「每區滿分都是 100、三區同尺規」
  這個**加權合成的前提就破了,而測試不會紅**。私人房那組同理(60+30+10)。
  改在:`communal-comfort-test.ts`、`comfort-test.ts`(共用區那條 + 私人房那條)、
  `furniture-tier-test.ts:198-201`。
  **有做突變測試證明新斷言真的抓得到**:暫時把 `COMMUNAL_TIER_MAX` 改成 15 → 
  `❌ 每區 attrMax + categoryMax + tierMax === 100(交誼廳 50+30+15=95 …)`、**40 通過 / 1 失敗**;
  改回 20 後恢復全綠。(舊的 `≤` 版本這個突變會靜默通過。)
- **2. `App.vue` 共用區列的分母改用常數推導**:原本 `pct` 與 `/100` 是**字面 100**,
  而私人房的 `comfortParts` 是嚴格從 `COMFORT_LIMITS` 推導的。現在 import `COMMUNAL_LIMITS`,
  用 `areaMax(id) = attrMax + categoryMax + tierMax` 當各區分母、
  `totalMax = Σ(areaMax × weight)` 當合成分母(三區同尺規時它就等於單區上限)。
  目前畫面數字完全一樣(因為三項本來就合 100),但來源不再是巧合——與第 1 項一起才有意義。
- **3. 空共用區的斷言原本是空轉的**:舊寫法 `const emptyLike = (0*0+0+0) * cleanlinessMultiplier(…)`
  前半是字面常數,整條等於 `0 === 0`,**完全沒有執行 `communalAreaBreakdown` 的空區路徑**,
  給了假的安全感。改成**真的把洗衣間的家具全數移出 `placements.list`**、呼叫
  `communalAreaBreakdown("laundry")` 驗完整路徑,再原樣塞回。實測空區:
  `attr 0 / cat 0 / tier 0 → quality 0`(有限數非 NaN——屬性部分是 `max × w/(w+18)`,
  全空時是 `0/(0+18)` 而不是 `0/0`)、bucket 全標記為缺、整潔錨回到無收納的 50 → ×0.75;
  整棟合成分數 50.794 仍為有限數;還原後洗衣間回到 6 件、quality 28.91。(3 條新斷言)
- **4. `communalQuality()` 每小時被重算兩倍(純效能重構)**:`applyStat()` 內
  `baselines()` 與 `wbAnchor` 各算一次**完全相同**的東西。改成在 `applyStat` 算一次、
  同時餵給兩處;`baselines(rt, comfort?, communalQ?)` 多一個可選參數,未帶入時
  fallback 就地計算(`stats-model-test`/`arc-test` 等外部呼叫走這條,行為不變)。
  **實測掃描趟數**(探針包 `placements.list` 的 `Symbol.iterator` 計數):
  單次 `communalBreakdown()` = **18 趟**全表掃描(3 區 × 6 趟:`roomAttributes`、
  `roomCategories`、`areaKinds`、`tierPointsIn`、`cleanlinessBaseline` 內的
  `roomAttributes` + `roomCleanPower`);每遊戲小時(種子局 2 位租客)
  **83 → 47 趟**,正好省下每位租客一次多餘的 breakdown(4 位租客時為 144 → 72)。
  **「行為逐位元不變」的證明**:把 `4fdb79c` 用 `git archive` 展開到 untracked 目錄
  (未用 worktree/stash),兩邊跑同一支探針——固定種子 168 遊戲小時,逐小時 dump 每位租客的
  mood/stress/energy/wellbeing/affinity/cleanliness/satisfaction + `baselines()` 的
  mood/stress + log 筆數 + pendingEvent id(共 4,032 個欄位),外加 `state.money`、
  `ledgerCount`、`communalQ`、三個 delta。**輸出 byte-for-byte 完全相同**(各 58,276 bytes;
  money 57534、ledgerCount 28、communalQ 35.282330097087、delta 全 0 兩邊一致)。
  另有 `balance-test` 綠燈與測試內既有的決定性斷言佐證。
- **5. 警語寫到會踩到它的人眼前**:「改了種子擺放要重新校準 `COMMUNAL_NEUTRAL`」原本只寫在
  `comfort.ts` 與工作日誌裡,但**會踩到它的人(正在動家具的 Codex)不會讀那兩處**。
  在 `src/floor/map.ts` 的 `INITIAL_PLACEMENTS` 上方加了註解警語,明講動 lounge/bathroom/
  laundry 三區要重新校準常數、**不是跑 `--update` 蓋掉漂移**,並註明改私人房不受此限。
  **`map.ts` 僅新增註解,零程式碼變更**(diff 為 +12/−1,全部是註解;動手前已確認該檔不是 dirty)。
- **驗證**:`communal-comfort-test` 38 → **41 通過 / 0 失敗**;`comfort-test` **45 通過**;
  `furniture-tier-test` **31 通過**;`stats-model-test` **11 通過**(`:91-94` 的 clean 59.1 >
  dirty 54.8 與修正前**完全相同的數字**,`baselines()` 多一個可選參數沒有稀釋因果);
  `balance-test`(**未加 `--update`**)→ **✅ 快照一致**;`npm test` **66/66**(69.3s);
  `npm run typecheck` 乾淨;`npm run build` 132 modules、2.37s。
- **UI 驗證**(第 2 項動了面板,重跑):`npm run ui:shot -- rent` 18 張、`Errors: 0`;
  自訂拆解面板流程(gitignore 的 `artifacts/` 下、**跑完已刪**)390/412/430 三寬度重測 →
  **橫向溢出 false、`.cbd-communal` 不溢出自身也不溢出 viewport、標籤與數值欄零截斷、
  四列皆不換行**,顯示值三寬度一致(`🏢 公共空間=35 ▴`、`交誼廳=50/100`、`浴室=22/100`、
  `洗衣間=18/100`)。bucket chip 列 390 下三區換行、412/430 下僅浴室(7 chip)換行——
  `.cbd-buckets` 本來就是 `flex-wrap: wrap`,與私人房那列同設計,非溢出。
- [ ] **(新留待辦)`communalBaselineDelta` 的 wellbeing 係數 0.05 實質不可感知**:
      它只把 wbAnchor(65)上下挪約 1 點,而 wellbeing 的回歸只有 1%/h,玩家幾乎感覺不到。
      日後若要加強共用區的手感,**這是第一個該動的旋鈕**(mood 0.10 / stress 0.06 相對有感)。
      調整時記得共用區影響力應維持低於私人房(0.16 / 0.10 / 0.08)。
- **未動**:`COMMUNAL_NEUTRAL` 維持硬編 + 測試守門(reviewer 判定為正確取捨,不改成執行期計算);
  `comfort.ts` 本次零變更;`cozyHomePass`/`dirtyComplaintPass`、`placements.roomAttributes()`、
  `furniture/tier.ts`、`persistence.ts`、`balance-snapshot.json` 全數未動。

### 2026-07-30 — 文件對帳:清孤兒條目、收散落待辦、加驗收錨點制(Claude Code)

- **目的/使用者需求**:過去兩天四批功能 + Codex 兩批美術上線,`工作日誌.md` 與
  `docs/交接與待辦.md` 沒跟上。**純文件任務**,零 `src/`／`scripts/`／`worker/`／`.ui-lab/` 變更。
  Branch `docs/reconcile-2`,基準 HEAD `cc9c07c`(開工前 `git status --short` 乾淨,
  Codex 的 `rent-character-art-v1` lease 已標「已完成」)。

- **A. 清掉六處孤兒條目(程式已完成、文件仍說待辦)**
  1. 第四節「樓外世界錨點」`- [ ]` →`[x]`(`c4ee922`／`ce4be56`,2026-07-29)。
  2. 第四節「共用區舒適度延伸」`- [ ]` → `[x]`(`4fdb79c`／`99b9a36`,2026-07-30)。
  3. 路線圖第 3 項「租客錢包」補刪除線(2026-07-17 就完成,第四節自己早標了 ✅)。
  4. 路線圖第 4 項「月度全樓事件鏈」補刪除線(`cbbdfbc`)。
  5. `tierPart` 夾值 0~12 → **私人房 `TIER_MAX = 10`**(`comfort.ts:58`,`8504345` 已從 12 降到 10),
     並補上**共用區另用 `COMMUNAL_TIER_MAX = 20`**(`:265`)這個原本兩份文件都沒寫的事實。
     ⚠️ **歷史條目 `:2714` 的「clamp 到 0~12」保留不改**——那是 `92eeff3` 當時的正確敘述,
     `:2804` 的 review 條目已記載 12 → 10,歷史自洽。
  6. 第四、五節的「上次與程式碼對帳」日期 2026-07-25 → **2026-07-30(HEAD `cc9c07c`)**。

- **B. 把只寫在日期紀錄內文的待辦提升為正式清單條目**(接手者不逐條翻就不會知道)
  - 第四節新增「**美術第二批留項**」5 條(來源:2026-07-29 電視／椅子美術的「已知限制／後續」
    與 2026-07-30 人物美術的「刻意不納入本批」)。
  - 第四節新增「**測試債**」5 條(來源:2026-07-29 tier 第二批與紀念物下架批)。
  - 第五節新增「**共用區舒適度的軟性限制**」4 條(來源:2026-07-30 兩則共用區條目)。
  - **C-9 重新定義**:原文「可裁剪 todayLog 條數」**那半已經實作了**——
    `worker/index.ts:319` 是 `selectDiverseNarrativeLines(clampArr(c.todayLog, 20, 200), 10)`,
    `:361` 的 `events` 同樣夾 12 選 6。本條已窄化成**純快取**,並標為待使用者決定是否結案。

- **🔴 自行複核時額外發現的三處過期**(brief 未列)
  1. **「AI 色碼對比安全化」的檔案位置寫錯**:Codex 日誌寫 `generate.ts`,但 `src/sim/generate.ts`
     完全沒有 palette 相關程式。實際在 **`src/sim/invite.ts:63-64` 的 `pickColor()`**:
     `typeof v === "string" && HEX.test(v) ? v : pool[…]`——只驗 hex **格式**,
     `HAIR_COLORS`/`SKIN_TONES` 等四個池子只是格式錯時的 fallback,**不是白名單**。
     風險敘述因此成立且更嚴重:AI 回一個語法正確的 `#fdfdfd` 髮色就會被原封不動採用。
     （✅ **已於 2026-07-30 修掉**,見同日「AI 色碼對比安全化」日期紀錄。）
  2. **`drawActivityChair` 在 `floorScene.ts:370` 不是 `:369`**(`:369` 是它的 doc comment)。
  3. **共用區 review 批留了第四條散落待辦**(brief 只列三條):`communalBaselineDelta` 的
     wellbeing 係數 0.05 實質不可感知,已一併收進第五節。

- **C. `docs/交接與待辦.md` 同步**(原本停在 `2f1be4e`,落後 8 個 commit)
  - 標題與最新交接段:`2f1be4e` → **`cc9c07c`**;基準 63/63 + 129 modules →
    **實測 66/66(68.1s)+ 132 modules**(本次真的重跑了 `npm test` 與 `npm run build`,
    沒有沿用日誌數字);舊基準降級為歷史快照。
  - 第七節末「實際剩餘項」整份重寫,新增 L(美術)／M(測試債)／N(共用區軟限制)／
    K(跨專案)四段;`:31-32` 的「樓外世界錨點是目前唯一還沒動工的低成本內容項」已刪除。
  - `:356-357` 那個把讀者導向失效清單的指標,補註說明它當時指向的清單本身已失效。
  - `:62` 的「路線圖僅剩第 6 項樓外世界錨點未動工」→ 更正為內容項已全數結案。

- **D. 補一條沒人追蹤的跨專案待辦**:`.ui-lab/compose.yaml` 的 NBA 8000 埠映射自 2026-07-16
  被暫時註解掉(Windows 保留埠 7912-8011 衝突),兩份文件都只寫「跑 NBA 互動檢查前要還原」,
  **但沒有任何待辦追蹤這個還原狀態**。已收進交接文件的 K 段。
  ⚠️ **只加文件記錄,沒有動 `.ui-lab/compose.yaml`**(它在工作區根、屬於別的 lease、且有未提交變更)。

- **E. 結構性防範:🔍 驗收錨點制**(選這個的理由見下)
  - 第四、五節**每一條 `- [ ]` 後面都必須附一行可 grep 的驗收錨點**,
    例如 PWA 那條就是「🔍 驗收:`rg -n "vite-plugin-pwa" package.json vite.config.ts` 有命中 → 已完成」。
    沒有完成訊號的長期項(如「持續微調平衡」)一律標 `🔍 驗收:長期項,無完成訊號`。
  - **為什麼選這個而不是「文件開頭寫一段對帳程序」**:通用程序無法告訴你「這一條」該 grep 什麼,
    對帳的人還是得先讀懂每個條目、再自己想錨點——那正是這次失效的環節。錨點綁在條目上,
    判斷成本降到「跑一行 grep、看有沒有命中」,不需要理解條目在講什麼。
  - **為什麼不寫腳本**:腳本本身會變成需要維護的技術債(錨點改了要同步改腳本),
    而 grep 指令直接寫在條目旁邊,改條目時一定會看到。
  - **同時封掉「同一件事在兩處各追蹤一次」這個根因**:路線圖(2026-07-16 規劃紀錄)加了
    ⚠️ 補註明訂「本路線圖的完成狀態不具權威性,一律以第四節為準,新待辦不要開在這裡」;
    交接文件也在檔頭與第七節末明訂它是第四、五節的**摘要**。
    「月度全樓事件鏈」正是因為在路線圖與交接文件各有一份獨立狀態才殘留兩次。
  - **四方向走路 sprite** 在第四節美術債與新的美術第二批留項各出現一次,已互相交叉引用
    並註明「同一項只能派工一次」,不再是兩份獨立狀態。

- **F. 順手補齊「只在一份文件裡」的兩處**(否則「權威清單在第四、五節」這句話是假的)
  - 交接文件 J 段的**五條小殘留**(章節卡堆疊間距未驗、離線收束跳過的抉擇不補彈、
    「全員擠交誼廳」多人站位、`dream|<id>` 冷卻鍵殘留、夢境彩蛋未跑 `ui:shot`)
    原本**只**存在於交接文件,主日誌沒有對應條目。已補進第五節並各給錨點。
  - D 段的 `.ui-lab/compose.yaml` 待辦同樣兩份都放(主日誌第五節「跨專案」+ 交接文件 K 段)。
  - 結果:第四、五節共 **30 條 `- [ ]`**,與交接文件第七節末**一對一對應**;
    交接文件檔頭已明訂它是摘要、衝突時以主日誌為準,並寫上 30 這個數字供快速核對。

- **驗證:自己跑了一遍設計的對帳程序**(第四、五節全部 30 條 `- [ ]` 逐條 grep)
  - **全部 30 條確認為真的未實作**,無新增孤兒條目
    (其中 3 條標為「長期項/需人工目檢」,無程式碼訊號)。重點證據:
    `socialFulfillment` 只命中 `tick.ts` 一支(仍是註解自稱的「簡版」);
    `CHAR_WALK_(UP|DOWN)|facingDir|facing4` 零命中;worker 零 cache 層;
    `invite.ts`/`parts.ts` 無任何 contrast/luminance(⚠️ **此條已於同日稍後被實作推翻**:
    見「AI 色碼對比安全化」紀錄,現在 `parts.ts` 有亮度帶、`src/pixel/color.ts` 有 `relLuma`);
    `scene.ts:511` 的 `lie()` 確認**沒有**
    `drawAppearanceOverlay`(同檔 `stand()`/`sit()` 都有);`drawActivityChair(ctx, a)` 簽章無區分參數;
    5 張舊 crop 仍在 `scripts/limezu-manifest.json:6,7,24,25,27` 但已不在
    `src/art/limezu.ts` 的 `LIMEZU_FURNITURE_IDS`(**線上不會載入壁爐錯圖,只是素材檔死區**);
    `economy.ts` 的 `memorial` 只出現在 `:304`(搬移)與 `:389`(變賣),
    **`buyFurniture`(`:252-262`)函式體內沒有任何 `price <= 0`／memorial 守門**,
    且 `scripts/` 無 `buyFurniture("memorial` 測試;`rentPaidTotal` 零命中;
    `balance-snapshot.json` 兩位租客 `logs` 都是 **60(已在 `LOG_CAP` 飽和)**、
    `ledgerCount: 40`、無 emoji 計數/雜湊欄位;`shop-test` 不在 `run-all.ts` 且無 `check(`;
    `COZY_COMFORT = 52`／`COZY_CLEAN = 80` 未動(**翻轉窗口風險未消除,只是被共用區的
    獨立管道繞開**);`communalQuality()` 仍是無參數的全樓單一值;
    `COMMUNAL_ATTR_MAX = 50` 未動;`communalBaselineDelta` 的 wellbeing 仍是 `cd * 0.05`;
    `placements.ts:70` 直接展開 `upgradeAttributes(roomId)`、無共用區守門;
    `wrangler.toml` 無 D1/KV binding;無 `vite-plugin-pwa`／`@capacitor`／onboarding;
    `startGroupSession|startMultiSession` 零命中(多人 session 仍未做)、
    `src/sim/social.ts` 無 `dream|` 清理(`removeTenantRelations` 在 `:368`)。
  - `npm test` **66/66 通過(68.1s)**、`npm run build` **132 modules / 2.27s**
    (兩個數字都是本次實測,用來取代交接文件裡過期的 63/63 與 129 modules)。
  - `git diff --check` 通過(無空白問題)。
- **已知限制**:兩份文件裡的行號引用會隨檔案增長漂移,已在每處註明「認**日期紀錄標題**比較可靠」。
- **硬規則保留**:本次零程式碼變更,未成年排除戀愛線、成人內容遮蔽式且預設關、
  AI 白名單／夾值／消毒全數未觸及。

### 2026-07-30 — AI 色碼對比安全化:亮度帶夾值、膚色白名單、髮膚分離、三道防線(Claude Code)

- **目的/使用者需求**:`src/sim/invite.ts` 的 `pickColor()` **只驗 hex 格式**
  (`typeof v === "string" && HEX.test(v) ? v : pool[…]`),四個色池只是格式錯時的 fallback、
  **不是白名單**。AI 回一個語法合法的 `#fdfdfd` 髮色就會被原封不動採用,人物糊成一片白。
  同檔的 `hairStyle`/`accessory` 走的是真白名單——**只有顏色沒有**,本批補上這個不一致。

- **🔴 為什麼不用「vs 背景」的 WCAG 對比**(這是本批最重要的設計決定)
  - 實測**自家色池對背景的 CR 幾乎全在 1.0~1.3**:`SHIRT #5aa06a` vs r301 地板 `#b08a5e`
    → **CR 1.00**。用那個模型會把**現有美術整批判成違規**。
  - 背景也不是常數:`public/assets/limezu/floors.png` 各區平均亮度 r301 **L=0.215**、
    洗衣間 **L=0.617**,而人物會走遍所有區域;夜間 `dayNightTint()` 還會再壓縮對比。
  - 改用**背景無關**的兩條規則:**per-slot 亮度帶夾值** + **髮膚 ΔL**。
    髮色永遠貼著臉,是唯一「背景無關、永遠成立」的對比對象,也是「人臉糊成一團」的真實風險。

- **四個池的實測相對亮度(WCAG relative luminance)與最終採用的帶**
  | 槽位 | 池實測 L 範圍 | 最暗/最亮色 | 採用亮度帶 |
  |---|---|---|---|
  | `HAIR_COLORS`(8) | `0.0154 ~ 0.3800` | `#241f2c` / `#c8a050` | **`[0.010, 0.42]`** |
  | `SHIRT_COLORS`(8) | `0.1593 ~ 0.7630` | `#4a6ac8` / `#e8e2d4` | **`[0.12, 0.80]`** |
  | `PANTS_COLORS`(6) | `0.0558 ~ 0.0827` | `#3d4257` / `#6a4a3a` | **`[0.04, 0.30]`** |
  | `SKIN_TONES` | `0.3895 ~ 0.6376`(擴充後 `0.1229 ~ 0.7490`) | — | 走**白名單 snap**,不用帶 |
  - **每條帶都刻意涵蓋自家池的實測範圍再留餘裕**。特別是 `HAIR` 上限取 **0.42**
    (而非勘查建議的 0.34)——0.34 會**誤殺自家的 `#c8a050`(L=0.380) 與 `#b58a4a`(L=0.285)**。
  - **🔴 `PANTS` 是刻意放寬的**:實測池寬度只有 **0.027**(`0.056~0.083`),照抄會把 AI 給的
    任何合理淺色褲子(卡其、淺牛仔)壓成全黑。放寬到 **0.30** 讓淺褲可用,同時仍擋掉白褲
    (`#ffffff` → `#949494`,L 0.296)。這是**有意識的取捨,不是抄漏**。

- **`SKIN_TONES` 從 4 色 append 擴充到 9 色**(⚠️ **只能 append**,見下方零 RNG)
  原 4 色 `#f0c19a`(L 0.590)、`#e8b088`(0.500)、`#f4c9a6`(0.638)、`#d99a6c`(0.390);
  新增 5 色 `#8d5524`(0.123)、`#a5673f`(0.181)、`#b57a52`(0.244)、`#c68e6a`(0.324)、
  `#ffdbac`(0.749)。**最深色 L=0.123 是刻意的下限**:髮色帶下限 0.010,
  要讓「往暗夾髮色」永遠能達成 ΔL ≥ 0.10,膚色最暗必須 ≥ 0.11。

- **實際變更**
  - **新增 `src/pixel/color.ts`**(全純函式、零副作用、零 RNG):`hexToRgb`、`relLuma`、
    `contrastRatio`(僅供診斷/測試,**不用於判定**)、`clampLuma`(用既有 `shade()` 往白/黑
    線性內插,**色相保留**;固定 0.5% 步長表取第一個入帶者,**決定性**)、
    `nearestInPool`(RGB 歐氏距離,零 RNG)、`separateHairFromSkin`。
  - **`src/pixel/parts.ts`**:三條亮度帶常數、`MIN_HAIR_SKIN_DELTA = 0.10`、
    `sanitizeAppearanceColors()`(純函式)與 `sanitizeAppearanceInPlace()`;`SKIN_TONES` append 擴充。
    形狀刻意照抄 `growth.ts` 的 `sanitizeGrowthTags`:**純函式、無副作用、可重入、冪等**。
  - **三道防線**(既有存檔已含髒顏色,`persistence.ts` 的 `setCustomAppearance` 原本完全沒消毒):
    1. `src/sim/invite.ts` — 源頭,`pickColor` 之後統一過一次消毒。
    2. `src/sim/gameState.ts` `makeRuntime` — 就在 `sanitizeGrowthTags` 隔壁,涵蓋 `moveIn` 與種子租客。
    3. `src/sim/persistence.ts` `load` — 與 `sanitizeGrowthTags` 同一行區塊、同一個 pattern。
       `importSave()` 走 localStorage + reload,實際反序列化仍走 `load()`,**自動被第 3 道覆蓋**。
  - **不升 `SAVE_VERSION`**:欄位型別與結構沒變、消毒冪等、就地修好
    (`sanitizeGrowthTags` 當初也沒升版)。消毒後不會自動寫回存檔,**沒有加任何持久化副作用旗標**。
  - **`worker/index.ts`(純軟性)**:`INVITE_SYSTEM` prompt 加一段亮度指引(髮色偏深、
    避免接近白色、skin 必須自然膚色、髮膚明暗要拉開);`INVITE_SCHEMA` 四個顏色欄位加 `description`。
    **架構上真正的防線在前端**(worker 自己註明「原封不動透傳;白名單/夾值消毒在前端統一做」)。

- **🔴 零 RNG(審查重點)**
  - `randomAppearance()`(`parts.ts`)**函式體 byte-identical**,5 次 `Math.random()` 一次不增不減
    ——它經由 `recruit.ts:301` 在 **seeded 路徑**上,增減任何一次都會位移序列、炸掉 balance 快照。
  - `SKIN_TONES` **只 append 到尾端**,`pick()` 消耗的隨機數個數不變。
  - **`pickColor` 的 fallback 分支原封不動**,只在「格式合法」那一支之後接夾值
    ——連 `Math.random()` 的呼叫次數都完全不變。
  - 消毒本身全部是決定性運算(最近距離 / 固定步長表),**沒有任何 `Math.random()`**。

- **🔴 冪等性**:夾值會在 invite → `makeRuntime` → **每次 `load`** 反覆執行。
  `clampLuma` 已在帶內就原樣返回;`separateHairFromSkin` 的候選一律限制在帶內,
  成功時結果同時滿足帶與 ΔL(第二次直接短路),失敗時輸入即輸出、候選集合不變。
  因此 `sanitize(sanitize(x)) === sanitize(x)`,已用測試釘住
  (否則玩家**每讀一次檔頭髮就暗一階**,最後全變黑)。

- **測試(擴充既有檔案,自動落在 `run-all.ts` 的 REGRESSION 內)**
  - `scripts/invite-test.ts` **34 → 50 條**。
    ⚠️ 順手修掉一條**假保護**:舊的「非法色碼 → 回退池內色」只驗 hex 格式,`#fdfdfd` 也能通過;
    已改成實質斷言(真的在 `HAIR_COLORS` 裡 + 在帶內 + 膚色在白名單 + ΔL 達標)。
    新增:`#fdfdfd` 髮色夾到帶內、`#000000` 衣色抬到帶內、`#ffffff` 褲色夾到帶內、
    色相保留(用 **R/G/B 相對大小關係**驗,不比精確值——`shade()` 往白內插會洗掉飽和度)、
    膚色 `#00ff00` snap 進白名單、**冪等性**、**髮膚 ΔL(8×13 = 104 組)**、
    膚色極深時改往亮夾、以及 **🔴 護欄**。
  - **🔴 護欄測試結果**:`HAIR_COLORS` 8 色、`SHIRT_COLORS` 8 色、`PANTS_COLORS` 6 色
    夾值後**全部原樣返回**;`SKIN_TONES` 9 色 snap 後**全部原樣返回**;
    `SEED_APPEARANCES` 兩位種子租客的手繪配色消毒後**完全不變**。
    這條是本方案相對「vs 背景」方案的**關鍵分野**——沒有它就無法證明沒把自家美術判違規。
  - `scripts/distinct-test.ts` **14 → 23 條**:存檔往返消毒——把 raw 存檔的
    `runtimes.tenant_chen_engineer.tenant.appearance` 竄改成 `#ffffff` 髮 / `#000000` 衣 /
    `#ffffff` 褲 / `#00ff00` 膚,`save()`/`load()` 後斷言四項都已修好、渲染層拿到的也是消毒後的顏色,
    並再存讀一次驗證**不會繼續變暗**。

- **驗證結果(全部實測)**
  - `npx tsx scripts/invite-test.ts` → **50 通過 / 0 失敗**
  - `npx tsx scripts/distinct-test.ts` → **23 通過 / 0 失敗**
  - `npx tsx scripts/balance-test.ts`(**未加 `--update`**)→ **快照一致**,零漂移
    (顏色不進模擬,且 RNG 呼叫次數未變)
  - `npm test` → **66/66 支通過(67.6s)**
  - `npm run build` → **133 modules / 2.31s**(新增 `color.ts`,132 → 133;既有 >500kB 警告不變)
  - `npm run typecheck` → app(`vue-tsc`)與 worker(`tsc`)**皆無錯**
  - `npm run ui:shot -- rent` → **18 張、`errors: []`**。`02-feed` 與 `03-finance` 九張
    **hash 與改動前完全相同**;`01-floor` 六張 hash 有變,但**已證實與本次改動無關**:
    在乾淨 `main`(`git stash` 後)重跑一次,`01-floor` 對自己前一次也不同
    ——該畫面吃真實時鐘(貓的位置/日夜 tint)。**人工目檢兩張 floor 圖:兩位種子租客的
    髮色、膚色、衣色完全一致,唯一差異是橘貓走到別處。**

- **視覺驗證(壞顏色真的被修正)**:臨時改 `scripts/render-preview.ts` 塞入毒色
  `{hairColor:#fdfdfd, shirt:#f8f8f2, pants:#fafafa, skin:#00ff00}` 產 before/after PNG。
  - **before**:人物是一團**近白色的糊塊**,髮/衣/褲三者完全分不出邊界,臉是**螢光綠**。
  - **after**:`hairColor → #adadad`(L 0.418)、`shirt → #e7e7e1`、`pants → #949494`(L 0.296)、
    `skin → #8d5524`(L 0.123);臉變成暖棕、與灰髮明確分離(ΔL 0.295),褲子與上衣也分得開。
  - **比對完已 `git checkout -- scripts/render-preview.ts` 還原,未提交。**
  - ⚠️ **離線/線上落差**:離線腳本在 Node 下載不到 LimeZu atlas(`limezu.ts:230-264` 預載失敗
    → 落回程序地板 `FLOOR_TINT`),所以離線背景比線上暗(程序 laundry `#9aa2a8` L=0.42
    vs 線上 `#c0d2cc` L=0.62)。**線上更嚴苛,離線圖過關不代表線上過關**——
    最壞情況是靠測試的數值斷言釘死,不靠圖。

- **已知限制 / 後續**
  - **髮膚 ΔL 會調整 72 組「池內髮色 × 池內膚色」中的 10 組**(例:`#8a4a5a` 髮 + `#a5673f` 膚
    → 髮壓暗成 `#763f4d`)。這是規則的**正確行為**(兩者 L 只差 0.07,11px 頭像上真的會糊),
    per-slot 護欄仍全數通過。**種子租客與現有截圖不受影響**(ΔL 分別是 0.55 / 0.51)。
  - **膚色極深時「往暗夾髮色」不可能達標**,`separateHairFromSkin` 會改往亮夾。
    以現行白名單(最暗 L=0.123)這條**走不到**,是純防禦分支,已用直接呼叫該純函式的測試覆蓋。
  - **刻意不做**「`getTheme()` 第二層防護」:違反專案「在邊界消毒」的既有慣例
    (`growth.ts` 也是在 load/create 消毒、不是使用時),且需要 memo。三道防線已足夠。
  - **刻意不加角色 sprite 描邊**:那是美術改動不是消毒,會動到 `appearance-test.ts` 的
    像素斷言與所有截圖,且解決不了髮膚辨識度。**記為美術第三批候選。**
  - 平衡快照**未更新**(不需要,零漂移)。

- **硬規則保留**:未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒
  ——本批正是在**強化**最後一條。

### 2026-07-30 — AI 色碼安全化 review 後修正:膚色池產品決定、格式回退補三道等強、ΔL 影響面釘死(Claude Code)

承上一筆 commit `13af730` 的 review(reviewer 做了 **36,864 組窮舉:4096 色 × 9 膚色**,
確認非冪等 0 組、ΔL 未達標 0 組、髮色出帶 0 組,亮度帶與膚色下限的判斷獨立驗證為正確)。
以下是 review 提出的五項收尾。

- **🔴 產品決定:`SKIN_TONES` 4 → 9 色是「刻意擴大隨機租客膚色多樣性」,不只是為了當 snap 靶**

  上一筆日誌只寫了「append only → 隨機數個數不變」與「最深 L=0.123 是刻意下限」,
  **漏講了本 commit 影響面最大的視覺後果**,補記如下。

  `pick()` 是 `a[Math.floor(rnd * a.length)]`,池長度 4 → 9 會讓**同一個 seed 映射到不同 index**。
  `scripts/balance-snapshot.json` 不含顏色欄位,所以平衡快照零漂移(已實測);
  但**新生成角色的膚色分佈實際上變了**,這是有意為之:

  - **新生成的隨機租客約 `5/9 ≈ 55.6%` 會抽到新增的 5 色**
    (`#8d5524` / `#a5673f` / `#b57a52` / `#c68e6a` / `#ffdbac`)。
  - 原 4 色的亮度範圍是 `L ∈ [0.3895, 0.6376]`,**新增 5 色全部落在這個範圍之外**
    ——所以同樣是 `≈ 55.6%` 的新角色膚色會超出舊亮度範圍(往深 4 色、往極淺 1 色);
    剩下 `4/9 ≈ 44.4%` 仍落在原本那 4 色。
  - **既有存檔不受影響**:舊 4 色 snap 到自己是 identity(已實測),
    而舊角色的膚色只可能來自舊 4 色池(種子租客的 `#f0c19a`/`#f4c9a6` 也在池內),
    所以**存量角色膚色 100% 不變**。唯一會變的是 AI 特邀租客給的池外膚色——那正是本批要修的。
  - **理由**:租客本來就該有更廣的膚色範圍,這對這款遊戲是好事。
    擴充不是消毒的副作用,是想要的效果。
  - **刻意不拆成 `SKIN_TONES` / `SKIN_SNAP_POOL` 兩個池**:review 提了這個選項
    (拆池可讓 `randomAppearance()` 維持舊 4 色、只讓 snap 用 9 色)。
    **不採納——既然我們「要」這個多樣性,拆池等於把想要的效果關掉。**

- **必修 2:第 2、3 道防線補格式回退(三道等強)**
  - `sanitizeAppearanceColors` 原本對格式不合法的色碼**原樣放行**,只有 `skin` 因為
    `nearestInPool` 而意外有回退。實測:
    `{hairColor:"red", shirt:"not-a-color", pants:"#GGGGGG"}` 三項原樣穿過;`"#fff"` 也穿過。
  - `invite.ts` 路徑有 `pickColor` 擋在前面所以安全,但 **`makeRuntime`/`load` 沒有任何格式驗證**,
    而 `importSave()` 會吃玩家提供的任意 JSON。手改存檔塞 `"#fff"`(三碼 hex 是合法 CSS)進去,
    渲染層 `shade()` 用 `hex.slice(1,3)/(3,5)/(5,7)` 硬切六碼 → `parseInt("")` → `NaN`
    → 產生 `#ffNaNNaN` 這種無效 `fillStyle`,畫面壞掉。
  - ⚠️ **這是 `main` 就有的洞**(`main` 的 `load` 根本沒消毒),**不是本批造成的回歸**;
    但既然宣稱「三道防線」,就該讓三道等強。
  - 已在 `sanitizeAppearanceColors` 開頭加 `coerceHex()`:四個槽位都先驗
    `^#[0-9a-fA-F]{6}$`(**三碼 hex 一律視為不合法**),不合法就換成該池首色
    (`HAIR_COLORS[0]`/`SHIRT_COLORS[0]`/`PANTS_COLORS[0]`/`SKIN_TONES[0]`,決定性、零 RNG)。
    回退色本身都是池內色(在帶內、snap 為 identity),**不影響冪等與護欄**。

- **必修 3:護欄測試改名 + 釘死 ΔL 的 blast radius**
  - 舊測試名「四池全部原樣返回」技術上為真但單獨引用會誤導:`guard()` 只呼叫 `clampLuma`,
    **沒經過 ΔL**;SKIN 那條雖走完整消毒,但髮色寫死 `#241f2c`(L 0.0154)對 9 色膚色
    ΔL 都遠超門檻,**也沒觸發 ΔL 路徑**。已改名為「per-slot 夾值後原樣返回」並加註說明。
  - **新增釘死測試**:`HAIR_COLORS × SKIN_TONES` 全交叉 8×9 = **72 組,恰 10 組被 ΔL 調整**,
    這 10 組的期望輸出全部寫進斷言表。以後誰動亮度帶或膚色池,影響面變動會被測試叫住:

    | 髮色 | 膚色 | 調整後 | L(髮)→L(髮') | ΔL |
    |---|---|---|---|---|
    | `#4a3a2a` | `#8d5524` | `#32271d` | 0.0465 → 0.0222 | 0.1007 |
    | `#7a4530` | `#8d5524` | `#3d2318` | 0.0861 → 0.0226 | 0.1003 |
    | `#7a4530` | `#a5673f` | `#75422e` | 0.0861 → 0.0788 | 0.1018 |
    | `#b58a4a` | `#b57a52` | `#836436` | 0.2850 → 0.1421 | 0.1015 |
    | `#b58a4a` | `#c68e6a` | `#a17b42` | 0.2850 → 0.2214 | 0.1026 |
    | `#5a3020` | `#8d5524` | `#3f2217` | 0.0439 → 0.0226 | 0.1002 |
    | `#c8a050` | `#d99a6c` | `#b08d46` | 0.3800 → 0.2872 | 0.1022 |
    | `#c8a050` | `#c68e6a` | `#9c7d3e` | 0.3800 → 0.2208 | 0.1031 |
    | `#8a4a5a` | `#8d5524` | `#3e2128` | 0.1104 → 0.0227 | 0.1002 |
    | `#8a4a5a` | `#a5673f` | `#763f4d` | 0.1104 → 0.0794 | 0.1012 |

    (全部都是「深色髮 × 深色膚」或「金髮 × 中深膚」——兩者 L 只差 0.01~0.07,
    在 11px 頭像上真的會糊,調整是規則的正確行為。)

- **必修 4:`tenancy.ts:105` 不再註冊未消毒物件**
  - `makeRuntime` 先 `JSON.parse(JSON.stringify(t))` 深拷貝**才**消毒,所以 `:105` 原本
    `setCustomAppearance(tenant.id, tenant.appearance)` 塞進 renderer 的是**未消毒的 `tenant` 本體**,
    靠下一行 `refreshAppearances()` 用乾淨副本覆蓋回來。**結果正確但那是巧合不是設計**
    ——`:106` 哪天被移走或加早退條件就破功。
  - 已改成用 `rt.tenant.appearance`(makeRuntime 消毒過的副本)。**該檔僅此一行變更。**

- **必修 5:`refreshAppearances()` 的第 4 個未消毒入口**
  - `gameState.ts` 的 `rt.tenant.appearance ??= { ...ap }`——舊存檔缺外觀的種子租客會就地補上
    `SEED_APPEARANCES` **原值、跳過三道防線**。目前無害(種子值本來就乾淨,已有護欄測試釘住),
    但同樣是「靠資料剛好乾淨」。已在其後補 `sanitizeAppearanceInPlace()`。

- **順手(文件措辭)**
  - `docs/交接與待辦.md` 的「現有 30 條 `- [ ]`/`- [x]`」字面會被讀成「總共 30 個 checkbox」,
    實際那兩節共 49 個。已改寫成「**30 條受追蹤項目**」。
  - 驗收錨點原本只靠 `parts.ts` 與 `color.ts` 命中,**`invite.ts` 本身零命中**。
    已在 `invite.ts` 的消毒呼叫處補一句含 `luminance`／`contrast` 的註解,三個路徑現在都命中。

- **不做**:`contrastRatio` 是 dead code(只給測試/診斷用)——**保留**,依 review 指示不刪。

- **驗證結果(全部實測,本輪重跑)**
  - `npx tsx scripts/invite-test.ts` → **52 通過 / 0 失敗**(50 → 52:新增 ΔL 釘死、髒格式回退)
  - `npx tsx scripts/distinct-test.ts` → **23 通過 / 0 失敗**
  - `npx tsx scripts/balance-test.ts`(**未加 `--update`**)→ **快照一致**,零漂移
  - `npm test` → **66/66 支通過**
  - `npm run build` → **133 modules**
  - `npm run typecheck` → app 與 worker 皆無錯
  - **格式回退未破壞既有保證**:四個池護欄(HAIR 8/8、SHIRT 8/8、PANTS 6/6、SKIN 9/9)
    與種子租客護欄**全部仍綠**,冪等測試全綠——合法色碼的 identity 完全不受影響。

- **已知限制**
  - 膚色池擴充的視覺後果如上方產品決定所述(新角色 ≈55.6% 抽到新增色),**這是刻意的**。
  - `randomAppearance()` 的 `Math.random()` 呼叫次數仍然完全未變(本輪未動該函式)。
  - 平衡快照**未更新**(不需要,零漂移)。

### 2026-07-30 — 睡眠效率:床的 tier 放大恢復效率(家具 tier 第二階段;Claude Code)

- **目的/使用者需求**:tier 第一階段(`7ce59a7`)只把品質接上**房間舒適度**,床的貴賤對
  「睡得好不好」仍然零影響——`double_bed` 的 promptHint 早就寫著「換了大床之後睡得特別沉」,
  數值上卻完全沒這回事。本次把床的 tier 接上**睡眠恢復效率**。
  Branch `feat/sleep-tier`,基準 `main` = `11d758e`(開工前 `git status --short` 乾淨)。

- **實際變更與主要檔案**(**7 檔** = 5 程式/測試 + 2 文件,0 新增、0 `.vue`)
  ⚠️ 本條原誤寫「6 檔」,2026-07-30 review 修正。
  ⚠️ 下面的 `1.15 / 1.35` 是**本批當時的值**;同日 review 後已改為 **`1.12 / 1.22`**,
  見下一則「睡眠效率 review 收尾」紀錄。
  - **`src/furniture/tier.ts`**:新增 `SLEEP_MULT = { budget: 1.0, standard: 1.15, premium: 1.35 }`、
    `SLEEP_MULT_RANGE = { min: 0.5, max: 2.0 }` 與 `sleepMultiplier(def)`。
    形狀刻意對齊 `TIER_POINTS` 的 `0 / 0.5 / 1.5`——**premium 的躍升大於 standard**,
    讓「再往上加一階」始終有感。**未標 tier 直接複用 `tierOf()`**(→ `DEFAULT_TIER` = standard),
    **刻意不在本檔開第二套 fallback 規則**:tier 語意在全系統只有一份,
    未標 tier 的家具在舒適度是 standard、在睡眠也必須是 standard。
  - **`src/sim/generate.ts`**:`GenCtx` 加選配 `furnitureDefId?: string`;EFFECT 展開改成
    `EFFECT[key] × mult`。`SLEEP_STATES`(`sleeping_on_bed` / `sleeping_on_couch`)以外
    一律 `mult = 1`,`SLEEP_SCALED = ["energy", "stress"]` 以外的欄位不動。
  - **`src/sim/tick.ts`**(**3 行**):`applyHour` 開頭 hoist `let usedDefId`、
    `:432` 附近 `usedDefId = tgt.placement.defId`、`generateHourly({…})` 多傳一欄。
    **`setFurniturePose`/`resolveTarget`/`applyStat` 一行未動**,沒有新增參數穿透任何中間層——
    `applyHour` 內部本來就拿得到 `tgt.placement`,只是 `let tgt` 宣告在 `else` 區塊內。
  - **`src/furniture/catalog.ts`**:**只改 4 張床的 `effectHint` 字串**
    (`睡眠恢復 ×1.00/×1.15/×1.35 · …`)。刻意**不塞進 `tierChipText()`**——
    `+1.5` 是舒適度點數、`×1.35` 是睡眠乘數,兩個不同單位擠一個 chip 會語意混淆,
    而且上一批已在 390px 實測過最長品名剛好單行,再加字會直接撞到已知寬度邊界。
    `effectHint` 本來就是獨立 chip、兩個 SFC(`FurnitureShop.vue:102`、`FurnitureInfo.vue:44`)
    都已在渲染 ⇒ **`.vue` 一行未動、零 UI 迴歸、不需要跑 `ui:shot`**。
  - **`scripts/furniture-tier-test.ts`**:擴充 §10(**31 → 51 通過**),見下。
  - `工作日誌.md`、`docs/交接與待辦.md`(第四節條目與交接文件兩處「尚未接…睡眠效率」)。

- **🔴 只乘 energy 與 stress,不乘 mood、不動 wellbeing**(自製探針實測,不是拍腦袋)
  - **energy 的 +9 有 41~47% 被 100 上限吃掉**:seed 20260710 / 240h 下,
    陳家豪 70 個睡眠小時平均只生效 **5.30/9**(25 小時完全浪費)、林小婕 **4.80/9**(27 小時浪費)。
    仍是主軸,但要知道帳面效果比名目小近一半。
  - **mood 的 +2 對林小婕已頂到上限 15 次**(平均生效 1.17/2),加成幾乎全浪費,
    還會讓「心情滿分」更常態化 ⇒ **不乘**。
  - **stress 的 -5 對陳家豪 100% 生效**(0 次觸底),且 stress 有 homeostasis 拉回、
    不會像 energy 那樣黏死在極值 ⇒ **「好床真的有感」主要靠它**。
  - wellbeing 的 +0.3 太小(單日 ±2),乘了看不出來 ⇒ **不動**。

- **🔴 零漂移:自己重跑驗證過,不是轉述**
  - 探針實測 seed 20260710 / 240h(與 balance-test 同條件):
    `70 陳家豪 sleeping_on_bed single_bed@r301 [budget]` + `70 林小婕 … single_bed@r302 [budget]`
    = **140/140 睡眠小時全在 budget,零例外**。
  - 而且是**結構性**的:`sleeping_on_bed` 的 slot role 恆為 `"bed"`,`STATE_KINDS`
    (`routine.ts:41-46`)不覆蓋睡眠 → 落回 `ROLE_KINDS.bed = ["bed"]`;`shared_sofa` 的
    `sprite.kind` 是 `"sofa"` 永遠不匹配,**租客不可能跑去交誼廳睡**。r301~r304 四間全是
    `single_bed`(budget)。
  - `budget = 1.0` 是**精確值**,`9 * 1.0 === 9` 位元級成立 ⇒ `git diff main -- scripts/balance-snapshot.json`
    **完全為空**,`balance-test`(**未加 `--update`**)綠燈。
  - 為此**全系統只准有 `EFFECT[key] × mult` 這一種形式**:任何「先四捨五入再乘」
    「先加 bonus 再乘」的變體都會在 budget 路徑引入浮點誤差。測試已釘死(§10-3 最後一條)。

- **🔴 範圍紀律:本階段只做睡眠**
  勘查實測:種子局其他「用家具恢復」的活動**全部踩在 premium 家具上**——
  `mic_desk`@r302(streaming 72h)、`gaming_desk`@r301(working 41h)、
  `shared_sofa`@lounge(reading 20h + playing_with_cat 10h)、`lounge_tv`@lounge(watching_tv 20h)、
  `bathtub`(taking_bath)。**只要範圍越過睡眠一步,零漂移立刻破功且是大破。**
  已在 `furniture-tier-test.ts` §10-4 用 9 組斷言把這件事**主動擋住**。

- **⚠️ 留給第三階段的坑(擴充時才會踩到)**
  1. **`effectState` 會讓「畫面狀態」與「數值狀態」分家**:`generate.ts:95` 的 key 是
     `ctx.effectState ?? ctx.state`,而 `decideState`(`tick.ts:170-184`)在**浴室輪替與洗衣**時
     會覆寫 `effectState`。擴充到泡澡後會出現「泡的是 premium 浴缸、乘數卻乘在 `showering` 上」。
     **睡眠完全沒有這個問題**(床的 slot 從不觸發 effectState 覆寫),所以本批不處理。
  2. **`energy → satisfaction → rollEvent → pendingEvent → `cozyHomePass:616` 被跳過**
     這條間接鏈存在(`satisfaction.ts:15` 含 `0.1 × energy`)。**本批在 ×1.0 下完全不動**,
     但第三階段一旦讓 energy 真的位移,這條鏈會跟著動,務必連 `cozyHomePass` 一起看。
  3. **`logs` 在 `LOG_CAP = 60` 已飽和,對「多推日誌」零偵測力**,不能拿它當證據
     (本功能不推日誌所以無感)。

- **測試**(`scripts/furniture-tier-test.ts`,**31 → 51 通過 / 0 失敗**;
  選擇擴充既有檔案而非開新檔——係數與文案同檔的原則也適用於測試,且它**已在 REGRESSION 內**,
  省掉最容易漏的一步)
  - §10-1 `sleepMultiplier(budget)` **嚴格 `=== 1.0`**(不是 `≈`)且 `9 × 它 === 9`;三階嚴格遞增;
    三階都落在 `SLEEP_MULT_RANGE` 內且**夾值沒有偷偷改動現行係數**;premium 躍升 > standard。
  - §10-2 未知 defId / `UNKNOWN_DEF` / 未標 tier → 走 `tierOf()` 的 standard、**不拋例外**;
    完全不帶家具 → 退回 ×1.0(防禦性 fallback,與 budget 床效果完全相同)。
  - §10-3 energy 三階遞增(9 / 10.35 / 12.15)、stress 三階加強(-5 / -5.75 / -6.75)、
    **mood 與 wellbeing 三階完全相同**、`premium === budget × 1.35`(誤差 < 1e-12)。
  - §10-4 **範圍紀律**:9 組非睡眠活動(含 premium 的沙發/電視/浴缸/書桌/直播桌)帶了家具
    **零差異**。
  - §10-5 **不變條件**:`INITIAL_PLACEMENTS` 中所有 `sprite.kind === "bed"` 的家具(4 張)
    **皆為 budget**。未來有人把種子床換成 `double_bed`/`canopy_bed`,這條會**主動報警**提醒重建快照。
  - §10-6 四張床的 `effectHint` 都必須寫出**實際的** `SLEEP_MULT` 值(改了係數不能忘了改文案)。
  - §10-7 端到端:同一格 `single_bed` → `canopy_bed`(真的走 `addPlacement`/`removePlacementAt`/
    `furnitureAt`),同一初始 energy 跑 5 小時睡眠 → energy 65 < 80.75、stress 35 > 26.25。
  - §10-8 **零 RNG**:覆寫 `Math.random` 計數,算睡眠乘數 **0 次呼叫**;同輸入重複呼叫結果相同。
  - `sleeping_on_couch` 給了乘數(對稱、預留),但**刻意沒寫行為驗證測試**——
    `data/routines.json` 完全沒有它、`src/` 無任何路徑產生它、實測 240h 出現 **0 次**,
    寫了也跑不到,只有純函式測試有意義。

- **驗證結果**(全部實跑)
  - `npx tsx scripts/balance-test.ts`(**未加 `--update`**)→ **✅ 快照一致**;
    `git diff main -- scripts/balance-snapshot.json` **0 行**。
  - `npx tsx scripts/furniture-tier-test.ts` → **51 通過 / 0 失敗**。
  - `npx tsx scripts/stats-model-test.ts` → **11 通過 / 0 失敗**
    (`:71-79` 的「energy 為資源」24h 斷言仍成立:24h 內 13 種取值)。
  - `npm test` → **66/66 支通過**(70.3s);`npm run build` → 成功;`npm run typecheck` → app + worker 乾淨。
  - **手動效果驗證**(一次性探針,**跑完已刪、不進 commit**):在 r301 把 `single_bed` 換成
    `canopy_bed`,seed 20260710 / 240h,陳家豪:
    **stress 均值 38.12 → 29.47(−8.65,−22.7%)**、最終 stress 38.6 → 30.8;
    energy 均值 81.16 → 83.83、**滿格(=100)小時數 43 → 56**。
    **對照組林小婕(r302 維持 budget 床)每一項數字位元級不變**(energy min 42、
    stress 均值 11.70、最終 96 / 8.7)⇒ 乘數確實只作用在換了床的那個人身上。

- **🔴 一個與預期不符、但屬正確行為的實測結果(請第三階段的人知道)**
  中控預期「陳家豪 energy 最低點會從 14 明顯上升」,**實測是 14 → 14,完全沒動**。
  這**不是**乘數沒生效,而是 energy 這條管道在種子局**已經飽和**:
  - 睡眠期間 energy 早就會回滿到 100(換床後「完全浪費」的睡眠小時 **25/70 → 36/70**,
    滿格小時數 43 → 56),兩種床都是「天亮時 100 出門」;
  - 因此**日間低谷只由清醒時的消耗決定,與睡眠恢復速率無關**——換好床只是**更早**睡飽,
    不會讓低谷變高。`energy < 20` 的小時數同樣 4 → 4 不變。
  - 換言之,本功能玩家**真正感覺得到的載體是 stress(−22.7%)**,energy 只在均值上小幅浮動。
    這與上面「+9 有 41~47% 被上限吃掉」的實測完全一致,只是到了 premium 幾乎 100% 被吃掉。
  - **給第三階段的啟示**:若要讓 energy 有感,該動的不是乘數而是**上限/消耗側**
    (或改成「提早睡飽 → 釋放時數」這類設計),再往上加睡眠乘數的邊際效益已經很低。

- **已知限制/後續**
  - `sleeping_on_couch` 目前是**死狀態**(無任何路徑產生),它的乘數是預留。
  - 睡眠以外的家具恢復活動(沙發/電視/泡澡/書桌)**刻意留第三階段**,
    接上時必須連同 `balance-snapshot.json` 重新校準,並先處理上面第 1 點的 `effectState` 坑。
  - **平衡快照未更新**(不需要,零漂移)。**未 push**。

### 2026-07-30 — 睡眠效率 review 收尾:係數下修、排隊分支地雷、死碼與凍結(Claude Code)

承接上一筆(`2427e9c`)的 review 意見,同 branch `feat/sleep-tier`。
review 結論是「無阻斷性問題」,以下五項為收尾必修。

- **🔴 必修 1:係數下修 `1.15 / 1.35` → `1.12 / 1.22`(premium 曾是嚴格最優解)**
  - reviewer 做了**歸因隔離**(執行期關掉乘數但保留 premium 床的屬性),證實上一筆日誌
    那個 −22.7% 裡 **93% 來自本次新機制**、只有 −1.6% 來自第一階段的舒適度加分。
  - 由此換算「每點 stress 的價格」,發現**貴的床反而每元更划算**:
    `double_bed` $14,000/−3.82 = **$3,660/點**、`canopy_bed` $22,000/−8.65 = **$2,543/點**
    ⇒ premium 的每元效益比 standard 高 **44%**,`canopy_bed` 成為嚴格最優解、
    中階床沒有存在意義。這違反「tier 是花錢有回報的**選擇**」的初衷。
  - **根因**:第一階段的 `TIER_POINTS = 0 / 0.5 / 1.5` 敢給 premium 大躍升,是因為
    舒適度那條管道被 homeostasis 的 `K = 0.06` 嚴重稀釋;**睡眠這條管道沒有那層稀釋**,
    效果對 `(mult − 1)` 幾乎線性,照抄形狀就會直接放大成 ROI 失衡。
  - **新值 `1.12 / 1.22`**:對上 22000/14000 = 1.571 的價格比,`0.22/0.12 = 1.83`
    ⇒ 每元效益差由 44% 縮到 **實測 15.2%**(中控線性外推預估 ~17%,實測略低)。
  - **改後重量的 $/stress 點對照表**(240h / seed 20260710 / 陳家豪@r301,與 reviewer 同法):

    | 床 | 價格 | stress 均值 | 相對 budget 降幅 | $/點 |
    |---|---|---|---|---|
    | `single_bed`(budget 1.0) | $6,000 | 38.1182 | — | — |
    | `double_bed`(standard 1.12) | $14,000 | 34.9901 | **−3.128** | **$4,476** |
    | `canopy_bed`(premium 1.22) | $22,000 | 32.4561 | **−5.662** | **$3,885** |

    線性度覆核:`5.662 / 3.128 = 1.810` vs 係數外推 `0.22 / 0.12 = 1.833`(比值 0.987)
    ⇒ **這條管道確實近乎線性**,中控的外推方法成立。
  - 測試改成**守門員式**而非釘死數字:`furniture-tier-test.ts` §10-1 新增
    「premium 每元效益 ≤ standard 的 1.25 倍」(實測 **1.17**;初版 1.15/1.35 是 1.44,
    **這條斷言會直接擋下當初那組係數**),避免日後有人再把形狀抄回舒適度那套。

- **🔴 必修 2:`usedDefId` 的賦值位置(第三階段的地雷)**
  - 原本 `usedDefId = tgt.placement.defId` 設在**浴室排隊分支之後**,所以被擠成
    `waiting_for_bathroom`(沒真的用到設備)的租客仍會帶著那件家具的 id 出去。
  - 現在無害(只有睡眠吃乘數,而**睡眠不排隊**),但**第三階段接浴缸時
    排隊中的人會吃到 premium 浴缸的乘數**。已把賦值移進
    「目標格 = 家具互動格」的判斷內(`tick.ts:435-441`),並在該處留下警語註解。
  - **行為零變更的證明**:`balance-test`(未加 `--update`)✅ 綠燈、
    `git diff main -- scripts/balance-snapshot.json` **0 行**、
    探針實測 `state.money` = **59921**(與快照的 `money: 59921` 一致)、
    陳家豪 stress 均值 38.1182 / energy 均值 81.1625 / energy 最低 14 與修改前完全相同。

- **必修 3:死碼與可變 export**
  - 移除 `tier.ts` 的 `?? SLEEP_MULT[DEFAULT_TIER]` 與 `generate.ts` 的 `?? 1`——
    `tierOf()` 回傳 `FurnTier`、`SLEEP_MULT` 是涵蓋三階的完整 `Record`,
    這兩條 fallback **永遠走不到**,留著只會讓讀者以為有 undefined 路徑。
    兩處都改成一行註解說明「為什麼不需要 fallback」。
  - `SLEEP_MULT` 與 `SLEEP_MULT_RANGE` 改用 **`Object.freeze`**(前者另加 `satisfies
    Record<FurnTier, number>` 保留完整性檢查)。原本 `SLEEP_MULT` 是可變的 export 物件,
    reviewer 在探針裡直接於執行期改掉了它;`sleepMultiplier` 的 `[0.5, 2.0]` 夾值
    只擋得住「字面值寫錯」而且是**靜默改寫**,擋不住竄改。
    選 `Object.freeze` 而不只是 `as const`,是因為**只有它有執行期效力**。
    新增斷言 `Object.isFrozen(SLEEP_MULT)`。

- **必修 4:文件筆誤** — 上一筆日誌的「6 檔」更正為 **7 檔**(5 程式/測試 + 2 文件)。

- **必修 5:把排隊分支這個坑記進日誌** — 即上面必修 2 的內容。
  ⚠️ **給第三階段的檢查清單**(接浴缸/沙發/電視/書桌時):
  1. **確認排隊者不吃乘數**——`waiting_for_bathroom` 不是「在用浴缸」,
     且這條路徑未來可能被改回來,接手時請重新確認 `usedDefId` 仍在判斷內。
  2. **`effectState` 會讓畫面狀態與數值狀態分家**(`generate.ts` 的 key 是
     `ctx.effectState ?? ctx.state`,`decideState` 在浴室輪替與洗衣時會覆寫),
     會出現「泡的是 premium 浴缸、乘數卻乘在 `showering` 上」。
  3. **重算 ROI 對照表**——不要照抄舒適度的 `0 / 0.5 / 1.5` 形狀(見必修 1 的根因)。

- **順手**:§10-7 的換床驗證用 (900,900) 這個樓層外座標(靠 `addPlacement` 不驗邊界),
  加了一條前提斷言確認該格原本是空的——維持低成本,但不再是「安靜地量到別件家具」。

- **保留不動**(依 review 裁示)
  - **energy 乘數保留**。reviewer 認同種子局的飽和分析,但指出它在**睡眠被截斷**的分支
    才是主角:`stress ≥ 90 → pacing` 會吃掉整段睡眠、作息位移、事件把 energy 打到低點時,
    從 40 睡 4 小時 budget 到 76、premium 到 88.6,差距實打實。
    它是**「壞狀態下的復原加速器」**,只在種子局那種每天 7 小時飽和睡眠下才無感。
  - `canopy_bed` 的 `effectHint` 依 review 允許微調成
    **「睡眠恢復 ×1.22 · 睡得最安穩,累垮時恢復也最快」**——
    前半對應實際載體 stress,後半保留「睡眠被截斷時的復原」語感。

- **驗證結果**(全跑)
  - `npx tsx scripts/balance-test.ts`(**未加 `--update`**)→ **✅ 快照一致**;
    `git diff main -- scripts/balance-snapshot.json` **0 行**;`money` **59921** 未變。
  - `npx tsx scripts/furniture-tier-test.ts` → **51 → 54 通過 / 0 失敗**。
  - `npx tsx scripts/stats-model-test.ts` → **11 通過 / 0 失敗**。
  - `npm test` → **66/66**(67.7s);`npm run build` → **133 modules**、2.28s;
    `npm run typecheck` → app + worker 乾淨。
  - 一次性探針(三組床各跑 240h)**跑完已刪、不進 commit**。

- **已知限制/後續**:平衡快照**未更新**(不需要,仍零漂移);**未 push**。
  睡眠以外的家具恢復活動仍留第三階段,務必先讀上面那份檢查清單。

### 2026-07-30 — 樓寵物中途送養與「幸福新家」退場機制（Codex）

玩家長期選擇讓圓夢房客的動物留在公寓時，舊版會無上限累積樓寵物。本批將「留下來」
改成有容量、有過渡期、也保留故事痕跡的完整退場流程。

- **容量規則**
  - 永久樓寵物上限 **2 隻**；公寓內另有 **1 個中途照顧位**。
  - 超出的中途動物會先住進「合作中途家庭」，不再生成樓層 agent、事件或招租相容性影響，
    避免畫面與模擬繼續擁擠。
  - 舊存檔若已有 3 隻以上永久樓寵物，不會擅自刪除：寵物面板會要求玩家**剛好選 2 隻**
    永久留下，其餘才開始媒合。

- **退場流程**
  - 樓寵物可在新面板選「替牠找新家」，採二次確認；房客自己的動物只顯示、不允許房東送養。
  - 媒合期固定為 **5～8 個遊戲日**，由寵物資料做決定性雜湊，不耗用模擬 RNG，
    重讀存檔也不會改變期限。
  - 期限到時會清掉該動物的配對／冷卻資料並離開 `state.pets`，寫入最多保留 50 筆的
    `petHomes` 幸福新家紀錄。房客離開時託付的動物會以「與原主人團圓」收束；
    玩家主動送養則記為「由領養家庭接手」。
  - 中途期間若永久名額空出，玩家可取消媒合、讓牠正式留下；名額已滿時不可取消。

- **圓夢離場與舊存檔**
  - 未滿永久容量時仍提供「帶走／留在公寓／先由公寓中途」三選。
  - 永久名額已滿時不再提供會繼續堆積的「留在公寓」，改為「帶走／中途」。
  - 存檔版本 **v4 → v5**，補上 `petHomes` 與既有樓寵物安置欄位；載入後若有修復會立即回存。

- **UI**
  - 樓層控制列新增 `🐾 寵物`，超量舊檔會顯示警示徽章。
  - 新增「現在／幸福新家」底部面板：呈現永久與中途容量、剩餘媒合天數、舊檔選擇器、
    房客寵物唯讀卡及歷史去向。

- **主要檔案**
  - 核心與存檔：`src/sim/pets.ts`、`src/sim/persistence.ts`、`src/sim/gameState.ts`、
    `src/sim/wishes.ts`、`src/types.ts`、`src/store.ts`
  - 樓層／UI：`src/floor/petAgents.ts`、`src/components/PetPanel.vue`、
    `src/components/RecruitPanel.vue`、`src/App.vue`
  - 測試：`scripts/pet-test.ts`、`scripts/wish-test.ts`、`scripts/qol-test.ts`

- **驗證結果（全部實跑）**
  - 寵物／心願／存檔／舊功能專項：pet **90**、wish **153**、qol **24**、legacy **62**，
    全數通過。
  - `npm test`：**66/66 支通過**（pet 73 → 90、qol 22 → 24）。
  - `npm run typecheck`：app + worker 乾淨。
  - `npm run build`：成功，Vite **136 modules**；僅保留既有的單一 chunk > 500 kB 警告。
  - `npm run ui:shot -- rent`：三種手機尺寸共 **18 張**，`report.json`
    **0 errors／0 warnings**，已逐張檢查，新增寵物按鈕未與導覽或快轉控制重疊。
  - 互動瀏覽器當下沒有可用連線，因此未補拍展開後的面板；其元件由 typecheck、正式 build
    與流程測試驗證。這是目前唯一未完成的視覺人工操作檢查。

- **版本狀態**：以 `f971229` 為開始基準；本批由 `main` push 觸發 Cloudflare 自動部署，
  實際部署 commit 與正式站驗證以 Git／Cloudflare 狀態為準。

### 2026-07-31 — 群體劇情與樓層畫面同步（Codex）

修正「文字說房客一起到某處，樓層畫面卻仍各做各的」的落差。本批加入短暫的群體場景
導演，不改寫一般作息或平衡資料，只在事件演出期間以較高優先權接管參與者走位。

- **群體場景導演**
  - 新增 `src/floor/groupScene.ts`，支援交誼廳、天台與歡送會等 3～4 人場景。
  - 交誼廳會依家具阻擋狀態挑出不重疊的固定站位；天台沒有既有地圖區域，因此採樓層內
    的夕景小舞台，並隱藏參與者在主平面的重複 sprite。
  - 優先權為「歡送會 > 月度章節 > 日常社群 > 雙人互動 > 一般作息」；演出開始前會清除
    參與者衝突的雙人 session，結束後自動回到原本作息。
  - 場景同時依現實時間與遊戲時間過期，快轉後不會留下卡住的人物；站位全程決定性，
    不消耗 `Math.random()`。

- **劇情串接**
  - 「傍晚頂樓乘涼」、「雨天窩在交誼廳」、「週末電影夜」不再於午夜提前發生：
    午夜只決定參與者並寫入排程，到文案指定的 18:00／15:00／20:00 才結算與演出。
  - 排程進存檔，版本升為 **v6**；v5 舊檔會補空佇列，午夜到演出時段之間重新整理也不會
    遺失事件。已答應參加的人即使一般作息原本是外出，該時段仍會回來入鏡。
  - 樓層聚餐的「房東請客／住戶 AA」會把參與者帶到交誼廳桌邊；「婉拒」不建立假場景。
  - 月度章節中明寫住戶聚在交誼廳的都更爭論、颱風停電與共度長夜，也會同步多人走位與特效。

- **歡送會**
  - `farewellSendoff()` 在 `moveOut()` 前建立演員快照，保存姓名與角色配色。
  - 離開者從 runtime／房間占用移除後，歡送小舞台仍能把本人與留下的鄰居一起畫出來；
    不會再出現文字說全樓餞行、畫面卻只剩其他人的情況。

- **主要檔案**
  - 場景與樓層：`src/floor/groupScene.ts`、`src/floor/agents.ts`、
    `src/floor/pairSession.ts`、`src/components/FloorMap.vue`
  - 事件與時序：`src/sim/community.ts`、`src/sim/floorChain.ts`、
    `src/sim/tenancy.ts`、`src/sim/tick.ts`
  - 存檔：`src/sim/persistence.ts`、`src/sim/gameState.ts`、`src/types.ts`
  - 測試：`scripts/group-scene-test.ts`、`scripts/qol-test.ts`、`scripts/run-all.ts`

- **驗證結果（全部實跑）**
  - 新增群體場景測試 **14/14**；社群 **43/43**、章節 **50/50**、心願／歡送
    **153/153**、存檔與遷移 **25/25**。
  - `npm test`：**67/67 支通過**；`balance-test` 維持既有快照，沒有更新平衡基準。
  - `npm run typecheck`：app + worker 乾淨。
  - `npm run build`：成功，Vite **137 modules**；僅保留既有單一 chunk > 500 kB 警告。
  - `npm run ui:shot -- rent`：三種手機尺寸共 **18 張**，`report.json`
    **0 errors／0 warnings**，已逐張檢查。
  - 另以 390×844 專用低頻情境實際觸發並目檢「三人天台同框」與「退租後歡送快照」，
    DOM 斷言與兩張截圖皆通過；一次性腳本／截圖留在忽略的 UI Lab artifacts，不進 commit。

- **版本狀態**：以 `0b40d42` 為開始基準；本批尚未 push／部署。

### 2026-07-31 — 房客相性積怨與畢業紀念物收起（Codex）

加入「好感」之外的積怨軸，讓生活習慣不合的房客會逐步磨擦，也讓房東在事件中的選擇
能實際改善或惡化關係；同時補上畢業紀念物的安全退場入口，避免展示品無限累積。

- **相性、積怨與衝突節奏**
  - 相性會說明作息、噪音與界線感等具體原因；負相性配對不再靠一般社交穩定變成好友。
  - 關係新增獨立 `tension` 與上次自然口角時間；舊 v6 存檔缺欄位時自動補 0，並完整存回。
  - 同一配對自然口角有 12 遊戲小時冷卻；自然社交／打鬥全樓每日最多 2 場，使用固定存檔鍵，
    不會每天新增鍵；積怨每日自然下降 2。
  - 冷戰與打架要求積怨至少 70。冷戰自然結束會降溫；搬走時同步清除相關冷戰，避免幽靈關係。
  - 群體活動依相性折算關係增益；房東調解、共同完成事情會降怨，偏袒／警告或糾紛選擇可能加深心結。

- **玩家可讀資訊**
  - 招租卡會在入住前顯示與現住戶的最低相性風險及原因，全程決定性、不消耗 RNG。
  - 關係頁會列出只有積怨、尚未累積好感的配對，顯示好感／積怨雙條、相性原因、冷戰風險與降溫提示。
  - 高積怨或負相性會阻止自然成為情侶；既有取向、成年與唯一伴侶守門仍保留。

- **畢業紀念物收起**
  - 紀念物資訊新增「收起紀念物（$0）」；按鈕後還需通過第二次確認，不提供退款。
  - 操作只移除房內展示並立即存檔，不會刪除歷任房客、告別信或成就。
  - 使用專用的由上往下精確移除流程；即使紀念物與舊家具重疊，也只刪紀念物，不會誤刪底層家具。

- **驗證結果（全部實跑）**
  - 專項：relationship tension **12/12**、conflict **41/41**、community **48/48**、wish **158/158**。
  - `npm test`：**68/68 支通過**；`balance-test` 與既有快照零差異，未更新平衡基準。
  - `npm run typecheck`：app + worker 乾淨。
  - `npm run build`：成功，Vite **137 modules**；僅保留既有單一 chunk > 500 kB 警告。
  - `npm run ui:shot -- rent`：三種手機尺寸共 **18 張**，`report.json`
    **0 errors／0 warnings**，已逐張檢查，沒有溢位或導覽遮擋。
  - 互動瀏覽器當下沒有可用實例，因此未補拍低頻彈窗；招租提示、積怨資料與紀念物確認後端流程
    由型別、正式 build、存檔往返及重疊移除測試覆蓋。

- **已知範圍**
  - 每日 2 場上限目前限制自然社交／打鬥，不包含劇本排程或群體事件。
  - 永久隔音可阻止噪音公審事件，但尚未降低自然社交中的噪音相性口角率。

- **版本狀態**：以 `eb8305f` 為開始基準；本批尚未 push／部署。

<!-- ===== 新日期紀錄請加在這條線以下 / APPEND NEW DATE ENTRIES BELOW ===== -->

---

## 新紀錄追加點(APPEND HERE)

> ⬇️ **下一則日期紀錄請直接加在本節之下**(檔案最末端),格式沿用第七節:
> `### YYYY-MM-DD — 主題(執行 Agent)`,內容含目的／需求、實際變更與主要檔案、
> 驗證結果、已知限制或後續、硬規則是否保留。
>
> 加完後請順手做兩件事:
> 1. 把新標題補進第七節開頭的「日期紀錄索引」。
> 2. 若本次動到待辦狀態,同步更新**第四、五節**的 `- [ ]` / `- [x]` 與驗收錨點。

<!-- 新紀錄從這裡開始 -->

### 2026-07-31 — 工作日誌可導覽化:加目錄、封存 07-24 以前紀錄、設追加點(Claude Code)

- **目的／使用者需求**:本檔已達 3908 行,接手者很難找到「現在該做什麼」。本次只做**文件結構整理**,
  目標是可導覽 + 現況優先,**不刪任何內容、不改寫任何歷史判斷**。
- **實際變更(僅 2 個文件檔,未動任何程式碼)**:
    - `工作日誌.md`:檔頭新增「目錄」(七節 + 附錄 + 追加點的錨點連結,並標出第四、五節是現行待辦);
      第七節標題改為「進度補記(逐日日期紀錄,2026-07-25 起;更早見封存檔)」並加封存指標與
      `<details>` 收合的「日期紀錄索引」(34 則);檔尾新增 `## 新紀錄追加點(APPEND HERE)`
      與 HTML 註解分隔線,明確標示下一則日期紀錄要加在哪。
    - `docs/工作日誌-封存.md`(新增):**2026-07-12 ～ 2026-07-24 的 65 則日期紀錄逐字搬移**,
      自帶目錄與「不要在此追加新紀錄」的警語。
    - 第四、五節中 5 處指向已封存紀錄的交叉引用改寫為「見封存檔 …」(僅改指標,不改判斷);
      第四節可信度提示補上封存檔位置。歷史日期紀錄內文中的同類引用**刻意不動**(歷史段落保留原狀)。
- **斷點理由**:2026-07-25 是第一次「待辦清單與實際程式碼對帳」,其後又對帳兩輪(07-27、07-30),
  該日之前的逐日紀錄已全數收斂進第四、五節,封存不影響單一事實來源。
  第四、五節與驗收錨點制**全部留在主檔**。
- **驗證**:
  - 零資訊遺失——整理前的 **98 則**日期紀錄整理後仍是 98 則(主檔 33 + 封存 65,另加本則共 34),
    標題序列完全相同,且以腳本對每則做**逐字 body 比對全數相同**;
    第一～六節除上述 5 處指標外逐字相同。
  - 行數 `wc -l` 3907 → 主檔 2572 + 封存 1523 = **4095**(多出的是目錄、索引、指標與本則)。
  - 錨點——主檔 43 個、封存檔 65 個,以 GitHub slug 規則比對標題,**失效 0**。
  - `git diff --check` 乾淨。純文件變更,未跑 `npm test` / `build`。
- **已知限制／後續**:封存檔僅在斷點以前;若日後主檔再度膨脹,可沿用同樣做法把下一段搬過去。
- **硬規則**:未觸及任何遊戲/AI/戀愛/消毒邏輯;既有硬規則全數保留。

### 2026-07-31 — 人物美術第三批:活動椅依家具種類分造型(Claude Code)

- **目的／使用者需求**:桌前／電視前的臨時活動椅原本只有一種寫死的棕色木椅,不論坐在電競桌、
  直播設備台還是電視前都長一樣。本批讓它**依家具種類分成三種造型**,提高「一眼看出這人在幹嘛」的辨識度。
  硬性前提:**零 `agents.ts` 改動**(Codex 正在同一份 checkout 的 `main` 上工作,其 `eb8305f`
  改動正落在 `agents.ts:98-121`,而 `:103` 的 `a.facing` 與 `:117` 的 `a.seatBack` 都在該 hunk 內)。

- **實際變更(僅 `src/floor/floorScene.ts` 一支程式檔)**
    - `drawActivityChair()` 由 5 行寫死 `rect` 拆成「造型判定 + 四個繪圖函式」:
      `drawGamingChair` / `drawOfficeChair` / `drawLoungeStool` / `drawPlainChair`。
    - 新增 `activityChairStyle(a)` 與 `CHAIR_STYLE_BY_KIND`(`desk`→電競椅、`mic_desk`→辦公椅、
      `tv`→矮凳;這三種正是 `tick.ts:348` 唯一會給出 `activitySurface === "chair"` 的 kind)。
    - import 由 `getPlacements` 擴充為 `getPlacements, placementInteract, placementFootprint`。

- **三種造型的設計理由**:活動椅畫在角色**背後**,而坐姿 sprite(`CHAR_SIT`,11×14,畫在 `+3/+1`)
  會遮掉椅子正中央,所以辨識度不能靠椅面花紋,只能靠**側緣輪廓 + 底座 + 色相**這三件會露在角色外的元素。
    - **電競椅(`gaming_desk`)**:椅背拉高到 `y+1`、另加一條**高過頭頂**的頭枕,左右各一片 2px
      外露的**撞色紅包覆側翼**(`#b0453a`,上緣壓深做厚度),深藍黑殼 + 氣壓中柱 + 五爪底座。
      紅色是全樓層唯一的高彩度點,遠看就知道那是電競位。
    - **一般辦公椅(`mic_desk`)**:椅背只到 `y+3`(不過頭頂)、灰藍網布加兩道橫紋,
      側邊改成**腰高的短扶手**(`y+9` 起、只有 3px 高),單柱雙輪。與電競椅的差別是
      「有沒有紅、側件在肩高還是腰高、有沒有頭枕」,三個訊號同時不同。
    - **矮凳／交誼廳軟座(`tv` 系:`tv_console` / `lounge_tv` / `lounge_console`)**:
      **完全沒有椅背**——這是三者中最強的輪廓差異;改成厚布坐墊(橄欖綠 `#77836a`,
      與周圍暖木地板 `#a5825a`/`#b08a5e` 明確分色)加外八木腳。看電視本來就常坐矮凳,
      沒椅背也符合「臨時、隨性」的定位。
    - 全部沿用同檔既有程序美術風格:`rect` 拼像素、對齊 16px tile、低彩度(唯一例外是電競椅
      刻意的撞色紅,且面積只有 2×8×2 px)。

- **反查四鄰的做法與為何不改 `agents.ts`**
    - `a.seatBack` 來自 `agents.ts:117` ← `rt.activitySurface === "chair"` ← `tick.ts:348-351`,
      但 `Agent` 上**沒有** defId/kind。原訂做法是在 `agents.ts` 加一個 `a.seatKind`(2 行),
      正好落在 Codex 剛改過的 `:117`,故**改在 `floorScene` 內唯讀反查**,達成零 `agents.ts` 改動。
    - **實測推翻了「掃四鄰」的假設**。`activitySurface === "chair"` 時
      `rt.activityTile = fallbackTile`(`tick.ts:378`),而 `fallbackTile` 是
      `routine.ts:184 standingTile()` 的結果 = catalog 的 `interact` 偏移格(電競桌/電視櫃是 `dr: 2`),
      被擋時再退到家具外圈。實跑初始擺設得到:

      | 家具 | 佔位 | interact | 實際座位格 | 與家具的 Chebyshev | 四鄰掃描結果 |
      |---|---|---|---|---|---|
      | `gaming_desk` | (4,1) 2×1 | (4,3) 被擋 | **(3,1)** | 1 | 命中,但**會先撞到 `single_bed`** |
      | `tv_console` | (3,6) 2×1 | (3,8) 被擋 | **(2,5)** | 1(**斜對角**) | **全空,漏掉** |
      | `mic_desk` | (10,1) 2×1 | (11,3) 可走 | **(11,3)** | **2** | 只有 `floor_lamp`,**漏掉** |
      | `lounge_tv` | (10,9) 2×1 | (10,10) 可走 | **(10,10)** | 1 | 命中 |

      也就是說 `furnitureAt(a.c, a.r)` **必定**回 null(4/4),四鄰掃描只中 **2/4**。
    - 因此改成**反向比對**,兩段式:
      1. `placementInteract(p)` 與角色所在格完全吻合 → 直接命中(`mic_desk`、`lounge_tv` 走這條);
      2. 否則取 **Chebyshev ≤ 2 內最近**的一件 chair-kind 家具(`gaming_desk`、`tv_console` 走這條)。
         半徑 2 同時涵蓋 `claimCrowdTarget`(`agents.ts:219`)把人擠開一兩格的情形。
      因為先用 kind 白名單過濾,`gaming_desk` 座位旁的 `single_bed`(kind `bed`)不會被誤取。
    - **fallback 行為**:兩條都不中就回傳 `"plain"`,畫回**與本批之前逐像素相同**的棕色木椅,
      既不拋錯也不會漏畫椅子。玩家自由擺放家具導致座位偏移時,最差只是造型退回原樣。

- **刻意不做的兩項(附理由)**
    - **躺姿疊外觀(`src/pixel/scene.ts:511` 的 `lie()`)跳過**。`composeScene()` 全 repo 唯一呼叫端是
      離線腳本 `scripts/render-preview.ts:192`,**玩家看不到**;玩家真正看到的躺姿走
      `floorScene.ts drawLying()`,那裡**早就有完整髮型分支**(long/ponytail/spiky/bob)且支援
      `poseRotation` 四向。改 `lie()` 只會影響開發用的 `preview.png`,效益為零。
    - **四方向人物另開一批**。規模是本批的 4~6 倍(6~7 張新基底 sprite + `parts.ts` 30+ 個新視角
      overlay),且是唯一會撞 Codex 的項目。**已確認的零衝突路徑備忘**:在 `floorScene` 內
      **唯讀推導方向**——走路中用 `a.path[0]` 減 `{a.c, a.r}`(`pathfind` 是 4 鄰 BFS,保證正交),
      靜止時掃四鄰找家具。這樣同樣可做到零 `agents.ts` 改動,代價是「停下瞬間回正面」。
    - **atlas 清理(`scripts/limezu-manifest.json` 的 5 筆死 crop)另起獨立 commit**,
      讓 `furniture.png` 的二進位 diff 好 review／好 revert。

- **驗證結果(全部實跑)**
  - 目視:自組「五種座位對照圖」(電競椅／辦公椅／矮凳×2／回退木椅並排放大 10×),
    **三種造型 + 回退全部出現且可辨識**;紅側翼、灰藍扶手、無椅背橄欖坐墊三個訊號一眼可分。
    對照腳本與輸出圖為暫時性產物,**驗證後已刪除,未進 commit**。
  - 端對端:另以真實 sim 跑滿 24 小時,`activitySurface === "chair"` 共 **19 人次 / 6 組**
    不重複座位,反查結果 **6/6 正確、回退成 `plain` 者 0 組**
    (陳家豪 gaming/working_at_desk→電競椅、watching_tv→矮凳;林小婕 streaming/idle→辦公椅、
    watching_tv→矮凳)。
  - `npx tsx scripts/render-floor.ts`:成功寫出 768×1584,無破圖(此腳本不傳 agents,故不含人物)。
  - `furniture-art-test.ts` **90/90**、`limezu-art-test.ts` **79/79**、
    `rotation-pose-test.ts` **29/29**、`roomcam-test.ts` **9/9**(亮度門檻未被較深的椅子擦到)。
  - `npm test`:**68/68 支通過**,耗時 74.1s。`balance-test` 快照零差異,未更新平衡基準。
  - `npm run typecheck`:app + worker 皆乾淨。`npm run build`:成功,Vite **137 modules**,
    僅保留既有單一 chunk > 500 kB 警告。
  - `npm run ui:shot -- rent`:三種手機尺寸共 **18 張**,`report.json` **0 errors / 0 warnings**,
    已逐張檢查無破圖;如預期該時點兩位租客都站著,縮圖看不到椅子細節(椅子細節由上面的對照圖驗)。

- **已知限制／後續**
  - Chebyshev ≤ 2 是啟發式:玩家把兩件 chair-kind 家具擺得極近時,取最近的一件,
    極端情況可能挑錯造型——純美術影響,不影響任何模擬數值。
  - 三種造型目前只吃 sprite `kind`,不吃 `tier`;日後要做「平價/精品電競椅」需再擴一層。
  - 第四節「桌前／電視前的臨時活動椅只有單一棕色造型」已同步劃掉(`- [x]`);
    同節的「四方向人物」與「舊 renderer 躺姿」兩條**刻意維持未勾選**,理由如上。

- **硬規則**:純繪圖層改動,未觸及模擬、AI、戀愛或消毒邏輯;未成年排除戀愛線、成人內容遮蔽式
  且預設關、AI 輸出白名單／夾值／消毒等既有硬規則全數保留。
- **版本狀態**:branch `feat/art-batch3`,以 `b4cdd55` 為基準;**未 push、未併回 `main`**。

### 2026-07-31 — atlas 清理:移除 5 筆已被程序美術取代的死 crop(Claude Code)

- **目的／使用者需求**:第四節「atlas PNG／manifest 仍含 5 張不再使用的舊 crop」結案。
  `tv_console`、`lounge_tv`、`shared_sofa`、`loveseat`、`wood_chair` 早在
  2026-07-29 那批就被 `src/furniture/render.ts` 的專屬程序美術取代、也已從
  `src/art/limezu.ts` 的 runtime 白名單移除,但 manifest 與 atlas PNG 裡的像素還留著。
  **本項與同日「人物美術第三批」拆成獨立 commit**,讓二進位 diff 好 review、好 revert。

- **實際變更(2 個檔)**
    - `scripts/limezu-manifest.json`:`furniture` 區塊刪掉那 5 筆;另新增
      `replaced_by_procedural` 說明區塊,逐件記錄「被哪張程序圖取代 + 原 crop/atlas 座標」,
      避免日後有人不明就裡把它們加回去(會與程序美術重複)。建置腳本與測試都不讀這個 key。
    - `public/assets/limezu/furniture.png`:重跑 `python scripts/build-limezu-atlas.py` 產出。

- **先確認管線是可重現的才動手**:在**還沒改 manifest 前**先原樣重跑一次建置,
  三張 PNG 的 sha256 與版控中的完全相同(`git status` 乾淨),證明本機 PIL 產出是決定性的、
  重跑不會挾帶無關的二進位雜訊。這是動 atlas 的前置條件,不通過就會依交辦指示直接跳過本項。

- **兩個硬性限制的達成證明**
    - **尺寸維持 128×128**:重建後 `Image.open(...).size == (128, 128)`,
      `limezu-art-test.ts:85-88` 的尺寸斷言照過。
    - **其餘 18 個 frame 的 sx/sy 座標與像素不變**:atlas 座標在 manifest 中是**手工指定**的
      (不是自動排版),刪除項目不會讓其他 frame 重排。另以腳本把重建前後的 PNG 對
      `LIMEZU_FURNITURE_FRAMES` 全部 **18 筆**逐一裁出矩形做 `tobytes()` 比對,
      **18/18 逐像素完全相同**;manifest 剩餘 17 筆 crop 也另比一次,同樣全同。
      因此 `src/art/limezu.ts` 與 `scripts/limezu-art-test.ts` **零改動**(`git status` 只有 2 個檔)。
    - 5 塊死區重建後 alpha extrema 全為 `(0, 0)`,即**完全透明**,確認像素真的被清掉。
    - `floors.png` 與 `walls.png` 的 sha256 與清理前**完全相同**,沒有連帶改到。

- **實際效益(如預期地小)**:`furniture.png` **8,696 → 6,992 bytes(−1,704,−19.6%)**;
  非透明像素 **11,607 → 7,795(−3,812,−32.8%)**。atlas 尺寸不變,所以**解碼成本與 GPU
  記憶體佔用零改善**,純粹是整潔與「素材清單即事實來源」的一致性。

- **驗證結果(全部實跑)**
  - `limezu-art-test.ts` **79/79**(含尺寸斷言與「5 個舊 id 已從 runtime 映射移除」)、
    `furniture-art-test.ts` **90/90**。
  - `npm test`:**68/68 支通過**,耗時 77.0s。`npm run typecheck`:app + worker 乾淨。
    `npm run build`:成功,僅保留既有 chunk > 500 kB 警告。
  - `npm run ui:shot -- rent`:18 張、**0 errors / 0 warnings**,逐張確認床、桌、冰箱、
    植栽、洗衣機、沙發、電視等家具全部照常顯示,**沒有因清 atlas 而破圖或缺圖**。

- **已知限制／後續**
  - 第四節該條的 `rg` 驗收錨點已**改寫**:新增的 `replaced_by_procedural` 說明區塊含有那 5 個
    字串,舊錨點會照樣命中而誤判未完成,故改成只檢查 `furniture` 區塊的 python 精確檢查。
  - 原始付費素材仍不進版控;要復原這 5 筆請回查 git 歷史或 manifest 的說明區塊。

- **硬規則**:純素材檔整理,未觸及任何遊戲/AI/戀愛/消毒邏輯;既有硬規則全數保留。
- **版本狀態**:branch `feat/art-batch3`,接在同日「人物美術第三批」之後;**未 push、未併回 `main`**。

### 2026-07-31 — 活動椅 review 收尾:半徑收窄至 1、加版本號 memo、補技術債(Claude Code)

- **目的／需求**:中控 review 通過但列了四項必修。本則是收尾,只動
  `src/floor/floorScene.ts` 的反查函式與文件,造型像素、atlas、`agents.ts` 全部零改動。

- **必修 1:Chebyshev 半徑 2 → 1(這是真的 bug,不只是保守化)**
  - 原本寫「半徑 2 是為了涵蓋 `claimCrowdTarget` 擠位」,**這個理由不成立**:
    `agents.ts:219-240` 的 `dc = radius - Math.abs(dr)` 是 **Manhattan** 環、上界到 **4**,
    半徑 2 既蓋不住上界、又把誤吸面積翻倍。被擠開的人**本來就該安全回退成木椅**,不該猜。
  - 已獨立重現 reviewer 指出的錯誤(表列為實跑結果,半徑 2 → 半徑 1):

    | 情境 | 格 | 半徑 2 | 半徑 1 |
    |---|---|---|---|
    | `gaming_desk` 使用者停在 row 4 | (1,4)/(4,4)/(6,4) | **stool(錯)** | plain |
    | `tv_console` 使用者停在 row 3 | (2,3)/(3,3) | **gaming(錯)** | plain |
    | r301↔r302 跨牆帶 | (7,3) | **gaming(隔牆)** | plain |
    | 同上 | (8,3) | **office(隔牆)** | plain |

    成因:row 4 到 `gaming_desk` 是 dy=3(出圈)、到 `tv_console` 是 dy=2(入圈),
    於是電競位被畫成矮凳;反向亦然。觸發條件真實存在——互動格 `(4,3)` 被佔、snap 失敗時
    agent 會停在 row 4 但仍照畫坐姿。
  - 另做**全樓層 16×33 掃描**:半徑 1 之下「同時落在兩件 chair-kind 家具涵蓋區」的格數為 **0**
    (半徑 2 之下存在重疊)。半徑抽成具名常數 `CHAIR_LOOKUP_RADIUS` 並在註解寫明不要放大。
  - **重跑 24 小時 sim 反查:仍是 19 人次 / 6 組、6/6 正確、回退 0 組**,正確率未因收窄而下降。

- **必修 2:效能——選了 `placements.version` memo,不是提前 break**
  - 改動前是「每幀 × 每個坐姿角色」各跑一次全表掃描(約 35 件),每件都
    `getDef` + `placementInteract` + `placementFootprint`,後兩者各配置新物件。
  - **沒有採用 `dist === 0` 提前 break**,兩個理由:
    (a) 對本情境是**死碼**——家具佔位格是 blocked,走 `chair` 分支的角色永遠站在家具**外面**,
        `dist` 不可能是 0;
    (b) 更關鍵的是**任何距離分支的提前 break 都會改變答案**:優先序不是單調的,
        「互動格吻合」優先度最高但幾何距離可能是 2(`mic_desk` 正是 interact 吻合卻 dist=2),
        單趟掃描裡一旦因距離命中就 break,會漏掉排在後面、其實該優先採用的互動格吻合項。
  - 改成以**格座標**為鍵的 `Map` memo,`placements.version` 一變就整份作廢。鍵用格座標而非
    tenantId,同一格換人坐也能命中。已實測三種異動的 version 都會 ++:
    `placeAt` 0→1、`moveFurnitureTo` 1→3(remove+add 各一次)、`sellFurnitureAt` 3→4,
    因此不會給出過期答案;`pathfind.ts:41` 早就用同一個版本號快取障礙格,沿用同一套機制。

- **必修 3:註解補上「跨牆誤吸」**——已在函式註解明寫這是純幾何啟發式、
  **完全不看牆與房間邊界**,靠牆擺放可能隔牆吸到隔壁房家具,半徑收到 1 後風險大降但未消失。

- **必修 4:技術債正式條目**——第五節新增「活動椅造型用幾何啟發式反查家具」:
  `tick.ts:439-441` 手上就有 `tgt.placement`(`usedDefId` 甚至已 hoist),之所以改用幾何猜測
  純粹是為了守本批 lease 的「零 `agents.ts` 改動」。修法是由 `tick.ts` 把 defId 存進 runtime
  直傳給 renderer,**用事實取代猜測**,順帶消掉跨牆誤吸與每幀全表掃描。附驗收錨點。

- **順手項:atlas 驗收錨點補上像素證明**——原錨點只驗 manifest。已改成一行 PIL 檢查,
  同時驗「manifest 殘留」「尺寸仍 128×128」「5 塊死區 alpha extrema 全為 (0,0)」,
  實跑輸出 `manifest殘留 [] |尺寸 (128, 128) |死區非全透明 []`。

- **驗證結果(全部實跑)**
  - `furniture-art-test` **90/90**、`limezu-art-test` **79/79**、`rotation-pose-test` **29/29**、
    `roomcam-test` **9/9**、`balance-test`(未加 `--update`)**快照一致**。
  - `npm test` **68/68**(73.2s);`npm run typecheck` app + worker 乾淨;`npm run build` 成功。
  - 24 小時反查驗證見上(6/6 正確、零回退)。造型像素零改動,故未重跑 `ui:shot`。

- **已知限制／後續**:跨牆誤吸與幾何啟發式本身的殘留風險已收進第五節技術債,
  等 `agents.ts` 可動時一併解決。
- **硬規則**:純繪圖層與文件改動,未觸及模擬、AI、戀愛或消毒邏輯;既有硬規則全數保留。
- **版本狀態**:branch `feat/art-batch3`,接在同日「atlas 清理」之後;**未 push、未併回 `main`**。

### 2026-08-01 — 四方向人物:背面/側面基底 sprite、疊層與 renderer 內方向推導(Claude Code)

- **目的／使用者需求**:人物在樓層上**永遠正面**——走上樓、走左右、走下樓長得一模一樣,
  只有 `stand_face` 會補一顆側眼當提示。本批把人物升級成**四方向**(下=正面、上=背面、
  左右=側面),並在 **`agents.ts` 零改動**的前提下完成。

- **實際變更(4 個檔)**
  - `src/pixel/sprites.ts`:新增 **6 張手繪基底 sprite**(全部 11×19)+ `mirrorSprite()` +
    `CharView` 型別 + `CHAR_SPRITES` 查表。
  - `src/pixel/parts.ts`:新增 `HAIR_OVERLAYS_BACK` / `HAIR_OVERLAYS_SIDE`(**10 組新髮型疊層**)、
    `ACCESSORY_OVERLAYS_BACK` / `ACCESSORY_OVERLAYS_SIDE`、`overlayViewOf()`;
    `pat()` 加 `mirror` 參數;`drawAppearanceOverlay()` 加**選用**的 `view` 參數(預設 `"front"`)。
  - `src/floor/floorScene.ts`:`agentView()` 方向推導 + `lastFacingDir` 記憶 + sprite dispatch;
    **移除 `drawFacingCue()`**。
  - `scripts/appearance-test.ts`:擴充成四視角的越界/像素/輪廓檢查,並新增方向推導優先序斷言。

- **6 張新 sprite 的設計要點**
  - **背面**(`CHAR_STAND_BACK` / `CHAR_WALK_BACK_A` / `_B`):rows0-8 **全是髮色**,
    **沒有 `k` 眼睛、沒有 `m` 嘴**,只在 row8 露一小段暗膚 `f` 當後頸。這是「背對鏡頭」
    唯一且最強的辨識訊號。高光改成 col4→col3→col2 的斜向反光帶(而不是左右對稱兩點,
    避免被誤讀成眼睛)。衣服改用 `CHAR_SIT_BACK` 既有的「左亮右暗、無領口」打光,
    和正面的對稱領口分開。走路兩幀的軀幹與腿部**取正面 A/B 的鏡射關係**
    (背面 A 用正面 B 的下半身,反之亦然),因為從背後看左右是相反的。
  - **側面**(`CHAR_STAND_SIDE_R` / `CHAR_WALK_SIDE_R_A` / `_B`,只畫朝右):
    臉的右緣逐列外推再內收——row2→col8 額、row3→col8 眉、**row4-5→col9 鼻樑與鼻尖**、
    row6→col7(人中一次內縮 2 格)、row7→col7 唇、row8→col5 頸;頭頂也往後(左)偏一格。
    這條起伏才是「側臉不是正臉」的關鍵,單靠「把臉挪到右半邊」在 11px 下看不出來
    (第一版就是這樣,目檢直接退回重畫)。眼睛只有 **1 顆**(row5 col7),嘴 1 個。
    軀幹輪廓從正面的 **9 格收到 7 格**、**只露一隻手**;腳掌朝前伸出。
  - **鏡射做法**:朝左**不另外手繪**。`mirrorSprite()` 逐列反轉字串,在模組載入時
    一次算出 `CHAR_STAND_SIDE_L` 等三個常數(**不是每幀算**,零額外配置),
    再由 `CHAR_SPRITES` 這張 `Record<CharView, {stand,walkA,walkB}>` 查表。
    單一手繪來源 → 左右永遠對稱,測試也直接斷言 `side_l === mirrorSprite(side_r)`。
  - **坐姿刻意不動**:`CHAR_SIT_BACK` 早就存在,但本批**不碰坐姿的方向邏輯**,
    避免和活動椅那批的 `seatBack` 打架(見下方「沒做什麼」)。

- **髮型疊層:5 種 × {背面, 側面} = 10 組**
  疊層座標系與正面完全相同(11 寬、sprite 原點),但兩個視角的著力點不同:
  - **背面**沒有瀏海可遮,辨識點改成「後腦簾子/辮子」——長髮是蓋過後頸一路垂到上背(row11)
    的簾子、鮑伯是齊頸且**下擺外翻**(寬到 col0/col9)、馬尾是**沿背脊垂下的辮子**(row5→row12)
    加一圈亮色髮圈、刺蝟從背後看一樣是尖刺、短髮只補頭頂反光。
  - **側面**髮量往後腦(左)堆並往左外擴到 col0:長髮垂到肩胛下、鮑伯齊下巴且下擺外翻、
    馬尾**先往後翹再垂下**(側面最好認的一種)、刺蝟的尖刺往後偏。
  - ⚠️ 背面的長髮與馬尾**刻意垂到 rows9-13**(上背),所以 `appearance-test` 的垂直越界檢查
    對 back/side 放寬到 `-2..13`(止於軀幹下緣 row13,不會掉到腿上);**正面維持原本的 `-2..10` 不變**。

- **配件在各視角的處置表**(依「看得見才畫」的原則)

  | 配件 | 背面 | 側面 |
  |---|---|---|
  | `none` | —(空) | —(空) |
  | `glasses` | **不畫**(從背後完全看不到鏡片) | 畫,簡化成**單片鏡片 + 一根往後的鏡腳** |
  | `round_glasses` | **不畫** | 畫,簡化成**單顆圓框 + 鏡腳**(框內留白讓眼睛透出) |
  | `cap` | 畫:**去掉向前的帽簷**,補一條後扣帶 | 畫:**帽簷向前伸出輪廓外**(側面最強辨識點) |
  | `bow` | 畫(髮上蝴蝶結從背後最清楚;位置是正面的鏡射) | 畫,移到腦後 |
  | `headphones` | 畫:頭帶 + **雙耳罩**(背面反而最完整) | 畫:頭帶 + **單邊耳罩**(遠側被頭擋住) |

  「背面眼鏡類完全不畫」是**刻意**的,而且比硬畫更正確。代價是背面的 `none`/`glasses`/
  `round_glasses` 三者疊層像素完全相同,所以測試裡「六種配件輪廓互異」這條在背面改成
  「眼鏡類**必須**為 0 像素 + 其餘四種互異」,不是放寬,是換成更精確的斷言。

- **方向推導:在 `floorScene` 唯讀,零 `agents.ts` 改動**
  優先序(`agentView()`):
  1. `pairSession` 的 `stand_face` facing(`a.facing !== 0`)→ 維持既有行為,側面朝向對方
  2. 移動中 → `a.path[0]` 減 `{a.c, a.r}`。`findPath()` 是 **4 鄰 BFS(無對角)**
     (`pathfind.ts:57-62`),差值保證是正交四方向之一
  3. 靜止 → 沿用最後已知方向
  4. 都沒有 → 正面
  - **例外**:`pose === "cook_pair"` 一律回正面。既有的 `drawCookingCue()` 是照**正面基底**
    畫的抬手 + 鍋鏟像素,套在背面/側面骨架上會錯位;並肩料理本來就面向流理台,不失真。
  - **為什麼用 renderer-local 的 `Map<tenantId, CharView>` 而不是寫回 `Agent`**:
    (a) 朝向純粹是畫面問題,寫回模擬層會讓 `Agent` 多背一個沒人用的欄位,而該檔是別的 agent
        的活動區,本批的硬性約束就是不碰它;
    (b) 「靜止沿用最後方向」需要**跨幀**記憶,而 `tickAgents` 每幀重寫 `a.facing`
        (`agents.ts:103`),放在那裡會被覆蓋;
    (c) **決定性**:離線單幀渲染(`render-floor.ts` / `roomcam-test`)時這張表是空的,
        規則 3 落空 → 一律回退規則 4 的正面,和舊行為完全一致。另外export 了
        `clearFacingMemory()` 供測試把狀態清乾淨。
    Map 大小上限 = 樓層租客數(個位數),不需要主動清理。

- **`drawFacingCue()` 的處置:整個移除**
  它原本是「在正面基底上補一顆朝同伴方向的眼睛與鼻尖」的補丁,前提是**基底永遠正面**。
  現在 `stand_face` 且 `facing !== 0` 會直接畫**真正的側面 sprite**,側臉本身就自帶單眼與
  凸出的鼻樑,再補一顆點只會落在錯的位置(它寫死 `x+6`/`x+3`,和新的側臉幾何對不上)。
  `facing === 0`(垂直相鄰)舊版本來就 early-return 不畫,所以**沒有行為退化**。

- **驗證(全部實跑)**
  - **對照表 PNG 目檢(本批主力驗收)**:離線產了三張——姿勢 × 方向 × 5 髮型 ×{有/無配件}
    的 14×12 格對照表、四方向四髮型的 14 倍放大檢視、以及走 `composeFloor` 真實 dispatch 的
    樓層帶狀圖。結論:**四個方向一眼可分**(正面雙眼對稱 / 背面純髮無臉 / 側面單眼且臉偏向一側,
    左右互為鏡像);**背面確實看不到臉**;側面輪廓在第二版重畫後成立(第一版只把臉挪到右半邊,
    目檢看起來仍像正臉,已退回重畫鼻樑起伏);五種髮型在四個視角都仍認得出是同一種
    (長髮=最長的簾子、鮑伯=齊頸外翻、馬尾=辮子/後翹、刺蝟=尖刺、短髮=無額外輪廓)。
    **三張 PNG 與產圖腳本跑完即刪,未進 commit。**
  - **方向推導硬驗證**:離線用「畫/不畫同一幀的像素差集 vs 預期 sprite 遮罩」逐條比對
    四條優先序 + cook_pair 例外,**10/10 通過**;同一組斷言已用 `agentView()` 直接
    永久化進 `appearance-test.ts`(不需畫布)。
  - `appearance-test.ts` **36 通過 / 0 失敗**(擴充前 17 條)。
  - `roomcam-test.ts` **9/9**、`rotation-pose-test.ts` **29/29**。
  - `balance-test.ts`(**未加 `--update`**)**快照一致** → 證明 `SKIN_TONES` /
    `ALL_HAIR_STYLES` / `ALL_ACCESSORIES` 三個陣列的順序與長度**一個字元都沒動**,
    `randomAppearance()` 的 seeded 序列零位移。
  - `npm test` **68/68 支通過(74.4s)**;`npm run typecheck` app + worker 乾淨;
    `npm run build` 成功。
  - `npm run ui:shot -- rent`:三種手機尺寸共 **18 張**,`report.json` **0 errors / 0 warnings**,
    已逐張檢查沒有破圖。⚠️ 只有 `01-floor*` 那 6 張看得到人物,而且是 16px tile 的全樓層縮圖,
    對 11×19 的細節判斷幫助有限,**不當作美術驗收**(美術驗收是上面的對照表)。

- **我沒做什麼(刻意留下的邊界)**
  - **坐姿的方向邏輯沒動**。`CHAR_SIT` 一律正面、`CHAR_SIT_BACK` 一律由 `scene.ts` 的房間鏡頭
    自己決定,`floorScene` 的 `pose === "sit"` 分支維持原樣。理由是避免和活動椅那批的
    `seatBack` 打架;要接的話應該在同一批一起做。
  - **`src/pixel/scene.ts`(離線 renderer)沒動**。它畫的是房間細看鏡頭裡的靜態人物,
    沒有 `path`/`moving` 可推方向;`drawAppearanceOverlay()` 的 `view` 參數有預設值 `"front"`,
    所以它的行為與像素**完全不變**。
  - **`src/floor/agents.ts` 零改動**(硬性約束)、`groupScene.ts` / `petAgents.ts` / 任何 `.vue` /
    `src/sim/*` / `scripts/run-all.ts` / `balance-snapshot.json` 皆未觸碰;**未新增測試檔**。
  - 躺姿(`drawLying`)維持既有的髮型/配件分支,本批未擴充。

- **已知限制／後續**
  - 四方向只覆蓋**站與走**。坐、躺、`cook_pair` 一律正面。
  - `lastFacingDir` 不隨租客搬離而清除(上限是租客數,實務上無影響);要嚴謹可在
    `createAgents()` 時呼叫 `clearFacingMemory()`,但那需要動 `agents.ts`,留給後續。
  - 背面的長髮/馬尾會蓋住上背,所以走路時背面的手臂擺動在這兩種髮型下較不明顯。
- **硬規則**:純繪圖層與渲染層改動,未觸及模擬、AI、戀愛或消毒邏輯;
  未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒等既有硬規則全數保留。
- **版本狀態**:branch `feat/char-facing4`,以 `dc65603` 為基準;**未 push、未併回 `main`**。

### 2026-08-01 — 四方向人物 review 收尾:側面馬尾拉開辨識度、鏡射軸守衛、註解修正(Claude Code)

- **目的／使用者需求**:中控 review 通過(無阻斷性問題),但用色彩感知量測抓出三處收尾。
  本則處理必修 1~3 並補一條技術債。

- **必修 1:側面馬尾與鮑伯只差 4px(全批最弱)**
  - reviewer 量的是「站姿基底 + 髮型疊層畫完後,兩種髮型之間**換色的像素數**」。
    我先自寫同一套量測復現了它的全部數字(front 9 / back 10 / side_r 4,
    以及四視角站姿的 front↔back 48),確認量法一致才動手。
  - 病因:`HAIR_OVERLAYS_SIDE` 的 `ponytail` 與 `bob` 在側面幾乎重疊——側面頭部的
    髮區是 cols1-4,兩者畫在 cols0-3 的像素**大多落在本來就是髮色的格子上**,
    真正看得到的只有 col0 那幾點與腦後空白處。舊馬尾只垂到 y=7,和鮑伯的下擺同高。
  - 修法:把側面馬尾改成**高處束起 → 往後翹出輪廓外 → 收成 1px 細辮子一路垂到 y=12**
    (垂過肩後、蓋在軀幹上)。刻意做成「細而長」,和長髮的「寬簾子」、
    鮑伯的「齊頸即止」三者互相分開,而不是只把馬尾加粗。
  - 結果:**side_r / side_l 的最小差異 4 → 9**,與正面的 9 齊平;
    同時 ponytail/short 也從 8 → 13。其餘視角未受影響(front 9、back 10 不變)。

  改後的四視角髮型兩兩換色像素矩陣(對角線為 `-`):

  | front | short | long | ponytail | spiky | bob |
  |---|---|---|---|---|---|
  | short | - | 20 | 13 | 14 | 20 |
  | long | 20 | - | 17 | 28 | **9** |
  | ponytail | 13 | 17 | - | 21 | 14 |
  | spiky | 14 | 28 | 21 | - | 28 |
  | bob | 20 | **9** | 14 | 28 | - |

  | back | short | long | ponytail | spiky | bob |
  |---|---|---|---|---|---|
  | short | - | 33 | 24 | 15 | **10** |
  | long | 33 | - | 28 | 41 | 31 |
  | ponytail | 24 | 28 | - | 31 | 28 |
  | spiky | 15 | 41 | 31 | - | 19 |
  | bob | **10** | 31 | 28 | 19 | - |

  | side_r(side_l 逐格相同) | short | long | ponytail | spiky | bob |
  |---|---|---|---|---|---|
  | short | - | 17 | 13 | 13 | 10 |
  | long | 17 | - | 12 | 24 | 13 |
  | ponytail | 13 | 12 | - | 24 | **9** |
  | spiky | 13 | 24 | 24 | - | 17 |
  | bob | 10 | 13 | **9** | 17 | - |

  各視角最小值:**front 9(long/bob)、back 10(short/bob)、side_r 9(ponytail/bob)、
  side_l 9(ponytail/bob)**。改動前 side 是 4。

  - **新斷言**:`appearance-test.ts` 加「四視角髮型兩兩最少換色 ≥ **8** px」。
    門檻取 8 的理由:實測最小值是 9,取略低一格是為了留給日後微調的空間
    (例如換髮色或微調一兩個像素不該紅燈),但任何把辨識度砍回「幾乎看不出差別」
    的改動都會立刻擋下。**負向驗證**:把側面馬尾改回舊版短辮子,該條立刻紅燈並印出
    `side_r=4(ponytail/bob) side_l=4(ponytail/bob)`,確認不是空轉的斷言。

- **必修 2:`pat()` 的鏡射軸沒有寬度守衛**
  - `pat()` 的鏡射是 `SPRITE_W - 1 - c`,**以固定 11 為軸**而不是逐列長度。
    目前所有疊層列都是 11 寬,但將來若有人寫了 10 寬的行,朝左側面會**靜默位移 1px**。
  - 關鍵是既有的「`side_l` 疊層 === `side_r` 的鏡射」那條**抓不到**——兩邊用同一個公式,
    誤差會對消。所以另加一條直接驗寬度的斷言:`parts.ts` 新 export `SPRITE_W` 與
    `overlayRowWidths()`(回傳每一列的 `id` + `width`),測試斷言全部 === `SPRITE_W`。
  - **負向驗證**:把 `side/glasses` 的那列改成 10 寬,新斷言紅燈並精準指出
    `acc/side/glasses/0[0]=10`;而同一次執行裡**鏡射那條仍是綠的**,
    正好實證了 reviewer 說的「對消、抓不到」。

- **必修 3:`lastFacingDir` 的註解與事實不符**
  - 舊註解寫「大小上限 = 樓層租客數(個位數),不需要主動清理」是**錯的**:
    tenantId 是帶時間戳的唯一值(如 `tenant_r303_1785546924760_0`),退租時 key 不會被移除,
    所以這張表在長時間遊玩下是**單調成長**的,上限是「本次 session 曾出現過的所有租客數」。
  - 依 review 指示**不改程式**(每筆一個短字串鍵 + 一個列舉值,幾百筆也在數十 KB 等級,
    且 session 結束就整份消失),只把註解改成正確描述,並註明要清的話最自然的掛點是
    `createAgents()`、但那要動 `agents.ts`。

- **新增技術債(第五節)**:「`stand_face` 垂直相鄰時朝向不精確」。
  `pairSession.ts:79-82` 的 `facingToward()` 只在水平相鄰時給 `±1`,垂直一律 `0`,
  於是會落到規則 3「沿用最後走過的方向」——從下方走上來剛好正對(比舊版好),
  但從左側繞過來就會渲染成側面。**不是退化**(舊版一律正面也沒面向對方),
  只是換了一種不精確。修法是讓 `sessionFor()` 把對方 tile 或已算好的四方向一起回傳,
  擺脫 `facing: -1|0|1` 這個只表達得了左右的型別;需要動 `agents.ts` 與 `pairSession.ts`,
  本批 lease 明文禁止,故留待下批。已附驗收錨點。

- **驗證(全部實跑)**
  - `appearance-test.ts` **38 通過 / 0 失敗**(本次 +2 條);兩條新斷言都做過負向驗證(見上)。
  - `roomcam-test.ts` **9/9**、`rotation-pose-test.ts` **29/29**。
  - `balance-test.ts`(**未加 `--update`**)**快照一致**——本批只動疊層像素與註解,
    三個 RNG 陣列依然零觸碰。
  - `npm test` **68/68(71.6s)**;`npm run typecheck` app + worker 乾淨;`npm run build` 成功。
  - **對照表 PNG 重產目檢**:四方向 × 五髮型 × 站/走A/走B,13 倍放大。側面馬尾改動後
    細辮子垂在肩後、走路兩幀都貼合,與長髮的寬簾子、鮑伯的齊頸下擺三者一眼可分,
    其他視角未受影響。**PNG 與兩支量測/產圖腳本跑完即刪,未進 commit。**
  - 未重跑 `ui:shot`:本次只動了側面馬尾疊層的幾個像素與註解/測試,
    上一則已確認 18 張 0 errors / 0 warnings,且樓層縮圖本來就看不出這級別的差異。

- **已知限制／後續**:見上方新增的第五節技術債條目。
- **硬規則**:純繪圖層與測試/註解改動,未觸及模擬、AI、戀愛或消毒邏輯;既有硬規則全數保留。
- **版本狀態**:branch `feat/char-facing4`,接在同日「四方向人物」之後;**未 push、未併回 `main`**。

### 2026-08-01 — 文件對帳:清兩個孤兒、修三個壞錨點、補 10 條漏記、錨點制加三規則(Claude Code)

- **目的／使用者需求**:上一次整理(`b4cdd55`)建立了「驗收錨點制」,本次是**第一次機械執行**
  ——逐條把第四、五節的錨點 grep 一遍,再拿結果和條目狀態對帳。
  25 條當時可驗證的錨點裡 **22 條正確、3 條錯誤**,錯誤的三條全部指向同一個結構性問題:
  **錨點是寫條目時的「預言」,沒人在完成時回頭把它改成「證據」**。
  本次只做文件:打勾、修錨點、補漏記、同步交接文件、把該問題寫成規則。**未動任何程式碼。**

- **實際變更(僅 2 個文件檔)**:`工作日誌.md`、`docs/交接與待辦.md`。
  `src/`、`scripts/`、`worker/`、`docs/工作日誌-封存.md` 全數未觸碰。

#### A. 兩個孤兒條目(程式早已完成、checkbox 仍掛著)

1. **四方向人物**——第四節「內容與呈現(美術債)」與「美術第二批留項」**兩處各有一條**,
   都還是 `[ ]`。實際已於 `569d763` + `21b8b5c` 完成:`src/pixel/sprites.ts:263` 的
   `export type CharView`、`:416-418` 三組 stand/walkA/walkB、`src/floor/floorScene.ts:359`
   的 `agentView()`、`src/pixel/parts.ts:343-345` 的 `overlayViewOf()` 疊層與鏡射。
   兩處都改 `[x]`,並**收斂成一主一從**:完整說明與錨點只留在第一處,第二處改成純指標
   ——**重複列本身就是孤兒的來源**(一處劃掉、另一處忘了劃),兩邊各寫一份必然再度分歧。
   同時依錨點規則 3,把「只覆蓋站與走」另立成第五節的一條 `- [ ]`。
2. **月度事件鏈「全員擠交誼廳」的多人站位演出**——已由 Codex `eb8305f` 完成:
   `src/floor/groupScene.ts`(159 行新檔)的 `startGroupScene()` / `groupSceneFor()` /
   `groupSceneView()`,`src/sim/floorChain.ts:56` 的 `scene?: { layout }` 且 `:98`/`:163`/`:186`
   三個 stage 已接上,`loungeTiles()` 依家具阻擋挑不重疊站位,
   `src/floor/agents.ts:98` 優先於 pair session。
   除了打勾,**條目內文也一併更正**——它原本寫著「`startPairSession` 仍是 pair-only」,
   那句話現在是錯的(演出層已支援多人)。

#### B. 三個壞錨點的改寫(附實跑結果)

| 條目 | 舊錨點 | 實跑 | 新錨點 | 實跑 |
|---|---|---|---|---|
| 四方向人物 | `rg -n "CHAR_WALK_(UP\|DOWN)\|facingDir\|facing4" src/pixel/ src/floor/` | **0 命中**(誤判未完成) | `rg -n "CharView\|CHAR_STAND_BACK\|agentView" src/pixel/sprites.ts src/floor/floorScene.ts` | **8 命中**,正確反映已完成 |
| 多人站位演出 | `rg -n "startGroupSession\|startMultiSession" src/` | **0 命中**(誤判未完成) | `rg -n "startGroupScene\|groupSceneFor" src/floor/groupScene.ts src/floor/agents.ts src/sim/floorChain.ts` | **6 命中**,正確反映已完成 |
| `buyFurniture` 紀念物 | `rg -n "price <= 0\|memorial" src/sim/economy.ts` | **2 命中**(`:304` `movePlacement`、`:389` `sellFurniture`)→ **偽陽性** | `rg -A10 "export function buyFurniture" src/sim/economy.ts \| rg "price <= 0\|memorial"` | **0 命中**,正確反映未完成 |

  - 前兩條是**同一種錯**:實作採用的命名(`BACK`/`SIDE_R`/`SIDE_L`、`startGroupScene`)
    與當初猜的不同,錨點沒跟著改。後果是漏判——把做完的事說成沒做。
  - 第三條最危險:條目要求的是「**在 `buyFurniture` 函式體內**」有守門,
    而單行 `rg` 表達不了這個限定,會被同檔其他函式的守門命中。
    實際複核 `src/sim/economy.ts:252-261` 的 `buyFurniture()` **仍然只檢查
    `state.money < def.price` 與 `findFreeSlot`**,沒有 `price <= 0` 或 memorial 守門。
    照「命中即完成」機械執行會把它劃成完成 → **把沒做的事說成做完了**。
    兩段式寫法已在本 repo 實跑驗證(ripgrep 14.1.1,`|` 管線串接兩層 `rg`)。

#### C. 補進 10 條漏記待辦(原本只活在日期紀錄內文,機械掃描看不到)

- **Codex 三批留下的 5 條**(新小節「Codex 三批留下的漏記待辦」):
  每日 2 場自然社交／打鬥上限不含劇本排程與群體事件 🟢、
  永久隔音可擋噪音公審但尚未降低自然社交的噪音相性口角率 🟡、
  🐾 寵物面板展開後未補拍人工點擊視覺檢查 🟢、
  租客張力批的低頻彈窗(招租相性提示、紀念物二次確認)未補拍 🟢、
  天台場景採「樓層內夕景小舞台 + 隱藏主平面重複 sprite」而非真實地圖區域 🟢。
- **睡眠效率批與四方向人物批留下的 4 條**(新小節):
  `usedDefId` 排隊分支地雷 🟡、四方向只覆蓋站與走(坐/躺/`cook_pair`)🟡、
  `lastFacingDir` Map 單調成長 🟢、背面長髮/馬尾蓋住上背 🟢。
- **🔴 家具 tier 第三階段——本次最重要的一條,已獨立成第四節的 `- [ ]`**。
  它先前**只寫在一個 `[x]` 條目的內文** + 交接文件的括號裡,**沒有自己的 checkbox
  也沒有錨點**,機械掃描永遠看不到——連續兩輪對帳都沒把它撈出來,正是規則 3 的來由。
  條目寫明三件接手者必須先知道的事:(1) 沙發休息/看電視/泡澡/書桌在種子局
  **全踩 premium 家具**(`mic_desk` 72h、`gaming_desk` 41h、`shared_sofa`、`lounge_tv`、
  `bathtub`),一接就得**接受這是刻意的平衡調整並跑 `balance-test --update`**;
  (2) **不可照抄舒適度 `tierPart` 的形狀**,恢復效率是乘法通道、作用在不同資源軸,
  ROI 表要重新推導;(3) 接手前先讀 2026-07-30「睡眠效率 review 收尾」的
  「給第三階段的檢查清單」與 `usedDefId` 那條。
- ⚠️ **刻意沒列**「側面 ponytail/bob 辨識度」——已於 `21b8b5c` 解決
  (最小換色 4px → 9px,並加了 ≥8px 斷言 + 負向驗證),補列會製造假待辦。

#### D. 一處錯誤行號

`dream|<id>` 冷卻鍵殘留那條寫「清理點在 `src/sim/social.ts:368`」,
實際 `removeTenantRelations()` 在 **`src/sim/social.ts:485`**(`:368` 附近是 `encounter()`
的衝突判定,與本條無關)。已更正並補上鍵前綴的定義位置
(`src/sim/dreams.ts:55` 的 `DREAM_CD_PREFIX = "dream|"`)。

#### E. `docs/交接與待辦.md` 同步(先前落後 7 個 commit)

依嚴重度:

1. 🚨 **存檔版本**:寫「已升 v5」,實際 `src/sim/persistence.ts:28` 已是 **`SAVE_VERSION = 6`**
   (Codex `eb8305f` 為了把群體場景排程佇列寫進存檔而升的)。**照 v5 寫遷移會直接出錯**,
   這是本次修掉的最危險一處。已在最上方新增獨立警語段並改寫第一節「現況快照」。
2. **L-4 活動椅**:原文寫「`drawActivityChair(ctx, a)` 寫死一組棕色、簽章無區分參數」
   ——**與現況完全相反**(`19fca0e` + `dc65603` 已改成 `activityChairStyle()` 反查後
   分派四種造型 + 半徑收窄至 1 + 版本號 memo)。改標 ✅ 並就地寫明「原文與現況相反」。
3. **L-5 atlas 死 crop**:原文寫「日後重建時順手清」——`fcbdd30` 已清完
   (`furniture.png` 8,696 → 6,992 bytes,18 個 frame 逐格不變)。改標 ✅。
4. **L-2 四方向人物、J-3 多人站位**:兩條殘留孤兒,一併標 ✅ 並更正內文(同 A 節)。
5. **基準數字**:66/66 → **68/68**、Vite 132 → **137 modules**、HEAD `cc9c07c` → **`21b8b5c`**;
   舊數字降級為歷史快照而非刪除。
6. **三整套新系統完全缺席**:`groupScene`、`petHomes`、租客 `tension` 積怨軸在本檔
   **各 0 次提及**。已在最上方「最新交接」補完整介紹,第一節「現況快照」補一句話摘要,
   第六節「檔案地圖」新增一小節列出四塊(三系統 + 四方向人物)的入口檔案與行號。
7. **「共 30 條、一對一對應」的宣告已失效**:數量與內容都對不上。改成用腳本重數的實況
   (42 受追蹤 = 36 未完成 + 6 已完成帶錨點,其中 35 條可機械執行),
   並寫出 29 → 36 的變化過程(+10 補記、−3 完成)。
8. 標題日期 2026-07-30 → **2026-08-01**,並改成點出本批主題。
9. 順帶更新「目前優先候選」與「這幾批留下的小殘留」兩段的過期內容,
   新增 O(Codex 5 條)、P(睡眠效率/四方向 4 條)兩段與 L/J 段的完成標記。

#### F. 錨點制新增三條規則(本次最重要的結構性改進)

寫在第四節開頭錨點制說明的後面,標題「📐 錨點三規則(2026-08-01 對帳後新增)」:

1. **完成一條時,若實際符號與錨點不符,改 `[x]` 的同時必須把錨點改寫成真實符號。**
   讓錨點從「我猜完成後程式碼長這樣」變成「這條為什麼算完成」的證據。
2. **「函式體內」「僅限某檔某段」這類限定條件不能用單行 `rg` 表達,必須用兩段式**
   `rg -A<n> "<錨>" <檔> | rg "<樣式>"`。單行 `rg` 只知道「這個檔裡有這個字」,
   不知道它落在哪個函式;限定條件寫在內文而錨點沒有 → **偽陽性**,比漏判更危險。
3. **任何 `[x]` 條目的內文若含「尚未／留待／第 N 階段」字樣,必須另開一條 `- [ ]` 並自帶錨點。**
   掛在 `[x]` 內文裡的殘留工作對機械掃描是隱形的——掃描只看 checkbox 狀態。

  三條規則各自附了本次抓到的實例,讓後人知道它們不是憑空訂的。

#### 驗證(對帳後把全部錨點重跑一遍)

- **受追蹤條目 42 條**(第四節 16 未完成 + 6 已完成、第五節 20 未完成;
  另有一批錨點制之前的無錨點舊 `[x]` 不計)。
  **未完成條目的錨點覆蓋率 100%(36/36)**。
- **其中 35 條可機械執行**,7 條標「長期項／需人工目檢/無程式碼訊號」
  (平衡調整、`/api/*` rate limit、章節卡堆疊間距、夢境 `ui:shot`、
  🐾 面板補拍、低頻彈窗補拍、背面長髮目檢)。
- **35 條全部實跑,結果與條目狀態 100% 一致**:
  - **6 條 `[x]` 全部命中**——四方向走路 sprite / 四方向人物(同一指令)、AI 色碼對比安全化、
    活動椅造型、atlas 清理(python 兩段檢查印出 `manifest殘留 [] |尺寸 (128, 128) |死區非全透明 []`)、
    多人站位演出。
  - **29 條 `[ ]` 全部正確判定為未完成**。其中 8 條是「有命中但條件不成立」型
    (`socialFulfillment` 只命中 `tick.ts` 一支、`COZY_COMFORT` 仍是 52、
    `COMMUNAL_ATTR_MAX` 仍是 50、`communalQuality()` 仍無 tenantId 多載、
    `upgradeAttributes` 無共用區守門、`pendingChainEvent` 只命中註解、
    balance 快照仍只有 `logs` 無 emoji 計數、`8000:8000` 命中但仍被 `#` 註解)
    ——這類條目的判定仍需讀一眼命中內容,已是既有設計。
  - **殘留孤兒 0 個、偽陽性 0 個。**
- ⚠️ **本次新寫的錨點被自己的規則 2 抓到一次**:`usedDefId` 那條初稿寫單行
  `rg -n "usedDefId|furnitureDefId" scripts/`,實跑**命中**
  `scripts/furniture-tier-test.ts:266` 的 `function deltasOf(st, furnitureDefId?)`
  ——那只是呼叫 `generateHourly` 的測試輔助參數,**完全沒有斷言排隊者不吃乘數**,
  是不折不扣的偽陽性。當場改成綁定 `waiting_for_bathroom` 情境的兩段式後實跑 0 命中。
  這件事本身就是規則 2 的最佳佐證:**偽陽性很容易寫出來,而且寫的人當下不會發現。**
- 純文件變更,`git diff --check` 乾淨;未跑 `npm test` / `build`
  (最近基準:2026-08-01 四方向 review 收尾的 `npm test` 68/68、Vite 137 modules)。

- **已知限制／後續**:
  - 第五節仍有 4 條「有命中但要靠人判斷值有沒有變」型的錨點
    (`COMMUNAL_ATTR_MAX`、`upgradeAttributes`、`pendingChainEvent`、`communalQuality(`),
    嚴格說也適用規則 2 的兩段式。本次**刻意不動**——它們不在本次對帳的授權範圍內,
    且都是「條件不成立時判未完成」的保守方向(漏判而非偽陽性),風險低於 `buyFurniture` 那條。
    下一輪對帳可順手收掉。
  - 錨點制仍是人工維護。規則 1 依賴「完成的人記得回頭改錨點」,和它要取代的
    「交付時記得回來劃掉」是同一種人性風險;差別只在**規則 1 的失敗會被下一次機械執行抓到**
    (錨點 0 命中但條目是 `[x]`,或反過來),而舊做法的失敗會一直沉默下去。
- **硬規則**:純文件變更,未觸及模擬、AI、戀愛或消毒邏輯;
  未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒等既有硬規則全數保留。
- **版本狀態**:branch `docs/reconcile-3`,以 `21b8b5c` 為基準;**未 push、未併回 `main`**。

### 2026-08-01 — 一樓寵物咖啡廳設計草案:畫進樓層地圖、常備訂單研發、租客維持三樓(Claude Code)

- ⚠️ **狀態:設計草案,待使用者審閱後才進入實作,本批尚未動任何程式碼。**
  第四、五節**刻意不新增任何 `- [ ]` 待辦**——設計尚未核准,還不是待辦。核准後才會開 task lease 與待辦條目。

- **目的／使用者需求**:使用者要求為這棟樓設計「一樓寵物咖啡廳」,作為房東經營主線的延伸。
  本批只產出設計文件,把使用者已拍板的方向、既有系統的接縫、與零漂移風險寫成可審閱的一份草案。

- **實際變更(僅 1 個新增文件檔,未動任何程式碼)**
  - `docs/一樓寵物咖啡廳-設計.md`(新增):十二節設計草案,基準 commit `d1af916`。
    含定位、樓層架構、美術盤點、核心迴圈、顧客模型、三條敘事接縫、存檔、零漂移策略、
    六期分期實作計畫、未解問題。

- **三個已拍板的決定(2026-08-01,使用者)**
  - **呈現方式:畫進樓層地圖**——不做獨立分頁。代價是需要 viewport;做法是把
    `PixelDollhouse.vue:122` 房間特寫**已在用**的 `composeFloor` scale+translate 窗格邏輯
    抽成共用 `FloorViewport`,3F/1F 兩個固定機位切換,不做自由捲動與縮放。
    這把最大成本項從「全新鏡頭子系統」降級成「重構一個已有測試覆蓋的元件」。
  - **經營節奏:常備訂單 ＋ 研發決策**——進貨每日自動補水位,離線不斷貨、不懲罰;
    玩家的真決策集中在低頻高重量的研發(同時只能一項,照抄既有日數倒數)。
  - **樓層編號:租客維持三樓,存檔零遷移**——1F 咖啡廳、2F 不進遊戲、3F 仍是 `r301`~`r304`。
    理由:`roomId` 是 `state.occupancy`、`placements[].room`、`upgradeState.byRoom` 的 key 且全都進存檔,
    改名等於升 `SAVE_VERSION` 並替所有舊檔寫遷移,換來的只是數字好聽,不值得。

- **順手解掉的既有 🔴 待辦(兩條)**
  - 🔴 **訪客 NPC**:卡住的真正原因是「每個看得見的人都綁死 `TenantRuntime`」,
    假 runtime 會污染收租/社交/日記/成就/週報十幾個 pass。設計提出 `CafeGuest`——
    形狀抄 `GroupSceneActor`,**不進 `state.runtimes`**,存 `state.cafe.guests`(cap 6),
    離開即移除。既有描述說「需新 sprite 與場景」高估成本:sprite 那半已被 `parts.ts`
    加四方向基底解決。
  - 🔴 **G-12 樓層擴建**:本設計就是它的具體化身,且證明**不需要 `floorId` 維度**——
    往下延伸同一張 grid(`GRID_H` 33→52)、加一道樓板分隔帶與一段樓梯即可,
    尋路、agents、petAgents、groupScene 全部自動生效,租客能真的走下樓。

- **驗證結果**:純文件變更,`git diff --check` 乾淨;檔頭「日期紀錄索引」新錨點對得到本則標題。
  **未跑 `npm test` / `npm run build` / `ui:shot`——本批零程式碼變更,無可測目標。**
  (最近基準:2026-08-01 四方向 review 收尾的 `npm test` 68/68。)

- **已知限制／後續**:
  - 設計本身尚未核准,文件內所有數值(客流係數、投資價格、日淨利 30–50% 目標)都是草案值,
    未經 `balance-test` 驗證。
  - 文件已標出三個必須避開的漂移陷阱:咖啡廳**不可**列進 `COMMUNAL_AREA_IDS`
    (否則 `COMMUNAL_NEUTRAL = 35.28233009708738` 失效、全樓基準線位移)、
    `INITIAL_PLACEMENTS` **只 append 永不 insert**、`at_cafe` visualState 需設 `CAFE_FIRST_DAY` 閘門。
  - 三處硬編白名單(`placements.ts:140`、`routine.ts:166`、`comfort.ts:308`)在第 1 期必須一起檢視,
    漏改會靜默出錯。

- **硬規則**:純文件變更,未觸及模擬、AI、戀愛或消毒邏輯;
  未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒等既有硬規則全數保留。
- **版本狀態**:branch `docs/cafe-design`,以 `d1af916` 為基準;**未 push、未併回 `main`**。

### 2026-08-01 — 一樓寵物咖啡廳工作分解:22 個可認養工作項、三條平行主幹、CAFE-03 唯一瓶頸(Claude Code)

- ⚠️ **狀態:工作分解草案,尚未動任何程式碼,任何項目動工前須先開 lease。**
  第四、五節**刻意不新增任何 `- [ ]` 待辦**——設計與分解都還沒核准,還不是待辦。
  認養任一項工作前,必須先從 `coordination/templates/task.md` 複製一張 lease 到 `coordination/tasks/`,
  檔名用項目 ID(例如 `cafe-03-map-grid.md`),lease 的「允許修改／禁止修改」直接抄分解文件該項的清單。

- **目的／使用者需求**:把上一則的咖啡廳設計草案拆成可認養、可平行、可驗收的工作項,
  讓兩個 Agent(Claude Code 與 Codex)在同一個 checkout 上不撞車地分工。

- **實際變更(僅 1 個新增文件檔,未動任何程式碼)**
  - `docs/一樓寵物咖啡廳-工作分解.md`(新增):工作分解與認養表,設計依據 `docs/一樓寵物咖啡廳-設計.md`,
    基準 commit `d1af916`。含認養規則、依賴圖、貫穿全程規則、逐項工作、建議認養順序、與既有待辦的收斂。

- **分解結果**
  - **22 個可認養工作項 `CAFE-01`~`CAFE-22`**,外加 **1 項不可認養的中控驗證 `CAFE-00`**
    (balance hook 逐項驗證;屬整合責任、需跨項目全域視角,依專案 `CLAUDE.md` 由中控執行)。
  - **三條可平行的主幹**:**地圖線**(01→02→03→…)、**顧客線**(07→…)、**經濟線**(11→…)。
    顧客線與經濟線在 CAFE-03 完成前就能開工,只是最後要接起來。
  - **`CAFE-03`(地圖擴充:`GRID_H` 33→52 與 Region)是全案唯一的序列瓶頸**——它動 `map.ts`
    且位於關鍵路徑,那段期間另一方必須避開所有 `src/floor/`。
  - 每一項都附**「允許修改／禁止修改」檔案清單**與**一行可 grep 的驗收錨點**,
    沿用第四、五節既有的驗收錨點制(含它的三條規則);要判斷前置項是否完成,跑錨點即可,不必問人。

- **三條貫穿全案、寫進每一張 lease 的鐵則**
  - 🔴 **不得把任何 cafe region 加進 `src/sim/comfort.ts` 的 `COMMUNAL_AREA_IDS`**——
    一加 `COMMUNAL_NEUTRAL = 35.28233009708738` 立刻失效,全樓租客基準線位移、平衡快照全紅。
  - 🔴 **`INITIAL_PLACEMENTS`(`map.ts:150`)只能 append,永不中段插入**——
    `resolveTarget`(`routine.ts:176`)取陣列**第一個**符合的,中段插入會讓全樓租客的
    沙發/電視/廚房目標**靜默改道**。
  - 🔴 **`map.ts` rows 0–32 的任何既有矩形一格都不能動**——現有三樓維持原樣,這是零漂移的第一道防線。

- **建議 Codex 優先認養的三項**(依「誰寫的誰最熟」):
  **CAFE-21**(cafe venue;`groupScene.ts` 是它建的)、**CAFE-22**(寵物下樓;`petAgents.ts` 是它近期改的)、
  **CAFE-04**(樓梯與樓板分隔帶美術;人物美術第一、二批都是它做的)。

- **驗證結果**:純文件變更,`git diff --check` 乾淨;檔頭「日期紀錄索引」新錨點對得到本則標題。
  **未跑 `npm test` / `npm run build` / `ui:shot`——本批零程式碼變更,無可測目標。**
  (最近基準:2026-08-01 四方向 review 收尾的 `npm test` 68/68。)

- **已知限制／後續**:
  - 設計與分解**都尚未核准**;核准前不得認養任何項目,也不會有待辦條目。
  - 分解文件內的工期假設與依賴順序尚未經實作驗證;真正開工後若錨點與實際符號不符,
    依錨點制第一條規則,改狀態的同時要把錨點改寫成真實符號。
  - 兩個 Agent 共用同一個工作樹,**同時只能有一方持有未提交的變更**;認養時務必先
    `git status --short`,一次只認養一項,做完提交或合併再認養下一項。

- **硬規則**:純文件變更,未觸及模擬、AI、戀愛或消毒邏輯;
  未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒等既有硬規則全數保留。
- **版本狀態**:branch `docs/cafe-design`,以 `1a6fc34` 為基準;**未 push、未併回 `main`**。

### 2026-08-01 — CAFE-01 FloorViewport 抽取:任意矩形鏡頭、房間特寫逐位元不變(Codex)

- **目的／使用者需求**:使用者同意從一樓寵物咖啡廳工作分解選一項開始動工。本批認養無前置依賴的
  `CAFE-01`,把房間特寫既有的 scale/translate 鏡頭抽成可供下一項樓層切換器重用的共用函式。
- **實際變更**:
  - 新增 `src/floor/viewport.ts`:定義 `FloorViewportRect`、任意矩形建構、置中且限制於樓層邊界的
    `centeredViewportRect`,以及維持像素化與既有 rounding 順序的 `renderFloorViewport`。
  - `src/components/PixelDollhouse.vue`:改用共用 viewport 計算與繪製;仍直接呼叫原本的
    `composeFloor`,未改其本體、繪製順序、8×7 格視窗或 3 倍縮放。
  - `scripts/roomcam-test.ts`:既有房間鏡頭驗證改走共用函式,並新增舊／新路徑的像素 buffer
    逐位元比較,防止後續重構造成亮度門檻抓不到的細微畫面漂移。
- **驗證結果**:
  - `npx tsx scripts/roomcam-test.ts`:10/10 通過,含「FloorViewport 與舊鏡頭逐位元相同」。
  - `npm test`:68/68 支測試通過;`balance-test` OK。
  - `npm run typecheck`:app 與 Worker 全數通過。
  - `npm run build`:成功（138 modules transformed）；僅保留既有 500 kB chunk 警告。
  - workspace root `npm run ui:shot -- rent`:390×844、412×915、430×932 共 18 張，
    `report.json` 為 0 errors／0 warnings；已逐張人工檢查，無空白、裁切、變形或模糊退化。
- **限制／後續**:`CAFE-01` 只提供共用鏡頭能力，尚未修改 `FloorMap.vue`、地圖高度或任何咖啡廳內容；
  下一個地圖線項目是 `CAFE-02` 樓層切換器。
- **硬規則**:未修改 `composeFloor()`、`FloorMap.vue`、地圖 rows 0–32、模擬、平衡、AI、戀愛或消毒邏輯。
- **版本狀態**:`main`,基準 `8ee1cfc`;本批已提交、尚未 push。

### 2026-08-01 — CAFE-02 樓層切換器:固定 3F/1F 機位、空白一樓驗收(Codex)

- **目的／使用者需求**:使用者要求接續完成咖啡廳第二項工作。本批在 `CAFE-01` 共用 viewport
  之上加入固定樓層切換，不做自由捲動或縮放；地圖尚未擴充時，1F 應明確呈現空白機位。
- **實際變更**:
  - `src/components/FloorMap.vue`:新增 `FLOOR_VIEWS`，固定 3F `(0,0,16,33)` 與
    1F `(0,34,16,18)`；加入具 tab 語意與選取狀態的「3F 租屋樓／1F 咖啡廳」切換器。
  - canvas 尺寸、`renderFloorViewport` 繪製、點擊格座標、房間標籤與人物／寵物名牌都改以目前
    viewport 為基準。切到尚未存在的 1F 前先清可見 canvas，因此不會殘留上一幀的三樓畫面。
  - 現有交誼廳、大門與群體演出覆蓋層只在 3F 顯示；切回 3F 後房間與名牌完整恢復。
  - 新增 `scripts/floor-view-test.ts` 並列入 `scripts/run-all.ts` 正式回歸，鎖定兩個矩形、共用
    viewport、動態 canvas 尺寸、全域點擊換算、無障礙 tab 與「不得加入自由手勢」合約。
  - `src/App.vue` 無須修改；切換狀態由 `FloorMap` 自己管理，沒有增加跨元件耦合。
- **驗證結果**:
  - `npx tsx scripts/floor-view-test.ts`:8/8 通過。
  - `npm test`:69/69 支測試通過；`balance-test` OK，既有 `roomcam-test` 10/10 仍通過。
  - `npm run typecheck`:app 與 Worker 全數通過；`npm run build`:成功（138 modules transformed），
    僅保留既有 500 kB chunk 警告。
  - workspace root `npm run ui:shot -- rent`:390×844、412×915、430×932 共 18 張，
    0 errors／0 warnings；逐張人工檢查，切換器未擠壞樓層、操作列或其他頁面。
  - in-app Browser 互動驗證：1F 選取後 canvas 為 `256×288` 且畫面空白；切回 3F 為
    `256×528`，房間／人物標籤恢復；browser console 零錯誤。
- **限制／後續**:本批沒有修改 `GRID_H` 或建立咖啡廳 Region，所以 1F 空白是預期狀態；下一項
  `CAFE-03` 才會把地圖擴到 52 rows。切換器已先完成正確的全域格座標換算，可直接承接該地圖。
- **硬規則**:未修改 `floorScene.ts`、`agents.ts`、`map.ts`、rows 0–32、模擬、平衡、AI、戀愛或消毒邏輯。
- **版本狀態**:`main`,基準 `55e7a70`;本批已提交、尚未 push。

### 2026-08-01 — CAFE-03 地圖擴充:52 rows、六個 Region、跨樓層尋路(Codex)

- **目的／使用者需求**:使用者要求接續完成咖啡廳第三項、也是全案唯一關鍵路徑。本批把既有
  單一 grid 往下擴至 1F，要求三樓逐格零漂移、可從三樓走到一樓店門，且不得讓咖啡廳污染
  住宅公共舒適度基準。
- **實際變更**:
  - `src/floor/map.ts`:`GRID_H` 33→52；新增 `CafeRegion` 與 `CAFE_RECTS` 六區：
    `stairs`、`cafe_floor`、`cafe_counter`、`cafe_pet`、`cafe_back`、`cafe_entrance`。
    重疊優先序讓吧台／寵物區覆蓋主廳；既有 row 32 `entrance` 在最後覆蓋 `stairs`，所以
    三樓入口格維持不變，row 33–35 再自然接成樓梯。
  - `src/sim/placements.ts` 的 `FLOOR_REGIONS` 與 `src/sim/routine.ts` 的 `COMMUNAL` 僅加入
    四個咖啡廳室內區。`stairs`／`cafe_entrance` 刻意不可擺家具，避免堵住唯一跨樓層動線。
  - 新增 `scripts/cafe-map-test.ts` 並列入 `scripts/run-all.ts`：鎖定舊三樓與初始家具 SHA-256、
    六區矩形／覆蓋關係、模組載入時網格高度、跨樓層 BFS、擺放與 routine 白名單，以及
    `COMMUNAL_AREA_IDS`／`COMMUNAL_NEUTRAL` 禁區。
- **零漂移證據**:
  - rows 0–32 的序列化 SHA-256 仍為
    `49d377afd1be8169d9864c7ab4bee8a44266951e93c16d6f131414d82301c6e7`。
  - 35 筆 `INITIAL_PLACEMENTS` SHA-256 仍為
    `34073680172e1dd99775acfd2449edb4a4c4f552d22b85a86df9575bdecad22b`。
  - `src/sim/comfort.ts` 與 `scripts/balance-snapshot.json` diff 均為空；獨立 `balance-test` 一致。
- **驗證結果**:
  - `npx tsx scripts/cafe-map-test.ts`:21/21 通過；三樓每個地圖可走格在純拓樸上都可抵達
    1F 門口，套用現有家具障礙後三樓入口仍可抵達。
  - `npm test`:70/70 支測試通過；`npm run typecheck` app／Worker 通過；`npm run build`
    成功（138 modules transformed），僅保留既有 500 kB chunk 警告。
  - workspace root `npm run ui:shot -- rent`:三種手機寬度共 18 張，0 errors／0 warnings，
    逐張檢查確認三樓比例與內容未改。
  - in-app Browser 切到 1F：256×288 畫布正確呈現樓梯口、主廳輪廓與下方店門，無三樓殘影，
    browser console 零錯誤。
- **已知限制／後續**:
  - 現有 r302 `(10,7)` 是修改前就被家具與牆圍住的單格口袋；地圖純拓樸全連通，且實際三樓
    入口到 1F 門口路徑成立。本批未移動任何家具去改寫既有盤面。
  - 1F 目前沿用通用地板／牆面繪製；樓梯與樓板美術屬 `CAFE-04`，分區 tint／atlas 屬 `CAFE-05`。
  - `floorScene.ts` 的 `FLOOR_H` 由公式正確得到 832 且有測試，但該行舊的 `// 384` 註解依本項
    禁止修改範圍未動，待下一項合法觸及該檔時同步修正。
- **硬規則**:`COMMUNAL_AREA_IDS` 仍嚴格只有 lounge／bathroom／laundry；未修改 rows 0–32 矩形、
  `INITIAL_PLACEMENTS`、`floorScene.ts`、`agents.ts`、平衡快照、AI、戀愛或消毒邏輯。
- **版本狀態**:`main`,基準 `cf36aea`;本批已提交、尚未 push。

### 2026-08-01 — CAFE-04 樓梯與樓板分隔帶美術:LimeZu 木階、離線 fallback、兩層清楚斷面(Codex)

- **目的／使用者需求**:使用者要求接續完成一樓寵物咖啡廳第四項。本批只處理 row 33 樓板橫切面
  與 `stairs` 區的俯視樓梯，不提前動 CAFE-05 的咖啡廳地板 tint／獨立 atlas，也不碰家具、人物或模擬。
- **實際變更**:
  - `scripts/limezu-manifest.json`、`src/art/limezu.ts`、`public/assets/limezu/walls.png`：從指定的
    `17_Visibile_Upstairs_System_16x16.png` 選入兩塊完全不透明的 16×16 木階踏面與一塊暖灰樓板
    切面，append 到 `walls.png` 的 y=48 新列；atlas 由 64×48 擴為 64×64，既有 9 個建築 frame
    與 18 個家具 frame 座標全數不動，`furniture.png` SHA-256 仍為
    `d64359a5f94a42c09046dc646be0fc777f9930504f7af65166268d99273114ea`。
  - `src/floor/floorScene.ts`：新增並 dispatch `drawFloorDivider()`／`drawStairs()`。分隔帶橫跨
    row 33 全寬，2×4 格樓梯依 `CAFE_RECTS.stairs` 覆蓋中央開口；有 atlas 時交錯鋪 LimeZu 木階，
    圖片不可用時走同尺寸程序踏面，側軌與收邊在兩條路徑都維持清楚。同步修正 `FLOOR_H` 過時的
    `// 384` 註解為 `// 832`。
  - `scripts/limezu-art-test.ts` 擴到 81 項，交叉驗證 manifest／atlas 尺寸、frame 邊界與來源、
    1:1 繪製及 `composeFloor` dispatch 順序。新增 `scripts/cafe-stair-art-test.ts` 並列入正式回歸，
    鎖定只影響 rows 32–35、樓梯 32×64 邊界、row 33 全寬與 fallback 像素 SHA-256
    `c9c90a8edb8ca21fed44c3c2ed3f5e43fe50348933089cc4c43d2004adf5a8bf`。
- **驗證結果**:
  - `npx tsx scripts/limezu-art-test.ts`:81/81；`npx tsx scripts/cafe-stair-art-test.ts`:5/5。
  - `npm test`:71/71 支全綠（含 `balance-test`）；`npm run typecheck`:app／Worker 通過；
    `npm run build`:138 modules transformed，僅既有 500 kB chunk 提示。
  - 離線 `render-floor.ts` 產出 768×2496 fallback 圖，目視確認 row 33 橫切面完整、中央樓梯連續，
    三樓與一樓在沒有 atlas 的路徑也清楚分層。
  - workspace root `npm run ui:shot -- rent`:390×844、412×915、430×932 共 18 張，
    `report.json` 0 errors／0 warnings；逐張檢查樓層、動態、收支頁皆無空白、裁切或版面退化。
  - in-app Browser 實際切到 1F：canvas 256×288、LimeZu 木階在畫面頂端清楚接入；切回 3F 正常，
    兩個狀態的 browser console 均為 0 warnings／0 errors。
- **已知限制／後續**:一樓仍沿用通用地板底色，且尚無咖啡廳家具；分區 tint／`cafe.png` 屬
  `CAFE-05`，吧台、桌椅、貓跳台等 catalog 屬 `CAFE-06`，本批刻意不越界。
- **硬規則**:未修改 `map.ts`、`INITIAL_PLACEMENTS`、`comfort.ts`、`furniture.png`、既有 18 個
  家具 frame、`drawAgent`／人物繪製、模擬、平衡、AI、戀愛或消毒邏輯。
- **版本狀態**:`main`,基準 `5d43e0f`;本批已完成待提交、尚未 push。

### 2026-08-01 — CAFE-02 UX 修訂:雙分頁改為獨立樓層頁面與單一目的地按鈕(Codex)

- **目的／使用者需求**:使用者確認原先構想是用一顆按鈕直接前往另一樓層的完整畫面，不是讓
  3F／1F 以雙分頁在同一張圖內互相連結。本批依此將樓層導覽改為兩個獨立頁面狀態，同一時間
  只顯示目前樓層與唯一目的地按鈕。
- **實際變更**:
  - `src/components/FloorMap.vue`:移除 `tablist`／雙樓層 tab；3F 頁只顯示「前往 1F 寵物咖啡廳」，
    1F 頁只顯示「返回 3F 租屋樓」。切換時以 keyed `floor-wrap` 替換整個樓層視窗並加入短淡入，
    兩頁各有獨立標題、背景與頁面 aria-label；內部仍共用既有 grid 與 `FloorViewport`，未複製地圖資料。
  - `scripts/floor-view-test.ts`:改鎖定單一目的地按鈕、雙向頁面替換、獨立頁面標籤，以及不得回歸
    `tablist`／`tab`／舊 `floor-switcher`。
  - `docs/一樓寵物咖啡廳-設計.md`、`docs/一樓寵物咖啡廳-工作分解.md`:將 CAFE-02 的產品決策、
    UX 圖、完成條件與驗收錨點同步改為「玩家分頁、程式共圖」和單一目的地按鈕。
- **驗證結果**:
  - `npx tsx scripts/floor-view-test.ts`:10/10；`npm test`:71/71 全綠（含 `balance-test`）。
  - `npm run typecheck`:app／Worker 通過；`npm run build`:138 modules transformed，僅既有 500 kB
    chunk 提示。
  - workspace root `npm run ui:shot -- rent`:390×844、412×915、430×932 共 18 張，`report.json`
    0 errors／0 warnings；逐張檢查樓層、動態、收支頁均無裁切或版面退化。
  - in-app Browser 互動驗收:3F 初始只有「前往 1F 寵物咖啡廳」；點擊後整頁切為 1F、地圖標籤
    為「1F 寵物咖啡廳地圖」，且只剩「返回 3F 租屋樓」；返回後完整恢復 3F。兩個狀態的 tab
    數量都為 0。
- **硬規則／影響面**:未修改 `App.vue`、`map.ts`、`floorScene.ts`、模擬、平衡快照、AI、戀愛或
  消毒邏輯；既有 3F／1F viewport 座標不變。
- **版本狀態**:`main`,基準 `2b1b37c`;本批已完成待提交、尚未 push。

### 2026-08-01 — CAFE-05 咖啡廳分區底色與獨立 LimeZu atlas(Codex)

- **目的／範圍**:延續 CAFE-03 的六個一樓 Region 與 CAFE-04 的樓梯斷面，讓 1F 寵物咖啡廳
  在尚未擺入家具前也能從地板辨識吧台、寵物互動區、後場與入口；同時建立不污染既有家具 atlas
  的咖啡廳專用美術資產，供下一批 CAFE-06 catalog 接線。
- **實際變更**:
  - `src/floor/floorScene.ts`:只擴充 `FLOOR_TINT`，為 `stairs`、`cafe_floor`、`cafe_counter`、
    `cafe_pet`、`cafe_back`、`cafe_entrance` 加入六組暖木、磚紅、鼠尾草綠、後場灰與入口棕色；
    未改地圖矩形、viewport、尋路或模擬資料。
  - 新增 `public/assets/limezu/cafe.png`（144×64）與 `LIMEZU_CAFE_ATLAS_URL`／13 個 frame 合約。
    素材取自專案既有 LimeZu Ice Cream Shop singles，另加入 Kitchen espresso machine；包含菜單、
    招牌、三種展示櫃、收銀台、咖啡機、桌椅、吧台、飲料站與甜筒站。
  - `scripts/limezu-manifest.json` 記錄每個來源檔、裁切框與 atlas 座標；`build-limezu-atlas.py`
    新增來源／邊界／重疊檢查，並在像素未變時保留既有 PNG 原始位元，避免不同 Pillow 壓縮版本
    無意改寫 `furniture.png`、`floors.png`、`walls.png`。
  - `scripts/limezu-art-test.ts` 擴為 87 項，驗證 cafe atlas 尺寸、ID、frame、來源白名單、無重疊、
    邊界與六個 Region tint；本批只建立 atlas 合約，尚未提前加入 CAFE-06 家具 catalog 或 runtime preload。
- **可重現建置**:在相同建置容器連跑 atlas builder 兩次，四張 PNG 的 SHA-256 皆完全一致：
  `furniture.png`=`d64359a5f94a42c09046dc646be0fc777f9930504f7af65166268d99273114ea`、
  `floors.png`=`9b79aabdde91e98f99a447415e7b4ab9b014c1b29754cf0149be2d01621b42e7`、
  `walls.png`=`680a267d323997546370284eab449828de99f7f12200f9952e0d1eb39f182d35`、
  `cafe.png`=`2fc50f8b8a616753f1928e6ad93e1cb479df798a1682465cacd17c594a9cb292`；
  既有三張 atlas 無 working-tree 差異。
- **驗證結果**:
  - `npx tsx scripts/limezu-art-test.ts`:87/87；`npm test`:71/71 全綠（含 `balance-test`）。
  - `npm run typecheck`:app／Worker 通過；`npm run build`:138 modules transformed，僅既有 500 kB
    chunk 提示。
  - workspace root `npm run ui:shot -- rent`:390×844、412×915、430×932 共 18 張，`report.json`
    0 errors／0 warnings；逐張檢查樓層、動態、收支頁，無裁切或版面退化。
  - in-app Browser 實際由 3F 按鈕切入 1F：六個區域底色清楚分離、地圖唯一且返回按鈕唯一；
    返回後 3F 標題恢復，browser error log 為 0。
- **硬規則／後續**:未修改 `map.ts`、家具 catalog、初始擺設、人物、模擬、平衡、AI、戀愛或
  消毒邏輯，也未改既有三張 atlas。下一項 CAFE-06 才會將 cafe frames 接成可擺放的咖啡廳家具。
- **版本狀態**:`main`,基準 `dff61c5`;本批已完成待提交、尚未 push。

### 2026-08-01 — CAFE-06 咖啡廳家具 catalog、atlas runtime 與公開上線閘門(Codex)

- **目的／範圍**:接續 CAFE-05，把獨立 `cafe.png` 從靜態資產接成可購買、可跨頁擺進 1F 的
  咖啡廳家具；每件保留程序 recipe，確保圖片缺失或 Canvas 繪製失敗時仍有完整 fallback。
- **家具資料**:
  - `src/furniture/catalog.ts` 只在陣列尾端 append 12 件：點餐吧台、商用濃縮咖啡機、甜點展示櫃、
    手寫菜單板、小圓桌、正／側面椅、大型貓跳台、寵物軟墊、後場貨架、進貨木箱與大型冷藏櫃。
  - 每件都有明確 `tier`、正價格、非空 recipe、footprint／互動點與敘事鉤子；全部沿用
    `placement: "communal"` 才能放進 cafe region，但 `unlocksStates=[]`、`social=false`，不提前讓
    既有租客在 CAFE-20 閘門前改道下樓。
- **atlas runtime**:
  - `src/art/limezu.ts` 為 13 個 cafe frame 補齊 1:1 `dx/dy`，新增獨立、非阻塞、共用 promise 的
    `preloadLimezuCafeAtlas()`；既有 `tryDrawLimezuFurniture()` 會依 id 選家具或 cafe atlas，兩張
    狀態與 warning 彼此隔離。載入／繪製失敗都回傳 false，讓 catalog recipe 接手。
  - `src/main.ts` 只多呼叫一個非阻塞 preload；未修改 `render.ts` 的 dispatch，既有家具路徑不變。
- **測試／驗證**:
  - `data-catalog-test.ts`:49/49，新增 12 件存在／唯一、明確 tier／正價格／recipe 與不提前解鎖租客。
  - `furniture-art-test.ts`:164/164，12 件 fallback 都有整數像素輸出、四向 transform 與畫框邊界；
    另鎖定吧台收銀區與冷藏櫃雙門辨識特徵。
  - `limezu-art-test.ts`:94/94，新增 Node 無 Image、並行 preload、1:1 source/destination、draw failure
    recipe fallback、load failure warn-once；既有三張 atlas 與 cafe manifest 檢查仍全綠。
  - `npm test`:71/71 全綠；獨立 `balance-test` 快照一致；`npm run typecheck` app／Worker 通過；
    `npm run build`:138 modules transformed，僅既有 500 kB chunk 提示。
  - workspace root `npm run ui:shot -- rent`:390×844、412×915、430×932 共 18 張，`report.json`
    0 errors／0 warnings；逐張檢查樓層、動態、收支均無裁切或退化。
  - in-app Browser 實際驗證：商店能看到全部 12 件；購買 $16,000 點餐吧台後仍保持擺放模式切到
    1F，在 `cafe_counter` 區預覽／確認成功，金額 87,534→71,534，正式畫面顯示 atlas 像素，
    browser error log 為 0。
- **上線決策與文件修正**:
  - CAFE-06 完成後只適合內部預覽：空間、商店與家具成立，但尚無顧客、認養或營運閉環。
  - 公開版建議做到 CAFE-01～15，另提前納入 CAFE-22，最後由 CAFE-00 逐 hook 驗證；這樣玩家才
    能從 UI 完成開張、補貨、日結、投資與一次真實可達的顧客認養。CAFE-16～21 可延後。
  - `docs/一樓寵物咖啡廳-設計.md` 與工作分解同步補上 `openCafe()`、開張按鈕、認養接受／婉拒、
    CAFE-22 寵物可達性及完整端到端公開閘門，避免只靠 grep／單元測試就誤判可上線。
- **硬規則／限制**:未修改任何既有家具欄位、`render.ts`、`tier.ts`、`map.ts`、
  `INITIAL_PLACEMENTS`、`COMMUNAL_AREA_IDS`、模擬、平衡快照或任何 PNG。家具目前由共用商店購買；
  真正的咖啡廳營運設備效果仍必須在 CAFE-14 走 `state.cafe.upgrades`，不能把 placement 當營運狀態。
- **版本狀態**:`main`,基準 `59a1585`;本批已完成待提交、尚未 push／deploy。

### 2026-08-01 — CAFE-07 獨立顧客資料層：姓名池、決定性外觀與純函式離場(Codex)

- **目的／範圍**:接續公開 MVP 的顧客線，先建立不會被收租、社交、日記等租客 pass 誤處理的
  `CafeGuest` 輕量資料層。本批只做型別、內容與純函式，不接 UI、走位或全域 cafe state。
- **實際變更**:
  - `src/types.ts`:新增 `CafeGuestIntent`（`coffee`／`adopt`／`rent`）與 `CafeGuest`；欄位包含
    id、姓名、完整 `Appearance`、抵達／離開時間及選填座位格，沒有 Tenant 的財務、關係或劇情狀態。
  - `src/content/cafeGuestNames.ts`:新增 32 筆獨立姓名，全部不與 `recruit.ts` 的 20 筆
    `NAME_IDENTITIES` 重疊；採 append-only 順序，避免後續相同 seed 換名字。
  - `src/sim/cafeGuests.ts`:新增 FNV-1a 決定性雜湊、`generateCafeGuest()`、cap 6 的
    `appendCafeGuest()`、指定離場與到期離場純函式。相同 seed／sequence／抵達時間會得到相同姓名、
    外觀、意圖與 1～3 遊戲小時停留期；不讀寫 `state`，也不呼叫 `Math.random()`。
  - `scripts/cafe-guest-test.ts` 新增 24 項斷言並登錄 `run-all.ts` 正式回歸集：姓名唯一／不撞租客、
    生成決定性、欄位白名單、三種意圖、cap 6、輸入與陣列不變、到點離場、零 RNG 與
    `state.runtimes` 零污染。
- **設計文件校正**:
  - 原設計以 `randomAppearance()` 表示重用人物外觀，但該函式實際會消耗 6～7 次 `Math.random()`，
    與 CAFE-07 的零 RNG 驗收衝突。設計與工作分解已改成「沿用同一批外觀白名單，以顧客 seed
    決定性選取」；未修改 `parts.ts` 的既有色池、順序或租客生成行為。
  - CAFE-19 後續把顧客轉成租屋應徵者時，應沿用 `CafeGuest.appearance`，不重新抽一次外觀。
- **驗證結果**:
  - `npx tsx scripts/cafe-guest-test.ts`:24/24；`npm test`:72/72（含新增 regression 與 sim-trace）。
  - `npx tsx scripts/balance-test.ts`:快照一致；`npm run typecheck`:app／Worker 通過。
  - `npm run build`:138 modules transformed，成功；僅既有大於 500 kB chunk 提示。
  - 本批沒有任何 UI 或繪圖接線，因此未重跑 UI Lab；下一項 CAFE-08 接顧客走位與繪製時再做
    三種手機尺寸與 1F 實際畫面驗收。
- **硬規則／後續**:未修改 `state.runtimes`、`NAME_IDENTITIES`、`gameState.ts`、存檔、地圖、
  `COMMUNAL_AREA_IDS`、平衡快照、AI、戀愛或既有外觀白名單。CAFE-11 建立 `state.cafe` 前，顧客
  只存在於純函式回傳值；下一項 CAFE-08 才會建立獨立 guest agent 走位與繪製。
- **版本狀態**:`main`,基準 `4b5a43f`;本批已完成待提交、尚未 push／deploy。

### 2026-08-01 — CAFE-08 顧客進出走位與獨立人物繪製(Codex)

- **目的／範圍**:接續 CAFE-07 的純 `CafeGuest` 資料，完成獨立於租客 runtime 的進場、入座、
  離場與人物繪製。本批不提前建立 `state.cafe`，也不在未開張的正式 1F 頁面硬塞示範顧客。
- **實際變更**:
  - `src/floor/guestAgents.ts`:新增 `GuestAgent` 顯示期生命週期
    `entering → seated → leaving → departed`。顧客由兩格 `cafe_entrance` 決定性進場，以 BFS 前往
    未被家具阻擋且未被其他顧客占用的座位；到 `leavesMs` 後走回入口才隱藏／移除。保留中央主走道，
    六位同時進場也不互卡，全程不呼叫 `Math.random()`。
  - `src/floor/floorScene.ts`:在既有動態物件 y-sort 中加入選填的第八個 `guests` 參數與
    `drawGuest()`；直接使用 `CafeGuest.appearance` 快照，行走時支援四方向 sprite，入座時使用坐姿，
    不查租客 registry、不建立假 `TenantRuntime`。
  - `scripts/cafe-guest-agent-test.ts`:新增 17 項斷言，涵蓋決定性排序與座位、家具阻擋 fallback、
    六客同時入座、到期走回入口、畫面混排、隱藏顧客不繪製及零 RNG；並登錄 `run-all.ts`。
  - `scripts/render-floor.ts`:新增選填 `--cafe-guests`，可離線生成三位已入座顧客的 1F 長圖，
    不改正式遊戲狀態。
- **設計文件校正**:原設計的「到期直接從陣列移除」會讓人物原地消失，已改為到期先進入
  `leaving`、抵達入口後才移除。CAFE-11 建立 `state.cafe`、CAFE-13 產生客流後才接正式 `FloorMap`；
  咖啡廳未開張時維持零顧客。
- **驗證結果**:
  - `npx tsx scripts/cafe-guest-agent-test.ts`:17/17；`npm test`:73/73（含 sim-trace）。
  - `npx tsx scripts/cafe-guest-test.ts`:24/24；`appearance-test.ts`:38/38；`roomcam-test.ts`:10/10。
  - `npx tsx scripts/balance-test.ts`:快照一致；`npm run typecheck`:app／Worker 通過；
    `npm run build`:138 modules transformed，成功，僅既有大於 500 kB chunk 提示。
  - `npm run ui:shot -- rent`:390×844、412×915、430×932 共 18 張，report 0 errors／0 warnings；
    已逐張檢查，樓層、動態、收支畫面無裁切或回歸。
  - 離線 `render-floor --cafe-guests` 長圖可見三位顧客分散入座；互動瀏覽器實測
    3F → 1F → 3F 按鈕切頁正常，1F 未開張不生成假顧客，console 0 errors。
- **硬規則／後續**:未修改 `src/floor/agents.ts`、`petAgents.ts`、`groupScene.ts`、
  `gameState.ts`、`state.runtimes`、存檔或平衡快照。CAFE-08 已完成訪客 NPC 的資料、走位與繪製
  抽象；正式生成接線留給 CAFE-11／13。依公開 MVP 序列，下一項為 CAFE-09 認養意圖接既有送養。
- **版本狀態**:`main`,基準 `c5288f8`;本批已完成待提交、尚未 push／deploy。

### 2026-08-01 — CAFE-09 認養意圖接入幸福新家(Codex)

- **目的／範圍**:讓 `intent === "adopt"` 的咖啡廳顧客能安全接到既有樓寵物幸福新家流程。
  本批只建立 sim 命令與驗證，不提前加入 `state.cafe`、Vue 抉擇面板或寵物下樓行為。
- **實際變更**:
  - `src/sim/pets.ts`:新增 `CAFE_GUEST_ADOPTION_DESTINATION = "咖啡廳客人領養"` 與
    `acceptCafeGuestAdoption()`。入口只接受資料完整的 adopt 顧客，以及尚未媒合的永久樓寵物；
    非認養顧客、房客寵物、foster／partner foster、未知 id 均零變更拒絕。
  - 接受後直接重用未修改的 `finishRehoming()`，沿用刪除寵物、清除 pair／cooldown、名冊 cap 與
    單次通知；再把該次新名冊項目的 id 改成穩定 `cafe:<guestId>`、destination 改成咖啡廳去向，
    補上顧客與寵物相遇文案並立即 `save()`。同一顧客重複點擊不能領走第二隻。
  - `scripts/cafe-adoption-test.ts`:新增 17 項斷言並登錄 `run-all.ts`，涵蓋成功快照、立即退場、
    destination、穩定 id、存檔、清理引用、所有拒絕分支、顧客輸入不變、runtime 零污染與零 RNG。
- **驗證結果**:
  - `npx tsx scripts/cafe-adoption-test.ts`:17/17；`npx tsx scripts/pet-test.ts`:90/90；
    `npx tsx scripts/cafe-guest-test.ts`:24/24。
  - `npm test`:74/74（含新增 regression 與 sim-trace）；`npx tsx scripts/balance-test.ts`:快照一致。
  - `npm run typecheck`:app／Worker 通過；`npm run build`:138 modules transformed，成功，僅既有
    大於 500 kB chunk 提示。本批無 UI 變更，因此未重跑 Mobile UI Lab。
- **硬規則／後續**:未修改 `beginRehoming()`、`finishRehoming()`、`rehomeDurationMs`、
  `PetHomeEntry`／`PetRehomingDestination` 型別、game state、存檔 schema、Vue、寵物 agent 或平衡快照。
  CAFE-15 才提供接受／婉拒操作；CAFE-22 讓樓寵物實際進入 `cafe_pet`，公開端到端驗收不能以
  本批直接呼叫函式取代。依序下一項為 CAFE-10 顧客意圖泡泡。
- **版本狀態**:`main`,基準 `4ce7f2c`;本批已完成待提交、尚未 push／deploy。

### 2026-08-01 — CAFE-10 三種顧客意圖泡泡與 atlas fallback(Codex)

- **目的／範圍**:讓已入座的咖啡廳顧客以直觀圖示表達 `coffee`／`adopt`／`rent`，同時維持
  CAFE-08 的正式客流閘門；本批只新增渲染能力，不提前建立 `state.cafe` 或在 1F 塞入假顧客。
- **美術與 runtime**:
  - `scripts/limezu-manifest.json` 與 `public/assets/limezu/cafe.png`:從既有授權的
    `UI_thinking_emotes_animation_16x16.png` 裁入咖啡、愛心、信封各兩幀，append 在 y=64；atlas
    由 144×64 擴為 144×80，原有 13 個咖啡廳家具 frame 的像素位置不變。
  - `src/art/limezu.ts`:在既有 cafe atlas preload 上新增六個 frame 合約與 1:1 繪製入口；每 500ms
    依 frame 偶／奇切換，形成約 1 秒循環。載入前或 `drawImage` 失敗時回傳 false 並沿用 cafe
    warn-once，交由場景畫程序 fallback。
  - `src/floor/floorScene.ts`:只對 `seated`、未移動且可見的 `GuestAgent` 畫泡泡，位置在頭頂，並在
    人物／家具 y-sort 後的資訊層輸出；程序 fallback 分別使用棕色咖啡杯、紅色愛心與藍色信封。
    未修改人物 sprite、`src/pixel/*`、模擬狀態或顧客資料。
- **驗證與工具**:
  - 新增 `scripts/cafe-intent-bubble-test.ts` 並登錄正式回歸：12/12，涵蓋三圖示、顯示守門、
    `composeFloor` 整合、輸入不變與零 RNG；`limezu-art-test.ts` 擴為 99/99，驗證 manifest、atlas
    邊界／不重疊、兩幀來源、1:1 繪製與失敗 fallback。
  - `render-floor --cafe-guests` 固定輸出三種 intent，768×2496 離線圖已目視確認三位入座顧客的
    咖啡杯／愛心／信封都清楚；`npm test`:75/75，`balance-test` 快照一致，`typecheck` app／Worker
    通過，`build`:138 modules transformed，只有既有大於 500 kB chunk 提示。
  - Mobile UI Lab 390×844、412×915、430×932 共 18 張，report 0 errors／0 warnings 並逐張檢查；
    in-app Browser 實測 3F → 1F → 3F 的單一目的地按鈕正常，未開張 1F 維持零假顧客，console 0 errors。
- **已知限制／後續**:正式顧客與泡泡要等 CAFE-11 建立 `state.cafe`、CAFE-13 接客流後才在公開
  頁面出現。下一項依序為 CAFE-11 state.cafe schema。
- **版本狀態**:`main`,基準 `4e0da33`;本批已完成待提交、尚未 push／deploy。
