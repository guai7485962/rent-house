# 《房東監視中》開發工作日誌

> 更新日期:2026-08-02
> 一款「異步掛機觀察型」直式手機模擬遊戲:玩家是房東,透過俯視娃娃屋觀察租客生活、佈置家具、招租收租;劇情由 **AI(Gemini)依歷史即時生成**,並能影響走向。

## 線上位置
- **遊戲網址(手機可玩):** https://rent-house.guai7485962.workers.dev
- **Repo:** github.com/guai7485962/rent-house(push 到 `main` → Cloudflare 自動 build + deploy)
- **架構:** 靜態前端(Vue+Vite)+ Cloudflare Worker(同時服務網站與 `/api/narrate`);AI 金鑰存 Worker Secret `GEMINI_API_KEY`。

## 目錄與文件地圖

> 🧭 **接手者請先讀根目錄 [`STATUS.md`](STATUS.md)**(≤120 行,含現在狀態、下一步與完整文件地圖)。
> 本檔在 2026-08-02 重整過:待辦與技術債移到 `docs/待辦.md`,已完成系統移到 `docs/系統總覽.md`,
> 2026-07-31 以前的逐日紀錄移到 `docs/工作日誌-封存.md`。
>
> **本檔現在只剩三件事**:如何執行/驗證、AI 供應商設定、**2026-08-01 起**的逐日紀錄。

- [一、如何執行 / 驗證](#一如何執行--驗證) — 測試與部署指令、無頭測試模板、部署驗證
- [附:AI 供應商設定](#附ai-供應商設定)
- [二、逐日進度紀錄(2026-08-01 起)](#二逐日進度紀錄2026-08-01-起) — 更早的見封存檔
- [新紀錄追加點(APPEND HERE)](#新紀錄追加點append-here) — 📌 **檔尾;下一則日期紀錄加在這裡**

**其他文件(都不需要預先讀,按需查閱)**
- [`STATUS.md`](STATUS.md) — 🧭 唯一必讀入口:現在狀態、下一步、文件地圖
- [`docs/待辦.md`](docs/待辦.md) — ⭐ 待辦與技術債的**唯一權威清單**(含 🔍 驗收錨點制)
- [`docs/系統總覽.md`](docs/系統總覽.md) — 已完成系統清單、地雷紀錄、檔案地圖
- [`docs/工作日誌-封存.md`](docs/工作日誌-封存.md) — 2026-07-12 ～ 2026-07-31 的逐日紀錄(逐字封存)
- [`docs/一樓寵物咖啡廳-設計.md`](docs/一樓寵物咖啡廳-設計.md) / [`docs/一樓寵物咖啡廳-工作分解.md`](docs/一樓寵物咖啡廳-工作分解.md) — 咖啡廳設計與 CAFE-xx 工作項
- [`docs/設計檢討與優化.md`](docs/設計檢討與優化.md) — 原始設計檢討
- [`docs/AI觀察回饋設計.md`](docs/AI觀察回饋設計.md) — AI 觀察回饋機制設計

---
## 一、如何執行 / 驗證
```bash
npm run dev                      # 本地開發(http://localhost:5173)
npx tsx scripts/sim-trace.ts 24  # 無頭:走位健全性
npx tsx scripts/part-test.ts     # 跨租客事件 + 收支 + 時間 7×
npx tsx scripts/qol-test.ts      # QoL:擺放預覽判定 + 分批快轉 + 存檔匯出入/重載還原
npx tsx scripts/move-test.ts     # 家具移動(跨房/失敗留原位)
npx tsx scripts/rent-test.ts     # 調租談判(降租/容忍/拒絕/冷卻/同居者擋下)
npx tsx scripts/stats-model-test.ts  # 數值模型(homeostasis/生病/精力)
npx tsx scripts/balance-test.ts      # balance 快照 diff(刻意調平衡後 --update)
npx tsx scripts/variety-test.ts  # 文本庫變化 + 時段替換
npx tsx scripts/feed-test.ts     # 動態 Feed 彙整/未讀/存檔往返
npx tsx scripts/arc-test.ts      # 劇情弧消毒/開推收全流程/伏筆 flag
npm run build                    # 產出 dist/
npx wrangler deploy --dry-run    # 檢查 Worker 打包
# 部署:git push（Cloudflare 自動 build+deploy）
```

```powershell
# PowerShell 每個 session 要先補 node 路徑
$env:Path = "C:\Program Files\nodejs;$env:Path"
cd C:\Users\User\claude_try\rent_house
npm test                          # 正式回歸集(REGRESSION)+ sim-trace
npm run typecheck                 # typecheck:app(vue-tsc)+ typecheck:worker
```

### 開發流程細節(2026-08-02 由 `docs/交接與待辦.md` 併入)

- **無頭測試模板**:檔頭先 mock `localStorage`(`(globalThis as any).localStorage = {...}`),
  再 `await import("../src/store")`。mock AI 用 `setNarrateImplForTest()`、`diaryTiming.gapMs = 1`。
  **新增測試務必把檔名加進 `scripts/run-all.ts` 的 `REGRESSION` 陣列**,否則不會進 CI。
- **機率型測試**:檔頭在 import store **之前**加 mulberry32 固定種子蓋掉 `Math.random`,
  否則 CI 會間歇紅燈。跑測試需在 UTC+8 或 `TZ=Asia/Taipei`(CI 已設 `env: TZ: Asia/Taipei`)。
- **commit**:訊息繁中、結尾 `Co-Authored-By: Claude <模型名> <noreply@anthropic.com>`。
- **部署驗證**:Bash 的 curl 在此機沙盒無網路;用 PowerShell:
  ```powershell
  $r = Invoke-WebRequest -Uri "https://rent-house.guai7485962.workers.dev" -UseBasicParsing
  # 取 assets/index-*.js 檔名,再抓該 js;中文字串要用 UTF8 解碼才搜得到:
  $b = (Invoke-WebRequest -Uri "<js url>" -UseBasicParsing).RawContentStream.ToArray()
  [System.Text.Encoding]::UTF8.GetString($b).Contains("某新字串")
  ```
- **改存檔結構**:`SAVE_VERSION++` + 在 `persistence.ts` 的 `MIGRATIONS` 加一段升級函式;
  新欄位用 `?? 預設值` 讀取通常就夠。**現行 `SAVE_VERSION = 6`**
  (`src/sim/persistence.ts:28`),要改請從 `MIGRATIONS[6]` 往上加。

## 附:AI 供應商設定
- 執行時日記 AI = **Gemini 免費層 → Cloudflare Workers AI(Qwen3)免費額度 → 內建模板**。平淡日常優先用 `gemini-3.1-flash-lite`,事件機會或劇情弧等重要日用具免費層的 `gemini-3-flash-preview`。
- Gemini key 設為 Worker Secret `GEMINI_API_KEY`;Workers AI 使用 `wrangler.toml` 的 `AI` binding,不需要另放 key。日記路徑不會自動使用可能計費的 Claude。
- Fable(claude-fable-5)僅用於**製作階段**一次性生成文本庫(走 Claude 訂閱,非執行時)。


---

## 二、逐日進度紀錄(2026-08-01 起)

> 📦 **2026-07-12 ～ 2026-07-31 的紀錄已逐字搬到 [`docs/工作日誌-封存.md`](docs/工作日誌-封存.md)**
> (未摘要、未刪改)。本節只保留 **2026-08-01 起**的紀錄。
>
> 🔍 **不要通讀本節**。要找特定日期或工作項編號請用 grep,例如:
> `rg -n "^### 2026-08-02" 工作日誌.md`、`rg -n "CAFE-18" 工作日誌.md docs/工作日誌-封存.md`。
>
> ✍️ **每筆條目 ≤15 行**,只寫:目的／實際變更與主要檔案／驗證結果／已知限制。
> 推理過程、逐行說明、設計論證**不入日誌**——那些寫進 `docs/` 下的設計文件。

### 2026-08-01 — 四方向人物:背面/側面基底 sprite、疊層與 renderer 內方向推導(Claude Code)

- **目的／使用者需求**:人物在樓層上**永遠正面**——走上樓、走左右、走下樓長得一模一樣,
  只有 `stand_face` 會補一顆側眼當提示。本批把人物升級成**四方向**(下=正面、上=背面、
  左右=側面),並在 **`agents.ts` 零改動**的前提下完成。

- **實際變更(4 個檔)**
  - `src/pixel/sprites.ts`:新增 **6 張手繪基底 sprite**(全部 11×19)+ `mirrorSprite()` +
    `CharView` 型別 + `CHAR_SPRITES` 查表。
  - `src/pixel/parts.ts`:新增 `HAIR_OVERLAYS_BACK` / `HAIR_OVERLAYS_SIDE`(**10 組新髮型疊層**)、
    `ACCESSORY_OVERLAYS_BACK` / `ACCESSORY_OVERLAYS_SIDE`、`overlayViewOf()`;
    `pat()` 加 `mirror` 參數;`drawAppearanceOverlay()` 加**選用**的 `view` 參數(預設 `"front"`)。
  - `src/floor/floorScene.ts`:`agentView()` 方向推導 + `lastFacingDir` 記憶 + sprite dispatch;
    **移除 `drawFacingCue()`**。
  - `scripts/appearance-test.ts`:擴充成四視角的越界/像素/輪廓檢查,並新增方向推導優先序斷言。

- **6 張新 sprite 的設計要點**
  - **背面**(`CHAR_STAND_BACK` / `CHAR_WALK_BACK_A` / `_B`):rows0-8 **全是髮色**,
    **沒有 `k` 眼睛、沒有 `m` 嘴**,只在 row8 露一小段暗膚 `f` 當後頸。這是「背對鏡頭」
    唯一且最強的辨識訊號。高光改成 col4→col3→col2 的斜向反光帶(而不是左右對稱兩點,
    避免被誤讀成眼睛)。衣服改用 `CHAR_SIT_BACK` 既有的「左亮右暗、無領口」打光,
    和正面的對稱領口分開。走路兩幀的軀幹與腿部**取正面 A/B 的鏡射關係**
    (背面 A 用正面 B 的下半身,反之亦然),因為從背後看左右是相反的。
  - **側面**(`CHAR_STAND_SIDE_R` / `CHAR_WALK_SIDE_R_A` / `_B`,只畫朝右):
    臉的右緣逐列外推再內收——row2→col8 額、row3→col8 眉、**row4-5→col9 鼻樑與鼻尖**、
    row6→col7(人中一次內縮 2 格)、row7→col7 唇、row8→col5 頸;頭頂也往後(左)偏一格。
    這條起伏才是「側臉不是正臉」的關鍵,單靠「把臉挪到右半邊」在 11px 下看不出來
    (第一版就是這樣,目檢直接退回重畫)。眼睛只有 **1 顆**(row5 col7),嘴 1 個。
    軀幹輪廓從正面的 **9 格收到 7 格**、**只露一隻手**;腳掌朝前伸出。
  - **鏡射做法**:朝左**不另外手繪**。`mirrorSprite()` 逐列反轉字串,在模組載入時
    一次算出 `CHAR_STAND_SIDE_L` 等三個常數(**不是每幀算**,零額外配置),
    再由 `CHAR_SPRITES` 這張 `Record<CharView, {stand,walkA,walkB}>` 查表。
    單一手繪來源 → 左右永遠對稱,測試也直接斷言 `side_l === mirrorSprite(side_r)`。
  - **坐姿刻意不動**:`CHAR_SIT_BACK` 早就存在,但本批**不碰坐姿的方向邏輯**,
    避免和活動椅那批的 `seatBack` 打架(見下方「沒做什麼」)。

- **髮型疊層:5 種 × {背面, 側面} = 10 組**
  疊層座標系與正面完全相同(11 寬、sprite 原點),但兩個視角的著力點不同:
  - **背面**沒有瀏海可遮,辨識點改成「後腦簾子/辮子」——長髮是蓋過後頸一路垂到上背(row11)
    的簾子、鮑伯是齊頸且**下擺外翻**(寬到 col0/col9)、馬尾是**沿背脊垂下的辮子**(row5→row12)
    加一圈亮色髮圈、刺蝟從背後看一樣是尖刺、短髮只補頭頂反光。
  - **側面**髮量往後腦(左)堆並往左外擴到 col0:長髮垂到肩胛下、鮑伯齊下巴且下擺外翻、
    馬尾**先往後翹再垂下**(側面最好認的一種)、刺蝟的尖刺往後偏。
  - ⚠️ 背面的長髮與馬尾**刻意垂到 rows9-13**(上背),所以 `appearance-test` 的垂直越界檢查
    對 back/side 放寬到 `-2..13`(止於軀幹下緣 row13,不會掉到腿上);**正面維持原本的 `-2..10` 不變**。

- **配件在各視角的處置表**(依「看得見才畫」的原則)

  | 配件 | 背面 | 側面 |
  |---|---|---|
  | `none` | —(空) | —(空) |
  | `glasses` | **不畫**(從背後完全看不到鏡片) | 畫,簡化成**單片鏡片 + 一根往後的鏡腳** |
  | `round_glasses` | **不畫** | 畫,簡化成**單顆圓框 + 鏡腳**(框內留白讓眼睛透出) |
  | `cap` | 畫:**去掉向前的帽簷**,補一條後扣帶 | 畫:**帽簷向前伸出輪廓外**(側面最強辨識點) |
  | `bow` | 畫(髮上蝴蝶結從背後最清楚;位置是正面的鏡射) | 畫,移到腦後 |
  | `headphones` | 畫:頭帶 + **雙耳罩**(背面反而最完整) | 畫:頭帶 + **單邊耳罩**(遠側被頭擋住) |

  「背面眼鏡類完全不畫」是**刻意**的,而且比硬畫更正確。代價是背面的 `none`/`glasses`/
  `round_glasses` 三者疊層像素完全相同,所以測試裡「六種配件輪廓互異」這條在背面改成
  「眼鏡類**必須**為 0 像素 + 其餘四種互異」,不是放寬,是換成更精確的斷言。

- **方向推導:在 `floorScene` 唯讀,零 `agents.ts` 改動**
  優先序(`agentView()`):
  1. `pairSession` 的 `stand_face` facing(`a.facing !== 0`)→ 維持既有行為,側面朝向對方
  2. 移動中 → `a.path[0]` 減 `{a.c, a.r}`。`findPath()` 是 **4 鄰 BFS(無對角)**
     (`pathfind.ts:57-62`),差值保證是正交四方向之一
  3. 靜止 → 沿用最後已知方向
  4. 都沒有 → 正面
  - **例外**:`pose === "cook_pair"` 一律回正面。既有的 `drawCookingCue()` 是照**正面基底**
    畫的抬手 + 鍋鏟像素,套在背面/側面骨架上會錯位;並肩料理本來就面向流理台,不失真。
  - **為什麼用 renderer-local 的 `Map<tenantId, CharView>` 而不是寫回 `Agent`**:
    (a) 朝向純粹是畫面問題,寫回模擬層會讓 `Agent` 多背一個沒人用的欄位,而該檔是別的 agent
        的活動區,本批的硬性約束就是不碰它;
    (b) 「靜止沿用最後方向」需要**跨幀**記憶,而 `tickAgents` 每幀重寫 `a.facing`
        (`agents.ts:103`),放在那裡會被覆蓋;
    (c) **決定性**:離線單幀渲染(`render-floor.ts` / `roomcam-test`)時這張表是空的,
        規則 3 落空 → 一律回退規則 4 的正面,和舊行為完全一致。另外export 了
        `clearFacingMemory()` 供測試把狀態清乾淨。
    Map 大小上限 = 樓層租客數(個位數),不需要主動清理。

- **`drawFacingCue()` 的處置:整個移除**
  它原本是「在正面基底上補一顆朝同伴方向的眼睛與鼻尖」的補丁,前提是**基底永遠正面**。
  現在 `stand_face` 且 `facing !== 0` 會直接畫**真正的側面 sprite**,側臉本身就自帶單眼與
  凸出的鼻樑,再補一顆點只會落在錯的位置(它寫死 `x+6`/`x+3`,和新的側臉幾何對不上)。
  `facing === 0`(垂直相鄰)舊版本來就 early-return 不畫,所以**沒有行為退化**。

- **驗證(全部實跑)**
  - **對照表 PNG 目檢(本批主力驗收)**:離線產了三張——姿勢 × 方向 × 5 髮型 ×{有/無配件}
    的 14×12 格對照表、四方向四髮型的 14 倍放大檢視、以及走 `composeFloor` 真實 dispatch 的
    樓層帶狀圖。結論:**四個方向一眼可分**(正面雙眼對稱 / 背面純髮無臉 / 側面單眼且臉偏向一側,
    左右互為鏡像);**背面確實看不到臉**;側面輪廓在第二版重畫後成立(第一版只把臉挪到右半邊,
    目檢看起來仍像正臉,已退回重畫鼻樑起伏);五種髮型在四個視角都仍認得出是同一種
    (長髮=最長的簾子、鮑伯=齊頸外翻、馬尾=辮子/後翹、刺蝟=尖刺、短髮=無額外輪廓)。
    **三張 PNG 與產圖腳本跑完即刪,未進 commit。**
  - **方向推導硬驗證**:離線用「畫/不畫同一幀的像素差集 vs 預期 sprite 遮罩」逐條比對
    四條優先序 + cook_pair 例外,**10/10 通過**;同一組斷言已用 `agentView()` 直接
    永久化進 `appearance-test.ts`(不需畫布)。
  - `appearance-test.ts` **36 通過 / 0 失敗**(擴充前 17 條)。
  - `roomcam-test.ts` **9/9**、`rotation-pose-test.ts` **29/29**。
  - `balance-test.ts`(**未加 `--update`**)**快照一致** → 證明 `SKIN_TONES` /
    `ALL_HAIR_STYLES` / `ALL_ACCESSORIES` 三個陣列的順序與長度**一個字元都沒動**,
    `randomAppearance()` 的 seeded 序列零位移。
  - `npm test` **68/68 支通過(74.4s)**;`npm run typecheck` app + worker 乾淨;
    `npm run build` 成功。
  - `npm run ui:shot -- rent`:三種手機尺寸共 **18 張**,`report.json` **0 errors / 0 warnings**,
    已逐張檢查沒有破圖。⚠️ 只有 `01-floor*` 那 6 張看得到人物,而且是 16px tile 的全樓層縮圖,
    對 11×19 的細節判斷幫助有限,**不當作美術驗收**(美術驗收是上面的對照表)。

- **我沒做什麼(刻意留下的邊界)**
  - **坐姿的方向邏輯沒動**。`CHAR_SIT` 一律正面、`CHAR_SIT_BACK` 一律由 `scene.ts` 的房間鏡頭
    自己決定,`floorScene` 的 `pose === "sit"` 分支維持原樣。理由是避免和活動椅那批的
    `seatBack` 打架;要接的話應該在同一批一起做。
  - **`src/pixel/scene.ts`(離線 renderer)沒動**。它畫的是房間細看鏡頭裡的靜態人物,
    沒有 `path`/`moving` 可推方向;`drawAppearanceOverlay()` 的 `view` 參數有預設值 `"front"`,
    所以它的行為與像素**完全不變**。
  - **`src/floor/agents.ts` 零改動**(硬性約束)、`groupScene.ts` / `petAgents.ts` / 任何 `.vue` /
    `src/sim/*` / `scripts/run-all.ts` / `balance-snapshot.json` 皆未觸碰;**未新增測試檔**。
  - 躺姿(`drawLying`)維持既有的髮型/配件分支,本批未擴充。

- **已知限制／後續**
  - 四方向只覆蓋**站與走**。坐、躺、`cook_pair` 一律正面。
  - `lastFacingDir` 不隨租客搬離而清除(上限是租客數,實務上無影響);要嚴謹可在
    `createAgents()` 時呼叫 `clearFacingMemory()`,但那需要動 `agents.ts`,留給後續。
  - 背面的長髮/馬尾會蓋住上背,所以走路時背面的手臂擺動在這兩種髮型下較不明顯。
- **硬規則**:純繪圖層與渲染層改動,未觸及模擬、AI、戀愛或消毒邏輯;
  未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒等既有硬規則全數保留。
- **版本狀態**:branch `feat/char-facing4`,以 `dc65603` 為基準;**未 push、未併回 `main`**。

### 2026-08-01 — 四方向人物 review 收尾:側面馬尾拉開辨識度、鏡射軸守衛、註解修正(Claude Code)

- **目的／使用者需求**:中控 review 通過(無阻斷性問題),但用色彩感知量測抓出三處收尾。
  本則處理必修 1~3 並補一條技術債。

- **必修 1:側面馬尾與鮑伯只差 4px(全批最弱)**
  - reviewer 量的是「站姿基底 + 髮型疊層畫完後,兩種髮型之間**換色的像素數**」。
    我先自寫同一套量測復現了它的全部數字(front 9 / back 10 / side_r 4,
    以及四視角站姿的 front↔back 48),確認量法一致才動手。
  - 病因:`HAIR_OVERLAYS_SIDE` 的 `ponytail` 與 `bob` 在側面幾乎重疊——側面頭部的
    髮區是 cols1-4,兩者畫在 cols0-3 的像素**大多落在本來就是髮色的格子上**,
    真正看得到的只有 col0 那幾點與腦後空白處。舊馬尾只垂到 y=7,和鮑伯的下擺同高。
  - 修法:把側面馬尾改成**高處束起 → 往後翹出輪廓外 → 收成 1px 細辮子一路垂到 y=12**
    (垂過肩後、蓋在軀幹上)。刻意做成「細而長」,和長髮的「寬簾子」、
    鮑伯的「齊頸即止」三者互相分開,而不是只把馬尾加粗。
  - 結果:**side_r / side_l 的最小差異 4 → 9**,與正面的 9 齊平;
    同時 ponytail/short 也從 8 → 13。其餘視角未受影響(front 9、back 10 不變)。

  改後的四視角髮型兩兩換色像素矩陣(對角線為 `-`):

  | front | short | long | ponytail | spiky | bob |
  |---|---|---|---|---|---|
  | short | - | 20 | 13 | 14 | 20 |
  | long | 20 | - | 17 | 28 | **9** |
  | ponytail | 13 | 17 | - | 21 | 14 |
  | spiky | 14 | 28 | 21 | - | 28 |
  | bob | 20 | **9** | 14 | 28 | - |

  | back | short | long | ponytail | spiky | bob |
  |---|---|---|---|---|---|
  | short | - | 33 | 24 | 15 | **10** |
  | long | 33 | - | 28 | 41 | 31 |
  | ponytail | 24 | 28 | - | 31 | 28 |
  | spiky | 15 | 41 | 31 | - | 19 |
  | bob | **10** | 31 | 28 | 19 | - |

  | side_r(side_l 逐格相同) | short | long | ponytail | spiky | bob |
  |---|---|---|---|---|---|
  | short | - | 17 | 13 | 13 | 10 |
  | long | 17 | - | 12 | 24 | 13 |
  | ponytail | 13 | 12 | - | 24 | **9** |
  | spiky | 13 | 24 | 24 | - | 17 |
  | bob | 10 | 13 | **9** | 17 | - |

  各視角最小值:**front 9(long/bob)、back 10(short/bob)、side_r 9(ponytail/bob)、
  side_l 9(ponytail/bob)**。改動前 side 是 4。

  - **新斷言**:`appearance-test.ts` 加「四視角髮型兩兩最少換色 ≥ **8** px」。
    門檻取 8 的理由:實測最小值是 9,取略低一格是為了留給日後微調的空間
    (例如換髮色或微調一兩個像素不該紅燈),但任何把辨識度砍回「幾乎看不出差別」
    的改動都會立刻擋下。**負向驗證**:把側面馬尾改回舊版短辮子,該條立刻紅燈並印出
    `side_r=4(ponytail/bob) side_l=4(ponytail/bob)`,確認不是空轉的斷言。

- **必修 2:`pat()` 的鏡射軸沒有寬度守衛**
  - `pat()` 的鏡射是 `SPRITE_W - 1 - c`,**以固定 11 為軸**而不是逐列長度。
    目前所有疊層列都是 11 寬,但將來若有人寫了 10 寬的行,朝左側面會**靜默位移 1px**。
  - 關鍵是既有的「`side_l` 疊層 === `side_r` 的鏡射」那條**抓不到**——兩邊用同一個公式,
    誤差會對消。所以另加一條直接驗寬度的斷言:`parts.ts` 新 export `SPRITE_W` 與
    `overlayRowWidths()`(回傳每一列的 `id` + `width`),測試斷言全部 === `SPRITE_W`。
  - **負向驗證**:把 `side/glasses` 的那列改成 10 寬,新斷言紅燈並精準指出
    `acc/side/glasses/0[0]=10`;而同一次執行裡**鏡射那條仍是綠的**,
    正好實證了 reviewer 說的「對消、抓不到」。

- **必修 3:`lastFacingDir` 的註解與事實不符**
  - 舊註解寫「大小上限 = 樓層租客數(個位數),不需要主動清理」是**錯的**:
    tenantId 是帶時間戳的唯一值(如 `tenant_r303_1785546924760_0`),退租時 key 不會被移除,
    所以這張表在長時間遊玩下是**單調成長**的,上限是「本次 session 曾出現過的所有租客數」。
  - 依 review 指示**不改程式**(每筆一個短字串鍵 + 一個列舉值,幾百筆也在數十 KB 等級,
    且 session 結束就整份消失),只把註解改成正確描述,並註明要清的話最自然的掛點是
    `createAgents()`、但那要動 `agents.ts`。

- **新增技術債(第五節)**:「`stand_face` 垂直相鄰時朝向不精確」。
  `pairSession.ts:79-82` 的 `facingToward()` 只在水平相鄰時給 `±1`,垂直一律 `0`,
  於是會落到規則 3「沿用最後走過的方向」——從下方走上來剛好正對(比舊版好),
  但從左側繞過來就會渲染成側面。**不是退化**(舊版一律正面也沒面向對方),
  只是換了一種不精確。修法是讓 `sessionFor()` 把對方 tile 或已算好的四方向一起回傳,
  擺脫 `facing: -1|0|1` 這個只表達得了左右的型別;需要動 `agents.ts` 與 `pairSession.ts`,
  本批 lease 明文禁止,故留待下批。已附驗收錨點。

- **驗證(全部實跑)**
  - `appearance-test.ts` **38 通過 / 0 失敗**(本次 +2 條);兩條新斷言都做過負向驗證(見上)。
  - `roomcam-test.ts` **9/9**、`rotation-pose-test.ts` **29/29**。
  - `balance-test.ts`(**未加 `--update`**)**快照一致**——本批只動疊層像素與註解,
    三個 RNG 陣列依然零觸碰。
  - `npm test` **68/68(71.6s)**;`npm run typecheck` app + worker 乾淨;`npm run build` 成功。
  - **對照表 PNG 重產目檢**:四方向 × 五髮型 × 站/走A/走B,13 倍放大。側面馬尾改動後
    細辮子垂在肩後、走路兩幀都貼合,與長髮的寬簾子、鮑伯的齊頸下擺三者一眼可分,
    其他視角未受影響。**PNG 與兩支量測/產圖腳本跑完即刪,未進 commit。**
  - 未重跑 `ui:shot`:本次只動了側面馬尾疊層的幾個像素與註解/測試,
    上一則已確認 18 張 0 errors / 0 warnings,且樓層縮圖本來就看不出這級別的差異。

- **已知限制／後續**:見上方新增的第五節技術債條目。
- **硬規則**:純繪圖層與測試/註解改動,未觸及模擬、AI、戀愛或消毒邏輯;既有硬規則全數保留。
- **版本狀態**:branch `feat/char-facing4`,接在同日「四方向人物」之後;**未 push、未併回 `main`**。

### 2026-08-01 — 文件對帳:清兩個孤兒、修三個壞錨點、補 10 條漏記、錨點制加三規則(Claude Code)

- **目的／使用者需求**:上一次整理(`b4cdd55`)建立了「驗收錨點制」,本次是**第一次機械執行**
  ——逐條把第四、五節的錨點 grep 一遍,再拿結果和條目狀態對帳。
  25 條當時可驗證的錨點裡 **22 條正確、3 條錯誤**,錯誤的三條全部指向同一個結構性問題:
  **錨點是寫條目時的「預言」,沒人在完成時回頭把它改成「證據」**。
  本次只做文件:打勾、修錨點、補漏記、同步交接文件、把該問題寫成規則。**未動任何程式碼。**

- **實際變更(僅 2 個文件檔)**:`工作日誌.md`、`docs/交接與待辦.md`。
  `src/`、`scripts/`、`worker/`、`docs/工作日誌-封存.md` 全數未觸碰。

#### A. 兩個孤兒條目(程式早已完成、checkbox 仍掛著)

1. **四方向人物**——第四節「內容與呈現(美術債)」與「美術第二批留項」**兩處各有一條**,
   都還是 `[ ]`。實際已於 `569d763` + `21b8b5c` 完成:`src/pixel/sprites.ts:263` 的
   `export type CharView`、`:416-418` 三組 stand/walkA/walkB、`src/floor/floorScene.ts:359`
   的 `agentView()`、`src/pixel/parts.ts:343-345` 的 `overlayViewOf()` 疊層與鏡射。
   兩處都改 `[x]`,並**收斂成一主一從**:完整說明與錨點只留在第一處,第二處改成純指標
   ——**重複列本身就是孤兒的來源**(一處劃掉、另一處忘了劃),兩邊各寫一份必然再度分歧。
   同時依錨點規則 3,把「只覆蓋站與走」另立成第五節的一條 `- [ ]`。
2. **月度事件鏈「全員擠交誼廳」的多人站位演出**——已由 Codex `eb8305f` 完成:
   `src/floor/groupScene.ts`(159 行新檔)的 `startGroupScene()` / `groupSceneFor()` /
   `groupSceneView()`,`src/sim/floorChain.ts:56` 的 `scene?: { layout }` 且 `:98`/`:163`/`:186`
   三個 stage 已接上,`loungeTiles()` 依家具阻擋挑不重疊站位,
   `src/floor/agents.ts:98` 優先於 pair session。
   除了打勾,**條目內文也一併更正**——它原本寫著「`startPairSession` 仍是 pair-only」,
   那句話現在是錯的(演出層已支援多人)。

#### B. 三個壞錨點的改寫(附實跑結果)

| 條目 | 舊錨點 | 實跑 | 新錨點 | 實跑 |
|---|---|---|---|---|
| 四方向人物 | `rg -n "CHAR_WALK_(UP\|DOWN)\|facingDir\|facing4" src/pixel/ src/floor/` | **0 命中**(誤判未完成) | `rg -n "CharView\|CHAR_STAND_BACK\|agentView" src/pixel/sprites.ts src/floor/floorScene.ts` | **8 命中**,正確反映已完成 |
| 多人站位演出 | `rg -n "startGroupSession\|startMultiSession" src/` | **0 命中**(誤判未完成) | `rg -n "startGroupScene\|groupSceneFor" src/floor/groupScene.ts src/floor/agents.ts src/sim/floorChain.ts` | **6 命中**,正確反映已完成 |
| `buyFurniture` 紀念物 | `rg -n "price <= 0\|memorial" src/sim/economy.ts` | **2 命中**(`:304` `movePlacement`、`:389` `sellFurniture`)→ **偽陽性** | `rg -A10 "export function buyFurniture" src/sim/economy.ts \| rg "price <= 0\|memorial"` | **0 命中**,正確反映未完成 |

  - 前兩條是**同一種錯**:實作採用的命名(`BACK`/`SIDE_R`/`SIDE_L`、`startGroupScene`)
    與當初猜的不同,錨點沒跟著改。後果是漏判——把做完的事說成沒做。
  - 第三條最危險:條目要求的是「**在 `buyFurniture` 函式體內**」有守門,
    而單行 `rg` 表達不了這個限定,會被同檔其他函式的守門命中。
    實際複核 `src/sim/economy.ts:252-261` 的 `buyFurniture()` **仍然只檢查
    `state.money < def.price` 與 `findFreeSlot`**,沒有 `price <= 0` 或 memorial 守門。
    照「命中即完成」機械執行會把它劃成完成 → **把沒做的事說成做完了**。
    兩段式寫法已在本 repo 實跑驗證(ripgrep 14.1.1,`|` 管線串接兩層 `rg`)。

#### C. 補進 10 條漏記待辦(原本只活在日期紀錄內文,機械掃描看不到)

- **Codex 三批留下的 5 條**(新小節「Codex 三批留下的漏記待辦」):
  每日 2 場自然社交／打鬥上限不含劇本排程與群體事件 🟢、
  永久隔音可擋噪音公審但尚未降低自然社交的噪音相性口角率 🟡、
  🐾 寵物面板展開後未補拍人工點擊視覺檢查 🟢、
  租客張力批的低頻彈窗(招租相性提示、紀念物二次確認)未補拍 🟢、
  天台場景採「樓層內夕景小舞台 + 隱藏主平面重複 sprite」而非真實地圖區域 🟢。
- **睡眠效率批與四方向人物批留下的 4 條**(新小節):
  `usedDefId` 排隊分支地雷 🟡、四方向只覆蓋站與走(坐/躺/`cook_pair`)🟡、
  `lastFacingDir` Map 單調成長 🟢、背面長髮/馬尾蓋住上背 🟢。
- **🔴 家具 tier 第三階段——本次最重要的一條,已獨立成第四節的 `- [ ]`**。
  它先前**只寫在一個 `[x]` 條目的內文** + 交接文件的括號裡,**沒有自己的 checkbox
  也沒有錨點**,機械掃描永遠看不到——連續兩輪對帳都沒把它撈出來,正是規則 3 的來由。
  條目寫明三件接手者必須先知道的事:(1) 沙發休息/看電視/泡澡/書桌在種子局
  **全踩 premium 家具**(`mic_desk` 72h、`gaming_desk` 41h、`shared_sofa`、`lounge_tv`、
  `bathtub`),一接就得**接受這是刻意的平衡調整並跑 `balance-test --update`**;
  (2) **不可照抄舒適度 `tierPart` 的形狀**,恢復效率是乘法通道、作用在不同資源軸,
  ROI 表要重新推導;(3) 接手前先讀 2026-07-30「睡眠效率 review 收尾」的
  「給第三階段的檢查清單」與 `usedDefId` 那條。
- ⚠️ **刻意沒列**「側面 ponytail/bob 辨識度」——已於 `21b8b5c` 解決
  (最小換色 4px → 9px,並加了 ≥8px 斷言 + 負向驗證),補列會製造假待辦。

#### D. 一處錯誤行號

`dream|<id>` 冷卻鍵殘留那條寫「清理點在 `src/sim/social.ts:368`」,
實際 `removeTenantRelations()` 在 **`src/sim/social.ts:485`**(`:368` 附近是 `encounter()`
的衝突判定,與本條無關)。已更正並補上鍵前綴的定義位置
(`src/sim/dreams.ts:55` 的 `DREAM_CD_PREFIX = "dream|"`)。

#### E. `docs/交接與待辦.md` 同步(先前落後 7 個 commit)

依嚴重度:

1. 🚨 **存檔版本**:寫「已升 v5」,實際 `src/sim/persistence.ts:28` 已是 **`SAVE_VERSION = 6`**
   (Codex `eb8305f` 為了把群體場景排程佇列寫進存檔而升的)。**照 v5 寫遷移會直接出錯**,
   這是本次修掉的最危險一處。已在最上方新增獨立警語段並改寫第一節「現況快照」。
2. **L-4 活動椅**:原文寫「`drawActivityChair(ctx, a)` 寫死一組棕色、簽章無區分參數」
   ——**與現況完全相反**(`19fca0e` + `dc65603` 已改成 `activityChairStyle()` 反查後
   分派四種造型 + 半徑收窄至 1 + 版本號 memo)。改標 ✅ 並就地寫明「原文與現況相反」。
3. **L-5 atlas 死 crop**:原文寫「日後重建時順手清」——`fcbdd30` 已清完
   (`furniture.png` 8,696 → 6,992 bytes,18 個 frame 逐格不變)。改標 ✅。
4. **L-2 四方向人物、J-3 多人站位**:兩條殘留孤兒,一併標 ✅ 並更正內文(同 A 節)。
5. **基準數字**:66/66 → **68/68**、Vite 132 → **137 modules**、HEAD `cc9c07c` → **`21b8b5c`**;
   舊數字降級為歷史快照而非刪除。
6. **三整套新系統完全缺席**:`groupScene`、`petHomes`、租客 `tension` 積怨軸在本檔
   **各 0 次提及**。已在最上方「最新交接」補完整介紹,第一節「現況快照」補一句話摘要,
   第六節「檔案地圖」新增一小節列出四塊(三系統 + 四方向人物)的入口檔案與行號。
7. **「共 30 條、一對一對應」的宣告已失效**:數量與內容都對不上。改成用腳本重數的實況
   (42 受追蹤 = 36 未完成 + 6 已完成帶錨點,其中 35 條可機械執行),
   並寫出 29 → 36 的變化過程(+10 補記、−3 完成)。
8. 標題日期 2026-07-30 → **2026-08-01**,並改成點出本批主題。
9. 順帶更新「目前優先候選」與「這幾批留下的小殘留」兩段的過期內容,
   新增 O(Codex 5 條)、P(睡眠效率/四方向 4 條)兩段與 L/J 段的完成標記。

#### F. 錨點制新增三條規則(本次最重要的結構性改進)

寫在第四節開頭錨點制說明的後面,標題「📐 錨點三規則(2026-08-01 對帳後新增)」:

1. **完成一條時,若實際符號與錨點不符,改 `[x]` 的同時必須把錨點改寫成真實符號。**
   讓錨點從「我猜完成後程式碼長這樣」變成「這條為什麼算完成」的證據。
2. **「函式體內」「僅限某檔某段」這類限定條件不能用單行 `rg` 表達,必須用兩段式**
   `rg -A<n> "<錨>" <檔> | rg "<樣式>"`。單行 `rg` 只知道「這個檔裡有這個字」,
   不知道它落在哪個函式;限定條件寫在內文而錨點沒有 → **偽陽性**,比漏判更危險。
3. **任何 `[x]` 條目的內文若含「尚未／留待／第 N 階段」字樣,必須另開一條 `- [ ]` 並自帶錨點。**
   掛在 `[x]` 內文裡的殘留工作對機械掃描是隱形的——掃描只看 checkbox 狀態。

  三條規則各自附了本次抓到的實例,讓後人知道它們不是憑空訂的。

#### 驗證(對帳後把全部錨點重跑一遍)

- **受追蹤條目 42 條**(第四節 16 未完成 + 6 已完成、第五節 20 未完成;
  另有一批錨點制之前的無錨點舊 `[x]` 不計)。
  **未完成條目的錨點覆蓋率 100%(36/36)**。
- **其中 35 條可機械執行**,7 條標「長期項／需人工目檢/無程式碼訊號」
  (平衡調整、`/api/*` rate limit、章節卡堆疊間距、夢境 `ui:shot`、
  🐾 面板補拍、低頻彈窗補拍、背面長髮目檢)。
- **35 條全部實跑,結果與條目狀態 100% 一致**:
  - **6 條 `[x]` 全部命中**——四方向走路 sprite / 四方向人物(同一指令)、AI 色碼對比安全化、
    活動椅造型、atlas 清理(python 兩段檢查印出 `manifest殘留 [] |尺寸 (128, 128) |死區非全透明 []`)、
    多人站位演出。
  - **29 條 `[ ]` 全部正確判定為未完成**。其中 8 條是「有命中但條件不成立」型
    (`socialFulfillment` 只命中 `tick.ts` 一支、`COZY_COMFORT` 仍是 52、
    `COMMUNAL_ATTR_MAX` 仍是 50、`communalQuality()` 仍無 tenantId 多載、
    `upgradeAttributes` 無共用區守門、`pendingChainEvent` 只命中註解、
    balance 快照仍只有 `logs` 無 emoji 計數、`8000:8000` 命中但仍被 `#` 註解)
    ——這類條目的判定仍需讀一眼命中內容,已是既有設計。
  - **殘留孤兒 0 個、偽陽性 0 個。**
- ⚠️ **本次新寫的錨點被自己的規則 2 抓到一次**:`usedDefId` 那條初稿寫單行
  `rg -n "usedDefId|furnitureDefId" scripts/`,實跑**命中**
  `scripts/furniture-tier-test.ts:266` 的 `function deltasOf(st, furnitureDefId?)`
  ——那只是呼叫 `generateHourly` 的測試輔助參數,**完全沒有斷言排隊者不吃乘數**,
  是不折不扣的偽陽性。當場改成綁定 `waiting_for_bathroom` 情境的兩段式後實跑 0 命中。
  這件事本身就是規則 2 的最佳佐證:**偽陽性很容易寫出來,而且寫的人當下不會發現。**
- 純文件變更,`git diff --check` 乾淨;未跑 `npm test` / `build`
  (最近基準:2026-08-01 四方向 review 收尾的 `npm test` 68/68、Vite 137 modules)。

- **已知限制／後續**:
  - 第五節仍有 4 條「有命中但要靠人判斷值有沒有變」型的錨點
    (`COMMUNAL_ATTR_MAX`、`upgradeAttributes`、`pendingChainEvent`、`communalQuality(`),
    嚴格說也適用規則 2 的兩段式。本次**刻意不動**——它們不在本次對帳的授權範圍內,
    且都是「條件不成立時判未完成」的保守方向(漏判而非偽陽性),風險低於 `buyFurniture` 那條。
    下一輪對帳可順手收掉。
  - 錨點制仍是人工維護。規則 1 依賴「完成的人記得回頭改錨點」,和它要取代的
    「交付時記得回來劃掉」是同一種人性風險;差別只在**規則 1 的失敗會被下一次機械執行抓到**
    (錨點 0 命中但條目是 `[x]`,或反過來),而舊做法的失敗會一直沉默下去。
- **硬規則**:純文件變更,未觸及模擬、AI、戀愛或消毒邏輯;
  未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒等既有硬規則全數保留。
- **版本狀態**:branch `docs/reconcile-3`,以 `21b8b5c` 為基準;**未 push、未併回 `main`**。

### 2026-08-01 — 一樓寵物咖啡廳設計草案:畫進樓層地圖、常備訂單研發、租客維持三樓(Claude Code)

- ⚠️ **狀態:設計草案,待使用者審閱後才進入實作,本批尚未動任何程式碼。**
  第四、五節**刻意不新增任何 `- [ ]` 待辦**——設計尚未核准,還不是待辦。核准後才會開 task lease 與待辦條目。

- **目的／使用者需求**:使用者要求為這棟樓設計「一樓寵物咖啡廳」,作為房東經營主線的延伸。
  本批只產出設計文件,把使用者已拍板的方向、既有系統的接縫、與零漂移風險寫成可審閱的一份草案。

- **實際變更(僅 1 個新增文件檔,未動任何程式碼)**
  - `docs/一樓寵物咖啡廳-設計.md`(新增):十二節設計草案,基準 commit `d1af916`。
    含定位、樓層架構、美術盤點、核心迴圈、顧客模型、三條敘事接縫、存檔、零漂移策略、
    六期分期實作計畫、未解問題。

- **三個已拍板的決定(2026-08-01,使用者)**
  - **呈現方式:畫進樓層地圖**——不做獨立分頁。代價是需要 viewport;做法是把
    `PixelDollhouse.vue:122` 房間特寫**已在用**的 `composeFloor` scale+translate 窗格邏輯
    抽成共用 `FloorViewport`,3F/1F 兩個固定機位切換,不做自由捲動與縮放。
    這把最大成本項從「全新鏡頭子系統」降級成「重構一個已有測試覆蓋的元件」。
  - **經營節奏:常備訂單 ＋ 研發決策**——進貨每日自動補水位,離線不斷貨、不懲罰;
    玩家的真決策集中在低頻高重量的研發(同時只能一項,照抄既有日數倒數)。
  - **樓層編號:租客維持三樓,存檔零遷移**——1F 咖啡廳、2F 不進遊戲、3F 仍是 `r301`~`r304`。
    理由:`roomId` 是 `state.occupancy`、`placements[].room`、`upgradeState.byRoom` 的 key 且全都進存檔,
    改名等於升 `SAVE_VERSION` 並替所有舊檔寫遷移,換來的只是數字好聽,不值得。

- **順手解掉的既有 🔴 待辦(兩條)**
  - 🔴 **訪客 NPC**:卡住的真正原因是「每個看得見的人都綁死 `TenantRuntime`」,
    假 runtime 會污染收租/社交/日記/成就/週報十幾個 pass。設計提出 `CafeGuest`——
    形狀抄 `GroupSceneActor`,**不進 `state.runtimes`**,存 `state.cafe.guests`(cap 6),
    離開即移除。既有描述說「需新 sprite 與場景」高估成本:sprite 那半已被 `parts.ts`
    加四方向基底解決。
  - 🔴 **G-12 樓層擴建**:本設計就是它的具體化身,且證明**不需要 `floorId` 維度**——
    往下延伸同一張 grid(`GRID_H` 33→52)、加一道樓板分隔帶與一段樓梯即可,
    尋路、agents、petAgents、groupScene 全部自動生效,租客能真的走下樓。

- **驗證結果**:純文件變更,`git diff --check` 乾淨;檔頭「日期紀錄索引」新錨點對得到本則標題。
  **未跑 `npm test` / `npm run build` / `ui:shot`——本批零程式碼變更,無可測目標。**
  (最近基準:2026-08-01 四方向 review 收尾的 `npm test` 68/68。)

- **已知限制／後續**:
  - 設計本身尚未核准,文件內所有數值(客流係數、投資價格、日淨利 30–50% 目標)都是草案值,
    未經 `balance-test` 驗證。
  - 文件已標出三個必須避開的漂移陷阱:咖啡廳**不可**列進 `COMMUNAL_AREA_IDS`
    (否則 `COMMUNAL_NEUTRAL = 35.28233009708738` 失效、全樓基準線位移)、
    `INITIAL_PLACEMENTS` **只 append 永不 insert**、`at_cafe` visualState 需設 `CAFE_FIRST_DAY` 閘門。
  - 三處硬編白名單(`placements.ts:140`、`routine.ts:166`、`comfort.ts:308`)在第 1 期必須一起檢視,
    漏改會靜默出錯。

- **硬規則**:純文件變更,未觸及模擬、AI、戀愛或消毒邏輯;
  未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒等既有硬規則全數保留。
- **版本狀態**:branch `docs/cafe-design`,以 `d1af916` 為基準;**未 push、未併回 `main`**。

### 2026-08-01 — 一樓寵物咖啡廳工作分解:22 個可認養工作項、三條平行主幹、CAFE-03 唯一瓶頸(Claude Code)

- ⚠️ **狀態:工作分解草案,尚未動任何程式碼,任何項目動工前須先開 lease。**
  第四、五節**刻意不新增任何 `- [ ]` 待辦**——設計與分解都還沒核准,還不是待辦。
  認養任一項工作前,必須先從 `coordination/templates/task.md` 複製一張 lease 到 `coordination/tasks/`,
  檔名用項目 ID(例如 `cafe-03-map-grid.md`),lease 的「允許修改／禁止修改」直接抄分解文件該項的清單。

- **目的／使用者需求**:把上一則的咖啡廳設計草案拆成可認養、可平行、可驗收的工作項,
  讓兩個 Agent(Claude Code 與 Codex)在同一個 checkout 上不撞車地分工。

- **實際變更(僅 1 個新增文件檔,未動任何程式碼)**
  - `docs/一樓寵物咖啡廳-工作分解.md`(新增):工作分解與認養表,設計依據 `docs/一樓寵物咖啡廳-設計.md`,
    基準 commit `d1af916`。含認養規則、依賴圖、貫穿全程規則、逐項工作、建議認養順序、與既有待辦的收斂。

- **分解結果**
  - **22 個可認養工作項 `CAFE-01`~`CAFE-22`**,外加 **1 項不可認養的中控驗證 `CAFE-00`**
    (balance hook 逐項驗證;屬整合責任、需跨項目全域視角,依專案 `CLAUDE.md` 由中控執行)。
  - **三條可平行的主幹**:**地圖線**(01→02→03→…)、**顧客線**(07→…)、**經濟線**(11→…)。
    顧客線與經濟線在 CAFE-03 完成前就能開工,只是最後要接起來。
  - **`CAFE-03`(地圖擴充:`GRID_H` 33→52 與 Region)是全案唯一的序列瓶頸**——它動 `map.ts`
    且位於關鍵路徑,那段期間另一方必須避開所有 `src/floor/`。
  - 每一項都附**「允許修改／禁止修改」檔案清單**與**一行可 grep 的驗收錨點**,
    沿用第四、五節既有的驗收錨點制(含它的三條規則);要判斷前置項是否完成,跑錨點即可,不必問人。

- **三條貫穿全案、寫進每一張 lease 的鐵則**
  - 🔴 **不得把任何 cafe region 加進 `src/sim/comfort.ts` 的 `COMMUNAL_AREA_IDS`**——
    一加 `COMMUNAL_NEUTRAL = 35.28233009708738` 立刻失效,全樓租客基準線位移、平衡快照全紅。
  - 🔴 **`INITIAL_PLACEMENTS`(`map.ts:150`)只能 append,永不中段插入**——
    `resolveTarget`(`routine.ts:176`)取陣列**第一個**符合的,中段插入會讓全樓租客的
    沙發/電視/廚房目標**靜默改道**。
  - 🔴 **`map.ts` rows 0–32 的任何既有矩形一格都不能動**——現有三樓維持原樣,這是零漂移的第一道防線。

- **建議 Codex 優先認養的三項**(依「誰寫的誰最熟」):
  **CAFE-21**(cafe venue;`groupScene.ts` 是它建的)、**CAFE-22**(寵物下樓;`petAgents.ts` 是它近期改的)、
  **CAFE-04**(樓梯與樓板分隔帶美術;人物美術第一、二批都是它做的)。

- **驗證結果**:純文件變更,`git diff --check` 乾淨;檔頭「日期紀錄索引」新錨點對得到本則標題。
  **未跑 `npm test` / `npm run build` / `ui:shot`——本批零程式碼變更,無可測目標。**
  (最近基準:2026-08-01 四方向 review 收尾的 `npm test` 68/68。)

- **已知限制／後續**:
  - 設計與分解**都尚未核准**;核准前不得認養任何項目,也不會有待辦條目。
  - 分解文件內的工期假設與依賴順序尚未經實作驗證;真正開工後若錨點與實際符號不符,
    依錨點制第一條規則,改狀態的同時要把錨點改寫成真實符號。
  - 兩個 Agent 共用同一個工作樹,**同時只能有一方持有未提交的變更**;認養時務必先
    `git status --short`,一次只認養一項,做完提交或合併再認養下一項。

- **硬規則**:純文件變更,未觸及模擬、AI、戀愛或消毒邏輯;
  未成年排除戀愛線、成人內容遮蔽式且預設關、AI 輸出白名單/夾值/消毒等既有硬規則全數保留。
- **版本狀態**:branch `docs/cafe-design`,以 `1a6fc34` 為基準;**未 push、未併回 `main`**。

### 2026-08-01 — CAFE-01 FloorViewport 抽取:任意矩形鏡頭、房間特寫逐位元不變(Codex)

- **目的／使用者需求**:使用者同意從一樓寵物咖啡廳工作分解選一項開始動工。本批認養無前置依賴的
  `CAFE-01`,把房間特寫既有的 scale/translate 鏡頭抽成可供下一項樓層切換器重用的共用函式。
- **實際變更**:
  - 新增 `src/floor/viewport.ts`:定義 `FloorViewportRect`、任意矩形建構、置中且限制於樓層邊界的
    `centeredViewportRect`,以及維持像素化與既有 rounding 順序的 `renderFloorViewport`。
  - `src/components/PixelDollhouse.vue`:改用共用 viewport 計算與繪製;仍直接呼叫原本的
    `composeFloor`,未改其本體、繪製順序、8×7 格視窗或 3 倍縮放。
  - `scripts/roomcam-test.ts`:既有房間鏡頭驗證改走共用函式,並新增舊／新路徑的像素 buffer
    逐位元比較,防止後續重構造成亮度門檻抓不到的細微畫面漂移。
- **驗證結果**:
  - `npx tsx scripts/roomcam-test.ts`:10/10 通過,含「FloorViewport 與舊鏡頭逐位元相同」。
  - `npm test`:68/68 支測試通過;`balance-test` OK。
  - `npm run typecheck`:app 與 Worker 全數通過。
  - `npm run build`:成功（138 modules transformed）；僅保留既有 500 kB chunk 警告。
  - workspace root `npm run ui:shot -- rent`:390×844、412×915、430×932 共 18 張，
    `report.json` 為 0 errors／0 warnings；已逐張人工檢查，無空白、裁切、變形或模糊退化。
- **限制／後續**:`CAFE-01` 只提供共用鏡頭能力，尚未修改 `FloorMap.vue`、地圖高度或任何咖啡廳內容；
  下一個地圖線項目是 `CAFE-02` 樓層切換器。
- **硬規則**:未修改 `composeFloor()`、`FloorMap.vue`、地圖 rows 0–32、模擬、平衡、AI、戀愛或消毒邏輯。
- **版本狀態**:`main`,基準 `8ee1cfc`;本批已提交、尚未 push。

### 2026-08-01 — CAFE-02 樓層切換器:固定 3F/1F 機位、空白一樓驗收(Codex)

- **目的／使用者需求**:使用者要求接續完成咖啡廳第二項工作。本批在 `CAFE-01` 共用 viewport
  之上加入固定樓層切換，不做自由捲動或縮放；地圖尚未擴充時，1F 應明確呈現空白機位。
- **實際變更**:
  - `src/components/FloorMap.vue`:新增 `FLOOR_VIEWS`，固定 3F `(0,0,16,33)` 與
    1F `(0,34,16,18)`；加入具 tab 語意與選取狀態的「3F 租屋樓／1F 咖啡廳」切換器。
  - canvas 尺寸、`renderFloorViewport` 繪製、點擊格座標、房間標籤與人物／寵物名牌都改以目前
    viewport 為基準。切到尚未存在的 1F 前先清可見 canvas，因此不會殘留上一幀的三樓畫面。
  - 現有交誼廳、大門與群體演出覆蓋層只在 3F 顯示；切回 3F 後房間與名牌完整恢復。
  - 新增 `scripts/floor-view-test.ts` 並列入 `scripts/run-all.ts` 正式回歸，鎖定兩個矩形、共用
    viewport、動態 canvas 尺寸、全域點擊換算、無障礙 tab 與「不得加入自由手勢」合約。
  - `src/App.vue` 無須修改；切換狀態由 `FloorMap` 自己管理，沒有增加跨元件耦合。
- **驗證結果**:
  - `npx tsx scripts/floor-view-test.ts`:8/8 通過。
  - `npm test`:69/69 支測試通過；`balance-test` OK，既有 `roomcam-test` 10/10 仍通過。
  - `npm run typecheck`:app 與 Worker 全數通過；`npm run build`:成功（138 modules transformed），
    僅保留既有 500 kB chunk 警告。
  - workspace root `npm run ui:shot -- rent`:390×844、412×915、430×932 共 18 張，
    0 errors／0 warnings；逐張人工檢查，切換器未擠壞樓層、操作列或其他頁面。
  - in-app Browser 互動驗證：1F 選取後 canvas 為 `256×288` 且畫面空白；切回 3F 為
    `256×528`，房間／人物標籤恢復；browser console 零錯誤。
- **限制／後續**:本批沒有修改 `GRID_H` 或建立咖啡廳 Region，所以 1F 空白是預期狀態；下一項
  `CAFE-03` 才會把地圖擴到 52 rows。切換器已先完成正確的全域格座標換算，可直接承接該地圖。
- **硬規則**:未修改 `floorScene.ts`、`agents.ts`、`map.ts`、rows 0–32、模擬、平衡、AI、戀愛或消毒邏輯。
- **版本狀態**:`main`,基準 `55e7a70`;本批已提交、尚未 push。

### 2026-08-01 — CAFE-03 地圖擴充:52 rows、六個 Region、跨樓層尋路(Codex)

- **目的／使用者需求**:使用者要求接續完成咖啡廳第三項、也是全案唯一關鍵路徑。本批把既有
  單一 grid 往下擴至 1F，要求三樓逐格零漂移、可從三樓走到一樓店門，且不得讓咖啡廳污染
  住宅公共舒適度基準。
- **實際變更**:
  - `src/floor/map.ts`:`GRID_H` 33→52；新增 `CafeRegion` 與 `CAFE_RECTS` 六區：
    `stairs`、`cafe_floor`、`cafe_counter`、`cafe_pet`、`cafe_back`、`cafe_entrance`。
    重疊優先序讓吧台／寵物區覆蓋主廳；既有 row 32 `entrance` 在最後覆蓋 `stairs`，所以
    三樓入口格維持不變，row 33–35 再自然接成樓梯。
  - `src/sim/placements.ts` 的 `FLOOR_REGIONS` 與 `src/sim/routine.ts` 的 `COMMUNAL` 僅加入
    四個咖啡廳室內區。`stairs`／`cafe_entrance` 刻意不可擺家具，避免堵住唯一跨樓層動線。
  - 新增 `scripts/cafe-map-test.ts` 並列入 `scripts/run-all.ts`：鎖定舊三樓與初始家具 SHA-256、
    六區矩形／覆蓋關係、模組載入時網格高度、跨樓層 BFS、擺放與 routine 白名單，以及
    `COMMUNAL_AREA_IDS`／`COMMUNAL_NEUTRAL` 禁區。
- **零漂移證據**:
  - rows 0–32 的序列化 SHA-256 仍為
    `49d377afd1be8169d9864c7ab4bee8a44266951e93c16d6f131414d82301c6e7`。
  - 35 筆 `INITIAL_PLACEMENTS` SHA-256 仍為
    `34073680172e1dd99775acfd2449edb4a4c4f552d22b85a86df9575bdecad22b`。
  - `src/sim/comfort.ts` 與 `scripts/balance-snapshot.json` diff 均為空；獨立 `balance-test` 一致。
- **驗證結果**:
  - `npx tsx scripts/cafe-map-test.ts`:21/21 通過；三樓每個地圖可走格在純拓樸上都可抵達
    1F 門口，套用現有家具障礙後三樓入口仍可抵達。
  - `npm test`:70/70 支測試通過；`npm run typecheck` app／Worker 通過；`npm run build`
    成功（138 modules transformed），僅保留既有 500 kB chunk 警告。
  - workspace root `npm run ui:shot -- rent`:三種手機寬度共 18 張，0 errors／0 warnings，
    逐張檢查確認三樓比例與內容未改。
  - in-app Browser 切到 1F：256×288 畫布正確呈現樓梯口、主廳輪廓與下方店門，無三樓殘影，
    browser console 零錯誤。
- **已知限制／後續**:
  - 現有 r302 `(10,7)` 是修改前就被家具與牆圍住的單格口袋；地圖純拓樸全連通，且實際三樓
    入口到 1F 門口路徑成立。本批未移動任何家具去改寫既有盤面。
  - 1F 目前沿用通用地板／牆面繪製；樓梯與樓板美術屬 `CAFE-04`，分區 tint／atlas 屬 `CAFE-05`。
  - `floorScene.ts` 的 `FLOOR_H` 由公式正確得到 832 且有測試，但該行舊的 `// 384` 註解依本項
    禁止修改範圍未動，待下一項合法觸及該檔時同步修正。
- **硬規則**:`COMMUNAL_AREA_IDS` 仍嚴格只有 lounge／bathroom／laundry；未修改 rows 0–32 矩形、
  `INITIAL_PLACEMENTS`、`floorScene.ts`、`agents.ts`、平衡快照、AI、戀愛或消毒邏輯。
- **版本狀態**:`main`,基準 `cf36aea`;本批已提交、尚未 push。

### 2026-08-01 — CAFE-04 樓梯與樓板分隔帶美術:LimeZu 木階、離線 fallback、兩層清楚斷面(Codex)

- **目的／使用者需求**:使用者要求接續完成一樓寵物咖啡廳第四項。本批只處理 row 33 樓板橫切面
  與 `stairs` 區的俯視樓梯，不提前動 CAFE-05 的咖啡廳地板 tint／獨立 atlas，也不碰家具、人物或模擬。
- **實際變更**:
  - `scripts/limezu-manifest.json`、`src/art/limezu.ts`、`public/assets/limezu/walls.png`：從指定的
    `17_Visibile_Upstairs_System_16x16.png` 選入兩塊完全不透明的 16×16 木階踏面與一塊暖灰樓板
    切面，append 到 `walls.png` 的 y=48 新列；atlas 由 64×48 擴為 64×64，既有 9 個建築 frame
    與 18 個家具 frame 座標全數不動，`furniture.png` SHA-256 仍為
    `d64359a5f94a42c09046dc646be0fc777f9930504f7af65166268d99273114ea`。
  - `src/floor/floorScene.ts`：新增並 dispatch `drawFloorDivider()`／`drawStairs()`。分隔帶橫跨
    row 33 全寬，2×4 格樓梯依 `CAFE_RECTS.stairs` 覆蓋中央開口；有 atlas 時交錯鋪 LimeZu 木階，
    圖片不可用時走同尺寸程序踏面，側軌與收邊在兩條路徑都維持清楚。同步修正 `FLOOR_H` 過時的
    `// 384` 註解為 `// 832`。
  - `scripts/limezu-art-test.ts` 擴到 81 項，交叉驗證 manifest／atlas 尺寸、frame 邊界與來源、
    1:1 繪製及 `composeFloor` dispatch 順序。新增 `scripts/cafe-stair-art-test.ts` 並列入正式回歸，
    鎖定只影響 rows 32–35、樓梯 32×64 邊界、row 33 全寬與 fallback 像素 SHA-256
    `c9c90a8edb8ca21fed44c3c2ed3f5e43fe50348933089cc4c43d2004adf5a8bf`。
- **驗證結果**:
  - `npx tsx scripts/limezu-art-test.ts`:81/81；`npx tsx scripts/cafe-stair-art-test.ts`:5/5。
  - `npm test`:71/71 支全綠（含 `balance-test`）；`npm run typecheck`:app／Worker 通過；
    `npm run build`:138 modules transformed，僅既有 500 kB chunk 提示。
  - 離線 `render-floor.ts` 產出 768×2496 fallback 圖，目視確認 row 33 橫切面完整、中央樓梯連續，
    三樓與一樓在沒有 atlas 的路徑也清楚分層。
  - workspace root `npm run ui:shot -- rent`:390×844、412×915、430×932 共 18 張，
    `report.json` 0 errors／0 warnings；逐張檢查樓層、動態、收支頁皆無空白、裁切或版面退化。
  - in-app Browser 實際切到 1F：canvas 256×288、LimeZu 木階在畫面頂端清楚接入；切回 3F 正常，
    兩個狀態的 browser console 均為 0 warnings／0 errors。
- **已知限制／後續**:一樓仍沿用通用地板底色，且尚無咖啡廳家具；分區 tint／`cafe.png` 屬
  `CAFE-05`，吧台、桌椅、貓跳台等 catalog 屬 `CAFE-06`，本批刻意不越界。
- **硬規則**:未修改 `map.ts`、`INITIAL_PLACEMENTS`、`comfort.ts`、`furniture.png`、既有 18 個
  家具 frame、`drawAgent`／人物繪製、模擬、平衡、AI、戀愛或消毒邏輯。
- **版本狀態**:`main`,基準 `5d43e0f`;本批已完成待提交、尚未 push。

### 2026-08-01 — CAFE-02 UX 修訂:雙分頁改為獨立樓層頁面與單一目的地按鈕(Codex)

- **目的／使用者需求**:使用者確認原先構想是用一顆按鈕直接前往另一樓層的完整畫面，不是讓
  3F／1F 以雙分頁在同一張圖內互相連結。本批依此將樓層導覽改為兩個獨立頁面狀態，同一時間
  只顯示目前樓層與唯一目的地按鈕。
- **實際變更**:
  - `src/components/FloorMap.vue`:移除 `tablist`／雙樓層 tab；3F 頁只顯示「前往 1F 寵物咖啡廳」，
    1F 頁只顯示「返回 3F 租屋樓」。切換時以 keyed `floor-wrap` 替換整個樓層視窗並加入短淡入，
    兩頁各有獨立標題、背景與頁面 aria-label；內部仍共用既有 grid 與 `FloorViewport`，未複製地圖資料。
  - `scripts/floor-view-test.ts`:改鎖定單一目的地按鈕、雙向頁面替換、獨立頁面標籤，以及不得回歸
    `tablist`／`tab`／舊 `floor-switcher`。
  - `docs/一樓寵物咖啡廳-設計.md`、`docs/一樓寵物咖啡廳-工作分解.md`:將 CAFE-02 的產品決策、
    UX 圖、完成條件與驗收錨點同步改為「玩家分頁、程式共圖」和單一目的地按鈕。
- **驗證結果**:
  - `npx tsx scripts/floor-view-test.ts`:10/10；`npm test`:71/71 全綠（含 `balance-test`）。
  - `npm run typecheck`:app／Worker 通過；`npm run build`:138 modules transformed，僅既有 500 kB
    chunk 提示。
  - workspace root `npm run ui:shot -- rent`:390×844、412×915、430×932 共 18 張，`report.json`
    0 errors／0 warnings；逐張檢查樓層、動態、收支頁均無裁切或版面退化。
  - in-app Browser 互動驗收:3F 初始只有「前往 1F 寵物咖啡廳」；點擊後整頁切為 1F、地圖標籤
    為「1F 寵物咖啡廳地圖」，且只剩「返回 3F 租屋樓」；返回後完整恢復 3F。兩個狀態的 tab
    數量都為 0。
- **硬規則／影響面**:未修改 `App.vue`、`map.ts`、`floorScene.ts`、模擬、平衡快照、AI、戀愛或
  消毒邏輯；既有 3F／1F viewport 座標不變。
- **版本狀態**:`main`,基準 `2b1b37c`;本批已完成待提交、尚未 push。

### 2026-08-01 — CAFE-05 咖啡廳分區底色與獨立 LimeZu atlas(Codex)

- **目的／範圍**:延續 CAFE-03 的六個一樓 Region 與 CAFE-04 的樓梯斷面，讓 1F 寵物咖啡廳
  在尚未擺入家具前也能從地板辨識吧台、寵物互動區、後場與入口；同時建立不污染既有家具 atlas
  的咖啡廳專用美術資產，供下一批 CAFE-06 catalog 接線。
- **實際變更**:
  - `src/floor/floorScene.ts`:只擴充 `FLOOR_TINT`，為 `stairs`、`cafe_floor`、`cafe_counter`、
    `cafe_pet`、`cafe_back`、`cafe_entrance` 加入六組暖木、磚紅、鼠尾草綠、後場灰與入口棕色；
    未改地圖矩形、viewport、尋路或模擬資料。
  - 新增 `public/assets/limezu/cafe.png`（144×64）與 `LIMEZU_CAFE_ATLAS_URL`／13 個 frame 合約。
    素材取自專案既有 LimeZu Ice Cream Shop singles，另加入 Kitchen espresso machine；包含菜單、
    招牌、三種展示櫃、收銀台、咖啡機、桌椅、吧台、飲料站與甜筒站。
  - `scripts/limezu-manifest.json` 記錄每個來源檔、裁切框與 atlas 座標；`build-limezu-atlas.py`
    新增來源／邊界／重疊檢查，並在像素未變時保留既有 PNG 原始位元，避免不同 Pillow 壓縮版本
    無意改寫 `furniture.png`、`floors.png`、`walls.png`。
  - `scripts/limezu-art-test.ts` 擴為 87 項，驗證 cafe atlas 尺寸、ID、frame、來源白名單、無重疊、
    邊界與六個 Region tint；本批只建立 atlas 合約，尚未提前加入 CAFE-06 家具 catalog 或 runtime preload。
- **可重現建置**:在相同建置容器連跑 atlas builder 兩次，四張 PNG 的 SHA-256 皆完全一致：
  `furniture.png`=`d64359a5f94a42c09046dc646be0fc777f9930504f7af65166268d99273114ea`、
  `floors.png`=`9b79aabdde91e98f99a447415e7b4ab9b014c1b29754cf0149be2d01621b42e7`、
  `walls.png`=`680a267d323997546370284eab449828de99f7f12200f9952e0d1eb39f182d35`、
  `cafe.png`=`2fc50f8b8a616753f1928e6ad93e1cb479df798a1682465cacd17c594a9cb292`；
  既有三張 atlas 無 working-tree 差異。
- **驗證結果**:
  - `npx tsx scripts/limezu-art-test.ts`:87/87；`npm test`:71/71 全綠（含 `balance-test`）。
  - `npm run typecheck`:app／Worker 通過；`npm run build`:138 modules transformed，僅既有 500 kB
    chunk 提示。
  - workspace root `npm run ui:shot -- rent`:390×844、412×915、430×932 共 18 張，`report.json`
    0 errors／0 warnings；逐張檢查樓層、動態、收支頁，無裁切或版面退化。
  - in-app Browser 實際由 3F 按鈕切入 1F：六個區域底色清楚分離、地圖唯一且返回按鈕唯一；
    返回後 3F 標題恢復，browser error log 為 0。
- **硬規則／後續**:未修改 `map.ts`、家具 catalog、初始擺設、人物、模擬、平衡、AI、戀愛或
  消毒邏輯，也未改既有三張 atlas。下一項 CAFE-06 才會將 cafe frames 接成可擺放的咖啡廳家具。
- **版本狀態**:`main`,基準 `dff61c5`;本批已完成待提交、尚未 push。

### 2026-08-01 — CAFE-06 咖啡廳家具 catalog、atlas runtime 與公開上線閘門(Codex)

- **目的／範圍**:接續 CAFE-05，把獨立 `cafe.png` 從靜態資產接成可購買、可跨頁擺進 1F 的
  咖啡廳家具；每件保留程序 recipe，確保圖片缺失或 Canvas 繪製失敗時仍有完整 fallback。
- **家具資料**:
  - `src/furniture/catalog.ts` 只在陣列尾端 append 12 件：點餐吧台、商用濃縮咖啡機、甜點展示櫃、
    手寫菜單板、小圓桌、正／側面椅、大型貓跳台、寵物軟墊、後場貨架、進貨木箱與大型冷藏櫃。
  - 每件都有明確 `tier`、正價格、非空 recipe、footprint／互動點與敘事鉤子；全部沿用
    `placement: "communal"` 才能放進 cafe region，但 `unlocksStates=[]`、`social=false`，不提前讓
    既有租客在 CAFE-20 閘門前改道下樓。
- **atlas runtime**:
  - `src/art/limezu.ts` 為 13 個 cafe frame 補齊 1:1 `dx/dy`，新增獨立、非阻塞、共用 promise 的
    `preloadLimezuCafeAtlas()`；既有 `tryDrawLimezuFurniture()` 會依 id 選家具或 cafe atlas，兩張
    狀態與 warning 彼此隔離。載入／繪製失敗都回傳 false，讓 catalog recipe 接手。
  - `src/main.ts` 只多呼叫一個非阻塞 preload；未修改 `render.ts` 的 dispatch，既有家具路徑不變。
- **測試／驗證**:
  - `data-catalog-test.ts`:49/49，新增 12 件存在／唯一、明確 tier／正價格／recipe 與不提前解鎖租客。
  - `furniture-art-test.ts`:164/164，12 件 fallback 都有整數像素輸出、四向 transform 與畫框邊界；
    另鎖定吧台收銀區與冷藏櫃雙門辨識特徵。
  - `limezu-art-test.ts`:94/94，新增 Node 無 Image、並行 preload、1:1 source/destination、draw failure
    recipe fallback、load failure warn-once；既有三張 atlas 與 cafe manifest 檢查仍全綠。
  - `npm test`:71/71 全綠；獨立 `balance-test` 快照一致；`npm run typecheck` app／Worker 通過；
    `npm run build`:138 modules transformed，僅既有 500 kB chunk 提示。
  - workspace root `npm run ui:shot -- rent`:390×844、412×915、430×932 共 18 張，`report.json`
    0 errors／0 warnings；逐張檢查樓層、動態、收支均無裁切或退化。
  - in-app Browser 實際驗證：商店能看到全部 12 件；購買 $16,000 點餐吧台後仍保持擺放模式切到
    1F，在 `cafe_counter` 區預覽／確認成功，金額 87,534→71,534，正式畫面顯示 atlas 像素，
    browser error log 為 0。
- **上線決策與文件修正**:
  - CAFE-06 完成後只適合內部預覽：空間、商店與家具成立，但尚無顧客、認養或營運閉環。
  - 公開版建議做到 CAFE-01～15，另提前納入 CAFE-22，最後由 CAFE-00 逐 hook 驗證；這樣玩家才
    能從 UI 完成開張、補貨、日結、投資與一次真實可達的顧客認養。CAFE-16～21 可延後。
  - `docs/一樓寵物咖啡廳-設計.md` 與工作分解同步補上 `openCafe()`、開張按鈕、認養接受／婉拒、
    CAFE-22 寵物可達性及完整端到端公開閘門，避免只靠 grep／單元測試就誤判可上線。
- **硬規則／限制**:未修改任何既有家具欄位、`render.ts`、`tier.ts`、`map.ts`、
  `INITIAL_PLACEMENTS`、`COMMUNAL_AREA_IDS`、模擬、平衡快照或任何 PNG。家具目前由共用商店購買；
  真正的咖啡廳營運設備效果仍必須在 CAFE-14 走 `state.cafe.upgrades`，不能把 placement 當營運狀態。
- **版本狀態**:`main`,基準 `59a1585`;本批已完成待提交、尚未 push／deploy。

### 2026-08-01 — CAFE-07 獨立顧客資料層：姓名池、決定性外觀與純函式離場(Codex)

- **目的／範圍**:接續公開 MVP 的顧客線，先建立不會被收租、社交、日記等租客 pass 誤處理的
  `CafeGuest` 輕量資料層。本批只做型別、內容與純函式，不接 UI、走位或全域 cafe state。
- **實際變更**:
  - `src/types.ts`:新增 `CafeGuestIntent`（`coffee`／`adopt`／`rent`）與 `CafeGuest`；欄位包含
    id、姓名、完整 `Appearance`、抵達／離開時間及選填座位格，沒有 Tenant 的財務、關係或劇情狀態。
  - `src/content/cafeGuestNames.ts`:新增 32 筆獨立姓名，全部不與 `recruit.ts` 的 20 筆
    `NAME_IDENTITIES` 重疊；採 append-only 順序，避免後續相同 seed 換名字。
  - `src/sim/cafeGuests.ts`:新增 FNV-1a 決定性雜湊、`generateCafeGuest()`、cap 6 的
    `appendCafeGuest()`、指定離場與到期離場純函式。相同 seed／sequence／抵達時間會得到相同姓名、
    外觀、意圖與 1～3 遊戲小時停留期；不讀寫 `state`，也不呼叫 `Math.random()`。
  - `scripts/cafe-guest-test.ts` 新增 24 項斷言並登錄 `run-all.ts` 正式回歸集：姓名唯一／不撞租客、
    生成決定性、欄位白名單、三種意圖、cap 6、輸入與陣列不變、到點離場、零 RNG 與
    `state.runtimes` 零污染。
- **設計文件校正**:
  - 原設計以 `randomAppearance()` 表示重用人物外觀，但該函式實際會消耗 6～7 次 `Math.random()`，
    與 CAFE-07 的零 RNG 驗收衝突。設計與工作分解已改成「沿用同一批外觀白名單，以顧客 seed
    決定性選取」；未修改 `parts.ts` 的既有色池、順序或租客生成行為。
  - CAFE-19 後續把顧客轉成租屋應徵者時，應沿用 `CafeGuest.appearance`，不重新抽一次外觀。
- **驗證結果**:
  - `npx tsx scripts/cafe-guest-test.ts`:24/24；`npm test`:72/72（含新增 regression 與 sim-trace）。
  - `npx tsx scripts/balance-test.ts`:快照一致；`npm run typecheck`:app／Worker 通過。
  - `npm run build`:138 modules transformed，成功；僅既有大於 500 kB chunk 提示。
  - 本批沒有任何 UI 或繪圖接線，因此未重跑 UI Lab；下一項 CAFE-08 接顧客走位與繪製時再做
    三種手機尺寸與 1F 實際畫面驗收。
- **硬規則／後續**:未修改 `state.runtimes`、`NAME_IDENTITIES`、`gameState.ts`、存檔、地圖、
  `COMMUNAL_AREA_IDS`、平衡快照、AI、戀愛或既有外觀白名單。CAFE-11 建立 `state.cafe` 前，顧客
  只存在於純函式回傳值；下一項 CAFE-08 才會建立獨立 guest agent 走位與繪製。
- **版本狀態**:`main`,基準 `4b5a43f`;本批已完成待提交、尚未 push／deploy。

### 2026-08-01 — CAFE-08 顧客進出走位與獨立人物繪製(Codex)

- **目的／範圍**:接續 CAFE-07 的純 `CafeGuest` 資料，完成獨立於租客 runtime 的進場、入座、
  離場與人物繪製。本批不提前建立 `state.cafe`，也不在未開張的正式 1F 頁面硬塞示範顧客。
- **實際變更**:
  - `src/floor/guestAgents.ts`:新增 `GuestAgent` 顯示期生命週期
    `entering → seated → leaving → departed`。顧客由兩格 `cafe_entrance` 決定性進場，以 BFS 前往
    未被家具阻擋且未被其他顧客占用的座位；到 `leavesMs` 後走回入口才隱藏／移除。保留中央主走道，
    六位同時進場也不互卡，全程不呼叫 `Math.random()`。
  - `src/floor/floorScene.ts`:在既有動態物件 y-sort 中加入選填的第八個 `guests` 參數與
    `drawGuest()`；直接使用 `CafeGuest.appearance` 快照，行走時支援四方向 sprite，入座時使用坐姿，
    不查租客 registry、不建立假 `TenantRuntime`。
  - `scripts/cafe-guest-agent-test.ts`:新增 17 項斷言，涵蓋決定性排序與座位、家具阻擋 fallback、
    六客同時入座、到期走回入口、畫面混排、隱藏顧客不繪製及零 RNG；並登錄 `run-all.ts`。
  - `scripts/render-floor.ts`:新增選填 `--cafe-guests`，可離線生成三位已入座顧客的 1F 長圖，
    不改正式遊戲狀態。
- **設計文件校正**:原設計的「到期直接從陣列移除」會讓人物原地消失，已改為到期先進入
  `leaving`、抵達入口後才移除。CAFE-11 建立 `state.cafe`、CAFE-13 產生客流後才接正式 `FloorMap`；
  咖啡廳未開張時維持零顧客。
- **驗證結果**:
  - `npx tsx scripts/cafe-guest-agent-test.ts`:17/17；`npm test`:73/73（含 sim-trace）。
  - `npx tsx scripts/cafe-guest-test.ts`:24/24；`appearance-test.ts`:38/38；`roomcam-test.ts`:10/10。
  - `npx tsx scripts/balance-test.ts`:快照一致；`npm run typecheck`:app／Worker 通過；
    `npm run build`:138 modules transformed，成功，僅既有大於 500 kB chunk 提示。
  - `npm run ui:shot -- rent`:390×844、412×915、430×932 共 18 張，report 0 errors／0 warnings；
    已逐張檢查，樓層、動態、收支畫面無裁切或回歸。
  - 離線 `render-floor --cafe-guests` 長圖可見三位顧客分散入座；互動瀏覽器實測
    3F → 1F → 3F 按鈕切頁正常，1F 未開張不生成假顧客，console 0 errors。
- **硬規則／後續**:未修改 `src/floor/agents.ts`、`petAgents.ts`、`groupScene.ts`、
  `gameState.ts`、`state.runtimes`、存檔或平衡快照。CAFE-08 已完成訪客 NPC 的資料、走位與繪製
  抽象；正式生成接線留給 CAFE-11／13。依公開 MVP 序列，下一項為 CAFE-09 認養意圖接既有送養。
- **版本狀態**:`main`,基準 `c5288f8`;本批已完成待提交、尚未 push／deploy。

### 2026-08-01 — CAFE-09 認養意圖接入幸福新家(Codex)

- **目的／範圍**:讓 `intent === "adopt"` 的咖啡廳顧客能安全接到既有樓寵物幸福新家流程。
  本批只建立 sim 命令與驗證，不提前加入 `state.cafe`、Vue 抉擇面板或寵物下樓行為。
- **實際變更**:
  - `src/sim/pets.ts`:新增 `CAFE_GUEST_ADOPTION_DESTINATION = "咖啡廳客人領養"` 與
    `acceptCafeGuestAdoption()`。入口只接受資料完整的 adopt 顧客，以及尚未媒合的永久樓寵物；
    非認養顧客、房客寵物、foster／partner foster、未知 id 均零變更拒絕。
  - 接受後直接重用未修改的 `finishRehoming()`，沿用刪除寵物、清除 pair／cooldown、名冊 cap 與
    單次通知；再把該次新名冊項目的 id 改成穩定 `cafe:<guestId>`、destination 改成咖啡廳去向，
    補上顧客與寵物相遇文案並立即 `save()`。同一顧客重複點擊不能領走第二隻。
  - `scripts/cafe-adoption-test.ts`:新增 17 項斷言並登錄 `run-all.ts`，涵蓋成功快照、立即退場、
    destination、穩定 id、存檔、清理引用、所有拒絕分支、顧客輸入不變、runtime 零污染與零 RNG。
- **驗證結果**:
  - `npx tsx scripts/cafe-adoption-test.ts`:17/17；`npx tsx scripts/pet-test.ts`:90/90；
    `npx tsx scripts/cafe-guest-test.ts`:24/24。
  - `npm test`:74/74（含新增 regression 與 sim-trace）；`npx tsx scripts/balance-test.ts`:快照一致。
  - `npm run typecheck`:app／Worker 通過；`npm run build`:138 modules transformed，成功，僅既有
    大於 500 kB chunk 提示。本批無 UI 變更，因此未重跑 Mobile UI Lab。
- **硬規則／後續**:未修改 `beginRehoming()`、`finishRehoming()`、`rehomeDurationMs`、
  `PetHomeEntry`／`PetRehomingDestination` 型別、game state、存檔 schema、Vue、寵物 agent 或平衡快照。
  CAFE-15 才提供接受／婉拒操作；CAFE-22 讓樓寵物實際進入 `cafe_pet`，公開端到端驗收不能以
  本批直接呼叫函式取代。依序下一項為 CAFE-10 顧客意圖泡泡。
- **版本狀態**:`main`,基準 `4ce7f2c`;本批已完成待提交、尚未 push／deploy。

### 2026-08-01 — CAFE-10 三種顧客意圖泡泡與 atlas fallback(Codex)

- **目的／範圍**:讓已入座的咖啡廳顧客以直觀圖示表達 `coffee`／`adopt`／`rent`，同時維持
  CAFE-08 的正式客流閘門；本批只新增渲染能力，不提前建立 `state.cafe` 或在 1F 塞入假顧客。
- **美術與 runtime**:
  - `scripts/limezu-manifest.json` 與 `public/assets/limezu/cafe.png`:從既有授權的
    `UI_thinking_emotes_animation_16x16.png` 裁入咖啡、愛心、信封各兩幀，append 在 y=64；atlas
    由 144×64 擴為 144×80，原有 13 個咖啡廳家具 frame 的像素位置不變。
  - `src/art/limezu.ts`:在既有 cafe atlas preload 上新增六個 frame 合約與 1:1 繪製入口；每 500ms
    依 frame 偶／奇切換，形成約 1 秒循環。載入前或 `drawImage` 失敗時回傳 false 並沿用 cafe
    warn-once，交由場景畫程序 fallback。
  - `src/floor/floorScene.ts`:只對 `seated`、未移動且可見的 `GuestAgent` 畫泡泡，位置在頭頂，並在
    人物／家具 y-sort 後的資訊層輸出；程序 fallback 分別使用棕色咖啡杯、紅色愛心與藍色信封。
    未修改人物 sprite、`src/pixel/*`、模擬狀態或顧客資料。
- **驗證與工具**:
  - 新增 `scripts/cafe-intent-bubble-test.ts` 並登錄正式回歸：12/12，涵蓋三圖示、顯示守門、
    `composeFloor` 整合、輸入不變與零 RNG；`limezu-art-test.ts` 擴為 99/99，驗證 manifest、atlas
    邊界／不重疊、兩幀來源、1:1 繪製與失敗 fallback。
  - `render-floor --cafe-guests` 固定輸出三種 intent，768×2496 離線圖已目視確認三位入座顧客的
    咖啡杯／愛心／信封都清楚；`npm test`:75/75，`balance-test` 快照一致，`typecheck` app／Worker
    通過，`build`:138 modules transformed，只有既有大於 500 kB chunk 提示。
  - Mobile UI Lab 390×844、412×915、430×932 共 18 張，report 0 errors／0 warnings 並逐張檢查；
    in-app Browser 實測 3F → 1F → 3F 的單一目的地按鈕正常，未開張 1F 維持零假顧客，console 0 errors。
- **已知限制／後續**:正式顧客與泡泡要等 CAFE-11 建立 `state.cafe`、CAFE-13 接客流後才在公開
  頁面出現。下一項依序為 CAFE-11 state.cafe schema。
- **版本狀態**:`main`,基準 `4e0da33`;本批已完成待提交、尚未 push／deploy。

### 2026-08-01 — CAFE-11 `state.cafe` schema 與顧客接線:存檔、tick pass、FloorMap 上線(Claude Code)

- **目的／需求**:CAFE-07~10 把顧客系統做成「接收陣列的純函式,刻意不接 state」,
  `FloorMap.vue` 對顧客零引用。本批建立 `state.cafe`、接上存檔,並把顧客系統真正接線到
  模擬與畫面上,讓 1F 從「有渲染能力但沒有資料來源」變成完整迴圈。
- **`state.cafe` 的完整形狀(設計文件 §8,一次定義完整,後續分期只填內容不改 schema)**:

  ```ts
  state.cafe = {
    open: false,          // 玩家花錢開張才 true(開張動作 CAFE-14、UI CAFE-15)
    standingOrders: {},   // Record<原料 id, 常備量>(CAFE-12)
    stock: {},            // Record<原料 id, 數量>(CAFE-12)
    research: null,       // { id, startedDay, days, invested } | null(CAFE-16)
    completed: [],        // string[](CAFE-16)
    upgrades: [],         // string[];刻意不進 placements,免得咖啡機被搬到三樓(CAFE-14)
    guests: [],           // CafeGuest[],cap CAFE_GUEST_CAP = 6 ← 本批唯一實際讀寫的欄位
    popularity: 0,        // (CAFE-13)
    history: [],          // CafeDayRecord[],cap CAFE_HISTORY_CAP = 60(CAFE-13)
  };
  ```

- **存檔:additive 選填欄位,`SAVE_VERSION` 仍為 6**(慣例見 `persistence.ts` 的 `floorChain`
  區塊)。save 端多存一個 top-level `cafe`;load 端 `sanitizeCafeState(s.cafe, state.gameMs)`
  逐欄正規化(比照 `sanitizeGrowthTags` 在 load 時的角色):壞顧客整筆丟掉、髮型/配件走既有白名單、
  顏色過 `sanitizeAppearanceInPlace`、`history` 取最後 60 筆、`guests` 去重後套 cap。
  **`MIGRATIONS` 一行未動**,舊檔沒有 `cafe` 就直接拿 `defaultCafe()`。
- **`guests` 入存檔的決定與理由**:**入存檔**。理由是「重開遊戲後客人還在店裡」比「每次都空店」
  更符合掛機觀察的體感;而 `leavesMs` 是遊戲時間,離線久了本來就該走光——所以 load 當下立刻用
  `removeDepartedCafeGuests(guests, state.gameMs)` 精確過濾。這樣「離線幾分鐘 → 客人還在」與
  「離線一整天 → 店裡是空的」是同一條規則的兩個端點,不需要兩套行為,也不必為了「載入要不要清空」
  另外設旗標。
- **顧客生成/離場 pass**:`cafeGuestPass(hour)` 加在 `tick.ts`(同 `roomVisitPass`／
  `dirtyComplaintPass` 的擺法),呼叫點在 `hourlyTick` 每租客迴圈之後、緊接 `outingEncounterPass`,
  比照 `dreamPass` 的位置。營業時段 10~20 點,每小時以 `cafeGuestHash("cafe-arrival|日|時") % 100`
  決定是否來客(門檻 `CAFE_ARRIVAL_CHANCE = 55`),滿 `CAFE_GUEST_CAP` 就不再收。
  **本批刻意不做客流公式**(招牌等級 × 天氣 × 星期是 CAFE-13 的事)。
- **零漂移是靠 `open` 這個天然閘門**:pass 的第一行就是 `if (!state.cafe.open) return;`。
  開張要玩家花錢,無頭的 balance 快照局永遠不會開張 ⇒ 整個 pass 連碰都碰不到,比
  `DREAM_FIRST_DAY`／`OUTING_FIRST_DAY` 那種日數閘門更乾淨。另外兩條同時成立:
  **零新增 `Math.random()`**(來客與否走 `cafeGuestHash`,顧客內容走 `generateCafeGuest` 既有的
  決定性雜湊)、**完全不碰 `state.runtimes`**(顧客是平行輕量體,收租/社交/日記/成就全數不受影響)。
- **離場的寬限期(`CAFE_GUEST_LINGER_MS = 2 遊戲小時`)**:UI Lab 自訂流程實測時發現,若 pass 一到
  `leavesMs` 就把顧客從 state 移除,`FloorMap` 那段「走回 `cafe_entrance` 才消失」的演出永遠不會發生
  (顧客會原地 pop 掉),違反設計文件 §6.2。改成寬限期內交給畫面演、超過寬限期(或離線補進度一次跑掉
  很多小時)才由資料層當保險絲收乾淨——玩家整場沒打開 1F 頁面也不會累積過期顧客塞滿 cap。
  load 端沒有進行中的演出,所以維持精確過濾、不套寬限期。
- **Render 接線**:`FloorMap.vue` 以 `guestAgentSignature(state.cafe.guests)` 判斷重建、
  每幀 `tickGuestAgents(…, state.gameMs, vacuumBlocked)`、把 `guestAgents` 傳進 `composeFloor`
  的第 8 個參數,並用 `departedGuestIds()` + `removeCafeGuest()` 把走到門口的顧客移出 state。
  只加顧客接線,樓層切換器與既有繪製邏輯一行未動。
- **驗證結果**:
  - 新增 `scripts/cafe-state-test.ts` 並登錄 `run-all.ts` 的 `REGRESSION`:**48/48**。涵蓋 schema
    預設值、`SAVE_VERSION === 6` 硬斷言、未開張時 pass 零狀態變更、開張後 72 小時零 `Math.random`、
    兩次跑同起點逐欄相同、只在營業時段來客、cap 生效、寬限期前後的離場行為、存檔往返、
    離線過久自動清空、舊檔(無 `cafe`)載入不報錯拿預設值、`sanitizeCafeState` 的各種壞資料護欄,
    以及 **`state.runtimes` key 集合零污染**。
  - `npx tsx scripts/balance-test.ts`(未 `--update`):快照一致;`git diff main -- scripts/balance-snapshot.json` 為空。
  - `npm test`:**76/76**(含新增 regression 與 sim-trace);`npm run typecheck` app／Worker 通過;
    `npm run build`:141 modules transformed,僅既有大於 500 kB chunk 提示。
  - Mobile UI Lab 390×844／412×915／430×932 共 18 張,`report.json` 0 errors／0 warnings 並逐張檢查,
    3F 與收支頁零回歸。另以容器內 Playwright 自訂流程(跑完即刪、不進 commit)注入一份開張存檔,
    切到 1F 目視確認四位顧客各自走到座位、入座後才顯示咖啡杯／愛心／信封三種意圖泡泡,console 0 errors。
- **已知限制／後續**:本批**沒做**客流公式(CAFE-13)、開張動作與價格(CAFE-14)、咖啡廳面板 UI
  (CAFE-15)、常備訂單與庫存(CAFE-12)、研發(CAFE-16);`standingOrders`／`stock`／`research`／
  `completed`／`upgrades`／`popularity`／`history` 七個欄位本批只定義形狀、不被任何程式寫入。
  未開張時玩家在 1F 仍然看不到任何顧客,這是刻意的。未成年排除戀愛線、成人內容預設關、
  AI 輸出白名單/夾值/消毒等硬規則全部保留。
- **版本狀態**:branch `feat/cafe-11-state`,基準 `a066576`;未 push、未 merge、未部署。

### 2026-08-01 — CAFE-12 常備訂單與庫存:原料資料與補貨／消耗／損耗純函式(Claude Code)

- **目的／需求**:設計文件 §5.2 的常備訂單迴圈。**本批刻意只做純函式與資料,不接 tick**——
  沿用 CAFE-07~10 那條被驗證過的切法:先做成「接收參數的純函式」,接線留給下一項。
- **工作方式變更(使用者裁示)**:兩個 Agent 從本批起**直接共用 `main`,不再開 feature branch**,
  改靠工作日誌追溯。開工前 `git status --short` 為空;本批只碰下列四個檔案,全數落在
  `coordination/tasks/cafe-12-standing-orders.md` 的允許範圍內。

#### 一、`src/content/cafeIngredients.ts`(新增)— 原料資料

純資料、無函式,風格比照 `observationLines.ts` / `cafeGuestNames.ts`。**六種原料**:

| id | 名稱 | 單價 | 生鮮 | 分支 | 建議常備量 | perGuest |
|---|---|---|---|---|---|---|
| `coffee_bean` | 咖啡豆 | $15 | ✗ 乾貨 | 沖煮 | 20 | 0.40 |
| `milk` | 牛奶 | $12 | ✓ 生鮮 | 沖煮 | 15 | 0.30 |
| `flour` | 麵粉 | $8 | ✗ 乾貨 | 烘焙 | 10 | 0.13 |
| `butter` | 奶油 | $18 | ✓ 生鮮 | 烘焙 | 8 | 0.08 |
| `cat_can` | 貓罐頭 | $25 | ✗ 乾貨 | 寵物餐點 | 8 | 0.10 |
| `pet_fresh` | 寵物鮮食 | $20 | ✓ 生鮮 | 寵物餐點 | 6 | 0.06 |

- **為什麼從文件的四項增為六項**:原文四項裡**只有牛奶是生鮮**,「只有生鮮會損耗」這條規則
  會退化成單一原料的特例,玩家感知不到、也測不動。補成 **三鮮三乾**、且對齊 §5.3 研發樹的
  三條主幹(沖煮／烘焙／寵物餐點)**各配一乾一鮮**之後,沒有任何一條研發線可以完全免疫缺貨,
  也沒有任何一條會被損耗整批掃掉。
- **定價**:文件舉例的四項**單價與常備量原封不動**($15/$12/$8/$25、20/15/10/8),免得跟已拍板的
  範例表對不上(測試有硬斷言把關)。兩個新項取在既有價帶 $8–$25 之內:奶油 $18(烘焙裡最貴的料)、
  寵物鮮食 $20(比常溫罐頭便宜,因為它會壞——玩家本來就該有理由不囤它)。
- ⚠️ **貓罐頭刻意歸類為乾貨**:罐頭的存在意義就是常溫保存。寵物線的生鮮由「寵物鮮食」承擔,
  不讓寵物餐點整條免疫損耗。
- `perGuest` 的取值讓 **30 位客人時的消耗量剛好重現設計文件 §5.2 範例表的「今日消耗」欄**
  (咖啡豆 12 / 牛奶 9 / 麵粉 4 / 貓罐頭 3),不是憑空猜的數字。
- 陣列**只能 append,不可插入或重排**:錢不夠時的配額依宣告順序進行,重排等於靜默改平衡。

#### 二、`src/sim/cafe.ts`(新增)— 四個純函式

```ts
restockPlan(standingOrders, stock, money) → RestockPlan
consumeStock(stock, demand)               → ConsumeResult
applySpoilage(stock, options?)            → SpoilageResult
dailyDemand(guestCount)                   → Record<原料 id, 需求量>   // CAFE-13 的橋接點
```

**1. 補貨 `restockPlan`——錢不夠時「只補得起多少補多少」的具體行為**

不是單輪貪婪,是**兩段式配額**(旋鈕 `RESTOCK_RESERVE_RATIO = 0.5`):

1. 預算 = `max(0, floor(money))`;
2. **第一輪(保底)**:照宣告序,每種原料先補到 `ceil(常備量 × 0.5)` 的水位;
3. **第二輪(補滿)**:再照同一順序把剩下的補滿;
4. 每一步只買 `floor(剩餘預算 / 單價)` 買得起的整數量。

為什麼要有保底輪:**單輪貪婪會讓清單末尾的原料整批掛零**(該品項當日完全缺貨,而且永遠是同幾項倒楣);
兩段式讓錢不夠時缺的是「廣度」而不是「深度」,才符合「不是懲罰,是敘事」。實測(建議常備量、空庫存):

```
整櫃成本 1024;持有 $500 → 花 498、剩 2
  咖啡豆 10/20  牛奶 8/15  麵粉 5/10  奶油 4/8  貓罐頭 4/8  寵物鮮食 2/6   ← 每種都補到一些
持有 $600 → 花 593、剩 7(保底輪 518 全額跑完,餘額拿去補咖啡豆)
持有 $1024 → 全補滿、剩 0(邊界)
```

回傳 `underfunded` / `missingUnits` / `fulfillment` 三個欄位,就是為了讓 **CAFE-13 能推一則
「今天只補得起七成」的日誌**。保證:`totalCost <= money`、`moneyAfter >= 0`、庫存不變負。

**2. 消耗 `consumeStock`——缺貨的後果**

逐項 `used = min(庫存, 需求)`,庫存不變負。缺貨只有兩個後果,而且都只是回傳值:

- `shortages`(缺貨原料 id,照宣告序)→ CAFE-13 據此推「客人撲空」日誌;
- `crowdMultiplier = max(0.7, 1 − 缺貨種數 × 0.08)` → CAFE-13 據此打折當日客流。

**數值選擇的理由**:每缺一種扣 8%,但 `CROWD_MULTIPLIER_FLOOR = 0.7` 夾底——
六種原料全空也只掉三成。缺貨是「今天生意差一點」,不是失敗狀態。沒有罰款、沒有連鎖懲罰。

**3. 損耗 `applySpoilage`——只有生鮮會壞**

```
損耗 = floor(max(0, 庫存 − SPOILAGE_FREE_UNITS) × SPOILAGE_RATE)   // 6 與 0.1
```

乾貨(`perishable: false`)完全跳過,**一單位都不會壞**(設計文件 §5.2 明訂)。

**為什麼要有免損耗額度 6**:它造出一個刻意的**下界**——當「庫存 − 6 < 10」時 `floor` 讓損耗歸 0,
庫存停住不再減少。三個好處:

- **照設計文件 §5.2 的常備量玩,損耗恆為 0**(牛奶 15、奶油 8、寵物鮮食 6 全在門檻內)。
  也就是說「設一次就三個月不管」的玩家**一單位都不會壞**;只有刻意囤到 16 以上才開始每天壞一點點。
- 反覆套用會**收斂到 15 並停住,永遠不會把櫃子清空**:`30 → 28 → 26 → 24 → 23 → … → 16 → 15 → 15 → …`。
- 這條下界同時是**誤呼叫的保險絲**。`applySpoilage` 是「每日」函式不是「載入」函式,連套兩次 = 過了兩天
  (第二次從更小的基數再壞一次,這是正確語意不是 bug);但**即使有人誤接到載入流程**,庫存也只會收斂到 15
  而不會每讀一次檔就少一次直到歸零——這正是 AI 色碼那批踩過的坑,這次用數學把它擋死,測試有 200 次
  連續套用的收斂斷言。
- `options.freeUnits` / `options.rate` 留給 CAFE-14 的「大型冷藏」投資項調低損耗,**本項不實作投資**。

`wastedValue` 只給日誌措辭用,**本項不扣款**。

#### 三、🔴 刻意不做的事,以及留給誰

| 刻意不做 | 留給 |
|---|---|
| **扣款**(`addMoney`)。`restockPlan` 只回傳「應該扣多少」 | **CAFE-13** 日結時統一做 |
| **推日誌**。缺貨／補不滿／損耗只是回傳值上的欄位 | **CAFE-13** 決定措辭與時機 |
| **接 `tick.ts`**、日結 pass、客流公式、天氣／星期係數 | **CAFE-13** |
| **開張動作 `openCafe()` 與 5 個投資項**(含大型冷藏調損耗) | **CAFE-14** |
| **常備量設定 UI、缺貨警告、庫存顯示** | **CAFE-15** |
| 研發樹對菜單／客單價的影響 | CAFE-16 |

`src/sim/tick.ts`、`src/sim/economy.ts`、`src/sim/gameState.ts`、`SAVE_VERSION`、`MIGRATIONS`
**一行未動**。`state.cafe.standingOrders` / `stock` 兩個欄位本批仍然沒有任何程式寫入——
CAFE-11 定好形狀、本批做好算法、CAFE-13 才會把兩者接起來。

#### 四、零 RNG 與零漂移怎麼保證

- **零 RNG**:全檔沒有 `Math.random()`,也不需要雜湊——補貨、消耗、損耗全是決定性算術。
  測試除了「呼叫期間覆寫 `Math.random` 計數 === 0」之外,**另外剝掉註解後直接掃 `cafe.ts` 程式碼本體**,
  硬斷言不含 `Math.random` / `addMoney`,也不 import `gameState` / `tick` / `economy`。
- **零漂移**:本檔零副作用——不 import `state`、不寫任何全域、不呼叫 `addMoney`、不掛任何 pass。
  沒有東西會在快照局裡被執行,所以零漂移是**結構上必然**而不是調參調出來的。
- **浮點陷阱**:IEEE754 下 `0.4 × 30 === 12.000000000000002`,直接 `ceil` 會得到 13,
  §5.2 的範例表就對不上了。加了 `quantize()`(先 `toFixed(6)` 再取整)並在測試裡硬斷言 30 人 = 12/9/4/3。
- **迭代次序**:所有輸出一律照 `CAFE_INGREDIENTS` 宣告序,**不用 `Object.keys()` 的插入序**
  (存檔往返、玩家改常備量都會改變插入序,那是不穩定的)。

#### 五、驗證結果

- 新增 `scripts/cafe-supply-test.ts` 並**登錄 `run-all.ts` 的 `REGRESSION`**:**73/73 通過**。
  涵蓋:零 RNG(計數 + 原始碼掃描)、同輸入連跑兩次逐欄相同、錢不夠只補得起多少補多少、
  **預算 0～1200 逐元全掃**不超支／不負庫存／不超買／金額自洽、錢剛好 1024 的邊界、
  生鮮會壞乾貨不會、缺貨判定與 0.7 下界、`applySpoilage` 連續套用兩次與 200 次收斂、
  空的 `standingOrders`(玩家還沒設)不報錯、未知原料 id／負數／NaN／小數的壞資料防護、
  三個純函式都不修改輸入物件。
- `npx tsx scripts/balance-test.ts`(**未 `--update`**):快照一致;
  `git diff -- scripts/balance-snapshot.json` **為空**。
- `npm test`:**77/77**(76 → 77,新增本批一支);`npm run typecheck` app／Worker 皆通過;
  `npm run build`:141 modules transformed,只有既有的 >500 kB chunk 提示。
- 本批**沒有 UI 變更**,故未跑 `ui:shot`。
- 未成年排除戀愛線、成人內容預設關、AI 輸出白名單/夾值/消毒等硬規則全部保留(本批未觸及)。

- **已知限制／後續**:純函式尚無任何呼叫端,玩家在遊戲中還看不到任何效果——這是刻意的。
  下一項為 **CAFE-13 日結 pass 與客流公式**:掛在 `tick.ts` 換日區塊 `collectRent()` 正後方,
  呼叫 `dailyDemand()` → `consumeStock()` → `restockPlan()` → `applySpoilage()`,
  用 `addMoney(-totalCost, …, "cafe")` 扣款,並把 `underfunded` / `shortages` / `totalSpoiled`
  三個旗標寫成日誌。**`crowdMultiplier` 該乘在當日還是隔日客流,由 CAFE-13 決定**,本批不預設立場。
- **版本狀態**:**直接提交在 `main`**(不開分支、不 push),基準 `53b42c7`;未 push、未部署。

---

### 2026-08-01 — CAFE-13 日結 pass 與客流公式:咖啡廳第一次真的會動(Claude Code)

- **目的／使用者需求**:CAFE-11 接上了顧客、CAFE-12 做好了補貨／消耗／損耗的純函式但**沒有呼叫端**。
  本項把兩者接起來:客流公式(設計文件 §5.5)、日結 pass(掛在換日區塊 `collectRent()` 正後方)、
  以及缺貨／補不滿／損耗三個旗標的敘事日誌(§5.2「不是懲罰,是敘事」)。
  硬性數值目標是 §5.1 的「成熟期日淨利 ≈ 日租金的 30–50%」——**咖啡廳不該比收租賺錢**。

#### 一、實際變更與主要檔案

| 檔案 | 變更 |
|---|---|
| `src/sim/cafe.ts` | 追加四節純算式:設備能力 `cafeCapability()`、客流 `cafeCrowd()`、客單價 `cafeTicketPrice()`／人氣 `nextCafePopularity()`、日結敘事 `cafeDailyLine()`。**CAFE-12 的三個純函式簽名一字未動。** |
| `src/sim/tick.ts` | 新增 `cafeDailyPass()` 與日誌落點 `pushCafeLog()`;換日區塊 `collectRent();` 正後方插入 `cafeDailyPass();` **一行**。 |
| `src/sim/gameState.ts` | `TxnCategory` 加 `"cafe"`(扁平 union,純 additive,既有六項一項未刪)。 |
| `src/components/FinancePanel.vue` | **被迫的一行**:`CAT_LABEL` 是 `Record<TxnCategory, string>`,不補 `cafe: "咖啡廳"` 會直接 typecheck 失敗。只加一個 key,版型與邏輯未動。 |
| `scripts/cafe-daily-test.ts`(新增) | 86 條斷言,並**登錄 `run-all.ts` 的 `REGRESSION`**。 |

**為什麼日結 pass 放在 `tick.ts` 而不是 `cafe.ts`**:`scripts/cafe-supply-test.ts` 有硬斷言掃描
`cafe.ts` 的原始碼本體,要求它零 `Math.random`、零 `addMoney`、不 import `gameState`／`tick`／`economy`。
日結 pass 這三件事都要做。既有的 `cafeGuestPass()` 本來就住在 `tick.ts`
(見 `scripts/cafe-state-test.ts` 的 import),所以沿用同一條界線:**算式一律在 `cafe.ts`、pass 一律在 `tick.ts`**。
兩邊的檔頭註解都寫明了這件事。

#### 二、客流公式與每個係數的選值理由

形狀直接抄 `coinLaundryIncome()`(`economy.ts:27`)的「設備數 × 係數 × 條件數」:

```
基礎客流 = 招牌等級 × 每級客流 × 天氣係數 × 星期係數 × (1 + 人氣加成) × (戶外座位晴天加成)
實際客流 = min(基礎客流, 產能上限)
營收     = 實際成交客數 × 客單價
客單價   = 基礎價 × (1 + Σ 研發加成)      ← 研發加成恆 0,留給 CAFE-16
淨利     = 營收 − 原料成本 − 固定成本
```

| 常數 | 值 | 選值理由 |
|---|---|---|
| `CAFE_CROWD_PER_SIGN_LEVEL` | 22 | **回推出來的,不是拍腦袋**(見第三節的兩點聯立)。招牌 1 級 ≈ 22 人、2 級 ≈ 44 人被產能夾到 40 人,兩點剛好架出 §5.1 的目標帶。 |
| `CAFE_BASE_CAPACITY` | 26 | 刻意**略高於** 22:平日不會撞天花板,只有晴天週末(22×1.15×1.25 ≈ 32)會被夾住 ⇒「尖峰時段流失客人」是玩家真的感覺得到的事,第二台咖啡機才有存在意義。 |
| `CAFE_CAPACITY_PER_MACHINE` | 14 | 26 + 14 = 40,恰好承接招牌 2 級的 44 人。 |
| 天氣係數 | 晴 1.15 / 陰 1.00 / 雨 0.70 / 悶熱 1.05 | **依 `weatherForDay()` 的分佈(晴40/陰25/雨25/悶熱10)加權平均 = 0.99**。天氣是 ±15~30% 的質地,不是長期的系統性加稅。悶熱刻意 **>1**:躲進有冷氣的店裡喝冰的是加分(與「雨天不想出門」不同),也讓「戶外座位」的晴天加成不是唯一的天氣互動。 |
| 星期係數 | 日 1.25 / 一~四 0.90 / 五 1.05 / 六 1.25 | **七日平均 = 1.021**。同樣不是加稅,只是把「平日冷清、週五回溫、週末最旺」這條最容易被感知的節奏做出來。索引 0=週日,對齊 `week.ts` 的 `weekdayOf()`。 |
| `CAFE_POPULARITY_SWING` | 0.25 | `state.cafe.popularity`(CAFE-11 定的欄位,一直沒有人用)在此接上:0→100 對基礎客流加最多 +25%,順利一天 +2、缺貨 −4、只是補不滿 −1。從 0 爬到滿要 50 天,「成熟期」名副其實;夾在 0~100 所以永遠不會失控。 |
| `CAFE_BASE_TICKET` | 36 | 見第三節。與六種原料的每客成本 **$15.78** 一起看:毛利率約 54%。刻意讓原料佔得夠重(約 46% 營收),常備訂單那條機制才有存在感。 |
| `CAFE_FIXED_COST` | 370 | 見第三節。 |

天氣與星期**都用現成的確定性來源**,一個新的隨機源都沒發明:
`weatherForDay()`(`weather.ts:21`,splitmix32)與 `weekdayOf()`(`week.ts:13`,讀日曆)。

#### 三、日淨利 vs 日租金:實算數字

種子局兩位租客月租 14000 + 16500,日租金 = `round(14000/30) + round(16500/30)` = 467 + 550 = **$1,017**
(測試裡是從 `state.runtimes` 現算的,沒有寫死)。

**成熟期單日算式(客流打到產能上限 40 人)**:

```
營收   40 × $36                                   = $1,440
原料   咖啡豆   ceil(40×0.4)  = 16 × $15          =   $240
       牛奶     ceil(40×0.3)  = 12 × $12          =   $144
       麵粉     ceil(40×0.13) =  6 × $8           =    $48
       奶油     ceil(40×0.08) =  4 × $18          =    $72
       貓罐頭   ceil(40×0.1)  =  4 × $25          =   $100
       寵物鮮食 ceil(40×0.06) =  3 × $20          =    $60    小計 $664
固定                                               =   $370
淨利   1440 − 664 − 370                            =   $406    → 日租金的 39.9%
```

**56 個遊戲日的實測平均(測試會印出來,不是宣稱)**:

| 情境 | 平均客流 | 日營收 | 日淨利 | 佔日租金 |
|---|---|---|---|---|
| 成熟期(招牌+第二台咖啡機+戶外座位+大型冷藏,人氣滿) | 39.6 | $1,427 | **$398** | **39.1%** ✅ |
| 剛開張(零投資,人氣 0) | 24.1 | $866 | $68 | 6.6% |

`CAFE_BASE_TICKET = 36` 與 `CAFE_FIXED_COST = 370` 是解這個兩點聯立解出來的:

```
成熟期 40 人:  40P − 664 − F ≈ 400     (目標帶中段)
剛開張 26 人:  26P − 462 − F ≈ 100     (不能是負的,否則開店變成陷阱)
相減 → 14P ≈ 502 → P ≈ 36,回代 F ≈ 370
```

第二條約束是刻意加的:**只盯著成熟期調參,很容易做出一間「開了就每天虧錢、要先燒掉幾萬塊才轉正」的店**,
那會讓玩家後悔按下開張鍵。現在剛開張是 +$68/日——賺得很少但不虧,錢要靠投資才變多,這正是 §5.1
「房租進來 → 投資 → 客流上升 → 再投資」那條迴圈想要的形狀。

#### 四、`crowdMultiplier` 乘當日還是隔日:選**當日**

CAFE-12 明確把這個決定留給本項。選當日,兩個理由:

1. **`CafeState` 的形狀被凍結(lease 的紅線)**。隔日折扣需要一個跨日欄位存「昨天缺了幾種」,
   而咖啡廳只有一個 top-level key、九個欄位已經定死——**隔日方案根本沒有地方存**,
   只能從 `history` 反推,那是更脆的耦合,而且 `history` 有 cap、會被砍掉。
2. **同一帳日內因果閉合**。玩家翻 `history` 看到「客人少 / 營收低 / 缺貨」落在同一列,三個數字互相解釋;
   隔日折扣會讓第 N 天的低客流必須回頭翻第 N−1 天才看得懂。體感上就是
   「今天下午三點賣完了,後面的客人看看菜單就走」——這也正好是日誌文案的樣子。

上游的語意也站在這一邊:`ConsumeResult.crowdMultiplier` 的註解本來就寫「缺貨造成的**當日**客流折扣」。

**同時補了一個洞**:營收乘的是 `crowdMultiplier × fulfillment`,不是只有前者。
只乘 `crowdMultiplier` 的話,常備量全設 0 → 零進貨成本卻仍能拿七成營收(`CROWD_MULTIPLIER_FLOOR = 0.7`)
⇒ **不進貨變成最優解**。再乘 `fulfillment`(實際用掉／需要的原料比)之後,空櫃子 = 零營收——
這不是懲罰,是算術:沒有料就沒有東西賣。設計文件承諾的「最慘只掉三成」講的是客流折扣本身,那條下界原封未動。

#### 五、日誌前綴:🥐,而且**刻意不加進過濾名單**

- **選一個沒被用過的字**:既有的 ☕🍰🧁🍮🐾🏪🫖🧾🧋🍵 都已經有主人(grep 確認過)。
  🥐 全 repo 零出現,測試裡有一條斷言會**掃過整個 `src/`**,確保它只出現在 `cafe.ts` 一個檔案。
- **不加進 `narration.ts:376` 的 `/^[🔮🌀🌱💤]/u`**:那個字元類過濾的是「系統回饋日誌」——
  AI 自己寫的東西或既有日誌的二次敘述,回流會讓 AI 摘要自己的摘要。
  咖啡廳日結**是第一手世界事件**(今天真的賣完了、今天真的丟了一盒奶油),判準上該進 AI 素材。
  前例是 `outing.ts:39-40` 的 🏪 巧遇,同樣是第一手事件、同樣刻意不列入過濾名單。
  測試有一條硬斷言:過濾字元類仍然是 `🔮🌀🌱💤` 一字未動,且不含 🥐。
- **日誌落點**:日結沒有「當事租客」,但這則日誌必須進得了 `rt.log`(`buildNarrateCtx()` 只讀 `rt.log`,
  進不去就不可能成為 AI 素材)。作法抄 `pets.catJournalPass()` 的樓貓筆記:掛在**依 id 排序的第一位**在住租客身上
  (不用 `Object.values()[0]`——鍵的插入序會被搬入／搬出改變);全樓沒人時退回 `notify()`。
- **一天最多推一則**,優先序 缺貨 > 補不滿 > 損耗,平順的一天完全不推。
  日結每天都跑,三則全推會讓咖啡廳淹掉整個動態頁(稀疏哲學同 `floorChain` 的「每次 pass 最多推一話」)。
  平順日的資訊留在 `history`,那是 CAFE-15 面板要顯示的東西。

三則文案範例(各四句一組,用 FNV-1a 依日期決定性選句,零 RNG):

```
🥐 打烊前兩小時就掛出了「咖啡豆售完」的牌子,幾位常客摸摸貓也就回去了。
🥐 跟供應商說這次先少進一點,牛奶的箱子疊起來比平常矮了一截(只補到5成)。
🥐 整理冰箱時,從最深處翻出一盒過期的奶油,只好默默丟掉。
```

#### 六、🔴 刻意不做的事,以及留給誰

| 刻意不做 | 留給 |
|---|---|
| **產能上限的投資項與 `openCafe()` 開張動作**、五個投資項的價格與購買流程 | **CAFE-14**。本項只做**讀取面**:`cafeCapability()` 把 `CAFE_UPGRADE_IDS` 的五個 id 對應到招牌等級／產能／晴天加成／損耗參數。今天 `state.cafe.upgrades` 恆為空 ⇒ 全部算不出效果。CAFE-14 只要把 id 塞進陣列就會活過來,呼叫端一行都不用改。 |
| **客單價的研發加成**。`cafeResearchTicketBonus()` 恆回 0,不猜任何研發 id 的數值 | **CAFE-16**。⚠️ 現在是 39.1%,單一個「+8% 客單價」會推到約 43%;**兩項以上就會頂到 50% 上緣**,CAFE-16 必須連帶重新定標(調 `CAFE_FIXED_COST` 或讓研發同時帶成本)。 |
| **UI**:今日單數、熱銷品、缺貨警告、常備量設定、投資項按鈕、開張按鈕 | **CAFE-15**。本項連一個 `.vue` 的版型都沒動(`FinancePanel.vue` 只補了一個 `CAT_LABEL` key,見第一節)。 |
| **貓跳台與軟墊**(效果在認養詢問頻率上,與日結無關) | CAFE-14 / CAFE-09 線 |
| 顧客個體與日結客流的對帳(`cafeGuestPass()` 生的是店裡看得到的人,日結算的是全日總客數,兩者刻意不綁) | 未列項;真要綁需要先想清楚「畫面上同時最多 6 人」與「一天 40 人」的關係,不在本項範圍 |

`src/sim/economy.ts`、`src/sim/comfort.ts`、`src/floor/map.ts`、`src/pixel/parts.ts`、
`SAVE_VERSION`、`MIGRATIONS`、`CafeState` 的形狀、`scripts/balance-snapshot.json`
**一行未動**。換日區塊既有 pass 的順序也一行未動(測試有斷言:`collectRent()` 前面仍是
`relationshipDailyPass()`、`cafeDailyPass()` 後面仍是 `resetDiaryQuota()`)。

#### 七、零 RNG 與零漂移怎麼保證

- **零漂移**:`cafeDailyPass()` 第一行就是 `if (!cafe.open) return;`。開張要玩家花錢(CAFE-14),
  **無頭的 balance 快照局永遠不會開張** ⇒ `money`、`ledgerCount`、`logs` 全部碰不到。
  這比日數閘門乾淨,連 `CAFE_FIRST_DAY` 都不需要(設計文件 §9 第 2 點原話)。
  測試有一條專門的斷言:未開張時連跑 30 個遊戲日,`money` / `ledger` / `state.cafe` / 全樓日誌長度
  **逐欄 JSON 相同**,而且刻意先把 `standingOrders`／`stock`／`popularity`／`history` 都塞滿內容再跑。
- **零 RNG**:天氣走 splitmix32、星期讀日曆、選句走 FNV-1a。測試覆寫 `Math.random` 計數,
  未開張 30 日與開張 30 日**兩段都是 0**(第二段另有一條「確實有動到錢」的斷言,避免假通過)。

#### 八、驗證結果

- 新增 `scripts/cafe-daily-test.ts` 並**登錄 `run-all.ts` 的 `REGRESSION`**:**86/86 通過**。涵蓋:
  未開張零變更(逐欄 JSON)、開張前後兩段零 `Math.random`、同日同狀態連跑兩次逐欄相同、
  天氣／星期／招牌／人氣／產能各自對客流的影響與加權平均檢查、壞資料(NaN 天氣／星期／人氣)不產生 NaN、
  身無分文開店 20 天 money 不變負／不 NaN／庫存不變負、ledger 金額總和與 money 變動對得起來、
  `history` cap 60 與砍舊留新、**成熟期日淨利佔日租金比例的硬斷言**、三個旗標各推對應日誌、
  三旗標同時成立仍只推一則且優先講缺貨、平順日零日誌、前綴不在過濾字元類且全 `src` 唯一、
  接線錨點(`cafeDailyPass()` 就在 `collectRent()` 正後方、只有一個呼叫點、既有鄰居未變)。
- `npx tsx scripts/balance-test.ts`(**未 `--update`**):快照一致;
  `git diff -- scripts/balance-snapshot.json` **為空**。
- `npm test`:**78/78**(77 → 78,新增本批一支);`npm run typecheck` app／Worker 皆通過;
  `npm run build`:143 modules transformed,只有既有的 >500 kB chunk 提示。
- 本批**沒有版型變更**(只加一個 `CAT_LABEL` 文字 key),故未跑 `ui:shot`。
- 未成年排除戀愛線、成人內容預設關、AI 輸出白名單／夾值／消毒等硬規則全部保留(本批未觸及)。

- **已知限制／後續**:
  1. 玩家目前**仍然開不了張**(沒有 `openCafe()`、沒有按鈕),所以遊戲內看不到任何效果——這是刻意的,開張是 CAFE-14／CAFE-15。
  2. 成熟期客流幾乎每天都被產能上限夾住(平均 39.6 / 上限 40),天氣與星期的質地在成熟期會被壓平。
     這是「成熟的店就是滿載」的合理結果,但 CAFE-14 若要再加產能投資,得一併重新驗這條 30–50%。
  3. CAFE-16 的研發加成會直接推高比例,見第六節的警告。
- **版本狀態**:**直接提交在 `main`**(不開分支、不 push),基準 `8394ecf`;未 push、未部署。

---

### 2026-08-02 — 咖啡廳工作交接給 Codex:現況總表、可用介面、CAFE-14 與 CAFE-15 施工說明(Claude Code)

> 📌 **這一則是自足的交接文件**:接手一樓寵物咖啡廳,只要讀這一則就能動工,不必問任何人。
> `coordination/tasks/` 的 lease **不進版控**(`coordination/README.md` 明訂),所以所有必要資訊都寫在這裡。
> 需要某一項的施工細節時,再回頭讀 CAFE-11／12／13 三則各自的紀錄、`docs/一樓寵物咖啡廳-設計.md`
> 與 `docs/一樓寵物咖啡廳-工作分解.md`。**本則只改兩個文件,零程式碼。**

#### 一、CAFE-01~22 現況總表

狀態是**逐項實跑驗收錨點**確認的(錨點寫在 `docs/一樓寵物咖啡廳-工作分解.md` 每個項目底下),
不是照抄 commit message。`origin/main` 停在 `8ee1cfc`,以下 13 個 commit 全部未 push。

| 項目 | 狀態 | commit | 錨點命中的真實符號 |
|---|---|---|---|
| CAFE-01 FloorViewport 抽出 | ✅ 完成 | `55e7a70` | `src/floor/viewport.ts`:`viewportRect()` / `centeredViewportRect()` / `renderFloorViewport()` / `FloorViewportRect` |
| CAFE-02 獨立樓層頁面切換 | ✅ 完成 | `cf36aea` + `dff61c5`(UX 修訂) | `FloorMap.vue:31` `FLOOR_VIEWS`、`:42` `floorPageNavigation`、`:47` `switchFloorPage()` |
| CAFE-03 地圖擴充 GRID_H + Region | ✅ 完成 | `5d43e0f` | `map.ts:22` `GRID_H = 52`、`:79` `cafe_floor: { c0:1, r0:36, c1:14, r1:47 }` |
| CAFE-04 樓梯與樓板美術 | ✅ 完成 | `2b1b37c` | `floorScene.ts:1074` `drawFloorDivider()`、`:1092` `drawStairs()`(dispatch 在 `:117-118`) |
| CAFE-05 地板 tint 與 cafe atlas | ✅ 完成 | `59a1585` | `limezu.ts:14` `LIMEZU_CAFE_ATLAS_URL = "/assets/limezu/cafe.png"`;`limezu-manifest.json:25` `cafe_atlas` 144×80 |
| CAFE-06 咖啡廳家具 catalog | ✅ 完成 | `4b5a43f` | `catalog.ts:866` `cafe_counter`、`:888` `espresso_machine`;`limezu.ts:313` `preloadLimezuCafeAtlas()`;`main.ts:14` 非阻塞 preload |
| CAFE-07 CafeGuest 型別與姓名池 | ✅ 完成 | `c5288f8` | `src/sim/cafeGuests.ts`、`src/content/cafeGuestNames.ts`、`types.ts` 的 `CafeGuest` / `CafeGuestIntent` |
| CAFE-08 顧客走位與繪製 | ✅ 完成 | `4ce7f2c` | `guestAgents.ts:94` `createGuestAgents()`、`:144` `tickGuestAgents()`;`floorScene.ts:179` `drawGuest()` |
| CAFE-09 認養意圖 → 送養 | ✅ 完成 | `4e0da33` | `pets.ts:65` `CAFE_GUEST_ADOPTION_DESTINATION = "咖啡廳客人領養"`、`:171` `acceptCafeGuestAdoption()` |
| CAFE-10 意圖泡泡 | ✅ 完成 | `a066576` | `floorScene.ts:197` `drawIntentBubble()`、`:203` `tryDrawLimezuThinkingEmote()` |
| CAFE-11 `state.cafe` schema 與存檔 | ✅ 完成 | `53b42c7` | `gameState.ts:193` `defaultCafe()`、`:252` `sanitizeCafeState()`;`persistence.ts:155` 存、`:250` 讀 |
| CAFE-12 常備訂單與庫存 | ✅ 完成 | `8394ecf` | `cafe.ts:165` `restockPlan()` 的 `standingOrders` 參數;`content/cafeIngredients.ts` 的 `CAFE_INGREDIENTS` |
| CAFE-13 日結 pass 與客流公式 | ✅ 完成 | `aa6507f` | `tick.ts` 換日區塊 `collectRent();` 正後方第一行就是 `cafeDailyPass();` |
| CAFE-14 投資項與開張 | ❌ 未做 | — | ⚠️ 錨點 `openCafe\|cafeUpgrades\|CAFE_UPGRADES` 會**命中 `cafe.ts:410` 的一行註解**(內容是「這件事留給 CAFE-14」),那是假陽性。真正判準:`openCafe()` 與 `src/content/cafeUpgrades.ts` **都不存在** |
| CAFE-15 咖啡廳面板 UI | ❌ 未做 | — | `src/components/CafePanel.vue` 不存在,`App.vue` 零引用 |
| CAFE-16 研發資料與倒數 | ❌ 未做 | — | ⚠️ 錨點 `cafeResearch\|CAFE_RESEARCH` 會**命中 `cafe.ts:587` 的 `cafeResearchTicketBonus()`**,但那是恆回 0 的樁,不是研發系統。真正判準:`src/content/cafeResearch.ts` 不存在 |
| CAFE-17 菜單與客單價 | ❌ 未做 | — | `cafe.ts` 無 `menuItems` / `avgTicket` |
| CAFE-18 研發面板 UI | ❌ 未做 | — | 依賴 CAFE-15,元件本身不存在 |
| CAFE-19 租屋意圖 → 招租 | ❌ 未做 | — | `recruit.ts` 無 cafe applicant 入口函式 |
| CAFE-20 `at_cafe` visualState | ❌ 未做 | — | `types.ts` / `routine.ts` 無 `at_cafe` |
| CAFE-21 groupScene cafe venue | ❌ 未做 | — | `groupScene.ts` 無 `"cafe"` venue |
| CAFE-22 寵物下樓 | ❌ 未做 | — | `petAgents.ts` 無 `cafe_pet` |

**公開上線閘門**(工作分解 §CAFE-15 後):最低範圍是 CAFE-01~15 全完成,另把 CAFE-22 提前納入。
所以**剩下的必做只有三項:CAFE-14、CAFE-15、CAFE-22**;CAFE-16~21 可延後。

#### 二、已完成部分的可用介面(接手施工直接照這份用)

以下簽名全部是**實際讀檔確認**的,不是憑印象寫的。

##### `src/sim/cafeGuests.ts` — 顧客資料層(純函式、零 RNG、不讀寫全域 state)

| 符號 | 簽名 | 用途 |
|---|---|---|
| `CAFE_GUEST_CAP` | `= 6` | 店裡同時最多幾位顧客。`appendCafeGuest()` 與 `sanitizeCafeState()` 都吃這個上限 |
| `cafeGuestHash` | `(value: string) => number` | FNV-1a 32-bit。**咖啡廳線唯一的「亂數」來源**;要做任何機率一律用它,不要新增 `Math.random()` |
| `generateCafeGuest` | `(input: GenerateCafeGuestInput) => CafeGuest` | 建立一位決定性顧客。input 欄位:`seed`(string\|number,必填)、`arrivedMs`(必填)、`sequence?`、`intent?`、`seatTile?`、`excludeNames?`。同 `seed`+`sequence`+`arrivedMs` 永遠得到同一位;未指定 `intent` 時 coffee 70% / adopt 20% / rent 10%;停留 1~3 遊戲小時 |
| `appendCafeGuest` | `(guests: readonly CafeGuest[], guest: CafeGuest) => CafeGuest[]` | 未滿 cap 且 id 不重複才加入,**回傳新陣列**(不改輸入) |
| `removeCafeGuest` | `(guests: readonly CafeGuest[], guestId: string) => CafeGuest[]` | 玩家處理完或走到門口時移除 |
| `removeDepartedCafeGuests` | `(guests: readonly CafeGuest[], gameMs: number) => CafeGuest[]` | 只留 `leavesMs > gameMs` 的顧客 |

##### `src/floor/guestAgents.ts` — 顧客畫面層(**不進 `state.runtimes`、不進 `tickAgents`**)

| 符號 | 簽名 | 用途 |
|---|---|---|
| `createGuestAgents` | `(guests: readonly CafeGuest[], blocked = currentBlocked()) => GuestAgent[]` | 依 id 固定排序後分配入口與座位;不修改傳入的 `CafeGuest` |
| `tickGuestAgents` | `(agents: GuestAgent[], dt: number, gameMs: number, blockedCells: ReadonlySet<string> = ∅, blocked = currentBlocked()) => void` | 每幀推進走位。`gameMs >= leavesMs` 後改走回入口,抵達才標 `departed`/`hidden` |
| `guestAgentSignature` | `(guests: readonly CafeGuest[]) => string` | 畫面層判斷「要不要重建 agents」用的簽名(id:leavesMs:座位) |
| `departedGuestIds` | `(agents: readonly GuestAgent[]) => string[]` | 已走到門口的 id,排序後回傳;caller 據此呼叫 `removeCafeGuest()` |
| `cafeGuestSeatCandidates` | `(blocked = currentBlocked()) => Tile[]` | 決定性座位候選:偏好格優先,其餘只收真正的 `cafe_floor` 可走格 |

另有 `CAFE_GUEST_ENTRY_TILES`(`{7,51}`/`{8,51}`)、`CAFE_GUEST_PREFERRED_SEATS`(6 格)、
`GuestAgentPhase = "entering" | "seated" | "leaving" | "departed"`、`GuestAgent` 介面。

##### `src/sim/cafe.ts` — 咖啡廳全部算式

🔴 **這個檔案有原始碼掃描硬斷言**(`scripts/cafe-supply-test.ts`):剝掉註解後不得含
`Math.random` / `addMoney`,也不得 import `gameState` / `tick` / `economy`。
**算式一律留在 `cafe.ts`、需要碰 state 或錢的 pass 一律寫在 `tick.ts`** —— 這條界線不要打破。

*CAFE-12 的四個純函式(簽名一字未改過,可安心依賴)*

| 符號 | 簽名 | 用途 |
|---|---|---|
| `restockPlan` | `(standingOrders: Readonly<Record<string,number>>, stock: Readonly<Record<string,number>>, money: number) => RestockPlan` | 依常備訂單算今日補貨。**不扣款、不改輸入**。錢不夠時兩段式配額(先各補到 `ceil(常備量×0.5)`,再補滿)。回傳 `lines` / `totalCost`(保證 `<= money`)/ `stock` / `missingUnits` / `underfunded` / `fulfillment` / `moneyAfter`(保證 `>= 0`) |
| `consumeStock` | `(stock, demand) => ConsumeResult` | 扣今日消耗,庫存不變負。回傳 `lines` / `stock` / `shortages` / `fulfillment` / `crowdMultiplier`(`1 − 缺貨種數×0.08`,夾底 `0.7`) |
| `applySpoilage` | `(stock, options: SpoilageOptions = {}) => SpoilageResult` | 只有生鮮會壞;`損耗 = floor(max(0, 庫存 − freeUnits) × rate)`,預設 `freeUnits=6`、`rate=0.1`。**是「每日」函式,不可掛進載入流程**。回傳 `lines` / `stock` / `totalSpoiled` / `totalWastedValue` |
| `dailyDemand` | `(guestCount: number) => Record<string, number>` | 客流 → 各原料需求量(`ceil(客流 × perGuest)`,已 quantize 修浮點) |

*CAFE-13 追加的算式與 CAFE-14 的讀取面*

| 符號 | 簽名／值 | 用途 |
|---|---|---|
| `cafeCapability` | `(upgrades: readonly string[] = []) => CafeCapability` | **CAFE-14 的讀取面**。把已購買 id 轉成 `{ signLevel, capacity, outdoorSeats, spoilage }`。`upgrades` 今天恆為空 ⇒ 回 `{1, 26, false, {}}` |
| `CAFE_UPGRADE_IDS` | `{ signboard:"cafe_signboard", secondMachine:"cafe_second_machine", outdoorSeats:"cafe_outdoor_seats", coldStorage:"cafe_cold_storage", petTower:"cafe_pet_tower" }` | 五個投資項 id。**把 id 塞進 `state.cafe.upgrades` 效果就會生效**,呼叫端一行都不用改 |
| `cafeCrowd` | `(input: CafeCrowdInput) => CafeCrowdResult` | input `{ weather, weekday, signLevel, capacity, popularity, outdoorSeats? }`;回 `{ base, guests, weatherMultiplier, weekdayMultiplier, popularityMultiplier, cappedByCapacity }` |
| `cafeTicketPrice` | `(completed: readonly string[] = []) => number` | 客單價 = `round(36 × (1 + 研發加成))` |
| `cafeResearchTicketBonus` | `(_completed: readonly string[] = []) => number` | ⚠️ **恆回 0 的樁**,留給 CAFE-16。見第六節警告 |
| `nextCafePopularity` | `(current: number, outcome: { shortages: number; underfunded: boolean }) => number` | 隔日人氣。順利 +2、缺貨 −4、只是補不滿 −1,夾 0~100 |
| `cafeDailyLine` | `(input: CafeDailyLineInput) => string` | 日結敘事一句(**含 🥐 前綴**)。`kind: "shortage" \| "underfunded" \| "spoilage"`,選句用 FNV-1a 依 `day` 決定性挑,零 RNG |
| `getCafeIngredient` | `(id: string) => CafeIngredient \| undefined` | 依 id 取原料定義;未知 id 回 `undefined` |
| `suggestedStandingOrders` | `() => Record<string, number>` | **CAFE-15 的常備量預設值來源**。目前只有測試在用,產品端零呼叫 |

常數(全部 `export`,可直接在 UI 顯示或做斷言):`RESTOCK_RESERVE_RATIO=0.5`、
`CROWD_PENALTY_PER_SHORTAGE=0.08`、`CROWD_MULTIPLIER_FLOOR=0.7`、`SPOILAGE_FREE_UNITS=6`、
`SPOILAGE_RATE=0.1`、`CAFE_BASE_CAPACITY=26`、`CAFE_CAPACITY_PER_MACHINE=14`、
`CAFE_COLD_STORAGE_FREE_MULT=2`、`CAFE_COLD_STORAGE_RATE_MULT=0.5`、`CAFE_CROWD_PER_SIGN_LEVEL=22`、
`CAFE_WEATHER_MULTIPLIER`(晴 1.15／陰 1.00／雨 0.70／悶熱 1.05)、
`CAFE_WEEKDAY_MULTIPLIER = [1.25,0.9,0.9,0.9,0.9,1.05,1.25]`(索引 0=週日)、
`CAFE_OUTDOOR_SUNNY_BONUS=0.15`、`CAFE_POPULARITY_SWING=0.25`、`CAFE_POPULARITY_MAX=100`、
`CAFE_POPULARITY_GAIN=2`、`CAFE_POPULARITY_LOSS=4`、`CAFE_POPULARITY_SOFT_LOSS=1`、
`CAFE_BASE_TICKET=36`、`CAFE_FIXED_COST=370`、`CAFE_LOG_PREFIX="🥐"`。

##### `src/sim/gameState.ts` — state 與存檔正規化

| 符號 | 簽名 | 用途 |
|---|---|---|
| `defaultCafe` | `() => CafeState` | 九個欄位的預設值。舊存檔沒有 `cafe` 就直接拿這份,**不需要升 `SAVE_VERSION`**(仍為 6) |
| `sanitizeCafeState` | `(raw: unknown, gameMs: number) => CafeState` | load 端逐欄正規化:壞顧客整筆丟掉、髮型/配件走既有白名單、顏色過 `sanitizeAppearanceInPlace`、`guests` 去重後 `removeDepartedCafeGuests` 再套 cap、`history` 取最後 60 筆 |
| `CAFE_HISTORY_CAP` | `= 60` | 日結紀錄上限(`gameState.ts:184`) |

##### `src/sim/tick.ts` — 兩個 pass(唯一會碰 state 與錢的地方)

- `cafeGuestPass(hour)` — 顧客生成/離場,掛在 `hourlyTick` 每租客迴圈之後、`outingEncounterPass` 前。
  營業時段 10~20 點,門檻 `CAFE_ARRIVAL_CHANCE = 55`,離場寬限 `CAFE_GUEST_LINGER_MS = 2 遊戲小時`。
- `cafeDailyPass()` — 日結,掛在換日區塊 **`collectRent();` 正後方**(測試有斷言:前面仍是
  `relationshipDailyPass()`、後面仍是 `resetDiaryQuota()`)。順序是
  客流 → `consumeStock` → 營收進帳 → `restockPlan` 扣款 → 固定成本 → `applySpoilage` → 人氣 → `history` → 日誌。
- `pushCafeLog()` — 日結日誌掛在**依 id 排序的第一位在住租客**的 `rt.log`(抄 `pets.catJournalPass()`);
  全樓沒人時退回 `notify()`。一天最多推一則,優先序 缺貨 > 補不滿 > 損耗,平順的一天不推。

##### `state.cafe` 九個欄位:用途與**目前真的有沒有被讀寫**

| 欄位 | 型別 | 用途 | 目前讀 | 目前寫 |
|---|---|---|---|---|
| `open` | `boolean` | 是否已開張。**兩個 pass 的第一行閘門** | ✅ `tick.ts:536`、`tick.ts:610` | ❌ **沒有任何程式寫它**(沒有 `openCafe()`)⇒ 遊戲內永遠是 `false` |
| `standingOrders` | `Record<原料 id, 常備量>` | 每日補到的水位 | ✅ `tick.ts:633` 餵 `restockPlan()` | ❌ 沒有 UI ⇒ 恆為 `{}`(CAFE-15) |
| `stock` | `Record<原料 id, 數量>` | 庫存 | ✅ `tick.ts:624` | ✅ `tick.ts:639`(`cafe.stock = rot.stock`) |
| `research` | `CafeResearch \| null` | 進行中的研發 | ❌ | ❌ 只有 `defaultCafe()` / `sanitizeCafeState()` 碰它(CAFE-16) |
| `completed` | `string[]` | 已完成研發 | ✅ `tick.ts:629` 餵 `cafeTicketPrice()`,但加成恆 0 | ❌(CAFE-16) |
| `upgrades` | `string[]` | 已購買投資項 | ✅ `tick.ts:613` 餵 `cafeCapability()` | ❌ **沒有購買流程**(CAFE-14) |
| `guests` | `CafeGuest[]` cap 6 | 店裡的客人 | ✅ `tick.ts` + `FloorMap.vue:102-112` | ✅ 同上 |
| `popularity` | `number` 0~100 | 人氣,對基礎客流最多 +25% | ✅ `tick.ts:619` | ✅ `tick.ts:640` |
| `history` | `CafeDayRecord[]` cap 60 | 日結紀錄 `{ day, guests, revenue, cost, net }` | ❌ **還沒有任何讀取端** | ✅ `tick.ts:645` |

⇒ 一句話:**九個欄位裡只有 `stock` / `guests` / `popularity` / `history` 有寫入端**,
`open` / `standingOrders` / `upgrades` 三個等 CAFE-14 與 CAFE-15,`research` / `completed` 等 CAFE-16。

#### 三、下一項 CAFE-14(投資項與開張)的完整施工說明

**現況**:只做好**讀取面** —— `cafeCapability()` 與五個 `CAFE_UPGRADE_IDS`。
**價格、`openCafe()`、購買流程全部待做**。做好之後,把 id 塞進 `state.cafe.upgrades` 效果就會生效,
`tick.ts` 的呼叫端一行都不用改。

**五個投資項(名稱、價格、效果見 `docs/一樓寵物咖啡廳-設計.md` §5.4)**:

| 投資項 | 設計文件價格 | 效果 | 已接好的讀取面 |
|---|---|---|---|
| 店面招牌 `cafe_signboard` | $30,000 | 基礎客流 ↑,且提高租屋詢問頻率 | `signLevel` 1 → 2(客流 ×2) |
| 第二台咖啡機 `cafe_second_machine` | $18,000 | 尖峰時段不再流失客人 | `capacity` 26 → 40 |
| 戶外座位 `cafe_outdoor_seats` | $25,000 | 晴天客流 ↑,雨天無效 | 晴天 ×1.15 |
| 大型冷藏 `cafe_cold_storage` | $15,000 | 生鮮損耗 ↓ | `freeUnits` ×2、`rate` ×0.5 |
| 貓跳台與軟墊 `cafe_pet_tower` | $12,000 | 寵物停留 ↑ → 認養詢問頻率 ↑ | **尚未接**(效果在認養線,與日結無關) |

**語意與既有的 `buyUpgrade()`(`src/sim/economy.ts:358`)完全同構 —— 沿用那套形狀**:

- **一次性、不可退、永久生效**;已買過要回 `{ ok: false, reason: "..." }` 而不是重複扣款
- 檢查順序照 `buyUpgrade()`:`def 存在?` → `已擁有?` → `state.money < def.price?` → 才 `addMoney(-price, 說明, 分類)`
- 交易分類用既有的 `"cafe"`(CAFE-13 已把它加進 `TxnCategory`,`FinancePanel.vue` 的 `CAT_LABEL` 也已有 `cafe: "咖啡廳"`)
- 允許修改的檔案:`src/sim/cafe.ts`、新增 `src/content/cafeUpgrades.ts`;
  **禁止修改 `buyUpgrade()` 本體與 `upgradeState`**
- 開張價格「與第 3 期經濟測試一起定標,不在文件先猜常數」——參考第七節的數值基準決定

**完成條件**:餘額不足不能開張且 `money` 不變負;重複開張不重複扣款;未開張 balance 快照零漂移。

🔴 **若再加產能投資,必須重驗日淨利仍在日租金的 30–50%**。
成熟期客流已經幾乎每天被產能上限夾住(平均 39.6 / 上限 40),再加產能會直接把淨利推上去。

#### 四、下一項 CAFE-15(面板 UI)的完整施工說明

- **版型參照既有的 `FinancePanel.vue` / `UpgradePanel.vue`**,新增 `src/components/CafePanel.vue`,
  `src/App.vue` 只做接線。**禁止改其他既有 `.vue`。**
- **未開張時**顯示開張成本與開張按鈕;**開張後**要顯示:
  - **今日單數**(`state.cafe.history` 最後一筆的 `guests`)
  - **熱銷品**(可從 `dailyDemand()` / `consumeStock()` 的 `lines` 推)
  - **缺貨警告**(`ConsumeResult.shortages`;另可用 `CafeCrowdResult.cappedByCapacity` 提示「該加設備了」)
  - **常備量設定**(寫入 `state.cafe.standingOrders`)
  - **投資項**(CAFE-14 做好的購買流程)
  - **開張按鈕**
  - 以及顧客認養詢問的接受／婉拒(接 `acceptCafeGuestAdoption()`,`pets.ts:171`)
- **`suggestedStandingOrders()`(`cafe.ts:108`)是常備量的預設值來源** —— 玩家沒設過就用它。
  目前產品端零呼叫,CAFE-15 是它的第一個呼叫端。
- `state.cafe.history` 目前**沒有任何讀取端**,面板是它的第一個消費者。

🔴 **驗收必須跑三種手機寬度(390 / 412 / 430)的 `ui:shot`**,確認**無橫向溢出、無換行破版**。
從工作區根執行 `npm run ui:shot -- rent`,完成前逐張檢查 `artifacts/ui-lab/rent/` 的 PNG 與 `report.json`。
⚠️ 容器內 Vite 不偵測改動(會送舊版造成假陰性),請確認是重新 build 過的版本。

#### 五、🔴 三條會讓 balance 全紅的鐵則(每個接手者都必須知道)

1. **不得把任何 cafe region 加進 `COMMUNAL_AREA_IDS`**(`src/sim/comfort.ts`)。
   加了 `COMMUNAL_NEUTRAL = 35.28233009708738` 立刻失效,全樓租客基準線位移,快照全紅。
2. **`INITIAL_PLACEMENTS`(`map.ts:150`)只能 append,不能中段插入**。
   `resolveTarget`(`routine.ts:176`)取陣列**第一個**符合的,插入會讓全樓租客的沙發/電視/廚房目標靜默改道。
3. **`map.ts` rows 0–32 一格都不能動**。現有三樓是零漂移的第一道防線。

**外加第四條(CAFE-14 專屬)**:目前零漂移靠的是 **`state.cafe.open` 這個天然閘門** ——
兩個 pass 的第一行都是 `if (!cafe.open) return;`,而開張要玩家花錢,**無頭的 balance 快照局永遠不會開張**,
所以連 `CAFE_FIRST_DAY` 之類的日數閘門都不需要。
🔴 **CAFE-14 做開張功能時,務必確認無頭 balance 測試不會意外開張**(例如不要在 `defaultCafe()`、
存檔遷移、或任何自動流程裡把 `open` 設成 `true`)。這道閘門一破,整份快照會同時變紅而且很難分辨是誰造成的。

每一項的收工檢查:`npm test`、`npm run build`、`npm run typecheck`、
`npx tsx scripts/balance-test.ts`(**不得 `--update`**)。程式與 `工作日誌.md` 同一個 commit。

#### 六、兩個必須傳下去的警告

⚠️ **一、`cafeResearchTicketBonus()` 恆回 0,CAFE-16 不能只是往上加**
現在日淨利是日租金的 **39.1%**。單一研發項「+8% 客單價」會把它推到約 **43%**,
**兩項以上就會頂到 50% 的上緣**(設計文件 §5.1 的硬性目標帶是 30–50%)。
所以 **CAFE-16 必須連帶重新定標**——調 `CAFE_FIXED_COST`,或讓研發項同時帶成本,
不能只是把加成往上疊。

⚠️ **二、CAFE-13 修掉的 exploit,後續改營收公式時不要把保護拆掉**
營收原本只乘 `crowdMultiplier`(下界 `CROWD_MULTIPLIER_FLOOR = 0.7`),
導致**常備量全設 0 → 零進貨成本卻仍能拿七成營收 → 不進貨變成最優解**。
現在 `tick.ts:625` 是 `served = round(crowd.guests × crowdMultiplier × fulfillment)`,
再乘 `fulfillment`(實際用掉／需要的原料比)之後,空櫃子 = 零營收。
這不是懲罰、是算術:沒有料就沒有東西賣。設計文件承諾的「最慘只掉三成」講的是**客流折扣本身**,
那條下界原封未動。**後續改動營收公式時不要把 `fulfillment` 這一項拿掉。**

#### 七、已知的數值基準(讓後續改動有對照)

- **日租金 $1,017**:種子局兩位租客月租 14000 + 16500,`round(14000/30) + round(16500/30)` = 467 + 550。
  測試裡是從 `state.runtimes` 現算的,沒有寫死。
- **成熟期日淨利 $398 = 日租金的 39.1%**(招牌 + 第二台咖啡機 + 戶外座位 + 大型冷藏,人氣滿,
  56 個遊戲日實測平均:客流 39.6、日營收 $1,427)。
- **剛開張 $68/日**(零投資、人氣 0,客流 24.1、日營收 $866)—— 賺得少但**不虧**,開店不是陷阱。
- **客單價 $36、固定成本 $370 是解聯立來的**,不是拍腦袋(推導見 CAFE-13 那則日誌第三節):
  ```
  成熟期 40 人:  40P − 664 − F ≈ 400
  剛開張 26 人:  26P − 462 − F ≈ 100     ← 第二條約束刻意加的:不能是負的
  相減 → 14P ≈ 502 → P ≈ 36,回代 F ≈ 370
  ```
- **`CAFE_CROWD_PER_SIGN_LEVEL = 22` 也是回推的**:招牌 1 級 ≈ 22 人、2 級 ≈ 44 人被產能夾到 40 人,
  兩點剛好架出 30–50% 的目標帶。
- 天氣係數加權平均 **0.99**、星期係數七日平均 **1.021** —— 兩者都是**質地**,不是長期加稅。

#### 八、版本狀態

- `main` = `aa6507f`,**領先 `origin/main`(`8ee1cfc`)14 個 commit,全部未 push、未部署**。
- **未 push 的理由**:現在推上去會部署一個「一樓看得到、但開不了張」的半成品——
  玩家能切到 1F 看見咖啡廳的空間與家具,卻沒有任何開張按鈕,`state.cafe.open` 永遠是 `false`。
- **建議 CAFE-14 + CAFE-15 完成後再一起推**,那時才是一條完整的「開張 → 補貨 → 日結 → 再投資」閉環。
  (工作分解的公開上線閘門另要求把 CAFE-22 一併納入,否則認養流程在實際畫面不可達。)
- 工作方式:兩個 Agent **直接共用 `main`,不開 feature branch**,靠工作日誌追溯。
  開工前先 `git status --short`,有不屬於你的 dirty 檔案就先停手。

- **本則的驗證**:純文件,未跑測試(未動任何程式碼)。CAFE-01~22 的 22 條驗收錨點全部實跑,
  結果與上表 100% 一致;`docs/一樓寵物咖啡廳-工作分解.md` 同步標記 CAFE-01~13 完成並附 commit,
  另依「驗收錨點制第一條規則」把 5 條與真實符號不符的錨點改寫成實際存在的符號。

### 2026-08-02 — CAFE-14 開張與五項永久投資:純交易結果、$12,000 定標(Codex)

- **目的／範圍**:接續 Claude 完成的 CAFE-13，補上 `state.cafe.open` 與 `upgrades` 的安全寫入結果，
  讓下一項 CAFE-15 能直接接開張／投資 UI；沿用同一個 `main`，未建立新 branch。
- **開張與投資資料**:
  - 新增 `src/content/cafeUpgrades.ts`：定義穩定的五個 id、名稱、效果與設計文件原價（招牌 $30,000、
    第二台咖啡機 $18,000、戶外座位 $25,000、大型冷藏 $15,000、貓跳台與軟墊 $12,000）。
  - `CAFE_OPENING_COST = $12,000`：種子局 $52,000 付完仍買得起點餐吧台 $16,000 與濃縮咖啡機
    $18,000，保留 $6,000 周轉；搭配 CAFE-13 的剛開張正日淨利，不會讓開店成為資金陷阱。
- **純交易命令**:
  - `src/sim/cafe.ts` 新增 `openCafe()`／`buyCafeUpgrade()`／`getCafeUpgrade()`。成功結果包含下一份
    `CafeState`、精確 `cost`／`moneyAfter`、可讀 label 與 `category: "cafe"`；失敗保留原 state
    參考與原餘額、cost 為 0。下一項 UI 只需把成功結果交給既有金流／save，不必重寫判斷。
  - 開張與投資都不修改輸入；重複開張、未知投資、未開張先投資、重複投資與餘額不足均拒絕。
    五個 id 仍直接餵既有 `cafeCapability()`，招牌、產能、戶外座位與冷藏效果不改日結呼叫端；
    貓跳台永久記錄，認養頻率效果留在後續認養線。
  - 保留 `cafe.ts` 的硬邊界：沒有匯入全域 state／tick／economy，沒有呼叫金流或 RNG；未修改
    `buyUpgrade()`、`upgradeState`、`defaultCafe()`、換日 pass、地圖、既有平衡旋鈕或快照。
- **驗證**:
  - 新增 `scripts/cafe-upgrade-test.ts` 並登錄正式回歸：27/27，涵蓋資料價格、$52,000 開店校準、
    所有成功／拒絕分支、不變性、精確扣款、五項能力、交易分類、原始碼邊界與零 RNG。
  - `cafe-supply-test.ts`:73/73；最終樹 `npm test`:79/79 支全綠（前一輪 `arc-test` 曾由 runner 重試通過，最終輪直接通過）；
    `npm run typecheck`:app／Worker 通過；`npm run build`:144 modules transformed，只有既有大於 500 kB
    chunk 提示；`balance-test` 快照一致，未使用 `--update`。
  - 本批沒有 Vue／CSS／畫布變更，因此未重跑 Mobile UI Lab；CAFE-15 接面板時必須跑三手機寬度。
- **已知限制／後續**:目前仍沒有玩家可點擊的開張按鈕；這是下一項 CAFE-15 的 UI 接線範圍。
  所以本批不 push／deploy，避免公開一個可見但仍不可操作的半成品。公開上線仍需 CAFE-15 與 CAFE-22。
- **版本狀態**:`main`,基準 `f7fd0fa`;本批已完成待提交，尚未 push／deploy。

### 2026-08-02 — CAFE-15 咖啡廳營運面板:開張、常備量、投資與認養決策(Codex)

- **目的／範圍**:接續 Claude 的 CAFE-11～13 與 Codex 的 CAFE-14，在同一個 `main` 完成玩家可操作的
  咖啡廳營運面板；未建立新 branch，未碰其他既有面板。
- **實作**:
  - 新增 `src/components/CafePanel.vue`：以手機 bottom sheet 呈現未開張／已開張兩種狀態。未開張顯示
    $12,000 成本與資金檢查；已開張顯示最近單數、人氣、淨利、產能、熱銷品、缺貨預估與空狀態。
  - 常備量使用本地草稿，只有「套用常備量」才寫入六種已知原料並 `save()`；輸入正規化為 0～99 的
    整數，「恢復建議」不會直接改動遊戲狀態。
  - 五項永久投資直接讀 `CAFE_UPGRADES`。開張／投資先取得 CAFE-14 的純交易結果，成功才套用
    `state.cafe`、透過 `addMoney(..., "cafe")` 扣款並存檔；失敗不扣款。
  - 認養區列出 `intent === "adopt"` 的顧客與可用永久樓寵物；接受時先跑
    `acceptCafeGuestAdoption()`，只有成功才移除顧客，婉拒則只清掉詢問並保留寵物。
  - `src/App.vue` 僅增加「☕ 咖啡廳」操作按鈕、面板掛載與樣式；原本獨立的 3F／1F 頁面切換設計不變。
- **驗證**:
  - 專項：`cafe-upgrade-test.ts` 27/27、`cafe-adoption-test.ts` 17/17。
  - 完整回歸：`npm test` 79/79 支全綠；`npm run typecheck` app／Worker 通過；`npm run build`
    147 modules transformed，只有既有大於 500 kB chunk 提示；`balance-test.ts` 快照一致，未更新基準。
  - Mobile UI Lab：390×844、412×915、430×932 的樓層／動態／收支共 18 張 PNG 全部逐張檢視；
    `report.json` 為 0 errors／0 warnings，五顆樓層操作按鈕沒有橫向溢出或裁切。
  - 本機瀏覽器實際走完：$73,927 → 開張後 $61,927 → 貓跳台投資後 $49,927；咖啡豆常備量設為
    21 後關閉／重開仍保留，投資完成標記也持久化；收支頁出現「咖啡廳開張」與
    「咖啡廳投資:貓跳台與軟墊」兩筆各 −$12,000，console 0 errors／warnings。
- **已知界線／上線判斷**:CAFE-15 已完成認養 UI 與 sim 接線，但目前永久樓寵物尚不會實際進入
  `cafe_pet`；因此玩家端認養端到端仍不可達。依公開上線閘門，**現在剩下必做只有 CAFE-22**；
  CAFE-16～21 研發與擴充可延後。
- **版本狀態**:`main`,基準 `d480496`;本批已完成待提交，尚未 push／deploy。

### 2026-08-02 — CAFE-22 永久樓寵物下樓:決定性白天時段、跨樓層尋路與公開閘門(Codex)

- **目的／範圍**:完成咖啡廳公開最低範圍的最後一項，讓永久樓寵物真的能從三樓走進一樓
  `cafe_pet`，補上認養流程的畫面可達性；沿用同一個 `main`，未修改 `src/sim/pets.ts`。
- **實作**:
  - `src/floor/petAgents.ts` 新增 `petAgentRegion()` 與 10:00～15:59、35% 的下樓規則。只有
    `state.cafe.open`、`ownerId === "landlord"`、永久安置、白天且沒有有效配對演出時才可能命中。
  - 判定沿用 `cafeGuestHash()`，key 為 `petId + 當地日期小時`；同寵物同小時結果固定，沒有增加
    `Math.random()` 呼叫。未命中或不合資格時直接回原 `pet.hangout`。
  - `createPetAgents()` 與一般遊蕩／配對目標改讀有效區域；命中後沿既有 `findPath()` 跨樓層走到
    未被家具阻擋的 `cafe_pet` 格。全程不改寫 `Pet.hangout`、pair 或其他 sim state。
  - 新增 `scripts/cafe-pet-agent-test.ts` 並登錄正式 runner：12 項涵蓋 closed、夜間、房客／中途寵物、
    active pair、決定性與零新增 RNG、建立位置、跨樓層路徑、hangout 不變，以及畫面可達寵物完成認養。
- **驗證**:
  - 專項 `cafe-pet-agent-test.ts` 12/12；既有 `pet-test.ts` 90/90、`cafe-adoption-test.ts` 17/17。
  - `npm test` 80/80 支全綠；`npm run typecheck` app／Worker 通過；`npm run build` 147 modules
    transformed，只有既有大於 500 kB chunk 提示；`balance-test.ts` 快照一致，未更新基準。
  - Mobile UI Lab 重新 build 後，390×844、412×915、430×932 的樓層／動態／收支共 18 張 PNG
    全部逐張檢視；`report.json` 為 0 errors／0 warnings，沒有新增溢出、裁切或渲染錯誤。
- **已知界線／上線判斷**:公開最低功能範圍（CAFE-01～15 + CAFE-22）已達成；CAFE-16～21
  研發、租屋意圖與同框演出可延後。但目前 CAFE-15 面板仍列出所有永久樓寵物，CAFE-09 也不讀
  ephemeral agent 座標，所以尚未強制「寵物此刻已抵達 `cafe_pet` 才能接受認養」。若產品要這條硬規則，
  必須另開共享位置狀態工作項，不能改寫 `hangout` 造成模擬漂移。
- **版本狀態**:`main`,基準 `958df72`;本批已完成待提交，尚未 push／deploy。

### 2026-08-02 — 一樓寵物咖啡廳公開部署：main、CI 與 production 驗證（Codex）

- **發布範圍**：將公開最低功能範圍 CAFE-01～15 + CAFE-22 共 18 個 commit 推送至既有 `main`；
  功能發布 commit 為 `56cdeac`，未建立新 branch。
- **GitHub CI**：run `30730715680` 完成且 conclusion 為 `success`；執行頁：
  `https://github.com/guai7485962/rent-house/actions/runs/30730715680`。
- **Cloudflare production**：由既有 Git integration 隨 `main` push 自動部署，沒有再執行手動
  `wrangler deploy`，避免同一版本重複發布。正式網址：`https://rent-house.guai7485962.workers.dev/`。
- **線上驗證**：正式站回應 `HTTP/1.1 200 OK`，首頁載入 `/assets/index-CrA2vggW.js`；實際下載該
  production bundle 後，確認「一樓寵物咖啡廳正式開張」、「套用常備量」與
  「前往 1F 寵物咖啡廳」三個新功能標記都存在。
- **驗證界線**：發布前完整回歸仍為 `npm test` 80/80、app／Worker typecheck 通過、build 通過、
  balance snapshot 不變，Mobile UI Lab 18 張三手機寬度截圖為 0 errors／0 warnings；線上驗證未呼叫
  付費 AI 端點。

### 2026-08-02 — CAFE-16 研發資料與遊戲日倒數：10 項前兩層、純交易與平衡護欄（Codex）

- **目的／範圍**：依使用者「繼續下一個優化」接續已上線的咖啡廳，完成工作分解下一項 CAFE-16；
  沿用同一個 `main`，不改 Vue、tick、地圖、CafeState schema 或 SAVE_VERSION。
- **研發資料**：
  - 新增 `src/content/cafeResearch.ts`，固定 10 個 id：第一層「基礎沖煮／烘焙／寵物餐點」，第二層
    「手沖單品、拿鐵拉花、冷萃／司康、貓咪造型餅乾／寵物友善點心、寵物生日蛋糕」。第三層的
    季節限定豆、造型拿鐵與下午茶套餐沒有偷跑。
  - 根節點為 2 天、$2,500～3,000；第二層為 3～4 天、$3,500～5,000。冷萃另需大型冷藏，
    寵物生日蛋糕另需貓跳台與軟墊；生日蛋糕只留下 special-event 資料旗標，不在本項觸發事件。
  - `docs/一樓寵物咖啡廳-設計.md` 已加入實際定標表，工作分解同步將 CAFE-16 標為完成。
- **純函式 API**：
  - `getCafeResearch()`／`cafeResearchRequirementsMet()`／`availableCafeResearch()` 提供穩定查詢；未開張、
    前置未滿、已完成或已有 active 時都不會誤列為可開始。
  - `startCafeResearch()` 同構既有開張／投資交易：成功回新 CafeState、精確 cost／moneyAfter、label 與
    `category: "cafe"`；失敗保持原 state 參考、cost 0、餘額不動。同時只能進行一項，開始時把日序、
    天數與投入金額寫進既有 `CafeResearch` 欄位。
  - `cafeResearchDaysLeft()` 以 `startedDay + days` 為到期日；`advanceCafeResearch()` 未到期回原參考，
    到期後清空 active、去重 append completed。未知 future id 不擅自吞掉玩家投入，而是保留原資料並回提示。
- **平衡決策**：本批不偷跑 CAFE-17 的菜單／客單價，`cafeResearchTicketBonus()` 仍為 0，`CAFE_BASE_TICKET`
  $36 與 `CAFE_FIXED_COST` $370 都不變。重新驗算發現舊交接的「單一 +8% 約 43%」低估：取整後客單價
  $39 會讓成熟期約多 $119／日，$398 → 約 $517，即日租金 $1,017 的約 50.8%；因此留給 CAFE-17
  連同菜單效果重新定標，不能直接疊加。
- **驗證**：
  - 新增並登錄 `cafe-research-test.ts`：42/42，涵蓋資料樹、研究／設備前置、所有交易拒絕、輸入不變、
    單一 active、N／N−1／0 日邊界、完成去重／冪等、future id 保留、零 RNG 與純函式邊界。
  - 咖啡廳相關回歸 12/12 支全綠；最終 `npm test` 為 81/81 支全綠（80 支正式測試＋sim-trace）。
  - `npm run typecheck` 的 app／Worker 通過；`npm run build` 為 148 modules transformed，只有既有
    大於 500 kB chunk 提示；`balance-test.ts` 快照一致，未使用 `--update`。
  - 本批沒有 UI／CSS／畫布變更，因此不重跑 Mobile UI Lab。
- **已知限制／後續**：CAFE-16 是可持久化的資料與純命令層，目前玩家還沒有按鈕可開始研發，也沒有
  自動 tick 接線。下一步 CAFE-17 應定義菜單與安全的客單價／客群效果；CAFE-18 再把開始、倒數與完成
  接進 `CafePanel.vue`，成功後透過既有 `addMoney(..., "cafe")` 與 `save()` 落帳。
- **版本狀態**：`main`，基準 `8d753d8`；本批已完成待提交，尚未 push／deploy。

### 2026-08-02 — CAFE-17 菜單、客群標籤與安全客單價：$36／$37／$38 里程碑（Codex）

- **目的／範圍**：接續 CAFE-16，讓 `state.cafe.completed` 不再只是研發 id，而能穩定產生菜單與實際
  日結客單價；沿用同一個 `main`，未修改 tick、CafeState、顧客意圖比例或任何 UI。
- **菜單資料**：
  - 開張即有招牌美式咖啡 $34、每日烘焙小點 $36、基礎寵物小點 $38，三項平均正好維持既有
    `CAFE_BASE_TICKET = $36`，也涵蓋 brew／bake／pet 三類既有原料。
  - `src/content/cafeResearch.ts` 的 10 項研發各補一個標價與 audience：日常客、單品愛好者、拍照客、
    冰飲客、甜食客、下午茶客、親子／打卡客、寵物家庭、毛孩同行客與慶生客。這些是菜單結構標籤，
    刻意不改 CAFE-07 的 `coffee / adopt / rent = 70 / 20 / 10`，避免污染認養與未來招租。
  - `menuItems(completed)` 以 Set 去重、忽略未知 id，再按 `CAFE_RESEARCH` 宣告順序輸出；completed
    的存檔順序不會改菜單順序。完整前兩層是 3 個基礎＋10 個研發品項，生日蛋糕保留特殊事件旗標。
- **客單價定標**：
  - `avgTicket()` 採第二層菜單多樣性里程碑：完成 0～1 項維持 $36、2～4 項升 $37、5 項以上 $38；
    `CAFE_MAX_AVG_TICKET = 38` 是硬上限。根節點建立客群但不直接加價，任何完成順序都只升不降。
  - `cafeTicketPrice()` 保留既有簽名並直接回 `avgTicket()`，所以 `tick.ts` 已有的
    `cafeTicketPrice(cafe.completed)` 自動生效，不需要改日結；`cafeResearchTicketBonus()` 則由平均價反推，
    未知／重複 id 無法灌水。
  - 56 日實際日結、成熟投資與人氣滿：無研發 `$398／日 = 日租金 39.1%`；完整研發
    `$477／日 = 46.9%`。研發增加 `$79／日`，仍在設計的 30～50% 目標帶，也遠低於日租金 `$1,017`。
- **驗證**：
  - 新增並登錄 `cafe-menu-test.ts`：23/23，涵蓋基礎／完整菜單、順序、unknown／duplicate、標價、客群、
    特殊事件、$36／$37／$38 門檻、1,024 種 completed 組合硬上限、合法進度單調、零 RNG 與 56 日日結。
  - CAFE-16 測試同步把歷史性的「滿研發仍 $36」改為正確的「零研發無未賺取加成」，仍為 42/42。
  - 咖啡廳相關回歸 13/13 支全綠；最終 `npm test` 為 82/82 支全綠（81 支正式測試＋sim-trace）。
  - app／Worker typecheck 通過；production build 148 modules，只有既有大於 500 kB chunk 提示；
    balance snapshot 一致，未使用 `--update`。
  - 本批沒有 Vue／CSS／畫布變更，因此不重跑 Mobile UI Lab。
- **已知限制／後續**：一般玩家目前仍沒有研發操作入口；CAFE-18 應把 `startCafeResearch()`、
  `cafeResearchDaysLeft()`、`advanceCafeResearch()`、`menuItems()` 與 `avgTicket()` 接進 `CafePanel.vue`，
  並在成功開始時走既有 `addMoney(..., "cafe")` 與 `save()`。本批沒有偷跑該 UI。
- **版本狀態**：`main`，基準 `284f95f`；本批已完成待提交，尚未 push／deploy。

### 2026-08-02 — CAFE-18 研發面板 UI：開始、倒數、到期新品與可讀研發路線（Codex）

- **目的／範圍**：接續 CAFE-16／17，把已完成的純研發交易、倒數、菜單與客單價正式接進玩家可操作的
  咖啡廳營運面板；沿用同一個 `main`，不改 App、tick、CafeState schema、存檔版本或顧客意圖比例。
- **研發操作與狀態**：
  - `src/components/CafePanel.vue` 新增「新品研發」卡；顯示已知完成數、十項前兩層路線、研究／設備前置、
    成本、遊戲日數、解鎖菜單與客群。可開始項才啟用；資金不足、前置未滿或已有 active 都不能送出。
  - 成功開始時先取得 `startCafeResearch()` 的純交易結果，再 `Object.assign(state.cafe, result.cafe)`、
    走既有 `addMoney(..., "cafe")` 扣款並 `save()`；失敗不扣款。active 狀態只顯示目前名稱、剩餘日、
    投入金額與 0～100% 進度，其餘九項暫時收起，避免手機抽屜過長。
  - 面板開啟時監看既有 `gameDayIndex()`；到期只在 `advanceCafeResearch().changed` 時寫回、存檔並提示，
    完成數只計 CAFE_RESEARCH 已知 id，進度分母與百分比也對舊／異常存檔做夾值。
- **菜單呈現**：新增「目前菜單」卡，固定順序顯示三個基礎品項與已完成研發新品的售價／客群，並顯示
  `avgTicket()` 的目前平均客單與第二層完成 2／5 項跨 `$37／$38` 的里程碑；沒有另寫一套價格公式。
- **設計／交接**：`docs/一樓寵物咖啡廳-設計.md` 新增 CAFE-18 面板與資料流說明；工作分解把 CAFE-18
  標為完成，現況更新為 CAFE-01～18 + CAFE-22 完成。明確留下範圍界線：面板關閉時沒有全域 watcher，
  快轉／離線到期會在下次開面板 lazy settlement；若要在背景日結前套用新品客單價，另立 tick 接線工作。
- **驗證**：
  - 專項：`cafe-research-test.ts` 42/42、`cafe-menu-test.ts` 23/23。
  - 完整回歸：`npm test` 82/82 支全綠；app／Worker typecheck 通過；production build 148 modules，只有
    既有大於 500 kB chunk 提示；`balance-test.ts` 快照一致，未使用 `--update`。
  - Mobile UI Lab：390×844、412×915、430×932 的樓層／動態／收支共 18 張 PNG 全部逐張檢視；
    `report.json` 為 0 errors／0 warnings，既有樓層按鈕、地圖與收支面板沒有溢出或裁切。
  - 本機瀏覽器實際走完既有已開店存檔：`$49,315 → $46,815` 開始「基礎沖煮」，面板依序顯示
    `2 天／0% → 1 天／50% → 完成 1/10`，完成提示與「本日配方咖啡 $36」同步出現；收支頁有
    `咖啡廳研發:基礎沖煮 −$2,500`。390／412／430px 的 panel/body `scrollWidth === clientWidth`，
    console 0 errors／warnings。
- **已知限制／後續**：CAFE-18 按工作分解只修改 UI，因此不在面板關閉時背景結算，且歷史日結不回溯；
  下一個既定擴充是 CAFE-19「租屋意圖 → 招租」，也可先另開一項研發 tick 接線再進顧客線。
- **版本狀態**：`main`，基準 `bd8fa43`；本批完成待提交，尚未 push／deploy。

### 2026-08-02 — CAFE-18B 背景研發結算：面板關閉也會到期、當日營收立即吃新品（Codex）

- **目的／範圍**：補上 CAFE-18 明確留下的 lazy settlement 缺口；玩家關閉咖啡廳面板、使用快轉或
  離線補進度時，研發也會自然完成。沿用同一個 `main`，不改 CafePanel、CafeState schema、SAVE_VERSION、
  研發成本／天數、顧客意圖或 balance snapshot。
- **實作**：
  - `src/sim/tick.ts` 的 `cafeDailyPass()` 保留「未開張立即 return」零漂移閘門，在 `gameDayIndex()`
    取得後、客流與營收之前呼叫既有冪等 `advanceCafeResearch()`。只有 `changed === true` 才
    `Object.assign` 寫回既有 reactive cafe 並透過 `notify()` 留一則完成通知。
  - 完成順序刻意早於 `cafeTicketPrice(cafe.completed)`，所以達成第 2／5 項第二層的當天日結就直接使用
    `$37／$38`，不必等隔天，也不回溯先前 history。未到期、已完成重跑與未知 future id 都不修改／不通知。
  - 沒有在 `cafeDailyPass()` 內另呼叫 `save()`；正常 `syncToNow()`、同步快轉與 UI 分批快轉本來就在整批
    hourly ticks 後統一保存，避免日結中途多一次 localStorage 寫入。`noticeLog` 與 cafe 會一起保存。
- **測試**：
  - `scripts/cafe-daily-test.ts` 從 86 項擴至 97 項：涵蓋 direct daily pass 的到期去重、單次通知、零 RNG、
    未到期不動，以及第二個第二層研發到期時同日 `revenue === guests × $37`；另真正從當地 23:00 執行
    `hourlyTick(false)` 跨午夜，證明沒有掛載 CafePanel 仍會完成、通知一次且同筆 history 已吃新客單價。
  - `cafe-research-test.ts` 42/42、`cafe-menu-test.ts` 23/23；`npm test` 82/82 支全綠。
  - app／Worker typecheck 通過；production build 148 modules，只有既有大於 500 kB chunk 提示；
    `balance-test.ts` 快照一致，未使用 `--update`。
- **文件／視覺界線**：設計文件與工作分解新增 CAFE-18B，移除「面板關閉只能下次開啟才補結算」的現況限制。
  本批沒有 Vue、CSS、canvas 或可見版面變更，因此依專案規則不重跑 Mobile UI Lab。
- **時間語意**：仍沿用 CAFE-16 存檔既定的 `gameDayIndex`（22:00 日界）；面板開著時最早 22:00 由 watcher
  結清，面板關閉時最遲當地午夜由 cafe daily pass 結清，兩條都保證午夜營收使用新品。
- **已知限制／後續**：歷史營收不回溯重算；異常存檔若 `open=false` 卻有 active research，仍由未開張閘門
  保留不動，正常 UI 不會產生此狀態。下一個既定擴充回到 CAFE-19「租屋意圖 → 招租」。
- **版本狀態**：`main`，基準 `4249155`；本批完成待提交，尚未 push／deploy。

### 2026-08-02 — 積怨自然口角、房間隔音折抵與衝突額度語意收斂（Codex）

- **目的／範圍**：補齊積怨軸的後續平衡：相性中性／良好的配對也會因既有積怨提高自然口角機率；
  房間隔音只折抵噪音相性造成的自然口角壓力；每日兩場上限明確只管自然口角與打架，不吞掉分手、
  社群口角或待決群體事件。沿用目前四個咖啡研發 commit 之上的 `main`，咖啡邏輯與文件均未修改。
- **機率與 RNG 護欄**：
  - `CompatibilityDetail` 新增結構化 `hasNoiseConflict`，不靠中文原因字串判斷噪音來源。
  - 自然口角拆成 `0.03 + compatibilityPressure + tensionPressure - noiseReduction`，最後夾在 3%～65%；
    負相性配對的 `compatibilityPressure = 0.01 + (-comp) × 0.08`，並完整保留既有
    `tension × 0.0035` 加壓。只有相性中性／良好的配對採 `max(0, tension - 60) × 0.0035`：
    0～60 點維持舊 3%，接近冷戰風險後才平滑增加（65 點 +1.75pp、70 點 +3.5pp、100 點 +14pp），
    避免單次事件或一般公審讓原本合拍的配對連鎖惡化。
  - 保留原本無條件的一次 conflict roll 與分支內 RNG 次序；沒有新增、刪除或提前跳過亂數呼叫。
- **隔音折抵**：`src/sim/acoustics.ts` 新增決定性的 `noiseConflictMitigation()`；只讀造成噪音者
  （`noisy`／`gamer`）的房間，比例為 `soundproof / 8` 夾在 0～1。$3,000 局部工程 4 點折半，
  $12,000 完整改建 8 點全抵；若雙方同時是來源則平均兩間，沒有房間的來源按 0。該比例由
  `socialPass()` 注入 `encounter()`，不讓純社交模組反向 import store，也不改噪音公審既有硬阻隔規則。
- **額度語意**：`EncounterResult.naturalConflict` 只在隨機自然口角分支為 true；`socialPass()` 只用它
  累計每日額度與嘗試升級冷戰。分手雖維持 conflict tone／心碎演出，但不再占額度或誤接冷戰；腳本與
  群體衝突刻意留在額度外，避免已排程事件或玩家稍後解算的待決事件被錯算到另一個遊戲日。
- **驗證**：
  - `relationship-tension-test.ts`：含 3%／20% 精確邊界、負相性 tension=60 維持既有 41%、
    正相性 tension=60 仍為 3%、tension=65 提升至 4.75%、結構化噪音旗標，以及節流時仍為既有
    三次 RNG 呼叫；最終 **22/22**。
  - `acoustics-laundry-test.ts` 24/24：含來源房 0／4／8 點隔音、安靜方房間不反向折抵、
    雙向來源取平均、無房來源按 0、非噪音配對為 0；
    `community-test.ts` 50/50：含分手與腳本口角不占每日額度；`conflict-test.ts` 41/41。
  - `npm test` **82/82** 支全綠；`balance-test.ts` 與既有固定種子快照一致。本批**沒有**使用
    `--update`，也沒有修改 `scripts/balance-snapshot.json`。
  - app／Worker typecheck 通過；production build 148 modules，只有既有大於 500 kB chunk 提示；
    `git diff --check` 通過。本批沒有 Vue、CSS 或 canvas 變更，因此不重跑 Mobile UI Lab。
- **版本狀態**：`main`，基準 `50db482`；本批待提交，尚未 push／deploy，並完整保留先前四個咖啡研發 commit。

---

## 新紀錄追加點(APPEND HERE)

> ⬇️ **下一則日期紀錄請直接加在本節之下**(檔案最末端)。格式:
> `### YYYY-MM-DD — 主題(執行 Agent)`,**全條 ≤15 行**,含:
> 目的／需求、實際變更與主要檔案、驗證結果、已知限制或後續。
>
> 加完後請順手做兩件事:
> 1. 更新根目錄 `STATUS.md` 的「現在狀態」與「下一步」,**與程式碼放同一個 commit**。
> 2. 若本次動到待辦狀態,同步更新 `docs/待辦.md` 的 `- [ ]` / `- [x]` 與 🔍 驗收錨點。

<!-- 新紀錄從這裡開始 / APPEND NEW DATE ENTRIES BELOW -->

### 2026-08-02 — 文件重整:新增 STATUS.md 作為唯一必讀入口(Claude Code)

- **目的**:使用者反映每開新 session 讓 agent 了解進度都要吃掉大量 token。本檔當時
  427 KB／4738 行,而 `CLAUDE.md`／`AGENTS.md` 都寫「開始工作前先閱讀 `工作日誌.md`」。
- **變更**:新增 `STATUS.md`(106 行,現況／下一步／待決策／阻塞／文件地圖/常用指令)為唯一必讀入口;
  本檔第四、五節拆出 `docs/待辦.md`,第一～三節拆出 `docs/系統總覽.md`,07-31 以前逐日紀錄
  移入 `docs/工作日誌-封存.md`(本檔 427→153 KB);`docs/交接與待辦.md` 併入待辦後 `git rm`;
  `CLAUDE.md`／`AGENTS.md` 改為只讀 `STATUS.md` 並新增「每筆日誌 ≤15 行」與 STATUS 同步規則;
  新增 `.claude/commands/status.md`(`/status`)。順修 4 處過期敘述(咖啡廳設計狀態列、
  工作分解部署狀態、隔音技術債錨點指錯檔、`map.ts` 網格 16×33→16×52)。
- **驗證**:`npm test` 82/82、app+worker typecheck 通過、build 成功、balance 快照一致(未 `--update`)。
  逐日條目數 131 筆重整前後相同,搬移區段以 `diff` 逐字對帳。本批無程式碼變更,不重跑 UI Lab。
- **已知限制**:編輯過程把本檔與封存檔正規化為 CRLF,`git diff` 差異看起來偏大,用 `-w` 檢視。
  `docs/交接與待辦.md` 第九～十八節的早期平衡推導理由未搬,用 `git show 8d753d8:docs/交接與待辦.md` 取回。

### 2026-08-02 — CAFE-19 租屋意圖 → 招租(Claude Code 子 Agent)

- **目的**:`intent === "rent"` 顧客一直只生成、沒人消費;本項讓他能變成帶初始好感的應徵者。
- **變更**:`src/sim/recruit.ts` 新增唯一入口 `acceptCafeGuestApplicant(guest, roomId)`(比照 CAFE-09
  認養入口回 `{ ok, text }`),組成 `Applicant` 追加進 `state.applicantPools[roomId]`;初始好感做成
  **偏好權重 ×1.25、單項上限 10**(不是直接加星等),才不會被 `rescoreApplicants()` 覆寫 `stars` 時抹掉;
  `Applicant` 加 `fromCafeGuestId`／`cafeFavorMultiplier` 選填欄位。`src/sim/cafeGuests.ts` 新增
  `cafeGuestGender()` 綁定姓名與性別。新增 `scripts/cafe-rent-intent-test.ts` 並登記進 `run-all.ts`。
- **驗證**:新測試 26/26(拒絕條件、欄位相容、加成與上限、重算後加成不消失、決定性零 `Math.random`、
  冪等與池量上限);`npm test` **83/83**、app+worker typecheck、build 通過;balance 快照零漂移
  (未 `--update`)。本批無 `.vue` 變更,不跑 UI Lab。
- **已知限制／後續**:UI 未接線(畫面上還叫不到入口)、顧客不會自動從咖啡廳離場,兩者留給後續;
  池上限 6 位,跨房同名與同一顧客重複接受皆擋下。下一步 CAFE-20／CAFE-21。

### 2026-08-02 — CAFE-19 UI 接線:咖啡廳「租屋詢問」卡(Claude Code 子 Agent)

- **目的**:上一批的 `acceptCafeGuestApplicant()` 沒有呼叫端,被 Rollup tree-shake,production
  bundle 裡根本沒這段程式碼;本批把 UI 接上,功能才真的能用。
- **變更**:`src/components/CafePanel.vue` 新增「租屋詢問」卡(完全比照同檔認養卡,改藍紫色系):
  `rentGuests`／`vacantRooms`(`ROOM_APPEARANCE` ∩ `isVacant`,顯示成「303 房」)兩個 computed,
  `onAcceptRentInquiry()`／`onDeclineRentInquiry()` 兩個 handler,重用既有 `removeHandledGuest()`;
  沒空房時警示且「安排看房」disabled。下拉改 `:value` + `@change`(空 `v-model` 會顯示空白列),預設＝第一間空房。
- **驗證**:`npm test` **83/83**、app+worker typecheck、build 通過、balance 快照零漂移(未 `--update`);
  關鍵錨點 `grep -F "這位顧客目前沒有租屋意願" dist/assets/*.js` **命中**(tree-shaking 已解除)。
  UI Lab `npm run ui:shot -- rent` 18 張 0 error 0 warning,另以容器內 Playwright 自訂流程實拍
  租屋卡的四種狀態(兩位顧客／已選房／接受後／無空房)存於 `artifacts/ui-lab/rent/cafe-19-rent-intent/`。
- **已知限制／後續**:顧客仍不會自動離場;認養卡的下拉維持原本 `v-model` 寫法(空值同樣會顯示空白列),
  本批未一併調整以免影響既有行為。下一步 CAFE-20／CAFE-21。

### 2026-08-02 — CAFE-20 `at_cafe` visualState:租客下樓坐咖啡廳(Claude Code 子 Agent)

- **目的**:本案唯一新增 visualState、也是漂移風險最高的一項;規格要求設 `CAFE_FIRST_DAY` 閘門。
- **變更**:`types.ts` 加 `at_cafe`(僅此一個)。`sim/routine.ts` 是唯一插入點——`routineSlot()` 在
  **兩道閘門 AND**(`state.cafe.open` + `gameDayIndex() >= CAFE_FIRST_DAY(14)`)且非 `pendingEvent`
  時回傳 `CAFE_SIT_SLOT`,**不動 `tick.ts` 一行**;挑日子/時段走 `cafeSitHourForDay()`
  (`(day+stableTenantHash)%5`、10～19 點候選、避開睡眠外出浴室洗衣),零 `Math.random`、
  不迭代 `state.runtimes`;新增 Role `cafe` 把目標錨在一樓 `cafe_floor`。`observationLines.ts`
  補 13 句;`DollhouseView.vue`/`PixelDollhouse.vue` 各補一行標籤(完整 Record,不補則 typecheck 紅)。
  `data/routines.json` 未動,且 `expandSpans()` 主動擋掉寫死的 `at_cafe`。
- **驗證**:新增 `scripts/cafe-at-cafe-test.ts` 22/22 並登記進 `run-all.ts`(含「第 0～9 遊戲日即使
  咖啡廳已開張也產不出 `at_cafe`」的零漂移釘子);`npm test` **84/84**、typecheck、build 通過;
  balance 快照**零漂移**(未 `--update`);UI Lab 18 張 0 error 0 warning,bundle hash 與本機一致。
- **已知限制/後續**:renderer 不在允許清單 ⇒ 角色會走到一樓但只有站姿、無專屬演出;房間細看表現
  同 `away`(只換標籤);`EFFECT` 表無 `at_cafe` ⇒ 該小時數值增量為 0。三項留後續。下一步 CAFE-21。

### 2026-08-02 — CAFE-21 cafe venue 與 `at_cafe` 數值效果:咖啡廳 22 項收尾(Claude Code)

- **目的**:補完咖啡廳最後一項工作項,並補上 CAFE-20 留下的「數值真空」。
- **變更**:`groupScene.ts` 的 `GroupSceneVenue` 加 `"cafe"`,走 `lounge` 那條路線(真實座標、
  `hidden: false`)。新增 `cafeTiles()`,**只收 grid 上真的是 `cafe_floor` 的空格**——
  `cafe_counter`／`cafe_pet` 會覆蓋掉矩形一部分,只看 blocked 會把吧台內側與寵物區誤當座位。
  既有的 `venue === "lounge"` 三元判斷改走 `isPlacedVenue()`／`venueTiles()`,對 lounge 與
  rooftop 等價。`generate.ts` 的 `EFFECT` 新增 `at_cafe: { mood: 3, stress: -3, energy: 1,
  wellbeing: 0.3 }`,既有數值一格未動。
- **驗證**:新增 `scripts/cafe-venue-effect-test.ts` 31/31 並登記進 `run-all.ts`(含 lounge/rooftop
  走位黃金值回歸釘子、既有 EFFECT 抽驗、第 0～9 遊戲日仍 0 次 `at_cafe`);`npm test` **85/85**、
  typecheck、build 通過;balance 快照**零漂移**(未 `--update`);UI Lab 18 張 0 error 0 warning,
  容器 bundle hash `index-CLW4jlOi.js` 與本機一致。
- **已知限制/後續**:renderer 仍無 `at_cafe` 專屬演出(坐姿+咖啡杯)與房間細看表現,留後續工作項。
  本項規格註明優先讓 Codex 認養,當時無 Codex session,經使用者指示由 Claude Code 完成。

### 2026-08-02 — 咖啡廳家具玩法化 + toast 被面板蓋住的 bug(Claude Code)

- **目的**:使用者實玩回報「不知道咖啡廳怎麼開始,一樓沒有任何家具」「套用常備量按了沒反應」。
- **變更**:①`App.vue` 的 `.toast` `z-index` 50 → **200**——面板 overlay 是 100～140,
  先前只要有面板開著 toast 就永遠看不到(常備量其實有存檔),是全 app 的 bug 不只咖啡廳。
  ②`sim/placements.ts` 新增 `placeCafeStarterSet()`(寫死座標、逐件過 `canPlaceFree()`)與
  `cafeAmbiancePoints()`;`CafePanel.onOpen()` 開張成功後**免費**擺上吧台 ×1 + 桌 ×3 + 椅 ×6。
  ③`sim/cafe.ts` 新增純函式 `cafeAmbianceMultiplier()`(`SWING = 0.2`、`FULL_POINTS = 60`),
  由 `tick.cafeDailyPass()` 餵 `ambiancePoints` 進 `cafeCrowd()`,`cafe.ts` 的純函式界線未動;
  設計理由寫進 `docs/一樓寵物咖啡廳-設計.md` §5.6 §5.7。
- **驗證**:新增 `scripts/cafe-furniture-test.ts` 38/38 並登記進 `run-all.ts`;`npm test` **86/86**、
  typecheck、build 通過;balance 快照**零漂移**(未 `--update`);UI Lab 18 張 0 error 0 warning、
  容器 bundle `index-X76Hd5OO.js` 與本機一致;另以容器內 Playwright 自訂流程實拍 6 張
  (`artifacts/ui-lab/rent/cafe-furniture/`),`elementFromPoint` 驗到 toast 確實在面板之上。
- **中控收尾**:子 Agent 回報的變賣套利(贈品零售 $39,400、拆賣回收 $19,700 > 開張費 $12,000
  ⇒ 一次性淨賺 $7,700)當批直接修掉:**開張費調到 $22,000**,拆賣改為穩虧 $2,300。
  `cafe-upgrade-test.ts` 原「開張費仍買得起吧台」斷言的前提(玩家自購吧台)已不成立,
  改寫成「仍買得起第二台咖啡機」+ 新增反套利下界斷言,86/86 全綠、balance 仍零漂移。

### 2026-08-03 — 舊存檔補發咖啡廳開張贈品(SAVE_VERSION 6→7)(Claude Code)

- **目的**:使用者實玩回報「打開全部空的,不知道如何開始」。追出原因:上一批的
  `placeCafeStarterSet()` **只在按下開張的那一刻呼叫**,且沒有任何 migration ⇒
  **在該功能上線前就已經開張的存檔永遠拿不到贈品,一樓會永遠是空的。**
- **變更**:`placements.ts` 抽出 `starterPlacementsAgainst(existing)` —— 不碰模組狀態的版本
  (升級是在 `placements.list` 還原**之前**對序列化存檔動手,那時不能用讀模組的 `canPlaceFree()`);
  `persistence.ts` 加 `MIGRATIONS[6]`:已開張且一樓無咖啡廳家具才補,玩家已自行佈置則尊重現況。
  只跑一次(升完 `v=7` 不再回到本函式),所以事後搬走/賣掉不會被塞回來。
- **驗證**:`cafe-furniture-test.ts` 加 8 條(補發/未開張不補/已佈置不補/不重複補/純函式不碰模組/
  贈品互不重疊),45/45;`npm test` **86/86**、typecheck、build 通過;balance 快照零漂移。
- **順帶改寫兩條釘住舊前提的斷言**:`cafe-state-test.ts` 的 `SAVE_VERSION === 6`
  (原意是「CAFE-11 的 additive 欄位不該升版」,但資料補發本來就必須升版)改成
  「每個舊版都升得到 SAVE_VERSION」的行為斷言;「存檔版本仍寫入 6」改成跟著 `SAVE_VERSION` 走,
  不再寫死數字。兩條都不是改數字了事,是把意圖重新表達。

### 2026-08-03 — 補發守衛誤擋修正 + v7→v8 追補(Claude Code)

- **目的**:使用者問「我的咖啡廳已經放置一個貓跳台了,這樣是不是就不會補家具?」——是,會被擋掉。
- **變更**:①`MIGRATIONS[6]` 原本有「一樓已有任何咖啡廳家具就整組跳過」的守衛,放一件貓跳台
  (`cat_tower`／`cafe_cat_tower` 都是可擺放家具)就拿不到任何補發。「有一件家具」與
  「已經佈置好了」是兩回事,而防覆蓋本來就由 `starterPlacementsAgainst()` 逐格重疊檢查負責
  ⇒ 該守衛既太粗又多餘,直接移除。②**v7 已經上線過**,受影響的存檔已被標成 v7、
  升級表不回頭 ⇒ 光修 `MIGRATIONS[6]` 對他們永遠不生效,必須加 `MIGRATIONS[7]` 追補。
  重跑安全:已拿到贈品的存檔座標全被佔住,一件也不會多加。
- **驗證**:`cafe-furniture-test.ts` 加 5 條(貓跳台情境、佔住座標只跳過該件、卡在 v7 的存檔
  補齊、已補過的不重複補),49/49;`npm test` **86/86**、typecheck、build、balance 零漂移。
- **教訓**:`SAVE_VERSION === <數字>` 這種斷言一天內壞了兩次,每次都只是把數字往上改一格,
  擋不到任何真正的錯誤 —— 已改成行為斷言「每個舊版本都升得到 `SAVE_VERSION`」並刪掉寫死的那條。

### 2026-08-03 — 家具商店的租屋／咖啡廳分頁(Claude Code)

- **目的／使用者需求**:實玩回報「把租房的家具和咖啡廳的家具做個分類」——12 件咖啡廳家具
  原本散在 8 個 `FurnCategory` 裡,佈置一樓得在臥室家具中間翻找。
- **變更**:`catalog.ts` 加選填欄位 `venue`(`FurnVenue = "rent" | "cafe"`,未標 = 租屋)並標記
  CAFE-06 那 12 件,另出 `venueOf()` / `isShopListed()` 給商店與測試共用;`FurnitureShop.vue`
  header 下加「🏠 租屋樓層 49 / ☕ 咖啡廳 12」分頁(空類別不出標題、切頁捲動歸零);
  `FloorMap.vue` 加 `floorChange` emit、`App.vue` 據此讓 1F 開商店預設咖啡廳分頁。
  判準用顯式欄位不用 `id` 前綴——`espresso_machine` 就是沒有 `cafe_` 前綴的咖啡廳家具。
- **驗證**:新增 `scripts/shop-venue-test.ts`(28 條,含「所有 `cafe_*` 都標了 venue」防漏標、
  49+12=61 不重不漏、牆面與 $0 紀念物仍不上架)並登記進 `run-all.ts`;`npm test` **87/87**、
  typecheck、build 皆過,balance 快照零漂移(未用 `--update`);UI Lab 18 張 0 error 0 warning,
  另在 `artifacts/ui-lab/rent/shop-tabs/` 實拍 7 張兩分頁切換與 1F 預設分頁。
- **已知限制**:`venue` 只影響商店分頁,不擋擺放——玩家仍可把咖啡廳家具放到三樓(刻意保留)。

### 2026-08-03 — 咖啡廳 renderer 收尾:at_cafe 的坐姿與咖啡杯(Claude Code)

- **目的／使用者需求**:CAFE-20 遺留的未竟事項——租客會下樓走到一樓咖啡廳,但只有站姿、
  沒有咖啡杯,房間細看表現同 `away` 只換標籤。
- **變更**:`catalog.ts` 加**選填** `seat?: "on" | "at"`(`on`=跨上家具、`at`=坐在家具前補椅子),
  標記咖啡廳兩款椅(`on`)與小圓桌(`at`);`tick.ts` 的 `setFurniturePose()` 第一行改成
  「沒 `kind` **且**沒 `seat` 才 return」,`SEATED_STATES` 加 `at_cafe`(**不對 defId 特判**);
  `floorScene.ts` 加 `drawCoffeeCup()`(純程序像素,零新圖檔)+ 熱氣 + 空咖啡廳時補畫
  `drawCafeChair()`(配色抄 LimeZu atlas);`pixel/scene.ts` 的 `drawTenant()` 加 `at_cafe` 分支。
- **驗證**:新增 `scripts/cafe-seat-pose-test.ts`(45 條,第一節用「全目錄 × 全 visualState ×
  4 rotation × 4 fallback = 24576 格」比對照抄舊邏輯的參考實作,釘死既有姿勢);`npm test`
  **88/88**、typecheck、build 皆過,balance 快照零漂移(未用 `--update`);UI Lab 18 張
  0 error 0 warning,另在 `artifacts/ui-lab/rent/cafe-renderer/` 實拍坐姿 + 杯子 + 房間細看。
- **已知限制**:`pixel/scene.ts` 的 `drawTenant()` 已被 app bundle tree-shake(只剩 render-preview
  在用),玩家看到的房間細看是 `PixelDollhouse`;另發現既有的 `.pixel-room` 2px 壓扁(見 `STATUS.md`)。

### 2026-08-03 — 修復房間細看在手機上被壓成 2px(既有 bug)(Claude Code)

- **目的**:咖啡廳 renderer 那批的實作 Agent 順帶發現 `.pixel-room` 在受限視窗高度下塌陷。
  中控用 Lab 容器實測驗證(非僅採信回報)。
- **根因**:父層 `main.room-main` 是 `flex:1; min-height:0; flex-direction:column` 的捲動容器,
  `.pixel-room` 是它的 flex item 卻沒有 `flex-shrink: 0`。加上自身 `line-height: 0` 與
  「canvas 是 `height:auto` 的取代元素」,min-content 高度算成 0 ⇒ 自動最小尺寸擋不住收縮。
  canvas 本身仍是 312px,但容器被壓到只剩上下 border、又 `overflow: hidden` ⇒ **玩家全看不到**。
  canvas 註解寫的「絕不塌成一條線」只在非 flex 脈絡成立,單靠它擋不住。
- **變更**:`PixelDollhouse.vue` 的 `.pixel-room` 加 `flex-shrink: 0`,並把上面這段原因寫成註解。
- **驗證**:容器內 Playwright 量 `getBoundingClientRect()`,390×844 進房間細看 ——
  **修正前 2px / `flex-shrink: 1`,修正後 314px / `flex-shrink: 0`**;
  baseline 是 `git stash` 掉修正後重建量的,確認不是本批引入。`npm test` 88/88、typecheck、build 通過。
- **為何舊截圖沒抓到**:歷來 `ui:shot` 與自訂實拍都用 `fullPage`,fullPage 會把版面撐開,
  正好繞過這個只在受限視窗高度出現的塌陷。

### 2026-08-04 — 咖啡廳重設計 P1:逐位顧客結帳(Claude Code)

- **目的**:使用者實玩後「他們有點餐嗎 他們有消費嗎 什麼都不知道」。營收原本是換日時
  `客流 × 平均客單價` 一條公式,與畫面上的顧客完全無關。P1 讓帳本等於畫面上發生的事。
- **變更**:`cafe.ts` 新增配方/熱門度/選品/逐位結帳算式(純函式,零 RNG,FNV-1a 選品);
  `cafeIngredients.ts` 六種單價與常備量全部重訂(低單價 × 多單位,13 品項毛利 48.7～57.9%);
  `tick.ts` 新增 `cafeHourlyPass(hour)`(掛 `cafeGuestPass` 後),`cafeDailyPass` 縮編成
  進貨/固定/損耗/寫 history;`types.ts` + `gameState.ts` 新增 `cafe.sales`(cap 14 天);
  `persistence.ts` `SAVE_VERSION 8 → 9` + `MIGRATIONS[8]`。**未動任何 `.vue` 與 `floor/*`**。
- **驗證**:新增 `scripts/cafe-per-guest-test.ts`(65 條,含**離線一致性對打**:線上逐時 11 小時
  vs `syncToNow()` 一次補 11 小時,money/stock/popularity/sales 逐欄相同);`npm test` **89/89**、
  typecheck、build 通過;**balance 快照零漂移,未用 `--update`**。
- **已知限制**:§4.7 實測(`scripts/cafe-opening-sim.ts`)補貨精準 **+$95**(設計 +$98)吻合,
  但缺貨 **−$0**(設計 −$89)、備貨過量 **+$59**(設計 −$114)——原因寫在重設計文件 §4.7,
  未擅自調參數,待使用者拍板是否加碼懲罰。P2 才會把畫面接上去。

### 2026-08-04 — 咖啡廳重設計 P2:顧客動線、點餐演出與開店關店(Claude Code)

- **目的**:P1 之後「畫面上的顧客」與「結帳的顧客」仍是兩批人,整個重設計的主張是空的。
- **變更**:每一筆結帳都 `spawnCafeGuestForOrder()` 生出一位帶著該訂單的可見顧客
  ⇒ **合流完成**。`GuestAgentPhase` 擴充成 `entering → ordering → served/refused → seated
  → leaving`;`drawGuestInfo` 依 phase 分派:點餐泡泡(商品圖示 + 價格)、`+$XX` 浮字、❌ 泡泡。
  **廢除硬編的 `CAFE_GUEST_PREFERRED_SEATS`**(它正是「顧客站在空地板」的根因,贈品刻意避開那六格),
  改查 `placements` 的 `seat` 欄位;沒座位的自動變外帶。`drawGuest` 加 `guestSeated()` 分支畫 `CHAR_SIT`。
  開店(亮)/打烊(暗)在畫面上明確可分。
- **驗證**:`npm test` **90/90**(新增 `cafe-p2-flow-test.ts` 52 條)、typecheck、build 通過;
  balance 快照零漂移;UI Lab 18 張 0 error;另實拍 6 張動線(打烊暗店/點餐泡泡 ☕$34、🍰$36/
  帶單顧客/缺貨/入座)。
- **中控收尾**:子 Agent 因 session limit 中斷於「測試未登記、UI 未拍」。接手時新測試有 2 條紅,
  查證後**兩條都是測試量錯而非程式錯**:一條用「顧客數淨變化」當「進場數」(外帶客 0.25 小時就走,
  每小時都有人離場);一條直接比對消毒前後的 `guests` 陣列(消毒用嚴格 `gameMs`、pass 用含寬限期的,
  濾得比較兇是正確行為)。兩條都改成量對的東西,不是放寬。

### 2026-08-04 — 咖啡廳重設計 P3:進貨移到開店前、損耗調校、銷售排行(Claude Code)

- **目的**:P1 實測顯示需求 3「會虧錢」不成立(缺貨 −$0、備貨過量 **+$59**)。
  根因是進貨掛在日結、且是「補到常備量」⇒ 少備料同時也少付錢。
- **變更**:新增 `tick.ts` 的 `cafeRestockPass(hour)`,09:00~20:00 之間當日尚未進貨時
  扣款一次(旗標 `CafeSalesDay.restocked` **進存檔**,比照 P1 的 `settled`);
  `cafeDailyPass()` 縮編成「固定開銷 + 對**打烊後剩餘庫存**算損耗 + 寫 history」。
  `cafe.ts` 的 `SPOILAGE_FREE_UNITS` 15→**23**、`SPOILAGE_RATE` 0.1→**0.9**(推導在常數註解);
  新增純函式 `cafeSalesRanking()` / `suggestStandingOrdersFromSales()`,`CafePanel.vue` 加
  「銷售排行」區塊與「依上週銷量建議」按鈕。
- **驗證**:`npm test` **91/91**(新增 `cafe-p3-economy-test.ts` 57 條)、typecheck、build 全綠;
  balance 快照**零漂移(未 `--update`)**;`ui:shot -- rent` 18 張 0 error + 面板實拍 6 張
  (`artifacts/ui-lab/rent/cafe-p3-panel/`)。§4.7 實測:①+$99 ②+$2 ③**−$35** ④−$346 ⑤−$132。
- **已知限制**:③ 已成立但未到設計值 −$114;② 只砍一種原料仍 +$2 是**結構性**的
  (常備訂單是「補到水位」),真正會痛的是新增對照組 ⑤「備錯料」−$132。**未硬調參數**,下一步 P4a。

### 2026-08-04 — 咖啡廳重設計 P4a:招牌分級與員工產能(Claude Code)

- **目的**:拍板 Q1「咖啡廳可以慢慢成長取代收租」。原天花板只有淨租金的 36%。
- **變更**:招牌 Lv1→Lv4(新增 `signboardLv3/Lv4` **追加在最後**,既有五項 id/順序/價格一格未動
  ⇒ 舊存檔零影響、不需升版);新增 `CafeUpgrade.requires` 強制逐級升。
  產能改成 `min(外帶底量 + 席次 × 迴轉率, 員工數 × 每人杯數)`,席次走 P2 的 `cafeSeatSpots()`;
  `CafeState.extraStaff` + `hireCafeStaff/fireCafeStaff` 純函式,薪資在日結扣、一天一次。
  客單價上限 $38→$55(硬上限保留)。**聲譽即時化 P1 就做完了,本期未重工。**
- **中控收尾**:子 Agent 誠實回報一個回歸——新產能公式讓開張期 +$99→+$195,
  連帶使 P3 剛做成立的「備太多反而虧」退回 −$35→**+$74**(不再虧)。根因是舊的
  `CAFE_BASE_CAPACITY = 26` 一直**兼任開張期的平衡錨**。損耗旋鈕已無餘裕((23,0.9) 是唯一解),
  所以把 `CAFE_STAFF_CUPS_PER_DAY` 35→**26** 還原那個錨:開張期產能回到 26(校準不變)、
  名店期 5 人 = 130 仍吃得下 129.9 客流。
- **驗證**:`npm test` **92/92**(新增 41 條)、typecheck、build、balance 零漂移。
  開張期 +$109、備貨過量 **−$23**、備錯料 −$124、放著不管 −$346(需求 3 恢復);
  名店期全設備 **$1,358 = 淨租金 125%**、過度擴張 −$254(需求 1 成立)。

### 2026-08-04 — 咖啡廳重設計 P4b:員工站上畫面、排隊、人力區塊(Claude Code)

- **目的**:使用者追問「員工看得到嗎?會看到他幫顧客結帳嗎?結帳在櫃檯嗎?」四題都是「要」。
  P4a 把員工做成數字,本期讓它看得見(重設計文件 §4.9;整個重設計到此結案)。
- **變更**:新增 `floor/staffAgents.ts`(idle 擦杯子 / serving 面向顧客+製作中泡泡 / busy 加倍節奏;
  人數只讀 `cafeStaffCount()`,打烊回空陣列)。`sim/placements.ts` 新增 `cafeStaffSpots()`
  (吧台後方)與 `cafeQueueTiles()`(往顧客側的人龍),都由吧台 footprint+interact 推軸、零硬編。
  `floor/guestAgents.ts` 的 `entering` 多一段排隊:同時結帳人數 = `min(員工數, 點餐位數)`。
  `CafePanel.vue` 新增「人力」區塊(人數/日薪、負荷進度條滿載轉紅、雇用/資遣),並修掉
  「產能 N 單」漏帶席次與員工的 bug(4 人 15 席:修前 26 → 修後 85)。**未動模擬層。**
- **驗證**:`npm test` **93/93**(新增 `cafe-p4b-staff-test.ts` 54 條)、typecheck、build 全綠、
  balance 快照**零漂移(未 `--update`)**;`ui:shot -- rent` 18 張 0 error(容器 bundle hash 一致)
  + 容器內 Playwright 實拍存 `artifacts/ui-lab/rent/cafe-p4b-staff/`。
- **已知限制**:點餐位受吧台寬度限制(贈品 2 格 = 4 位),第 5 位以上員工不再增加同時服務數;
  客流本就被 `min(base, capacity)` 夾住。設計初稿的「排太久放棄離開」會改金流,**未做**。

### 2026-08-05 — A 批:排太久放棄離開 + 姓名池 20→72 + 寵物率 0.22→0.45(Claude Code)

- **目的/需求**:使用者實玩回報三點(第四點「店貓辣椒」另開 B 批)。放棄離開由使用者拍板加入。
- **變更**:`sim/cafe.ts` 新增純函式 `cafeAbandonCount()`(一小時「想上門 − 產能」超過 8 位才有人走,
  每小時上限 3)與 `CAFE_POPULARITY_ABANDON_LOSS = 1`(`cafeServicePopularity()` 加選填第四參數);
  `sim/tick.ts` 在 `cafeHourlyPass()` 生出這批顧客並推一天最多一則日誌;`floor/guestAgents.ts`
  只加一個出口:`order.abandoned` 的人永遠拿不到點餐位,排滿 18 現實秒轉 `leaving` 走回店門。
  **$0 是「從沒收過」不是退款**——那批人本來就不在 `min(base, capacity)` 裡,金流算式一行未改。
  `sim/recruit.ts` 的 `NAME_IDENTITIES` **append** 52 筆藝名風格名字(既有 20 筆與順序一字未動;
  刻意不用真實藝人本名,因為 AI 會替角色生成私生活敘事),並把自帶寵物率抽成 `PET_CHANCE = 0.45`。
- **驗證**:`npm test` **95/95**(新增 `cafe-abandon-test.ts` 58 條、`name-pet-rate-test.ts` 25 條)、
  typecheck、build 全綠、balance 快照**零漂移(未 `--update`)**。既有六支咖啡廳平衡測試一條未改。
  實測:招牌 Lv4 只雇 1 人 ⇒ 一週 13 位放棄,雇第 2 位 ⇒ 0 位且營收翻倍;
  寵物「平均 121 天一隻 → 60 天一隻」(`scripts/pet-arrival-sim.ts`),永久樓寵物仍 2/2 不爆量。
- **已知限制**:姓名池長度改變會位移 `Math.random` 序列 ⇒ `pet-test` 的機率區間改綁 `PET_CHANCE`、
  `community-test` 的「揪團關係上升」補上「compatibility ≤ −2 依設計不加分」的分支(兩者都非回歸)。
  🔴 `adopt_cat`(撿到寵物)只由 AI 事件帶進來、規則事件目錄一則都沒有 ⇒ 實際機率 0,已列待決策。

### 2026-08-05 — B 批:店貓「辣椒」(白底虎斑,不送養、不佔寄養名額)(Claude Code)

- **目的/需求**:使用者實玩回報第四點——一隻叫辣椒的店貓,全樓溜達、能互動,但不可送養也不佔寄養欄位。
- **變更**:`types.ts` 加選填 `Pet.shopCat`;`sim/pets.ts` 新增專屬哨兵飼主 `SHOP_CAT_OWNER`
  (刻意不等於 `HOUSE_PET_OWNER` ⇒ 她本來就不在 `housePetEntries()` 這條永久名額/送養總入口裡)、
  `ensureShopCat()`(第一行 `if (!state.cafe.open) return false`,固定 record key 保唯一)、
  四條出場路徑各補明確守衛、`pickShopCatHangout()` 巡浴室/洗衣間/套房、店貓專屬觀察筆記與上工通知;
  `floor/petAgents.ts` 沿用 CAFE-22 的 `petAgentRegion()` 讓她 08–21 點下一樓(55%);
  `floor/floorScene.ts` **append** `CAT_PALS[4]` 白底虎斑(三花補丁色抽成選填、預設同原常數);
  `components/PetPanel.vue` 新增「一樓店貓」區塊(無送養按鈕)。
- **驗證**:`npm test` **96/96**(新增 `scripts/shop-cat-test.ts` 42 條)、typecheck、build 全綠、
  balance 快照**零漂移(未 `--update`)**;既有四花色像素指紋改動前後同為 `9d1cc780408535f2`。
  `ui:shot -- rent` 18 張 0 error,另用容器內 Playwright 實拍 1F/3F 辣椒本人(`artifacts/ui-lab/rent/shop-cat/`)。
- **已知限制**:她只在咖啡廳開張後存在(這是快照零漂移的前提,也是「店貓」的敘事設定);
  `SAVE_VERSION` 維持 10(選填欄位 + `ensureShopCat()` 消毒)。

### 2026-08-07 — 月度全樓事件鏈:資料抽檔 + 3 → 8 條(Claude Code)

- **目的/需求**:使用者要求「增加每月事件的文本」(只有 3 條 ⇒ 三個月就重複);規格見 `docs/待辦.md` 第四節。
- **變更**:新增 `src/content/floorChains.ts`(純資料,零 sim import)——既有 3 條**逐字搬移**,
  另加 5 條:`noise_complaint_chain`/`water_outage`/`stray_litter`/`night_market`/
  `old_resident_return`,每條 4 話、抉擇 3 選項(花錢 $900~$3,000 / 出力 bond 4 / 擺爛)、
  每階段 `notice` + 2 句變體。`sim/floorChain.ts` 只留邏輯,新增決定性旁白挑選(key 含開章日)、
  `{alumni}` 代入與 `STAGE_HOOKS`/`CHOICE_HOOKS` 掛勾表(整潔、wellbeing、積怨+清噪音記憶、
  **真的領養小貓**、咖啡廳聲譽、記憶標籤)。
- **驗證**:`npm test` **97/97**(新增 `scripts/floor-chain-data-test.ts` 45 條)、typecheck、
  build 全綠、balance 快照**零漂移(未 `--update`)**。抽檔當下以
  `git show HEAD:src/sim/floorChain.ts` 為第二份實作跑 200 遊戲日對照 ⇒ **逐位元相同**,
  基準凍結成 `scripts/floor-chain-baseline.json` 供回歸持續比對。
- **已知限制**:`SAVE_VERSION` 維持 10(沿用 `state.floorChain` 形狀、零新欄位);
  `data/events.json` 未動 ⇒ 規則事件版 `adopt_cat` 仍是 0,已更新為待決策條目。

### 2026-08-08 — 咖啡廳:缺貨歸因到原料 + 常備量上限修正(Claude Code)

- **目的/需求**:使用者實玩回報「原料和商品對不起來,不知道那個商品缺貨要多進什麼原料」「賺不太到錢」。
- **變更**:🔴 移除 `content/cafeIngredients.ts` 過期的 `usedIn`(P1 之前的字串,與菜單對不上),
  商品 ↔ 原料一律由 `recipe` 推導;`sim/cafe.ts` 新增 `cafeRecipeLines()`/`cafeIngredientMenuUse()`/
  `cafeItemShortageCauses()`/`cafeIngredientShortageBlame()`(純函式);`types.ts` 新增選填
  `CafeSalesDay.missedBy`(品項→原料→份數)、`tick.ts` 逐位結帳時把實際 `till.missing` 記進去、
  `gameState.ts` 加 `nestedNumberRecord` 消毒。`CafePanel.vue` 排行列顯示「要補 X:每份 ×N、共差 M 單位」,
  常備量列顯示「用於…」與「害 N 單做不出來」,熱銷品改讀實際銷量。
  🔴 **順手修 bug**:常備量輸入夾值上限 99 < 內建建議值 130 ⇒ 一按套用咖啡豆就被砍到 99,改 999。
- **驗證**:`npm test` **98/98**(新增 `scripts/cafe-recipe-clarity-test.ts` 42 條)、typecheck、build 全綠、
  balance 快照**零漂移(未 `--update`)**;`cafe-opening-sim` / `cafe-growth-sim` 輸出與改動前逐字相同。
  `ui:shot -- rent` 18 張 0 error,另用容器內 Playwright 實拍缺貨面板(`artifacts/ui-lab/rent/cafe-recipe/`)。
- **已知限制**:`SAVE_VERSION` 維持 10(選填欄位);**未調任何平衡數值**——精準補貨 +$109/日
  已高於設計值 +$98,差距全在歸因(補錯料 +$8 ~ −$144/日),理由見 `docs/待辦.md`。

### 2026-08-08 — 樓層按鈕列優化:移除「1 天」快轉鍵(Claude Code)

- **目的**:使用者要求「切換咖啡廳和其他按鈕可以優化,把往後 24 小時的按鈕刪掉,後台留著就好」。
- **變更**:`App.vue` 移除「⏩ 1 天」按鈕(五顆擠一列,390px 下`家具商店`被擠成兩行)。
  **快轉 24 小時的能力沒有拿掉** —— `onMounted` 掛 `window.rentDebug.fastForward(hours)`,
  dev console 仍叫得到;刻意不留隱藏按鈕,那still會佔版面與 tab 焦點。
  版面重配:三顆功能鍵等寬 `flex:1`、快轉鍵 `flex:1.15`(它是主要動作),全部 `nowrap`。
- **驗證**:`npm test` 98/98、typecheck 通過、`ui:shot` 18 張 0 errors;
  實拍確認 390px 下四顆全部單行、`家具商店` 不再折行。本批純顯示層,未動任何模擬邏輯。

### 2026-08-08 — 文件對帳:刪重複條目、STATUS.md 從 254 行瘦回 122 行(Claude Code)

- **目的**:使用者要求更新工作進度。對帳後發現要修的不是「漏記」而是「記錯與過度膨脹」。
- **修正一**:P4a 有**兩條重複條目**,且第一條是子 Agent 在中控修正**之前**寫的 ——
  內容寫「產能 員工數×35」「名店期 101%」「備太多反而虧回到 +$74,留給使用者拍板」,
  但中控當場就把 `CAFE_STAFF_CUPS_PER_DAY` 改回 26、把那條回歸修掉了。過期條目已刪,
  保留正確的那條。
- **修正二**:`STATUS.md` 膨脹到 **254 行**(自訂上限 120),`現在狀態` 一節就佔 128 行的
  逐批細節,還寫著「六批未提交」(早已 push)。這正是該檔維護規則第 2 條要防的
  ——「本檔是導航,不是內容」。重寫成 122 行:現在狀態只留同步狀態/驗證/存檔版本,
  成果改成一張「玩家現在可以做什麼」的表,下一步只留真正還沒做的三項。
- **驗證**:純文件批,未動任何程式碼;`git diff --stat -- src/ scripts/` 為空。

### 2026-08-09 — 樓層切換鈕移到畫面下半部(Claude Code)

- **目的**:使用者要求「切換第一樓和第三樓的按鈕可以放到畫面下半部」。原本在地圖上方的
  「目前樓層」卡片右側,手機上要伸到畫面頂端才按得到。
- **走過的彎路**:先試「留在 `FloorMap` 但挪到 canvas 下方」——**整顆被裁掉**,
  因為 `.floor-main` 是 `overflow: hidden` 而 canvas 已經吃滿高度(與先前 `.pixel-room`
  被壓成 2px 同一類問題)。與其跟 overflow 纏鬥,改放進本來就在畫面底部、不受該裁切
  影響的動作區。
- **變更**:`FloorMap.vue` 移除該按鈕、`defineExpose({ switchFloorPage })`;
  `App.vue` 在 `.floor-actions` 上方新增獨立一列的 `.floor-switch`(撐滿寬度、44px 高),
  文案由 App 既有的 `floorView` 決定。頂部只留「目前樓層」標示——那是辨識所在位置的資訊。
  獨立一列而非擠進下面四顆:它是「換場景」不是「開面板」,且四顆已是 390px 的極限。
- **驗證**:`npm test` 98/98、typecheck、build、balance 零漂移;`ui:shot` 18 張 0 errors,
  實拍確認按鈕在四顆動作鍵正上方。`floor-view-test.ts` 那條掃原始碼的斷言跟著改成跨兩檔驗
  (不變式「每層只有一顆目的地按鈕」沒變,只是按鈕與觸發點分開了)。

### 2026-08-09 — 收支與咖啡廳營運併成一顆入口、樓層切換鈕併回動作列(Claude Code)

- **目的**:使用者反映上一批之後底部變成三排按鈕,並問「咖啡廳營運和收支是否可以同一顆」。
- **變更**:新增 `components/OpsTabs.vue`(分頁文案與樣式的唯一出處,`role="tablist"`),
  `FinancePanel.vue`／`CafePanel.vue` 的 header 換成它、改 emit `switchTab` 交給 App 決定,
  兩個面板的內容與既有測試一行未動。`App.vue` 把 `showFinance`／`showCafe` 收斂成
  `opsTab`(同一時間只掛一個面板),底部導覽「💰 收支」成為唯一入口,**在 1F 預選咖啡廳分頁**
  (沿用家具商店的場地分頁規則,一樓仍一鍵直達營運)。空出的位置給樓層切換鈕 ⇒ 併回四顆一列;
  短文案與完整文案一起放進 `FloorMap.vue` export 的 `FLOOR_PAGE_NAV`,App import 不再照抄三元式。
- **驗證**:新增 `scripts/ops-panel-test.ts` 16 條並登記進 `run-all.ts`;`npm test` **99/99**、
  typecheck、build 全綠、balance 零漂移(未 `--update`);`ui:shot -- rent` 18 張 0 error 0 warning,
  另以容器內 Playwright 量到動作列四顆 `top` 全等於 729(單行未折行),並實拍分頁互切與
  1F 預設分頁存 `artifacts/ui-lab/rent/ops-tabs/`。**已部署**:線上 `index-CACXEe-j.js` 驗到
  `營運分頁`／`ops-tab`／`1F 咖啡廳`,舊的 `cafe-btn`／`floor-switch-arrow`／`1F PET CAFÉ` 均已消失。
- **已知限制**:咖啡廳面板原本的 `1F PET CAFÉ` 眉標與 `<h2>` 被分頁列取代;導覽文案仍固定為「收支」。

### 2026-08-09 — 咖啡廳分頁改可收合、常備量加 ±1／±5(Claude Code)

- **目的/需求**:使用者反映咖啡廳分頁太長(九個區塊要一路捲才找得到常備量),並要求常備量加 ± 按鈕。
- **變更**:`CafePanel.vue` 九個區塊的 `.section-head` 從 `div` 改成整列可點的 `button`
  (`aria-expanded` + ▾、收起時 `margin-bottom: 0`),內容包進 `v-if="openSections.X"`;
  **預設只展開「營運觀察」與「常備量」**,收起時標題列右邊仍留摘要(產能／研發進度／該補幾項…),
  不展開也知道要不要點。兩張詢問卡例外:有顧客在等就展開,面板開著時進來的由 `watch` 補開。
  常備量列改 `label` → `div`(button 包在 label 裡會連帶 focus 輸入框)並加 `−5/−1/+1/+5`,
  共用既有 `safeUnits()` 夾在 0～`MAX_STANDING_ORDER`、到邊界 disable;**動的是草稿**,
  寫進 `state.cafe.standingOrders` 的路徑仍只有「套用常備量」。
- **驗證**:新增 `scripts/cafe-panel-collapse-test.ts` 15 條並登記進 `run-all.ts`;`npm test` **100/100**、
  typecheck、build 全綠、balance 零漂移(未 `--update`);`ui:shot -- rent` 18 張 0 error 0 warning。
  容器內 Playwright 實測:面板 body `scrollHeight` **4319 → 1800px**、預設開合狀態正確、
  `130 →+5+1→ 136 →−5→ 131`、從 2 減 5 夾成 0 且兩顆減號 disabled、快捷鍵 46×34px。
  **已部署**:線上 `index-CfaYsZir.js` 驗到 `aria-expanded`／`chev`／收起摘要文案／± 的 aria-label。
- **已知限制**:收合狀態是元件區域狀態,關掉面板再開會回到預設(不進存檔)。

### 2026-08-09 — 職業 15→24、職業目標 8→14、劇情弧本地種子目錄(Claude Code)

- **目的/需求**:使用者要求「多加一些職業、職業目標、劇情目標」;規模與做法經使用者拍板。
- **變更**:①`recruit.ts` 的 `ARCHETYPES` **append** 9 種職業(只用既有 7 把作息 key、核心標籤只用
  既有 id 才吃得到噪音/口角/偷吃等規則);`後端工程師`／`ASMR 實況主` 早就寫在 `wishes.ts` 卻沒有
  原型(死引用),一併補上。②`wishes.ts` 加 6 條心願,每條掛**不同的玩家槓桿**(收納+品味家具/
  隔音家具/精力壓力/現金與欠租/整潔/鄰居關係),ASMR 移到專屬的 `grow_channel`、退休教師移到
  `teach_someone`;新增 `roomAttributesOf()` 讓「房東買家具」變成推進條件(不成環)。
  ③新增純資料 `content/storyArcs.ts`(10 條種子)與引擎 `sim/localArc.ts`(`tick` 換日呼叫,零 RNG,
  `rt.arc` 非空就不動手 ⇒ AI 有額度仍由 AI 主導);`applyArcTone()` 從 `narration.ts` 搬進 `arcs.ts`
  給兩條路徑共用;`StoryArc` 加選填 `seedId`/`localDay`,`SAVE_VERSION` 不動。
- **驗證**:新增 `scripts/local-arc-test.ts` 28 條並登記進 `run-all.ts`;`npm test` **101/101**、
  typecheck、build 全綠。**balance 已 `--update`**:先把 `localArcPass()` 隔離關掉重跑,快照
  **逐欄一致** ⇒ 證明新增職業與新增心願本身零漂移,唯一漂移來自本地弧的 mood/stress 脈衝
  (money 59921→59919、陳的錢包 +$2、兩人 satisfaction ±0.1),與 AI 弧本來就會做的事同一類。
  **已部署**:線上 `index-C53JdTrW.js` 逐項驗到 9 個新職業、6 條新心願、10 條弧種子的文案。
- **已知限制**:舊存檔保留既有 `wish.id`(`ensureWishes()` 只重指派非法 id)⇒ ASMR 林在舊檔仍是 `finish_masterwork`。無 `.vue` 變更,未跑 UI Lab。

### 2026-08-10 — 送養紀錄的寵物與新飼主像素合照(Claude Code)

- **目的/需求**:使用者要求「寵物送養成功後,紀錄裡出現一張寵物和新飼主的照片,像素風格即可」。
- **變更**:新增 `floor/petPhoto.ts`(72×56 邏輯像素的純函式:背景三種變體 + 相框 + 新飼主用既有
  `CHAR_STAND`／`drawAppearanceOverlay`、貓狗是本檔專屬的**正面坐姿**,樓層上那組是走路側面)、
  `components/PetPhoto.vue`(canvas 維持邏輯解析度,放大交給 CSS `image-rendering: pixelated`),
  掛進 `PetPanel.vue` 的「幸福新家」每張卡。🔴 **照片不進存檔**——`PetHomeEntry` 只加
  `adopterName?`／`adopterAppearance?` 兩個選填欄位,缺的時候依 `id` 決定性推導一位飼主
  ⇒ 舊紀錄照樣有照片、`SAVE_VERSION` 不動;`acceptCafeGuestAdoption()` 會留下顧客的真實外觀。
  花色從 `floorScene.ts` 原封搬到新的 `floor/petPalettes.ts`(色碼與順序未動),兩邊共用同一份。
- **驗證**:新增 `scripts/pet-photo-test.ts` 20 條並登記進 `run-all.ts`(決定性、零 RNG、
  每個貓狗花色都畫得出來且互異、既有色碼未動、紀錄裡沒有任何圖片資料);`npm test` **102/102**、
  typecheck、build 全綠、balance 零漂移(未 `--update`);`ui:shot -- rent` 18 張 0 error 0 warning,
  另以容器內 Playwright 塞四筆紀錄實拍(`artifacts/ui-lab/rent/pet-photo/`),四張 canvas 皆
  72×56 attr／144×112 CSS、100% 有內容、37～40 種顏色、0 console error。
  **已部署**:線上 `index-CKamBfKk.js` 驗到合照 aria-label、`pet-photo` 樣式、`pixelated` 與各花色色碼。
- **已知限制**:「通過審核的新家庭」沒有具體人物,合照裡的人是由紀錄 id 推導的路人。
